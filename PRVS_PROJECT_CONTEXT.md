# PRVS RO Dashboard — Claude Project Context
> **This file lives in the Claude Project knowledge base.**
> It is the mobile-accessible bridge between the Claude Project (iPhone) and the Cowork session (laptop).
> Updated at the end of every Cowork session as part of the STOP checklist.
> The full technical memory lives in CLAUDE_CONTEXT.md inside the rv-dashboard repo.

---

## 🗂 Project Identity

| Field | Value |
|---|---|
| **Project** | Patriots RV Services (PRVS) Dashboard |
| **Owner** | Roland Shepard — roland@patriotsrvservices.com |
| **Live URL** | https://patriotsrv.github.io/rv-dashboard/ |
| **GitHub Repo** | https://github.com/PatriotsRV/rv-dashboard |
| **Current Version** | **v1.417 (index.html)** · v1.14 (worklist-report.html) · v1.33 (checkin.html) · **v1.1 (closed-ros.html — Session 72 B-1 reactivation fix, tag `closed-ros-v1.1`)** · v1.8 (customer-checkin.html) · v1.3 (time-off.html) · send-parts-report v1.8 — Session 72 |
| **Supabase Project** | axfejhudchdejoiwaetq |
| **Cowork Workspace** | rv-dashboard folder on Roland's laptop |

---

## 📐 Implementation Specs (2026-04-10 — Perplexity)

> **Five implementation specs in `docs/specs/`.** Written by Perplexity Computer, reviewed by Roland, merged via PR #15. Claude Cowork executes one session/phase at a time.

| Spec | Sessions | Priority | Status |
|---|---|---|---|
| `SECURITY_REMEDIATION.md` — 10 security issues (XSS, RBAC, auth gaps, API key exposure, etc.) | S1–S7 | 🔴 **ASAP** | ✅ **ALL COMPLETE** — 2026-04-11 |
| `TWILIO_SMS_SPEC.md` — Full SMS integration replacing Kenect | Phase 1–3 | 🔴 After port | ⏳ Not started |
| `TOAST_SYSTEM_SPEC.md` — Replace alert() with toast notifications | 1 session | 🟠 High | ✅ **COMPLETE** — 2026-04-11 |
| `UNIFIED_SEARCH_SPEC.md` — Global search bar | 1 session | 🟠 High | ✅ **COMPLETE** — 2026-04-12 |
| `MODULARIZATION_ROADMAP.md` — Split index.html into 18 ES modules | Phase 0–19 | 🟡 Long-term | ⏳ Not started |

**Next up:** Twilio SMS after number port, then Modularization long-term

**Workflow:** Perplexity researches/writes specs → pushes to `docs/specs/` → Claude Cowork reads and executes

---

## 📋 ACTIVE TODO LIST
> Priorities: 🔴 Blocking · 🟠 High · 🟡 Medium
> Use this list from your iPhone to log updates between laptop sessions.

### 🔴 Blocking / Top Priority
- ~~**Security Remediation**~~ ✅ **ALL 10 ISSUES COMPLETE** — 2026-04-11.
- ~~**Status casing bug class (GH#29c/d ghost-write + capital-P)**~~ ✅ **PERMANENTLY CLOSED AT DB LAYER — Session 72 (2026-05-23).** Three-layer defense: Layer 1 application (checkin.html v1.33 + closed-ros.html v1.1) + Layer 2 database (CHECK constraint on `repair_orders` + `cashiered` enforcing 11-status canonical set, + `repair_orders.status` default 'Not On Lot') + smoke-tested with deliberate capital-P insert → Postgres correctly threw `23514 check_violation`. Bug now mechanically impossible regardless of which code path writes the status. Full 7-scan report at `docs/qa/SESSION_72_STATUS_CASING_SCAN.md`. Migration: `supabase/migrations/status_casing_check_constraint.sql` applied + verified. Patch: closed-ros.html v1.1 commit `b006577`, tag `closed-ros-v1.1`.
- ~~**GH#29c — Ghost writes to `repair_orders.status`**~~ ✅ **RESOLVED Session 71 (2026-05-22, checkin.html v1.33).** 11 of 16 ghost-write rows traced to a single line in `checkin.html` clockIn that has, since the very first upload of the file on 2026-03-08 (commit `b5ed919`), silently written `repair_orders.status='In Progress'` with ZERO audit_log entry. ALSO had a wrong-casing bug ('In Progress' capital P, but index.html canonical is 'In progress' lowercase). Three managers (Brandon, Lynn, Bobby) had been manually re-typing status via the dropdown for 6 weeks to fix the casing. v1.33 fix: `const IN_PROGRESS='In progress'`, snapshot priorStatus, insert audit_log row attributed to `<tech> (auto via clock-in)`, insert notes row with `⏱ AUTO-STATUS:` prefix. Both wrapped in try/catch (non-fatal). Commit `f168970`. **git blame proves NOT a Claude regression** — the bug is original to b5ed919. Historical backfill ran 2026-05-22 — 21 capital-P rows normalized; `capital_p_remaining=0`. **Pending Roland action:** publish GitHub Release v1.33.
- ~~**GH#29d — Investigate ghost-write path**~~ ✅ **DONE Session 71.** Investigation executed: grepped every `.update('repair_orders')` site across all 7 HTML files + audited all 10 Supabase edge functions (zero writes to repair_orders from any) + checked all SQL migrations for triggers (zero on repair_orders). `checkin.html` line 1227 was the only unaudited path.
- **GH#39 — Sendblue Integration Phase 1 sandbox signup** (Session 66) — Sign up for Sendblue free sandbox, capture API credentials, run test sends to 2 iPhone techs + 2 Android techs + 1 Android customer. Document results in `/research/sendblue_test.md`. **Decision gate — Phase 2 only proceeds if Phase 1 passes.** Supersedes Twilio as the immediate SMS path (GH#1/GH#4 v1/v2/v3 deferred pending Phase 5 decision). ⏳ Roland action — Phase 1 signup
- **GH#1 — Twilio number port** — DEFERRED Session 66 pending GH#39 Phase 5 decision. Full guide in `docs/specs/TWILIO_SMS_SPEC.md`. ⏳ Deferred
- **GH#4 v1 — Twilio Stage 1 tech SMS reminders** — DEFERRED Session 66 pending GH#39 Phase 5 decision. Session 55 setup work preserved (number purchased, A2P Brand approved, Campaign 2x rejected on opt-in evidence). Sendblue handles SMS A2P registration on their infrastructure — no Twilio A2P needed for immediate path. ⏳ Deferred

### 🟠 High Priority
- **GH#29f — Fourth ghost-write path — Jim Last `On Lot → Ready for pickup` silent flip (Session 72)** — PRVS-78B1-2F50 Jim Last had `status='Ready for pickup'` written 2026-05-22 16:36 CDT with NO audit_log entry AND NO notes entry. Last audit was Roland's `→ On Lot` on 2026-05-20 21:46:58. Roland confirmed NOT a manual Supabase Studio edit — implies a fourth ghost-write code path. **Note:** DB CHECK constraint now blocks non-canonical values, but this path can still write lowercase canonical values without audit. Candidates: checkin.html clockOut, parts-arrived auto-status, QA/QC sign-off, anything from the work-order page, any code that writes 'Ready for pickup'. Next: grep all .html for `'Ready for pickup'` writes + status updates that don't call `writeAuditLog`. ~45 min. ⏳ Open — backlog
- **`writeAuditLog()` may use stale `old_value` from local cache (Session 72)** — latent bug surfaced during Jim Last investigation. Audit rows on 2026-05-20 all show `old_value='In Progress'` even though prior writes between audits set Jim Last to other statuses. Suggests `writeAuditLog()` reads `old_value` from `currentData` (local in-memory snapshot from page render) instead of re-fetching from DB. Defensive fix: SELECT current status before the UPDATE in `updateROStatus()` + `confirmSchedule()`. Low priority — doesn't corrupt data, just makes `old_value` less useful for forensics. ⏳ Open — backlog
- **GH#29e — Third ghost-write path investigation (Session 71)** — 3 'Not On Lot' April reversions surfaced by the Session 71 ghost-write enumerator with no audit trail: PRVS-440C-B7BE (Machen), PRVS-789B-23A7 (Bryan Shoe), PRVS-5226-074D (Stony Rutledge). Code grep finds zero paths in current repo that write `'Not On Lot'` to existing row. All 3 are April; zero May — possibly self-resolved. Next: `git log -p --all -S "'Not On Lot'" -- '*.html'` for historical paths; if nothing, monitor for new occurrences. ~30 min. ⏳ Open — backlog
- **Cashiered archiver silently failing on 2 rows (Session 71)** — pg_cron `archive-cashiered-ros` (jobid=10) IS scheduled and active, but 2 rows marked Delivered/Cashed Out are still in `repair_orders`: PRVS-5B71-9775 Mikle Hartz (should have archived 5/16), PRVS-MA04-4CBF Mark Condry (5/23 is next run). Likely FK or RLS blocking parent DELETE. Next: `SELECT * FROM cron.job_run_details WHERE jobid=10 ORDER BY start_time DESC LIMIT 10;` for errors; then add explicit child cleanup or ON DELETE CASCADE. ~30 min. ⏳ Open — backlog
- **GH#36 — Realtime sync + auto-refresh** (Session 56) — **Phase 1 (~30 min):** add `setInterval(loadDataFromSupabase, 90_000)` to dashboard pages (index.html, worklist-report, closed-ros, analytics). **Phase 2 (~1 session):** Supabase Realtime channel subscription on `repair_orders` for instant cross-user sync. Phase 2 supersedes Phase 1. ⏳ Open — Phase 1 first
- ~~**GH#37 — Modal readability**~~ ✅ **DONE v1.413 (Session 60)** — opaque `.modal-content` CSS + darker overlay. All 5 affected modals fixed.
- **GH#31 — Finish Work List Reporting (MAJOR umbrella)** — v1.3–v1.13 shipped (Session 52 + Session 65). Remaining sub-items: aggregate dollar_value rollups, per-RO enriched chips, velocity/throughput analytics, alt chart sort orders, inline time-edit UI, time-window filter, manager scoring KPI, AI manager-of-managers groundwork, export/print, comparison/trending, NEW: tech-deduped Total Logged stat-strip chip. ✅ Sub-item #8 (per-silo labor attribution) DONE Session 65 v1.10. 🔄 Initiative — many sessions
- ~~**GH#33 Phase 4 — Cross-RO clock-in guardrail**~~ ✅ **DONE Session 65 (2026-05-10)** — Server BEFORE INSERT trigger + per-tech UNIQUE index installed. Historical backfill ran (11 Ignacio rows, 81.3h phantom labor stripped). checkin.html v1.32 + worklist-report.html v1.13 surface trigger fires as informational "↻ switched RO" badge. Closes the gap that allowed Ignacio's May 5 triple clock-in.
- **GH#33 Phase 2 — Systemic historical dupe scan across all time_logs** — After Phase 1 baseline (1 week), scan full table for (tech, ro) pairs with 2+ clock-ins within 30s. Preview → Roland reviews → batch delete. ⏳ Open — after 1 week of Phase 1 baseline
- **GH#26 — New RO Statuses: "Not on Lot" + "On Lot"** (Session 51) — Verify CHECK constraint, filter buttons + badge styles, wire customer-checkin modes. Pairs with GH#27. ⏳ Open
- **GH#27 — Drop-Off form RO lookup + pre-populate** (Session 51) — Look up existing `status='Not on Lot'` RO by phone/email/name. Pre-populate fields. Multi-match → picker. On submit: transition RO + audit + fire GH#28 email. ⏳ Open
- **GH#28 — Customer Arrived / Dropped Off staff email** (Session 51) — New `customer_arrived` email type. Action banner. ⏳ Open
- **GH#23 — Morning Manager Report (data quality banner fix)** — $0.00 warning not firing for sr_manager reports. 🔄 In Progress
- **GH#23 — Map service_silo values to each manager in staff table** — Needed for per-silo filtering. ⏳ Open
- **GH#20 — Slack Integration (remaining triggers)** — Test `ro_urgency_critical`, `part_received`, `warranty_ro_opened`. 🔄 In Progress
- **GH#5 — Work Order Redesign** (Session 64 umbrella, folded GH#5c) — 13-phase plan, ~38 hr / ~11 sessions. Phase A1+A2 ✅ shipped v1.414 via PR #22 (Session 67 merge). Remaining phases C–M gated on Roland running Phase B SQL migration (`supabase/migrations/wo_redesign_phase_b.sql`) — additive-only, relaxes silo CHECK 5→8, adds line-item columns. Phases C (manager check-off), D (parts on line items), E (two labor fields), F (tech check-in to line items), G (sub-items), H (templates with parts), I (Lightspeed copy/paste), J (EOD labor backfill), K (variance analytics), L (Manager Work List tier upgrade), M (remove flag). 🔄 Active
- **GH#17 — Customer Check-In Page** — Front desk RO intake + RAF e-signature. Session 50: Lead Staff Notification built; mode rename; shared-secret auth. Living form — Roland will add fields pre-go-live. 🔄 In Progress
- **GH#6 — QuickBooks Payroll Integration** (Session 57, IN PROGRESS) — Push `time_logs` + `time_off_requests` weekly to QB as TimeActivity entries. Intuit Developer app created + App Details 100% complete. **Blocked on: Compliance section (0% / ~40 min).** Roland actions to complete: (A) Complete Compliance at developer.intuit.com → PRVS Dashboard → Keys and credentials → Production → Compliance. (B) Copy production Client ID + Secret to password manager. (C) Run OAuth Playground → authorize Patriots RV QB company → capture Realm ID + Refresh Token. (D) Store 4 Supabase secrets: `QB_CLIENT_ID`, `QB_CLIENT_SECRET`, `QB_REALM_ID`, `QB_REFRESH_TOKEN`. (E) Look up QB Employee IDs (QB → Payroll → Employees → URL has ID). (F) Look up QB Pay Item IDs (Regular, Sick, Vacation, Personal, General). Then Claude builds: `qb_employee_id` on staff, `qb_pay_items` config table, `qb_sync_log`, `quickbooks-sync` Edge Function, weekly pg_cron. 🔄 Blocked on Roland Compliance + credentials

### 🟡 Medium Priority / Open
- **GH#4 v2 — Twilio Stage 2** — wire 4:45 PM reminder pg_cron + smoke-test. ⏳ Blocked on GH#4 v1
- **GH#4 v3 — Twilio Stage 3 customer SMS** — after GH#1 number port. ⏳ Blocked on GH#1
- **GH#34 — Offline-queue clock_out race with auto-logout** — edge case, low priority until observed in production. ⏳ Open
- **Migrate lead_staff_notify recipient to `app_config`** — Currently hardcoded to repair@patriotsrvservices.com. ⏳ Open
- **Test remaining lead_staff_notify variants** — Warranty-only, hybrid drop off, returning customer variants. ⏳ Open
- **GH#11 — Solar Battery Bank Wh** — Show Wh alongside Ah. ⏳ Open
- **GH#9 — Parts form autocomplete** — Suggest from history. ⏳ Open
- **GH#21 — checkin.html Auth Persistence Fix** — Add persistSession + storageKey + getSession() restore. ⏳ Open
- **GH#20b — QR print layout update** — RV Owner Name + RV Info alongside key-tag QR for 4.3"×6.3" laminating pouch. ⏳ Open
- **GH#24 Phase 2 — Shop Operations: Parts Returns** — Reverse-direction parts workflow. ⏳ Open
- ~~**GH#24 Phase 3 — Shop Operations: Shop Tasks + Time Logging**~~ ✅ **DONE v1.31 (Session 61)** — Shop activity picker on checkin.html, `shop_activity` column on `time_logs`. **⚠️ Roland must run 2 SQL migrations in Supabase SQL Editor:** `shop_activity_time_logs.sql` + `cron_archive_cashiered_ros.sql`
- **GH#8 — Switchblade tile view** — Compact tile layout mode. ⏳ Open
- **Update solar parts pricing** — Current Epoch + Victron catalog pricing. ⏳ Open
- **`isManagerOrAbove()` helper refactor** — Consolidate 5+ scattered role checks. ⏳ Open

### 🟡 Medium Priority / Roland Actions
- **GitHub Release closed-ros.html v1.1 — Session 72 B-1 reactivation fix** — Go to https://github.com/PatriotsRV/rv-dashboard/releases/new — tag `closed-ros-v1.1` already pushed, target `main`, commit `b006577`. Title "closed-ros.html v1.1 — Fix reactivation ghost-write (Session 72 B-1)". Notes from v1.1 header in closed-ros.html. 5 min. ⏳ Roland action
- **GitHub Release checkin.html v1.33 — Ghost-write + casing fix (Session 71)** — Go to https://github.com/PatriotsRV/rv-dashboard/releases/new — tag `v1.33-checkin` (or include in next index.html major release), target `main`, commit `f168970`. Title "checkin.html v1.33 — Fix ghost-write to repair_orders.status (GH#29c/GH#29d)". Notes from v1.33 header in checkin.html. 5 min. ⏳ Roland action
- **Run weekend regression QA matrix (Session 71)** — `docs/qa/SESSION_71_WEEKEND_QA.md`. 10 sections, 50+ test rows. Section 1 = v1.33 verification (MUST PASS). Log any failures in the Issues table at the bottom of the doc. ⏳ Roland action — this weekend
- **Complete QB Compliance section** — developer.intuit.com → PRVS Dashboard → Keys and credentials → Production → Compliance (~40 min). 🔴 BLOCKS GH#6
- **Supabase: Maximize log retention** — Settings → Logs. ⏳ Roland action
- **Create parts@patriotsrvservices.com** — Email group for parts notifications. ⏳ Roland action
- **GitHub Release v1.414 — WO Redesign Phase A1+A2** (Session 67) — Go to https://github.com/PatriotsRV/rv-dashboard/releases/new — tag `v1.414`, target `main`, title "v1.414 — WO Redesign Phase A1+A2". Notes can copy from PR #22 description + squash-merge commit `64a3de2` message. 5 min Roland task. ⏳ Roland action
- **GitHub Release v1.415 — Insurance WO Writer role** (Session 69) — Same process: tag `v1.415`, target `main`, title "v1.415 — Insurance WO Writer role (Brandon Dillon)". Notes can copy from the v1.415 header comment in index.html + commit `ce2d3d6` message. 5 min Roland task. ⏳ Roland action
- **Verify Brandon Insurance WO Writer end-to-end** (Session 69) — Walk Brandon through the 7-test plan delivered at end of session: (1) Cross-silo visibility — + Add Service for every silo. (2) Restricted UI on fresh WO — banner + filtered statuses + editable $. (3) Pricing lock after save + reopen. (4) DB-level guardrails via DevTools console direct API calls. (5) Templates save + apply both work. (6) Silo manager (Riley) unrestricted regression. (7) Bobby Thatcher (other parts_manager, no role) still blocked. ⏳ Roland action
- **GitHub Release v1.410/v1.411/v1.411-hotfix/v1.412/v1.413** — Optional: tag/release individually or wait for next consolidated release. ⏳ Roland action — optional
- **Modularization** — Long-term. Spec ready in `docs/specs/MODULARIZATION_ROADMAP.md`. ⏳ Not started

---

## ✅ Recently Completed
- ✅ **Session 72 — Status casing bug class permanently closed at the DB layer (2026-05-23, commit `b006577`, tag `closed-ros-v1.1`)** — Roland ran a 7-scan static-analysis pass on the entire status-casing surface (literal census, write-audit gap, backfill blind-spot, constants inventory, CHECK constraint design, related enum casing, i18n fallback) producing `docs/qa/SESSION_72_STATUS_CASING_SCAN.md` (~600 lines). Key blocking finding: closed-ros.html line 816 was the THIRD ghost-write path — `status: 'In Progress'` (capital P) + no audit on reactivation INSERT. Patched closed-ros.html v1.0 → v1.1: `const IN_PROGRESS = 'In progress'` + audit_log + notes inserts on reactivation, all wrapped in try/catch. Then applied `supabase/migrations/status_casing_check_constraint.sql` — CHECK constraints on `repair_orders` AND `cashiered` enforcing the 11-status canonical set + `repair_orders.status` column default 'Not On Lot'. Smoke test with deliberate capital-P insert correctly threw Postgres `23514 check_violation`. Bug class now MECHANICALLY IMPOSSIBLE at the DB layer. Final ghost-write enumerator surfaced 5 surviving rows (none capital-P): Hartz + Condry cashiered archiver miss (S71 carryover), Machen + Stony GH#29e Not-On-Lot reversions (S71 carryover), and NEW PRVS-78B1-2F50 Jim Last — `Ready for pickup` written 2026-05-22 with zero audit and zero note; Roland confirmed NOT a manual edit, so fourth ghost-write code path exists (GH#29f). Latent bug also found: `writeAuditLog()` may read `old_value` from stale local cache. **Pending Roland:** publish GitHub Release `closed-ros-v1.1`.
- ✅ **checkin.html v1.33 — Ghost-write to repair_orders.status FIXED + casing bug FIXED (2026-05-22, Session 71, commit `f168970`)** — Resolves both GH#29c (reopened Session 67) and GH#29d (Session 67 investigation TODO) in a single surgical commit. Two independent bugs in tech clockIn, both originating in commit `b5ed919` (2026-03-08 'Add files via upload') and confirmed via `git blame` to NOT be Claude regressions — important for restoring Roland's confidence after the "code corruption" worry. (1) **Ghost write**: status update silently rewrote `repair_orders.status='In Progress'` with ZERO audit_log entry whenever prior status was anything else. Production audit found 11 of 16 cross-RO ghost-write rows traced to this single line — Ed Gordon, Jeannie Hickman, David Hensley, Rick Hinton, Neal Silvers, Mark Saint Pe, Steve & Alana Smith, Dez Rock, Don Mullins, Roland's training RO, even PRVS - SHOP OPERATIONS. (2) **Wrong casing**: wrote `'In Progress'` (capital P) but index.html's canonical value used by every dropdown/filter/color map/Spanish key is `'In progress'` (lowercase p) — so every tech clock-in left a row with default-gray badge, missed In-Progress filter, undercounted stats, fell back to literal English for Spanish. Audit-log evidence on Jeannie Hickman: Brandon (2026-04-06), Lynn (2026-04-14), Bobby (2026-05-18 + 2026-05-22) had been manually re-typing status via the dropdown for 6 weeks to fix the casing each time — audit_log captured all four fixes; tech overwrites between were silent. **v1.33 fix:** `const IN_PROGRESS = 'In progress'`, snapshot priorStatus BEFORE update, insert audit_log row attributed to `<tech> (auto via clock-in)`, insert notes row with `⏱ AUTO-STATUS:` prefix. Both wrapped in try/catch (non-fatal). No DB schema change. **Historical backfill ran 2026-05-22:** 21 capital-P rows normalized to lowercase; `capital_p_remaining=0` confirmed. **Weekend regression QA matrix delivered at `docs/qa/SESSION_71_WEEKEND_QA.md`** (10 sections, 50+ tests). **Two new open follow-ups:** GH#29e (3 'Not On Lot' April reversions with no audit trail — possibly self-resolved) + cashiered archiver investigation (cron scheduled but 2 rows didn't archive — likely FK blocking parent DELETE). **Pending Roland actions:** publish GitHub Release v1.33; run the weekend QA matrix.
- ✅ **v1.417 — Lynn admin-button flakiness ROOT-CAUSE FIX (2026-05-21, Session 70, commit `c9b21a8`)** — Lynn (Admin in user_roles + Sr Manager in staff, on v1.416 deployed code) still saw only 2 buttons (Closed ROs + Time Off) the morning after v1.416 deployed. DB check confirmed her `user_roles.Admin` row was perfect — bug was purely JS-side. v1.416 had hardened `loadUserRoles()` internals but a SECOND silent-demote bug remained: session-restore Step 2 ("Fall back to Google token if Supabase session gone") set `sessionRestoredFromCache=true` and the One Tap prompt skip-condition keyed on that flag, preventing the signInWithIdToken callback from ever firing — so `supabaseSession` stayed null, `userRoles` stayed `[]`, every `isAdmin()/hasRole()` gate failed silently. Pattern fit Lynn's "intermittent over multiple days" symptom: Supabase JWTs expire ~hourly; overnight tabs have dead Supabase + warm Google token (1+ hr), triggering Step 2 instead of Step 1. **Fix: 5 surgical edits to index.html** — (1) `getUserInfo()` awaits `loadUserRoles()` before paint, (2) Step 2 fallback awaits `loadUserRoles()` before `updateViewModeDropdown()`, (3) callback skip narrowed from `sessionRestoredFromCache \|\| supabaseSession` → just `supabaseSession`, (4) post-`signInWithIdToken` success re-runs `loadUserRoles` + `updateViewModeDropdown` + `updateAuthStatus(true)`, (5) One Tap prompt skip flipped from `!sessionRestoredFromCache` → `!supabaseSession`. `sessionRestoredFromCache` preserved for UI hydration but no longer used as proxy for "RBAC is active". worklist-report.html + customer-checkin.html unchanged (no Step 2 path). GH#41 verified DB perfect (no INSERT needed). GH#42 added + closed same session. **Lynn must hard-refresh (Ctrl+F5 on Windows) once GitHub Pages deploys** (~2 min after push). **Roland to publish GitHub Releases v1.416 + v1.417.**
- ✅ **v1.416 — Hardened loadUserRoles() against silent-demote (FIRST HALF of Lynn fix) (2026-05-19, Session 69 follow-up, commit `89059d9`)** — Roland shipped from Mac terminal between Sessions 69 and 70. Split `loadUserRoles()` into two INDEPENDENT role sources: user_roles via users.id (wrapped in own try/catch) + staff-table merge (runs unconditionally). `.single()` → `.maybeSingle()` on staff query. Mirrored pattern in worklist-report.html v1.14 + customer-checkin.html v1.7. Restored Lynn's Sr-Manager-tier buttons. Admin-tier required v1.417 follow-up + the GH#41 DB verification (passed).
- ✅ **customer-checkin.html v1.8 — Gilbert "not found" / cashiered search (2026-05-20, Session 70 WIP, commit `13c28d6`)** — Returning-customer search now queries `cashiered` in addition to `repair_orders` so customers whose last RO has been archived (Saturday 5 PM cron OR manual Delivered/Cashed Out) can still be found. Both queries run in parallel via Promise.all; active row preferred when customer is in both; "Past customer" pill on cashiered hits. `loadPreviousROs()` gets same dual-table treatment. Silent search-error `warn()` replaced with user-visible toast.
- ✅ **v1.415 — Insurance WO Writer role / Brandon Dillon cross-silo WO write (2026-05-18 PM, Session 69, commit `ce2d3d6`)** — New `Insurance WO Writer` role in `roles` + `user_roles` lets Brandon Dillon input WO data from insurance estimates and silo-manager templates into any RO across any of the 5 currently-allowed service silos (8 once Phase B SQL is exercised), without being able to approve, close, reassign, delete, or re-edit pricing. Migration `BRANDON_INSURANCE_WO_WRITER.sql` ran clean — verified 1,1,1,1. Two BEFORE triggers (`enforce_insurance_wo_writer_swo_limits`, `enforce_insurance_wo_writer_st_limits`) enforce column-level guardrails RLS can't natively. JS: `canManageSilo()` extended + new `isInsuranceWoWriterOnly(silo)` helper; restricted UI in WO form (banner, locked $, filtered statuses, "Silo manager assigns" placeholder, hidden ✕ on existing rows). Brandon can save + apply templates (RLS already permissive). **Roland to publish GitHub Release v1.415 + walk Brandon through the 7-test verification plan.**
- ✅ **time-off.html v1.3 — Admin-only employee picker restored (2026-05-18 AM, Session 68, commit `cfab831`)** — Admins can now submit time off records on behalf of any active employee (Sr Manager, Manager, Tech, Admin) directly from the Time Off page. New employee dropdown at the top of the request modal (visible to Admins only); defaults to the admin themselves on every open. `employee_email` = selected employee; `submitted_by_email` = logged-in admin (audit trail preserved on List view's *Submitted By* column). Non-admin roles unchanged — Sr Managers, Managers, and Techs continue to self-submit only. No DB migration required (schema already supported this from v1.0).
- ✅ **v1.414 — WO Redesign Phase A1 + A2 MERGED to main (2026-05-17, Session 67, PR #22 squash → commit `64a3de2`)** — Missing-WO 3-tier escalating badge (amber 0–2d → orange 3–5d → red+pulse 6+d) on standard RO cards + compact pills + Manager Work List rows. Per-silo WO summary chips with task count, completion %, est hours, and mini progress bar. Phase B SQL migration verified pre-applied (21/21 checks pass). Production verified at v1.414. **GH#29c REOPENED** — `repair_orders.status` ghost writes confirmed via audit_log evidence (Ed Gordon + Jeannie Hickman + others). Investigation TODO logged as GH#29d.
- ✅ **GH#33 Phase 4 — Cross-RO clock-in guardrail SHIPPED (2026-05-10, Session 65, commits `05d4a30` + `a53374f`)** — Closes the gap that allowed Ignacio Ochoa to be clocked into 3 ROs simultaneously on May 5. Three layers: (1) `supabase/migrations/cross_ro_clockin_guardrail.sql` — `auto_close_prior_open_session()` BEFORE INSERT trigger + `idx_time_logs_one_open_per_tech` UNIQUE partial index. Catches every code path (foreground, offline replay, cached page, manual SQL). (2) `cross_ro_clockin_historical_backfill.sql` — one-time backfill, ran 2026-05-10: **11 rows rewritten (all Ignacio's), 81.3 hours phantom labor stripped.** (3) `checkin.html v1.32` — observability toast + drainQueue auto-close accumulator + new 23505 handler. **Both SQL migrations ran by Roland 2026-05-10.** Trigger=1, index=1 verified. Per Roland's "just clock into the new RO" policy, `worklist-report.html v1.13` reframes trigger fires as INFORMATIONAL purple "↻ switched RO" badge (not a discipline event).
- ✅ **worklist-report.html v1.10–v1.13 — GH#31 sub-item #8 + Tech tiles to top + per-tech daily drill-down + wall-clock overlap math (2026-05-10, Session 65, commits `2ab9027` + `5ebdd78` + `05d4a30` + `67137ae`)** — Per-silo labor split (Roof bar shows only Roof labor, not full RO total). Tech Check-In tiles moved from end-of-report to TOP. Per-tech daily drill-down modal with 7-day mini-bars (color-banded by daily total) + click-through modal showing day's clock-ins + cumulative total + RO labels + service types + close-reason badges. Coaching banner threshold uses wall-clock. Wall-clock overlap math fixes Ignacio's 15.5h-from-3-overlapping-sessions bug → now shows 9.4h wall-clock with red overlap banner. Per-row OVERLAP badges. New `auto_replaced_by_new_session` close_reason rendered as informational purple "↻ switched RO" badge.
- ✅ **time-off.html v1.2 — GH#38 partial day + employee dropdown removal (2026-04-30, Session 62, commits `8ec431d` + `5bf2552`)** — v1.1: All users now submit time off for themselves only. v1.2: Full Day / Partial Day toggle; `partial_hours NUMERIC(4,1)` column on DB (NULL = full day, 0.5–8.0 = partial hours); fractional day stats; calendar/list shows `⏱ Xh` chips for partials; reason required for all requests. Migration `add_partial_hours_to_time_off.sql` run by Roland.
- ✅ **checkin.html v1.31 — GH#24 Phase 3 Shop Activity Picker (2026-04-29, Session 61, commit `1b54a319`)** — Techs clocking into Shop ROs see a purple activity-chip grid (5 activities). Activity stored in new `time_logs.shop_activity` column. **Requires: `shop_activity_time_logs.sql` migration run.**
- ✅ **Saturday Cashiered RO Archiver — pg_cron (2026-04-29, Session 61, commit `1b54a319`)** — Replaces the old Google Sheets Saturday 5 PM archiver. `archive_cashiered_ros()` SECURITY DEFINER function + pg_cron job every Saturday 22:00 UTC = 5 PM CDT. **Requires: `cron_archive_cashiered_ros.sql` migration run.**
- ✅ **v1.413 — GH#37 Modal readability fix (2026-04-28, Session 60, commits `263d528` + `6ee6da6`)** — 5 modals were transparent because `.modal-content` had zero CSS. Fixed with opaque background + darker overlay + blur backdrop.
- ✅ **send-parts-report v1.8 — Parts Report estimate bug fixed (2026-04-28, Session 59)** — Bobby was seeing 5 estimate-only ROs in Section 1. Fix: added `.not("parts_status","eq","estimate")` + `.is("deleted_at",null)`. Commit `521aeb9`.

---

## 📱 How to Use This Project from Your iPhone

**To add a TODO item:**
> "Add to TODO (high priority): [describe the task]"

**To mark something done:**
> "Mark as done: [task name or description]"

**To log an idea for later:**
> "New idea: [describe it] — add as low priority TODO"

**To check what's next:**
> "What are my highest priority open items?"

**At your next laptop session**, tell Claude Cowork:
> "Here are my iPhone updates since last session: [paste or summarize the above]"
Claude will merge them into CLAUDE_CONTEXT.md automatically.

---

## 🏗 Tech Stack (quick reference)
- **Frontend:** Vanilla JS single-file `index.html` — deployed via GitHub Pages
- **Database:** Supabase (PostgreSQL) — project ref `axfejhudchdejoiwaetq`
- **Auth:** Google Identity Services → Supabase `signInWithIdToken`
- **Storage:** Supabase Storage (`rv-media` bucket)
- **Edge Functions:** `roof-lookup` (AI), `send-quote-email` (email — v1.9), `send-parts-report` (cron), `send-er-report` (cron), `claude-vision-proxy` (estimate scanner), `slack-notify` (Slack webhooks), `send-manager-report` (cron — GH#23), `process-scheduled-notifications` (cron — scheduled notifications)
- **SMS:** Twilio (planned — full spec in `docs/specs/TWILIO_SMS_SPEC.md`)
- **Roles:** Supabase RBAC via `user_roles` table — Admin, Sr Manager, Manager, Parts Manager, Tech

---

## 🔑 Key Rules (for Claude when working in this project)
- The authoritative technical memory is **CLAUDE_CONTEXT.md** in the GitHub repo — always read it at laptop session start
- Never use `accessToken` as an auth guard for Supabase — use `getSB() && supabaseSession`
- Always run `bash scripts/backup.sh` before every `git push`
- Always bump the version number in both the HTML comment block AND the visible `<span>` badge in the header
- Parts request notes use `type:'ro_status'` with body prefix `🔩 PARTS REQUESTED:` — never `type:'parts_request'`
- Commit and push after every meaningful change — never let work sit uncommitted

---

*Last updated: 2026-05-23 — Session 72 (END) — index v1.417 · worklist-report v1.14 · checkin.html v1.33 · **closed-ros.html v1.1** (was v1.0, tag `closed-ros-v1.1`) · customer-checkin.html v1.8 · time-off.html v1.3 · send-parts-report v1.8. Status-casing bug class PERMANENTLY CLOSED at the DB layer. Session 72 produced the 7-scan static-analysis report at `docs/qa/SESSION_72_STATUS_CASING_SCAN.md`. Patched closed-ros.html v1.1 (commit `b006577`) — `const IN_PROGRESS = 'In progress'` + audit_log + notes on reactivation, fixing the THIRD ghost-write path. Applied + verified `supabase/migrations/status_casing_check_constraint.sql` — CHECK constraints on `repair_orders` AND `cashiered` (11-status canonical set) + `repair_orders.status` default 'Not On Lot'. Capital-P smoke test threw `23514 check_violation` as expected. **New backlog spawned:** GH#29f (Jim Last `On Lot → Ready for pickup` silent flip on 2026-05-22 — Roland confirmed NOT a manual edit, so fourth ghost-write code path exists), writeAuditLog stale-cache latent bug, GitHub Release `closed-ros-v1.1` Roland action. **Carryover from S71:** GH#29e Not-On-Lot reversions (Machen + Stony), cashiered archiver miss (Hartz + Condry).*

*Older — Last updated: 2026-05-22 — Session 71 (END) — index v1.417 · worklist-report v1.14 · **checkin.html v1.33** (was v1.32) · customer-checkin.html v1.8 · time-off.html v1.3 · send-parts-report v1.8. Session 71 RESOLVED GH#29c + GH#29d: checkin.html v1.33 (commit `f168970`) fixes the ghost-write to `repair_orders.status` AND the 'In Progress'/'In progress' casing bug, both originating in commit `b5ed919` (2026-03-08 'Add files via upload') and confirmed via `git blame` + `git log --pickaxe-regex` to NOT be Claude regressions. Production audit found 11 of 16 ghost-write rows traced to checkin.html line 1227; three managers (Brandon, Lynn, Bobby) had been manually re-typing status to fix the casing for six weeks; tech overwrites between their fixes were silent until v1.33. v1.33 wraps the status flip with proper `audit_log` + `notes` inserts (attributed to `<tech> (auto via clock-in)`) and uses canonical lowercase 'In progress'. Historical backfill ran post-deploy: 21 capital-P rows normalized; `capital_p_remaining=0`. Weekend regression QA matrix delivered: `docs/qa/SESSION_71_WEEKEND_QA.md` (10 sections, 50+ tests). Two open follow-ups: GH#29e (3 'Not On Lot' April reversions with no audit trail — likely self-resolved) + cashiered archiver investigation (cron scheduled but 2 rows didn't archive — likely FK blocking parent DELETE). **Pending Roland:** publish v1.33 GitHub Release; run the weekend QA matrix this weekend.*

*Older — Last updated: 2026-05-21 — Session 70 (END) — index v1.417 · Lynn admin-button flakiness ROOT-CAUSE FIX (Step 2 fallback bypass of loadUserRoles). 5 surgical edits, linear history ce2d3d6 → 89059d9 → 13c28d6 → c9b21a8.*

*Older — Last updated: 2026-05-18 PM — Session 69 (END) — **index v1.415** · worklist-report v1.13 · checkin.html v1.32 · customer-checkin.html v1.6 · time-off.html v1.3 · send-parts-report v1.8. Session 69 delivered: Insurance WO Writer role (GH#40) — Brandon Dillon cross-silo WO write privilege without approve/close/reassign/delete/re-price. New `Insurance WO Writer` role in `roles` + `user_roles`. `BRANDON_INSURANCE_WO_WRITER.sql` migration ran clean (verified 1,1,1,1) — adds `is_insurance_wo_writer()` helper, extends 4 RLS policies, installs 2 BEFORE triggers (`enforce_insurance_wo_writer_swo_limits` for dollar_value lock + terminal WO statuses; `enforce_insurance_wo_writer_st_limits` for tech-assignment block + terminal task statuses). JS: `canManageSilo()` extended + new `isInsuranceWoWriterOnly(silo)` helper; restricted UI in `openBuildWOForm` (banner, locked $ when > 0, filtered WO statuses) and `buildWOTaskRowHtml` (tech `<select>` replaced with placeholder + hidden field, filtered task statuses, hidden ✕ on existing rows); `submitWOForm` forces unassigned tech + skips delete branch. Templates RLS already permissive. Single commit `ce2d3d6` on main (rebased onto squash-merge from PR #22). 7-test verification plan delivered to Roland. **Pending:** GitHub Release v1.414 + v1.415 (Roland action), Brandon Tests 1–7 (Roland action), GH#29d ghost-write investigation (open).*

*Older — Last updated: 2026-05-18 AM — Session 68 (END) — index v1.414 · worklist-report v1.13 · checkin.html v1.32 · customer-checkin.html v1.6 · **time-off.html v1.3** · send-parts-report v1.8. Session 68 delivered: time-off.html v1.2 → v1.3 (Admin-only employee picker restored on request modal — Admins can submit time off on behalf of any active employee; `employee_email` = selected, `submitted_by_email` = logged-in admin, audit trail preserved; non-admins unchanged). Single commit `cfab831` on main. No DB migration required.*

*Older — Last updated: 2026-05-17 — Session 67 (END) — index v1.413 → **v1.414** (WO Redesign Phase A1+A2 merged via PR #22 squash, commit `64a3de2`). GH#29c REOPENED (`repair_orders.status` ghost-write evidence on Ed Gordon + Jeannie Hickman); GH#29d investigation TODO added. Local OAuth `localhost:8765` permanently enabled in Google Cloud Console for future smoke-tests.*

*Older — Last updated: 2026-05-10 — Session 65 (END) — index v1.413 · worklist-report v1.13 · checkin.html v1.32 · customer-checkin.html v1.6 · time-off.html v1.2. Session delivered: GH#31 sub-item #8 (per-silo labor split) ✅ + Tech Check-In tiles to top + per-tech daily drill-down modal + wall-clock overlap math + GH#33 Phase 4 (cross-RO clock-in guardrail: server trigger + per-tech UNIQUE index + historical backfill + display reframe) ✅. 6 commits to main: `2ab9027` → `5ebdd78` → `05d4a30` → `a53374f` → `67137ae` (final). 2 SQL migrations ran by Roland: trigger + index installed clean, historical backfill rewrote 11 Ignacio rows (81.3h phantom labor stripped). PR #22 from Session 64 still pending Roland review (index.html v1.414 on wo-redesign branch).*

*Older — Last updated: 2026-04-30 — Session 62 (END) — index v1.413 · worklist-report v1.9 · checkin.html v1.31 · customer-checkin.html v1.6 · time-off.html v1.2. Session delivered: time-off.html v1.1 (employee dropdown removal) + v1.2 (partial day support + required reason field). Migration add_partial_hours_to_time_off.sql run by Roland. Commits: 8ec431d + 5bf2552.*