# Enhancement Request Triage -- 2026-06-21 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-21T10:33Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.459** (checkin.html, customer-checkin.html v1.13, time-off.html v1.4).
**Total ERs:** 58 (incl. 3 test entries + 5 exact duplicates -> **50 real requests**).
**Triaged this run:** 57 ids in `needs_triage_ids` (every row except the n15 test entry, carried over unchanged).

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `time-off.html`, `supabase/functions/*`, SQL migrations)
by grepping the source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

---

## Headline

**Six ERs moved to DONE since the 2026-06-20 snapshot.** The carried verdicts in
`er_input.json` were stamped 2026-06-20 10:24, *before* v1.458/v1.459 shipped; this
re-triage grepped the current tree and confirmed the work is now in code:

- **n18** (Lynn) "keep plugged in + keys/no-keys/keypad code" and **n34** (Brandon)
  "confirm keys left with unit" -> **DONE v1.458 (S118)**: new `key_status` /
  `keypad_code` / `keep_plugged_in` columns (migration `keys_power_fields.sql`),
  Keys select + Keypad input + "Keep plugged in" checkbox on New + Edit RO
  (`ro-crud.js` L341-343/L422-424, edit form `index.html` L5858, normalize
  `utils.js` L250), plus a keys/power card chip row. Markers tag both ERs.
- **n37** (Roland) "key dates as a colored RO button + calendar entries + advance
  manager/admin notifications" -> **DONE Key Dates P1-P3 (v1.457 + v1.459)**: P1
  colored Drop/Promised/Pickup chips (`render.js`); P2 promised/pickup now
  create/update/delete per-silo Google Calendar events (`scheduling.js`
  `syncKeyDateCalendars`, migration `key_dates_cal_event_ids.sql`); P3 day-before
  + morning-of email reminders to silo managers + admins (`ro-crud.js`
  `_syncOneKeyDateReminder`). All three components of the ask are shipped.
- **n43** (Roland) "auto-remove a cashed-out RO from manager work lists the day
  after Cashiered/Delivered" -> **DONE (S118)**: daily prune job
  (migration `prune_cashiered_worklists.sql`; marker tags the ER).
- **n54** (Sofia) "David Flanagan: scheduled returning customer still duplicated
  and sent no auth form" -> **DONE v1.13 (S118)**: customer-checkin DUP-RO
  HARDENING adds a submit-time auto-dedupe matching an existing scheduled RO by
  normalized phone OR lowercased email even when the search match was not tapped
  (L2038-2048), closing the residual v1.12 gap. **The bug bucket is now empty.**
- **n56** (Lynn) "cannot edit time-off entries" -> **DONE v1.4 (S118)**:
  `time-off.html` `editRequest` (L806) opens the modal pre-filled and UPDATEs the
  record (L885); Edit button per row (L748). Marker tags the ER.

No new ERs arrived since the last run. No still-open bugs. The standout open
enhancements remain the **Notifications & Task-Tracking** cluster (6 ERs), the
**Parts inventory** subsystem (n6, XL), the **auth-cost** redesign (n38, XL), and
the **manager scoring / profit-margin** tail of the Manager Report (n39, gated).

---

## BUG bucket (front of the line)

None still open. The last open bug -- **n54** (returning-customer duplicate RO +
missing RAF) -- shipped in customer-checkin **v1.13** (S118); see Headline.

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation restores full RO detail (parts/time_logs/photos). | done |
| n2 | Ryan | Warranty category on new RO | Warranty RO type shipped v1.402 (`ro-crud.js` warranty branch; `render.js` warranty filter). | done |
| n5 | Lynn | Work list opened but couldn't view/do anything | Fixed Phase 14: `work-list.js` `renderWorkList`/`removeFromWorkList`/`_setWorkListSilo`. | done |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs` L223/`_setWorkListSilo` L263). | done |
| n17 | Lynn | Add "Ready to Work" RO status | Shipped v1.410 (`config.js` STATUS_PROGRESS_MAP slot 50). | done |
| n21 | Roland | Fix customer check-in, email out, remove photo, streamline | `customer-checkin.html` photo optional + RAF/lead-staff email on every drop-off. | done |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | FIXED v1.447 (`checkin.html` Return-to-Dashboard button). | done |
| n33 | Bobby | Amounts drop the period and zeros (425.00 -> 425) | FIXED v1.447 (2-decimal formatting on currency inputs; no data loss). | done |
| n40 | Sofia | Check-in no longer emails staff / no RAF copy | FIXED v1.10/v1.11 (`shouldNotifyStaff` fires every drop-off; RAF always sends). | done |
| n28 | Brandon | Parts + freight TOTAL in Manage Parts | DELIVERED v1.456: Ticket Totals bar (`parts.js`) -- Parts/Freight/Total/Retail. | done |
| n4 | Lynn | Scheduled future drop-off date + reminder | DELIVERED v1.457: Planned Drop Off Date on New+Edit RO + morning-before reminder + blue Drop chip. | done |
| n36 | Lynn | Scheduled drop-date field in entry dash | DELIVERED v1.457 (same Planned Drop Off Date field; duplicate intent of n4). | done |
| n24 | Brandon | Completion / pickup date on the order | DELIVERED v1.457: `pickup_date` field + green Pickup chip; v1.459 adds pickup calendar + reminders. | done |
| n45 | Brandon | Split Total RVs vs RVs-on-lot count | DELIVERED v1.453: `render.js` `statTotalOnLot` "N on lot" sub-line; v1.454 excludes Scheduled. | done |
| n47 | Ryan | Can't upload pictures from phone | FIXED v1.453: `photos.js` attaches file input to DOM before `.click()` (iOS Safari fix). | done |
| n48 | Brandon | Arrow keys to move through photos | DELIVERED v1.454: lightbox ArrowLeft/Right + Escape (`photos.js`). | done |
| n49 | Sofia | Returning check-in duplicates the RO | FIXED v1.12: dedupe guard updates the existing Scheduled RO; hardened v1.13 (see n54). | done |
| n52 | Brandon | Scheduled units counted as on-lot | DELIVERED v1.454: on-lot count + days-on-lot stats exclude "Scheduled" (`render.js`). | done |
| n53 | Brandon | Freight included in ticket totals | DELIVERED v1.456: Freight line = sum(core_charge) in the Ticket Totals bar (`parts.js`). | done |
| **n18** | Lynn | "Keep plugged in" + keys / no-keys / keypad code | **DELIVERED v1.458**: `key_status`/`keypad_code`/`keep_plugged_in` on New+Edit RO + card chips (`keys_power_fields.sql`). | **done (new)** |
| **n34** | Brandon | Confirm if customer leaves keys with unit | **DELIVERED v1.458**: Keys select (keys/no_keys/keypad) + keypad_code on the RO (`ro-crud.js` L341/L422). | **done (new)** |
| **n37** | Roland | Key dates as colored RO button + calendar + advance notifications | **DELIVERED P1-P3 (v1.457+v1.459)**: chips + per-silo Google Calendar events + day-before/morning-of manager+admin reminders. | **done (new)** |
| **n43** | Roland | Auto-remove cashed-out RO from manager work list | **DELIVERED (S118)**: daily prune job (`prune_cashiered_worklists.sql`). | **done (new)** |
| **n54** | Sofia | David Flanagan: scheduled returning customer duplicated + no RAF | **FIXED v1.13**: submit-time dedupe by phone OR email closes the v1.12 gap. | **done (new)** |
| **n56** | Lynn | Cannot edit time-off entries | **DELIVERED v1.4**: `time-off.html` `editRequest` opens prefilled + UPDATEs (L806/L885). | **done (new)** |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod and Travis last name should be Wombles, not Wimbles" | Data, not code. `staff.name` holds the value; already corrected in DB per S117 (no Wimbles rows remain). |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete jobs / too small on phone / drag-and-drop" | Exact duplicate of **n10** (identical text + timestamp); already declined. |
| n41 | Brandon | Completion / P-U date to record when units leave the lot | Duplicate of **n24** (now DELIVERED v1.457); already declined. |
| n42 | Sofia | Sheldon Gilbert returning check-in sent no email | Duplicate of **n40** (fixed v1.10/v1.11); already declined. |
| n44 | Roland | Auto-remove cashed RO from worklist | Exact duplicate of **n43** (timestamps 1s apart); already declined. |
| n51 | Sofia | Test on returning customer Roland Shepard Jr duplicated | Duplicate of **n49** (fixed v1.12, hardened v1.13); already declined. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Scheduling & key dates  *(n29)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n29 | Lynn | Promised date large/highlighted on tile (amber chip shipped v1.457); remaining: a dedicated sort-by-promised toggle | S |

-> The **Key Dates + Calendar** initiative is otherwise complete: n4/n24/n36/n37
all DONE (drop/promised/pickup fields, colored chips, calendar events, advance
reminders). Only the promised-date sort toggle remains (board priority score
already weights it).

### Theme 2 -- Notifications, reminders & task-tracking  *(n3, n8, n30, n32, n35, n50)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete + daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n35 | Lynn | Transfer clock-in job notes -> RO updates | M |
| n50 | Roland | Email the silo manager when another silo adds a service to an RO | M |

-> **Largest opportunity.** A "Task Tracker + Reminders" subsystem. v1.459 shipped
key-date (promised/pickup) reminders -- reusable infra (`scheduled_notifications`)
for the general scheduled-task notifier these ERs want.

### Theme 3 -- Parts  *(n6, n7, n9, n27)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier | L |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

### Theme 4 -- Work List usability  *(n10, n31)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n10 | Riley | Mobile: too small on phone + touch-drag reorder (delete + HTML5 DnD reorder shipped) | S |
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |

### Theme 5 -- RO fields & status nuance  *(n19, n20, n26, n55)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs existing Hybrid) |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + font/color | L |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning vs never-on-lot | M |
| n55 | Lynn | Hover near customer name to show key dates/tasks as a detail list | M (overlaps n30) |

-> n18 (keys/power) and n34 (keys confirm) shipped v1.458; see DONE.

### Theme 6 -- Work Orders / templates  *(n16, n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will | M |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague -- overlaps n22) |

### Theme 7 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

### Theme 8 -- Reporting & analytics  *(n39)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n39 | Roland | Admin-level Manager-worklist report -> manager scoring + profit-margin visibility | L -- P1-P4 + P4c Scheduled Reminders shipped (`send-manager-report` v2.3); scoring + profit-margin still gated |

### Theme 9 -- Check-in / authorization form  *(n46)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n46 | Sofia | Send the authorization form remotely for e-signature | L -- needs a public token-signing page + email link + signature store + staff notice |

### Theme 10 -- Time Off  *(n57)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n57 | Lynn | Exact absence time window (clock-out to return; AM/PM half-day) instead of total hours | M -- partial-day hours exist (duration only), not a start/end time-of-day |

---

## Recommended action today

1. **Close the six newly-resolved ERs** (set `status=done`): **n18** (b87eb2fb),
   **n34** (34fc03c2), **n37** (2b814250), **n43** (91a03260), **n54** (5c45bb8d),
   **n56** (ee50d11c). All confirmed in the current `pre-prod` tree (v1.458/v1.459,
   time-off v1.4, checkin v1.13, worklist auto-prune).
2. **No code changes are queued from this run** -- the bug bucket is empty.
3. **No new SQL** -- the only DATA item (n25) was already corrected in the DB.

Standout open work: the **Notifications & Task-Tracking** cluster (6 ERs; reuse the
new `scheduled_notifications` reminder infra), **n6** (parts inventory, XL), **n38**
(auth cost reduction, XL), and the gated **n39** manager-scoring / profit-margin
tail. The promised-date **sort toggle** (n29) is the only quick win left in the
otherwise-complete Key Dates initiative.
