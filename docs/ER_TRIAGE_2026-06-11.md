# Enhancement Request Triage -- 2026-06-11 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-11T11:24Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.449** (checkin.html v1.35, customer-checkin.html v1.11).
**Total ERs:** 46 (incl. 3 test entries + 4 exact/near duplicates -> **39 real requests**).
**Triaged this run:** 45 ids in `needs_triage_ids` (every row except the n15 test entry, carried over unchanged).

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `closed-ros.html`, `worklist-report.html`) by grepping
the source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

---

## Headline

The bug bucket remains empty. Every previously confirmed bug has shipped and is
re-verified in the current tree this run:

- **n40** (Sofia) "check-in no longer emails staff / no auth-form copy" -> fixed
  **v1.10/v1.11 (S97)**: `customer-checkin.html` `shouldNotifyStaff` (L2210) now
  fires on every drop-off, and the signed RAF always sends; v1.11 corrected the
  `getSignatureData` typo (L2194) that had aborted RAF emails. **Now DONE.**
- **n33** (Bobby) currency "425.00 shows as 425" and **n22** (Tipton) "no way
  back to dashboard after clock-out" remain fixed (v1.447); markers re-confirmed.

One brand-new ER arrived since the last run:

- **n45** (Brandon, 2026-06-10) -- the dashboard "Total RVs" / "RVs on Lot" stat
  should distinguish units physically on the lot from the total customer count.
  Mapped to `render.js` (`statTotal` L611, `totalRVs` L623) which currently shows
  the full filtered RO count, conflating on-lot units with "Not On Lot". The
  `onLotROs` exclusion filter already exists at L594, so this is a small
  dashboard-stat add. **NEEDED / S.**

Two more near-duplicate/auto-resolved items were confirmed: **n42** (Sofia,
returning-customer Sheldon Gilbert) folds into n40 (now fixed); **n41** (Brandon,
completion/PU date) folds into n24. The only non-enhancement item remains **n25**
-- a data-only staff-name correction (SQL below; Roland runs it).

---

## BUG bucket (front of the line)

None still open. The prior check-in email regression (n40) shipped in v1.10/v1.11
and is re-verified; n22/n33 stay fixed (v1.447).

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation restores full RO detail (parts/time_logs/photos) -> edit + add docs. | done (already) |
| n2 | Ryan | Warranty category on new RO | Warranty RO type (`ro-crud.js` `roType=='warranty'` branch L855; index.html warranty filter), shipped v1.402. | done (already) |
| n5 | Lynn | Work list opened but couldn't view/do anything | Fixed Phase 14: `work-list.js` `renderWorkList` L269 + `removeFromWorkList` L195 + `_setWorkListSilo` L263. | done |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs` L223 / `_setWorkListSilo` L263). | done |
| n17 | Lynn | Add "Ready to Work" RO status | Shipped v1.410 (`config.js` `STATUS_PROGRESS_MAP` L131 slot 50). | done |
| n21 | Roland | Fix customer check-in, email out, remove photo, streamline | `customer-checkin.html` v1.11 made photo optional + auto-emails the signed RAF on every drop-off; staff-notify gap closed (n40). | done |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | FIXED v1.447 (`checkin.html` v1.34/v1.35 Return-to-Dashboard button, L1454). | done |
| n33 | Bobby | Amounts drop the period and the zeros (425.00 -> 425) | FIXED v1.447 (`ro-crud.js` `editDollarValue` L843 `.toFixed(2)`; `parts.js` `MONEY_FIELDS` L288). No data loss. | done |
| n40 | Sofia | Check-in no longer emails staff / no auth-form copy | FIXED v1.10/v1.11 (`customer-checkin.html` `shouldNotifyStaff` L2210 fires on every drop-off; `getSignatureData` typo fixed L2194). | done |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod and Travis last name should be Wombles, not Wimbles, in tech assignment" | Data, not code. `staff.name` holds the value; not hardcoded. One SQL UPDATE (below). |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

Historical ROs that stored the tech name as free text are a separate optional backfill.

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete jobs / too small on phone / drag-and-drop" | Exact duplicate of **n10** (identical text + timestamp); already declined. Fold into n10. |
| n41 | Brandon | Completion / P-U date to record when units leave the lot | Duplicate of **n24** (same submitter); no such field exists (grep). Fold into n24. |
| n42 | Sofia | Returning customer (Sheldon Gilbert) check-in sent no email | Duplicate of **n40**; now fixed v1.10/v1.11 (notify fires for returning customers too). Fold into n40. |
| n44 | Roland | Remove cashed-out RO from manager worklist next day | Exact duplicate of **n43** (identical text, 1s apart). Fold into n43. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Scheduling & key dates  *(n4, n24, n29, n36, n37)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n4 | Lynn | Scheduled future drop-off date + email reminder to manager 1 day before | M |
| n36 | Lynn | Scheduled drop-date field in entry dashboard | S -- duplicate intent of n4 |
| n24 | Brandon | Completion / pickup date on the customer's order for reference | M -- canonical for n41 |
| n29 | Lynn | Promised date large + highlighted on tile; sort by promise date then days-on-lot | M |
| n37 | Roland | Surface RO date/time (pickup/dropoff/reminders) as a prominent colored RO button + integrate into business calendar(s) with advance notifications to managers/admins | L |

-> **"Key Dates + Calendar" initiative.** n37 is the umbrella ask that ties the
drop-off/promised/completion dates (n4/n24/n29/n36) to a calendar surface and
the notifications cluster below.

### Theme 2 -- Notifications, reminders & task-tracking  *(n3, n8, n20, n30, n32, n35)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + font/color | L (urgency partly exists) |
| n35 | Lynn | Transfer clock-in job notes -> RO updates | M |

-> **Largest opportunity.** A "Task Tracker + Reminders" subsystem; overlaps the
deferred Twilio/SMS work -- design as one initiative.

### Theme 3 -- Parts  *(n6, n7, n9, n27, n28)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n28 | Brandon | Parts + freight TOTAL in Manage Parts | S -- per-part Freight Charge exists (`pf_coreCharge` L217); summed total row not built. Clean quick win. |
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier | L -- canonical for n27 |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

### Theme 4 -- Work List usability  *(n10, n31, n43)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n10 | Riley | Mobile: too small on phone + touch-drag (delete + HTML5 drag-drop reorder now ship) | S -- mobile polish |
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |
| n43 | Roland | Auto-remove an RO from a manager's worklist the day after Cashiered Out/Delivered | M -- needs daily job or status hook |

### Theme 5 -- RO fields & status nuance  *(n18, n34, n26, n19, n16, n45)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n18 | Lynn | "Keep plugged in" flag; keys / no-keys / keypad code | M |
| n34 | Brandon | Confirm if customer leaves keys with the unit (RO / sign-in / auth form) | S -- overlaps n18 |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning vs never-on-lot | M |
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs existing Hybrid) |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |
| n45 | Brandon | Distinguish RVs currently on the lot from total customer count in the dashboard stat | S -- `render.js` `statTotal`/`totalRVs` (L611/L623) conflate on-lot + Not On Lot; `onLotROs` filter exists L594 |

### Theme 6 -- Work Orders / templates  *(n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will | M |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague -- overlaps n22) |

### Theme 7 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

### Theme 8 -- Reporting & analytics  *(n39)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n39 | Roland | Admin-level daily Manager-worklist report (parts/labor/costs, upcoming dates, ROs that should be in a worklist but are not) -> manager scoring + profit-margin visibility | L -- extends `worklist-report.html` |

---

## Recommended action today

1. **Close the resolved ERs**: n40 (set `status=done`; was the open check-in
   regression, now shipped). n42 (`declined`, duplicate of n40). n0/n14/n15/n1/n2/
   n5/n12/n17/n21/n22/n33 are already closed; n11/n41/n44 already folded.
2. **Run the n25 staff-name SQL** (Roland; data only).
3. **No code changes are queued from this run** -- the bug bucket is empty.

Standout near-term enhancement wins: **n28 (parts+freight total, S)**, **n45
(on-lot vs total RV count, S)**, and a coordinated **Key Dates + Calendar** pass
(Theme 1, anchored by Roland's n37). The two strategic asks -- **n38 (auth cost
reduction)** and **n39 (manager reporting/scoring)** -- still warrant a design
session with Roland before scoping.
