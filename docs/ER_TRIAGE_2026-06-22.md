# Enhancement Request Triage -- 2026-06-22 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-22T12:52Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.460** (S120 builds; checkin.html v1.38, customer-checkin.html v1.13, time-off.html v1.4). Session 121 staged a design-redesign bundle (CSS/visual only; no functional change to the modules below).
**Total ERs:** 58 (incl. 3 test entries + 5 exact duplicates -> **50 real requests**).
**Triaged this run:** 57 ids in `needs_triage_ids` (every row except the n15 test entry, carried over unchanged).

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `time-off.html`, `supabase/functions/*`, SQL migrations)
by grepping/reading the source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

This run's counts: **DONE 33** (incl. 2 test entries) - **NEEDED 18** - **DUPLICATE 5** - **DATA 1**.

---

## Headline

**Four ERs flipped NEEDED -> DONE this run** -- all shipped in **v1.460 (S120)** and
confirmed in the current tree (the June-21 snapshot still had them as `needed`; their
status was flipped to `done` and the code now backs them):

- **n10** (Riley) work-list touch-drag -> `js/work-list.js` L355 `touchstart/move/end`
  path so reorder works on phones/tablets (HTML5 DnD never fired on touch). Delete
  already worked. Residual phone sizing is minor/cosmetic. **Now DONE.**
- **n26** (Brandon) on-lot sub-status for units temporarily taken & returning ->
  `js/render.js` L662 `'Off Lot - Returning'` status (colored + excluded from the
  on-lot count). **Now DONE.**
- **n35** (Lynn) clock-in job notes -> RO updates -> `checkin.html` v1.38 L1435 copies
  the tech's clock-out work notes into the RO timeline as a `TECH NOTES` note. **Now DONE.**
- **n50** (Roland) email the silo manager when another silo adds a service ->
  `js/work-orders.js` L260 queues a `scheduled_notifications` row
  (`source=service_added_notify`; migration `service_added_notify_source.sql`). **Now DONE.**

**The bug bucket is empty.** Every reported defect (n22, n33, n40, n47, n49, n54) is
fixed and confirmed in current code.

The only non-enhancement item remains **n25** -- a data-only staff-name correction
(SQL below; already applied in DB per S117, no rows remain).

No new ERs arrived since the 2026-06-21 snapshot.

---

## BUG bucket (front of the line)

None still open. All reported defects are fixed and confirmed in current code
(n22 back-button v1.447, n33 currency v1.447, n40 check-in email v1.10/v1.11,
n47 phone photo upload v1.453, n49 dup-RO guard v1.12, n54 dup-RO hardening v1.13).

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation restores full RO detail (parts/time_logs/photos). | done (already) |
| n2 | Ryan | Warranty category on new RO | Warranty RO type v1.402 (`ro-crud.js` `roType=='warranty'`; render.js filter). | done (already) |
| n4 | Lynn | Scheduled future drop-off date + reminder | v1.457 Planned Drop Off Date (New+Edit RO) + morning-before reminder; blue Drop chip. | done (already) |
| n5 | Lynn | Work list opened but couldn't view/do anything | Phase 14: `work-list.js` `renderWorkList`/`removeFromWorkList`/`_setWorkListSilo`. | done |
| n10 | Riley | Work list: drag-and-drop on phone | **v1.460 (S120)** touch-drag (`work-list.js` L355). | done (already) |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs`/`_setWorkListSilo`). | done |
| n17 | Lynn | Add "Ready to Work" RO status | v1.410 (`config.js` STATUS_PROGRESS_MAP). | done |
| n18 | Lynn | "Keep plugged in" + keys/no-keys/keypad code | v1.458 Keys+Power fields (`ro-crud.js` L341/L422; migration `keys_power_fields.sql`). | done (already) |
| n21 | Roland | Fix customer check-in, email out, streamline | `customer-checkin.html` photo optional + forces RAF/staff email (v1.10/v1.11). | done (already) |
| n22 | Tipton | iPhone: no way back after clock-out | FIXED v1.447 (`checkin.html` Return-to-Dashboard button). | done (already) |
| n24 | Brandon | Completion / pickup date on the order | v1.457 `pickup_date` (`ro-crud.js` L254/L312) + green Pickup chip; v1.459 reminders. | done (already) |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning | **v1.460 (S120)** `'Off Lot - Returning'` (`render.js` L662). | done (already) |
| n28 | Brandon | Parts + freight TOTAL in Manage Parts | v1.456 Ticket Totals bar (`parts.js`): Parts/Freight/Total/Retail. | done (already) |
| n29 | Lynn | Promised date large/highlighted on tile | v1.457 prominent amber chip (red if overdue, `render.js`); sort-by-promised toggle deferred. | done (already) |
| n33 | Bobby | Amounts drop the period and the zeros | FIXED v1.447 (`ro-crud.js` `editDollarValue .toFixed(2)`; `parts.js` MONEY_FIELDS). | done (already) |
| n34 | Brandon | Confirm if customer leaves keys with the unit | v1.458 `key_status`/`keypad_code` (`ro-crud.js` L341/L422). | done |
| n35 | Lynn | Clock-in job notes -> RO updates | **v1.460 (S120)** `checkin.html` v1.38 L1435 TECH NOTES -> RO timeline. | done (already) |
| n36 | Lynn | Scheduled drop date in entry dashboard | v1.457 Planned Drop Off Date on New+Edit RO (duplicate intent of n4). | done (already) |
| n37 | Roland | RO date/time as colored button + business calendar | Key Dates P1-P3 (v1.457+v1.459): chips + `scheduling.js` `syncKeyDateCalendars` + `ro-crud.js` `_syncOneKeyDateReminder`. | done (already) |
| n39 | Roland | Admin-level daily Manager-worklist report | `supabase/functions/send-manager-report` v2.3 + P4c (daily cron); scoring/margin deferred. | done (already) |
| n40 | Sofia | Check-in: no email / no auth-form copy | FIXED v1.10/v1.11 (`customer-checkin.html` `shouldNotifyStaff` + RAF email; `getSignatureData` fix). | done (already) |
| n43 | Roland | Remove cashed-out RO from worklist next day | S118 daily prune (migration `prune_cashiered_worklists.sql`). | done (already) |
| n45 | Brandon | Total RVs: on-lot vs total customers | v1.453 `render.js` `statTotalOnLot` sub-line; v1.454 excludes Scheduled. | done (already) |
| n47 | Ryan | Can't upload pictures from phone | FIXED v1.453 (`photos.js` `uploadPhoto` attaches input to DOM before `.click()`). | done (already) |
| n48 | Brandon | Arrow keys to move through photos | v1.454 lightbox keydown (`photos.js` `navigateLightbox`). | done (already) |
| n49 | Sofia | Returning check-in duplicates an RO | FIXED v1.12 (`customer-checkin.html` dedupe guard); hardened v1.13 (see n54). | done (already) |
| n50 | Roland | Email silo manager on service-add to RO | **v1.460 (S120)** `work-orders.js` L260 `service_added_notify`. | done (already) |
| n52 | Brandon | "Scheduled to arrive" not counted as on-lot | v1.454 `render.js` on-lot/days-on-lot exclude 'Scheduled'. | done (already) |
| n53 | Brandon | Ticket totals should include freight | v1.456 Freight line in Ticket Totals bar (`parts.js`), satisfied by n28. | done (already) |
| n54 | Sofia | David Flanagan: returning check-in dup + no RAF | FIXED v1.13 (`customer-checkin.html` dedupe by phone OR email). | done (already) |
| n56 | Lynn | Can't edit time-off entries | v1.4 `time-off.html` `editRequest` (L805) UPDATEs in place; Edit button per row. | done (already) |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod and Travis last name should be Wombles, not Wimbles, in tech assignment" | Data, not code. `staff.name` holds the value; already corrected in DB per S117 (no Wimbles rows remain). SQL below. |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete jobs / too small / drag-and-drop" | Exact duplicate of **n10** (identical text + timestamp); already declined. Fold into n10 (now delivered v1.460). |
| n41 | Brandon | Completion / P-U date field | Duplicate of **n24** (now delivered v1.457). Already declined. |
| n42 | Sofia | Returning check-in sent no email (Sheldon Gilbert) | Duplicate of **n40** (fixed v1.10/v1.11). Already declined. |
| n44 | Roland | Remove cashed-out RO from worklist next day | Exact duplicate of **n43** (timestamps 1s apart). Already declined. |
| n51 | Sofia | Returning check-in dup test (Roland Shepard Jr) | Duplicate of **n49** (fixed v1.12/v1.13). Already declined. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Scheduling & key dates  *(n55, n57)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n55 | Lynn | Hover near customer name in dash view to show important dates/tasks as a list with details | M -- key-date chips exist (v1.457); no hover-aggregation; task list overlaps n30 |
| n57 | Lynn | Time-off: exact absence time window (clock-out -> return; AM/PM half-day) not a total of hours | M -- partial_hours captures duration, not a start/end time-of-day |

-> The Key Dates + Calendar initiative (n37) shipped P1-P3; these are the remaining
date/scheduling polish items.

### Theme 2 -- Notifications, reminders & task-tracking  *(n3, n8, n20, n30, n32)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time | L |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + font/color | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |

-> **Largest opportunity, still unbuilt.** v1.459 (key-date reminders) and v1.460
(service-add notify) delivered point notifications, but the general **Task Tracker +
Reminders** subsystem (digest, per-customer tasks, recurring/urgent task notices)
is not in code. Design as one initiative.

### Theme 3 -- Parts  *(n6, n7, n9, n27)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier | L |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

-> `parts.js` has no return/warranty state and no inventory subsystem (grep
confirmed absent). n7 is canonical for the return/warranty asks (n27).

### Theme 4 -- Work List usability  *(n31)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |

-> The other Work List asks (n10 touch-drag, n43 auto-prune) shipped. n31 needs a
check-in -> work-list integration that does not exist yet.

### Theme 5 -- RO fields & status nuance  *(n16, n19)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs existing Hybrid) |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |

### Theme 6 -- Work Orders / templates  *(n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will | M -- `work-orders.js` has load/save but no edit/reorder |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague -- overlaps n22) |

### Theme 7 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

### Theme 8 -- Check-In & Forms  *(n46)*  [new]
| # | Submitter | Request | LOE |
|---|---|---|---|
| n46 | Sofia | Send the RV authorization form (RAF) remotely for e-signature | L -- `customer-checkin.html` signs only on-site; needs token-signing page + email link + signature store |

---

## Recommended action today

1. **Close the four freshly-delivered ERs** (status already flipped to `done` in DB):
   **n10, n26, n35, n50** -- all confirmed shipped in v1.460 (S120). No further action.
2. **No code changes are queued from this run** -- the bug bucket is empty and the
   S120 builds are in prod-bound `pre-prod`.
3. **n25 staff-name SQL** is already applied (no Wimbles rows remain); re-run only if
   a regression reintroduces the misspelling.
4. Pending Roland release action: **GitHub Release v1.460 + checkin v1.38** (batch
   with the v1.451-v1.459 backlog tags).

Standout near-term enhancement wins: the **Task Tracker + Reminders** subsystem
(Theme 2 -- n3/n8/n20/n30/n32, the largest still-unbuilt cluster) and the **parts
return-to-supplier** state (n7, canonical for n27). The two strategic asks --
**n38 (auth cost reduction, ~$500/mo)** and **n46 (remote e-signature)** -- warrant a
design session with Roland before scoping.
