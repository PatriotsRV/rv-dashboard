# ER Triage - 2026-08-29 (nightly runner, Phase 2a)

**Scope:** incremental pass (`full_pass=false`). 1 ER newly classified (`472ccadb`);
the other 106 rows carry their existing `triage_bucket` / `triage_loe` /
`triage_verdict` forward unchanged.
**Verified against:** HEAD `505e22b` - index.html **v1.502**, messages.html **v1.24**,
checkin.html v1.38, customer-checkin.html v1.17, closed-ros.html v1.3b.
**Input snapshot:** `er_input.json`, fetched 2026-08-29T13:03:33Z. 107 ER rows total.

**Bucket counts (all 107):** Done 62 - Needed 29 - Duplicate 13 - Data 2 - Untriaged 1
**Open work items (DB `status='unreviewed'`):** 30 = 29 Needed + 1 Duplicate awaiting `declined`.
**Bugs: 0.** The four bugs carried by the 2026-08-22 report (`225a2535`, `d94a418a`,
`5ca724ff`, `d4d0eeba`) all shipped in v1.497, and `09084bc5` shipped in v1.494 -
every one is now bucket `done` with the DB row already `status='done'`.

**Grouping:** by function first (the grouping Roland asked for on 2026-08-22),
then LOE/risk within each group.

> Line references inside carried-over verdicts were stamped at the HEAD current on
> the day that verdict was written (v1.487 through v1.501); they are indicative, not
> re-anchored to `505e22b`. Only `472ccadb` was re-grepped tonight.

---

## GROUP 0 - BUGS

**None open.** Nothing in the ER table currently maps to a defect that still exists
in the code at `505e22b`. This is the first triage since Session 94 with an empty bug
bucket. See the "Bugs cleared since 2026-08-22" appendix for what closed them.

---

## GROUP 1 - RO Statuses & Board Sort (4 Needed + 1 Duplicate - ship as ONE batch)

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `44419de9` | Lynn | 08-12 | General UI/UX | needed | S x4 | `js/config.js` STATUS_PROGRESS_MAP L151-164; `index.html` select L7038-7051, filter btns L1085-1096, inline maps L1579/L4489; `js/render.js` statusColorMap L727-739, ALL_STATUSES L741-745 | None of the 4 values (Need Estimate, Awaiting Estimate, Estimate Declined, Job Declined/Close) exist. Each status add hits ~9 hardcoded UI spots PLUS both DB CHECK constraints per the S172 lesson or saves throw 23514. Overlaps `972af03c`. |
| `972af03c` | Lynn | 07-17 | General UI/UX | needed | S | `js/config.js` STATUS_PROGRESS_MAP L151-171 | No Canceled/Cancelled status; the only `cancelled` hits in the repo are the scheduled_notifications and review_requests CHECKs. Add status + color/priority maps, exclude from on-lot and queue counts, keep the record for notes. Canonical for `eb00a68d` and half of `611589ba`. |
| `3e746c18` | Lynn | 08-03 | General UI/UX | needed | S | `js/config.js` STATUS_PROGRESS_MAP; `js/render.js` L656-658, L718-721 | Case-insensitive grep for totaled/totalled returns zero hits. Distinct terminal value from Canceled, so kept separate, but ONE build should add both and exclude both from the queue / on-lot counts. |
| `d1844e46` | Lynn | 07-16 | General UI/UX | needed | S | `js/render.js` renderBoard calculatePriority L30-39, shop-ops pin L35-37 | Board sorts on a single blended priority score; grep for sortBy/sortSelect/sortMode finds nothing in index.html or js/render.js. Needs a selectable strict promised-date-then-oldest-on-lot mode with the Shop Ops pin preserved. |
| `611589ba` | Lynn | 08-24 | General UI/UX | duplicate | - | `js/config.js` STATUS_PROGRESS_MAP L151-171; `index.html` select L7262-7281; `js/render.js` ALL_STATUSES L790-797 | Both halves are already open ERs: CANCELED is `972af03c`, Closed is `44419de9`'s JOB DECLINED/CLOSE. Adds no new work item. **Still `unreviewed` in the DB - recommend `declined` with a pointer.** |

**Risk note (unchanged):** every status add touches both CHECK twins (`repair_orders`
+ `cashiered`). S172's lesson was that 'Off Lot - Returning' shipped UI-only and was
broken for 50 days. Confirm with Lynn whether all six new values survive or whether
Estimate Declined / Job Declined-Close / Canceled collapse. One migration, one release.

---

## GROUP 2 - RO Card: Notes Separation & Task Tracking (3 Needed)

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `a03677e6` | Brandon | 08-07 | General UI/UX | needed | S | `js/render.js` L553 (ro_status stream), L563 (customer_comm); `js/ro-crud.js` L302/L344/L379/L571/L582; `js/parts.js` L654/L855; `index.html` L3116/L3154 | Machine audit lines write as notes type `ro_status` and so crowd RO Status Updates - NOTIFICATION SENT/FAILED, SCHEDULED/CANCELLED, key-date and urgent rows, parts notes. customer_comm already renders separately. Small: retarget the type. **CAUTION: Roland reversed exactly this retarget for lead notes in v1.491 - confirm scope first.** |
| `af14b3cb` | Lynn | 05-21 | General UI/UX | needed | L | `js/shop-tasks.js` (no ro_id, no reminder column) | Confirmed absent: greps for ro_tasks / customer_task / task_tracker across js, index.html and supabase return zero hits. Needs a per-RO tasks table, an add/complete UI and a daily open-task reminder. `ece91cba` supplies the concrete spec (bulleted, anyone can add or check, record who and when). Pairs with `5925fc01`. |
| `5925fc01` | Lynn | 04-09 | New Feature Idea | needed | L | `supabase/functions/send-manager-report`; `js/work-orders.js` L293 service_added_notify; `js/ro-crud.js` L374 urgent_update_notify | PARTIAL. The daily in-progress digest half ships, and single-event notifies exist. Still absent: notify-on-every-RO-update and to-do scraping from status notes. Anchor of this theme; design with `af14b3cb`. |

---

## GROUP 3 - RO Fields & Card Readability (2 Needed)

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `a195e4ab` | Brandon | 08-20 | General UI/UX | needed | M | `index.html` Customer Pay block L7203-7246 / L7782-7804, method Check L7229/L7797, write L6562/L6718; `setROType` L2263-2267 | Zero check tracking: grep for check_number / checkNumber / payments = 0 hits. The only payment capture is the one-value hybrid-RO-only Customer Pay block (one amount + datePaid, no check number), saved flat into `insurance_data` JSONB and never shown on the card. Needs a per-RO checks table + add/list UI + cashiered mirror. |
| `6ff111e7` | Lynn | 08-27 | General UI/UX | needed | S | `js/render.js` Drop chip L468, Promised chip L469, hover row L436 | Prefer needed over done: `fa35e3df` already recolored the Drop chip #0a84ff -> #5cadff in v1.498, but Lynn filed this 5 days AFTER that shipped and adds a NEW size ask. Drop still renders at 0.72rem/700, the smallest of the three key dates, vs Promised at 0.78rem/800. Render/CSS only: lift Drop to Promised-level size and weight plus a stronger hue on the #1a1d24 card. |

---

## GROUP 4 - Messages UX Polish (6 Needed, all S - one messages-v1.25 batch)

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `472ccadb` **NEW** | Andrew Page | 08-28 | General UI/UX | needed | S | `js/messaging.js` msgAttach L347, `_handleAttachPick` L422, `initComposerExtras` L491-494; `js/work-list.js` L430; `js/work-orders.js` L485 | **Newly triaged tonight.** Drag a thumbnail into a conversation to send it (e.g. the aerial parking view). Confirmed absent at HEAD 505e22b: the composer has only a hidden file input plus a paperclip click, and `_handleAttachPick` reads `ev.target.files`; a repo-wide grep for dragover / dragenter / drop / dataTransfer.files hits only the two row-reorder handlers in work-list and work-orders. Small: add a drop zone on the composer that hands `e.dataTransfer.files` to the existing upload path (compression, 5MB MMS cap, 5-attachment cap and video-as-link split all come free). **Ask Andrew whether the thumbnail is an OS file or an in-app photo already in the RO library - the in-app-drag variant needs draggable thumbnails plus a URL-reuse path and is M, not S.** |
| `9aab7550` | Brandon | 07-21 | General UI/UX | needed | S | `messages.html` .unread-dot CSS L469, gate L1313, render L1319, markConversationUnread L1980 | Still only partial. S166 added a MANUAL 'Mark unread' button, but the visual treatment is unchanged: a single 8px gold dot applied only when last_direction is inbound AND status is open, with no row-level highlight. Small: bold/tinted unread row treatment. |
| `21fc3a9d` | Brandon | 07-21 | General UI/UX | needed | S | `messages.html` conversation list (fixed rem sizes) | Confirmed absent: grep for font-scale / fontScale / textSize across js, html and css returns zero hits. Small: add a persisted font-scale control for the conversation list. |
| `f1e6e311` | Brandon | 07-21 | General UI/UX | needed | S | `messages.html` convoSearch L604, programmatic resets L1151/L1170 | Confirmed absent: convoSearch is a plain text input with no clear affordance. Small: add a clear (x) button that empties the search and re-runs the filter. |
| `6e9ca4dc` | Brandon | 07-21 | General UI/UX | needed | S | `js/messaging.js` refreshSignaturePreview L369, #msgSigPreview L350/L374; `messages.html` mobile media query L565 | Valid, still present on desktop: the 'Added to every send:' preview is hidden only inside the mobile max-width media query, so desktop messages.html and the RO-card composer still show it. Small: remove it or collapse it behind a toggle in all viewports; the signature itself keeps sending. |
| `ca0f7ed9` | Brandon | 08-03 | Messages / Texting | needed | S | `messages.html` renderLeadInfo L1389, selectConversation L1744 | Confirmed absent: renderLeadInfo reads ONLY the conversation's own columns (c.rv, c.service_type), so an existing dashboard customer shows 'No RV or service type captured yet'. selectConversation already computes phone-matched live ROs as `linked` for renderContactInfo but calls renderLeadInfo(c) without them. Small: pass `linked`, fall back to RO rv. |

**All six are still one `messages-v1.25` session.** `472ccadb` joins Brandon's batch
cleanly - it lands in the same composer block as `6e9ca4dc`.

---

## GROUP 5 - Parts & Inventory (5 Needed)

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `2af47890` | Brandon | 08-25 | General UI/UX | needed | S | `js/parts.js` markPartReceived L449-478, savePartForm L539; `supabase/migrations/stamp_date_received_on_parts.sql` L33-36 | Prefer needed over done. DB half ships: trigger `trg_stamp_date_received` stamps date_received to the shop-local day on the transition into Received. Client half missing: markPartReceived sets part.status only, never part.dateReceived, so the in-memory row stays blank and a later savePartForm edit resends date_received null, which the trigger will not restamp (OLD.status already Received) - wiping the date. |
| `1e434aff` | Brandon | 08-25 | General UI/UX | needed | S | `js/parts.js` headers L259-267, renderPartsRows L121-145, pf_dateReceived L242 | Confirmed absent: the Manage Parts table has no receipt-date column and renderPartsRows prints no p.dateReceived; the value is only editable in the collapsed Add/Edit Part form. Small: render dateReceived beside the Received tick. **Depends on `2af47890` landing the stamp first.** |
| `8d885331` | Lynn | 04-09 | Parts & Ordering | needed | S | `js/parts.js` pf_supplier L181, pf_partName L190, pf_serviceSilo L208, pf_returnDeadline L226 | Parts-entry form exists and is unchanged. The ask is only to reorder those fields, which is quick once Lynn supplies the target layout or a mockup. **Blocked on detail, not on effort.** |
| `b19975d4` | Lynn | 04-09 | Parts & Ordering | needed | L | `js/config.js` PART_STATUSES L200; `js/parts.js` pf_returnDeadline L226 | Partial infra only: 'Returned' status and a Return Deadline input exist, but grep for rma / return_reason / supplier_credit across js and supabase returns zero hits - no RMA number, return reason, supplier credit or reverse-direction flow. Equals GH#24 Shop Ops Phase 2. Canonical for declined duplicate `86bbaf50`. |
| `14f35f95` | Lynn | 04-09 | Parts & Ordering | needed | XL | `js/parts.js` (per-RO line items only) | Confirmed still absent: word-boundary grep for inventory / on_hand / in_stock / stock_qty across js, supabase/migrations and supabase/functions returns zero hits. New subsystem: inventory table, receive/consume/adjust flows, a searchable priced stock view, links from the parts-request path. **Largest item on the board.** |

---

## GROUP 6 - Work Orders (1 Needed)

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `2f752d40` | Jason Rubin | 07-21 | Work Orders | needed | S | `js/work-orders.js` woFormDollar L360, woFormEstHours L365, save L649-650, dollarLocked gate L324 | Confirmed absent: dollar and hours are independent manual inputs and grep for LABOR_RATE / laborRate / labor_rate in the module returns zero hits (`js/insurance.js` laborRate L212 is the per-claim INSURANCE rate). Small: auto-populate dollar_value = effective hours x $195/hr, respecting the dollarLocked gate. |

---

## GROUP 7 - Check-In / Time Clock (2 Needed - same theme, build once)

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `6bb61db2` | Riley Scott | 07-22 | Time Clock / Check-In | needed | S | `js/render.js` compact-col-actions L228-235; `js/time-tracking.js`; `index.html` | Confirmed absent: the compact actions row has edit / parts / WO / notify / message / schedule but no check-in-out button, and grep for 'clocked in' / 'clocked into' / activeTimeLog returns nothing. Small: add a compact check-in/out action plus a login-time active-time_log banner shown across views. **Treat as the canonical spec for this theme.** |
| `e06d747e` | Riley Scott | 04-10 | Time Clock / Check-In | needed | S | `index.html`, `checkin.html`, `js/render.js` L228-235 | Confirmed absent: no floating or sticky tech check-in control on either page. Underspecified - almost certainly the same ask as `6bb61db2`. Fold in and build once. |

---

## GROUP 8 - Customer / Account Management (2 Needed)

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `0e172b2c` | Sofia | 07-16 | General UI/UX | needed | M | `js/duplicates.js` getBaseROId L35, base-id regex L36, findDuplicateGroups L40, keying L44, executeDupeMerge | Partial: there is an RO-level merge/delete engine, but it groups strictly on the PRVS-XXXX-XXXX base RO id, so two ROs created under different spellings of the same customer are never detected. Needs name/phone account dedup plus a verify-which-is-correct merge step. The one-off cleanup for this case was `3f254929` (already applied). |
| `5fbfa299` | Brandon | 06-30 | General UI/UX | needed | M | `js/ro-crud.js` archiveROInSupabase L662; `js/render.js`; `js/config.js` | Confirmed absent: the only archive path is the cashiered one; case-insensitive grep for archived / abandoned in js/render.js and js/config.js returns zero hits. Needs a soft-archive flag, a board filter and an un-archive path. **Largely solved by Group 1's Canceled status - decide whether the status alone is enough before building.** |

---

## GROUP 9 - RAF / E-Signature (2 Needed)

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `5fec3eca` | Sofia | 07-15 | General UI/UX | needed | S | `supabase/functions/send-quote-email` raf_copy branch L387 | Confirmed absent: the raf_copy payload destructures to / cc / customerName / roId / signedDate / rv / services / workType / phone / customerType / serviceDesc / warrantyDesc / promisedDate / signatureDataUrl with no customerAddress; the address appears only in the lead_staff_notify branch. Small: add customerAddress to the raf_copy payload and both templates. **Edge-function deploy, no dashboard release.** |
| `0448de62` | Sofia | 06-11 | General UI/UX | needed | L | `customer-checkin.html` on-device signature pad; `supabase/functions/send-quote-email` L386 | Confirmed absent: signing is kiosk-only and raf_copy only emails a copy after signing; grep for signing_token / remote_sign / signature_request / signing_link across js, supabase and the HTML pages returns nothing. Needs a tokenized public signing page, a delivery path and signed-form storage. Pairs with the S166 short-link machinery. |

---

## GROUP 10 - Misc UI Quick Wins

**Cleared.** All three items from the 2026-08-22 report shipped: `d432863a` favicon
and `29654af7` back-to-top in v1.498, `50175fce` inline Urgent Update in v1.498. The
one remaining cosmetic (`6ff111e7`, drop-date size/color) sits in Group 3 because it
is the direct follow-on to `fa35e3df`.

---

## GROUP 11 - Strategic / Needs Roland (2 Needed)

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | General UI/UX | needed | XL | `js/auth.js` GOOGLE_CONFIG import L69, gapi init L203-204, google.accounts One Tap + signInWithIdToken; `js/config.js` | Confirmed still Google-based; the kiosk uses the same path. Swapping to Supabase password / magic-link or shared role accounts drops roughly $500/mo but touches every page sign-in plus the email-keyed RBAC. **Dedicated design session with Roland.** |
| `47428f75` | Lynn | 08-03 | General UI/UX | needed | S | `time-off.html` modalStartTime L368, modalEndTime L372, per-row Edit L752 | **UNDERSPECIFIED** - 'update vacation/sick/personal calendar', no stated defect or target. time-off.html already has all three types, a color-coded month calendar, stats tiles, per-row Edit and exact partial-day windows. Filed Needed not Done because the ask maps to no gap without Lynn's detail; if it restates `ee50d11c` it is already delivered. **Ask Lynn.** |

---

## RECOMMENDED ACTION TODAY

1. **Ship the messages batch (`messages-v1.25`).** It is now six S items in one
   surface, and tonight's new ER (`472ccadb`) lands in the same composer block as
   `6e9ca4dc`. Highest value per session on the board, zero DB work, zero migration.
   Order inside the batch: `ca0f7ed9` (functional), `9aab7550`, `f1e6e311`,
   `6e9ca4dc`, `472ccadb`, `21fc3a9d`.
2. **Mark `611589ba` `declined`** with a pointer to `972af03c` + `44419de9`. That is
   the only status flip outstanding tonight and it takes the unreviewed count 30 -> 29.
   (Roland flips statuses; this runner changed nothing.)
3. **Two questions to send out** - both are blocking small builds:
   - **Andrew** (`472ccadb`): is the thumbnail you want to drag a file off your
     desktop, or a photo already attached to the RO? S vs M turns on the answer.
   - **Lynn** (`47428f75`): what is actually wrong with the time-off calendar?
     (Also still open from 08-22: `8d885331` parts field order.)
4. **Parts pair `2af47890` + `1e434aff`** is the next-best small batch after messages -
   but they must ship together and in that order, because `1e434aff` only has a date
   to display once `2af47890` stops wiping it.
5. **Nothing is on fire.** The bug bucket is empty for the first time since Session 94;
   there is no fix branch to open tonight.

### Next sessions (each roughly one session)
- Status batch (Group 1) - after Lynn/Roland confirm the six status names. One
  migration, **both CHECK twins**, one release.
- Parts receipt-date pair, then WO labor-rate autofill (`2f752d40`) plus the
  check-in button batch (`6bb61db2` + `e06d747e`).
- RAF address on the emailed copy (`5fec3eca`) - edge-function deploy only.

### Design-first (need input before code)
Task tracker (`af14b3cb` + `5925fc01`) - check tracking (`a195e4ab`) - account merge
(`0e172b2c`) - remote e-sign (`0448de62`) - parts returns (fold into GH#24) -
inventory XL (`14f35f95`) - auth replacement XL (`ed1b873e`).

### Blocked on submitter detail
`8d885331` (Lynn: target field order) - `47428f75` (Lynn: what is wrong) -
`472ccadb` (Andrew: OS file vs in-app photo).

---

# APPENDIX A - Bugs cleared since the 2026-08-22 report

| ER | By | Date | Shipped | Evidence |
|---|---|---|---|---|
| `225a2535` | Brandon | 08-05 | v1.497 (S178) | `js/insurance.js` L127-129 stamps `window._lastEstimateScan.roKey = editingROSupabaseId`; every consumer gates on it (renderCustomInsuranceFields L47-49, index.html L6633/L6810). Cross-customer insurance bleed closed. |
| `d94a418a` | Brandon | 07-27 | v1.497 (S178) | `js/render.js` L33-39 formatNotesNewestFirst reverses ENTRIES, not lines; the old split-newline/reverse is gone. Applied to RO Status and Customer Comm. |
| `5ca724ff` | Lynn | 08-06 | v1.497 (S178) | `js/work-list.js` L418-425 dragstart now calls `e.dataTransfer.setData` and sets `effectAllowed='move'`. Touch path untouched. |
| `d4d0eeba` | Brandon | 07-22 | v1.497 (S178) | `index.html` L4342-4344 Clear Filters retries scrollToROInBoard after re-render; `js/qr.js` L198-214 one-shot deep-link filter clear. Also closed duplicates `9031e101` and `1bc33dd6`. |
| `09084bc5` | Lynn | 08-04 | v1.494 (S171) | `archive_single_ro` RPC + `cashiered_full_detail_s171.sql` (7 dropped columns, 7 child mirrors); closed-ros.html v1.3b restores them on reactivate. |

Also newly `done` since the last report: `29654af7` (back-to-top, v1.498),
`d432863a` (favicon, v1.498), `50175fce` (inline urgent update, v1.498),
`ac8265c8` (RO mileage, v1.498), `fa35e3df` (blue-font contrast, v1.498).

---

# APPENDIX B - Data bucket (2 ERs, both already applied - no SQL to run tonight)

| ER | By | Date | Category | Bucket | Verdict |
|---|---|---|---|---|---|
| `6938c1af` | Ryan Dillon | 05-06 | General UI/UX | data | Wimbles -> Wombles staff-name correction, already applied in the live DB. RESIDUAL (not a runtime bug): seed file `supabase/migrations/staff_table.sql` L85-87 still inserts Rod/Zak/Travis as 'Wimbles', so a re-seed would reintroduce it. Re-apply if ever needed: `UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` Nothing executed by this runner. |
| `3f254929` | Sofia | 07-16 | General UI/UX | data | The misspelled Steve Hubert RO was soft-deleted in S178; only the correct Huebert RO remains. SQL of record: `SELECT id, ro_id, customer_name, status FROM repair_orders WHERE customer_name ILIKE '%ubert%';` then, with the confirmed wrong id, `UPDATE repair_orders SET deleted_at = now() WHERE id = '<wrong-uuid>' AND deleted_at IS NULL;` The wider merge-accounts ask stays open as `0e172b2c`. Nothing executed by this runner. |

---

# APPENDIX C - Duplicate bucket (13 ERs)

| ER | By | Date | DB status | Folds into |
|---|---|---|---|---|
| `611589ba` | Lynn | 08-24 | **unreviewed** | `972af03c` (Canceled) + `44419de9` (Job Declined/Close) - **flip to `declined`** |
| `aa508852` | Riley Scott | 04-09 | declined | `fd6c122d` (work list delete / mobile / drag) |
| `86bbaf50` | Lynn | 05-13 | declined | `b19975d4` (supplier returns / warranty) |
| `67daba15` | Brandon | 06-09 | declined | `d2561e11` (RO completion / pickup date) |
| `fa1c1d6e` | Sofia | 06-09 | declined | `a22739b9` (check-in email not sent) |
| `5057c7fc` | Roland | 06-09 | declined | `91a03260` (prune cashiered from worklists) |
| `fc6b29f9` | Sofia | 06-17 | declined | `298e0e67` (returning-customer duplicate RO) |
| `9031e101` | Riley Scott | 07-21 | declined | `d4d0eeba` (now Done, v1.497) |
| `1bc33dd6` | Brandon | 07-27 | declined | `d4d0eeba` (now Done, v1.497) |
| `2b78696c` | Lynn | 07-27 | declined | `29654af7` (now Done, v1.498) |
| `eb00a68d` | Lynn | 08-03 | declined | `972af03c` (Canceled status) |
| `2deb8f30` | Lynn | 08-13 | declined | `a03677e6` (notification lines out of RO Status) |
| `ece91cba` | Lynn | 08-17 | declined | `af14b3cb` (per-RO to-do list) - carries the best spec detail |

---

# APPENDIX D - Done bucket (62 ERs, carried over unchanged)

| ER | By | Date | Category | Delivered in / evidence |
|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | General UI/UX | Test entry ('Testing out the Make-A-Wish function'). Not a real request; already declined. |
| `79b2c805` | Sofia | 04-09 | New Feature Idea | closed-ros.html reactivateRO L838 restores a cashed-out RO from cashiered_* mirrors; js/photos.js uploadDocument L253 adds documents. |
| `75124b03` | Ryan Dillon | 04-09 | RO Management | v1.402 Warranty RO type at creation; board filter js/render.js L121, chip L195, badge L372. |
| `b2aa4bea` | Lynn | 04-09 | New Feature Idea | planned_dropoff_date on create/edit (js/ro-crud.js L413/L490) + blue Drop chip + day-before reminder. Same field as `468aa376`. |
| `18611eee` | Lynn | 04-09 | Bug Report | Was a real bug; js/work-list.js renderWorkList + removeFromWorkList L195 + drag-reorder + per-silo tabs. |
| `17492066` | Lynn | 04-09 | Notifications / Email | Per-RO Schedule Notification bell + scheduled_notifications.sql + process-scheduled-notifications cron. |
| `fd6c122d` | Riley Scott | 04-09 | Work List | v1.460: delete L195, drag-reorder mouse + touch, persisted by _saveWorkListOrder, mobile sizing fixed. |
| `98bd7498` | Brandon | 04-09 | RO Management | Per-silo Work List tabs (_renderWorkListSiloTabs L223, _setWorkListSilo L263). |
| `19dc2016` | Roland | 04-11 | Time Clock / Check-In | Test entry (ER form dictation). Already declined. |
| `1fe68261` | Rod Wombles | 04-13 | Work Orders | v1.467 (S128) js/shop-tasks.js + shop_tasks.sql for non-customer work. |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | v1.410 'Ready to Work' status (js/config.js L158, slot 50). |
| `b87eb2fb` | Lynn | 04-14 | RO Management | v1.458 keys_power_fields.sql: key_status, keypad_code, keep_plugged_in. Same build as `34fc03c2`. |
| `a5ff3e2d` | Lynn | 04-21 | RO Management | v1.465 'Warranty + Repair' RO type end to end, still billable. |
| `a7d1474e` | Lynn | 04-23 | RO Management | v1.466 urgent_update_field.sql + red URGENT banner js/render.js L324 + urgent_update_notify. |
| `dde90d2b` | Roland | 04-27 | RO Management | customer-checkin.html v1.17: photo optional, raf_copy L2530 + lead_staff_notify L2603 forced every submission. |
| `13e1cd87` | Tipton Scott | 04-30 | Time Clock / Check-In | v1.447 / checkin.html v1.34 (now v1.38): Return to Dashboard button after clock-out L1516. |
| `e27311bf` | Ryan Dillon | 05-04 | Work Orders | v1.464 WO task drag-reorder (_initWOTaskDrag L461); DOM order drives sort_order. |
| `d2561e11` | Brandon | 05-06 | New Feature Idea | v1.457 key_dates_pickup_date.sql + green Pickup chip js/render.js L397. |
| `dac9fdda` | Brandon | 05-06 | General UI/UX | v1.460 'Off Lot - Returning' status; excluded from the on-lot count L718-721. |
| `a8e90a7d` | Brandon | 05-20 | General UI/UX | v1.456 (S117) Manage Parts Ticket Totals: parts + freight + total. Same build as `671fa623`. |
| `1aeb3f58` | Lynn | 05-21 | General UI/UX | v1.457 large bold promised-date chip, red when overdue; feeds calculatePriority. Strict sort is open ER `d1844e46`. |
| `cbc70a86` | Lynn | 05-21 | General UI/UX | S128: customer-checkin.html L2381 rpc('add_ro_to_silo_work_lists'). |
| `3f415c88` | Lynn | 05-26 | General UI/UX | v1.463 Schedule Notification repeat none/daily/weekly + repeat-until (index.html snRepeat L2993). |
| `cd663d8f` | Bobby Thatcher | 05-28 | General UI/UX | Was cosmetic; fixed v1.447 - currency inputs show 2 decimals (js/ro-crud.js L1097, js/parts.js L307/L310). |
| `34fc03c2` | Brandon | 05-29 | New Feature Idea | v1.458 Keys selector on New and Edit RO + keys chip. Covers `b87eb2fb`. |
| `0b2e128c` | Lynn | 06-02 | General UI/UX | v1.460 / checkin.html v1.38: clock-out notes copied into the RO timeline (checkin.html L1455). |
| `468aa376` | Lynn | 06-02 | General UI/UX | planned_dropoff_date input on New + Edit RO (restored v1.457). Same field as `b2aa4bea`. |
| `2b814250` | Roland | 06-07 | Calendar | Key-date chips + hover panel + sync-ro-calendar (idempotent via cal_event_ids) + advance reminders. |
| `cd084233` | Roland | 06-07 | Work List | send-manager-report + send-admin-pnl-report + worklist-report.html. Manager scoring is a future extension. |
| `a22739b9` | Sofia | 06-08 | General UI/UX | Notify-gate regression; both sends forced on every check-in submission (v1.17). Canonical for `fa1c1d6e`. |
| `91a03260` | Roland | 06-09 | Work List | prune_cashiered_worklists.sql removes the RO the day AFTER Delivered/Cashed Out. Canonical for `5057c7fc`. |
| `cdd77a8b` | Brandon | 06-10 | General UI/UX | v1.453/v1.454 updateStats splits queue vs on-lot (nonShop L656-658, onLotCount L718-721). |
| `2c3d5633` | Ryan Dillon | 06-12 | General UI/UX | v1.453 js/photos.js L66 appends the upload input to the DOM before clicking (iOS Safari dropped-change fix). |
| `74a33621` | Brandon | 06-16 | General UI/UX | v1.454 lightbox arrow-key navigation + Escape (js/photos.js L396-404). |
| `298e0e67` | Sofia | 06-16 | General UI/UX | Data-integrity bug; check-in v1.17 reuses the scheduled RO (reusedScheduledRO L2059/L2165) + attach-vs-new dialog L2194. Canonical for `fc6b29f9`. |
| `9b823d25` | Roland | 06-16 | RO Management | v1.460 addServiceToRO L244 queues service_added_notify L293 to the owning silo manager. |
| `9b046808` | Brandon | 06-17 | General UI/UX | v1.454 'Scheduled' status excluded from days-on-lot and on-lot counts. |
| `671fa623` | Brandon | 06-18 | General UI/UX | v1.456 (S117) Freight line summed into Total Cost. Same build as `a8e90a7d`. |
| `5c45bb8d` | Sofia | 06-19 | General UI/UX | Duplicate-RO + missing-email field report (David Flanagan); both halves resolved in check-in v1.17. |
| `b06a285f` | Lynn | 06-19 | General UI/UX | v1.465 key-dates panel on the customer name (js/render.js kd-name-cal L367). |
| `ee50d11c` | Lynn | 06-19 | General UI/UX | time-off.html per-row Edit L752 -> editRequest(), submit flips to 'Update Record'. |
| `88395acc` | Lynn | 06-19 | General UI/UX | Time Off v1.5 exact absence window (add_absence_window_to_time_off.sql; modalStartTime L368 / modalEndTime L372). |
| `80390f36` | Lynn | 06-23 | General UI/UX | v1.462 Shop Operations tiles pinned top (aShop L35) + search token L61. |
| `7f2d0d70` | Lynn | 06-26 | General UI/UX | send-dropoff-report: Monday drop-off email, reads manager_work_lists to flag missing assignments. Pairs with `cbc70a86`. |
| `3f7fb4fc` | Brandon | 06-29 | General UI/UX | v1.468 shop ROs excluded from queue + on-lot; status dropdown replaced by a static label (cep-static-status L249). |
| `cb7742a8` | Lynn | 06-29 | General UI/UX | v1.469 / check-in v1.15: sync-ro-calendar posts to the correct team calendar for front-desk check-ins too. |
| `29654af7` | Lynn | 07-06 | General UI/UX | **v1.498 (S178)** #backToTopBtn index.html L8212-8220 + css/dashboard.css L3479-3498. Canonical for `2b78696c`. |
| `39b75fab` | Sofia | 07-09 | General UI/UX | v1.472 Documents/Photos rename, move-to-another-RO and delete, office-staff gated (canManageFiles L618). |
| `1c7f1410` | Andrew Page | 07-16 | New Feature Idea | js/messaging.js multi-media attach (L347 accept + multiple, cap 5, video/MMS split L578-579). |
| `d432863a` | Brandon | 07-21 | General UI/UX | **v1.498 (S178)** favicon + apple-touch-icon on all 13 pages; also closes the S139 favicon-404 noise. |
| `d4d0eeba` | Brandon | 07-22 | General UI/UX | **v1.497 (S178)** - see Appendix A. |
| `9d3806e7` | Brandon | 07-22 | General UI/UX | S154 customer-side Notes section on messages.html + customer_note_entries_s154.sql. |
| `ac394468` | Roland | 07-24 | General UI/UX | v1.482 / S158 erCategory covers every page (js/enhancement.js pageDefaults L43). RESIDUAL: erCategory exists in only 6 live HTML files, so messages / leads / time-off / guide cannot RAISE an ER - worth its own small ER. |
| `d62a26e4` | Roland | 07-24 | New Feature Idea | v1.482 / S158a multi-attachment (array L290, cap L295, per-file chips L386-392). |
| `93b00023` | Roland | 07-24 | New Feature Idea | v1.482 (S158): reopen-on-inbound + owner notify only on the needs-reply flip + 4:30pm send-unreplied-reminder sweep. |
| `d94a418a` | Brandon | 07-27 | General UI/UX | **v1.497 (S178)** - see Appendix A. |
| `50175fce` | Lynn | 07-27 | General UI/UX | **v1.498 (S178)** editUrgentUpdate js/ro-crud.js L404, wired index.html L3968; still fires urgent_update_notify. |
| `09084bc5` | Lynn | 08-04 | General UI/UX | **v1.494 (S171)** - see Appendix A. |
| `225a2535` | Brandon | 08-05 | Bug Report | **v1.497 (S178)** - see Appendix A. |
| `ac8265c8` | Lynn | 08-05 | General UI/UX | **v1.498 (S178)** add_mileage_s178.sql + Edit RO input index.html L7670 + card readout js/render.js L523-526. |
| `5ca724ff` | Lynn | 08-06 | General UI/UX | **v1.497 (S178)** - see Appendix A. |
| `fa35e3df` | Lynn | 08-18 | General UI/UX | **v1.498 (S178)** low-contrast blues lightened (css/dashboard.css L560/L609/L1210/L3039, L1930; js/render.js L26). |

---

# APPENDIX E - Untriaged carry-over (1 ER)

| ER | By | Date | Category | DB status | Note |
|---|---|---|---|---|---|
| `f73a1e5a` | Roland | 04-13 | General UI/UX | declined | 'Testing dictation 123 testing enhancement request dictation'. `triage_bucket` is NULL in the DB and the row was not in tonight's `needs_triage_ids`, so this runner did not classify it. Plainly a test entry, already declined; harmless, but worth a one-line backfill next full pass. |

---

*Generated by the nightly ER-triage runner (Phase 2a, STEPS 2-4). No code changed,
no DB writes, no status flips - Roland flips status himself. Verdict payload:
`er_verdicts.json`. Dashboard: `docs/er-triage-dashboard.html`.*
