# Enhancement Request Triage -- 2026-06-12 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-12T11:04Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.449** (checkin.html v1.35, customer-checkin.html v1.11).
**Total ERs:** 47 (incl. 3 test entries + 4 exact/near duplicates -> **40 real requests**).
**Triaged this run:** 46 ids in `needs_triage_ids` (every row except the n15 test entry, carried over unchanged).

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `closed-ros.html`, `worklist-report.html`,
`supabase/functions/*`) by grepping the source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

---

## Headline

The bug bucket stays empty. The one open bug from the prior run -- **n40** (Sofia,
"check-in no longer emails staff / no copy of the authorization form") -- shipped
in **v1.10/v1.11 (Session 97)** and is confirmed in the current tree:
`customer-checkin.html` `shouldNotifyStaff` now fires a `repair@` notice on every
drop-off, the signed RAF always sends, and v1.11 fixed the `getSignatureData`
typo (`getSignatureData` confirmed at L1754) that had aborted the RAF emails.
Its returning-customer duplicate **n42** (Sheldon Gilbert) folds into it. Both
are now resolved.

**One brand-new ER arrived 2026-06-11 (n46, Sofia):** "create a way to send an
authorization form remotely for e-signature." Verified NEEDED -- the RAF in
`customer-checkin.html` is signed **on-site only** (`signatureCanvas` L884 +
`getSignatureData` L1754); a grep for "remote" turns up nothing outside the
unrelated solar pages, so no remote-signing link/route exists today. LOE **L**:
needs a tokenized public signing page + email link + signature capture/store +
staff notice. New **Check-in & Authorization** theme.

The only non-enhancement item remains **n25** -- a data-only staff-name
correction (SQL below; Roland runs it).

---

## BUG bucket (front of the line)

None still open. The prior open bug (n40) shipped in v1.10/v1.11 -- see Headline.

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation restores full RO detail (parts/time_logs/photos) -> edit + add docs. | done (already) |
| n2 | Ryan | Warranty category on new RO | Warranty RO type shipped v1.402 (warranty filter + `roType=='warranty'` branch). | done (already) |
| n5 | Lynn | Work list opened but couldn't view/do anything | Fixed Phase 14: `work-list.js` `renderWorkList` (L269) + `removeFromWorkList` (L195) + `_setWorkListSilo` (L263). | done |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs` L223 / `_setWorkListSilo` L263). | done |
| n17 | Lynn | Add "Ready to Work" RO status | Shipped v1.410 (`config.js` STATUS_PROGRESS_MAP L131 slot 50). DB mislabeled in-progress. | done |
| n21 | Roland | Fix customer check-in, email out, remove photo, streamline | `customer-checkin.html` v1.11: photo optional + auto-emails signed RAF on every drop-off (S50/71/80/97). | done |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | FIXED v1.447 (`checkin.html` v1.34 Return-to-Dashboard button, L1454). | done |
| n33 | Bobby | Amounts drop the period and the zeros (425.00 -> 425) | FIXED v1.447 (`ro-crud.js` `editDollarValue` L843 `.toFixed(2)` + `parts.js` MONEY_FIELDS L288). No data loss. | done |
| n40 | Sofia | Check-in no longer emails staff / no RAF copy | FIXED v1.10/v1.11 (S97): `shouldNotifyStaff` fires every drop-off + RAF always sends; v1.11 fixed `getSignatureData` typo. | done |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod and Travis last name should be Wombles, not Wimbles, in tech assignment" | Data, not code. `staff.name` holds the value; not hardcoded. One SQL UPDATE (below). |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

Historical ROs that stored the tech name as free text are a separate optional backfill.

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete jobs / too small on phone / drag-and-drop" | Exact duplicate of **n10** (identical text + timestamp); already declined. Fold into n10. |
| n41 | Brandon | Completion / P-U date to record when units leave the lot | Duplicate of **n24** (same submitter); same Key Dates field. Fold into n24. |
| n42 | Sofia | Returning customer (Sheldon Gilbert) check-in sent no email | Duplicate of **n40**; same defect, now fixed v1.10/v1.11. Fold into n40. |
| n44 | Roland | Auto-remove cashed-out RO from manager work list | Exact duplicate of **n43** (timestamps 1s apart). Fold into n43. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Scheduling & key dates  *(n4, n24, n29, n36, n37)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n4 | Lynn | Scheduled future drop-off date + email reminder to manager 1 day before | M |
| n36 | Lynn | Scheduled drop-date field in entry dashboard | S -- duplicate intent of n4 |
| n24 | Brandon | Completion / pickup date on the customer's order (canonical for n41) | M |
| n29 | Lynn | Promised date large + highlighted on tile; sort by promise date then days-on-lot | M |
| n37 | Roland | Surface RO date/time (pickup/dropoff/reminders) as a prominent colored RO button + integrate into business calendar(s) with advance notifications | L |

-> **"Key Dates + Calendar" initiative.** n37 is the umbrella ask tying the
drop-off/promised/completion dates (n4/n24/n29/n36) to a calendar surface and
the notifications cluster below. `promised_date` already exists in the
`ro-crud.js` schema; no `completion_date`/`pickup_date` field (confirmed by grep).

### Theme 2 -- Notifications, reminders & task-tracking  *(n3, n8, n20, n30, n32, n35)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + font/color | L (urgency partly exists) |
| n35 | Lynn | Transfer clock-in job notes -> RO updates | M |

-> **Largest opportunity.** A "Task Tracker + Reminders" subsystem; overlaps the
deferred Twilio/SMS work (`supabase/functions/twilio-sms`,
`process-scheduled-notifications`) -- design as one initiative.

### Theme 3 -- Parts  *(n6, n7, n9, n27, n28)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n28 | Brandon | Parts + freight TOTAL in Manage Parts | S -- per-part Freight Charge (`pf_coreCharge` L217) exists; summed total row not built. Clean quick win. |
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier (canonical for n27) | L |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

### Theme 4 -- Work List usability  *(n10, n31, n43)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n10 | Riley | Mobile: too small on phone + touch drag-drop (delete + HTML5 DnD reorder now ship) | S -- mobile polish |
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |
| n43 | Roland | Auto-remove an RO from a manager's work list the day after Cashiered Out/Delivered (canonical for n44) | M -- needs a daily job or status hook |

### Theme 5 -- RO fields & status nuance  *(n16, n18, n19, n26, n34)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n18 | Lynn | "Keep plugged in" flag; keys / no-keys / keypad code | M |
| n34 | Brandon | Confirm if customer leaves keys with the unit (RO / sign-in / auth form) | S -- overlaps n18 |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning vs never-on-lot | M |
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs existing Hybrid) |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |

### Theme 6 -- Work Orders / templates  *(n23)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will | M -- `work-orders.js` has create/apply, no template editing/reorder |

### Theme 7 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

### Theme 8 -- Reporting & analytics  *(n39, n45)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n39 | Roland | Admin-level daily Manager-worklist report (parts/labor/costs, upcoming dates, ROs missing from a worklist) -> manager scoring + profit-margin visibility | L -- extends `worklist-report.html` |
| n45 | Brandon | Split "Total RVs" vs "RVs on Lot": `render.js` `statTotal` L611 / `totalRVs` L623 show the full filtered count, conflating on-lot with "Not On Lot" (`onLotROs` filter exists L594) | S -- dashboard-stat add |

### Theme 9 -- Check-in & authorization  *(n13, n46)*  [new theme]
| # | Submitter | Request | LOE |
|---|---|---|---|
| n46 | Sofia | Send the authorization form (RAF) remotely for e-signature -- RAF signs on-site only today (`signatureCanvas` L884 / `getSignatureData` L1754); no remote-signing link/route exists | L -- public token-signing page + email link + signature capture/store + staff notice |
| n13 | Riley | Tech check-in "float" for ease of access on the time-clock page | S (vague -- overlaps n22, now fixed) |

---

## Recommended action today

1. **Close the resolved ERs**: n40 (`done`, the prior open bug, now shipped), and
   confirm the already-closed n5, n12, n17, n21, n22, n33 (`done`).
   n0/n14/n15/n1/n2 are already closed.
2. **Decline the duplicates**: n11, n41, n42, n44 (`declined`; folded into
   n10/n24/n40/n43 respectively). n11/n42/n44 already declined in DB.
3. **Run the n25 staff-name SQL** (Roland; data only).
4. **No code changes are queued from this run** -- the bug bucket is empty.

Standout near-term enhancement wins: **n28 (parts+freight total, S)**,
**n45 (Total-vs-on-lot stat split, S)**, and a coordinated **Key Dates + Calendar**
pass (Theme 1, anchored by Roland's n37). New strategic asks still pending a
design session with Roland: **n38 (auth cost reduction)**, **n39 (manager
reporting/scoring)**, and now **n46 (remote RAF e-signature)** -- the latter
directly extends the customer-checkin/RAF work that resolved n40/n42.
