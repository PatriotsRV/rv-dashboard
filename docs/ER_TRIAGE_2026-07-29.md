# PRVS Enhancement-Request Triage - 2026-07-29 (headless runner, Phase 2a)

Nightly automated pass over `enhancement_requests` against the current modular
codebase (**index.html v1.484**, **messages.html v1.17**, checkin.html v1.34).
Input snapshot `er_input.json` fetched 2026-07-29 09:40 UTC: 88 rows,
`needs_triage_ids` = 87 (all but one carried-over test entry).

Read-only run. No code was changed, no SQL was executed, no ER status was
flipped - status changes and the emitted SQL are Roland's to run. Every
"Done" and "Bug" verdict below was re-verified by grepping/reading the live
code this pass, not carried over on faith.

## Board snapshot

| Status | Count |
|---|---|
| done | 49 |
| declined | 9 |
| unreviewed | 30 |
| **Total** | **88** |

## Bucket snapshot (87 classified + 1 carried over)

| Bucket | Count |
|---|---|
| done | 52 |
| needed | 22 |
| duplicate | 8 |
| bug | 3 |
| data | 2 |
| (carried over, untriaged) | 1 |

**No new ERs since the 2026-07-28 run** - the newest submission is still
2026-07-27 20:33 UTC. Every one of yesterday's 87 verdicts **still holds on
v1.484 / messages v1.17**; nothing that shipped in S161 (lead capture / create
RO from lead) closed an open item. Line references were refreshed where
index.html and messages.html shifted.

**The 30 open (`unreviewed`) ERs break down as: 22 Needed - 3 Bugs - 1 Data -
4 ready to close** (2 already delivered, 2 duplicates).

## Code checks run this pass

- `scrollToROInBoard` (index.html **L4094**, toast L4109) and `handleDeepLink`
  (js/qr.js L198, early returns L207/L210) - **both still bail silently**
  (confirms bugs d4d0eeba, 9031e101, 1bc33dd6)
- `js/render.js` L553 `split('\n') + reverse()` on the RO Status box - **still
  reverses every line**; L563 does the same to Customer Communication
  (confirms bug d94a418a, and extends it by one line)
- back-to-top / `window.scrollTo` across index.html + js + css - **none**
  (confirms 29654af7, 2b78696c)
- favicon / `rel=icon` / apple-touch-icon across all live `*.html` - **none**
  (confirms d432863a)
- `cancel` in js/config.js `STATUS_PROGRESS_MAP` (L151-164) + js/render.js
  `ALL_STATUSES` (L741-745) - **none** (confirms 972af03c)
- inventory / on_hand / in_stock / stock_qty across js + supabase - **none**
  (confirms 14f35f95); rma / return_reason / supplier_credit - **none**
  (confirms b19975d4)
- ro_tasks / task_tracker / customer_task across js + supabase/migrations -
  **none**; js/shop-tasks.js has no `ro_id` link (confirms af14b3cb, 5925fc01)
- `raf_copy` destructure in supabase/functions/send-quote-email L387 - **no
  `customerAddress`** (confirms 5fec3eca)
- sortBy / sortSelect / sortMode - **none**, board still sorts on the single
  blended `calculatePriority` (confirms d1844e46)
- LABOR_RATE / `195` in js/work-orders.js - **none** (confirms 2f752d40)
- messages.html v1.17: no font-scale control, `convoSearch` (L469) has no clear
  button, `#msgSigPreview` hidden only inside `@media (max-width:760px)` (L430)
  (confirms 21fc3a9d, f1e6e311, 6e9ca4dc)
- js/auth.js still Google-only (`GOOGLE_CONFIG` L69, gapi init L203-204)
  (confirms ed1b873e); signing_token / esign / remote_sign - **none**
  (confirms 0448de62)

---

## Bugs - front of the line (3)

| ER | By | Date | Category | Bucket | Mapped code | Verdict | Severity |
|---|---|---|---|---|---|---|---|
| `d4d0eeba` | Brandon Dillon | 07-22 | General UI/UX | **bug** | index.html L4094/L4109; js/qr.js L198-210 | **Canonical.** Two paths fail the same way. Work list: `scrollToROInBoard` only finds already-rendered cards, else toasts (the toast's Clear Filters re-renders but never scrolls). Messages: `handleDeepLink` returns silently when the RO is not in `currentFilteredData` or has no card. Fix = clear filters, re-render, scroll, highlight. | small (navigation, no data risk) |
| `9031e101` | Riley Scott | 07-21 | Work List | **bug** | js/work-list.js L328 -> index.html L4094 | Same root cause, iPhone work-list path. Tapping a unit on a filtered board dead-ends in the "RO not visible" toast. Fix with `d4d0eeba` in one change. | small (mobile navigation) |
| `d94a418a` | Brandon Dillon | 07-27 | General UI/UX | **bug** | js/render.js L553 (and L563) | RO Status box is drawn with `split('\n').map(escapeHtml).reverse()`, reversing **every line**, so a multi-line note reads bottom-line-first - exactly the reported "latter portion near the top". Entry boundaries are lost upstream (js/ro-crud.js joins entries with `\n`). The S159 history modal splits on the entry regex and is correct - reuse that split. **New this pass:** L563 does the same to the Customer Communication box. | data-display (render only, no data loss) |

All three are inside the Phase-2b auto-fix class (cosmetic / data-display /
small, no auth, money, deletes, DB or notifications) - `d4d0eeba` + `9031e101`
are one fix, `d94a418a` a second.

## Data corrections - SQL for Roland, nothing executed (2)

| ER | By | Date | Bucket | Verdict + SQL |
|---|---|---|---|---|
| `3f254929` | Sofia Pedroza | 07-16 | **data** | Keep Steve **Huebert**, remove the misspelled **Hubert** RO. Eyeball first: `SELECT id,ro_id,customer_name,status FROM repair_orders WHERE customer_name ILIKE '%ubert%';` then, once the wrong id is confirmed: `UPDATE repair_orders SET deleted_at=now() WHERE id='<wrong-id>';` The merge *feature* is tracked as `0e172b2c`. |
| `6938c1af` | Ryan Dillon | 05-06 | **data** | Already applied live - tech names read from `staff.name` and "Rod Wombles" spells correctly in this very table. **Residual (not a runtime bug):** the seed file `supabase/migrations/staff_table.sql` L85-87 still inserts Rod/Zak/Travis as "Wimbles", so a re-seed reintroduces it. Re-apply if ever needed: `UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |

## Ready to close (4)

| ER | By | Date | Bucket | Recommend | Why |
|---|---|---|---|---|---|
| `1c7f1410` | Andrew Page | 07-16 | **done** | `status=done` | Message media attach already ships: js/messaging.js `#msgAttach` with `multiple` + `accept image/*,application/pdf` (L219), `_msgAttachments` capped at 5 (L207) with removable chips (L256-264). |
| `9d3806e7` | Brandon Dillon | 07-22 | **done** | `status=done` | Customer-side Notes ship on messages.html v1.17: `detailNoteView` L530, edit button L528, `openNotesModal` L1360, append-only `customer_note_entries` (read L1326). |
| `1bc33dd6` | Brandon Dillon | 07-27 | **duplicate** | `status=declined` | Re-file of `d4d0eeba` (same submitter, 5 days apart, same defect). Fold in. |
| `2b78696c` | Lynn Titel-Shepard | 07-27 | **duplicate** | `status=declined` | Re-file of `29654af7` (back-to-top button). Fold in. |

---

## Needed - the 22 open enhancements, by theme then size

LOE mix: **2 XL - 4 L - 2 M - 14 S**. The board is dominated by small,
well-specified items; the two XLs are the only genuine subsystem builds.

### Messaging & e-sign (6 - the biggest cluster; 5 of 6 are S)

| ER | By | Date | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|---|
| `0448de62` | Sofia | 06-11 | **L** | Send an authorization form remotely for e-signature | Signing is kiosk-only (customer-checkin.html signature pad); `raf_copy` (send-quote-email L386) only emails a copy *after* signing. No `signing_token` / `esign` / `remote_sign` anywhere. Needs a tokenized public signing page + delivery + signed-form storage. Pairs with the Textly SMS channel. |
| `5fec3eca` | Sofia | 07-15 | **S** | Show the customer's address on the emailed authorization-form copy | `raf_copy` destructure (send-quote-email L387) has no `customerAddress`; the field exists only in the `lead_staff_notify` branch (L549, HTML L674, text L748). Add it to the payload + both templates. |
| `9aab7550` | Brandon | 07-21 | **S** | Highlight unchecked new messages more clearly | Partial: a gold `.unread-dot` renders (CSS L334, markup L1095) but only when `last_direction='inbound' AND status='open'` (L1089), and there is no row-level highlight. Add a bolder treatment + widen the condition so a reopened thread also flags. |
| `21fc3a9d` | Brandon | 07-21 | **S** | Rescale the conversation-list text | No user text-size control on messages.html v1.17 (no font-scale / fontScale / textSize / zoom). Add a persisted font-scale control. |
| `f1e6e311` | Brandon | 07-21 | **S** | Clear-name button in the messaging search bar | `convoSearch` (L469) is a plain input, reset only programmatically (L1001, L1020). Add a clear (x) that empties it and re-runs the filter (read L1059, handler L2144). |
| `6e9ca4dc` | Brandon | 07-21 | **S** | Drop the redundant "Added to every send" line | js/messaging.js writes it into `#msgSigPreview` (L222, L246); messages.html hides it **only** in the `max-width:760px` query (L430), so desktop + the RO-card composer still show it. Remove or collapse it in all viewports - the signature keeps sending. |

### Parts & Inventory (3)

| ER | By | Date | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|---|
| `14f35f95` | Lynn | 04-09 | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | Zero hits for inventory / on_hand / in_stock / stock_qty across js, migrations and functions; parts remain per-RO line items (js/parts.js). New subsystem: inventory table, receive/consume/adjust, searchable priced stock, links from the parts-request path. Largest item on the board. |
| `b19975d4` | Lynn | 04-09 | **L** | Part returned to / warrantied by the supplier | Partial infra only: `PART_STATUSES` includes 'Returned' (js/config.js L200) and a Return Deadline input exists (js/parts.js L226, read L343), but no RMA number, return reason, supplier credit or reverse-direction flow. = GH#24 Shop Ops Phase 2. Canonical for declined dup `86bbaf50`. |
| `8d885331` | Lynn | 04-09 | **S** | Rework the parts-entry field order | Form unchanged (pf_supplier L181, pf_partName L190, pf_qty L200, pf_status L204, pf_serviceSilo L208, pf_returnDeadline L226). Quick once Lynn supplies the target layout. **Blocked on detail, not effort.** |

### Board & Navigation UX (3 - all S)

| ER | By | Date | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|---|
| `29654af7` | Lynn | 07-06 | **S** | Button back to the top / to the search bar | No backToTop / back-to-top / `window.scrollTo` in index.html, js or css - only targeted `scrollIntoView`. Add a scroll-triggered control, present in the sidebar layout too. Canonical for dup `2b78696c`. |
| `d1844e46` | Lynn | 07-16 | **S** | Sort by promised date then oldest on lot, shop ops pinned | `renderBoard` sorts on the single blended `calculatePriority` (L31-39) with the shop pin at L35-37; no sortBy / sortSelect / sortMode exists. Add the strict sort option, keep the pin. `1aeb3f58` delivered only the promised-date chip. |
| `d432863a` | Brandon | 07-21 | **S** | Patriots logo as the browser tab icon | No favicon / `rel=icon` / apple-touch-icon in any live page. Add both to every page head (logo.png ships already but needs resizing to icon dimensions). |

### RO fields & status (3)

| ER | By | Date | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|---|
| `5fbfa299` | Brandon | 06-30 | **M** | Archive quoted-but-never-progressed customers/units | The only archive path is the cashiered one (js/ro-crud.js `archiveROInSupabase` L658, `archived_at` L692 -> `cashiered_child_archives`); no archived/abandoned flag, status or board filter. Needs a soft-archive flag + board filter + un-archive. The S161 lead-capture path keeps *future* quotes on the Messages board rather than as ROs, but existing quote-only ROs still need this. Pairs with `972af03c`. |
| `972af03c` | Lynn | 07-17 | **S** | Add a Canceled status; keep the record for notes | No Canceled/Cancelled status - `STATUS_PROGRESS_MAP` (L151-164) and `ALL_STATUSES` (L741-745) both end at Delivered/Cashed Out. Add the status + color/priority maps and exclude it from the on-lot and queue counts. |
| `50175fce` | Lynn | 07-27 | **S** | Move the urgent-update *input* onto the tile | `urgent_update` is writable only from New/Edit RO (index.html textarea L6796, `editUrgentUpdate` L7309 read L6263; js/ro-crud.js L414/L467/L491); the card banner is read-only and js/render.js L324 even titles it "change it via Edit RO". Add inline edit-in-place reusing the existing editField / notify path so it still fires `urgent_update_notify`. Follow-on to delivered `a7d1474e`. |

### Notifications & Task-Tracking (2)

| ER | By | Date | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|---|
| `5925fc01` | Lynn | 04-09 | **L** | Notify the right team on every RO update; daily list of in-progress jobs with to-dos scraped from notes | **Partial.** The daily-digest half is served by supabase/functions/send-manager-report, and single-event notifies exist (`service_added_notify` js/work-orders.js L293, `urgent_update_notify` js/ro-crud.js L370). Still absent: notify-on-every-update + to-do scraping. Anchor of this theme. |
| `af14b3cb` | Lynn | 05-21 | **L** | Per-customer task tracker + daily reminder until complete | js/shop-tasks.js is a non-customer shop checklist with no `ro_id` link and no reminder column; no ro_tasks / customer_task / task_tracker anywhere. Needs a per-RO tasks table, add/complete UI, daily open-task reminder. Pairs with `5925fc01`. |

### Time clock (2 - both S, both underspecified)

| ER | By | Date | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|---|
| `e06d747e` | Riley Scott | 04-10 | **S** | Tech check-in "float" for ease of access | No floating/sticky check-in control in index.html or checkin.html; js/render.js `compact-col-actions` (L228-235) offers edit / parts / WO / notify / message / schedule only. **Needs scoping with the submitter.** Pairs with `6bb61db2`. |
| `6bb61db2` | Riley Scott | 07-22 | **S** | Check-in/out button on the compact view + a live "what am I clocked into" indicator at login | Same missing compact action, and no persistent "clocked into" / active `time_log` indicator anywhere in index.html or js. Add the compact action plus a login-time active-time_log banner across views. |

### Auth & cost (1)

| ER | By | Date | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | Still Google-only: js/auth.js imports `GOOGLE_CONFIG` (L69), inits gapi (L203-204), signs in via One Tap + `signInWithIdToken` (L449); the check-in kiosk shares the path. Swapping to Supabase password/magic-link or shared role accounts touches every page sign-in **plus** the email-keyed RBAC. Roland wants to scope this together. |

### Work Orders & Shop tasks (1)

| ER | By | Date | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|---|
| `2f752d40` | Jason Rubin | 07-21 | **S** | Auto-populate WO dollar value from total hours x $195/hr | `woFormDollar` (L360) and `woFormEstHours` (L365) are independent manual inputs read separately on save (L649-650); no labor-rate arithmetic in the module. Compute `dollar_value` from the task `est_hours` rollup (else the WO-level figure) x $195, editable, respecting the existing `dollarLocked` readonly gate (L324). |

### Data integrity & merge (1 - new theme this pass)

| ER | By | Date | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|---|
| `0e172b2c` | Sofia | 07-16 | **M** | Merge customer accounts created under different spellings | Partial: js/duplicates.js has an RO-level merge/delete engine (`getBaseROId` L35, `findDuplicateGroups` L40, `openDuplicateManager` L51, `executeDupeMerge`) but groups strictly on the `PRVS-XXXX-XXXX` base RO id (L36, L44), so two ROs under different customer spellings are never detected. Needs name/phone account dedup + a verify-which-is-correct merge step. One-off cleanup for this case = `3f254929`. |

---

## Done + Duplicate - the closed 60 (52 done, 8 duplicate)

All re-verified this pass against v1.484 / messages v1.17.

| ER | By | Date | Category | Bucket | Mapped code (evidence) |
|---|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | General UI/UX | done | Test entry - no code mapping. Already declined; keep declined. |
| `79b2c805` | Sofia | 04-09 | New Feature Idea | done | closed-ros.html `reactivateRO` L838 (buttons L725/L813); js/photos.js `uploadDocument` L253 (input L207) |
| `75124b03` | Ryan | 04-09 | RO Management | done | v1.402 - js/ro-crud.js L1119 warranty branch; js/render.js filter L121, chip L195, badge L372 |
| `b2aa4bea` | Lynn | 04-09 | New Feature Idea | done | `planned_dropoff_date` js/ro-crud.js L409/L486/L508; Drop chip js/render.js L395; key_dates_reminder_sources.sql |
| `18611eee` | Lynn | 04-09 | Bug Report | done | Was real, fixed - js/work-list.js `removeFromWorkList` L195, silo tabs L223/L263 |
| `17492066` | Lynn | 04-09 | Notifications / Email | done | scheduled_notifications.sql + process-scheduled-notifications cron; repeat via index.html `snRepeat` L2876 |
| `fd6c122d` | Riley | 04-09 | Work List | done | v1.460 - delete L195, drag L304, touch L356+, `_saveWorkListOrder` L210/L383 |
| `aa508852` | Riley | 04-09 | Work List | duplicate | Identical text + timestamp to `fd6c122d`. Already declined. |
| `98bd7498` | Brandon | 04-09 | RO Management | done | Per-silo Work List tabs js/work-list.js L223/L263, first render L83 |
| `19dc2016` | Roland | 04-11 | Time Clock / Check-In | done | Test entry - no code mapping. Already declined; keep declined. |
| `1fe68261` | Rod Wombles | 04-13 | Work Orders | done | v1.467 - js/shop-tasks.js L21/L41/L153/L180 + shop_tasks.sql |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | done | v1.410 - 'Ready to Work' js/config.js L158 (slot 50), js/render.js ALL_STATUSES L741-745 |
| `b87eb2fb` | Lynn | 04-14 | RO Management | done | v1.458 - keys_power_fields.sql; js/ro-crud.js L411-413 create, L488-490 edit, L1102 repopulate |
| `a5ff3e2d` | Lynn | 04-21 | RO Management | done | v1.465 - `warranty_repair` js/ro-crud.js L1119; js/render.js L122/L196/L373; still billable |
| `a7d1474e` | Lynn | 04-23 | RO Management | done | v1.466 - urgent_update_field.sql; banner js/render.js L324, css L3446; notify js/ro-crud.js L370. Input-on-tile follow-up = open `50175fce` |
| `dde90d2b` | Roland | 04-27 | RO Management | done | Photo optional + both emails forced - customer-checkin.html `raf_copy` L2467, `lead_staff_notify` L2540 |
| `13e1cd87` | Tipton Scott | 04-30 | Time Clock / Check-In | done | v1.447 / checkin v1.34 - "Return to Dashboard" checkin.html L1516 (ES L441) |
| `e27311bf` | Ryan | 05-04 | Work Orders | done | v1.464 - js/work-orders.js `_initWOTaskDrag` L461 (L385, L583); DOM order drives sort_order |
| `d2561e11` | Brandon | 05-06 | New Feature Idea | done | v1.457 - key_dates_pickup_date.sql; Pickup chip js/render.js L397, panel L365 |
| `6938c1af` | Ryan | 05-06 | General UI/UX | data | Applied live; residual seed staff_table.sql L85-87 - SQL above |
| `dac9fdda` | Brandon | 05-06 | General UI/UX | done | v1.460 - 'Off Lot - Returning' js/config.js L154, dropdowns js/render.js L253/L451, excluded from onLotCount L718-721 |
| `86bbaf50` | Lynn | 05-13 | RO Management | duplicate | Supplier return/warranty = `b19975d4`. Already declined. |
| `a8e90a7d` | Brandon | 05-20 | General UI/UX | done | v1.456 - js/parts.js `totFreight` L153, `totCost` L155, rendered L160-161 |
| `1aeb3f58` | Lynn | 05-21 | General UI/UX | done | v1.457 - promised chip js/render.js L396 (red when overdue); promise + days-on-lot feed `calculatePriority` L33. Strict sort = open `d1844e46` |
| `cbc70a86` | Lynn | 05-21 | General UI/UX | done | S128 - customer-checkin.html L2319 `rpc('add_ro_to_silo_work_lists')` + work_list_autoadd_rpc.sql |
| `3f415c88` | Lynn | 05-26 | General UI/UX | done | v1.463 - `snRepeat` index.html L2876 (read L2944) -> one cancellable scheduled_notifications row per occurrence |
| `cd663d8f` | Bobby Thatcher | 05-28 | General UI/UX | done | v1.447 cosmetic fix - js/ro-crud.js L1093 `toFixed(2)`; js/parts.js MONEY_FIELDS L307/L310 |
| `34fc03c2` | Brandon | 05-29 | New Feature Idea | done | v1.458 - Keys selector on New + Edit RO (js/ro-crud.js L411-413/L488-490/L1102) + keys chip |
| `0b2e128c` | Lynn | 06-02 | General UI/UX | done | v1.460 - checkin.html clockOut writes a TECH NOTES `ro_status` row L1455 |
| `468aa376` | Lynn | 06-02 | General UI/UX | done | Planned drop-off on New + Edit RO (js/ro-crud.js L409/L486) -> Drop chip L395 + auto_dropoff_reminder |
| `2b814250` | Roland | 06-07 | Calendar | done | Key-date chips js/render.js L395-397 + panel L367 (css L3438-3441); sync-ro-calendar server-side, idempotent via `cal_event_ids`; advance reminders via scheduled_notifications |
| `cd084233` | Roland | 06-07 | Work List | done | send-manager-report + send-admin-pnl-report + worklist-report.html. Manager *scoring* layer = future extension, not this ER |
| `a22739b9` | Sofia | 06-08 | General UI/UX | done | Notify-gate regression fixed - both sends forced (customer-checkin.html L2467, L2540) |
| `67daba15` | Brandon | 06-09 | General UI/UX | duplicate | Completion / P-U date = `d2561e11`. Already declined. |
| `fa1c1d6e` | Sofia | 06-09 | General UI/UX | duplicate | Sheldon Gilbert no-email report = `a22739b9`. Already declined. |
| `91a03260` | Roland | 06-09 | Work List | done | prune_cashiered_worklists.sql - removes the day *after* Delivered, Chicago-local gate |
| `5057c7fc` | Roland | 06-09 | Work List | duplicate | Same as `91a03260`. Already declined. |
| `cdd77a8b` | Brandon | 06-10 | General UI/UX | done | v1.453/4 - `nonShop` js/render.js L656-658; `onLotCount` L718-721 excludes Not On Lot / Scheduled / Off Lot - Returning |
| `2c3d5633` | Ryan | 06-12 | General UI/UX | done | v1.453 - js/photos.js L66 `document.body.appendChild(input)` fixed the iOS dropped-change-event |
| `74a33621` | Brandon | 06-16 | General UI/UX | done | v1.454 - lightbox arrow keys js/photos.js L396-397 -> `navigateLightbox` L404; buttons L380/L382 |
| `298e0e67` | Sofia | 06-16 | General UI/UX | done | Dup-RO guard - customer-checkin.html `reusedScheduledRO` L2047/L2136, branch L2194 |
| `9b823d25` | Roland | 06-16 | RO Management | done | v1.460 - `addServiceToRO` L244 queues `service_added_notify` L293 + service_added_notify_source.sql |
| `fc6b29f9` | Sofia | 06-17 | General UI/UX | duplicate | Retest of `298e0e67`. Already declined. |
| `9b046808` | Brandon | 06-17 | General UI/UX | done | v1.454 - 'Scheduled' js/config.js L157; excluded from days-on-lot (L674-676) and on-lot count (L718-721) |
| `671fa623` | Brandon | 06-18 | General UI/UX | done | v1.456 - freight rolled into Total Cost (js/parts.js L153/L155/L160-161) |
| `5c45bb8d` | Sofia | 06-19 | General UI/UX | done | David Flanagan report - RO reuse (L2047/L2136) + forced `raf_copy` (L2467). Both halves resolved |
| `b06a285f` | Lynn | 06-19 | General UI/UX | done | v1.465 - key-dates panel js/render.js L363-367, css L3438-3441; chips shipped v1.457 |
| `ee50d11c` | Lynn | 06-19 | General UI/UX | done | time-off.html per-row Edit L752 -> `editRequest()` -> "Update Record" |
| `88395acc` | Lynn | 06-19 | General UI/UX | done | Time Off v1.5 - add_absence_window_to_time_off.sql + add_partial_hours_to_time_off.sql; `modalStartTime` L368 / `modalEndTime` L372 |
| `80390f36` | Lynn | 06-23 | General UI/UX | done | v1.462 - shop pin js/render.js L35-37 + 'shop operations shop' search token L61 |
| `7f2d0d70` | Lynn | 06-26 | General UI/UX | done | supabase/functions/send-dropoff-report - Monday roster + `manager_work_lists` reconciliation |
| `3f7fb4fc` | Brandon | 06-29 | General UI/UX | done | v1.468 - shop ROs out of queue (L656-658) + on-lot (L718-720); `cep-static-status` L249 replaces the dropdown |
| `cb7742a8` | Lynn | 06-29 | General UI/UX | done | v1.469 / check-in v1.15 - sync-ro-calendar owns team-calendar events; customer-checkin.html POST L2182 |
| `5fbfa299` | Brandon | 06-30 | General UI/UX | needed/M | (see Needed - RO fields & status) |
| `39b75fab` | Sofia | 07-09 | General UI/UX | done | v1.472 - js/photos.js `canManageFiles` rename/move/delete (L198, lightbox L376), office-staff gated |
| `1c7f1410` | Andrew Page | 07-16 | New Feature Idea | done | js/messaging.js `#msgAttach` multiple L219, cap 5 L207, chips L256-264. **Recommend `status=done`** |
| `9d3806e7` | Brandon | 07-22 | General UI/UX | done | messages.html `detailNoteView` L530/L528, `openNotesModal` L1360, customer_note_entries L1326. **Recommend `status=done`** |
| `ac394468` | Roland | 07-24 | General UI/UX | done | v1.482 - erCategory covers Messages/Texting + every page via js/enhancement.js `pageDefaults` L43-55. **Residual:** erCategory exists in only 6 live pages, so messages / leads / time-off / guide cannot *raise* an ER - worth its own small ER |
| `d62a26e4` | Roland | 07-24 | New Feature Idea | done | v1.482 - multi-attach L219/L202/L207 + chips L256-264; send chunks one media per MMS (carrier payload cap) |
| `93b00023` | Roland | 07-24 | New Feature Idea | done | v1.482 - textly-webhook reopen + 'reopened' event L505; owner notify on the needs-reply flip; send-unreplied-reminder + unreplied_eod_reminder_s158.sql cron |
| `1bc33dd6` | Brandon | 07-27 | General UI/UX | duplicate | Re-file of `d4d0eeba`. **Recommend `status=declined`** |
| `2b78696c` | Lynn | 07-27 | General UI/UX | duplicate | Re-file of `29654af7`. **Recommend `status=declined`** |
| `f73a1e5a` | Roland | 04-13 | General UI/UX | *(carried over)* | **Not in `needs_triage_ids`** - not re-classified this pass. Dictation test entry, already `declined`; its `triage_bucket` is still null and its admin_notes carry a mis-filed "[Triage 2026-06-26 S125] WO task drag-reorder" line. No work item either way. |

---

## Recommended action today

1. **Ship the bug class first - two changes cover all three bugs.**
   (a) `d4d0eeba` + `9031e101`: make `scrollToROInBoard` (index.html L4094) and
   `handleDeepLink` (js/qr.js L198) clear filters, re-render, *then* scroll and
   highlight. (b) `d94a418a`: stop reversing note lines in js/render.js L553
   **and L563** - reuse the S159 entry-regex split. Both are inside the
   Phase-2b auto-fix rails (data-display / small, no auth, money, deletes, DB
   or notifications).
2. **Close 4 ERs with a status flip** (no code): `1c7f1410` -> done,
   `9d3806e7` -> done, `1bc33dd6` -> declined, `2b78696c` -> declined. That
   takes `unreviewed` from 30 to 26.
3. **Run the two Data SQL statements** above at your convenience -
   `3f254929` (delete the misspelled Hubert RO) is the only one that matters
   operationally; `6938c1af` is a seed-file residual only.
4. **Then sweep the S-heavy Messaging cluster.** Five of the six
   Messaging & e-sign items are S and independent of each other
   (`5fec3eca`, `9aab7550`, `21fc3a9d`, `f1e6e311`, `6e9ca4dc`) - one session
   could plausibly land all five on messages.html v1.18 + a send-quote-email
   redeploy.
5. **Unblock the two underspecified S's** by asking the submitters:
   Lynn's parts field order (`8d885331`) and Riley's check-in float
   (`e06d747e` / `6bb61db2`, which should be scoped as one).
6. **Plan, don't rush, the two XLs:** the auth swap (`ed1b873e`, ~$500/mo at
   stake but security-critical) and parts inventory (`14f35f95`) each deserve
   their own scoping session.
