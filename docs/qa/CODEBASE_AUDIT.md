# PRVS Codebase Audit Report

Generated by `scripts/audit_codebase.py` against the repo at /sessions/rcw-01bhs36mvcaxboncr1sce8ne/mnt/rv-dashboard.

## Summary

| Severity | Count |
|---|---|
| BLOCKING | 0 |
| HIGH | 23 |
| MEDIUM | 36 |
| LOW | 42 |

| Bug class | Count |
|---|---|
| A — JS field name not in schema | 30 |
| B — repair_orders.status write without audit log (ghost-write) | 0 |
| C — .single() — review for 0-row tolerance | 12 |
| D — accessToken-only auth guard | 4 |
| E — Non-canonical status string literal | 33 |
| F — Hardcoded role/email constant (post-S2) | 0 |
| G — SQL ON CONFLICT without UNIQUE constraint | 12 |
| H — SQL ::jsonb cast without EXCEPTION wrapper | 3 |
| I — Redundant manual updated_at write (trigger owns it, S115) | 6 |
| K — Retired-rule / protocol drift in docs + skills (S143) | 1 |

## HIGH (23)

### Class A — JS field name not in schema (7)

- **solar.html:2987** — `insert` on `repair_orders` writes unknown column `customer_email` (not in schema)
  ```
  omer_name: customerInfo.name,
                customer_email: customerInfo.email || null,
  ```
- **solar.html:2988** — `insert` on `repair_orders` writes unknown column `customer_phone` (not in schema)
  ```
  : customerInfo.email || null,
                customer_phone: customerInfo.phone || null,
  ```
- **solar.html:2989** — `insert` on `repair_orders` writes unknown column `year` (not in schema)
  ```
  : customerInfo.phone || null,
                year: coachInfo.year || null,
  ```
- **solar.html:2990** — `insert` on `repair_orders` writes unknown column `make` (not in schema)
  ```
  year: coachInfo.year || null,
                make: coachInfo.make || null,
  ```
- **solar.html:2991** — `insert` on `repair_orders` writes unknown column `model` (not in schema)
  ```
  make: coachInfo.make || null,
                model: coachInfo.model || null,
  ```
- **solar.html:3264** — `update` on `repair_orders` writes unknown column `solar_quote_number` (not in schema)
  ```
  solar_quote_number: quoteNumber, solar_quote_sent
  ```
- **supabase/functions/sync-ro-calendar/index.ts:231** — `update` on `repair_orders` writes unknown column `cal_event_ids` (not in schema)
  ```
  cal_event_ids: ids
  ```

### Class D — accessToken-only auth guard (4)

- **index.html:1853** — Auth guard uses `!accessToken` alone — should be `!getSB() || !supabaseSession`
  ```
  if (!accessToken) return;
  ```
- **index.html:1875** — Auth guard uses `!accessToken` alone — should be `!getSB() || !supabaseSession`
  ```
  if (!accessToken) return;
  ```
- **index.html:2288** — Auth guard uses `!accessToken` alone — should be `!getSB() || !supabaseSession`
  ```
  if (!accessToken) throw new Error('Not connected to Google Drive');
  ```
- **index.html:3108** — Auth guard uses `!accessToken` alone — should be `!getSB() || !supabaseSession`
  ```
  if (!accessToken) return;
  ```

### Class G — SQL ON CONFLICT without UNIQUE constraint (12)

- **supabase/migrations/BRANDON_INSURANCE_WO_WRITER.sql:19** — ON CONFLICT (name) but no UNIQUE constraint on `name` defined in this file — verify external constraint exists
  ```
  surance WO Writer') ON CONFLICT (name) DO NOTHING;  -- 1b.
  ```
- **supabase/migrations/app_config_table.sql:29** — ON CONFLICT (key) but no UNIQUE constraint on `key` defined in this file — verify external constraint exists
  ```
  assis Calendar ID') ON CONFLICT (key) DO UPDATE SET value
  ```
- **supabase/migrations/cashiered_child_archives.sql:263** — ON CONFLICT (original_ro_id) but no UNIQUE constraint on `original_ro_id` defined in this file — verify external constraint exists
  ```
  l, now_ts     )     ON CONFLICT (original_ro_id) DO NOTHING     RETU
  ```
- **supabase/migrations/cron_admin_pnl_report.sql:23** — ON CONFLICT (key) but no UNIQUE constraint on `key` defined in this file — verify external constraint exists
  ```
  report recipients') ON CONFLICT (key) DO UPDATE SET value
  ```
- **supabase/migrations/cron_archive_cashiered_ros.sql:107** — ON CONFLICT (original_ro_id) but no UNIQUE constraint on `original_ro_id` defined in this file — verify external constraint exists
  ```
  now_ts     )     ON CONFLICT (original_ro_id) DO NOTHING;      --
  ```
- **supabase/migrations/customer_notes_s154.sql:61** — ON CONFLICT (phone_key) but no UNIQUE constraint on `phone_key` defined in this file — verify external constraint exists
  ```
  der by pk, upd desc on conflict (phone_key) do nothing;  -- 5.
  ```
- **supabase/migrations/kenect_phase4_transform_s142.sql:11** — ON CONFLICT (phone_key) but no UNIQUE constraint on `phone_key` defined in this file — verify external constraint exists
  ```
  onversations upsert ON CONFLICT (phone_key) DO NOTHING; -- mess
  ```
- **supabase/migrations/kenect_phase4_transform_s142.sql:69** — ON CONFLICT (phone_key) but no UNIQUE constraint on `phone_key` defined in this file — verify external constraint exists
  ```
  (kc.phone_key) = 10 on conflict (phone_key) do nothing;  -- ──
  ```
- **supabase/migrations/kenect_phase4_transform_s142.sql:111** — ON CONFLICT (kenect_message_id) but no UNIQUE constraint on `kenect_message_id` defined in this file — verify external constraint exists
  ```
  e_id = m.message_id on conflict (kenect_message_id) do nothing;  -- ──
  ```
- **supabase/migrations/pb_inbox_conversations.sql:167** — ON CONFLICT (phone_key) but no UNIQUE constraint on `phone_key` defined in this file — verify external constraint exists
  ```
  , k.created_at DESC ON CONFLICT (phone_key) DO NOTHING;  -- ──
  ```
- **supabase/migrations/review_requests_s154.sql:99** — ON CONFLICT (key) but no UNIQUE constraint on `key` defined in this file — verify external constraint exists
  ```
  ink} placeholder)') on conflict (key) do nothing;  -- ──
  ```
- **supabase/migrations/staff_table.sql:92** — ON CONFLICT (email) but no UNIQUE constraint on `email` defined in this file — verify external constraint exists
  ```
  ',           NULL)  ON CONFLICT (email) DO UPDATE SET   nam
  ```

## MEDIUM (36)

### Class E — Non-canonical status string literal (33)

- **closed-ros.html:24** — Status string literal `'In Progress'` does not match the 11 canonical values
  ```
  `status: 'In Progress'` (capital P) on the INSERT into
  ```
- **customer-checkin.html:2265** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status:           'pending',
  ```
- **index.html:146** — Status string literal `'outstanding'` does not match the 11 canonical values
  ```
  Post-Phase-2 dead-code cleanup: the legacy parts_status='outstanding' value
  ```
- **index.html:176** — Status string literal `'requested'` does not match the 11 canonical values
  ```
  request now sets parts_status='requested' (was 'outstanding', which was labeled
  ```
- **index.html:624** — Status string literal `'outstanding'` does not match the 11 canonical values
  ```
  v1.284: Parts chip states (GH#14) — three-state parts status system: 🔍 Part Sourcing (neon orange), ⚠️ Parts Outstanding (yellow), ✅ Parts Received (green); parts_status column on repair_orders; openPartsStatusModal for Manager/Admin; auto-flip to Received when all parts marked received; filter buttons for all three states; submitPartsRequest now sets parts_status='outstanding'
  ```
- **index.html:1054** — Status string literal `'Quality check'` does not match the 11 canonical values
  ```
  status: "Quality check",
  ```
- **index.html:2786** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  const cancelBtn = n.status === 'pending' ? `<button onclick="cancelScheduleNotification('${n.id}', ${filteredIndex})" style="padding:3px 8px;border-radius:5px;border:1px solid var(--border-color);background:transparent;color:var(--text-secondary);cursor:pointer;font-size:0.7rem;">Cancel</button>` : '';
  ```
- **index.html:2932** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status:           'pending',
  ```
- **index.html:2979** — Status string literal `'cancelled'` does not match the 11 canonical values
  ```
  .update({ status: 'cancelled' })
  ```
- **supabase/functions/process-review-requests/index.ts:80** — Status string literal `'sent'` does not match the 11 canonical values
  ```
  .update({ status: "sent", sent_at: new Date().toISOString(), error_message: null })
  ```
- **supabase/functions/process-review-requests/index.ts:85** — Status string literal `'skipped'` does not match the 11 canonical values
  ```
  .update({ status: "skipped", error_message: "opted out (STOP)" })
  ```
- **supabase/functions/process-review-requests/index.ts:90** — Status string literal `'failed'` does not match the 11 canonical values
  ```
  .update({ status: "failed", error_message: `textly-send ${resp.status}: ${JSON.stringify(data).slice(0, 300)}` })
  ```
- **supabase/functions/process-review-requests/index.ts:96** — Status string literal `'failed'` does not match the 11 canonical values
  ```
  .update({ status: "failed", error_message: String(e).slice(0, 300) })
  ```
- **supabase/functions/process-scheduled-notifications/index.ts:150** — Status string literal `'failed'` does not match the 11 canonical values
  ```
  status: "failed",
  ```
- **supabase/functions/process-scheduled-notifications/index.ts:155** — Status string literal `'failed'` does not match the 11 canonical values
  ```
  results.push({ id: row.id, status: "failed", reason: "no recipients" });
  ```
- **supabase/functions/process-scheduled-notifications/index.ts:171** — Status string literal `'sent'` does not match the 11 canonical values
  ```
  status: "sent",
  ```
- **supabase/functions/process-scheduled-notifications/index.ts:176** — Status string literal `'sent'` does not match the 11 canonical values
  ```
  results.push({ id: row.id, status: "sent", recipients: recipients.length });
  ```
- **supabase/functions/process-scheduled-notifications/index.ts:200** — Status string literal `'failed'` does not match the 11 canonical values
  ```
  status: "failed",
  ```
- **supabase/functions/process-scheduled-notifications/index.ts:205** — Status string literal `'failed'` does not match the 11 canonical values
  ```
  results.push({ id: row.id, status: "failed", reason: msg });
  ```
- **supabase/functions/projectblue-reconcile/index.ts:201** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status: "pending",
  ```
- **supabase/functions/projectblue-reconcile/index.ts:246** — Status string literal `'queued'` does not match the 11 canonical values
  ```
  if (pbStatus === "delivered" && (match.status === "queued" || match.status === "pending")) {
  ```
- **supabase/functions/projectblue-reconcile/index.ts:246** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  if (pbStatus === "delivered" && (match.status === "queued" || match.status === "pending")) {
  ```
- **supabase/functions/projectblue-reconcile/index.ts:247** — Status string literal `'sent'` does not match the 11 canonical values
  ```
  patch.status = "sent"; counts.status_upgraded++;
  ```
- **supabase/functions/projectblue-reconcile/index.ts:340** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status: "pending",
  ```
- **supabase/functions/projectblue-webhook/index.ts:415** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status: "pending",
  ```
- **supabase/functions/projectblue-webhook/index.ts:495** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status: "pending",
  ```
- **supabase/functions/review-feedback/index.ts:120** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status: "pending",
  ```
- **supabase/functions/send-er-completion/index.ts:137** — Status string literal `'done'` does not match the 11 canonical values
  ```
  if (er.status !== "done") return json({ ok: false, skipped: "status not done" });
  ```
- **supabase/functions/textly-webhook/index.ts:597** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status: "pending",
  ```
- **supabase/functions/textly-webhook/index.ts:677** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status: "pending",
  ```
- **supabase/functions/woosender-intake/index.ts:165** — Status string literal `'new'` does not match the 11 canonical values
  ```
  review_status: "new",
  ```
- **time-off.html:1070** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status:           'pending',
  ```
- **time-off.html:1079** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status:           'pending',
  ```

### Class H — SQL ::jsonb cast without EXCEPTION wrapper (3)

- **supabase/migrations/cron_admin_pnl_report.sql:38** — ::jsonb cast without nearby EXCEPTION handler — empty strings or malformed JSON will abort the transaction
  ```
  body := '{}'::jsonb,
  ```
- **supabase/migrations/cron_archive_cashiered_ros.sql:99** — ::jsonb cast without nearby EXCEPTION handler — empty strings or malformed JSON will abort the transaction
  ```
  THEN ro_row.insurance_data::jsonb
  ```
- **supabase/migrations/review_requests_s154.sql:207** — ::jsonb cast without nearby EXCEPTION handler — empty strings or malformed JSON will abort the transaction
  ```
  body := '{}'::jsonb,
  ```

## LOW (42)

### Class A — JS field name not in schema (23)

- **supabase/functions/process-review-requests/index.ts:79** — Table `review_requests` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/process-review-requests/index.ts:84** — Table `review_requests` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/process-review-requests/index.ts:89** — Table `review_requests` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/process-review-requests/index.ts:95** — Table `review_requests` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/projectblue-reconcile/index.ts:265** — Table `messages` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/projectblue-send/index.ts:235** — Table `messages` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/projectblue-send/index.ts:268** — Table `conversations` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/projectblue-webhook/index.ts:138** — Table `messages` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/projectblue-webhook/index.ts:293** — Table `conversations` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/projectblue-webhook/index.ts:347** — Table `conversation_events` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/projectblue-webhook/index.ts:357** — Table `conversation_events` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/review-feedback/index.ts:76** — Table `review_requests` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/review-feedback/index.ts:87** — Table `review_feedback` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/review-feedback/index.ts:94** — Table `review_requests` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/textly-send/index.ts:211** — Table `messages` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/textly-send/index.ts:244** — Table `conversations` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/textly-webhook/index.ts:215** — Table `messages` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/textly-webhook/index.ts:324** — Table `messages` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/textly-webhook/index.ts:407** — Table `conversations` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/textly-webhook/index.ts:476** — Table `conversation_events` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/textly-webhook/index.ts:486** — Table `conversation_events` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/woosender-intake/index.ts:143** — Table `woosender_leads` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real
- **supabase/functions/woosender-intake/index.ts:160** — Table `woosender_leads` is not in SCHEMA or SCHEMA_PARTIAL — update audit_codebase.py if real

### Class C — .single() — review for 0-row tolerance (12)

- **checkin.html:636** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .single();
  ```
- **checkin.html:1235** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  const { data, error } = await getSB().from('time_logs').insert(logData).select().single();
  ```
- **closed-ros.html:870** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .single();
  ```
- **customer-checkin.html:2133** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .single();
  ```
- **customer-checkin.html:2166** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  }).select().single();
  ```
- **customer-checkin.html:2509** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .single();
  ```
- **index.html:6368** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  }).select().single();
  ```
- **solar.html:108** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .single();
  ```
- **solar.html:139** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .select('value').eq('key', 'session').single();
  ```
- **solar.html:150** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .select('value').eq('key', 'last_quote').single();
  ```
- **solar.html:2992** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  }).select().single();
  ```
- **supabase/functions/woosender-intake/index.ts:169** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .single();
  ```

### Class I — Redundant manual updated_at write (trigger owns it, S115) (6)

- **checkin.html:1347** — `update` on `repair_orders` manually sets `updated_at` — redundant since trg_set_updated_at (S115) maintains it; remove for clarity
  ```
  pct_complete: 60,
                updated_at: now,
  ```
- **index.html:1476** — `update` on `repair_orders` manually sets `updated_at` — redundant since trg_set_updated_at (S115) maintains it; remove for clarity
  ```
  photo_library: libraryObj,
                updated_at: new Date().toISOString(),
  ```
- **index.html:2655** — `update` on `repair_orders` manually sets `updated_at` — redundant since trg_set_updated_at (S115) maintains it; remove for clarity
  ```
  photo_url: newUrl,
                            updated_at: new Date().toISOString()
  ```
- **index.html:4991** — `update` on `repair_orders` manually sets `updated_at` — redundant since trg_set_updated_at (S115) maintains it; remove for clarity
  ```
  photo_library:  libraryObj,
                updated_at:     new Date().toISOString(),
  ```
- **index.html:5015** — `upsert` on `config` manually sets `updated_at` — redundant since trg_set_updated_at (S115) maintains it; remove for clarity
  ```
  value: customInsuranceFields,
                    updated_at: new Date().toISOString(),
  ```
- **index.html:6360** — `update` on `wo_task_templates` manually sets `updated_at` — redundant since trg_set_updated_at (S115) maintains it; remove for clarity
  ```
  updated_at: new Date().toISOString()
  ```

### Class K — Retired-rule / protocol drift in docs + skills (S143) (1)

- **(skills):0** — [K0] Could not locate the prvs-* skills at /sessions/*/mnt/.claude/skills/ -- Class K did NOT check them. Skills are the most drift-prone copy; verify manually.

## DB Invariants (Class J — run manually)

These cannot be verified by a static file scan. Run each against the live DB via the read-only Supabase MCP or the SQL editor. NOTE: query the catalog (`pg_trigger` etc.) directly — `information_schema.triggers` is privilege-filtered and returns empty for objects the read-only MCP role does not own.

### Every table with an updated_at column has a maintaining BEFORE UPDATE trigger

_Expected: Zero rows. Any row is a table whose updated_at can silently go stale._

```sql
SELECT c.relname AS table_missing_updated_at_trigger
FROM pg_class c
JOIN pg_namespace n ON n.oid = c.relnamespace
WHERE n.nspname = 'public' AND c.relkind = 'r'
  AND EXISTS (SELECT 1 FROM pg_attribute a
              WHERE a.attrelid = c.oid AND a.attname = 'updated_at'
                AND NOT a.attisdropped)
  AND NOT EXISTS (SELECT 1 FROM pg_trigger t
                  JOIN pg_proc p ON p.oid = t.tgfoid
                  WHERE t.tgrelid = c.oid AND NOT t.tgisinternal
                    AND p.proname = 'set_updated_at')
ORDER BY 1;
```

