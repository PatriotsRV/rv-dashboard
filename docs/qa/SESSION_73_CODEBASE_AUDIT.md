# PRVS Codebase Audit Report

Generated by `scripts/audit_codebase.py` against the repo at /sessions/vigilant-modest-cray/mnt/rv-dashboard.

## Summary

| Severity | Count |
|---|---|
| BLOCKING | 0 |
| HIGH | 17 |
| MEDIUM | 49 |
| LOW | 13 |

| Bug class | Count |
|---|---|
| A — JS field name not in schema | 6 |
| B — repair_orders.status write without audit log (ghost-write) | 0 |
| C — .single() — review for 0-row tolerance | 13 |
| D — accessToken-only auth guard | 6 |
| E — Non-canonical status string literal | 48 |
| F — Hardcoded role/email constant (post-S2) | 0 |
| G — SQL ON CONFLICT without UNIQUE constraint | 5 |
| H — SQL ::jsonb cast without EXCEPTION wrapper | 1 |

## HIGH (17)

### Class A — JS field name not in schema (6)

- **solar.html:2987** — `insert` on `repair_orders` writes unknown column `customer_email` (not in schema)
  ```
  omer_name: customerInfo.name,
                customer_email: customerInfo.email || null,
  ```
- **solar.html:2988** — `insert` on `repair_orders` writes unknown column `customer_phone` (not in schema)
  ```
  : customerInfo.email || null,
                customer_phone: customerInfo.phone || null,
  ```
- **solar.html:2989** — `insert` on `repair_orders` writes unknown column `year` (not in schema)
  ```
  : customerInfo.phone || null,
                year: coachInfo.year || null,
  ```
- **solar.html:2990** — `insert` on `repair_orders` writes unknown column `make` (not in schema)
  ```
  year: coachInfo.year || null,
                make: coachInfo.make || null,
  ```
- **solar.html:2991** — `insert` on `repair_orders` writes unknown column `model` (not in schema)
  ```
  make: coachInfo.make || null,
                model: coachInfo.model || null,
  ```
- **solar.html:3264** — `update` on `repair_orders` writes unknown column `solar_quote_number` (not in schema)
  ```
  solar_quote_number: quoteNumber, solar_quote_sent
  ```

### Class D — accessToken-only auth guard (6)

- **index.html:5333** — Auth guard uses `!accessToken` alone — should be `!getSB() || !supabaseSession`
  ```
  if (!accessToken) return;
  ```
- **index.html:5355** — Auth guard uses `!accessToken` alone — should be `!getSB() || !supabaseSession`
  ```
  if (!accessToken) return;
  ```
- **index.html:5874** — Auth guard uses `!accessToken` alone — should be `!getSB() || !supabaseSession`
  ```
  if (!accessToken) throw new Error('Not connected to Google Drive');
  ```
- **index.html:6893** — Auth guard uses `!accessToken` alone — should be `!getSB() || !supabaseSession`
  ```
  if (!calId || !accessToken) { failed++; continue; }
  ```
- **index.html:7299** — Auth guard uses `!accessToken` alone — should be `!getSB() || !supabaseSession`
  ```
  if (!accessToken) return;
  ```
- **index.html:10515** — Auth guard uses `!accessToken` alone — should be `!getSB() || !supabaseSession`
  ```
  if (!accessToken) {
  ```

### Class G — SQL ON CONFLICT without UNIQUE constraint (5)

- **supabase/migrations/BRANDON_INSURANCE_WO_WRITER.sql:19** — ON CONFLICT (name) but no UNIQUE constraint on `name` defined in this file — verify external constraint exists
  ```
  surance WO Writer') ON CONFLICT (name) DO NOTHING;  -- 1b.
  ```
- **supabase/migrations/app_config_table.sql:29** — ON CONFLICT (key) but no UNIQUE constraint on `key` defined in this file — verify external constraint exists
  ```
  assis Calendar ID') ON CONFLICT (key) DO UPDATE SET value
  ```
- **supabase/migrations/cashiered_child_archives.sql:263** — ON CONFLICT (original_ro_id) but no UNIQUE constraint on `original_ro_id` defined in this file — verify external constraint exists
  ```
  l, now_ts     )     ON CONFLICT (original_ro_id) DO NOTHING     RETU
  ```
- **supabase/migrations/cron_archive_cashiered_ros.sql:107** — ON CONFLICT (original_ro_id) but no UNIQUE constraint on `original_ro_id` defined in this file — verify external constraint exists
  ```
  now_ts     )     ON CONFLICT (original_ro_id) DO NOTHING;      --
  ```
- **supabase/migrations/staff_table.sql:92** — ON CONFLICT (email) but no UNIQUE constraint on `email` defined in this file — verify external constraint exists
  ```
  ',           NULL)  ON CONFLICT (email) DO UPDATE SET   nam
  ```

## MEDIUM (49)

### Class E — Non-canonical status string literal (48)

- **closed-ros.html:24** — Status string literal `'In Progress'` does not match the 11 canonical values
  ```
  `status: 'In Progress'` (capital P) on the INSERT into
  ```
- **customer-checkin.html:2061** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status:           'pending',
  ```
- **index.html:49** — Status string literal `'outstanding'` does not match the 11 canonical values
  ```
  v1.284: Parts chip states (GH#14) — three-state parts status system: 🔍 Part Sourcing (neon orange), ⚠️ Parts Outstanding (yellow), ✅ Parts Received (green); parts_status column on repair_orders; openPartsStatusModal for Manager/Admin; auto-flip to Received when all parts marked received; filter buttons for all three states; submitPartsRequest now sets parts_status='outstanding'
  ```
- **index.html:3783** — Status string literal `'Quality check'` does not match the 11 canonical values
  ```
  status: "Quality check",
  ```
- **index.html:4445** — Status string literal `' + error.message, '` does not match the 11 canonical values
  ```
  showToast('Error updating status: ' + error.message, 'error');
  ```
- **index.html:7025** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  const cancelBtn = n.status === 'pending' ? `<button onclick="cancelScheduleNotification('${n.id}', ${filteredIndex})" style="padding:3px 8px;border-radius:5px;border:1px solid var(--border-color);background:transparent;color:var(--text-secondary);cursor:pointer;font-size:0.7rem;">Cancel</button>` : '';
  ```
- **index.html:7135** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status:           'pending',
  ```
- **index.html:7174** — Status string literal `'cancelled'` does not match the 11 canonical values
  ```
  .update({ status: 'cancelled' })
  ```
- **index.html:7473** — Status string literal `'Received'` does not match the 11 canonical values
  ```
  ${p.status !== 'Received' && p.status !== 'Installed'
  ```
- **index.html:7473** — Status string literal `'Installed'` does not match the 11 canonical values
  ```
  ${p.status !== 'Received' && p.status !== 'Installed'
  ```
- **index.html:7730** — Status string literal `'Received'` does not match the 11 canonical values
  ```
  const allDone = allParts.length > 0 && allParts.every(p => p.status === 'Received' || p.status === 'Installed');
  ```
- **index.html:7730** — Status string literal `'Installed'` does not match the 11 canonical values
  ```
  const allDone = allParts.length > 0 && allParts.every(p => p.status === 'Received' || p.status === 'Installed');
  ```
- **index.html:7784** — Status string literal `'Received'` does not match the 11 canonical values
  ```
  part.status = 'Received';
  ```
- **index.html:7801** — Status string literal `'Received'` does not match the 11 canonical values
  ```
  const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed');
  ```
- **index.html:7801** — Status string literal `'Installed'` does not match the 11 canonical values
  ```
  const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed');
  ```
- **index.html:9057** — Status string literal `'Backordered'` does not match the 11 canonical values
  ```
  return parts.some(p => p.status === 'Backordered' || p.status === 'Lost');
  ```
- **index.html:9057** — Status string literal `'Lost'` does not match the 11 canonical values
  ```
  return parts.some(p => p.status === 'Backordered' || p.status === 'Lost');
  ```
- **index.html:9260** — Status string literal `'Backordered'` does not match the 11 canonical values
  ```
  ${ro.partsJson ? (() => { try { const parts = JSON.parse(ro.partsJson); if (!parts.length) return ''; const hasBackordered = parts.some(p => p.status === 'Backordered' || p.status === 'Lost'); const hasSourcing = parts.some(p => p.status === 'Sourcing'); const hasOutstanding = parts.some(p => p.status === 'Ordered' || p.status === 'In Transit'); const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed' || p.status === 'Returned'); const color = hasBackordered ? 'red' : hasOutstanding ? 'yellow' : hasSourcing ? 'yellow' : 'green'; const worstLabel = hasBackordered ? (parts.filter(p => p.status==='Backordered'||p.status==='Lost').length + ' ' + t('Backordered')) : hasOutstanding ? (parts.filter(p => p.status==='Ordered'||p.status==='In Transit').length + ' ' + t('Outstanding')) : hasSourcing ? (parts.filter(p => p.status==='Sourcing').length + ' ' + t('Requested')) : t('All Received'); return '<div class="parts-badge ' + color + '" data-action="parts-badge" data-idx="' + index + '">🔩 ' + parts.length + ' Parts • ' + worstLabel + '</div>'; } catch(e) { return ''; } })() : ''}
  ```
- **index.html:9260** — Status string literal `'Lost'` does not match the 11 canonical values
  ```
  ${ro.partsJson ? (() => { try { const parts = JSON.parse(ro.partsJson); if (!parts.length) return ''; const hasBackordered = parts.some(p => p.status === 'Backordered' || p.status === 'Lost'); const hasSourcing = parts.some(p => p.status === 'Sourcing'); const hasOutstanding = parts.some(p => p.status === 'Ordered' || p.status === 'In Transit'); const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed' || p.status === 'Returned'); const color = hasBackordered ? 'red' : hasOutstanding ? 'yellow' : hasSourcing ? 'yellow' : 'green'; const worstLabel = hasBackordered ? (parts.filter(p => p.status==='Backordered'||p.status==='Lost').length + ' ' + t('Backordered')) : hasOutstanding ? (parts.filter(p => p.status==='Ordered'||p.status==='In Transit').length + ' ' + t('Outstanding')) : hasSourcing ? (parts.filter(p => p.status==='Sourcing').length + ' ' + t('Requested')) : t('All Received'); return '<div class="parts-badge ' + color + '" data-action="parts-badge" data-idx="' + index + '">🔩 ' + parts.length + ' Parts • ' + worstLabel + '</div>'; } catch(e) { return ''; } })() : ''}
  ```
- **index.html:9260** — Status string literal `'Sourcing'` does not match the 11 canonical values
  ```
  ${ro.partsJson ? (() => { try { const parts = JSON.parse(ro.partsJson); if (!parts.length) return ''; const hasBackordered = parts.some(p => p.status === 'Backordered' || p.status === 'Lost'); const hasSourcing = parts.some(p => p.status === 'Sourcing'); const hasOutstanding = parts.some(p => p.status === 'Ordered' || p.status === 'In Transit'); const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed' || p.status === 'Returned'); const color = hasBackordered ? 'red' : hasOutstanding ? 'yellow' : hasSourcing ? 'yellow' : 'green'; const worstLabel = hasBackordered ? (parts.filter(p => p.status==='Backordered'||p.status==='Lost').length + ' ' + t('Backordered')) : hasOutstanding ? (parts.filter(p => p.status==='Ordered'||p.status==='In Transit').length + ' ' + t('Outstanding')) : hasSourcing ? (parts.filter(p => p.status==='Sourcing').length + ' ' + t('Requested')) : t('All Received'); return '<div class="parts-badge ' + color + '" data-action="parts-badge" data-idx="' + index + '">🔩 ' + parts.length + ' Parts • ' + worstLabel + '</div>'; } catch(e) { return ''; } })() : ''}
  ```
- **index.html:9260** — Status string literal `'Ordered'` does not match the 11 canonical values
  ```
  ${ro.partsJson ? (() => { try { const parts = JSON.parse(ro.partsJson); if (!parts.length) return ''; const hasBackordered = parts.some(p => p.status === 'Backordered' || p.status === 'Lost'); const hasSourcing = parts.some(p => p.status === 'Sourcing'); const hasOutstanding = parts.some(p => p.status === 'Ordered' || p.status === 'In Transit'); const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed' || p.status === 'Returned'); const color = hasBackordered ? 'red' : hasOutstanding ? 'yellow' : hasSourcing ? 'yellow' : 'green'; const worstLabel = hasBackordered ? (parts.filter(p => p.status==='Backordered'||p.status==='Lost').length + ' ' + t('Backordered')) : hasOutstanding ? (parts.filter(p => p.status==='Ordered'||p.status==='In Transit').length + ' ' + t('Outstanding')) : hasSourcing ? (parts.filter(p => p.status==='Sourcing').length + ' ' + t('Requested')) : t('All Received'); return '<div class="parts-badge ' + color + '" data-action="parts-badge" data-idx="' + index + '">🔩 ' + parts.length + ' Parts • ' + worstLabel + '</div>'; } catch(e) { return ''; } })() : ''}
  ```
- **index.html:9260** — Status string literal `'In Transit'` does not match the 11 canonical values
  ```
  ${ro.partsJson ? (() => { try { const parts = JSON.parse(ro.partsJson); if (!parts.length) return ''; const hasBackordered = parts.some(p => p.status === 'Backordered' || p.status === 'Lost'); const hasSourcing = parts.some(p => p.status === 'Sourcing'); const hasOutstanding = parts.some(p => p.status === 'Ordered' || p.status === 'In Transit'); const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed' || p.status === 'Returned'); const color = hasBackordered ? 'red' : hasOutstanding ? 'yellow' : hasSourcing ? 'yellow' : 'green'; const worstLabel = hasBackordered ? (parts.filter(p => p.status==='Backordered'||p.status==='Lost').length + ' ' + t('Backordered')) : hasOutstanding ? (parts.filter(p => p.status==='Ordered'||p.status==='In Transit').length + ' ' + t('Outstanding')) : hasSourcing ? (parts.filter(p => p.status==='Sourcing').length + ' ' + t('Requested')) : t('All Received'); return '<div class="parts-badge ' + color + '" data-action="parts-badge" data-idx="' + index + '">🔩 ' + parts.length + ' Parts • ' + worstLabel + '</div>'; } catch(e) { return ''; } })() : ''}
  ```
- **index.html:9260** — Status string literal `'Received'` does not match the 11 canonical values
  ```
  ${ro.partsJson ? (() => { try { const parts = JSON.parse(ro.partsJson); if (!parts.length) return ''; const hasBackordered = parts.some(p => p.status === 'Backordered' || p.status === 'Lost'); const hasSourcing = parts.some(p => p.status === 'Sourcing'); const hasOutstanding = parts.some(p => p.status === 'Ordered' || p.status === 'In Transit'); const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed' || p.status === 'Returned'); const color = hasBackordered ? 'red' : hasOutstanding ? 'yellow' : hasSourcing ? 'yellow' : 'green'; const worstLabel = hasBackordered ? (parts.filter(p => p.status==='Backordered'||p.status==='Lost').length + ' ' + t('Backordered')) : hasOutstanding ? (parts.filter(p => p.status==='Ordered'||p.status==='In Transit').length + ' ' + t('Outstanding')) : hasSourcing ? (parts.filter(p => p.status==='Sourcing').length + ' ' + t('Requested')) : t('All Received'); return '<div class="parts-badge ' + color + '" data-action="parts-badge" data-idx="' + index + '">🔩 ' + parts.length + ' Parts • ' + worstLabel + '</div>'; } catch(e) { return ''; } })() : ''}
  ```
- **index.html:9260** — Status string literal `'Installed'` does not match the 11 canonical values
  ```
  ${ro.partsJson ? (() => { try { const parts = JSON.parse(ro.partsJson); if (!parts.length) return ''; const hasBackordered = parts.some(p => p.status === 'Backordered' || p.status === 'Lost'); const hasSourcing = parts.some(p => p.status === 'Sourcing'); const hasOutstanding = parts.some(p => p.status === 'Ordered' || p.status === 'In Transit'); const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed' || p.status === 'Returned'); const color = hasBackordered ? 'red' : hasOutstanding ? 'yellow' : hasSourcing ? 'yellow' : 'green'; const worstLabel = hasBackordered ? (parts.filter(p => p.status==='Backordered'||p.status==='Lost').length + ' ' + t('Backordered')) : hasOutstanding ? (parts.filter(p => p.status==='Ordered'||p.status==='In Transit').length + ' ' + t('Outstanding')) : hasSourcing ? (parts.filter(p => p.status==='Sourcing').length + ' ' + t('Requested')) : t('All Received'); return '<div class="parts-badge ' + color + '" data-action="parts-badge" data-idx="' + index + '">🔩 ' + parts.length + ' Parts • ' + worstLabel + '</div>'; } catch(e) { return ''; } })() : ''}
  ```
- **index.html:9260** — Status string literal `'Returned'` does not match the 11 canonical values
  ```
  ${ro.partsJson ? (() => { try { const parts = JSON.parse(ro.partsJson); if (!parts.length) return ''; const hasBackordered = parts.some(p => p.status === 'Backordered' || p.status === 'Lost'); const hasSourcing = parts.some(p => p.status === 'Sourcing'); const hasOutstanding = parts.some(p => p.status === 'Ordered' || p.status === 'In Transit'); const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed' || p.status === 'Returned'); const color = hasBackordered ? 'red' : hasOutstanding ? 'yellow' : hasSourcing ? 'yellow' : 'green'; const worstLabel = hasBackordered ? (parts.filter(p => p.status==='Backordered'||p.status==='Lost').length + ' ' + t('Backordered')) : hasOutstanding ? (parts.filter(p => p.status==='Ordered'||p.status==='In Transit').length + ' ' + t('Outstanding')) : hasSourcing ? (parts.filter(p => p.status==='Sourcing').length + ' ' + t('Requested')) : t('All Received'); return '<div class="parts-badge ' + color + '" data-action="parts-badge" data-idx="' + index + '">🔩 ' + parts.length + ' Parts • ' + worstLabel + '</div>'; } catch(e) { return ''; } })() : ''}
  ```
- **index.html:9260** — Status string literal `'Backordered'` does not match the 11 canonical values
  ```
  ${ro.partsJson ? (() => { try { const parts = JSON.parse(ro.partsJson); if (!parts.length) return ''; const hasBackordered = parts.some(p => p.status === 'Backordered' || p.status === 'Lost'); const hasSourcing = parts.some(p => p.status === 'Sourcing'); const hasOutstanding = parts.some(p => p.status === 'Ordered' || p.status === 'In Transit'); const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed' || p.status === 'Returned'); const color = hasBackordered ? 'red' : hasOutstanding ? 'yellow' : hasSourcing ? 'yellow' : 'green'; const worstLabel = hasBackordered ? (parts.filter(p => p.status==='Backordered'||p.status==='Lost').length + ' ' + t('Backordered')) : hasOutstanding ? (parts.filter(p => p.status==='Ordered'||p.status==='In Transit').length + ' ' + t('Outstanding')) : hasSourcing ? (parts.filter(p => p.status==='Sourcing').length + ' ' + t('Requested')) : t('All Received'); return '<div class="parts-badge ' + color + '" data-action="parts-badge" data-idx="' + index + '">🔩 ' + parts.length + ' Parts • ' + worstLabel + '</div>'; } catch(e) { return ''; } })() : ''}
  ```
- **index.html:9260** — Status string literal `'Lost'` does not match the 11 canonical values
  ```
  ${ro.partsJson ? (() => { try { const parts = JSON.parse(ro.partsJson); if (!parts.length) return ''; const hasBackordered = parts.some(p => p.status === 'Backordered' || p.status === 'Lost'); const hasSourcing = parts.some(p => p.status === 'Sourcing'); const hasOutstanding = parts.some(p => p.status === 'Ordered' || p.status === 'In Transit'); const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed' || p.status === 'Returned'); const color = hasBackordered ? 'red' : hasOutstanding ? 'yellow' : hasSourcing ? 'yellow' : 'green'; const worstLabel = hasBackordered ? (parts.filter(p => p.status==='Backordered'||p.status==='Lost').length + ' ' + t('Backordered')) : hasOutstanding ? (parts.filter(p => p.status==='Ordered'||p.status==='In Transit').length + ' ' + t('Outstanding')) : hasSourcing ? (parts.filter(p => p.status==='Sourcing').length + ' ' + t('Requested')) : t('All Received'); return '<div class="parts-badge ' + color + '" data-action="parts-badge" data-idx="' + index + '">🔩 ' + parts.length + ' Parts • ' + worstLabel + '</div>'; } catch(e) { return ''; } })() : ''}
  ```
- **index.html:9260** — Status string literal `'Ordered'` does not match the 11 canonical values
  ```
  ${ro.partsJson ? (() => { try { const parts = JSON.parse(ro.partsJson); if (!parts.length) return ''; const hasBackordered = parts.some(p => p.status === 'Backordered' || p.status === 'Lost'); const hasSourcing = parts.some(p => p.status === 'Sourcing'); const hasOutstanding = parts.some(p => p.status === 'Ordered' || p.status === 'In Transit'); const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed' || p.status === 'Returned'); const color = hasBackordered ? 'red' : hasOutstanding ? 'yellow' : hasSourcing ? 'yellow' : 'green'; const worstLabel = hasBackordered ? (parts.filter(p => p.status==='Backordered'||p.status==='Lost').length + ' ' + t('Backordered')) : hasOutstanding ? (parts.filter(p => p.status==='Ordered'||p.status==='In Transit').length + ' ' + t('Outstanding')) : hasSourcing ? (parts.filter(p => p.status==='Sourcing').length + ' ' + t('Requested')) : t('All Received'); return '<div class="parts-badge ' + color + '" data-action="parts-badge" data-idx="' + index + '">🔩 ' + parts.length + ' Parts • ' + worstLabel + '</div>'; } catch(e) { return ''; } })() : ''}
  ```
- **index.html:9260** — Status string literal `'In Transit'` does not match the 11 canonical values
  ```
  ${ro.partsJson ? (() => { try { const parts = JSON.parse(ro.partsJson); if (!parts.length) return ''; const hasBackordered = parts.some(p => p.status === 'Backordered' || p.status === 'Lost'); const hasSourcing = parts.some(p => p.status === 'Sourcing'); const hasOutstanding = parts.some(p => p.status === 'Ordered' || p.status === 'In Transit'); const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed' || p.status === 'Returned'); const color = hasBackordered ? 'red' : hasOutstanding ? 'yellow' : hasSourcing ? 'yellow' : 'green'; const worstLabel = hasBackordered ? (parts.filter(p => p.status==='Backordered'||p.status==='Lost').length + ' ' + t('Backordered')) : hasOutstanding ? (parts.filter(p => p.status==='Ordered'||p.status==='In Transit').length + ' ' + t('Outstanding')) : hasSourcing ? (parts.filter(p => p.status==='Sourcing').length + ' ' + t('Requested')) : t('All Received'); return '<div class="parts-badge ' + color + '" data-action="parts-badge" data-idx="' + index + '">🔩 ' + parts.length + ' Parts • ' + worstLabel + '</div>'; } catch(e) { return ''; } })() : ''}
  ```
- **index.html:9260** — Status string literal `'Sourcing'` does not match the 11 canonical values
  ```
  ${ro.partsJson ? (() => { try { const parts = JSON.parse(ro.partsJson); if (!parts.length) return ''; const hasBackordered = parts.some(p => p.status === 'Backordered' || p.status === 'Lost'); const hasSourcing = parts.some(p => p.status === 'Sourcing'); const hasOutstanding = parts.some(p => p.status === 'Ordered' || p.status === 'In Transit'); const allDone = parts.every(p => p.status === 'Received' || p.status === 'Installed' || p.status === 'Returned'); const color = hasBackordered ? 'red' : hasOutstanding ? 'yellow' : hasSourcing ? 'yellow' : 'green'; const worstLabel = hasBackordered ? (parts.filter(p => p.status==='Backordered'||p.status==='Lost').length + ' ' + t('Backordered')) : hasOutstanding ? (parts.filter(p => p.status==='Ordered'||p.status==='In Transit').length + ' ' + t('Outstanding')) : hasSourcing ? (parts.filter(p => p.status==='Sourcing').length + ' ' + t('Requested')) : t('All Received'); return '<div class="parts-badge ' + color + '" data-action="parts-badge" data-idx="' + index + '">🔩 ' + parts.length + ' Parts • ' + worstLabel + '</div>'; } catch(e) { return ''; } })() : ''}
  ```
- **index.html:9308** — Status string literal `'${escapeHtml(ro.status)}'` does not match the 11 canonical values
  ```
  data-original-status="${escapeHtml(ro.status)}">
  ```
- **index.html:10548** — Status string literal `', response.status + '` does not match the 11 canonical values
  ```
  warn('Could not fetch user info (status:', response.status + ')');
  ```
- **index.html:11129** — Status string literal `'completed'` does not match the 11 canonical values
  ```
  const completed = woTasks.filter(t => t.status === 'completed').length;
  ```
- **index.html:11477** — Status string literal `'cancelled'` does not match the 11 canonical values
  ```
  .update({ status: 'cancelled' })
  ```
- **index.html:11519** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status:           'pending',
  ```
- **index.html:11568** — Status string literal `'status'` does not match the 11 canonical values
  ```
  status:            'status',
  ```
- **index.html:12694** — Status string literal `'Sourcing'` does not match the 11 canonical values
  ```
  status:      'Sourcing',
  ```
- **index.html:13016** — Status string literal `' + err.message, '` does not match the 11 canonical values
  ```
  showToast('Error updating parts status: ' + err.message, 'error');
  ```
- **index.html:16131** — Status string literal `' + (err.message || err), '` does not match the 11 canonical values
  ```
  } catch(err) { showToast('Error updating status: ' + (err.message || err), 'error'); }
  ```
- **supabase/functions/process-scheduled-notifications/index.ts:150** — Status string literal `'failed'` does not match the 11 canonical values
  ```
  status: "failed",
  ```
- **supabase/functions/process-scheduled-notifications/index.ts:155** — Status string literal `'failed'` does not match the 11 canonical values
  ```
  results.push({ id: row.id, status: "failed", reason: "no recipients" });
  ```
- **supabase/functions/process-scheduled-notifications/index.ts:171** — Status string literal `'sent'` does not match the 11 canonical values
  ```
  status: "sent",
  ```
- **supabase/functions/process-scheduled-notifications/index.ts:176** — Status string literal `'sent'` does not match the 11 canonical values
  ```
  results.push({ id: row.id, status: "sent", recipients: recipients.length });
  ```
- **supabase/functions/process-scheduled-notifications/index.ts:200** — Status string literal `'failed'` does not match the 11 canonical values
  ```
  status: "failed",
  ```
- **supabase/functions/process-scheduled-notifications/index.ts:205** — Status string literal `'failed'` does not match the 11 canonical values
  ```
  results.push({ id: row.id, status: "failed", reason: msg });
  ```
- **supabase/functions/twilio-webhook/index.ts:115** — Status string literal `'received'` does not match the 11 canonical values
  ```
  status: 'received',
  ```
- **time-off.html:934** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status:           'pending',
  ```
- **time-off.html:943** — Status string literal `'pending'` does not match the 11 canonical values
  ```
  status:           'pending',
  ```

### Class H — SQL ::jsonb cast without EXCEPTION wrapper (1)

- **supabase/migrations/cron_archive_cashiered_ros.sql:99** — ::jsonb cast without nearby EXCEPTION handler — empty strings or malformed JSON will abort the transaction
  ```
  THEN ro_row.insurance_data::jsonb
  ```

## LOW (13)

### Class C — .single() — review for 0-row tolerance (13)

- **checkin.html:592** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .single();
  ```
- **checkin.html:1191** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  const { data, error } = await getSB().from('time_logs').insert(logData).select().single();
  ```
- **closed-ros.html:870** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .single();
  ```
- **customer-checkin.html:1993** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  }).select().single();
  ```
- **customer-checkin.html:2263** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .single();
  ```
- **index.html:11319** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  }).select().single();
  ```
- **index.html:11407** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  }).select().single();
  ```
- **index.html:14812** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  }).select().single();
  ```
- **index.html:14852** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  }).select().single();
  ```
- **solar.html:108** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .single();
  ```
- **solar.html:139** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .select('value').eq('key', 'session').single();
  ```
- **solar.html:150** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  .select('value').eq('key', 'last_quote').single();
  ```
- **solar.html:2992** — .single() — verify 0 rows can't legitimately occur, else use .maybeSingle()
  ```
  }).select().single();
  ```

