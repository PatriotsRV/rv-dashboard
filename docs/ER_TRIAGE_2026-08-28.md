# PRVS Enhancement-Request Triage - 2026-08-28 (headless runner)

Incremental pass over `enhancement_requests` against the current modular
codebase at HEAD **505e22b** (index.html **v1.502**, tasks.html **tasks-v1.3**).
Input snapshot `er_input.json` fetched 2026-08-28 19:19 UTC.

`needs_triage_ids` held exactly **one** row this run (`6ff111e7`, Lynn, 08-27).
That row was classified by reading the live code. **The other 105 rows were not
re-verified this run** - their `triage_bucket` / `triage_loe` / `triage_verdict`
are carried over verbatim from the run that last stamped them, and each verdict
below still names the HEAD it was checked against. See "Carried verdicts that
are now suspect" for the ones S185/S186 probably invalidated.

Runner is read-only: **no code was changed, no DB writes were made, no git
commands were run.** Status flips and the Data-bucket SQL are Roland's to run.

## Board snapshot

| DB status | Count |
|---|---|
| done | 62 |
| unreviewed | 29 |
| declined | 15 |
| **Total** | **106** |

## Verdict buckets (all 106 rows)

| Bucket | Count |
|---|---|
| done | 62 |
| needed | 28 |
| duplicate | 13 |
| data | 2 |
| bug | **0** |
| (no verdict) | 1 |

**Headline: the bug column is empty for the first time.** All five bugs carried
on the 2026-08-08 board are now closed - `09084bc5` in v1.494, then `225a2535`,
`d94a418a`, `d4d0eeba` and `5ca724ff` in the v1.497 batch. The board is 28 open
enhancements, 1 unreviewed duplicate to decline, and paperwork.

The one new ER this run (`6ff111e7`, Lynn, 08-27) classifies **Needed / S** and
lands in Scheduling & Dates.

## The one ER classified this run

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `6ff111e7` | Lynn | 08-27 | General UI/UX | **needed** | **S** | `js/render.js` Drop chip L468 (vs Promised L469), key-dates hover row L436, marker comment L71; `css/dashboard.css` `--bg-surface` L10 / `.ro-card` L427 | "enlarge the font and change color for drop date on RO - the blue is too difficult to see and is not large enough". NOT a duplicate of `fa35e3df`: that ER already moved this chip from `#0a84ff` to `#5cadff` in v1.498 (S178), and Lynn filed this **5 days after** v1.498 shipped, so propagation is not the explanation - she is saying the lightened blue still does not stand out, and she adds a brand-new **size** ask. Confirmed at HEAD 505e22b: the Drop chip is still the smallest and lightest of the three key dates - `font-size:0.72rem; font-weight:700` (L468) against the Promised chip's `0.78rem; 800` (L469); Pickup shares Drop's 0.72/700. Card surface is `#1a1d24`. Small, render/CSS only: lift Drop to at least Promised-level size and weight and pick a stronger hue, and carry the same color to the `kd-pop-row` at L436. Filed `needed` rather than `done` per the prefer-needed rail. |

## Carried verdicts that are now suspect (read before planning work)

Every carried verdict was stamped at or before HEAD `9c50b52` (v1.501). Since
then **v1.502 (S185 outstanding payments)** and the **S186 TASK MANAGER** landed
on `pre-prod`. Three carried `needed` verdicts appear to be overtaken by that
work, and this runner did **not** re-classify them because they were not in
`needs_triage_ids`:

- **`af14b3cb` (Lynn, L, "per-RO TO-DO list with daily reminder")** - its carried
  verdict says "greps for ro_tasks / task_tracker / customer_task return zero
  hits, and shop_tasks has no ro_id". That is no longer true. `b88ff19` added
  `supabase/migrations/task_manager_s186.sql` with a `tasks` table carrying
  `ro_uuid uuid references repair_orders(id)` (L15), a `task_events` table (L50),
  an `ro_activity_feed` view (L106), the `tasks.html` board, and the
  `send-task-reminders` edge function (SMS + email nag loop with escalation).
  This looks like the substance of the ask.
- **`5925fc01` (Lynn, L)** - same theme; the task/reminder half it was waiting on
  is what S186 built.
- **`a195e4ab` (Brandon, M, "track checks received on an RO")** - its carried
  verdict says "grep check_number / checkNumber / payments = 0 hits". S185 added
  `supabase/migrations/ro_receivables_s185.sql` and an `ro_receivables` table
  read in `js/ro-crud.js` L85-104 into a per-RO `_receivable` (L183) with count,
  total, overdue and expected-by, plus a card badge. Multiple open amounts per RO
  with dates is most of the ask; whether the **check number** field exists is the
  open question.

**Recommendation: run one full pass (`full_pass=true`) before any of these three
are scheduled as work.** Carrying them forward as "confirmed absent" would put
already-built features on the build queue.

---

## BUGS - front of the line (0)

None. Empty for the first time since this board started. The five bugs from the
2026-08-08 run all closed:

| ER | By | Closed in | Note |
|---|---|---|---|
| `09084bc5` | Lynn | v1.494 (S171) | Cash-out data loss - `archive_single_ro` RPC, `cashiered_full_detail_s171.sql` adds the 7 dropped columns and 7 child mirrors before the delete. |
| `225a2535` | Brandon | v1.497 (S178) | `_lastEstimateScan` now stamped with `roKey = editingROSupabaseId`; every consumer gates on it. |
| `d94a418a` | Brandon | v1.497 (S178) | `formatNotesNewestFirst` reverses ENTRIES, not lines. |
| `d4d0eeba` | Brandon | v1.497 (S178) | Clear-Filters toast retries `scrollToROInBoard`; `js/qr.js` clears filters once for the deep link. Closed `9031e101` + `1bc33dd6` too. |
| `5ca724ff` | Lynn | v1.497 (S178) | `dragstart` now sets `dataTransfer.setData` + `effectAllowed`. |

---

## NEEDED - open enhancements by theme (28)

### RO Fields & Status (6)

| ER | By | Date | LOE | Mapped code | Verdict (carried unless marked) |
|---|---|---|---|---|---|
| `44419de9` | Lynn | 08-12 | **S** | `js/config.js` STATUS_PROGRESS_MAP L151-164; index.html select L7038-7051, filter btns L1085-1096, inline maps L1579/L4489; `js/render.js` L253/L451, statusColorMap L727-739, ALL_STATUSES L741-745 | None of the 4 values exist at HEAD a405b7a (v1.494). Each add must hit 9 hardcoded spots PLUS both DB CHECKs per S172 or saves throw 23514. Overlaps `972af03c` CANCELED - confirm scope with Lynn; bundle with `972af03c` + `3e746c18`. |
| `972af03c` | Lynn | 07-17 | **S** | `js/config.js` STATUS_PROGRESS_MAP L151-164 | No Canceled / Cancelled RO status at HEAD 0223cb3; the only "cancelled" hits are the scheduled_notifications and review_requests CHECKs. Add the status plus color/priority maps, exclude from on-lot and queue counts, keep the record for notes. Canonical for duplicate `eb00a68d`; pairs with `3e746c18`. |
| `3e746c18` | Lynn | 08-03 | **S** | `js/config.js` L151-164; `js/render.js` L656-658, L718-721 | No TOTALED OUT at HEAD 0223cb3. Distinct value from `972af03c`'s Canceled, but ONE build should add both terminal statuses and exclude both from the queue / on-lot counts. |
| `a195e4ab` | Brandon | 08-20 | **M** | index.html Customer Pay block L7203-7246 / L7782-7804; `setROType` L2263-2267; insurance_data JSONB write L6562/L6718 | Zero check tracking at HEAD df519c6 (v1.495): one-value Customer Pay block, hybrid-RO-only, method Check but no check number, saved flat into `insurance_data`. Needs a per-RO checks table + add/list UI + cashiered mirror. **SUSPECT - see the stale-verdict section; S185 `ro_receivables` may already cover most of this.** |
| `0e172b2c` | Sofia | 07-16 | **M** | `js/duplicates.js` getBaseROId L35, regex L36, findDuplicateGroups L40, keying L44 | Partial at HEAD 0223cb3: the merge engine groups strictly on the `PRVS-XXXX-XXXX` base RO id, so two ROs under different spellings of one customer are never detected. Needs name/phone account dedup plus a verify-which-is-correct step. One-off cleanup is `3f254929`. |
| `5fbfa299` | Brandon | 06-30 | **M** | `js/ro-crud.js` archiveROInSupabase L662 | Only the cashiered archive path exists at HEAD 0223cb3; grep for archived / abandoned in render + config returns zero. No soft-archive flag, status or board filter for quoted-but-never-progressed units. Needs a flag, a board filter and an un-archive path. Pairs with `972af03c`. |

### Messaging & E-sign (7)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `0448de62` | Sofia | 06-11 | **L** | `customer-checkin.html` signature pad; `send-quote-email` raf_copy L386 | Signing is kiosk-only at HEAD 0223cb3; the raf_copy branch only emails a copy after signing. Grep for signing_token / remote_sign / signature_request / signing_link returns nothing. Needs a tokenized public signing page, a delivery path and signed-form storage. |
| `ca0f7ed9` | Brandon | 08-03 | **S** | `messages.html` renderLeadInfo L1389, call site L1744 | On messages.html v1.23, `renderLeadInfo` reads ONLY the conversation's own columns (`c.rv`, `c.service_type`), so an existing dashboard customer shows "No RV or service type captured yet". `selectConversation` already computes phone-matched live ROs as `linked` but calls `renderLeadInfo(c)` without them. Small: pass `linked`, fall back to RO rv. |
| `5fec3eca` | Sofia | 07-15 | **S** | `send-quote-email` raf_copy destructure L387 | The raf_copy payload has no `customerAddress` - the address appears only in the `lead_staff_notify` branch. Small: add it to the payload and both templates. Edge-fn deploy, no dashboard release. |
| `9aab7550` | Brandon | 07-21 | **S** | `messages.html` `.unread-dot` CSS L469, condition L1313, render L1319, markConversationUnread L1980 | Still only partial on messages.html v1.23. S166 added a MANUAL "Mark unread" button, but the visual treatment is unchanged: a single 8px gold dot applied only when `last_direction` is inbound AND status open, with no row-level highlight. Small: add a bold/tinted unread row. |
| `21fc3a9d` | Brandon | 07-21 | **S** | `messages.html` fixed rem sizing | Confirmed absent on messages.html v1.23: fixed rem font sizes with no user text-size control; grep for font-scale / fontScale / textSize returns zero hits. Small: add a persisted font-scale control for the conversation list. |
| `f1e6e311` | Brandon | 07-21 | **S** | `messages.html` convoSearch L604, resets L1151 / L1170 | Confirmed absent on messages.html v1.23: `convoSearch` is a plain text input with no clear affordance, reset only programmatically. Small: add a clear (x) button that empties the search and re-runs the filter. |
| `6e9ca4dc` | Brandon | 07-21 | **S** | `js/messaging.js` refreshSignaturePreview L369, element L350, set L374; `messages.html` media query L565 | Valid and still present on desktop at HEAD 0223cb3: "Added to every send:" is hidden ONLY inside the mobile max-width media query, so desktop messages.html and the RO-card composer still show it. Small: remove or collapse it in all viewports; the signature itself keeps sending. |

### Notifications & Task-Tracking (3)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `5925fc01` | Lynn | 04-09 | **L** | `send-manager-report`; `js/work-orders.js` L293; `js/ro-crud.js` L374 | PARTIAL at HEAD 0223cb3. The daily in-progress digest half ships as `send-manager-report`, and single-event notifies exist (`service_added_notify`, `urgent_update_notify`). Still absent: notify-on-every-RO-update and to-do scraping from status notes. Anchor of this theme; pairs with `af14b3cb`. **SUSPECT - S186 built the task/reminder half.** |
| `af14b3cb` | Lynn | 05-21 | **L** | `js/shop-tasks.js` (no ro_id, no reminder column) | The shop checklist is non-customer and has no RO link at HEAD 0223cb3; grep for ro_tasks / customer_task / task_tracker returned zero hits. Needs a per-RO tasks table, an add/complete UI and a daily open-task reminder. Pairs with `5925fc01`. **SUSPECT - `task_manager_s186.sql` now has `tasks.ro_uuid` + `send-task-reminders`; re-triage before scheduling.** |
| `a03677e6` | Brandon | 08-07 | **S** | `js/render.js` L553 vs L563; writers at `process-scheduled-notifications` L187/L215, index.html L3116/L3154, `js/ro-crud.js` L302/L344/L379/L571/L582, `js/parts.js` L654/L855 | Confirmed at HEAD 0223cb3: machine audit lines write as notes type `ro_status` and crowd RO Status Updates - NOTIFICATION SENT/FAILED, SCHEDULED/CANCELLED, key-date and urgent rows, parts notes. `customer_comm` renders separately. Small: retarget the type. NOTE Roland reversed this retarget for LEAD NOTES in v1.491 - confirm scope with him first. Canonical for duplicate `2deb8f30`. |

### Parts & Inventory (5)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `14f35f95` | Lynn | 04-09 | **XL** | `js/parts.js` (per-RO line items only) | Confirmed absent at HEAD 0223cb3: word-boundary grep for inventory / on_hand / in_stock / stock_qty across js, migrations and functions returns zero hits. New subsystem: inventory table, receive/consume/adjust flows, a searchable priced stock view, and links from the parts-request path. Largest item on the board. |
| `b19975d4` | Lynn | 04-09 | **L** | `js/config.js` PART_STATUSES L200; `js/parts.js` pf_returnDeadline L226 | Partial infra only at HEAD 0223cb3: a "Returned" status and a Return Deadline input, but grep for rma / return_reason / supplier_credit returns zero - no RMA number, return reason, supplier credit or reverse-direction flow. Equals GH#24 Shop Ops Phase 2. Canonical for declined duplicate `86bbaf50`. |
| `8d885331` | Lynn | 04-09 | **S** | `js/parts.js` pf_supplier L181, pf_partName L190, pf_serviceSilo L208, pf_returnDeadline L226 | Form unchanged at HEAD 0223cb3; the ask is only to reorder the fields. Quick once Lynn supplies the target layout or a mockup. **Blocked on detail, not on effort.** |
| `2af47890` | Brandon | 08-25 | **S** | `js/parts.js` markPartReceived L449-478, savePartForm L539; `stamp_date_received_on_parts.sql` L33-36 | Prefer needed over done. DB half ships: trigger `trg_stamp_date_received` stamps `date_received` on the transition into Received. Client half missing: `markPartReceived` sets `part.status` only, never `part.dateReceived`, so a later `savePartForm` edit resends `date_received` null, which the trigger will not restamp (OLD.status already Received) - wiping the date. |
| `1e434aff` | Brandon | 08-25 | **S** | `js/parts.js` headers L259-267, renderPartsRows L121-145, pf_dateReceived L242 | Confirmed absent at HEAD 9c50b52 (v1.501): the Manage Parts table has no receipt-date column and `renderPartsRows` prints no `p.dateReceived`; the value is only editable in the collapsed Add/Edit Part form. Small: render `dateReceived` beside the Received tick. Depends on `2af47890` landing the stamp. |

### Scheduling & Dates (2)

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `6ff111e7` | Lynn | 08-27 | **S** | `js/render.js` Drop chip L468, Promised chip L469, hover row L436, marker L71; `css/dashboard.css` L10 / L427 | **NEW this run.** `fa35e3df` already recolored this chip `#0a84ff` -> `#5cadff` in v1.498, but Lynn filed this 5 days after that shipped and adds a NEW size ask. At HEAD 505e22b the Drop chip is still `0.72rem / 700`, the smallest key date, against Promised at `0.78rem / 800`. Small, render/CSS only: lift Drop to Promised-level size and weight plus a stronger hue on the `#1a1d24` card, and carry it to the hover row. |
| `47428f75` | Lynn | 08-03 | **S** | `time-off.html` L368, L372, L752 | UNDERSPECIFIED - "update vacation/sick/personal calendar", no stated defect or target. All three types, a color-coded calendar, per-row Edit and exact partial-day windows already ship. Filed Needed rather than Done because the ask maps to no gap without Lynn's detail; if it restates `ee50d11c` it is already delivered. **Ask the submitter.** |

### Board & Navigation UX (1)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `d1844e46` | Lynn | 07-16 | **S** | `js/render.js` calculatePriority L30-39, shop pin L35-37 | Confirmed absent at HEAD 0223cb3: the board sorts on one blended priority score; grep for sortBy / sortSelect / sortMode finds nothing, so there is no selectable strict promised-then-oldest order. Small: add the sort option while keeping the shop pin. Related to `1aeb3f58`, which delivered only the promised-date chip. |

### Time Clock & Check-In (2)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `6bb61db2` | Riley | 07-22 | **S** | `js/render.js` compact-col-actions L228-235; `js/time-tracking.js` | Confirmed absent at HEAD 0223cb3: the compact actions row has edit / parts / WO / notify / message / schedule but no check-in-out button, and grep for "clocked in" / activeTimeLog returns nothing. Small: add a compact check-in/out action plus a login-time active-time_log banner across views. **Canonical spec for this theme.** |
| `e06d747e` | Riley | 04-10 | **S** | `js/render.js` compact-col-actions L228-235 | Confirmed absent at HEAD 0223cb3: no floating or sticky tech check-in control on any page. Underspecified - scope the target page and behavior with the submitter. Almost certainly the same ask; fold into `6bb61db2` and build once. |

### Work Orders & Shop Tasks (1)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `2f752d40` | Jason Rubin | 07-21 | **S** | `js/work-orders.js` woFormDollar L360, woFormEstHours L365, save L649-650, dollarLocked L324 | Confirmed absent at HEAD 0223cb3: dollar and hours are independent manual inputs and grep for LABOR_RATE / laborRate / labor_rate in the module returns zero (`js/insurance.js` laborRate L212 is the per-claim INSURANCE rate). Small: auto-populate `dollar_value` = effective hours x $195/hr, respecting the `dollarLocked` gate. |

### Auth & Cost (1)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | **XL** | `js/auth.js` L69, L203-204, One Tap + signInWithIdToken | Still fully Google-based at HEAD 0223cb3, kiosk included. Swapping to Supabase password / magic-link or shared role accounts drops roughly $500/mo but touches every page sign-in plus the email-keyed RBAC. Scope with Roland in a dedicated session. |

---

## DATA - corrections for Roland (2)

Nothing below was executed by this runner.

| ER | By | Date | Verdict + SQL (carried) |
|---|---|---|---|
| `3f254929` | Sofia | 07-16 | Data correction, not code - and **already carried out**. The misspelled Steve Hubert RO was soft-deleted in S178; only the correct Huebert RO remains. SQL of record:<br>`SELECT id, ro_id, customer_name, status FROM repair_orders WHERE customer_name ILIKE '%ubert%';`<br>then, with the confirmed wrong id:<br>`UPDATE repair_orders SET deleted_at = now() WHERE id = '<wrong-uuid>' AND deleted_at IS NULL;`<br>The wider merge-accounts ask stays open as `0e172b2c` (needed/M). |
| `6938c1af` | Ryan Dillon | 05-06 | Already applied in the live DB (tech names read from `staff.name`; submitter "Rod Wombles" now spells correctly in this very table). RESIDUAL, not a runtime bug: `supabase/migrations/staff_table.sql` L85-87 still seeds Rod/Zak/Travis as "Wimbles", so a re-seed would reintroduce it. Re-apply if ever needed:<br>`UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |

---

## DUPLICATES - fold and decline (13)

| ER | By | Date | Folds into | DB status | Note (carried) |
|---|---|---|---|---|---|
| `611589ba` | Lynn | 08-24 | `972af03c` + `44419de9` | **unreviewed - recommend declined** | Both halves are already open ERs. CANCELED is `972af03c` (canonical; `eb00a68d` already declined into it); Closed is `44419de9`'s JOB DECLINED/CLOSE. Re-verified absent at HEAD f501151 (v1.500): STATUS_PROGRESS_MAP L151-171 lists 15 statuses, no Closed and no Canceled. Adds no new work item; ships with the status batch. |
| `aa508852` | Riley | 04-09 | `fd6c122d` | declined | Identical text and timestamp. |
| `86bbaf50` | Lynn | 05-13 | `b19975d4` | declined | Gordon window returned to Vogt = supplier return tracking. |
| `67daba15` | Brandon | 06-09 | `d2561e11` | declined | RO completion / pick-up date, delivered via `repair_orders.pickup_date`. |
| `fa1c1d6e` | Sofia | 06-09 | `a22739b9` | declined | Sheldon Gilbert check-in email; both sends are now forced. |
| `5057c7fc` | Roland | 06-09 | `91a03260` | declined | Prune cashed-out RO from the work list; delivered. |
| `fc6b29f9` | Sofia | 06-17 | `298e0e67` | declined | Retest report of the duplicate-RO guard. |
| `9031e101` | Riley | 07-21 | `d4d0eeba` | declined | iPhone work-list tap did not reach the unit. Canonical fixed in v1.497. |
| `1bc33dd6` | Brandon | 07-27 | `d4d0eeba` | declined | Same scroll-to-customer defect Brandon filed 07-22. Canonical fixed in v1.497. |
| `2b78696c` | Lynn | 07-27 | `29654af7` | declined | Same back-to-top ask Lynn filed 07-06. Canonical shipped in v1.498. |
| `eb00a68d` | Lynn | 08-03 | `972af03c` | declined | Same CANCELED status ask Lynn filed 07-17. |
| `2deb8f30` | Lynn | 08-13 | `a03677e6` | declined | Same ask: get machine-generated notification lines out of RO Status Updates. Lynn's "separate location view" wording folds in as spec. |
| `ece91cba` | Lynn | 08-17 | `af14b3cb` | declined | Same per-RO to-do list. Its added spec (bulleted, anyone can add or check, record who and when, plus the Repair Description vs RO Status split) folds into `af14b3cb`. |

---

## DONE - delivered (62)

Carried verdicts. The HEAD named in each row is the HEAD it was last verified
against, not this run's HEAD.

| ER | By | Date | Category | Mapped code | Verdict |
|---|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | General UI/UX | - | Test entry ("Testing out the Make-A-Wish function"). Not a real request. Already declined; keep declined. |
| `19dc2016` | Roland | 04-11 | Time Clock | - | Test entry ("Testing ER form / dictation"). Not a real request. Already declined; keep declined. |
| `79b2c805` | Sofia | 04-09 | New Feature | `closed-ros.html` reactivateRO L838, buttons L725/L813, flat map L958-963; `js/photos.js` uploadDocument L253 | Cashed-out ROs reactivate with parts/time_logs/notes/audit_log/insurance_scans plus the WO subtree, and documents can be added to the reopened RO. |
| `75124b03` | Ryan | 04-09 | RO Management | `js/ro-crud.js` warranty branch; `js/render.js` L121 / L195 / L372 | Warranty is a first-class RO type at creation with board filter, compact chip and card badge (plus the $0 No-Charge sub-badge). |
| `b2aa4bea` | Lynn | 04-09 | New Feature | `js/ro-crud.js` L413 / L490; `js/render.js` L395; `key_dates_reminder_sources.sql` | `planned_dropoff_date` persists on create and edit, drives the blue Drop chip and the day-before reminder. Same field as `468aa376`. |
| `18611eee` | Lynn | 04-09 | Bug Report | `js/work-list.js` renderWorkList, removeFromWorkList L195, drag L304, silo tabs L223/L263 | Was a real bug; added members are viewable, reorderable and removable. The desktop-drag half Lynn later reported was `5ca724ff`, fixed in v1.497. |
| `17492066` | Lynn | 04-09 | Notifications | `scheduled_notifications.sql`; `process-scheduled-notifications`; index.html snRepeat L2993 | The per-RO Schedule Notification bell sets a future date/time, message and recipients with daily/weekly repeat until a chosen date. Covers the advance-warning ask. |
| `fd6c122d` | Riley | 04-09 | Work List | `js/work-list.js` removeFromWorkList L195, drag L304/L389, touch L404-429, `_saveWorkListOrder` L210/L383 | Delivered v1.460: delete plus drag-reorder on mouse and touch, mobile sizing fixed. Canonical for declined duplicate `aa508852`. |
| `98bd7498` | Brandon | 04-09 | RO Management | `js/work-list.js` `_renderWorkListSiloTabs` L223, `_setWorkListSilo` L263 | Per-silo Work List tabs give a separate list per job category. |
| `1fe68261` | Rod Wombles | 04-13 | Work Orders | `js/shop-tasks.js` L21 / L153 / L180, Done button L130; `shop_tasks.sql` | Delivered v1.467: internal work not tied to a customer unit, separate from ROs and the board. Matches the poly-barrel / roof-bay examples. |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | `js/config.js` STATUS_PROGRESS_MAP L158; `js/render.js` ALL_STATUSES L741, statusColorMap L727 | Delivered v1.410: "Ready to Work" is a live RO status in both status dropdowns. |
| `b87eb2fb` | Lynn | 04-14 | RO Management | `keys_power_fields.sql`; `js/ro-crud.js` L415-417 / L492-494 | Delivered v1.458: `key_status`, `keypad_code` and `keep_plugged_in` persist and repopulate, with a keys chip on the card. Same build as `34fc03c2`. |
| `a5ff3e2d` | Lynn | 04-21 | RO Management | `js/ro-crud.js` warranty_repair branch; `js/render.js` filter L122, chip L196, badge L373 | Delivered v1.465: the "Warranty + Repair" RO type exists end to end and stays billable so the repair half bills normally through work orders. |
| `a7d1474e` | Lynn | 04-23 | RO Management | `urgent_update_field.sql`; `js/render.js` L324, compact siren L214; `js/ro-crud.js` urgent_update_notify L374 | Delivered v1.466: always-visible red URGENT banner on the card, and setting or changing it notifies silo managers and admins. Lynn's follow-up to move the INPUT onto the tile shipped as `50175fce` in v1.498. |
| `dde90d2b` | Roland | 04-27 | RO Management | `customer-checkin.html` v1.17 raf_copy L2530, lead_staff_notify L2603 | The photo is optional and the outbound email is forced on every submission, so the authorization-form copy and the staff notice both go out. |
| `13e1cd87` | Tipton Scott | 04-30 | Time Clock | `checkin.html` L1516, header note L36 | Was a real gap; delivered v1.447 (checkin.html v1.34, now v1.38): a "Return to Dashboard" button renders after clock-out. |
| `e27311bf` | Ryan | 05-04 | Work Orders | `js/work-orders.js` `_initWOTaskDrag` L461, dragstart L482, touch handlers | Delivered v1.464: every WO task row has a grip handle and drag-to-reorder; DOM order drives `sort_order`, so a template is edited by loading it, reordering, then Overwrite. |
| `d2561e11` | Brandon | 05-06 | New Feature | `key_dates_pickup_date.sql`; `js/ro-crud.js` L414 / L491; `js/render.js` L397, panel L365 | Delivered v1.457: `repair_orders.pickup_date` persists and surfaces as the green Pickup chip. Canonical for declined duplicate `67daba15`. |
| `dac9fdda` | Brandon | 05-06 | General UI/UX | `js/config.js` STATUS_PROGRESS_MAP L154; `js/render.js` onLotCount L718-721 | Delivered v1.460: "Off Lot - Returning" separates units temporarily taken by the customer from units never on the lot, and is excluded from the on-lot count. |
| `a8e90a7d` | Brandon | 05-20 | General UI/UX | `js/parts.js` totWholesale L152, totFreight L153, totCost L155, render L159-161 | Delivered v1.456: the Manage Parts Ticket Totals bar sums parts, freight and total cost. Same build as `671fa623`. |
| `1aeb3f58` | Lynn | 05-21 | General UI/UX | `js/render.js` L396, calculatePriority L30-39 | Delivered v1.457: the promised date renders as a large bold chip, red when overdue, and promise date plus days-on-lot feed the blended sort. The strict user-selectable sort is the open ER `d1844e46`. |
| `cbc70a86` | Lynn | 05-21 | General UI/UX | `customer-checkin.html` v1.17 L2381 rpc `add_ro_to_silo_work_lists`; `work_list_autoadd_rpc.sql` | Returning check-in inserts `manager_work_lists` rows for each involved silo lead, falling back to sr-managers for unmapped silos. |
| `3f415c88` | Lynn | 05-26 | General UI/UX | index.html snRepeat L2993, read L3061 | Delivered v1.463: Repeat none/daily/weekly plus a repeat-until date, expanded into one individually cancellable `scheduled_notifications` row per occurrence. |
| `cd663d8f` | Bobby Thatcher | 05-28 | General UI/UX | `js/ro-crud.js` L1097 toFixed(2); `js/parts.js` MONEY_FIELDS L307, applied L310 | Was a cosmetic bug; fixed v1.447. Currency inputs show 2 decimals on edit-populate and in the parts modal. No data loss occurred. |
| `34fc03c2` | Brandon | 05-29 | New Feature | `keys_power_fields.sql`; `js/ro-crud.js` L415-417 / L492-494 | Delivered v1.458: the Keys selector (keys left / no keys / keypad plus code, keep-plugged-in) is on New and Edit RO with a keys chip on each card. Covers `b87eb2fb`. |
| `0b2e128c` | Lynn | 06-02 | General UI/UX | `checkin.html` L1455, header note L5 | Delivered v1.460 (checkin v1.38): a tech's clock-out work notes are copied into the RO update timeline as a `ro_status` note row. |
| `468aa376` | Lynn | 06-02 | General UI/UX | `js/ro-crud.js` L413 / L490; `js/render.js` L395 | The planned drop-off date input exists on New RO and (restored v1.457) Edit RO, feeding the Drop-off chip and the `auto_dropoff_reminder` source. Same field as `b2aa4bea`. |
| `2b814250` | Roland | 06-07 | Calendar | `js/render.js` L395-397, kd-name-cal L367; `sync-ro-calendar`; `scheduled_notifications` | RO date/time values surface as colored key-date chips plus a hover/tap panel, business-calendar integration runs server-side (idempotent via `cal_event_ids`), and advance reminders go to managers and admins. |
| `cd084233` | Roland | 06-07 | Work List | `send-manager-report`; `send-admin-pnl-report`; `worklist-report.html` | Admin-level daily manager-worklist reporting ships with parts, labor and cost rollups. The manager scoring layer described at the end of the request remains a future extension, not this ER. |
| `a22739b9` | Sofia | 06-08 | General UI/UX | `customer-checkin.html` v1.17 raf_copy L2530, lead_staff_notify L2603 | Was a notify-gate regression; both sends are forced on every submission, so the check-in notice and the authorization-form copy both arrive. Canonical for declined duplicate `fa1c1d6e`. |
| `91a03260` | Roland | 06-09 | Work List | `prune_cashiered_worklists.sql` | Removes an RO from manager work lists the day AFTER it is marked Delivered / Cashed Out, deliberately keeping it visible on the day of cash-out. Canonical for declined duplicate `5057c7fc`. |
| `cdd77a8b` | Brandon | 06-10 | General UI/UX | `js/render.js` updateStats nonShop L656-658, onLotCount L718-721 | Delivered v1.453/v1.454: the queue figure drops Shop Operations ROs and the on-lot count excludes Not On Lot, Scheduled and Off Lot - Returning units, so the tile shows a true on-lot number. |
| `2c3d5633` | Ryan | 06-12 | General UI/UX | `js/photos.js` L66 `document.body.appendChild(input)` | Was a mobile-only functional bug; fixed v1.453. Attaching the upload input to the DOM before clicking it was the iOS Safari dropped-change-event root cause. |
| `74a33621` | Brandon | 06-16 | General UI/UX | `js/photos.js` keydown L400, navigateLightbox L396-397 / L404, prev/next L380 / L382 | Delivered v1.454: the photo lightbox supports arrow-key navigation plus Escape-to-close. |
| `298e0e67` | Sofia | 06-16 | General UI/UX | `customer-checkin.html` v1.17 reusedScheduledRO L2059 / L2165, attachedToActiveRO L2060 / L2194 | Was a data-integrity bug; fixed and strengthened in v1.17 - the returning-customer guard reuses an existing scheduled RO, and v1.17 adds a blocking attach-vs-new dialog. Canonical for declined duplicate `fc6b29f9`. |
| `9b823d25` | Roland | 06-16 | RO Management | `js/work-orders.js` addServiceToRO L244, notify L293; `service_added_notify_source.sql` | Delivered v1.460: when one silo adds a service to an RO the owning silo manager is emailed. Matches the roof-plus-water-heater example. |
| `9b046808` | Brandon | 06-17 | General UI/UX | `js/config.js` STATUS_PROGRESS_MAP L157; `js/render.js` L718-721 | Delivered v1.454: "Scheduled" is its own RO status and not-yet-arrived units are excluded from days-on-lot stats and the on-lot count. |
| `671fa623` | Brandon | 06-18 | General UI/UX | `js/parts.js` totFreight L153, totCost L155, render L159-161 | Delivered v1.456: Ticket Totals include a Freight line summed across parts and rolled into Total Cost. Same build as `a8e90a7d`. |
| `5c45bb8d` | Sofia | 06-19 | General UI/UX | `customer-checkin.html` v1.17 L2059 / L2165 / L2194, raf_copy L2530 | Was a duplicate-RO plus missing-email bug (David Flanagan field report); both halves resolved - the Scheduled path reuses the existing RO and the authorization-form copy is forced on every submission. |
| `b06a285f` | Lynn | 06-19 | General UI/UX | `js/render.js` kd-name-cal L367, rows L363-365, chips L395-397 | Delivered v1.465: a calendar icon beside the customer name opens a key-dates panel listing drop-off, promised and pickup in full on hover or tap. |
| `ee50d11c` | Lynn | 06-19 | General UI/UX | `time-off.html` Edit button L752 -> editRequest() | Was a real gap; time-off entries are editable rather than delete-only - Edit loads the entry back into the form and flips submit to "Update Record". |
| `88395acc` | Lynn | 06-19 | General UI/UX | `add_absence_window_to_time_off.sql`, `add_partial_hours_to_time_off.sql`; `time-off.html` L368 / L372 | Delivered Time Off v1.5: partial-day absence is captured as an exact clock-out-to-return window rather than an hour total. |
| `80390f36` | Lynn | 06-23 | General UI/UX | `js/render.js` renderBoard aShop L35, compare L37, search haystack L61 | Delivered v1.462: Shop Operations tiles pin to the top ahead of the priority sort, and a "shop operations shop" search token makes the tile findable. |
| `7f2d0d70` | Lynn | 06-26 | General UI/UX | `send-dropoff-report` | The automated Monday drop-off email lists this week's scheduled drops plus last week's received units and reads `manager_work_lists` to flag any not yet on the right team's list. Pairs with `cbc70a86`. |
| `3f7fb4fc` | Brandon | 06-29 | General UI/UX | `js/render.js` nonShop L656-658, L718-721, cep-static-status L249 | Was a small data-display bug; fixed v1.468. Shop ROs are excluded from RVs-in-Queue and the on-lot count, and the shop status dropdown is a static label. |
| `cb7742a8` | Lynn | 06-29 | General UI/UX | `sync-ro-calendar`; `customer-checkin.html` POST L2240, header note L22 | Delivered v1.469 (check-in v1.15): drop-off / promised / pickup events post server-side to the correct team calendar for every RO including front-desk check-ins. Fixes the repair-calendar miss Lynn reported. |
| `29654af7` | Lynn | 07-06 | General UI/UX | index.html `#backToTopBtn` L8212-8220, boot log L3748; `css/dashboard.css` L3479-3498 | DONE in v1.498 (S178). Smooth scroll-to-top button revealed after meaningful scroll, pinned bottom-left clear of the toast stack. Canonical for duplicate `2b78696c`. Previously bucket=needed/S. |
| `39b75fab` | Sofia | 07-09 | General UI/UX | `js/photos.js` canManageFiles L618, button row L198-201, lightbox L376 | Delivered v1.472: the Documents / Photos library supports rename, move-to-another-RO and delete, office-staff gated, so misfiled documents can be moved or removed. |
| `1c7f1410` | Andrew Page | 07-16 | New Feature | `js/messaging.js` L347 / L290 / L295 / L428-434, chips L386-392, send L578-579 | Photo/PDF attach shipped S151b/S158 and video shipped S166; `msgAttach` accepts image/pdf/video with multiple, capped at 5 with removable chips. DB row is now status=done. |
| `3f254929` see Data | - | - | - | - | (listed in the Data section) |
| `d432863a` | Brandon | 07-21 | General UI/UX | all repo `*.html` heads; index.html L263 marker | DONE in v1.498 (S178). Favicon plus apple-touch-icon markup is in every page head across 13 pages. Also closes the long-standing S139 favicon-404 console noise. Previously bucket=needed/S. |
| `d4d0eeba` | Brandon | 07-22 | General UI/UX | index.html L4342-4344; `js/qr.js` L198-214 | DONE in v1.497 (S178) - both navigation paths fixed. The Clear Filters toast action retries `scrollToROInBoard` after the re-render and reports an archived/missing RO; the messages `?ro=` deep link clears filters once behind a one-shot guard. Also closes declined duplicates `9031e101` and `1bc33dd6`. Previously bucket=bug. |
| `9d3806e7` | Brandon | 07-22 | General UI/UX | `messages.html` detailNoteView L671, edit L669, openNotesModal L1599, write L1624; `customer_note_entries_s154.sql` | The details pane has a customer-side Notes section plus an Add-Notes modal writing append-only rows keyed by phone. DB row is now status=done. |
| `ac394468` | Roland | 07-24 | General UI/UX | `js/enhancement.js` pageDefaults L43 | Delivered v1.482: the erCategory select carries Messages/Texting plus Solar, Leads, Closed ROs, Reports, Time Off and Employee Guide. RESIDUAL, stated not hidden: erCategory exists in only 6 live HTML files, so messages / leads / time-off / guide cannot RAISE an ER - worth its own small ER. |
| `d62a26e4` | Roland | 07-24 | New Feature | `js/messaging.js` L347 / L290 / L295 / L428-435, chips L386-392, send L578-579 | Delivered v1.482 / S158a: multi-attach up to 5 with removable chips; the send path splits video links from MMS media because a carrier payload cap silently stripped multi-media sends. |
| `93b00023` | Roland | 07-24 | New Feature | `textly-webhook` v1.4 L487 / L514-523 / L617; `send-unreplied-reminder`; `unreplied_eod_reminder_s158.sql` | Delivered v1.482, all three parts: inbound reopen with a `reopened` event row, owner notify only on the needs-reply flip, and the 4:30pm unread sweep. v1.4 disabled only the UNASSIGNED blast, not this ask. |
| `d94a418a` | Brandon | 07-27 | General UI/UX | `js/render.js` formatNotesNewestFirst L33-39, comment L28-32 | DONE in v1.497 (S178). Splits on the entry delimiter and reverses ENTRIES, not lines; the old split-newline/reverse that flipped every line inside a note is gone. Applied to both the RO Status box and Customer Comm. Previously bucket=bug. |
| `50175fce` | Lynn | 07-27 | General UI/UX | `js/ro-crud.js` editUrgentUpdate L404, export L1551; index.html edit-urgent wiring L3968 | DONE in v1.498 (S178). The red URGENT banner is clickable to edit or clear; the write targets the RO UUID, is audit-logged, and still fires `urgent_update_notify`. Follow-on to `a7d1474e`. Previously bucket=needed/S. |
| `09084bc5` | Lynn | 08-04 | General UI/UX | `js/ro-crud.js` L662-684 `archive_single_ro`; `cashiered_full_detail_s171.sql` L32-38 / L95-108 / L114-141 / L144; `closed-ros.html` v1.3b L898-904 | FIXED in v1.494 (S171); bucket moved bug -> done. The archive is a manager-gated RPC that writes the 7 previously dropped columns plus all seven child mirrors before the delete, and reactivate restores them. |
| `225a2535` | Brandon | 08-05 | Bug Report | `js/insurance.js` L127-129 roKey; renderCustomInsuranceFields L47-49; index.html L6633 / L6810; `js/ro-crud.js` L1376-1378 | DONE in v1.497 (S178) - the cross-customer bleed is closed. `_lastEstimateScan` is stamped with `roKey = editingROSupabaseId` and every consumer gates on it; stale chips are cleared at Edit open. Previously bucket=bug. |
| `ac8265c8` | Lynn | 08-05 | General UI/UX | `add_mileage_s178.sql`; index.html L7670 / L6743 / L6766; `js/ro-crud.js` L567 / L568 / L1294; `js/render.js` L523-526 | DONE in v1.498 (S178). `repair_orders.mileage` plus the cashiered twin column, Edit RO input beside VIN, audit label, and a card readout under the VIN. Previously bucket=needed/S. |
| `5ca724ff` | Lynn | 08-06 | General UI/UX | `js/work-list.js` L418-425 | DONE in v1.497 (S178). `dragstart` now calls `e.dataTransfer.setData(...)` and sets `effectAllowed='move'`, which is what Firefox and some Chrome paths require. The touch path is untouched. Previously bucket=bug. |
| `fa35e3df` | Lynn | 08-18 | General UI/UX | `css/dashboard.css` L560 / L609 / L1210 / L3039 / L1930; `js/render.js` L26 | DONE in v1.498 (S178). Low-contrast blues lightened for the dark card surface: `#3b82f6` -> `#7eb3ff`, `.log-duration` `#667eea` -> `#a5b4fc`, key-date Drop chip `#0a84ff` -> `#5cadff`. All five sites carry the `[ER fa35e3df v1.498 S178]` marker. Previously bucket=needed/S. **Lynn has since re-raised the Drop chip as `6ff111e7` (still too small, still too pale).** |

*(Two further `done`-bucket rows, `6938c1af` and `3f254929`, are listed in the
Data section above rather than repeated here.)*

## Not triaged, no verdict on record (1)

| ER | By | Date | DB status | Note |
|---|---|---|---|---|
| `f73a1e5a` | Roland | 04-13 | declined | "Testing dictation 123 testing enhancement request dictation" - has never appeared in `needs_triage_ids` and carries no `triage_bucket`. Reads as a test entry, already declined. Worth including in one full pass purely to give it a recorded verdict. |

---

## Recommended action today

1. **Run a FULL pass before scheduling any Notifications/Task-Tracking or
   check-tracking work.** S185 (`ro_receivables`) and S186 (the task manager:
   `tasks.ro_uuid`, `task_events`, `ro_activity_feed`, `send-task-reminders`)
   landed after every carried verdict was stamped. `af14b3cb`, `5925fc01` and
   `a195e4ab` all still read "confirmed absent, zero grep hits" and that is very
   likely wrong now. This is the highest-value item on the list precisely because
   it removes work rather than adding it.
2. **No bug branch this week.** The bug bucket is empty for the first time - the
   v1.494 and v1.497 batches closed all five. Nothing jumps the queue.
3. **Ship the new ER `6ff111e7` with any next cosmetic touch.** It is render/CSS
   only: raise the Drop chip in `js/render.js` L468 to at least the Promised
   chip's `0.78rem / 800` and pick a hue with more presence than `#5cadff` on the
   `#1a1d24` card, then mirror it into the `kd-pop-row` at L436. Worth a quick
   word with Lynn on which color she wants, since v1.498 already changed it once
   and she came back.
4. **Close out the paperwork - two rows, no code.** `611589ba` is still
   `unreviewed` and folds cleanly into `972af03c` + `44419de9`; recommend
   `declined` with a pointer. `f73a1e5a` should be picked up by the next full
   pass so it stops being a permanent blank.
5. **The status batch is still the biggest cheap win.** `972af03c` (Canceled),
   `3e746c18` (Totaled Out) and `44419de9` (4 estimate/decline statuses) touch
   the same 9 hardcoded spots and both DB CHECK twins. One migration, one
   release, and `611589ba` + `eb00a68d` fold in. Confirm the final name list with
   Lynn first - `44419de9` alone would take the canonical set past 20.
6. **One messaging-polish build still covers five S items:** `f1e6e311`,
   `21fc3a9d`, `9aab7550`, `6e9ca4dc` and `ca0f7ed9`, all confined to
   `messages.html` plus `js/messaging.js`.
7. **Two parts items are a matched pair:** `2af47890` (client half never sets
   `part.dateReceived`, so an edit wipes the trigger's stamp) then `1e434aff`
   (render the date in the table). Build in that order; `2af47890` is the one
   with a live data-loss edge.
8. **Unblock the underspecified S's by asking the submitters:** Lynn's parts
   field order (`8d885331`) and time-off ask (`47428f75`), and Riley's check-in
   float (`e06d747e`, fold into `6bb61db2`).
9. **Confirm scope on `a03677e6` before building.** Retargeting machine audit
   notes from `ro_status` to `customer_comm` is mechanical, but Roland reversed
   exactly that retarget for LEAD NOTES in v1.491 - get his call on which lines
   move and whether historical rows are backfilled.
10. **Plan, do not rush, the XLs:** the auth swap (`ed1b873e`) and parts
    inventory (`14f35f95`) each deserve their own scoping session.
