# Enhancement Request Triage — 2026-06-07 (Session 94)

**Source:** `enhancement_requests` table (live prod pull via authenticated dashboard, v1.446).
**Total ERs:** 37 (incl. 3 test entries + 1 exact duplicate → **33 real requests**).
**Method:** each ER contextualized against the current modular codebase (post-roadmap, v1.446) and sorted into:

- ✅ **DONE** — already delivered by the refactor or prior feature work; ER can be closed.
- 🐞 **BUG** — a defect/regression; verified whether it still exists post-refactor.
- 🛠 **NEEDED** — still a valid open enhancement, with a level-of-effort (LOE) estimate.

LOE key: **S** = <2h · **M** = ½–1 session · **L** = multi-session · **XL** = new subsystem.

---

## Headline

The serious bugs are already dead. The refactor (and the fixes that rode along with it) resolved every defect-class ER except cosmetic/data items:

- **n5** "work list — couldn't view or do anything once added" → fixed in **Phase 14** (`_workListActiveSilo` ReferenceError, Session 89).
- **n10/n11** "can't delete jobs" → Work List now has a working **× remove** button (`removeFromWorkList`).
- **n12** "different work lists per category" → delivered via **per-silo Work List tabs**.
- **n17** "Ready to Work status" → shipped **v1.410**.
- **n2** "Warranty category on new RO" → shipped **v1.402**.

What's left in the bug bucket is **one cosmetic display item (n33)**, **one small check-in UX gap (n22)**, and **one data correction (n25 — staff name spelling)**. None involves data loss.

---

## 🐞 BUG bucket (front of the line)

| # | Submitter | Request | Still exists? | Verdict / fix | LOE |
|---|---|---|---|---|---|
| n33 | Bobby Thatcher | "Amounts drop the period and the zeros — I put 425.00, come back and it says 425" | **Cosmetic only** | **No data loss.** `dollarValue` and part prices use `parseFloat` (value preserved) and the parts table renders `$425.00` correctly via `.toFixed(2)`. The "425" only appears in the editable `type=number` inputs (edit-RO modal `editDollarValue`; part form `pf_*`), which don't show trailing zeros. **Fix:** format currency inputs to 2 decimals on populate + on blur. Low risk. | S |
| n22 | Tipton Scott | iPhone: after clocking out of a job there's no way back to the dashboard — have to open a new tab | **Likely yes** (check-in flow) | Real UX gap on `checkin.html` — add a "Return to Dashboard" button/link on the post-clock-out screen. | S |
| n25 | Ryan Dillon | "Zak, Rod and Travis last name should be Wombles, not Wimbles, in tech assignment" | **Yes — data, not code** | `staff.name` holds `Rod/Zak/Travis Wimbles`. Not hardcoded anywhere. **Fix = 1 SQL UPDATE** (provided below). Historical ROs storing the tech name as text are a separate optional backfill. | S (data) |

**n25 fix SQL** (provided inline — no Supabase connector to auto-run):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

Already-resolved bug reports (close these): **n5, n10/n11 (delete portion)**.

---

## ✅ DONE bucket (close these ERs)

| # | Submitter | Request | Evidence |
|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry (already *declined*). |
| n14 | Roland | "Testing ER form / dictation" | Test entry (already *done*). |
| n15 | Roland | "Testing dictation 123" | Test entry (already *done*). |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivate → edit/add docs (already *done*). |
| n2 | Ryan | Warranty category on new RO | Warranty RO type, **v1.402** (already *done*). |
| n5 | Lynn | Work list opened but couldn't view/do anything | Fixed **Phase 14** (Session 89). Mark done. |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs`). Mark done. |
| n17 | Lynn | Add "Ready to Work" RO status | Shipped **v1.410** (currently mislabeled *in-progress*). Mark done. |

**Verify-with-Roland (lean DONE):**

| # | Submitter | Request | Note |
|---|---|---|---|
| n21 | Roland | Fix customer check-in, ensure submitted data emails out, remove photo, streamline | `customer-checkin.html` was overhauled across Sessions 71/80 (lead-staff email, RAF). Confirm the photo-removal + email-send expectations are fully met, then close. |

---

## 🛠 NEEDED bucket (open enhancements + LOE)

Grouped by theme — many ERs overlap and should be designed together rather than one-off.

### Theme 1 — Scheduling & key dates  *(n4, n24, n29, n36)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n4 | Lynn | Scheduled future drop-off date + email reminder to manager 1 day before | M |
| n36 | Lynn | Scheduled drop date field in entry dashboard | S — *duplicate of n4* |
| n24 | Brandon | Completion / pickup date on the customer's order for reference | S/M |
| n29 | Lynn | Promised date large + highlighted on tile; sort by promise date then days-on-lot | M |

→ **Recommend one "Key Dates" mini-initiative** covering drop-off, promised, completion/pickup + tile display + sort.

### Theme 2 — Notifications, reminders & task tracking  *(n3, n8, n20, n30, n32, n35)* — the biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ("Jesus needs to pick up window…") | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify on modify + font/color | M/L (urgency partly exists) |
| n35 | Lynn | Transfer clock-in job notes → RO updates | M |

→ **Largest opportunity.** A "Task Tracker + Reminders" subsystem. Overlaps the deferred Twilio/SMS work — design as one initiative.

### Theme 3 — Parts  *(n6, n7, n9, n27, n28)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n28 | Brandon | Parts + freight **total** in Manage Parts | **S — confirmed not built; clean quick win** |
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned to supplier / warrantied by supplier | M/L |
| n27 | Lynn | Parts missing / ordered-and-returned scenario (Gordon) | S — fold into n7 |
| n6 | Lynn | Parts inventory / in-house stock (order 25, 20 to stock, searchable) | XL — new subsystem |

### Theme 4 — Work List usability  *(n10/n11 residual, n31)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n10/n11 | Riley | Mobile: too small to adjust on phone; drag-and-drop reorder | M |
| n31 | Lynn | Returning check-in auto-adds customer to each involved team lead's work list | M/L |

### Theme 5 — RO fields & status nuance  *(n18, n34, n26, n19, n16)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n18 | Lynn | Fields: "keep plugged in" flag; keys / no keys / keypad code | S/M |
| n34 | Brandon | Confirm if customer leaves keys with the unit (RO / sign-in / auth form) | S — overlaps n18 |
| n26 | Brandon | On-lot sub-status for units temporarily taken & coming back vs never-on-lot | S/M |
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs existing Hybrid) |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |

### Theme 6 — Work Orders / Templates  *(n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will | M |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague — overlaps n22) |

---

## Recommended action today (single gated deployment)

1. **n33** — currency inputs show 2 decimals (cosmetic, low risk).
2. **n22** — "Return to Dashboard" button after clock-out on `checkin.html`.
3. **n25** — run the staff-name SQL (Roland; data only).
4. Mark resolved ERs **done** in the table: n5, n10/n11, n12, n17 (and n21 after confirmation).

Everything else is enhancement work to be scheduled. The two standout near-term wins beyond bugs: **n28 (parts+freight total, S)** and a coordinated **Key Dates** pass (Theme 1).
