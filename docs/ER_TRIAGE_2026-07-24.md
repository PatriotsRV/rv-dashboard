# Enhancement Request Triage -- 2026-07-24 (Phase 2a nightly runner)

**Source:** enhancement_requests table (snapshot er_input.json, fetched 2026-07-24T09:23Z; incremental pass).
**Codebase mapped against:** current pre-prod tree at **v1.481**.
**Total ERs:** 81 -- 80 triaged this run (ids in needs_triage_ids) + 1 carried over unchanged (`f73a1e5a`, declined; no prior verdict).
**Buckets (80 triaged):** 50 Done - 21 Needed - 6 Duplicate - 2 Bug - 1 Data.

Every ER was mapped to current code (js/ modules, index.html, checkin.html, customer-checkin.html, supabase/functions, SQL migrations) by grepping/reading the source, not assuming. Untrusted ER text (description / submitted_by_name / admin_notes) was treated strictly as data; no ER contained prompt-injection content.

LOE key: **S** = under 2h - **M** = half to one session - **L** = multi-session - **XL** = new subsystem.

---

## Headline

- The **bug bucket has 2 items**, both small navigation defects with the **same root cause** (`9031e101` + `d4d0eeba`): tapping an RO from the Work List or a messages ro-chip link silently fails / toasts "RO not visible" when the target RO is filtered out or not yet rendered. Fix once, close both. Front of the line.
- **Two prior open ERs flipped to Done** since the 2026-06-28 pass: messaging media attachments (`1c7f1410`, shipped S151b) and a customer-side Notes section in the messages pane (`9d3806e7`, shipped S154 v1.12b). Both verified in current code this run.
- **21 open Needed enhancements** across 10 themes. The mix skews small -- **2 XL - 4 L - 2 M - 13 S** -- a large cluster of quick messaging/UI wins plus the two strategic XLs (Auth swap, Parts inventory) that still need their own scoping session.
- **1 Data** correction (`3f254929`): remove the misspelled "Steve Hubert" RO after Roland confirms the id; the account-merge feature it also asks for is tracked as Needed `0e172b2c`.

---

## BUG bucket (front of the line)

| ER | By | Request | Verdict (severity + fix) |
|---|---|---|---|
| `9031e101` | Riley | Tapping a unit on iPhone from the Work List does not open the right unit | Bug (mobile, small): work-list.js nameSpan tap -> scrollToROInBoard (index.html L4047) only locates already-rendered cards; if the RO is filtered/off-screen it toasts "RO not visible" (now with a Clear Filters action) instead of navigating. Same root cause as `d4d0eeba`; fix = unfilter + scroll to the tapped RO. |
| `d4d0eeba` | Brandon | Fix tracking when clicking an RO in my list or a link in messages | Bug (navigation, small): clicking an RO from the work list (scrollToROInBoard, index.html L4047) or a messages ro-chip link (handleDeepLink js/qr.js L198, if(!card)return) silently fails when the RO is filtered out / not rendered. Same root cause as `9031e101`; fix both -- unfilter + scroll to target. |

## DATA bucket

| ER | By | Request | Fix (SQL handed to Roland; runner does not execute) |
|---|---|---|---|
| `3f254929` | Sofia | Delete the misspelled "Hubert" account; keep "Steve Huebert" | Data correction: keep Steve Huebert, remove the misspelled Hubert RO. `SELECT id,customer_name,status FROM repair_orders WHERE customer_name ILIKE '%hubert%';` then soft-delete the wrong RO after Roland confirms the id. The account-merge feature is Needed `0e172b2c`. |

## NEEDED bucket -- open enhancements by theme

Overlapping wishes are clustered so related ERs get designed together.

### Notifications & Task-Tracking  (2)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5925fc01` | Lynn | **L** | Notify the right team on every RO update; daily digest of all in-progress jobs with to-dos scraped from status notes | Partial: the daily in-progress digest is served by the Manager Daily Report (send-manager-report), but notify-on-every-RO-update and to-do scraping from status notes are unbuilt. Anchor of the initiative. |
| `af14b3cb` | Lynn | **L** | Per-customer task tracker with a daily reminder; tasks persist until marked complete | Confirmed absent: shop-tasks.js is a non-customer checklist with no ro_id/customer link or reminder column. Needs a per-RO tasks table + add/complete UI + a daily open-task reminder. Pairs with `5925fc01`. |

### Parts & Inventory  (3)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `14f35f95` | Lynn | **XL** | In-house parts inventory: order 25, use 5, 20 go to searchable priced stock | Confirmed absent: js/parts.js has no inventory/stock symbols; parts are per-RO line items only. New subsystem: inventory table + receive/consume/adjust + searchable priced stock + parts-request links. Largest board item. |
| `b19975d4` | Lynn | **L** | Track a part returned to supplier or warrantied by supplier | Partial infra only: config.js PART_STATUSES has Returned + return/warranty date fields, but no RMA#, credit tracking, return reason, or reverse flow. = GH#24 Shop Ops Phase 2. Canonical for declined dup `86bbaf50`. |
| `8d885331` | Lynn | **S** | Rework the field order on the parts-entry / ordering screen | Parts-entry form exists (parts.js); the ask is only to reorder fields. Quick once Lynn provides the target layout/mockup. Blocked on detail, not on effort. |

### RO fields & status  (3)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5fbfa299` | Brandon | **M** | Archive quoted-but-never-progressed customers/units | Confirmed absent: only the cashiered archive exists (ro-crud.js archiveROInSupabase); no archived/abandoned status flag or filter for quoted-but-never-progressed units. Pairs with `972af03c`. |
| `0e172b2c` | Sofia | **M** | Merge duplicate customer accounts (e.g. Hubert / Huebert) | Partial: duplicates.js has an RO-level merge/delete engine (openDuplicateManager/executeDupeMerge) but groups by base RO id, not customer identity. Needs name/phone account-dedup + verify-which-is-correct merge/delete. Data cleanup for this case is `3f254929`. |
| `972af03c` | Lynn | **S** | Add a Canceled status; keep the record for notes, do not delete | Confirmed absent: no Canceled/Cancelled in render.js ALL_STATUSES/statusColorMap or config.js. Small: add a Canceled status + color/priority maps, exclude from on-lot/queue counts, keep the record. Pairs with `5fbfa299`. |

### Messaging  (4)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `9aab7550` | Brandon | **S** | Highlight new/unchecked messages more clearly | Partial: messages.html already renders a gold unread-dot per conversation, but there is no row-level highlight for unchecked messages. Small: add a stronger unread treatment (bold row / tinted background). |
| `21fc3a9d` | Brandon | **S** | Rescale conversation-list text for readability | Confirmed absent: messages.html uses fixed rem font sizes (html 17.5px) with no user text-size control. Small: add a persisted font-scale/zoom for the conversation list. |
| `f1e6e311` | Brandon | **S** | A button to clear the messaging search bar faster | Confirmed absent: messages.html convoSearch is a plain text input with no clear/x button. Small: add a clear (x) button to reset the messaging conversation search quickly. |
| `6e9ca4dc` | Brandon | **S** | Remove the redundant "Added to every send" signature note at chat bottom | Valid: messaging.js refreshSignaturePreview renders a persistent "Added to every send:" signature preview (msgSigPreview) under the composer; users find it redundant. Small: remove or collapse the always-on preview. |

### E-sign & Auth form  (2)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `0448de62` | Sofia | **L** | Send an authorization form remotely for e-signature | Confirmed absent: signing is kiosk-only (customer-checkin.html on-device signature pad); raf_copy is only a post-signing email. Needs a tokenized public signing page + delivery + signed-form storage. Pairs with Sendblue Phase 3. |
| `5fec3eca` | Sofia | **S** | Show the customer address on the emailed copy of the authorization form | Confirmed absent: send-quote-email raf_copy branch (L387) does not destructure/emit customerAddress; the address appears only in the lead_staff_notify branch. Small: add customerAddress to the raf_copy payload + template. |

### Time clock  (2)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `e06d747e` | Riley | **S** | Tech check-in "float" for ease of access | Confirmed absent: no floating/quick tech check-in button in index.html or checkin.html. Underspecified; scope the page and behavior with the submitter. Pairs with `6bb61db2`. |
| `6bb61db2` | Riley | **S** | Compact-view check-in/out button + a login-time "clocked into X" reminder across views | Confirmed absent: render.js compact-col-actions has no check-in/out button and index.html shows no persistent clocked-into-X indicator. Small: add a compact check-in/out action + a login-time active time_log banner. Pairs with `e06d747e`. |

### Work Orders  (1)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `2f752d40` | Jason | **S** | Auto-populate the WO dollar amount from total hours x labor rate ($195/hr) | Confirmed absent: work-orders.js has manual estimated_hours + dollar_value fields with no auto-calc. Small: auto-populate WO dollar_value = total hours x posted labor rate ($195/hr), editable override. |

### Scheduling & Dates  (1)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `d1844e46` | Lynn | **S** | Sort dash by promised date then oldest on lot, shop ops pinned to top | Confirmed absent: board uses a single blended calculatePriority score; shop-ops pinning exists (render.js) but no selectable strict promised-date-then-oldest-on-lot sort. Small: add the strict sort option. |

### Auth & cost  (1)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `ed1b873e` | Roland | **XL** | Replace Google-email auth (~$500/mo) with database-specific auth | Confirmed still Google-based: auth.js signInWithIdToken + google.accounts One Tap + GOOGLE_CONFIG; kiosk too. Swap to Supabase password/magic-link or shared role accounts to drop ~$500/mo; touches every page sign-in + RBAC. Roland to scope. |

### General UI/UX  (2)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `29654af7` | Lynn | **S** | Back-to-top / jump-to-search button when scrolled down | Confirmed absent: index.html has only targeted scrollIntoView calls, no page-level scroll-to-top / jump-to-search button. Small: a scroll-triggered back-to-top / jump-to-search FAB. |
| `d432863a` | Brandon | **S** | Make the browser tab icon the Patriots logo | Confirmed absent: no favicon / apple-touch-icon / rel=icon in index.html or any page, so the tab shows a generic icon. Small: add a Patriots-logo favicon + apple-touch-icon to each page head. |

---

## DUPLICATE bucket

| ER | By | Request | Canonical |
|---|---|---|---|
| `aa508852` | Riley | Can't delete jobs / too small on phone / no drag-drop | Identical text/timestamp to `fd6c122d` (now Done). Fold into `fd6c122d`. |
| `86bbaf50` | Lynn | Gordon parts missing / window ordered and returned to Vogt | Same as `b19975d4` (supplier return/warranty tracking). Fold into `b19975d4` (Needed/L). |
| `67daba15` | Brandon | RO completion / pickup date for records | Same as `d2561e11` (now Done: pickup_date field + Pickup chip). Fold into `d2561e11`. |
| `fa1c1d6e` | Sofia | Returning check-in did not email (Sheldon Gilbert) | Same as `a22739b9` (now Done: RAF + staff email forced on every check-in). Fold into `a22739b9`. |
| `5057c7fc` | Roland | Remove cashed-out RO from the worklist the day after Delivered | Same as `91a03260` (now Done via prune_cashiered_worklists). Fold into `91a03260`. |
| `fc6b29f9` | Sofia | Returning-customer test duplicated the RO | Retest of `298e0e67` (now Done: check-in reuses the Scheduled RO). Fold into `298e0e67`. |

## DONE bucket (already delivered -- recommend status=done; test entries recommend declined)

| ER | By | Date | Request | Evidence (current code) |
|---|---|---|---|---|
| `17492066` | Lynn | 2026-04-09 | Future notifications ahead of time (e.g. Jesus picks up window) | Delivered via Schedule Notification: per-RO bell sets date/time, message and recipients; supports daily/weekly repeat until a chosen date. |
| `18611eee` | Lynn | 2026-04-09 | Work list opened but could not view/do anything | Fixed (Phase 14): work-list.js renderWorkList + removeFromWorkList + _workListActiveSilo; members viewable/removable once added. Was a bug, now resolved. |
| `33750cb3` | Roland | 2026-04-09 | Testing the Make-A-Wish function | Test entry; not a real request. No code action. Already declined. |
| `75124b03` | Ryan | 2026-04-09 | Warranty category on new RO creation | Delivered v1.402: Warranty RO type on new RO creation (index.html warranty filter + ro_type branch in ro-crud.js). |
| `79b2c805` | Sofia | 2026-04-09 | See/edit cashed-out (closed) customers, add documents | Delivered: closed-ros.html reactivation restores full RO detail (parts/time_logs/photos) so cashed-out customers reopen to edit and add documents. |
| `98bd7498` | Brandon | 2026-04-09 | Different work lists for jobs under different categories | Delivered: per-silo Work List tabs (work-list.js _renderWorkListSiloTabs/_setWorkListSilo) give different lists per category. |
| `b2aa4bea` | Lynn | 2026-04-09 | Date added for scheduled future drop | Delivered: scheduled future drop-off date field with 1-day-before manager email reminder (Key Dates pass). |
| `fd6c122d` | Riley | 2026-04-09 | Cannot delete jobs / too small on phone / no drag-drop | Delivered: work-list.js delete (removeFromWorkList) plus mobile sizing and drag-drop reorder. Canonical for declined dup `aa508852`. |
| `19dc2016` | Roland | 2026-04-11 | Testing ER form / dictation | Test entry; not a real request. No code action. Already declined. |
| `1fe68261` | Rod | 2026-04-13 | Tasks not tied to a customer unit (swap poly barrels, prep roof bays) | Delivered dashboard v1.467 (S128): Shop Tasks panel (js/shop-tasks.js) for internal work not tied to a unit; standalone list separate from ROs/reports. |
| `3dfc0acb` | Lynn | 2026-04-14 | Add a "Ready to Work" RO status | Delivered v1.410: Ready to Work RO status (STATUS_PROGRESS_MAP slot 50, all 4 status dropdowns). |
| `b87eb2fb` | Lynn | 2026-04-14 | Keep-plugged-in flag; keys / no keys / keypad code | Delivered: Keys field on RO (Keys/No keys/Keypad + code box + keep-plugged-in checkbox + keys chip). See `34fc03c2`. |
| `a5ff3e2d` | Lynn | 2026-04-21 | Combined "Warranty + Repair" RO type | Delivered v1.465: Warranty + Repair RO type; keeps Original RO#/Warranty Reason but is still billable (not zero-dollar) so repair bills normally through work orders. |
| `a7d1474e` | Lynn | 2026-04-23 | Always-visible urgent-updates field on tile + notify-on-modify | Delivered v1.466: always-visible red URGENT banner on the RO card (Urgent Update field); on set/change it emails the silo manager(s) + admins. |
| `dde90d2b` | Roland | 2026-04-27 | Fix customer check-in, force email out, remove photo, streamline | Delivered: customer-checkin.html v1.6 made the photo optional and forces the RAF/lead-staff email on every submission (S50/71/80). |
| `13e1cd87` | Tipton | 2026-04-30 | iPhone: no way back to dashboard after clock-out | Delivered v1.447 (checkin.html v1.34): Return-to-Dashboard button after clock-out. Was a bug; now fixed. |
| `e27311bf` | Ryan | 2026-05-04 | Edit WO templates; drag-and-drop reorder | Delivered: Build/Edit Work Order form has grip handles to drag-reorder task rows; edit a template by loading, reordering/changing, then Overwrite (work-orders.js). |
| `6938c1af` | Ryan | 2026-05-06 | Zak/Rod/Travis last name Wimbles -> Wombles in tech assignment | Data correction now applied (staff.name, not hardcoded). `UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |
| `d2561e11` | Brandon | 2026-05-06 | Completion / pickup date for reference on the order | Delivered: completion/pickup date field + Pickup key-date chip. Canonical for declined dup `67daba15`. |
| `dac9fdda` | Brandon | 2026-05-06 | On-lot sub-status: temporarily taken/returning vs never-on-lot | Delivered: on-lot sub-status distinguishing units temporarily taken and returning from units never on the lot yet. |
| `a8e90a7d` | Brandon | 2026-05-20 | Parts + freight total in Manage Parts | Delivered v1.456 (S117): Manage Parts Ticket Totals include a summed parts + freight total (parts.js). |
| `1aeb3f58` | Lynn | 2026-05-21 | Promised date large/highlighted on tile; sort by promise date then days-on-lot | Delivered: promised date large/highlighted on the main tile + sort by promise date then days-on-lot. |
| `cbc70a86` | Lynn | 2026-05-21 | Returning check-in auto-adds customer to each involved team-lead work list | Delivered Customer Check-In v1.14 (S128): drop-off/scheduled check-ins auto-add the customer to each involved team Manager Work List; unassigned silos route to sr-managers. |
| `3f415c88` | Lynn | 2026-05-26 | Notification button: schedule multiple days / weekly-until-complete | Delivered: Schedule Notification Repeat (Daily/Weekly) + Repeat-until date creates one reminder per occurrence, each individually cancellable. |
| `cd663d8f` | Bobby | 2026-05-28 | Amounts drop the period and zeros (425.00 -> 425) | Fixed v1.447: currency inputs show 2 decimals on blur and edit-populate (ro-crud.js editDollarValue toFixed(2); parts.js MONEY_FIELDS). Was a cosmetic bug, no data loss. |
| `34fc03c2` | Brandon | 2026-05-29 | Confirm whether the customer leaves keys with the unit | Delivered: Keys selector on New/Edit RO (Keys left/No keys/Keypad + keypad/lock code box + keep-plugged-in) with a keys chip on each card. Covers the keys asks (`b87eb2fb`). |
| `0b2e128c` | Lynn | 2026-06-02 | Transfer clock-in job notes into RO updates | Delivered: clock-in job notes transfer into repair order updates. |
| `468aa376` | Lynn | 2026-06-02 | Scheduled drop-date field in entry dashboard | Delivered: scheduled drop-date field in the entry dashboard (Key Dates pass). |
| `2b814250` | Roland | 2026-06-07 | RO date/time as a colored RO button + business-calendar integration | Delivered: RO date/time (pickup/dropoff/reminders) surfaced on the RO + business-calendar integration with advance notifications to managers/admins. |
| `cd084233` | Roland | 2026-06-07 | Admin-level daily Manager-worklist report | Delivered: admin-level daily Manager-worklist report showing manager activity, parts/labor and overall costs. |
| `a22739b9` | Sofia | 2026-06-08 | Check-in no longer emails staff/RAF copy | Fixed: check-in now forces the RAF + staff email on every submission (was a notify-gate regression in customer-checkin.html). Canonical for declined dup `fa1c1d6e`. |
| `91a03260` | Roland | 2026-06-09 | Remove cashed-out RO from a manager worklist the day after Delivered | Delivered via prune_cashiered_worklists: an RO is auto-removed the day after it is marked Cashiered Out/Delivered. Canonical for declined dup `5057c7fc`. |
| `cdd77a8b` | Brandon | 2026-06-10 | Split "Total RVs" between on-lot and total customers | Delivered: dashboard stat splits RVs currently on lot vs total customers (render.js statTotal/totalRVs). |
| `2c3d5633` | Ryan | 2026-06-12 | Cannot upload pictures from phone | Fixed: phone photo upload now completes (photos.js uploadPhoto). Was a mobile-only functional bug. |
| `298e0e67` | Sofia | 2026-06-16 | Returning check-in duplicates a future scheduled-drop RO | Fixed: returning check-in for a customer with a future Scheduled-drop RO now reuses that RO instead of inserting a duplicate. Was a data-integrity bug. Canonical for declined dup `fc6b29f9`. |
| `74a33621` | Brandon | 2026-06-16 | Arrow-key navigation through photos | Delivered: arrow-key navigation through the photo lightbox (photos.js openPhotoLightbox prev/next + keydown). |
| `9b823d25` | Roland | 2026-06-16 | Email the silo manager when another silo adds a service to an RO | Delivered: email to the silo manager when another silo adds a service to an RO (e.g. Roof manager adds a hot-water-heater service -> RV Service manager notified). |
| `9b046808` | Brandon | 2026-06-17 | Add a "scheduled to arrive on lot" status | Delivered: a scheduled-to-arrive-on-lot status so units with a future drop-off time are not counted as already on the lot. |
| `671fa623` | Brandon | 2026-06-18 | Manage Parts ticket totals should include freight | Delivered v1.456 (S117): Manage Parts Ticket Totals include a Freight line = sum(core_charge per part) (parts.js L153/L160). |
| `5c45bb8d` | Sofia | 2026-06-19 | Scheduled returning customer still duplicated + no RAF email | Fixed: a Scheduled returning customer no longer gets a duplicate RO and now receives the RAF email (David Flanagan report). Was a data-integrity + missing-email bug. |
| `88395acc` | Lynn | 2026-06-19 | Time-off exact window (half-day morning/afternoon, from-to times) | Delivered Time Off v1.5: partial-day window (Morning/Afternoon half-day or Custom clock-out/return times) instead of a total of hours. |
| `b06a285f` | Lynn | 2026-06-19 | Hover near customer name to show important dates | Delivered v1.465: calendar icon by the customer name shows all key dates (drop-off/promised/pickup) on hover/tap; chips shipped v1.457. |
| `ee50d11c` | Lynn | 2026-06-19 | Cannot edit time-off entries | Delivered: time-off entries are now editable (modify day/person/type), not delete-only (time-off.html). |
| `80390f36` | Lynn | 2026-06-23 | Pin Shop Operations tile to top + make it findable by search | Delivered v1.462: Shop Operations RO tiles pinned to the top of the board and reliably found by the search box. |
| `7f2d0d70` | Lynn | 2026-06-26 | Monday report of the week drop-offs cross-checked vs worklists | Delivered: automated Monday 7am CDT Drop-Off Check email (this week scheduled drops + last week received + flags any not yet on the right team work list). Pairs with `cbc70a86`. |
| `3f7fb4fc` | Brandon | 2026-06-29 | Remove shop RO status dropdown; do not count in queue/on-lot | Fixed v1.468: Shop Operations ROs no longer counted in queue/on-lot and their status dropdown removed (render.js). Was a small data-display bug. |
| `cb7742a8` | Lynn | 2026-06-29 | New check-in does not post the scheduled date to the team calendar | Delivered v1.469 (check-in v1.15): server-side sync posts scheduled/promised/pickup dates to the team Google Calendar for every RO, incl. front-desk check-ins. |
| `39b75fab` | Sofia | 2026-07-09 | Cannot remove/relocate documents scanned to the wrong RO | Delivered v1.472: Documents tab Rename/Move/Delete + photo Move/Delete (photos.js), office-staff only. The Marco DeAlba docs on the Ruiz RO were moved to his own RO. |
| `1c7f1410` | Andrew | 2026-07-16 | Messaging cannot attach media (photos/videos) | Now delivered (S151b, overrides prior Needed): messaging.js msgAttachBtn + msgAttach -> _compressImage -> upload to message-media bucket -> _msgAttachment; sendCustomerMessage passes media_url to projectblue-send. Verified in code. |
| `9d3806e7` | Brandon | 2026-07-22 | A notes section on the customer side of the messages pane | Delivered (S154 v1.12b): messages.html details pane has a customer-side Notes section (detailNoteView) + Add-Notes modal (openNotesModal/saveCustomerNote) storing append-only entries to customer_note_entries (migration customer_note_entries_s154.sql). Verified. |

---

## Carried over (not re-triaged this run)

| ER | By | Date | Status | Note |
|---|---|---|---|---|
| `f73a1e5a` | Roland | 2026-04-13 | declined | Not in needs_triage_ids; existing triage_bucket/triage_loe/triage_verdict are all null. Left unchanged. |

---

## Recommended action today

1. **Fix the bug class first.** `9031e101` and `d4d0eeba` share one root cause (navigating to an RO that is filtered out / not rendered). One small fix -- unfilter + scroll to the tapped RO in scrollToROInBoard (index.html L4047) and handleDeepLink (js/qr.js L198) -- closes both.
2. **Close the two newly-Done ERs:** `1c7f1410` (messaging media, S151b) and `9d3806e7` (customer-side notes, S154). Set status=done.
3. **Harvest the S-cluster of messaging/UI quick wins:** `d432863a` (favicon), `9aab7550` (unread highlight), `f1e6e311` (search clear-x), `6e9ca4dc` (drop the redundant signature preview), `21fc3a9d` (conversation text scaling), `29654af7` (back-to-top), `972af03c` (Canceled status), `d1844e46` (strict promised-date sort), `2f752d40` (WO auto dollar from hours), `5fec3eca` (address on the RAF copy email). Each is under 2h and mostly independent.
4. **Unblock the underspecified S items** by getting specifics from submitters: `8d885331` (parts-entry field order), `e06d747e` (tech check-in float) and `6bb61db2` (compact check-in + login clock indicator).
5. **Run the `3f254929` data SQL** once Roland confirms the wrong RO id.
6. **Scope, do not rush, the strategic items:** the auth swap (`ed1b873e`, ~$500/mo) and in-house parts inventory (`14f35f95`) are XL and each deserve their own design session; the L items (`5925fc01`, `af14b3cb`, `b19975d4`, `0448de62`) cluster naturally alongside them.
