# PRVS Enhancement-Request Triage — 2026-06-28 (Session 128)

Full pass over the open `enhancement_requests` against the current modular
codebase (index.html **v1.466**). Supabase MCP **read-only** this session —
any DB writes would be handed to Roland as guarded SQL. Verdicts verified by
grepping/reading the live code, not assumed.

## Board snapshot

| Status | Count |
|---|---|
| done | 40 |
| declined | 9 |
| unreviewed | 11 |
| **Total** | **60** |

**11 open** (all `unreviewed`). Headline finding: **every open ER is a valid
"Needed" enhancement — no bugs, no already-shipped items to close.** They were
the residue after S125–S127 cleared the quick wins, so what's left skews large,
but it is **not** uniformly L/XL: the mix is **2 XL · 4 L · 3 M · 2 S**.

## Code checks run this session

- `manager_work_lists` insert in `customer-checkin.html` / `leads.html` — **none** (confirms cbc70a86, 7f2d0d70 not done)
- task-tracker / per-customer todo subsystem — **none** (confirms af14b3cb, 5925fc01)
- parts return-to-supplier / warranty workflow — **none** (confirms b19975d4)
- parts inventory / stock — **none** (confirms 14f35f95)
- weekly drop-off report edge fn — **none** (send-manager-report has only Monday *escalation*, not a drop-off roster)

---

## Verdicts — the 11 open ERs (by theme, then size)

### Notifications & Task-Tracking (the biggest cluster — design together)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `cbc70a86` | Lynn | **M** | Returning check-in auto-adds the customer to each involved team lead's work list | Not done. On commit, map repair_type(s) → silo(s) → manager(s) and insert `manager_work_lists` rows, reusing the dashboard `addToWorkList` logic. Enables 7f2d0d70. |
| `7f2d0d70` | Lynn | **M** | Monday report of the week's drop-offs so teams confirm the customer is in their work list | No drop-off-reconciliation report exists. New weekly edge fn / section listing prior-week `planned_dropoff_date` ROs + target silo/manager. Newest ER (06-26). |
| `af14b3cb` | Lynn | **L** | Per-customer task tracker + daily reminder; tasks persist until marked complete | Needs a per-RO tasks table + add/complete UI + daily open-task reminder. |
| `5925fc01` | Lynn | **L** | Notify the right team on every RO update; daily list of all in-progress jobs with to-dos scraped from notes | **Partial** — the daily-digest half is largely served now by the Manager Daily Report. Still open: per-update notification + auto-scraping to-dos from status notes. |

### Parts & Inventory (second cluster)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `8d885331` | Lynn | **S** | Rework the parts-entry screen field order | Quick once Lynn gives the desired layout. **Blocked on detail, not effort.** |
| `b19975d4` | Lynn | **L** | Part returned-to-supplier / warrantied-by-supplier tracking | = GH#24 Shop Ops Phase 2. Reverse-direction flow + return reasons + credit/warranty tracking. Canonical for declined dup `86bbaf50`. |
| `14f35f95` | Lynn | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable stock w/ price) | New subsystem: inventory table + receive/consume/adjust + searchable stock + parts-request links. Largest item on the board. |

### Work Orders & Shop tasks

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `1fe68261` | Rod | **M** | Track tasks not tied to a customer unit (swap poly barrels, prep roof bays) | Overlaps the Shop Operations RO type, but those are single ROs not a recurring shared-task model. Needs a lightweight shop-task list. |

### Messaging & e-sign

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `0448de62` | Sofia | **L** | Send an authorization form remotely for e-signature | No remote-signing flow (RAF is kiosk-only today). Needs a tokenized public signing page + delivery + signed-form storage. Pairs with Sendblue Phase 3. |

### Auth & cost

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `ed1b873e` | Roland | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | Security-critical swap of the id-token + email-RBAC model (Supabase password/magic-link or shared role accounts). Touches every page's sign-in + RBAC. Roland wants to scope together. |

### Time clock

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `e06d747e` | solar | **S** | Tech check-in "float" for ease of access | Underspecified — likely a floating/sticky check-in button, but page + behavior unclear. Needs scoping with the submitter. |

---

## Recommended action today

1. **Quick scoped wins (M):** `cbc70a86` (auto-add to work list on check-in) +
   `7f2d0d70` (Monday drop-off report) are well-defined, pair naturally, and are
   the most shippable of what's left.
2. **Unblock the two S's** by asking the submitters for specifics: Lynn's parts
   field order (`8d885331`) and solar's check-in float (`e06d747e`). Both are
   small *once specified*.
3. **Plan, don't rush, the XL's:** the auth swap (`ed1b873e`) and parts
   inventory (`14f35f95`) each deserve their own scoping/spec session.
4. Nothing to close as Done/Duplicate this pass.
