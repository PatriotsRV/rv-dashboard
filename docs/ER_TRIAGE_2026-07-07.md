# PRVS Enhancement-Request Triage -- 2026-07-07 (headless nightly runner)

Incremental pass over `enhancement_requests` (63 of 64 rows flagged for triage;
1 carried over). Verdicts mapped against the **current** modular codebase by
grepping/reading the live code -- not assumed. Read-only run: no code changes,
no DB writes, no status flips (Roland flips status himself). Spot-checks this
session re-verified the prior 2026-07-06 code claims and all held.

## Board snapshot (current DB status)

| Status | Count |
|---|---|
| done | 45 |
| declined | 9 |
| unreviewed | 10 |
| **Total** | **64** |

## Triage buckets (this pass, all 64 ERs)

| Bucket | Count | Meaning |
|---|---|---|
| done | 47 | Already delivered / no code action (incl. 3 test entries) |
| needed | 10 | Valid open enhancement -- see themes below |
| duplicate | 6 | Folds into a canonical ER |
| data | 1 | Data correction (already applied) |

**Headline:** every reported *bug* has been fixed by prior sessions -- there is
**no still-existing bug** on the board. What remains open is **10 valid Needed
enhancements** (mix: **2 XL - 4 L - 1 M - 3 S**). Six duplicates and one data
correction can be closed; the rest are delivered.

## Code checks run this session (verification, not assumption)

- `js/parts.js` inventory/stock/on-hand handling -- **0 hits** (confirms 14f35f95 XL, b19975d4 L still open)
- `js/parts.js` parts+freight totals -- present (`totWholesale + totFreight = totCost`, :152-161) -> confirms a8e90a7d / 671fa623 done
- `js/auth.js` -- only `signInWithIdToken` (Google id-token); **no** `signInWithPassword`/magic-link -> confirms ed1b873e XL open
- `js/ro-crud.js archiveROInSupabase` -- inserts into `cashiered` (:654), no lost/quote disposition -> confirms 5fbfa299 M open
- `js/work-list.js` -- 21 touch/drag handlers (drag-drop reorder + touch) -> confirms fd6c122d done
- `customer-checkin.html` -- returning-customer duplicate guard present -> confirms 298e0e67 done
- `index.html` -- **no** back-to-top / scroll-to-search control (only element-specific scroll helpers) -> 29654af7 is new & open
- remote e-signature flow -- none (RAF kiosk-only, `#signatureCanvas`) -> confirms 0448de62 L open

---

## Open "Needed" ERs -- by theme, then size

### Notifications & Task-Tracking (design together)

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `af14b3cb` | Lynn | 05-21 | **L** | Per-customer task tracker + daily reminder; tasks persist until marked complete | Not built. `js/shop-tasks.js` is deliberately NON-customer, so it does not satisfy this. Needs a per-RO tasks table + add/complete UI + daily open-task reminder. |
| `5925fc01` | Lynn | 04-09 | **L** | Notify the right team on every RO update; daily list of in-progress jobs with to-dos scraped from notes | PARTIAL: daily digest now served by the Manager Daily Report (`send-admin-pnl-report`/`send-manager-report`). Still open: per-update notification + auto-scraping to-dos into a per-RO task list. Prefer needed over done. |

### Parts & Inventory

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `14f35f95` | Lynn | 04-09 | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | Confirmed NOT built (`js/parts.js` 0 inventory hits). New subsystem: inventory table + receive/consume/adjust + searchable priced stock + parts-request links. Largest item on the board. |
| `b19975d4` | Lynn | 04-09 | **L** | Part returned-to-supplier / warrantied-by-supplier tracking | Not built (`js/parts.js` has only free-text `warranty_period`). = GH#24 Shop Ops Phase 2: reverse flow + return reasons + credit/warranty tracking. Canonical for dup 86bbaf50. |
| `8d885331` | Lynn | 04-09 | **S** | Rework parts-entry field order | Layout tweak (`js/parts.js` pf_ fields). Quick once Lynn gives the target layout. BLOCKED on detail, not effort. |

### RO fields / status

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `5fbfa299` | Brandon | 06-30 | **M** | Archive customers/units that were only quoted and never worked | Not built. Archiving routes to `cashiered` (`js/ro-crud.js` :654), misclassifying un-worked quotes as completed P&L jobs. Needs a separate lost/quoted-only disposition + board filter so quote-only records do not pollute Weekly P&L. Prefer needed over done. |

### Messaging & e-sign

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `0448de62` | Sofia | 06-11 | **L** | Send an authorization form remotely for e-signature | No remote-signing flow (RAF is kiosk-only, `customer-checkin.html #signatureCanvas`). Needs a tokenized public signing page + delivery (email/SMS) + signed-form storage. Pairs with Project Blue messaging Phase 3. |

### Auth & cost

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | Confirmed open. Auth is Google id-token + email-keyed RBAC (`js/auth.js signInWithIdToken`; no password/magic-link). Alternatives: Supabase email/password or magic-link, or shared role accounts. Security-critical; touches every page sign-in + RBAC. Scope together. |

### Time clock

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `e06d747e` | solar | 04-10 | **S** | Tech check-in "float" for ease of access | Underspecified -- likely a floating/sticky check-in button, but page + behavior unclear (`checkin.html` is QR/roId-driven). Needs scoping with the submitter. |

### General UI / Navigation (new this pass)

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `29654af7` | Lynn | 07-06 | **S** | Button to jump to top of page / search bar after scrolling | Confirmed NOT built (`index.html` has no back-to-top / scroll-to-search control; only element-specific scroll helpers such as `scrollToROInBoard`). Needs a floating "to top" button shown after scrolling. Small, self-contained UI add. |

---

## Duplicates (fold into canonical, recommend declined)

| ER | By | Date | Folds into | Note |
|---|---|---|---|---|
| `aa508852` | Riley | 04-09 | `fd6c122d` | Work list too small on phone / cannot delete or drag-drop -- delivered (touch drag-drop v1.460). |
| `86bbaf50` | Lynn | 05-13 | `b19975d4` | Part returned/warrantied by supplier -- canonical is GH#24 Shop Ops Phase 2. |
| `67daba15` | Brandon | 06-09 | `d2561e11` | Completion / pick-up date for records -- delivered. |
| `fa1c1d6e` | Sofia | 06-09 | `a22739b9` | Returning-customer check-in email not sent -- same defect, now fixed. |
| `5057c7fc` | Roland | 06-09 | `91a03260` | Remove cashed-out RO from manager worklist -- delivered. |
| `fc6b29f9` | Sofia | 06-17 | `298e0e67` | Returning-customer check-in duplicated the RO -- confirming test, now fixed. |

## Data correction

| ER | By | Date | Verdict |
|---|---|---|---|
| `6938c1af` | Ryan | 05-06 | Staff last name Wimbles -> Wombles (Zak, Rod, Travis). SQL: `UPDATE staff SET last_name='Wombles' WHERE last_name='Wimbles';` Already applied (status done). No code work. |

## Delivered (Done) -- closed or ready to close

Bug reports that were verified **fixed** in current code: `18611eee`,
`fd6c122d`, `13e1cd87`, `cd663d8f`, `a22739b9`, `2c3d5633`, `298e0e67`,
`5c45bb8d`, `ee50d11c`, `cb7742a8`.

Feature requests verified **delivered** (cite): `79b2c805` (cashed-out editable
via Delivered/Cashed Out filter), `75124b03` + `a5ff3e2d` (Warranty & Warranty+
Repair RO types), `b2aa4bea` + `468aa376` (planned_dropoff_date), `17492066` +
`3f415c88` (scheduled_notifications reminders), `98bd7498` (silo work lists),
`1fe68261` (shop-tasks S128), `3dfc0acb` (Ready to Work status), `b87eb2fb` +
`34fc03c2` (keys/keypad/keep-plugged), `a7d1474e` (urgent-updates banner),
`dde90d2b` (check-in streamlined + email), `e27311bf` (editable WO templates),
`d2561e11` (completion/pickup date), `dac9fdda` + `9b046808` (Off Lot -
Returning / Scheduled dispositions), `a8e90a7d` + `671fa623` (parts + freight
totals), `1aeb3f58` + `b06a285f` (promised date + key-date hover panel),
`cbc70a86` (auto-add to worklist on check-in S128), `0b2e128c` (clock-in notes
to RO updates), `2b814250` (Google Calendar sync S129), `cd084233` (admin P&L
report), `91a03260` (cashed-out excluded from worklist), `cdd77a8b` (on-lot vs
total count), `74a33621` (arrow-key photo nav), `9b823d25` (new-service silo
notification), `88395acc` (partial-day absence window), `80390f36` (Shop Ops
tile pinned), `7f2d0d70` (Monday drop-off report S128), `3f7fb4fc` (shop RO
static status S129).

Test entries (no code, recommend declined): `33750cb3`, `19dc2016`, and the
carried-over `f73a1e5a` ("Testing dictation") -- already declined in DB.

---

## Recommended action today

1. **Close the housekeeping:** decline the 6 duplicates + 3 test entries, and
   confirm the `6938c1af` (Wimbles -> Wombles) data fix is applied. Nothing else
   to change in the DB -- Roland flips status.
2. **Quick wins to ship (S):** `29654af7` (back-to-top button) is fully
   specified and self-contained -- the most shippable item on the board. Unblock
   `8d885331` (parts field order) and `e06d747e` (check-in float) by asking the
   submitters for specifics; both are small once specified.
3. **Well-scoped medium:** `5fbfa299` (lost/quote-only disposition) removes a
   real P&L pollution problem and pairs with the archive flow.
4. **Plan, do not rush, the XLs:** the auth swap (`ed1b873e`, ~$500/mo saving)
   and parts inventory (`14f35f95`) each deserve their own scoping session.
5. **Notifications & Task-Tracking cluster** (`af14b3cb` + `5925fc01`) remains
   the largest open theme -- design the per-RO tasks table + per-update
   notification together.
