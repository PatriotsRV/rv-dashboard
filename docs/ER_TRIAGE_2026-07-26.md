# PRVS Enhancement-Request Triage - 2026-07-26 (nightly headless runner, Phase 2a)

Full re-pass over `enhancement_requests` against the current modular codebase
(index.html **v1.483**, S159 on `pre-prod`). Input snapshot fetched
2026-07-26T09:17Z: **84 rows**, of which **83** were flagged for (re)triage
(`triage_run_at < updated_at`). Every verdict below was re-verified by
grepping/reading the live code this pass - no verdict was carried on trust.

Read-only run: **no code changed, no DB writes, no git, no network.** Status
flips stay Roland's call; Data-bucket SQL is emitted, never executed. Every ER
`description` / `submitted_by_name` / `admin_notes` value was treated strictly
as data; an injection scan over the full input (ignore-previous-instructions,
run-this-command, DROP TABLE, UPDATE enhancement_requests) returned **zero**
hits, so nothing was flagged for human review on that basis.

## Board snapshot

| Status | Count |
|---|---|
| done | 49 |
| declined | 9 |
| unreviewed | 26 |
| **Total** | **84** |

## Triage buckets (the 83 re-triaged rows)

| Bucket | Count |
|---|---|
| done (already delivered) | 53 |
| needed (open enhancement) | 21 |
| duplicate | 6 |
| bug (still present) | 2 |
| data (SQL, no code) | 1 |

Of the **26 `unreviewed`** rows: **21 Needed**, **2 confirmed live Bugs**,
**1 Data fix**, and **2 that are already shipped and can be closed**
(`1c7f1410`, `9d3806e7`). LOE mix on the Needed bucket: **2 XL, 4 L, 2 M, 13 S**
- the board has shifted decisively small since 2026-06-28, because the S158
messaging round drained the mid-size work and what arrived since is polish.

## What changed since the last report (2026-06-28, v1.466)

| ER | Was | Now | Evidence |
|---|---|---|---|
| `1fe68261` | needed/M | **done** | v1.467 Shop Tasks panel (`js/shop-tasks.js` + `shop_tasks.sql`) |
| `cbc70a86` | needed/M | **done** | `customer-checkin.html` -> `rpc('add_ro_to_silo_work_lists')` (`work_list_autoadd_rpc.sql`) |
| `7f2d0d70` | needed/M | **done** | `supabase/functions/send-dropoff-report` Monday drop-off check |
| `ac394468` | needed/S | **done** | v1.482 S158: Messages / Texting + 6 more categories in all 6 `erCategory` selects |
| `d62a26e4` | needed/S | **done** | v1.482 S158a: `msgAttach` `multiple`, `_msgAttachments[]`, one media per MMS |
| `93b00023` | needed/M | **done** | textly-webhook v1.3 reopen + needs-reply notify + `send-unreplied-reminder` 4:30 cron |
| `1c7f1410` | needed | **done** | attach shipped S151b, widened S158 (photos + PDFs) - **flip to done** |
| `9d3806e7` | new | **done** | S154 messages Notes pane + `customer_note_entries_s154.sql` - **flip to done** |
| `9031e101` `d4d0eeba` | new | **bug** | still reproducible in v1.483 (see Bugs below) |
| 13 new ERs (07-15..07-24) | - | **needed/S** | messaging polish, board nav, WO labor math, favicon |

Nothing regressed: all 53 `done` verdicts were re-confirmed against v1.483.

## Code checks run this pass

- `msgAttach` / `_msgAttachments` in `js/messaging.js` - **multi-attach present**
  (`multiple`, `image/*,application/pdf`, `MSG_MEDIA_MAX_COUNT`, chunked sends) -> `d62a26e4`, `1c7f1410` done
- `erCategory` option lists across `*.html` - **6 selects carry all page areas**;
  `js/enhancement.js` `pageDefaults` L43-53 maps every page -> `ac394468` done,
  with a residual (the lamp modal itself is on only those 6 pages)
- `textly-webhook/index.ts` - `status="open"` L477, `reopened` event L501-510,
  needs-reply-flip notify L415/L605; `send-unreplied-reminder` + `unreplied_eod_reminder_s158.sql` -> `93b00023` done
- inventory / `on_hand` / `in_stock` / `stock_qty` in `js/*.js` + `supabase/migrations` - **none** (confirms `14f35f95`)
- `rma` / `return_reason` / `returned_to_supplier` / `credit_memo` - **none**; only `PART_STATUSES` 'Returned' (confirms `b19975d4`)
- `ro_tasks` / `customer_task` / `task_tracker` - **none**; `js/shop-tasks.js` has no `ro_id` (confirms `af14b3cb`)
- `Canceled` / `Cancelled` RO status - **absent** from `STATUS_PROGRESS_MAP` and `ALL_STATUSES`;
  the four `'cancelled'` hits are `scheduled_notifications` rows, not RO status (confirms `972af03c`)
- `window.scrollTo` / back-to-top control - **none** (confirms `29654af7`)
- `rel=icon` / `apple-touch-icon` in all 13 `*.html` - **none** (confirms `d432863a`)
- `signing_token` / `esign` / `remote_sign` across `supabase/` + `js/` - **none** (confirms `0448de62`)
- `customerAddress` in `send-quote-email` - present only in the `lead_staff_notify`
  branch; the `raf_copy` destructure at L387 omits it (confirms `5fec3eca`)
- `woFormDollar` / `woFormEstHours` - independent manual inputs, no labor-rate math (confirms `2f752d40`)
- `signInWithIdToken` + `GOOGLE_CONFIG` in `js/auth.js` - **still Google-based** (confirms `ed1b873e`)
- `scrollToROInBoard` (index.html L4079) + `handleDeepLink` (`js/qr.js` L198) - **both still bail on filtered-out ROs** (confirms `9031e101`, `d4d0eeba`)

---

## Bugs - front of the line (2, both still present in v1.483)

| ER | By | Severity | Request | Mapped code | Verdict |
|---|---|---|---|---|---|
| `9031e101` | Riley Scott | **small** (mobile nav, no data risk) | Tapping a unit in the Work List on iPhone does not go to the right unit | `js/work-list.js` L328 -> `scrollToROInBoard` (index.html L4079) | Still present. `scrollToROInBoard` only scans already-rendered `.ro-card[data-ro-index]` / `.ro-card-compact[data-ro-index]` nodes and matches on `ro._supabaseId`. If the RO is filtered off the board it falls through to a "RO not visible" toast with a Clear Filters action instead of navigating. Fix = clear filters, re-render, then scroll to the tapped RO. |
| `d4d0eeba` | Brandon Dillon | **small** (nav, no data risk) | Fix tracking when clicking an RO in my list or a link in Messages | index.html L4079; `js/qr.js` `handleDeepLink` L198 (early returns L207/L210); `messages.html` ro-chip `index.html?ro=<ro_id>` | Still present, two paths. Work-list path is the same root cause as `9031e101`. Deep-link path returns **silently** when the RO is not in `currentFilteredData` (L207) or has no rendered card (L210), so the Messages chip looks broken. Fix both together: clear filters, re-render, then scroll + highlight. |

Both fall inside the Phase 2b auto-fix class (small, deterministic, no auth /
money / delete / DB). They share one root cause and should ship as one fix.

## Data bucket - SQL for Roland (nothing was run)

| ER | By | Request | Verdict |
|---|---|---|---|
| `3f254929` | Sofia Pedroza | Correct spelling is Steve **Huebert**; delete the Hubert account | Data correction, no code change. Inspect first: `SELECT id,ro_id,customer_name,status FROM repair_orders WHERE customer_name ILIKE '%ubert%';` then, once the wrong id is confirmed: `UPDATE repair_orders SET deleted_at=now() WHERE id='<wrong-id>';` The merge *feature* Sofia also asks for is `0e172b2c` (Needed/M). |

---

## Needed bucket by theme (21)

### Messaging & e-sign (5) - the biggest cluster, all one surface

| ER | By | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|
| `0448de62` | Sofia | **L** | Send an authorization form remotely for e-signature | `customer-checkin.html` signature pad; `send-quote-email` `raf_copy` | No remote-signing flow: signing is kiosk-only and `raf_copy` is a post-signing email. No `signing_token` / token table anywhere. Needs a tokenized public signing page + delivery + signed-form storage. Now pairs naturally with the Textly SMS channel. |
| `9aab7550` | Brandon | **S** | Make unchecked new messages more clearly visible | `messages.html` `.unread-dot` CSS L260, markup L879, condition L873 | Partial: a gold dot renders, but only when `last_direction==='inbound' AND status==='open'`, and there is no row-level highlight. Add a bold row / tinted background and widen the condition so a reopened thread also flags. |
| `21fc3a9d` | Brandon | **S** | Rescale the conversation list text | `messages.html` fixed rem sizes | No user text-size control anywhere (no `font-scale` / `zoom`). Add a persisted font-scale for the list. |
| `f1e6e311` | Brandon | **S** | Clear (x) button on the messaging search box | `messages.html` `convoSearch` L395, cleared programmatically L788/L806 | Plain text input today. Add a clear button that resets the value and re-runs the filter (L845 / L1597 handler). |
| `6e9ca4dc` | Brandon | **S** | Drop the redundant "Added to every send" line under the composer | `js/messaging.js` `refreshSignaturePreview` L241-246 -> `#msgSigPreview` L222 | Valid, still present on desktop. `messages.html` L356 already hides it, but only inside `@media (max-width:760px)` and for space reasons - so desktop `messages.html` and the RO-card composer still show it. Remove it or collapse it behind a toggle in all viewports. |

### Parts & Inventory (3)

| ER | By | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|
| `14f35f95` | Lynn | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | `js/parts.js` (per-RO line items only) | Confirmed absent: no inventory / on_hand / in_stock symbol in `js/*.js` or `supabase/migrations`. New subsystem: inventory table + receive/consume/adjust + searchable priced stock + parts-request links. Largest item on the board. |
| `b19975d4` | Lynn | **L** | Part returned to / warrantied by supplier | `js/config.js` `PART_STATUSES` L200; `js/parts.js` `pf_returnDeadline` | Partial infra only: a 'Returned' status and a return-deadline date exist, but there is no RMA number, return reason, credit tracking or reverse-direction flow. Equals GH#24 Shop Ops Phase 2. Canonical for declined dup `86bbaf50`. |
| `8d885331` | Lynn | **S** | Rework the parts-entry field order | `js/parts.js` `pf_*` fields L181-235 | Form is unchanged; the ask is only to reorder. Quick once Lynn supplies the target layout. **Blocked on detail, not effort.** |

### RO fields / status (3)

| ER | By | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|
| `0e172b2c` | Sofia | **M** | Merge duplicate customer accounts after the customer verifies which is right | `js/duplicates.js` `getBaseROId` L35 / `findDuplicateGroups` L40 / `openDuplicateManager` L51 / `executeDupeMerge` | Partial: an RO-level merge/delete engine exists but groups by **base RO id**, not customer identity, so a misspelled-name second RO is never detected. Needs name/phone account dedup plus a verify-which-is-correct step. The one-off cleanup is `3f254929`. |
| `5fbfa299` | Brandon | **M** | Archive quoted-but-never-progressed customers/units | `js/ro-crud.js` `archiveROInSupabase` L646, `archived_at` L692 | Only the cashiered archive exists. No archived/abandoned flag, status or board filter. Needs a soft-archive flag + board filter + un-archive path. Pairs with `972af03c`. |
| `972af03c` | Lynn | **S** | Add status CANCELED - keep the record for notes | `js/config.js` `STATUS_PROGRESS_MAP` L151-164; `js/render.js` `ALL_STATUSES` L741 | Confirmed absent (the `'cancelled'` hits in `js/ro-crud.js` L291/L516 and index.html L2815/L3011 are notification rows). Add the status + color/priority maps, exclude from on-lot and queue counts, keep the record. Pairs with `5fbfa299`. |

### Notifications & Task-Tracking (3)

| ER | By | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|
| `5925fc01` | Lynn | **L** | Notify the right team on every RO update; daily list of in-progress jobs with to-dos scraped from notes | `send-manager-report`; `service_added_notify` / `urgent_update_notify` sources | Partial: the daily digest and single-event notifies exist; still open are notify-on-**every**-update and to-do scraping from status notes. Anchor of this theme. |
| `af14b3cb` | Lynn | **L** | Per-customer task tracker + daily reminder until complete | `js/shop-tasks.js` (no `ro_id`); no per-RO tasks table | Confirmed absent. Needs a per-RO tasks table + add/complete UI + a daily open-task reminder. Pairs with `5925fc01`. |
| `5fec3eca` | Sofia | **S** | Show the customer's address on the emailed authorization-form copy | `send-quote-email` `raf_copy` destructure L387; address only at L549/674/748 | Confirmed absent from the `raf_copy` payload. Add `customerAddress` to the payload plus both the HTML and text templates. |

### Board view & navigation (3, new cluster)

| ER | By | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|
| `d1844e46` | Lynn | **S** | Sort dash by promised date then oldest on lot, shop ops still pinned | `js/render.js` `calculatePriority` L33; shop pin L35-37 | Only one blended priority score exists; no selectable strict sort. Add the strict sort option while keeping the shop pin. |
| `29654af7` | Lynn | **S** | Back-to-top / jump-to-search button | index.html, `js/*.js` (only targeted `scrollIntoView`) | Confirmed absent. Scroll-triggered back-to-top button; do it in both layouts (`css/sidebar-layout.css`). |
| `d432863a` | Brandon | **S** | Patriots-logo tab icon | all 13 `*.html` heads | No favicon / apple-touch-icon anywhere. Add both to every page head. |

### Time clock (2)

| ER | By | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|
| `e06d747e` | Riley Scott | **S** | Tech check-in "float" for ease of access | `js/render.js` `compact-col-actions` L228-235; `checkin.html` | Confirmed absent. Underspecified - scope the page and behavior with the submitter. Pairs with `6bb61db2`, which specifies most of it. |
| `6bb61db2` | Riley Scott | **S** | Check-in/out button on compact view + a live "you are clocked into X" reminder at login | `js/render.js` L228-235 (no check-in action); no clocked-in indicator anywhere | Confirmed absent. Add a compact check-in/out action plus a login-time active `time_log` banner across views. |

### Work Orders (1)

| ER | By | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|
| `2f752d40` | Jason Rubin | **S** | Auto-populate the WO dollar amount from total hours x $195/hr | `js/work-orders.js` `woFormDollar` L360, `woFormEstHours` L365, read L649-650 | Independent manual inputs, no labor-rate constant anywhere. Auto-fill `dollar_value` from the effective hours (task `est_hours` rollup when present, else WO-level) with an editable override, respecting the existing `dollarLocked` readonly gate. |

### Auth & cost (1)

| ER | By | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|
| `ed1b873e` | Roland | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | `js/auth.js` `signInWithIdToken` + One Tap (GROUP D L449+); `js/config.js` `GOOGLE_CONFIG` L79 | Still fully Google-based, kiosk included. A swap to Supabase password/magic-link or shared role accounts would drop roughly $500/mo but touches every page sign-in plus the email-keyed RBAC. Roland wants to scope this together. |

---

## Full per-ER table (all 84)

Bucket key: **D** = done, **N** = needed, **B** = bug, **X** = duplicate,
**T** = data. `-` in the LOE column = not applicable.

| ER | By | Date | Category | Status | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|---|
| `33750cb3` | Roland Shepard Jr | 2026-04-09 | General UI/UX | declined | D | - | n/a | Test entry ("Testing out the Make-A-Wish function"). No code action; keep declined. |
| `79b2c805` | Sofia Pedroza | 2026-04-09 | New Feature Idea | done | D | - | `closed-ros.html` `reactivateRO`; `js/photos.js` `uploadDocument` | Cashed-out ROs reopen for edit (restores parts/time_logs/notes/photos from `cashiered_child_archives`) and documents can be added to any RO. |
| `75124b03` | Ryan Dillon | 2026-04-09 | RO Management | done | D | - | `js/ro-crud.js` `ro_type`; index.html warranty filter | v1.402 Warranty RO type on new-RO creation. |
| `5925fc01` | Lynn Titel-Shepard | 2026-04-09 | New Feature Idea | unreviewed | N | L | `send-manager-report`; notify sources | Partial: digest + single-event notifies exist; notify-on-every-update and to-do scraping do not. |
| `b2aa4bea` | Lynn Titel-Shepard | 2026-04-09 | New Feature Idea | done | D | - | `key_dates_reminder_sources.sql` (`auto_dropoff_reminder`) | Scheduled future drop-off date + 1-day-before manager reminder. |
| `18611eee` | Lynn Titel-Shepard | 2026-04-09 | Bug Report | done | D | - | `js/work-list.js` `renderWorkList` / `removeFromWorkList` / `_setWorkListSilo` | Phase 14 fix: work-list members are viewable and removable. Was a bug, resolved. |
| `14f35f95` | Lynn Titel-Shepard | 2026-04-09 | Parts & Ordering | unreviewed | N | XL | `js/parts.js` | No inventory/stock model at all. New subsystem. |
| `b19975d4` | Lynn Titel-Shepard | 2026-04-09 | Parts & Ordering | unreviewed | N | L | `js/config.js` `PART_STATUSES` L200 | 'Returned' status only; no RMA / reason / credit / reverse flow. GH#24 Shop Ops Phase 2. |
| `17492066` | Lynn Titel-Shepard | 2026-04-09 | Notifications / Email | done | D | - | `scheduled_notifications.sql`; `process-scheduled-notifications` | Per-RO Schedule Notification bell with future date/time, message, recipients and repeat. |
| `8d885331` | Lynn Titel-Shepard | 2026-04-09 | Parts & Ordering | unreviewed | N | S | `js/parts.js` `pf_*` L181-235 | Field-reorder only; blocked on the target layout, not effort. |
| `fd6c122d` | Riley Scott | 2026-04-09 | Work List | done | D | - | `js/work-list.js` `removeFromWorkList`, `_reorderWorkList` | Delete + mobile sizing + mouse/touch drag-drop (v1.460). Canonical for `aa508852`. |
| `aa508852` | Riley Scott | 2026-04-09 | Work List | declined | X | - | see `fd6c122d` | Identical text and timestamp to `fd6c122d`. Fold in; already declined. |
| `98bd7498` | Brandon Dillon | 2026-04-09 | RO Management | done | D | - | `js/work-list.js` `_renderWorkListSiloTabs` / `_setWorkListSilo` | Per-silo Work List tabs give a different list per category. |
| `e06d747e` | Riley Scott | 2026-04-10 | Time Clock / Check-In | unreviewed | N | S | `js/render.js` L228-235 | No floating check-in control. Underspecified; pairs with `6bb61db2`. |
| `19dc2016` | Roland Shepard Jr | 2026-04-11 | Time Clock / Check-In | declined | D | - | n/a | Test entry ("Testing ER form / dictation"). Keep declined. |
| `f73a1e5a` | Roland Shepard Jr | 2026-04-13 | General UI/UX | declined | (carried) | - | n/a | Not in this pass's `needs_triage_ids` (already triaged and unchanged); no bucket on record. Test entry ("Testing dictation 123"), already declined - left untouched. |
| `1fe68261` | Rod Wombles | 2026-04-13 | Work Orders | done | D | - | `js/shop-tasks.js`; `shop_tasks.sql` | v1.467 Shop Tasks panel tracks internal work not tied to a customer unit. **Newly done since the last report.** |
| `3dfc0acb` | Lynn Titel-Shepard | 2026-04-14 | Work Orders | done | D | - | `js/config.js` `STATUS_PROGRESS_MAP` slot 50 | v1.410 "Ready to Work" status, present in every dropdown. |
| `b87eb2fb` | Lynn Titel-Shepard | 2026-04-14 | RO Management | done | D | - | `keys_power_fields.sql` | v1.458 Keys / No keys / Keypad + code + keep-plugged-in, with a card chip. Same build as `34fc03c2`. |
| `a5ff3e2d` | Lynn Titel-Shepard | 2026-04-21 | RO Management | done | D | - | `js/ro-crud.js` `warranty_repair`; `js/render.js` badge/filter | v1.465 "Warranty + Repair" type - warranty details kept, still billable. |
| `a7d1474e` | Lynn Titel-Shepard | 2026-04-23 | RO Management | done | D | - | `urgent_update_field.sql`; `.urgent-update-banner` | v1.466 always-visible red URGENT banner; changes email silo managers + admins. |
| `dde90d2b` | Roland Shepard Jr | 2026-04-27 | RO Management | done | D | - | `customer-checkin.html`; `send-quote-email` `raf_copy` / `lead_staff_notify` | Photo optional, RAF + staff email forced on every submission. |
| `13e1cd87` | Tipton Scott | 2026-04-30 | Time Clock / Check-In | done | D | - | `checkin.html` (v1.34) | v1.447 Return-to-Dashboard button after clock-out. Was a bug, fixed. |
| `e27311bf` | Ryan Dillon | 2026-05-04 | Work Orders | done | D | - | `js/work-orders.js` `_initWOTaskDrag` | v1.464 drag-reorder WO task rows; edit a template by load, reorder, Overwrite. |
| `d2561e11` | Brandon Dillon | 2026-05-06 | New Feature Idea | done | D | - | `key_dates_pickup_date.sql`; `js/render.js` L397 | v1.457 pickup/completion date + green Pickup chip. Canonical for `67daba15`. |
| `6938c1af` | Ryan Dillon | 2026-05-06 | General UI/UX | done | D | - | `staff.name` (not hardcoded) | Data fix already applied (Wimbles -> Wombles); this table's own submitter now reads "Rod Wombles". Re-run SQL only if it recurs. |
| `dac9fdda` | Brandon Dillon | 2026-05-06 | General UI/UX | done | D | - | `js/config.js` slot 15; `js/render.js` | v1.460 "Off Lot - Returning" status, excluded from the on-lot count. |
| `86bbaf50` | Lynn Titel-Shepard | 2026-05-13 | RO Management | declined | X | - | see `b19975d4` | Gordon window ordered and returned to Vogt = supplier return tracking. Fold into `b19975d4`. |
| `a8e90a7d` | Brandon Dillon | 2026-05-20 | General UI/UX | done | D | - | `js/parts.js` `totFreight` / `totCost` L153-160 | v1.456 Manage Parts Ticket Totals (parts + freight + total). |
| `1aeb3f58` | Lynn Titel-Shepard | 2026-05-21 | General UI/UX | done | D | - | `js/render.js` L390-398 (comment cites this ER) | v1.457 promised date as a bold chip, red when overdue, plus promise-then-days-on-lot in the blended sort. Strict sort is `d1844e46`. |
| `af14b3cb` | Lynn Titel-Shepard | 2026-05-21 | General UI/UX | unreviewed | N | L | `js/shop-tasks.js` (no `ro_id`) | No per-RO task tracker or reminder. Pairs with `5925fc01`. |
| `cbc70a86` | Lynn Titel-Shepard | 2026-05-21 | General UI/UX | done | D | - | `customer-checkin.html` -> `add_ro_to_silo_work_lists` (`work_list_autoadd_rpc.sql`) | Returning check-in inserts `manager_work_lists` rows per involved silo lead. **Newly done since the last report.** |
| `3f415c88` | Lynn Titel-Shepard | 2026-05-26 | General UI/UX | done | D | - | Schedule Notification Repeat (v1.463) | Daily/weekly repeat + repeat-until expands into one cancellable row per occurrence. |
| `cd663d8f` | Bobby Thatcher | 2026-05-28 | General UI/UX | done | D | - | `js/ro-crud.js` L1093 `toFixed(2)`; `js/parts.js` `MONEY_FIELDS` | v1.447 currency inputs keep 2 decimals on blur and edit-populate. Cosmetic bug, no data loss. |
| `34fc03c2` | Brandon Dillon | 2026-05-29 | New Feature Idea | done | D | - | `keys_power_fields.sql` | v1.458 Keys selector on New/Edit RO + card chip. Covers `b87eb2fb`. |
| `0b2e128c` | Lynn Titel-Shepard | 2026-06-02 | General UI/UX | done | D | - | `checkin.html` `clockOut` -> "TECH NOTES" notes row | v1.460 clock-out work notes copied into the RO update timeline. |
| `468aa376` | Lynn Titel-Shepard | 2026-06-02 | General UI/UX | done | D | - | `js/ro-crud.js` `planned_dropoff_date` | Planned drop-off input on New RO and (restored v1.457) Edit RO, feeding the Drop-off chip + auto reminder. |
| `2b814250` | Roland Shepard Jr | 2026-06-07 | Calendar | done | D | - | `js/render.js` L363-398; `sync-ro-calendar` | Key dates surfaced as chips + a hover/tap panel, with server-side calendar sync and advance reminders. |
| `ed1b873e` | Roland Shepard Jr | 2026-06-07 | General UI/UX | unreviewed | N | XL | `js/auth.js` GROUP D L449+; `js/config.js` L79 | Still Google id-token + email-keyed RBAC. ~$500/mo swap; scope with Roland. |
| `cd084233` | Roland Shepard Jr | 2026-06-07 | Work List | done | D | - | `send-manager-report`; `send-admin-pnl-report`; `worklist-report.html` | Admin-level daily manager-worklist reporting with parts/labor/cost. The manager **scoring** layer is a future extension, not this ER. |
| `a22739b9` | Sofia Pedroza | 2026-06-08 | General UI/UX | done | D | - | `customer-checkin.html`; `send-quote-email` | Check-in notify-gate regression fixed; RAF + staff email forced. Canonical for `fa1c1d6e`. |
| `67daba15` | Brandon Dillon | 2026-06-09 | General UI/UX | declined | X | - | see `d2561e11` | Same as `d2561e11` (completion / pick-up date), which is done. Fold in. |
| `fa1c1d6e` | Sofia Pedroza | 2026-06-09 | General UI/UX | declined | X | - | see `a22739b9` | Same as `a22739b9` (Sheldon Gilbert check-in did not email). Fold in. |
| `91a03260` | Roland Shepard Jr | 2026-06-09 | Work List | done | D | - | `prune_cashiered_worklists.sql` | RO auto-removed from a manager work list the day after Cashiered Out / Delivered. Canonical for `5057c7fc`. |
| `5057c7fc` | Roland Shepard Jr | 2026-06-09 | Work List | declined | X | - | see `91a03260` | Exact duplicate of `91a03260`. Fold in. |
| `cdd77a8b` | Brandon Dillon | 2026-06-10 | General UI/UX | done | D | - | `js/render.js` `updateStats` | v1.453/v1.454 Total RVs splits on-lot vs total customers (Not On Lot / Scheduled / Off Lot - Returning / shop excluded). |
| `0448de62` | Sofia Pedroza | 2026-06-11 | General UI/UX | unreviewed | N | L | `customer-checkin.html` signature pad | Kiosk-only signing; no tokenized remote signing page exists. |
| `2c3d5633` | Ryan Dillon | 2026-06-12 | General UI/UX | done | D | - | `js/photos.js` L66 `document.body.appendChild(input)` | v1.453 fix: input attached to the DOM before click (iOS Safari dropped the change event). Mobile-only functional bug, resolved. |
| `74a33621` | Brandon Dillon | 2026-06-16 | General UI/UX | done | D | - | `js/photos.js` `openPhotoLightbox` / `navigateLightbox` | v1.454 arrow-key lightbox navigation + Escape-to-close. |
| `298e0e67` | Sofia Pedroza | 2026-06-16 | General UI/UX | done | D | - | `customer-checkin.html` existing-RO reuse | Returning check-in against a future Scheduled-drop RO reuses it instead of duplicating. Data-integrity bug, fixed. Canonical for `fc6b29f9`. |
| `9b823d25` | Roland Shepard Jr | 2026-06-16 | RO Management | done | D | - | `js/work-orders.js` `addServiceToRO`; `service_added_notify_source.sql` | v1.460 adding a service emails the owning silo manager. |
| `fc6b29f9` | Sofia Pedroza | 2026-06-17 | General UI/UX | declined | X | - | see `298e0e67` | Retest of `298e0e67`. Fold in. |
| `9b046808` | Brandon Dillon | 2026-06-17 | General UI/UX | done | D | - | `js/config.js` slot 45; `js/render.js` | v1.454 'Scheduled' excluded from on-lot and days-on-lot counts. |
| `671fa623` | Brandon Dillon | 2026-06-18 | General UI/UX | done | D | - | `js/parts.js` `totFreight` L153-160 | v1.456 Ticket Totals include a summed Freight line. |
| `5c45bb8d` | Sofia Pedroza | 2026-06-19 | General UI/UX | done | D | - | `customer-checkin.html` reuse + forced `raf_copy` | David Flanagan case: no duplicate RO and the RAF email now sends. Data-integrity + missing-email bug, fixed. |
| `b06a285f` | Lynn Titel-Shepard | 2026-06-19 | General UI/UX | done | D | - | `js/render.js` L363-367 `kd-name-cal` / `kd-pop`; `css/dashboard.css` | v1.465 calendar icon by the customer name opens a full key-dates panel on hover/tap. |
| `ee50d11c` | Lynn Titel-Shepard | 2026-06-19 | General UI/UX | done | D | - | `time-off.html` Edit L752 -> `editRequest` L864, "Update Record" L895 | Time-off entries are editable (day / person / type), not delete-only. |
| `88395acc` | Lynn Titel-Shepard | 2026-06-19 | General UI/UX | done | D | - | `add_partial_hours_to_time_off.sql`; `add_absence_window_to_time_off.sql` | Time Off v1.5 exact partial-day window (half day AM/PM or custom clock-out/return times). |
| `80390f36` | Lynn Titel-Shepard | 2026-06-23 | General UI/UX | done | D | - | `js/render.js` L35-37 shop sort; L61 search token | v1.462 Shop Operations tiles pinned to the top and reliably searchable. |
| `7f2d0d70` | Lynn Titel-Shepard | 2026-06-26 | General UI/UX | done | D | - | `supabase/functions/send-dropoff-report` | Automated Monday drop-off check flags units not yet on the right team work list. **Newly done since the last report.** |
| `3f7fb4fc` | Brandon Dillon | 2026-06-29 | General UI/UX | done | D | - | `js/render.js` `updateStats`; static status label | v1.468 shop ROs out of the queue/on-lot counts, status dropdown replaced by a label. Small data-display bug, fixed. |
| `cb7742a8` | Lynn Titel-Shepard | 2026-06-29 | General UI/UX | done | D | - | `supabase/functions/sync-ro-calendar`; `cal_event_ids` | v1.469 server-side sync posts drop-off/promised/pickup to the correct team calendar for every RO, front-desk check-ins included. |
| `5fbfa299` | Brandon Dillon | 2026-06-30 | General UI/UX | unreviewed | N | M | `js/ro-crud.js` `archiveROInSupabase` L646 | Only the cashiered archive exists; no abandoned/quoted archive flag or filter. |
| `29654af7` | Lynn Titel-Shepard | 2026-07-06 | General UI/UX | unreviewed | N | S | index.html / `js/*.js` | No back-to-top control anywhere. |
| `39b75fab` | Sofia Pedroza | 2026-07-09 | General UI/UX | done | D | - | `js/photos.js` `canManageFiles` / `deleteLibraryItem` / `renameLibraryDoc` / `moveLibraryItem` | v1.472 document rename/move/delete, office-staff gated - the Marco DeAlba docs can be moved off the Ruiz RO. |
| `5fec3eca` | Sofia Pedroza | 2026-07-15 | General UI/UX | unreviewed | N | S | `send-quote-email` `raf_copy` L387 | `customerAddress` missing from the `raf_copy` payload and templates. |
| `1c7f1410` | Andrew Page | 2026-07-16 | New Feature Idea | unreviewed | D | - | `js/messaging.js` `msgAttachBtn` / `msgAttach` L219 / `_msgAttachments` L202 | Attachments shipped S151b and widened S158 (photos **and** PDFs, up to 5). **Recommend status=done.** |
| `d1844e46` | Lynn Titel-Shepard | 2026-07-16 | General UI/UX | unreviewed | N | S | `js/render.js` L33/L35-37 | One blended priority sort only; no strict promised-then-oldest option. |
| `0e172b2c` | Sofia Pedroza | 2026-07-16 | General UI/UX | unreviewed | N | M | `js/duplicates.js` L35/L40/L51 | Merge engine groups by base RO id, not customer identity. Needs name/phone account dedup + verify step. |
| `3f254929` | Sofia Pedroza | 2026-07-16 | General UI/UX | unreviewed | T | - | `repair_orders` (data only) | Keep Steve Huebert, remove the misspelled Hubert RO. SQL emitted above; nothing was run. |
| `972af03c` | Lynn Titel-Shepard | 2026-07-17 | General UI/UX | unreviewed | N | S | `js/config.js` L151-164; `js/render.js` L741 | No Canceled RO status; the `'cancelled'` hits are notification rows. |
| `d432863a` | Brandon Dillon | 2026-07-21 | General UI/UX | unreviewed | N | S | all `*.html` heads | No favicon or apple-touch-icon on any page. |
| `9031e101` | Riley Scott | 2026-07-21 | Work List | unreviewed | **B** | - | `js/work-list.js` L328 -> index.html L4079 | **Bug, still present, small.** Work-list tap only finds rendered cards; filtered-out ROs get a toast instead of navigation. |
| `9aab7550` | Brandon Dillon | 2026-07-21 | General UI/UX | unreviewed | N | S | `messages.html` L260/L873/L879 | Unread dot exists but is narrowly conditioned and there is no row highlight. |
| `21fc3a9d` | Brandon Dillon | 2026-07-21 | General UI/UX | unreviewed | N | S | `messages.html` fixed rem sizes | No text-size control for the conversation list. |
| `2f752d40` | Jason Rubin | 2026-07-21 | Work Orders | unreviewed | N | S | `js/work-orders.js` L360/L365/L649-650 | Dollar and hours are independent manual inputs; no $195/hr auto-calc. |
| `f1e6e311` | Brandon Dillon | 2026-07-21 | General UI/UX | unreviewed | N | S | `messages.html` `convoSearch` L395 | No clear (x) button on the messaging search box. |
| `6e9ca4dc` | Brandon Dillon | 2026-07-21 | General UI/UX | unreviewed | N | S | `js/messaging.js` L222/L241-246; `messages.html` L356 | Signature preview still shows on desktop; the existing hide is mobile-only. |
| `6bb61db2` | Riley Scott | 2026-07-22 | Time Clock / Check-In | unreviewed | N | S | `js/render.js` L228-235 | No compact check-in/out action and no "clocked into X" indicator. |
| `d4d0eeba` | Brandon Dillon | 2026-07-22 | General UI/UX | unreviewed | **B** | - | index.html L4079; `js/qr.js` L198/L207/L210 | **Bug, still present, small.** Work-list tap toasts; the Messages deep link fails **silently**. Same root cause as `9031e101`. |
| `9d3806e7` | Brandon Dillon | 2026-07-22 | General UI/UX | unreviewed | D | - | `messages.html` `detailNoteView` / `openNotesModal` / `saveCustomerNote`; `customer_note_entries_s154.sql` | S154 customer-side Notes pane, 14 refs. **Recommend status=done.** |
| `ac394468` | Roland Shepard Jr | 2026-07-24 | General UI/UX | done | D | - | index.html L7497-7514 `erCategory`; `js/enhancement.js` L43-53 | v1.482 Messages / Texting + 6 more page categories in all 6 selects + `pageDefaults`. **Residual:** the lamp modal is still only on those 6 pages, so `messages.html`, `leads.html`, `time-off.html`, `review.html`, `guide.html` still cannot raise an ER. |
| `d62a26e4` | Roland Shepard Jr | 2026-07-24 | New Feature Idea | done | D | - | `js/messaging.js` L219 `multiple`, L202/L300 cap, L256-264 chips, L456-461 chunking | v1.482/S158a multi-attach (up to 5 photos/PDFs), sent as one media per MMS because a carrier payload cap silently stripped extras. |
| `93b00023` | Roland Shepard Jr | 2026-07-24 | New Feature Idea | done | D | - | `textly-webhook` L415/L477/L501-510/L605; `send-unreplied-reminder`; `unreplied_eod_reminder_s158.sql` | v1.482 all three parts: inbound sets `status='open'` + logs `reopened`, notify fires only on the needs-reply flip, and a 4:30pm unreplied sweep runs on cron. |

---

## Recommended action today

1. **Close the two already-shipped ERs.** `1c7f1410` (message attachments) and
   `9d3806e7` (customer notes in Messages) are delivered and verified in v1.483.
   Flipping them takes `unreviewed` from 26 to 24 and triggers the existing
   completion emails to Andrew and Brandon.
2. **Fix the two bugs as one change.** `9031e101` and `d4d0eeba` share a single
   root cause: navigation targets that only look at *already-rendered* cards
   (index.html L4079) or bail silently (`js/qr.js` L207/L210). Clear filters,
   re-render, then scroll and highlight - one small patch closes both, and it is
   inside the Phase 2b auto-fix rails (no auth, money, delete, DB, or notify).
3. **Then take the Messaging polish batch (4 x S) in one sitting.** `6e9ca4dc`,
   `f1e6e311`, `9aab7550`, `21fc3a9d` all touch `messages.html` / `js/messaging.js`
   and nothing else. Four submitter-visible wins for roughly one session, and it
   keeps the momentum of the S158 messaging round.
4. **Second batch, board polish (3 x S):** `d432863a` (favicon), `29654af7`
   (back-to-top), `d1844e46` (strict promised-date sort). Independent of the
   messaging batch and safe to ship alongside it.
5. **Hand Roland the one data fix.** Run the `%ubert%` SELECT for `3f254929`,
   confirm the wrong id by eye, then the single-row soft delete. Nothing was
   executed by this run.
6. **Unblock the two stalled S's by asking the submitters:** Lynn's parts field
   order (`8d885331`) and Riley's check-in float (`e06d747e`) are both small once
   specified - and `6bb61db2` already specifies most of what `e06d747e` wants, so
   they should be scoped as one item.
7. **Log the `ac394468` residual as its own small ER** rather than reopening a
   closed one: put the genie lamp on `messages.html`, `leads.html`,
   `time-off.html`, `review.html` and `guide.html`. The categories shipped; the
   lamp did not follow to all pages, and this report should not let that hide.
8. **Plan, do not rush, the XL/L items.** The auth swap (`ed1b873e`), parts
   inventory (`14f35f95`), supplier returns (`b19975d4`), per-RO task tracker
   (`af14b3cb`), notify-on-every-update (`5925fc01`) and remote e-sign
   (`0448de62`) each deserve their own scoping session.
