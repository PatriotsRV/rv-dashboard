# PRVS Enhancement-Request Triage -- 2026-07-05 (headless runner)

Incremental headless pass over `enhancement_requests` against the current
modular codebase (index.html **v1.471**). 62 of 63 rows re-triaged (the one
skipped, `f73a1e5a`, is a declined dictation-test entry). Every verdict was
verified by grepping/reading the live code -- not assumed. No DB writes, no
code changes: any status flips are Roland's to make.

Untrusted-data guard applied: each ER `description` / `admin_notes` was treated
strictly as data. No prompt-injection content was found this pass.

## Board snapshot (post-S128/S129 shipping)

| Bucket (AI verdict) | Count |
|---|---|
| done (delivered) | 46 |
| needed (open enhancement) | 9 |
| duplicate | 6 |
| data | 1 |
| **Total triaged** | **62** |

**Headline:** S128-S132 cleared the last of the shippable backlog. Three items
that were still "Needed" in the 06-28 report are now **delivered and verified**:
`cbc70a86` (auto-add to work lists on check-in, customer-checkin.html v1.14),
`7f2d0d70` (Monday drop-off report, send-dropoff-report edge fn), and
`1fe68261` (Internal Shop Task tracker, js/shop-tasks.js). Two check-in bugs
also shipped in S129: `3f7fb4fc` (shop ROs off the on-lot count) and `cb7742a8`
(check-in dates reach the team calendar).

**9 open ERs remain, all valid "Needed" enhancements -- no open bugs, nothing
already-shipped left to close.** One is new since the last pass: `5fbfa299`
(archive quote-only customers, 06-30). Size mix: **2 XL - 4 L - 1 M - 2 S**
(the two S's are blocked on submitter detail, not effort). See the theme
clusters below.

## Code checks run this pass (verification, not assumption)

- `manager_work_lists` insert in customer-checkin.html -- **present** at :2304
  (confirms `cbc70a86` done)
- `send-dropoff-report` edge fn -- **present**, driven by planned_dropoff_date
  (confirms `7f2d0d70` done)
- `js/shop-tasks.js` + `shop_tasks` table -- **present**, non-customer tasks
  (confirms `1fe68261` done; note it is deliberately NOT per-customer, so
  `af14b3cb` stays open)
- shop-RO status/count exclusion in js/render.js:637/700 -- **present**
  (confirms `3f7fb4fc` done)
- `sync-ro-calendar` POST from customer-checkin.html v1.15 -- **present**
  (confirms `cb7742a8` done)
- inventory / stock / on-hand in js/parts.js -- **none** (confirms `14f35f95`)
- supplier-return / return flow in js/parts.js -- **none**, only a
  `warranty_period` text field (confirms `b19975d4`)
- remote e-signature / tokenized public signing page -- **none**
  (confirms `0448de62`)
- Google id-token auth in js/auth.js (signInWithIdToken) -- **present**
  (confirms `ed1b873e` auth swap still open)
- `archiveROInSupabase` -> `cashiered` insert in js/ro-crud.js -- **present**;
  no lost/quote-only disposition (confirms `5fbfa299`)

---

## The 9 open "Needed" ERs (by theme, then size)

### Notifications & Task-Tracking

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `af14b3cb` | Lynn | **L** | Per-customer task tracker + daily reminder; tasks persist until marked complete | Not built. shop-tasks.js is deliberately NON-customer, so it does not satisfy this. Needs a per-RO tasks table + add/complete UI + daily open-task reminder. Same theme as 5925fc01. |
| `5925fc01` | Lynn | **L** | Notify the right team on every RO update; daily list of all in-progress jobs with to-dos scraped from notes | PARTIAL -- the daily digest is largely served now by the Manager/Admin daily reports. Still open: per-update notification + auto-scraping to-dos from status notes. Anchor of this initiative. |

### Parts & Inventory

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `14f35f95` | Lynn | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | New subsystem. No stock/inventory handling in js/parts.js. Inventory table + receive/consume/adjust + searchable stock + parts-request links. Largest item on the board. |
| `b19975d4` | Lynn | **L** | Part returned-to-supplier / warrantied-by-supplier tracking | No supplier-return flow (parts.js stores only a warranty_period text field). = GH#24 Shop Ops Phase 2: reverse-direction flow + return reasons + credit/warranty tracking. Canonical for declined dup 86bbaf50. |
| `8d885331` | Lynn | **S** | Rework the parts-entry screen field order | Layout tweak to the js/parts.js form. Quick once Lynn gives the desired layout. BLOCKED on detail, not effort. |

### RO fields / status

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5fbfa299` | Brandon | **M** | Archive quote-only customers who never moved forward with work | Not built. Archiving routes everything to Cashiered (ro-crud.js archiveROInSupabase), misclassifying un-worked quotes as completed P&L jobs. Needs a separate lost/quoted-only disposition + board filter. Newest ER (06-30). |

### Messaging & e-sign

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `0448de62` | Sofia | **L** | Send an authorization form remotely for e-signature | No remote-signing flow (RAF is kiosk-only today). Needs a tokenized public signing page + delivery + signed-form storage. Pairs with Project Blue messaging Phase 3. |

### Auth & cost

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `ed1b873e` | Roland | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | Security-critical swap of the id-token + email-RBAC model (js/auth.js signInWithIdToken). Supabase password/magic-link or shared role accounts. Touches every page sign-in + RBAC. Roland wants to scope together. |

### Time clock

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `e06d747e` | solar | **S** | Tech check-in "float" for ease of access | Underspecified -- likely a floating/sticky check-in button, but page + behavior unclear (checkin.html is QR/roId-driven today). Needs scoping with the submitter. |

---

## Delivered this cycle (moved to Done since 2026-06-28)

| ER | By | What shipped | Evidence |
|---|---|---|---|
| `cbc70a86` | Lynn | Check-in auto-adds RO to involved team leads' work lists | customer-checkin.html v1.14 :2304 |
| `7f2d0d70` | Lynn | Monday drop-off heads-up + last-week reconciliation | supabase/functions/send-dropoff-report (S128) |
| `1fe68261` | Rod | Internal Shop Task tracker (non-customer tasks) | js/shop-tasks.js + shop_tasks table (S128) |
| `3f7fb4fc` | Brandon | Shop ROs off the on-lot status dropdown + counts | js/render.js:637/700 (v1.468 S129) |
| `cb7742a8` | Lynn | Check-in scheduled dates reach the team calendar | customer-checkin.html v1.15 -> sync-ro-calendar (S129) |

The remaining 41 Done verdicts are stable carry-overs (spot-checked against the
current code): warranty/hybrid RO types, key-date + calendar sync, work-list
touch drag-drop, parts+freight totals, photo lightbox arrow-keys + upload fix,
time-off partial-window editing, urgent-updates tile, admin P&L report, and the
S94 currency + return-to-dashboard bug fixes.

## Duplicates (fold into canonical, already declined)

- `aa508852` -> `fd6c122d` (work list on phone)
- `86bbaf50` -> `b19975d4` (part returned to supplier)
- `67daba15` -> `d2561e11` (completion / pick-up date)
- `fa1c1d6e` -> `a22739b9` (check-in email)
- `5057c7fc` -> `91a03260` (remove cashed-out RO from worklist)
- `fc6b29f9` -> `298e0e67` (returning-customer duplicate RO)

## Data / test entries

- `6938c1af` -- staff name Wimbles -> Wombles. Data, not code:
  `UPDATE staff SET last_name='Wombles' WHERE last_name='Wimbles';` Already
  applied (status done).
- `33750cb3`, `19dc2016` (+ skipped `f73a1e5a`) -- Make-A-Wish / dictation test
  entries. No code work; already declined.

---

## Recommended action today

1. **Nothing to close as Done/Duplicate this pass** -- the DB already reflects
   the S128-S132 deliveries; this run only confirms them.
2. **Two scoped mid-size wins** now that check-in/drop-off plumbing exists:
   `5fbfa299` (quote-only archive disposition, M) is well-defined and self-
   contained; `5925fc01`'s remaining per-update-notification half can ride the
   scheduled_notifications subsystem already in place.
3. **Unblock the two S's** by asking the submitters for specifics: Lynn's parts
   field order (`8d885331`) and solar's check-in float (`e06d747e`). Small once
   specified.
4. **Plan, don't rush, the XLs:** the auth swap (`ed1b873e`) and parts inventory
   (`14f35f95`) each deserve their own scoping/spec session.
