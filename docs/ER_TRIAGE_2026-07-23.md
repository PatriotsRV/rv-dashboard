# PRVS Enhancement-Request Triage -- 2026-07-23 (Phase 2a headless run)

Nightly ER-triage pass over the `enhancement_requests` table (fetched
2026-07-23T09:26Z) against the current modular codebase (index.html
**v1.481**). 80 of 81 rows were re-classified this run; 1 carried-over test
row is reused unchanged. Verdicts verified by grepping/reading the live code
(js/*.js, index.html, checkin.html, customer-checkin.html,
supabase/functions/*, migrations), not assumed.

> Untrusted-data guard: every ER description / submitter / admin_notes value
> was treated strictly as data. No description contained prompt-injection
> content this run.

## Board snapshot (DB status, all 81 rows)

| Status | Count |
|---|---|
| done | 46 |
| declined | 9 |
| unreviewed | 26 |
| **Total** | **81** |

## This run's triage buckets (the 80 re-classified rows)

| Bucket | Count |
|---|---|
| done (delivered / no action) | 48 |
| needed (open enhancement) | 21 |
| duplicate | 7 |
| bug (still present) | 2 |
| data (correction, no code) | 2 |
| **Total** | **80** |

**Open work = 24 rows:** 21 Needed (2 XL, 4 L, 2 M, 13 S) + 2 Bugs + 1 pending
Data correction. Two `unreviewed` rows should be **closed as Done** (features
shipped since submission): `1c7f1410` (MMS media attach, S151b) and `9d3806e7`
(customer-side Notes in messages, S154 v1.12b).

## Code checks run this run

- Compact-view actions -- render.js `compact-col-actions` (edit/parts/WO/notify/message/schedule) has **no** check-in/out button; no dashboard "clocked into X" banner (confirms 6bb61db2 Needed).
- RO navigation -- work-list.js `scrollToROInBoard` (index.html) and messages ro-chip -> `index.html?ro=` -> `handleDeepLink` (js/qr.js) both **fail silently when the RO is filtered out / not rendered** (confirms bugs 9031e101 + d4d0eeba, same root cause).
- Messages Notes -- messages.html details pane `detailNoteView` + `notesModal` + `customer_note_entries` (migration customer_note_entries_s154.sql) present (confirms 9d3806e7 Done).
- Messaging media -- messaging.js `msgAttachBtn`/`msgAttach` -> message-media bucket -> `media_url` to projectblue-send (confirms 1c7f1410 Done).
- Parts inventory / supplier-return -- js/parts.js has no stock/inventory or RMA/credit flow (confirms 14f35f95 XL, b19975d4 L still open).
- Auth -- auth.js still `signInWithIdToken` + Google One Tap + GOOGLE_CONFIG (confirms ed1b873e still open).
- Edge fns present: send-dropoff-report, send-admin-pnl-report, sync-ro-calendar, process-scheduled-notifications; module shop-tasks.js -- all confirm their respective Done verdicts.

---

## Open "Needed" enhancements by theme (21)

### Notifications & Task-Tracking
| ER | By | Date | LOE | Request | Verdict |
|---|---|---|---|---|---|
| `5925fc01` | Lynn | 04-09 | **L** | Notify the right team on every RO update; daily list of in-progress jobs with to-dos scraped from notes | Partial: daily digest served by the Manager Daily Report; unbuilt = notify on every generic RO update + to-do scraping from status notes into a per-job list. Anchor of this initiative. |
| `af14b3cb` | Lynn | 05-21 | **L** | Per-customer task tracker + daily reminder; tasks persist until marked complete | Confirmed absent: shop-tasks.js has no ro_id/customer link or reminder. Needs a per-RO tasks table + add/complete UI + daily open-task reminder. Pairs with 5925fc01. |

### Parts & Inventory
| ER | By | Date | LOE | Request | Verdict |
|---|---|---|---|---|---|
| `14f35f95` | Lynn | 04-09 | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | Confirmed absent in js/parts.js. New subsystem: inventory table + receive/consume/adjust + searchable priced stock + parts-request links. Largest board item. |
| `b19975d4` | Lynn | 04-09 | **L** | Part returned-to-supplier / warrantied-by-supplier tracking | Infra only (PART_STATUSES 'Returned' + dates); no RMA#, credit, reason, reverse flow. = GH#24 Shop Ops Phase 2. Canonical for dup 86bbaf50. |
| `8d885331` | Lynn | 04-09 | **S** | Rework parts-entry field order | Form exists (parts.js); only a field reorder. Quick once Lynn gives the target layout. Blocked on detail, not effort. |

### Time clock
| ER | By | Date | LOE | Request | Verdict |
|---|---|---|---|---|---|
| `e06d747e` | Riley | 04-10 | **S** | Tech check-in "float" for ease of access | Confirmed absent: no floating/quick tech check-in button. Underspecified; scope page + behavior with submitter. Pairs with 6bb61db2. |
| `6bb61db2` | Riley | 07-22 | **S** | Check-in/out button on compact view + login-time "what unit you're clocked into" reminder across all views | Confirmed absent: compact-col-actions has no check-in button; no dashboard clocked-in banner (checkin.html has the Still-Clocked-In prompt only). Small: compact check-in/out action + login active-time_log banner. |

### Auth & cost
| ER | By | Date | LOE | Request | Verdict |
|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | Confirmed still Google-based (auth.js signInWithIdToken + One Tap + GOOGLE_CONFIG; kiosk too). Swap to Supabase password/magic-link or shared role accounts; touches every page sign-in + RBAC. Roland to scope. |

### Messaging & e-sign
| ER | By | Date | LOE | Request | Verdict |
|---|---|---|---|---|---|
| `0448de62` | Sofia | 06-11 | **L** | Send an authorization form remotely for e-signature | Confirmed absent: signing is kiosk-only; raf_copy is a post-signing email, no tokenized public page. Needs tokenized public page + delivery + signed-form storage. Pairs with Sendblue Phase 3. |
| `5fec3eca` | Sofia | 07-15 | **S** | Show the customer's address on the emailed authorization-form copy | Confirmed absent: send-quote-email raf_copy branch omits customerAddress (present only in lead_staff_notify). Small: add customerAddress to the raf_copy payload + template. |

### RO fields/status
| ER | By | Date | LOE | Request | Verdict |
|---|---|---|---|---|---|
| `5fbfa299` | Brandon | 06-30 | **M** | Archive quoted-but-never-progressed customers/units | Confirmed absent: only the cashiered archive exists (ro-crud.js archiveROInSupabase); no archived/abandoned flag or filter. Pairs with 972af03c. |
| `972af03c` | Lynn | 07-17 | **S** | Add status "Canceled" -- keep the record for notes, do not delete | Confirmed absent: no Canceled in render.js ALL_STATUSES or config.js. Small: add status + color/priority maps, exclude from on-lot/queue counts, keep record. Pairs with 5fbfa299. |

### UI & Navigation
| ER | By | Date | LOE | Request | Verdict |
|---|---|---|---|---|---|
| `29654af7` | Lynn | 07-06 | **S** | Back-to-top / jump-to-search button after scrolling | Confirmed absent: index.html has only targeted scrollIntoView, no page-level scroll-to-top/jump-to-search FAB. Small. |
| `d1844e46` | Lynn | 07-16 | **S** | Sort dash by promised date then oldest on lot, shop ops pinned | Confirmed absent: board uses one blended calculatePriority; shop-ops pinning exists but no selectable strict sort. Small: add the strict sort option. |
| `d432863a` | Brandon | 07-21 | **S** | Make the tab icon the Patriots logo (favicon) | Confirmed absent: no favicon/apple-touch-icon/rel=icon in any page. Small: add a Patriots-logo favicon + apple-touch-icon to each page head. |

### Duplicates & Merge
| ER | By | Date | LOE | Request | Verdict |
|---|---|---|---|---|---|
| `0e172b2c` | Sofia | 07-16 | **M** | Merge customer accounts (verify which is correct, delete the wrong one) | Partial: duplicates.js merges by base RO id, not customer identity. Needs name/phone account-dedup + verify-which-is-correct merge/delete. Data cleanup for the Huebert case is 3f254929. |

### Messaging UI
| ER | By | Date | LOE | Request | Verdict |
|---|---|---|---|---|---|
| `9aab7550` | Brandon | 07-21 | **S** | Highlight new/unchecked messages more clearly | Partial: messages.html has a gold unread-dot but no row-level highlight. Small: bold row / tinted background for unread. |
| `21fc3a9d` | Brandon | 07-21 | **S** | Rescale the conversation-list text for readability | Confirmed absent: fixed rem sizes, no user text-size control. Small: persisted font-scale/zoom for the conversation list. |
| `f1e6e311` | Brandon | 07-21 | **S** | Clear (x) button in the messaging search bar | Confirmed absent: convoSearch is a plain input with no clear button. Small: add a clear (x) button. |
| `6e9ca4dc` | Brandon | 07-21 | **S** | Remove the redundant "Added to every send" signature preview at chat bottom | Valid: messaging.js refreshSignaturePreview renders a persistent msgSigPreview; users find it redundant. Small: remove or collapse it. |

### Work Orders
| ER | By | Date | LOE | Request | Verdict |
|---|---|---|---|---|---|
| `2f752d40` | Jason | 07-21 | **S** | Auto-populate WO dollar amount from total hours x $195/hr | Confirmed absent: work-orders.js has manual estimated_hours + dollar_value, no auto-calc. Small: auto-populate dollar_value = hours x $195, editable override. |

---

## Bugs (still present in current code) (2)

| ER | By | Date | Request | Verdict |
|---|---|---|---|---|
| `9031e101` | Riley | 07-21 | Tapping a unit in the Work list on iPhone doesn't open the right unit | Bug (mobile, small): work-list.js click -> scrollToROInBoard (index.html) only finds cards already rendered; if the RO is filtered out/off-screen it toasts "RO not visible" instead of navigating. Same root cause as d4d0eeba; fix = unfilter + scroll to the tapped RO. |
| `d4d0eeba` | Brandon | 07-22 | Fix tracking when clicking an RO in my list or a link in the messages section | Bug confirmed: work-list click (scrollToROInBoard) AND the messages ro-chip -> index.html?ro= -> handleDeepLink (js/qr.js: "if(!card)return") both fail silently when the RO is filtered/not rendered. Same root cause as 9031e101; fix both -- unfilter + scroll to target. Severity: small (navigation). |

## Data corrections (no code change) (2)

| ER | By | Date | Request | Verdict |
|---|---|---|---|---|
| `6938c1af` | Ryan | 05-06 | Zak/Rod/Travis last name Wimbles -> Wombles in tech assignment | Data fix; already applied per S117. If any remain: `UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |
| `3f254929` | Sofia | 07-16 | Delete misspelled "Hubert" account; correct is "Steve Huebert" | Data correction (pending Roland). Find: `SELECT id,customer_name,status FROM repair_orders WHERE customer_name ILIKE '%hubert%';` then soft-delete the wrong RO after confirming the id. Merge feature is 0e172b2c. |

## Duplicates (fold into canonical; keep declined) (7)

| ER | By | Date | Request | Folds into |
|---|---|---|---|---|
| `aa508852` | Riley | 04-09 | Work list: can't delete, too small on phone, no drag-drop | -> `fd6c122d` (Done). |
| `86bbaf50` | Lynn | 05-13 | Gordon parts missing / window ordered and returned to Vogt | -> `b19975d4` (Needed/L, supplier return/warranty). |
| `67daba15` | Brandon | 06-09 | RO selection for completion / pickup date | -> `d2561e11` (Done, pickup_date). |
| `fa1c1d6e` | Sofia | 06-09 | Returning customer (Sheldon Gilbert) check-in did not email | -> `a22739b9` (Done, RAF email forced). |
| `5057c7fc` | Roland | 06-09 | Remove cashed-out RO from worklist day after Delivered | -> `91a03260` (Done, prune_cashiered_worklists). |
| `fc6b29f9` | Sofia | 06-17 | Returning-customer test duplicated the RO | -> `298e0e67` (Done, reuse Scheduled RO). |
| `5c45bb8d` | Sofia | 06-19 | David Flanagan scheduled check-in duplicated + no RAF email | -> `298e0e67` + `a22739b9` (both Done). |

## Delivered / Done (48)

| ER | By | Date | Request | Evidence |
|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | TEST entry (Make-A-Wish trial) | Not a real request; keep declined. |
| `79b2c805` | Sofia | 04-09 | See/edit cashed-out customers to add documents | closed-ros.html reactivateRO restores archived children. |
| `75124b03` | Ryan | 04-09 | Warranty category on new RO creation | warrantyROBtn -> ro-crud.js setROType; render.js badge/chip. |
| `b2aa4bea` | Lynn | 04-09 | Date added for scheduled future drop | planned_dropoff_date + reminder + Drop chip. Same as 468aa376. |
| `18611eee` | Lynn | 04-09 | Work list opened but couldn't view/act after adding | work-list.js add/remove/silo + drag persist to manager_work_lists. |
| `17492066` | Lynn | 04-09 | Future person-ahead-of-a-date notifications | scheduled_notifications + process-scheduled-notifications cron. |
| `fd6c122d` | Riley | 04-09 | Work list: can't delete, too small on phone, no drag-drop | work-list.js remove btn + draggable reorder + mobile layout. Canonical for aa508852. |
| `98bd7498` | Brandon | 04-09 | Different work lists per category | work-list.js silo tabs filter manager_work_lists by service_silo. |
| `19dc2016` | Roland | 04-11 | TEST entry (ER form dictation) | Not a real request; keep declined. |
| `1fe68261` | Rod | 04-13 | Tasks not tied to a customer unit | shop-tasks.js add/complete/reopen/delete standalone tasks. |
| `3dfc0acb` | Lynn | 04-14 | Status "Ready to Work" | index.html status option + filter; render.js color; ALL_STATUSES. |
| `b87eb2fb` | Lynn | 04-14 | Keep-plugged-in sign; keys/no-keys/keypad | Keys select + keypad code on RO modal; ro-crud.js persist; chips. Same as 34fc03c2. |
| `a5ff3e2d` | Lynn | 04-21 | RO type Warranty + Repair | ro_type warranty_repair on New/Edit RO + filter + badge. |
| `a7d1474e` | Lynn | 04-23 | Always-visible urgent-updates field + notify + font/color | urgent_update field + _notifyUrgentUpdate email; migration adds column. |
| `dde90d2b` | Roland | 04-27 | Fix/streamline check-in; remove photo; ensure email sent | Photo optional; RAF email forced on every submission (CC staff). |
| `13e1cd87` | Tipton | 04-30 | Back-to-dashboard button after clock out (iPhone) | checkin.html Return to Dashboard on clock-out screen -> index.html. |
| `e27311bf` | Ryan | 05-04 | Edit WO templates, drag-drop reorder | work-orders.js _initWOTaskDrag + saveWOTemplate sort_order. |
| `d2561e11` | Brandon | 05-06 | Completion / pickup date on customer order | pickup_date field + green Pickup chip. Canonical for 67daba15. |
| `dac9fdda` | Brandon | 05-06 | On-lot status for temporarily-removed returning units | "Off Lot - Returning" status + filter; excluded from on-lot count. |
| `a8e90a7d` | Brandon | 05-20 | Parts + freight total in manage parts | parts.js Ticket Totals (parts/freight/total/retail). Also satisfies 671fa623. |
| `1aeb3f58` | Lynn | 05-21 | Promised date large/highlighted, sort by promise then days | Blended calculatePriority weights promised + daysOnLot; chips. Strict sort is open d1844e46. |
| `cbc70a86` | Lynn | 05-21 | Returning check-in auto-adds to team leads' work lists | customer-checkin.html rpc add_ro_to_silo_work_lists inserts manager_work_lists. |
| `3f415c88` | Lynn | 05-26 | Schedule multiple/weekly reminders until complete | index.html MAX_OCCURRENCES expands daily/weekly rows until a date. |
| `cd663d8f` | Bobby | 05-28 | Money rounding drops the .00 | ro-crud.js + parts.js toFixed(2) on populate; cosmetic, no data loss. |
| `34fc03c2` | Brandon | 05-29 | Confirm keys left with unit | Keys/keypad build on RO modal; persisted; chips. Same as b87eb2fb. |
| `0b2e128c` | Lynn | 06-02 | Transfer clock-in job notes to RO updates | checkin.html inserts a "TECH NOTES:" ro_status line at clock-out. |
| `468aa376` | Lynn | 06-02 | Scheduled drop date in entry dash | plannedDropoffDate on New/Edit RO + kiosk. Same as b2aa4bea. |
| `2b814250` | Roland | 06-07 | Date components -> business calendar + notifications | sync-ro-calendar edge fn (idempotent per-date-type x silo events) + advance notice. |
| `cd084233` | Roland | 06-07 | Admin daily manager-worklist / P&L report | send-admin-pnl-report + worklist-report.html + cron. Manager scoring deferred. |
| `a22739b9` | Sofia | 06-08 | Check-in email + auth-form copy not received | sendRAFEmail on every drop-off (CC staff); raf_copy attaches signature. Canonical for fa1c1d6e. |
| `91a03260` | Roland | 06-09 | Remove cashed-out RO from worklist the day after Delivered | prune_cashiered_worklists() cron. Canonical for 5057c7fc. |
| `cdd77a8b` | Brandon | 06-10 | Total RVs: on-lot vs total customers gauge | render.js statTotalOnLot sub-line; Scheduled/Off-Lot excluded. |
| `2c3d5633` | Ryan | 06-12 | Can't upload pictures from phone | photos.js appends input before click() (iOS fix) + HEIC/HEIF convert. |
| `74a33621` | Brandon | 06-16 | Arrow keys to move through photos | photos.js Arrow keys -> navigateLightbox; handler on open/close. |
| `298e0e67` | Sofia | 06-16 | Returning check-in duplicates the RO | customer-checkin.html reusedScheduledRO UPDATEs the Scheduled RO. Canonical for fc6b29f9, 5c45bb8d. |
| `9b823d25` | Roland | 06-16 | Notify silo manager when a service is added | work-orders.js addServiceToRO enqueues service_added_notify. |
| `9b046808` | Brandon | 06-17 | Status "scheduled to arrive" (not yet on lot) | render.js excludes Scheduled from days-on-lot + on-lot count. |
| `671fa623` | Brandon | 06-18 | Include freight in ticket totals | parts.js Ticket Totals include freight. Same as a8e90a7d. |
| `b06a285f` | Lynn | 06-19 | Important-dates hover list near customer name | render.js kd-name-cal + kd-pop; dashboard.css hover/focus reveal. |
| `ee50d11c` | Lynn | 06-19 | Can't edit time-off entries | time-off.html editingRequestId + editRequest UPDATE + per-row Edit. |
| `88395acc` | Lynn | 06-19 | Time-off exact from-to times (half/whole day) | time-off.html absence_start_time/end_time range on chips. |
| `80390f36` | Lynn | 06-23 | Pin shop-operations tile to top / searchable | render.js shop-first sort + synthetic search token. |
| `7f2d0d70` | Lynn | 06-26 | Monday drop-off report so teams confirm worklist | send-dropoff-report edge fn grouped by silo w/ worklist flag. Shipped since S128. |
| `3f7fb4fc` | Brandon | 06-29 | Remove shop RO status dropdown; exclude from counts | render.js static status for Shop Ops; excluded from queue/on-lot. |
| `cb7742a8` | Lynn | 06-29 | Check-in scheduled date not on team calendar | customer-checkin.html POSTs to sync-ro-calendar per silo after create. |
| `39b75fab` | Sofia | 07-09 | Mis-filed scanned docs; can't remove documents | photos.js deleteLibraryItem + move-file modal, gated by canManageFiles(). |
| `1c7f1410` | Andrew | 07-16 | Messaging: no ability to attach media | NOW DELIVERED (S151b): msgAttach -> message-media bucket -> media_url to projectblue-send. **Recommend close as done.** |
| `9d3806e7` | Brandon | 07-22 | Notes section on the customer side in messages | Delivered (S154 v1.12b): messages.html Notes pane + modal -> customer_note_entries. **Recommend close as done.** If a different placement is wanted, reopen as Needed. |

## Carried over (not re-triaged this run)

| ER | By | Status | Note |
|---|---|---|---|
| `f73a1e5a` | Roland | declined | Test dictation entry; reused unchanged (no triage bucket). Keep declined. |

---

## Recommended action today

1. **Close 2 as Done** (shipped since submission): `1c7f1410` (MMS media
   attach) and `9d3806e7` (customer Notes in messages). Roland flips status.
2. **Fix the navigation bug class together** -- `9031e101` + `d4d0eeba` share
   one root cause: `scrollToROInBoard`/`handleDeepLink` bail when the target RO
   is filtered out or not rendered. Small fix: clear filters + scroll to the
   target RO on work-list tap and on `?ro=` deep-link. Front of the line.
3. **Batch the S messaging-UI wins** -- `f1e6e311` (clear-search x), `6e9ca4dc`
   (drop the redundant signature preview), `9aab7550` (unread row highlight),
   `21fc3a9d` (conversation-list font scale). All small, same file
   (messages.html / messaging.js), ship as one pass.
4. **Other quick S wins**: `d432863a` (Patriots favicon), `29654af7`
   (back-to-top FAB), `972af03c` (Canceled status), `2f752d40` (WO dollar
   auto-calc), `5fec3eca` (address on RAF copy), `d1844e46` (strict sort).
5. **Unblock the S's that need submitter detail**: `8d885331` (parts field
   order) and `e06d747e` (tech check-in float) -- small once specified.
6. **Emit the pending Data SQL** for `3f254929` (Huebert account) for Roland to
   run after he confirms the RO id.
7. **Plan, don't rush, the XL/L's**: auth swap (`ed1b873e`), parts inventory
   (`14f35f95`), supplier returns (`b19975d4`), remote e-sign (`0448de62`),
   task-tracking (`5925fc01`/`af14b3cb`), account merge (`0e172b2c`) -- each
   deserves its own scoping session.
