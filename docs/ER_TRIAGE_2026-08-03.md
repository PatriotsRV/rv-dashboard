# PRVS Enhancement-Request Triage - 2026-08-03 (headless nightly runner)

Incremental pass over `enhancement_requests` (snapshot fetched 2026-08-03
10:46 UTC) against the current modular codebase at **HEAD `7e8fadc`** -
index.html **v1.487**, checkin.html **v1.38**, customer-checkin.html **v1.17**,
messages.html **v1.17**. 88 rows in the table; **87 re-classified this pass**
(`needs_triage_ids`), 1 carried over untouched.

Every verdict below was re-verified by grepping / reading the live code at this
HEAD, not assumed. Read-only run: **no code changed, no DB writes, no git
commands.** Data-bucket SQL is emitted for Roland to run by hand.

## Board snapshot

| Status | Count |
|---|---|
| done | 49 |
| declined | 9 |
| unreviewed | 30 |
| **Total** | **88** |

## Bucket snapshot (this pass, 87 ERs)

| Bucket | Count | Notes |
|---|---|---|
| done | 51 | includes 2 test entries and 1 (`9d3806e7`) still `unreviewed` in the DB |
| needed | 23 | 2 XL, 4 L, 2 M, 15 S |
| duplicate | 9 | all fold into a named canonical ER |
| bug | 2 | both confirmed still present; small / data-display |
| data | 2 | 1 already applied (residual seed only), 1 open one-off cleanup |

Headline: **the open work is now mostly small.** 15 of the 23 open enhancements
are S, and the two confirmed bugs are both low-risk. The heavy items
(`14f35f95` parts inventory, `ed1b873e` auth swap) are unchanged and still want
their own scoping sessions.

## Code checks run this session (at HEAD `7e8fadc`)

Absence confirmed by zero-hit greps across `js/`, `supabase/`, `*.html`, `css/`:

- `ro_tasks` / `task_tracker` / `customer_task` - **none** (confirms `af14b3cb`, `5925fc01`)
- `inventory` / `on_hand` / `in_stock` / `stock_qty` - **none** (confirms `14f35f95`)
- `rma` / `return_reason` / `supplier_credit` - **none** (confirms `b19975d4`)
- `signing_token` / `remote_sign` / `signature_request` - **none** (confirms `0448de62`)
- `backToTop` / `back-to-top` / `scroll-to-top` / `scrollToTop` - **none** (confirms `29654af7`, `2b78696c`)
- `font-scale` / `fontScale` / `textSize` - **none** (confirms `21fc3a9d`)
- `favicon` / `rel=icon` / `apple-touch-icon` - **none** (confirms `d432863a`)
- `Canceled` / `Cancelled` in `js/config.js` + `js/render.js` - **none** (confirms `972af03c`)
- `archived` / `abandoned` in `js/config.js` + `js/render.js` - **none** (confirms `5fbfa299`)
- `LABOR_RATE` / `labor_rate` in `js/work-orders.js` - **none**; the only
  `laborRate` hits are `js/insurance.js` (per-claim insurance rate) and
  `solar.html` (solar quote rate) - neither is the posted shop rate (confirms `2f752d40`)
- `video` in `js/messaging.js` + `messages.html` - **none** (confirms `1c7f1410`)
- `customerAddress` in the `raf_copy` branch of `send-quote-email` - **absent**;
  present only in `lead_staff_notify` (confirms `5fec3eca`)

Presence re-confirmed for the two bugs (see the Bugs section) and spot-verified
for every Done citation in the tables below.

---

## Open enhancements - 23 "Needed" ERs by theme, then size

### Messaging & e-sign (largest cluster - 6, five of them S)

| ER | By | Date | Category | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `0448de62` | Sofia | 06-11 | General UI/UX | **L** | Send an authorization form remotely for e-signature | `customer-checkin.html` signature pad; `send-quote-email` L386 | Kiosk-only signing today; the `raf_copy` branch only emails a copy after signing. Needs a tokenized public signing page, a delivery path and signed-form storage. Pairs with the SMS channel. |
| `1c7f1410` | Andrew Page | 07-16 | New Feature Idea | **S** | Attach media (photos or **videos**) in messaging | `js/messaging.js` L219, L314 | Photo/PDF half shipped (S151b/S158); the VIDEO half did not. `input#msgAttach` accepts only `image/*,application/pdf`; `_handleAttachPick` branches on `isPdf` else compresses. Add `video/*`, skip compression, keep the 5MB MMS cap. |
| `9aab7550` | Brandon | 07-21 | General UI/UX | **S** | Highlight unchecked new messages more clearly | `messages.html` L334 (CSS), L1089, L1095 | Partial: a gold `.unread-dot` renders, but only when `last_direction='inbound' AND status='open'`, and there is no row-level highlight. Add a stronger unread treatment and widen the condition so a reopened thread also flags. |
| `21fc3a9d` | Brandon | 07-21 | General UI/UX | **S** | Rescale the conversation-list text | `messages.html` (fixed rem sizes) | No user text-size control anywhere. Add a persisted font-scale control for the conversation list. |
| `f1e6e311` | Brandon | 07-21 | General UI/UX | **S** | Clear button for the messaging search field | `messages.html` L469 (`convoSearch`), reset only at L1001/L1020 | Plain text input with no clear affordance; its only sibling is the New-conversation button. Add an (x) that empties the field and re-runs the filter. |
| `6e9ca4dc` | Brandon | 07-21 | General UI/UX | **S** | Remove the "Added to every send" line under every chat | `js/messaging.js` L241/L246 (`#msgSigPreview` L222); `messages.html` L430 inside the `max-width:760px` query (L417) | Still visible on **desktop** and in the RO-card composer - it is hidden only in the mobile media query. Remove it or put it behind a toggle in all viewports; the signature keeps sending either way. |

### Parts & Inventory

| ER | By | Date | Category | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `14f35f95` | Lynn | 04-09 | Parts & Ordering | **XL** | In-house parts stock (order 25, use 5, 20 to searchable priced stock) | `js/parts.js` (per-RO line items only) | Confirmed absent. New subsystem: inventory table, receive/consume/adjust flows, a searchable priced stock view, links from the parts-request path. Largest item on the board. |
| `b19975d4` | Lynn | 04-09 | Parts & Ordering | **L** | Part returned to / warrantied by the supplier | `js/config.js` L200 `PART_STATUSES`; `js/parts.js` L226 `pf_returnDeadline` | Partial infra only: a 'Returned' status and a Return Deadline date exist; no RMA number, return reason, supplier credit or reverse-direction flow. Equals GH#24 Shop Ops Phase 2. Canonical for `86bbaf50`. |
| `8d885331` | Lynn | 04-09 | Parts & Ordering | **S** | Reorder the parts-entry fields | `js/parts.js` L181, L190, L200, L204, L208, L226 | Form unchanged; the ask is only field order. Quick once Lynn supplies the target layout or a mockup. **Blocked on detail, not effort.** |

### Notifications & Task-Tracking

| ER | By | Date | Category | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `5925fc01` | Lynn | 04-09 | New Feature Idea | **L** | Notify the right team on every RO update; daily in-progress list with to-dos scraped from status notes | `send-manager-report`; `js/work-orders.js` L293; `js/ro-crud.js` L370 | **Partial.** Daily digest ships; single-event notifies exist. Still absent: notify-on-every-update and to-do scraping from notes. Anchor of this theme; pairs with `af14b3cb`. |
| `af14b3cb` | Lynn | 05-21 | General UI/UX | **L** | Per-customer task tracker with a daily reminder until complete | `js/shop-tasks.js` (no `ro_id`, no reminder column) | Confirmed absent. `shop-tasks.js` is a non-customer shop checklist. Needs a per-RO tasks table, an add/complete UI and a daily open-task reminder. |
| `5fec3eca` | Sofia | 07-15 | General UI/UX | **S** | Show the customer address on the emailed authorization-form copy | `send-quote-email` L386-387 (`raf_copy`), L549/L674/L748 (`lead_staff_notify`) | `raf_copy` never receives `customerAddress`; the staff-notify branch does. Add the field to the `raf_copy` payload and both templates. |

### RO fields / status

| ER | By | Date | Category | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `5fbfa299` | Brandon | 06-30 | General UI/UX | **M** | Archive customers/units that were only ever quoted | `js/ro-crud.js` L658/L692 (cashiered archive only) | The only archive path is the cashiered one; no soft-archive flag, status or board filter. Needs a flag, a board filter and an un-archive path. Pairs with `972af03c`. |
| `972af03c` | Lynn | 07-17 | General UI/UX | **S** | Add a Canceled status - keep the record for notes | `js/config.js` L151-163; `js/render.js` L741-745, L727-739 | No Canceled status exists; both status maps end at Delivered/Cashed Out. Add the status plus color/priority maps, exclude it from the on-lot and queue counts, keep the record. |
| `50175fce` | Lynn | 07-27 | General UI/UX | **S** | Move the urgent-update **input** onto the tile | `js/ro-crud.js` L414/L491/L1107; `js/render.js` L324 | `urgent_update` is writable only from New/Edit RO; the card banner is read-only and even says "change it via Edit RO". Add inline edit-in-place at the tile bottom reusing the existing `editField` / notify path so `urgent_update_notify` still fires. Follow-on to `a7d1474e`. |

### Board & Navigation UX (theme extension this pass)

| ER | By | Date | Category | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `29654af7` | Lynn | 07-06 | General UI/UX | **S** | Back-to-top / jump-to-search button | none (only targeted `scrollIntoView` on the board) | Confirmed absent. Add a scroll-triggered back-to-top / jump-to-search control, present in the sidebar layout too. Canonical for `2b78696c`. |
| `d1844e46` | Lynn | 07-16 | General UI/UX | **S** | Sort by promised date, then oldest on lot, shop ops pinned | `js/render.js` L30-39 (blended `calculatePriority`), pin L35-37 | The board has one blended score and no selectable sort; `sortBy`/`sortSelect`/`sortMode` exist only in `closed-ros.html` and `solar.html`. Add the strict sort option while keeping the shop pin. Related to `1aeb3f58` (chip only). |
| `d432863a` | Brandon | 07-21 | General UI/UX | **S** | Patriots-logo tab icon | no `favicon` anywhere | Every tab shows the generic browser icon. Add a favicon plus `apple-touch-icon` to each page head; `logo.png` ships already but needs resizing to icon dimensions. |

### Time clock

| ER | By | Date | Category | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `e06d747e` | Riley Scott | 04-10 | Time Clock / Check-In | **S** | Tech check-in "float" for ease of access | `js/render.js` L228-235 (`compact-col-actions`) | No floating/sticky check-in control on index.html or checkin.html; the compact actions are edit / parts / WO / notify / message / schedule only. **Underspecified** - scope page + behavior with the submitter. Pairs with `6bb61db2`. |
| `6bb61db2` | Riley Scott | 07-22 | Time Clock / Check-In | **S** | Check-in/out button in compact view + a live "am I clocked in" indicator on login | `js/render.js` L228-235; nothing in `js/time-tracking.js` | Both halves absent. Add a compact check-in/out action plus a login-time active-`time_log` banner shown across views. Pairs with `e06d747e`. |

### Work Orders & Shop tasks

| ER | By | Date | Category | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `2f752d40` | Jason Rubin | 07-21 | Work Orders | **S** | Auto-populate the WO dollar amount from total hours x $195/hr | `js/work-orders.js` L360, L365, L649-650, gate L324 | Dollar and hours are independent manual inputs with no labor-rate arithmetic in the module. Auto-populate `dollar_value` = effective hours x $195/hr, respecting the `dollarLocked` readonly gate. |

### Duplicates & Data Integrity (theme extension this pass)

| ER | By | Date | Category | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `0e172b2c` | Sofia | 07-16 | General UI/UX | **M** | Merge accounts created under different spellings (Hubert / Huebert) | `js/duplicates.js` L35-46, L150 | Partial: an RO-level merge/delete engine exists but groups strictly on the `PRVS-XXXX-XXXX` base RO id, so two ROs under different customer spellings are never detected. Needs name/phone account dedup plus a verify-which-is-correct step. One-off cleanup is `3f254929`. |

### Auth & cost

| ER | By | Date | Category | LOE | Request | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | General UI/UX | **XL** | Replace ~$500/mo Google-email auth with DB-specific auth | `js/auth.js` L69, L203-204, L541, L566, L978 | Still fully Google: `GOOGLE_CONFIG` + gapi + `google.accounts` One Tap (with the v1.486 `renderButton` fallback) + `signInWithIdToken`; the kiosk shares the path. A Supabase password / magic-link or shared-role swap saves the seats but touches every page sign-in plus the email-keyed RBAC. Scope with Roland. |

---

## Bugs - 2, both confirmed still present at HEAD `7e8fadc`

| ER | By | Date | Category | Severity | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `d4d0eeba` | Brandon | 07-22 | General UI/UX | **small** (navigation, no data risk) | `index.html` L4162, L4177; `js/qr.js` L198, L207, L210; `js/work-list.js` L328 | Two paths, one root cause. Work list: `scrollToROInBoard` only matches already-rendered `[data-ro-index]` cards, else toasts "RO not visible" - and that toast's Clear Filters action re-renders but never then scrolls. Messages: `handleDeepLink` returns silently when the RO is not in `currentFilteredData` or has no card. **Canonical for `9031e101` and `1bc33dd6` - one fix closes all three.** Fix: clear filters, re-render, scroll, highlight. |
| `d94a418a` | Brandon | 07-27 | General UI/UX | **data-display** (render only, no data loss) | `js/render.js` L553, L563; `js/ro-crud.js` (joins entries with newline) | `L553` builds the card RO Status box with `split(newline) + map(escapeHtml) + reverse()`, reversing **every line**, so a multi-line note renders bottom-line-first - exactly Brandon's "latter portion near the top". `L563` does the same to Customer Comm. Entry boundaries are already lost upstream. Fix: reuse the S159 history-modal entry-regex split for both boxes. |

Both fall inside the spec's auto-fixable class (small / data-display) and are the
front-of-the-line candidates for the next build session.

## Data corrections - 2 (SQL for Roland; nothing was executed)

| ER | By | Date | State | Verdict + SQL |
|---|---|---|---|---|
| `6938c1af` | Ryan | 05-06 | already applied | Tech names read from `staff.name` and the live DB is correct (submitter "Rod Wombles" spells right in this very table). **Residual, not a runtime bug:** `supabase/migrations/staff_table.sql` L85-87 still seeds Rod/Zak/Travis as "Wimbles", so a re-seed reintroduces it. Re-apply if ever needed: `UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |
| `3f254929` | Sofia | 07-16 | **open** | Keep Steve **Huebert**, remove the misspelled Hubert RO. Eyeball first: `SELECT id,ro_id,customer_name,status FROM repair_orders WHERE customer_name ILIKE '%ubert%';` then, once the wrong id is confirmed: `UPDATE repair_orders SET deleted_at=now() WHERE id='<wrong-id>';` The merge feature itself is `0e172b2c`. |

## Duplicates - 9 (fold into the canonical ER)

| ER | By | Date | Folds into | Why |
|---|---|---|---|---|
| `aa508852` | Riley Scott | 04-09 | `fd6c122d` (Done) | Identical text and timestamp - cannot delete jobs / too small on phone / no drag-drop. |
| `86bbaf50` | Lynn | 05-13 | `b19975d4` (Needed/L) | A Gordon window ordered and returned to Vogt is supplier return / warranty tracking. |
| `67daba15` | Brandon | 06-09 | `d2561e11` (Done) | RO completion / pick-up date for records - ships as `repair_orders.pickup_date`. |
| `fa1c1d6e` | Sofia | 06-09 | `a22739b9` (Done) | Same missing check-in email (Sheldon Gilbert); both sends are now forced. |
| `5057c7fc` | Roland | 06-09 | `91a03260` (Done) | Same request, same day - prune a cashed-out RO from the work list the day after. |
| `fc6b29f9` | Sofia | 06-17 | `298e0e67` (Done) | Retest report of the returning-customer duplicate-RO guard. |
| `9031e101` | Riley Scott | 07-21 | `d4d0eeba` (**Bug**) | Same iPhone work-list click-through failure; same `scrollToROInBoard` root cause. |
| `1bc33dd6` | Brandon | 07-27 | `d4d0eeba` (**Bug**) | Brandon's own re-report of the click-to-scroll defect. |
| `2b78696c` | Lynn | 07-27 | `29654af7` (Needed/S) | Same back-to-top button Lynn filed on 07-06. |

---

## Delivered - 51 "Done" verdicts (all re-verified at HEAD `7e8fadc`)

| ER | By | Date | Category | Mapped code | Verdict |
|---|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | General UI/UX | - | Test entry ("Testing out the Make-A-Wish function"). Already declined; keep declined. |
| `19dc2016` | Roland | 04-11 | Time Clock / Check-In | - | Test entry (ER-form dictation trial). Already declined; keep declined. |
| `79b2c805` | Sofia | 04-09 | New Feature Idea | `closed-ros.html` L838/L725/L813; `js/photos.js` L253/L207 | `reactivateRO` restores a cashed-out RO from `cashiered_child_archives` so it can be edited; `uploadDocument` adds documents to any RO. |
| `75124b03` | Ryan | 04-09 | RO Management | `js/ro-crud.js` L1119; `js/render.js` L122/L196/L373 | v1.402: Warranty is a first-class RO type at creation, with board filter, compact chip and card badge. |
| `b2aa4bea` | Lynn | 04-09 | New Feature Idea | `js/ro-crud.js` L409/L486; `js/render.js` L395; `key_dates_reminder_sources.sql` | `planned_dropoff_date` on create and edit, drives the blue Drop chip and the day-before manager reminder. Same field as `468aa376`. |
| `18611eee` | Lynn | 04-09 | Bug Report | `js/work-list.js` L195/L210/L223/L263/L304/L356 | Was a real bug. Added members are viewable, reorderable (mouse + touch) and removable, with per-silo tabs. |
| `17492066` | Lynn | 04-09 | Notifications / Email | `scheduled_notifications.sql`; `process-scheduled-notifications`; `index.html` L2944/L3012 | Per-RO Schedule Notification bell with future date/time, recipients and daily/weekly repeat-until. Covers the advance-warning ask. |
| `fd6c122d` | Riley Scott | 04-09 | Work List | `js/work-list.js` L195/L210/L304/L356/L383/L404 | v1.460: delete + drag-reorder on mouse and touch, persisted; mobile sizing fixed. Canonical for `aa508852`. |
| `98bd7498` | Brandon | 04-09 | RO Management | `js/work-list.js` L83/L223/L263 | Per-silo Work List tabs give a separate list per job category. |
| `1fe68261` | Rod Wombles | 04-13 | Work Orders | `js/shop-tasks.js` L21/L41/L153/L180; `shop_tasks.sql` | v1.467 (S128): internal work not tied to a customer unit, separate from ROs and the board - matches the poly-barrel / roof-bay examples. |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | `js/config.js` L158; `js/render.js` L741-745/L727-739 | v1.410: "Ready to Work" is a live status in both maps and the status dropdowns. |
| `b87eb2fb` | Lynn | 04-14 | RO Management | `keys_power_fields.sql`; `js/ro-crud.js` L411-413/L488-490 | v1.458: `key_status`, `keypad_code`, `keep_plugged_in` persisted on create and edit, keys chip on the card. Same build as `34fc03c2`. |
| `a5ff3e2d` | Lynn | 04-21 | RO Management | `js/ro-crud.js` L1119; `js/render.js` L122/L196/L373 | v1.465: "Warranty + Repair" RO type end to end, still billable so the repair half bills through work orders. |
| `a7d1474e` | Lynn | 04-23 | RO Management | `urgent_update_field.sql`; `js/render.js` L324/L214; `js/ro-crud.js` L370 | v1.466: always-visible red URGENT banner on the card; setting it notifies silo manager(s) + admins. Lynn's follow-up to move the input onto the tile is the open `50175fce`. |
| `dde90d2b` | Roland | 04-27 | RO Management | `customer-checkin.html` L2530/L2603 | Photo optional and both emails forced on every submission (`raf_copy` + `lead_staff_notify`). |
| `13e1cd87` | Tipton Scott | 04-30 | Time Clock / Check-In | `checkin.html` L1516 (ES string L441) | Was a real gap; v1.447 added a "Return to Dashboard" button after clock-out, so no new tab is needed for the next job. |
| `e27311bf` | Ryan | 05-04 | Work Orders | `js/work-orders.js` L461/L385/L583 | v1.464: every WO task row has a grip handle and drag-to-reorder; DOM order drives `sort_order`, so templates are edited by load-reorder-Overwrite. |
| `d2561e11` | Brandon | 05-06 | New Feature Idea | `key_dates_pickup_date.sql`; `js/ro-crud.js` L410/L487; `js/render.js` L397/L365 | v1.457: `pickup_date` plus the green Pickup chip and key-dates panel. Canonical for `67daba15`. |
| `dac9fdda` | Brandon | 05-06 | General UI/UX | `js/config.js` L154; `js/render.js` L727-739/L718-721 | v1.460: "Off Lot - Returning" separates units the customer temporarily took from units never on the lot, and is excluded from the on-lot count. |
| `a8e90a7d` | Brandon | 05-20 | General UI/UX | `js/parts.js` L152-155/L159-161 | v1.456 (S117): Manage Parts Ticket Totals bar sums parts, freight and total cost. Same build as `671fa623`. |
| `1aeb3f58` | Lynn | 05-21 | General UI/UX | `js/render.js` L396/L30-39 | v1.457: promised date as a large bold chip, red when overdue; promise date + days-on-lot both feed the blended sort. The strict selectable sort is the open `d1844e46`. |
| `cbc70a86` | Lynn | 05-21 | General UI/UX | `customer-checkin.html` L2381/L2375; `work_list_autoadd_rpc.sql` | S128: check-in calls `add_ro_to_silo_work_lists()` (SECURITY DEFINER), inserting `manager_work_lists` rows per involved silo lead with an sr-manager fallback. |
| `3f415c88` | Lynn | 05-26 | General UI/UX | `index.html` L2944/L3012 | v1.463: Repeat none/daily/weekly plus repeat-until, expanded into one individually cancellable `scheduled_notifications` row per occurrence. |
| `cd663d8f` | Bobby Thatcher | 05-28 | General UI/UX | `js/ro-crud.js` L1093; `js/parts.js` L307/L310 | Was a cosmetic bug; v1.447 shows 2 decimals on edit-populate and in the parts modal. No data was ever lost. |
| `34fc03c2` | Brandon | 05-29 | New Feature Idea | `keys_power_fields.sql`; `js/ro-crud.js` L411-413/L488-490 | v1.458: Keys selector (keys left / no keys / keypad + code, plus keep-plugged-in) on New and Edit RO, with a keys chip per card. Covers `b87eb2fb`. |
| `0b2e128c` | Lynn | 06-02 | General UI/UX | `checkin.html` L1455 (header note L5) | v1.460: a tech's clock-out work notes are copied into the RO update timeline as a `ro_status` note row. |
| `468aa376` | Lynn | 06-02 | General UI/UX | `js/ro-crud.js` L409/L486; `js/render.js` L395 | Planned drop-off date on New RO and (restored v1.457) Edit RO, feeding the Drop chip and `auto_dropoff_reminder`. Same field as `b2aa4bea`. |
| `2b814250` | Roland | 06-07 | Calendar | `js/render.js` L395-398/L367; `sync-ro-calendar`; `js/scheduling.js` L476 | Key dates surface as colored attention chips plus a hover/tap panel; calendar sync is server-side and idempotent via `cal_event_ids`; advance reminders go to managers/admins. |
| `cd084233` | Roland | 06-07 | Work List | `send-manager-report`; `send-admin-pnl-report`; `worklist-report.html` | Admin-level daily manager-worklist reporting with parts + labor + cost rollups. The manager **scoring** layer at the end of the request stays a future extension, not this ER. |
| `a22739b9` | Sofia | 06-08 | General UI/UX | `customer-checkin.html` L2530/L2603 | Was a notify-gate regression; both the check-in notice and the authorization-form copy are now forced on every submission. Canonical for `fa1c1d6e`. |
| `91a03260` | Roland | 06-09 | Work List | `prune_cashiered_worklists.sql` L20-27 | Removes an RO from manager work lists the day **after** Delivered/Cashed Out, gating on the Chicago-local last-update date - deliberately visible on the day of cash-out. Canonical for `5057c7fc`. |
| `cdd77a8b` | Brandon | 06-10 | General UI/UX | `js/render.js` L656-658/L718-721 | v1.453/v1.454: `updateStats` splits the counts - shop ROs drop out of the queue figure and `onLotCount` excludes Not On Lot / Scheduled / Off Lot - Returning. |
| `2c3d5633` | Ryan | 06-12 | General UI/UX | `js/photos.js` L66 | Was a mobile-only functional bug; v1.453 appends the file input to the DOM before clicking it (the iOS Safari dropped-change-event root cause). |
| `74a33621` | Brandon | 06-16 | General UI/UX | `js/photos.js` L396-397/L380/L382 | v1.454: lightbox arrow-key navigation plus Escape-to-close and prev/next buttons. |
| `298e0e67` | Sofia | 06-16 | General UI/UX | `customer-checkin.html` L2059/L2165/L2060/L2194 | Was a data-integrity bug; the returning-customer guard reuses an existing scheduled RO, and v1.17 adds a blocking attach-vs-new dialog for the explicit-selection case (the Tom Rather path). Canonical for `fc6b29f9`. |
| `9b823d25` | Roland | 06-16 | RO Management | `js/work-orders.js` L244/L293; `service_added_notify_source.sql` | v1.460: adding a service to an RO queues a `service_added_notify` row, emailing the owning silo manager - the roof-plus-water-heater example. |
| `9b046808` | Brandon | 06-17 | General UI/UX | `js/config.js` L157; `js/render.js` L718-721 | v1.454: "Scheduled" is its own status and is excluded from days-on-lot and the on-lot count, so future drop-offs no longer read as already on the lot. |
| `671fa623` | Brandon | 06-18 | General UI/UX | `js/parts.js` L153/L155/L159-161 | v1.456 (S117): Freight is summed across parts and rolled into Total Cost. Same build as `a8e90a7d`. |
| `5c45bb8d` | Sofia | 06-19 | General UI/UX | `customer-checkin.html` L2059/L2165/L2530 | Both halves of the David Flanagan report resolved: the Scheduled path reuses the existing RO (plus the v1.17 confirm) and the authorization-form copy is forced. |
| `b06a285f` | Lynn | 06-19 | General UI/UX | `js/render.js` L363-367/L395-398 | v1.465: a calendar icon beside the name opens a full key-dates panel on hover or tap, alongside the v1.457 summary chips. |
| `ee50d11c` | Lynn | 06-19 | General UI/UX | `time-off.html` L752 -> `editRequest()` | Was a real gap; time-off entries are editable, not delete-only - the row Edit button reloads the entry and flips submit to "Update Record", so day, person and type can all change. |
| `88395acc` | Lynn | 06-19 | General UI/UX | `add_absence_window_to_time_off.sql`; `add_partial_hours_to_time_off.sql`; `time-off.html` L368/L372/L817 | Time Off v1.5: partial-day absence is an exact clock-out-to-return window, not an hour total. |
| `80390f36` | Lynn | 06-23 | General UI/UX | `js/render.js` L35-37/L61 | v1.462: Shop Operations tiles pin above the priority sort and carry a "shop operations shop" search token, so search finds the tile. |
| `7f2d0d70` | Lynn | 06-26 | General UI/UX | `send-dropoff-report` L137-141/L176 | The automated Monday drop-off email lists this week's scheduled drops and last week's arrivals and reads `manager_work_lists` to flag any not yet on the right team's list - exactly the reconciliation asked for. Pairs with `cbc70a86`. |
| `3f7fb4fc` | Brandon | 06-29 | General UI/UX | `js/render.js` L656-658/L718-721/L249 | Was a small data-display bug; v1.468 excludes shop ROs from both counts and replaces the shop status dropdown with a static label. |
| `cb7742a8` | Lynn | 06-29 | General UI/UX | `sync-ro-calendar`; `customer-checkin.html` L2240/L22 | v1.469 (check-in v1.15): the edge fn owns drop-off / promised / pickup events server-side and posts them to the correct team calendar for every RO including front-desk check-ins - fixes the repair-calendar miss. |
| `39b75fab` | Sofia | 07-09 | General UI/UX | `js/photos.js` L198/L376 | v1.472: the document library supports rename, move-to-another-RO and delete, office-staff gated - so the Marco DeAlba docs on the Ruiz RO can be moved or removed. |
| `9d3806e7` | Brandon | 07-22 | General UI/UX | `messages.html` L528/L530; `customer_note_entries_s154.sql` | S154: a customer-side Notes section in the details pane plus an Add-Notes modal writing append-only rows keyed by phone. **DB row is still `unreviewed` - recommend `status=done`.** |
| `ac394468` | Roland | 07-24 | General UI/UX | `js/enhancement.js` L43-55 | v1.482 / S158: `erCategory` covers Messages/Texting, Solar, Leads, Closed ROs, Reports, Time Off and Employee Guide, and `pageDefaults` maps every page. **Residual:** `erCategory` ships in only 6 live HTML files (analytics, checkin, closed-ros, index, solar, worklist-report), so messages / leads / time-off / guide cannot *raise* an ER - worth its own small ER. |
| `d62a26e4` | Roland | 07-24 | New Feature Idea | `js/messaging.js` L202/L207/L219/L258-262 | v1.482 / S158a: `multiple` + up to 5 attachments with removable chips; the send path chunks one media per MMS because a carrier total-payload cap silently stripped multi-media sends. The multi-attachment ask is fully met. |
| `93b00023` | Roland | 07-24 | New Feature Idea | `textly-webhook` L505; `send-unreplied-reminder`; `unreplied_eod_reminder_s158.sql` | v1.482 (S158), all three parts: non-keyword inbound reopens the conversation and logs a `reopened` event; the owner notify fires only on the needs-reply flip, not every inbound; the 4:30pm unread sweep runs on a weekday cron. |

## Carried over - not re-classified this pass

| ER | By | Date | Category | DB status | Note |
|---|---|---|---|---|---|
| `f73a1e5a` | Roland | 04-13 | General UI/UX | declined | Not in `needs_triage_ids`, and the row carries no stored `triage_bucket` / `triage_verdict` to reuse. Content is a dictation test ("Testing dictation 123 testing enhancement request dictation") and it is already declined; no work item. Left untouched by this runner. |

---

## Recommended action today

1. **Fix the two bugs first - they are the whole bug class and both are cheap.**
   `d4d0eeba` (click-through never scrolls to the RO) also closes duplicates
   `9031e101` and `1bc33dd6`, so one navigation fix retires three ERs; the
   correct shape is clear-filters -> re-render -> scroll -> highlight, across
   both `scrollToROInBoard` and `js/qr.js handleDeepLink`. `d94a418a` is a
   two-line render change: stop `reverse()`-ing every line in `js/render.js`
   L553/L563 and reuse the S159 entry-regex split. Both are inside the
   auto-fixable class (small / data-display).

2. **Ship a Messages S-bundle.** Five of the six Messaging ERs are S and all
   live in `js/messaging.js` + `messages.html`: `6e9ca4dc` (drop the desktop
   "Added to every send" line), `f1e6e311` (clear-x on the conversation
   search), `9aab7550` (stronger unread treatment), `21fc3a9d` (font-scale
   control), `1c7f1410` (accept `video/*`). One branch, one regression pass.

3. **Then the cheap board/RO wins**, also all S: `d432863a` (favicon -
   genuinely minutes), `29654af7` (back-to-top, closes `2b78696c`),
   `972af03c` (Canceled status), `50175fce` (urgent-update inline edit),
   `d1844e46` (strict promised-then-oldest sort), `5fec3eca` (address on the
   RAF copy email), `2f752d40` (WO dollar = hours x $195).

4. **Hand Roland the one open data SQL** for `3f254929` (delete the misspelled
   Hubert RO) - eyeball the SELECT first. Nothing was executed by this runner.

5. **Close the books on `9d3806e7`** - it is Done in code but still
   `unreviewed` in the DB. Status flips are Roland's call.

6. **Ask the submitters to specify the two blocked S's:** Lynn's parts field
   order (`8d885331`) and Riley's check-in float (`e06d747e`). Both are small
   *once specified* and have been blocked on detail for months.

7. **Scope, do not rush, the two XL's:** the auth swap (`ed1b873e`, ~$500/mo at
   stake but it touches every page's sign-in and the email-keyed RBAC) and
   parts inventory (`14f35f95`). Each deserves its own spec session.
