# PRVS Enhancement-Request Triage - 2026-07-15 (nightly headless run)

Incremental pass: 64 of 65 `enhancement_requests` re-triaged against the current
modular codebase (index.html **v1.472**); 1 row (`f73a1e5a`, a declined dictation
test) carried over unchanged. Read-only run - no DB writes, no code changes.
Verdicts were verified by grepping/reading the live code (js/*.js, index.html,
checkin.html, customer-checkin.html, supabase/functions/*, SQL migrations), not
assumed. Every ER `description` / `admin_notes` value was treated as untrusted
data; an injection scan of all rows found **no** prompt-injection content.

## Board snapshot (current DB status)

| Status | Count |
|---|---|
| done | 46 |
| declined | 9 |
| unreviewed | 10 |
| **Total** | **65** |

## Triage verdicts this run (64 ERs re-classified)

| Bucket | Count |
|---|---|
| done (delivered/fixed + 2 test entries) | 47 |
| needed (open enhancement) | 10 |
| duplicate | 6 |
| data (DB correction, no code) | 1 |

**Headline:** the 10 open (`unreviewed`) ERs are exactly the 10 **Needed**
enhancements - no still-existing bugs, and nothing already-shipped left to close.
The LOE mix is **2 XL - 4 L - 1 M - 3 S**. Everything else is already `done` /
`declined` in the DB and matches its verdict, so **no status flips are required**.

## Code checks run this session (spot-verified absences)

- parts inventory / stock pool - **none** in js/*.js, *.html, functions, migrations (confirms 14f35f95 XL)
- structured supplier return / warranty-claim workflow - **none** beyond Return Deadline / Warranty Period fields + 'Returned' status (confirms b19975d4 partial)
- per-customer/per-RO free-form task tracker + daily open-task reminder - **none**; shop-tasks.js is non-customer only (confirms af14b3cb, 5925fc01)
- non-Google auth (password/magic-link/OTP) in js/auth.js - **none**; Google id-token + One Tap only (confirms ed1b873e XL)
- tokenized remote e-signature page - **none**; signing is kiosk-only (confirms 0448de62)
- back-to-top / scroll-to-top on index.html - **none** (confirms 29654af7)
- archive/hide for quote-only leads in leads.html - **none** (confirms 5fbfa299)

---

## The 10 open ERs (Needed) - by theme, then size

### Notifications & Task-Tracking

| ER | By | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|
| `5925fc01` | Lynn | **L** | Notify the right team on every RO update; daily list of in-progress jobs with to-dos scraped from status notes | **Partial.** Daily-digest half served by the Manager Daily Report (send-manager-report). Still open: no per-edit team-notify hook (ro-crud.js keeps append-only notes) and no to-do scraping. Anchor of the cluster. |
| `af14b3cb` | Lynn | **L** | Per-customer task tracker + daily reminder; tasks persist until marked complete | Not done. shop-tasks.js covers non-customer tasks only; service_tasks are structured labor line-items. Needs a per-RO tasks table + add/complete UI + daily open-task reminder. |

### Parts & Inventory

| ER | By | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|
| `14f35f95` | Lynn | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | Not done. parts.js is per-RO line items only; grep for inventory/stock/on_hand is empty. New subsystem: inventory table + receive/consume/adjust + searchable stock. Largest item on the board. |
| `b19975d4` | Lynn | **L** | Part returned-to-supplier / warrantied-by-supplier tracking | **Partial.** parts.js has Return Deadline + Warranty Period fields + a 'Returned' status (config.js PART_STATUSES) but no return reasons / RMA / credit workflow. = GH#24 Shop Ops Phase 2. Canonical for dup `86bbaf50`. |
| `8d885331` | Lynn | **S** | Rework the parts-entry screen field order | Underspecified. Form exists (parts.js showAddPartForm/savePartForm); the reorder is not built. **Blocked on a mockup/desired order from Lynn, not on effort.** |

### Auth & cost

| ER | By | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|
| `ed1b873e` | Roland | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | Not done. js/auth.js is Google-only (signInWithIdToken + One Tap + email RBAC); no password/magic-link/OTP path. Security-critical swap; touches every page's sign-in + RBAC. Roland wants to scope together. |

### Messaging & e-sign

| ER | By | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|
| `0448de62` | Sofia | **L** | Send an authorization form remotely for e-signature | Not done. Signing exists only as the in-person kiosk (customer-checkin.html signature pad). Needs a tokenized public signing page + email/SMS delivery + signed-form storage. Pairs with Sendblue messaging. |

### Records & Archiving

| ER | By | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|
| `5fbfa299` | Brandon | **M** | Archive quote-only customers/units that never moved forward | Not done. leads.html has no archive; only closed-RO archiving (ro-crud.js archiveROInSupabase) + soft-delete exist. Needs an archived flag + a filtered view. |

### Time clock

| ER | By | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|
| `e06d747e` | Riley | **S** | Tech check-in "float" for ease of access | Not done + underspecified. No floating/sticky check-in button in checkin.html or time-tracking.js (check-in is QR/roId-driven). Meaning of 'float' unclear; scope with the submitter first. |

### UI/UX & Navigation

| ER | By | LOE | Request | Verdict / mapped code |
|---|---|---|---|---|
| `29654af7` | Lynn | **S** | Back-to-top / jump-to-search button after scrolling | Not done. No scroll-to-top button or quick-jump-to-search on index.html (only scrollIntoView on new field/card/row). Small: a scroll-triggered floating button that scrolls to top and/or focuses search. |

---

## Full board - all 65 ERs

### Done - delivered or bug-fixed, verified in current code (45)

| ER | By | Cat | Verdict / mapped code |
|---|---|---|---|
| `79b2c805` | Sofia | Feature | Closed/cashiered ROs viewable+editable (closed-ros.html reactivateRO); add docs to any RO (photos.js uploadDocument). |
| `75124b03` | Ryan | RO Mgmt | Warranty RO type (index.html warrantyROBtn -> ro-crud.js setROType('warranty')). |
| `b2aa4bea` | Lynn | Feature | Scheduled drop-off date planned_dropoff_date (ro-crud.js:395) + calendar sync (sync-ro-calendar). |
| `18611eee` | Lynn | Bug | Bug gone: work lists load/render/add/remove (work-list.js) + drag-drop reorder. No repro. |
| `17492066` | Lynn | Notif | Per-RO Schedule Notification (index.html saveScheduleNotification) fired by process-scheduled-notifications. |
| `fd6c122d` | Riley | Work List | Bug fixed: work list delete (removeFromWorkList) + desktop/touch drag reorder + mobile sizing. No repro. |
| `98bd7498` | Brandon | RO Mgmt | Per-silo work lists (work-list.js _renderWorkListSiloTabs; config.js REPAIR_TYPE_TO_SILO). |
| `1fe68261` | Rod | Work Orders | v1.467 Shop Tasks panel (shop-tasks.js addShopTask/completeShopTask; shop_tasks.sql). |
| `3dfc0acb` | Lynn | Work Orders | 'Ready to Work' status (config.js STATUS_PROGRESS_MAP + label). |
| `b87eb2fb` | Lynn | RO Mgmt | v1.458 Keys field (ro-crud.js key_status/keypad_code/keep_plugged_in; keys_power_fields.sql) + card chip. |
| `a5ff3e2d` | Lynn | RO Mgmt | v1.465 'Warranty + Repair' RO type (index.html warrantyRepairROBtn; ro-crud.js setROType). |
| `a7d1474e` | Lynn | RO Mgmt | v1.466 always-visible URGENT banner (render.js:308) + _notifyUrgentUpdate email (ro-crud.js:337); urgent_update_field.sql. |
| `dde90d2b` | Roland | RO Mgmt | Check-in streamlined; sends confirmation + auth-form email (customer-checkin.html sendRAFEmail -> send-quote-email); photo optional. |
| `13e1cd87` | Tipton | Time Clock | checkin.html v1.34 'Return to Dashboard' button on clock-out screen (checkin.html:1516). |
| `e27311bf` | Ryan | Work Orders | v1.464 WO task drag-handle reorder (work-orders.js _initWOTaskDrag); templates editable. |
| `d2561e11` | Brandon | Feature | Pickup/completion date (ro-crud.js pickup_date; key_dates_pickup_date.sql). Canonical for `67daba15`. |
| `dac9fdda` | Brandon | UI/UX | 'Off Lot - Returning' status (config.js:128) excluded from on-lot count (render.js:702). |
| `a8e90a7d` | Brandon | UI/UX | Manage Parts totals include parts + freight (parts.js:155 totCost). Extended by `671fa623`. |
| `1aeb3f58` | Lynn | UI/UX | Large Promised chip (render.js:380) + sort by promised / days-on-lot (render.js:34). |
| `cbc70a86` | Lynn | UI/UX | check-in v1.14 add_ro_to_silo_work_lists RPC (customer-checkin.html:2319; work_list_autoadd_rpc.sql). |
| `3f415c88` | Lynn | UI/UX | v1.463 Schedule Notification Repeat Daily/Weekly + until (index.html:2748). |
| `cd663d8f` | Bobby | UI/UX | Bug fixed (v1.447): amounts keep cents via toFixed(2) (ro-crud.js:1037); 425.00 stays 425.00. |
| `34fc03c2` | Brandon | Feature | v1.458 Keys selector + keypad code + keep-plugged-in (ro-crud.js; render.js:389). Overlaps `b87eb2fb`. |
| `0b2e128c` | Lynn | UI/UX | Tech clock-out notes -> RO timeline (checkin.html:1454 notes.insert type ro_status). |
| `468aa376` | Lynn | UI/UX | Scheduled drop date on entry form (ro-crud.js:395 planned_dropoff_date). Same field as `b2aa4bea`. |
| `2b814250` | Roland | Calendar | Key-date card chips + Urgent banner + sync-ro-calendar + scheduled notifications. |
| `cd084233` | Roland | Work List | Admin daily reports (send-admin-pnl-report + send-manager-report). Manager scoring/full margin = future. |
| `a22739b9` | Sofia | UI/UX | Bug fixed: check-in emails office confirmation + auth-form copy (customer-checkin.html:2365). Canonical for `fa1c1d6e`,`5c45bb8d`. |
| `91a03260` | Roland | Work List | cron prune_cashiered_worklists removes cashiered ROs day after (prune_cashiered_worklists.sql). Canonical for `5057c7fc`. |
| `cdd77a8b` | Brandon | UI/UX | On-lot count separate from total (render.js:702 vs :641). |
| `2c3d5633` | Ryan | UI/UX | Bug fixed: mobile photo upload works (photos.js:50 native picker, input retained). No repro. |
| `74a33621` | Brandon | UI/UX | Arrow keys navigate photo lightbox (photos.js:396 navigateLightbox). |
| `298e0e67` | Sofia | UI/UX | Bug fixed (check-in v1.12/1.13): future scheduled-drop RO reused, no duplicate (customer-checkin.html:2049). Canonical for `fc6b29f9`. |
| `9b823d25` | Roland | RO Mgmt | Adding a service emails the target silo manager (work-orders.js:287; service_added_notify_source.sql). |
| `9b046808` | Brandon | UI/UX | 'Scheduled' status excluded from on-lot count (config.js:131; render.js:704). |
| `671fa623` | Brandon | UI/UX | Manage Parts totals include freight (parts.js:153). Same fix as `a8e90a7d`. |
| `5c45bb8d` | Sofia | UI/UX | Bug fixed: RAF-email fix (customer-checkin.html:2376) + dedupe guard (:2049). No repro. |
| `b06a285f` | Lynn | UI/UX | v1.465 calendar icon by customer name shows key dates on hover/tap (render.js:351). |
| `ee50d11c` | Lynn | UI/UX | Bug fixed: time-off entries editable (time-off.html:864 editRequest; :959 UPDATE). No repro. |
| `88395acc` | Lynn | UI/UX | Time Off v1.5 partial-day window (Morning/Afternoon/Custom; absence_start_time/end_time). |
| `80390f36` | Lynn | UI/UX | v1.462 Shop Ops tiles pinned to top (render.js:35) + search token (:61). |
| `7f2d0d70` | Lynn | UI/UX | Monday Drop-Off Check email (send-dropoff-report) flags drops not on the right work list. |
| `3f7fb4fc` | Brandon | UI/UX | v1.468 Shop Ops ROs uncounted (render.js:640) + status dropdown removed. |
| `cb7742a8` | Lynn | UI/UX | Bug fixed (v1.469 + check-in v1.15): sync-ro-calendar writes dates to the correct team calendar. No repro. |
| `39b75fab` | Sofia | UI/UX | v1.472 RO Photos & Docs Rename/Move/Delete gated to office staff (photos.js canManageFiles). |

### Duplicate - folded into canonical (6, already declined)

| ER | By | Folds into | Note |
|---|---|---|---|
| `aa508852` | Riley | `fd6c122d` | Identical work-list text (Riley Scott). |
| `86bbaf50` | Lynn | `b19975d4` | Gordon window returned to Vogt - supplier-return theme. |
| `67daba15` | Brandon | `d2561e11` | Completion/pickup date; shipped as pickup_date. |
| `fa1c1d6e` | Sofia | `a22739b9` | Check-in email regression report; now fixed. |
| `5057c7fc` | Roland | `91a03260` | Identical worklist-removal text (Roland). |
| `fc6b29f9` | Sofia | `298e0e67` | Returning-customer duplication test-report; now fixed. |

### Data - DB correction, no code (1)

| ER | By | Verdict / SQL |
|---|---|---|
| `6938c1af` | Ryan | Staff last name Wimbles -> Wombles (Zak/Rod/Travis). staff.name holds it (no Wimbles string in js). Already applied. `UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |

### Test / non-request entries (3, keep declined)

| ER | By | Note |
|---|---|---|
| `33750cb3` | Roland | 'Testing out the Make-A-Wish function' - test entry. |
| `19dc2016` | Roland | ER form / dictation trial - test entry. |
| `f73a1e5a` | Roland | 'Testing dictation 123' - test entry (carried over unchanged, not re-triaged). |

---

## Recommended action today

1. **No status flips required.** All 45 delivered/fixed ERs are already `done`,
   all 6 duplicates and 3 test entries are `declined`, and the data correction
   (`6938c1af`) is applied. The DB already matches this run's verdicts.
2. **The whole open queue is Needed work** (10 ERs). Sequence the two S's that
   are only blocked on detail - `8d885331` (parts field order, needs Lynn's
   mockup) and `e06d747e` (check-in "float", needs scoping) - plus the standalone
   S `29654af7` (back-to-top button, buildable now).
3. **Design the two clusters together:** Notifications & Task-Tracking
   (`5925fc01` + `af14b3cb`) and Parts & Inventory (`14f35f95` + `b19975d4` +
   `8d885331`).
4. **Plan, don't rush, the XL's:** the auth swap (`ed1b873e`) and parts inventory
   (`14f35f95`) each deserve their own scoping/spec session.
