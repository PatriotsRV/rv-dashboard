# Enhancement Request Triage -- 2026-06-24 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-24T10:24Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.460** (checkin.html v1.38, customer-checkin.html v1.13, time-off.html v1.4).
**Total ERs:** 59 (incl. 3 test entries n0/n14/n15 + 5 exact/near duplicates -> **51 real requests**).
**Triaged this run:** 58 ids in `needs_triage_ids` (every row except n15, the carried-over test entry, reused unchanged).

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `closed-ros.html`, `time-off.html`, `supabase/functions/*`,
SQL migrations) by grepping the source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

This run's tally: **done 33** (+1 carried-over test = 34 rows) - **needed 19** - **duplicate 5** - **data 1** - **bug 0**.

---

## Headline

The bug bucket remains empty -- every defect reported since Session 94 has shipped
and is confirmed in the current tree (currency formatting n33, clock-out return
button n22, iOS photo upload n47, returning-customer duplicate-RO + missing-RAF
n40/n49/n54, check-in email n40). No new defect arrived this cycle.

Nothing ER-relevant shipped between the 2026-06-23 pass and today's snapshot, so
the standing verdicts carry forward; all were re-verified against the v1.460 tree.
The S120 v1.460 deliveries stay confirmed: worklist touch-drag (n10), clock-out
notes -> RO timeline (n35), service-add email (n50), and the Off-Lot-Returning
status (n26).

**One brand-new ER this cycle -- n58 (Lynn):** "pin shop operations tile to top of
list; search never finds it." Verified against code: Shop ROs render a Shop
Operations badge (`render.js` L333) with `customerName` 'PRVS - SHOP OPERATIONS'
(`index.html` L5755), and the dashboard search haystack DOES include `customerName`
(`render.js` L42-54), so a text search on that term already matches. What is
genuinely missing is a pin/always-on-top affordance for the shop tile. Per the
"prefer needed over done" rail this is filed **NEEDED / S**, not done.

The Scheduling & Key Dates theme that anchored the prior reports is now fully
delivered (n4/n24/n29/n36/n37 all DONE via v1.457-v1.459); it no longer carries
any open NEEDED items.

---

## BUG bucket (front of the line)

None still open. All previously reported defects shipped and are confirmed in the
current tree -- see Headline.

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation restores full RO detail (parts/time_logs/photos) -> edit + add docs. | done (already) |
| n2 | Ryan | Warranty category on new RO | Warranty RO type v1.402 (`ro-crud.js` roType=='warranty'; `render.js` warranty filter L112). | done (already) |
| n4 | Lynn | Scheduled future drop-off date + reminder | v1.457: Planned Drop Off Date on New+Edit RO + morning-before email reminder; blue Drop chip (`render.js` L353). | done |
| n5 | Lynn | Work list opened but couldn't view/do anything | Phase 14: `work-list.js` renderWorkList (L269) + removeFromWorkList (L195) + _setWorkListSilo (L263). | done |
| n10 | Riley | Mobile worklist: delete + drag-and-drop reorder | v1.460: `work-list.js` touch-drag (touchstart/move/end via _reorderWorkList); delete already worked. | done |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`work-list.js` _renderWorkListSiloTabs L223 / _setWorkListSilo L263). | done |
| n17 | Lynn | Add "Ready to Work" RO status | v1.410 (`config.js` STATUS_PROGRESS_MAP slot 50, L132). | done |
| n18 | Lynn | "Keep plugged in" + keys/keypad | v1.458: key_status/keypad_code/keep_plugged_in on New+Edit RO (`ro-crud.js`) + card chips; migration keys_power_fields.sql. | done |
| n21 | Roland | Fix customer check-in, email out, remove photo | `customer-checkin.html` photo optional + auto-emails signed RAF every drop-off; staff-notify gap closed v1.10/v1.11. | done |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | v1.447: `checkin.html` v1.34 Return-to-Dashboard button. | done |
| n24 | Brandon | Completion / pickup date on the order | v1.457: pickup_date on New+Edit RO (`ro-crud.js`) + green Pickup chip (`render.js` L355); v1.459 calendar + reminders. | done |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning | v1.460: "Off Lot - Returning" status (`render.js` statusColorMap; `config.js` STATUS_PROGRESS_MAP L128; excluded from on-lot count). | done |
| n28 | Brandon | Parts + freight total in Manage Parts | v1.456: Ticket Totals bar (`parts.js`) -- Parts/Freight(sum core_charge)/Total Cost/Retail; recomputes on edit. | done |
| n29 | Lynn | Promised date large/highlighted + sort | v1.457: prominent amber promised chip, red if overdue (`render.js` L354); v1.459 reminders+calendar. Sort-by-promised deferred (priority score weights it). | done |
| n33 | Bobby | Currency drops the period/zeros (425.00 -> 425) | v1.447: 2-decimal formatting on edit-populate (`ro-crud.js` editDollarValue .toFixed(2)) + `parts.js` MONEY_FIELDS. Cosmetic, no data loss. | done |
| n34 | Brandon | Confirm if customer leaves keys | v1.458: key_status select (keys/no_keys/keypad) + keypad_code on New+Edit RO (`ro-crud.js`) + card chip. Marker ER 34fc03c2. | **done (DB still unreviewed -- flip)** |
| n35 | Lynn | Transfer clock-in job notes -> RO updates | v1.460: `checkin.html` v1.38 copies clock-out work notes into the RO timeline as a TECH NOTES ro_status note. | done |
| n36 | Lynn | Scheduled drop date in entry dash | v1.457: Planned Drop Off Date on New AND Edit RO + morning-before reminder (duplicate intent of n4, satisfied). | done |
| n37 | Roland | RO date/time -> colored button + business calendar + notifications | Key Dates P1-P3 (v1.457+v1.459): Drop/Promised/Pickup chips (`render.js`); per-silo Google Calendar events (`scheduling.js` syncKeyDateCalendars); day-before + morning-of reminders (`ro-crud.js`). | done |
| n39 | Roland | Admin-level daily Manager-worklist report | `supabase/functions/send-manager-report` v2.3 + P4c Scheduled Reminders, daily cron (P1-P4). Manager-scoring/profit-margin deferred (follow-up ER). | done |
| n40 | Sofia | Check-in no longer emails staff / RAF copy | v1.10/v1.11: `customer-checkin.html` shouldNotifyStaff fires every drop-off + signed RAF always sends; v1.11 fixed getSignatureData. | done |
| n43 | Roland | Remove cashed-out RO from worklist next day | S118: daily worklist auto-prune after Cashiered Out/Delivered; migration prune_cashiered_worklists.sql. | done |
| n45 | Brandon | Distinguish on-lot RVs vs total customers | v1.453: `render.js` distinct on-lot count (statTotalOnLot L667); v1.454 excludes Scheduled. | done |
| n47 | Ryan | Can't upload pictures from phone | v1.453: `photos.js` uploadPhoto attaches the file input to the DOM before .click() (iOS Safari change-event fix; tagged ER 2c3d5633). | done |
| n48 | Brandon | Arrow keys to move through photos | v1.454: photo lightbox keydown handler (`photos.js`) -- Arrow keys via navigateLightbox, Escape closes. | done |
| n49 | Sofia | Returning-customer scheduled drop-off duplicates RO | v1.12: `customer-checkin.html` dedupe guard UPDATEs the existing scheduled RO instead of inserting; v1.13 hardened by phone OR email. | done |
| n50 | Roland | Email manager when a service is added to an RO | v1.460: `work-orders.js` addServiceToRO queues a scheduled_notifications row (service_added_notify) to the new silo's manager(s); migration service_added_notify_source.sql. | done |
| n52 | Brandon | "Scheduled to arrive" not counted as on-lot | v1.454: `render.js` on-lot + days-on-lot stats exclude 'Scheduled' units (fixed the count rather than adding a status). | done |
| n53 | Brandon | Ticket totals should include freight | v1.456: Manage Parts Ticket Totals Freight line = sum(core_charge per part) (`parts.js`). Satisfied by n28's totals bar. | done |
| n54 | Sofia | David Flanagan returning check-in: no RAF + duplicate | v1.13: `customer-checkin.html` submit-time auto-dedupe matches scheduled RO by phone OR email even when the search match was not tapped. | done |
| n56 | Lynn | Can't edit time-off entries | v1.4: `time-off.html` editRequest opens the modal pre-filled and UPDATEs day/person/type; Edit button per row. Marker ER ee50d11c. | done |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod and Travis last name should be Wombles, not Wimbles, in tech assignment" | Data, not code. Lives in `staff.name`; already corrected in DB per S117 (no Wimbles rows remain). SQL below if any reappear. |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

Historical ROs that stored the tech name as free text are a separate optional backfill.

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | Can't delete jobs / too small / drag-and-drop | Exact duplicate of **n10** (identical text + near-identical timestamp); already declined. Fold into n10 (delivered v1.460). |
| n41 | Brandon | Completion / pickup date selection in RO | Duplicate of **n24** (completion/P-U date). Canonical delivered v1.457; already declined. |
| n42 | Sofia | Returning customer (Sheldon Gilbert) sent no check-in email | Duplicate of **n40** (same check-in-email defect). Canonical fixed v1.10/v1.11; already declined. |
| n44 | Roland | Remove cashed-out RO from worklist next day | Exact duplicate of **n43** (timestamps 1s apart). Fold into n43 (delivered S118); already declined. |
| n51 | Sofia | Returning-customer test (Roland Shepard Jr) duplicated | Duplicate of **n49** (returning-customer dup-RO). Canonical fixed v1.12/v1.13; already declined. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Notifications, reminders & task-tracking  *(n3, n8, n20, n30, n32)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes + per-update notification | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time | L |
| n20 | Lynn | Always-visible "urgent updates" field on the tile + notify-on-modify + font/color | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete + daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |

-> **Largest open opportunity.** A "Task Tracker + Reminders" subsystem. v1.459 key-date
reminders and v1.460 service-add notify exist, but no general scheduled-task notifier
or per-customer task store. Design as one initiative (overlaps the deferred Sendblue/SMS path).

### Theme 2 -- Parts  *(n6, n7, n9, n27)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier | L |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

### Theme 3 -- Work Orders / templates & internal tasks  *(n16, n23)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will | M |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |

### Theme 4 -- RO fields & status nuance  *(n19)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n19 | Lynn | "Warranty + Repair" combined RO type (clarify vs existing Hybrid) | M |

### Theme 5 -- Work List usability  *(n31)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |

### Theme 6 -- Check-in, Time Clock & Forms  *(n13, n46)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n13 | Riley | Tech check-in "float" / quick-access on the time-clock page | S (vague -- overlaps n22) |
| n46 | Sofia | Send the RV authorization form (RAF) remotely for e-signature | L -- needs tokenized public signing route |

### Theme 7 -- Dashboard tile usability  *(n55, n58)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n55 | Lynn | Hover near customer name to show important dates/tasks as a list w/ detail | M |
| n58 | Lynn | Pin Shop Operations tile to top of board + make it findable by search *(new)* | S |

-> Search already covers the shop tile via `customerName` 'PRVS - SHOP OPERATIONS'
(`render.js` L42-54); the open work is a pin/always-on-top affordance and an
optional shop-type quick filter. The hover-dates ask (n55) overlaps the unbuilt
task-tracker (n30).

### Theme 8 -- Time Off  *(n57)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n57 | Lynn | Exact absence time window (clock-out -> return time; AM/PM half-day) instead of total hours | M |

-> `partial_hours` captures DURATION, not a start/end time-of-day; add time-window fields.

### Theme 9 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

---

## Recommended action today

1. **Close the one straggler:** **n34** (keys-on-RO confirmation) is delivered in
   v1.458 but still `status=unreviewed` in the DB -- set `status=done`. Every other
   DONE/DUPLICATE ER is already at its terminal status (Roland flips status himself).
2. **No data SQL is required** -- the n25 staff-name correction was already applied
   (no Wimbles rows remain); the UPDATE above is only a safety net if any reappear.
3. **No code changes are queued from this run** -- the bug bucket is empty and the
   only new ER (n58) is a NEEDED/S enhancement, not a defect.

Standout near-term wins among the open NEEDED items: **n9 (parts field reorder, S)**,
**n58 (pin shop tile, S)**, and **n13 (tech check-in float, S)**. The biggest
strategic asks remain a coordinated **Task Tracker + Reminders** initiative (Theme 1),
**n38 (auth cost reduction, XL)**, and **n46 (remote RAF e-signature, L)** -- each
warrants a design session with Roland before scoping.
