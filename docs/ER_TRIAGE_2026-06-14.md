# Enhancement Request Triage -- 2026-06-14 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-14T10:27Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.449** (checkin.html v1.36, customer-checkin.html v1.11).
**Total ERs:** 48 (incl. 3 test entries + 4 exact/near duplicates -> **41 real requests**).
**Triaged this run:** 47 ids in `needs_triage_ids` (every row except the n15 test entry, carried over unchanged).

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `closed-ros.html`, `worklist-report.html`,
`supabase/functions/*`) by grepping the source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

---

## Headline

One new bug arrived since the last pass. **n47** (Ryan) "can't upload pictures
from phone -- acts like it accepts them then switches screens with no upload."
Mapped to `photos.js` `uploadPhoto` (L50-128): the index photo button creates a
**detached** dynamic `<input>` and an async `onchange` closure; on iOS Safari the
page is frequently reloaded on return from the camera, discarding the detached
input and its closure so `onchange` never fires -- no upload, no error toast,
matching the report exactly. Classified **BUG** (functional, mobile-only,
small-medium). Needs a device repro to nail the exact cause; the durable fix is a
persistent in-DOM file input plus a post-write verification. Prefer fixing over
closing.

All previously-confirmed "done" verdicts still hold against v1.449:

- **n22** (Tipton) Return-to-Dashboard button -- still present (`checkin.html`
  v1.34 `[ER BUGFIX S94] n22` marker, now riding at checkin.html v1.36).
- **n33** (Bobby) currency 2-decimal formatting -- `ro-crud.js` `editDollarValue`
  L843 `.toFixed(2)` + `parts.js` `MONEY_FIELDS` L288. Confirmed.
- **n40** (Sofia) check-in staff/RAF email -- `customer-checkin.html` v1.11
  `shouldNotifyStaff` fires on every drop-off + RAF always sends (`getSignatureData`
  L1754 typo fix). Confirmed.

The only data-only item remains **n25** (staff-name SQL; Roland runs it).

---

## BUG bucket (front of the line)

| # | Submitter | Request | Mapped code | Severity |
|---|---|---|---|---|
| n47 | Ryan | Can't upload pictures from phone; acts like it accepts them then switches screens with no upload | `photos.js` `uploadPhoto` L50-128 -- detached dynamic `<input>` + async `onchange` closure lost when iOS Safari reloads the page on camera return; `onchange` never fires, no error path | functional, mobile-only (small-medium); device repro needed |

Suggested fix direction: use a persistent in-DOM `<input type=file>` (not a
detached `createElement`), and verify the Storage write / surface a failure toast
so a silent no-op can no longer look like success. Do not close until reproduced
and fixed.

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation restores full RO detail (parts/time_logs/photos). | done |
| n2 | Ryan | Warranty category on new RO | Warranty RO type shipped v1.402 (`ro-crud.js` `roType=='warranty'` L855 + index warranty filter). | done |
| n5 | Lynn | Work list opened but couldn't view/do anything | Fixed Phase 14: `work-list.js` `renderWorkList`/`removeFromWorkList`/`_setWorkListSilo` (L195/L263/L269). | done |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs` L223/`_setWorkListSilo` L263). | done |
| n17 | Lynn | Add "Ready to Work" RO status | Shipped v1.410 (`config.js` STATUS_PROGRESS_MAP L131 slot 50). | done |
| n21 | Roland | Fix customer check-in, email out, remove photo, streamline | `customer-checkin.html` v1.11 photo optional + RAF/staff email every drop-off. | done |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | FIXED v1.447 (`checkin.html` v1.34 Return-to-Dashboard button; still present v1.36). | done |
| n33 | Bobby | Amounts drop the period and the zeros (425.00 -> 425) | FIXED v1.447 (`ro-crud.js` `editDollarValue` L843 `.toFixed(2)` + `parts.js` MONEY_FIELDS L288). | done |
| n40 | Sofia | Check-in stopped sending the email / RAF copy | FIXED v1.10/v1.11 (`shouldNotifyStaff` on every drop-off; `getSignatureData` L1754 typo fix). | done |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod and Travis last name should be Wombles, not Wimbles, in tech assignment" | Data, not code. `staff.name` holds the value; not hardcoded. One SQL UPDATE (below). |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

Historical ROs that stored the tech name as free text are a separate optional backfill.

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete jobs / too small on phone / drag-and-drop" | Exact duplicate of **n10** (identical text + timestamp); already declined. Fold into n10. |
| n41 | Brandon | Completion / P-U date to record when units leave the lot | Duplicate of **n24** (same submitter); same Key Dates field. Fold into n24. |
| n42 | Sofia | Returning customer (Sheldon Gilbert) check-in sent no email | Duplicate of **n40**; same defect, now fixed v1.10/v1.11. Fold into n40. |
| n44 | Roland | Auto-remove cashed-out RO from manager work list | Exact duplicate of **n43** (timestamps 1s apart). Fold into n43. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Scheduling & key dates  *(n4, n24, n29, n36, n37)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n4 | Lynn | Scheduled future drop-off date + email reminder to manager 1 day before | M |
| n36 | Lynn | Scheduled drop-date field in entry dashboard | S -- duplicate intent of n4 |
| n24 | Brandon | Completion / pickup date on the customer's order (canonical for n41) | M |
| n29 | Lynn | Promised date large + highlighted on tile; sort by promise date then days-on-lot (`promised_date` exists L244/L304; tile emphasis + sort do not) | M |
| n37 | Roland | RO date/time as a prominent colored RO button + integrate into business calendar(s) with advance notifications | L |

-> **"Key Dates + Calendar" initiative.** n37 is the umbrella ask tying the
drop-off/promised/completion dates (n4/n24/n29/n36) to a calendar surface and the
notifications cluster below.

### Theme 2 -- Notifications, reminders & task-tracking  *(n3, n8, n20, n30, n32, n35)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + font/color | L (urgency partly exists) |
| n35 | Lynn | Transfer clock-in job notes -> RO updates | M |

-> **Largest opportunity.** A "Task Tracker + Reminders" subsystem; overlaps the
deferred Twilio/SMS work -- design as one initiative.

### Theme 3 -- Parts  *(n6, n7, n9, n27, n28)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n28 | Brandon | Parts + freight TOTAL in Manage Parts (`pf_coreCharge` L217 + MONEY_FIELDS L288 exist; no summed total row) | S -- clean quick win |
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier (canonical for n27) | L |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

### Theme 4 -- Work List usability  *(n10, n31, n43)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n10 | Riley | Mobile: too small on phone; touch-drag reorder (delete + HTML5 DnD reorder shipped) | S -- mobile polish |
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |
| n43 | Roland | Auto-remove an RO from a manager's work list the day after Cashiered Out/Delivered (no auto-removal hook; manual `removeFromWorkList` only) | M |

### Theme 5 -- RO fields & status nuance  *(n18, n34, n26, n19, n16)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n18 | Lynn | "Keep plugged in" flag; keys / no-keys / keypad code | M |
| n34 | Brandon | Confirm if customer leaves keys with the unit (RO / sign-in / auth form) | S -- overlaps n18 |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning vs never-on-lot | M |
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs existing Hybrid) |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |

### Theme 6 -- Work Orders / templates  *(n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will (`work-orders.js` has load/save/apply, no edit/reorder) | M |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague -- overlaps n22) |

### Theme 7 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

### Theme 8 -- Reporting & analytics  *(n39, n45)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n39 | Roland | Admin-level daily Manager-worklist report (parts/labor/costs, upcoming dates, missing-from-worklist ROs) -> manager scoring + profit-margin visibility | L -- extends `worklist-report.html` |
| n45 | Brandon | Split the dashboard "Total RVs" / "RVs on Lot" stat so on-lot units are not conflated with "Not On Lot" (`render.js` `onLotROs` filter L594 exists; headline stat does not split it) | S -- dashboard-stat add |

### Theme 9 -- Check-In & e-signature  *(n46)*  [new]
| # | Submitter | Request | LOE |
|---|---|---|---|
| n46 | Sofia | Send the authorization form (RAF) remotely for e-signature (`customer-checkin.html` signs on-site only -- `signatureCanvas` + `getSignatureData` L1754; no remote-signing route) | L -- needs public token-signing page + email link + capture/store + staff notice |

---

## Recommended action today

1. **Fix the bug first: n47 (mobile photo upload).** Reproduce on an iPhone, then
   move the index photo upload to a persistent in-DOM file input and add a
   post-write verification / failure toast. Do not close n47 until reproduced and
   fixed (`fix/er-bugs-vX.Y` off `pre-prod`, version-bump + `[ER BUGFIX]` marker,
   `node --check`, local + prod Chrome regression).
2. **Close the resolved ERs:** set `status=done` for n5, n12, n17, n21, n22, n33,
   n40 (n1/n2 already done); set `declined` for the duplicates n11/n41/n42/n44.
   n0/n14/n15 are already closed.
3. **Run the n25 staff-name SQL** (Roland; data only).

Standout near-term enhancement wins remain **n28 (parts+freight total, S)**,
**n45 (split on-lot stat, S)**, and a coordinated **Key Dates + Calendar** pass
(Theme 1, anchored by Roland's n37). The strategic asks -- **n38 (auth cost
reduction)**, **n39 (manager reporting/scoring)**, and the new **n46 (remote
e-signature)** -- warrant a design session with Roland before scoping.
