# PRVS Enhancement-Request Triage -- 2026-07-09 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched
2026-07-09T10:27Z; incremental pass, `full_pass=false`).
**Codebase mapped against:** current `pre-prod` tree at **index.html v1.471**
(customer-checkin.html v1.16, checkin.html, edge functions under
`supabase/functions/`). Verdicts verified by grepping/reading the live code, not
assumed.
**Total ERs:** 64. **Triaged this run:** 63 ids in `needs_triage_ids`; 1
(`f73a1e5a`, "Testing dictation 123") carried over unchanged (already declined).

Each ER was sorted into exactly one bucket:

- DONE -- already delivered; ER can be closed. Evidence cited from current code.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate + theme.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

## Board snapshot

| Status (DB) | Count |
|---|---|
| done | 45 |
| declined | 9 |
| unreviewed | 10 |
| **Total** | **64** |

| Bucket (this run) | Count |
|---|---|
| done | 46 |
| needed | 10 |
| duplicate | 6 |
| data | 1 |
| bug (still open) | 0 |
| **Triaged** | **63** |

**10 open ERs, all NEEDED** -- no still-open bugs, nothing new to close as Done.
The open mix is **2 XL - 4 L - 1 M - 3 S**.

## Code checks run this session

- Parts inventory / stock in `js/parts.js` -- **none** (0 grep hits; confirms `14f35f95` XL still open).
- Parts supplier-return / warranty-return reverse flow in `js/parts.js` -- **none** (confirms `b19975d4` L).
- Per-customer / per-RO task-tracker table or todo scraping in `js/*` -- **none** (confirms `5925fc01`, `af14b3cb`).
- Auth model in `js/auth.js` -- only `signInWithIdToken` (Google id-token + email RBAC); **no** `signInWithPassword`/`signInWithOtp` path (confirms `ed1b873e` XL).
- Remote/tokenized e-signature flow -- **none**; RAF still kiosk-only `#signatureCanvas` in `customer-checkin.html` (confirms `0448de62` L).
- Lost / quoted-only disposition in `js/ro-crud.js` -- **none**; `archiveROInSupabase` still routes to `cashiered` (confirms `5fbfa299` M).
- Back-to-top / scroll-to-search control in `index.html` -- **none**; the lone "to-top" hit is a `bottom-to-top` code comment (confirms `29654af7` S).
- Confirmed the items that flipped needed->done in S128/S129 are real in code:
  `js/shop-tasks.js` + `shop_tasks.sql` (`1fe68261`), customer-checkin v1.14
  auto-add via `add_ro_to_silo_work_lists()` RPC (`cbc70a86`),
  `send-dropoff-report` edge fn (`7f2d0d70`), `warranty_repair` roType
  (`a5ff3e2d`), `.urgent-update-banner` (`a7d1474e`), `send-admin-pnl-report`
  (`cd084233`), `sync-ro-calendar` (`2b814250`/`cb7742a8`/`3f7fb4fc`).

Nothing has changed the buckets since the 2026-07-08 pass: the only commits since
(Session 135 check-in v1.16 date-echo fix + parts-report `date_received` fix, and
v1.470/1.471 Project Blue customer messaging) touch already-`done` areas and do
not deliver any open ER (they do make `0448de62` e-sign a natural Phase-3 rider on
the new `js/messaging.js`).

---

## BUG bucket (front of the line)

**None still open.** Every reported defect maps to a shipped fix confirmed in the
current tree -- see the DONE table (`18611eee`, `13e1cd87`, `cd663d8f`,
`a22739b9`, `2c3d5633`, `298e0e67`, `ee50d11c`, `cb7742a8`).

---

## NEEDED bucket (the 10 open ERs, by theme then size)

### Notifications & Task-Tracking

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `5925fc01` | Lynn | 04-09 | **L** | Notify the right team on every RO update; daily list of all in-progress jobs with to-dos scraped from notes | PARTIAL: daily digest served by Manager Daily Report (`send-admin-pnl-report`/`send-manager-report`). Still open: per-RO notify on EVERY update + auto-scrape to-dos into a per-job task list; no per-RO tasks table (grep 0). |
| `af14b3cb` | Lynn | 05-21 | **L** | Per-customer task tracker + daily reminder; tasks persist until marked complete | `js/shop-tasks.js` is deliberately NON-customer, so it does not satisfy this; no per-RO tasks table. Needs tasks table + add/complete UI + daily open-task reminder. Same theme as `5925fc01`. |

### Parts

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `14f35f95` | Lynn | 04-09 | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | `js/parts.js` has zero inventory/stock handling (grep 0). New subsystem: inventory table + receive/consume/adjust + searchable stock + parts-request links. Largest item on the board. |
| `b19975d4` | Lynn | 04-09 | **L** | Part returned-to-supplier / warrantied-by-supplier tracking | No supplier-return/warranty reverse flow in `js/parts.js` (grep 0). = GH#24 Shop Ops Phase 2: reverse flow + return reasons + credit/warranty tracking. Canonical for declined dup `86bbaf50`. |
| `8d885331` | Lynn | 04-09 | **S** | Rework the parts-entry field order | Layout tweak to `js/parts.js` `pf_` field order. Quick once Lynn gives the desired layout/mockup. BLOCKED on detail, not effort. |

### Auth & cost

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | Auth is Google id-token + email RBAC (`js/auth.js` `signInWithIdToken`; no password/OTP path, verified). Alternatives: Supabase password/magic-link or shared role accounts. Security-critical: every page sign-in + RBAC. Scope together. |

### Messaging & e-sign

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `0448de62` | Sofia | 06-11 | **L** | Send an authorization form remotely for e-signature | No remote-signing flow; RAF is kiosk-only (`customer-checkin.html` `#signatureCanvas`). Needs a tokenized public signing page + delivery + signed-form storage. Natural Phase-3 rider on new `js/messaging.js` (Project Blue). |

### RO fields / status

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `5fbfa299` | Brandon | 06-30 | **M** | Archive quote-only customers/units that never moved to work | Archiving routes to Cashiered (`js/ro-crud.js` `archiveROInSupabase`), misclassifying un-worked quotes as completed P&L jobs. Needs a separate lost/quoted-only disposition + board filter so they don't pollute Weekly P&L. |

### Time clock

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `e06d747e` | Riley (solar) | 04-10 | **S** | Tech check-in "float" for ease of access | Underspecified -- likely a floating/sticky check-in button, but page + behavior unclear (checkin.html is QR/roId-driven). Needs scoping with the submitter. |

### Dashboard Navigation

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `29654af7` | Lynn | 07-06 | **S** | Back-to-top / jump-to-search button after scrolling | No back-to-top/scroll-to-search control in `index.html` (only `scrollToROInBoard`-style element helpers). Needs a floating "to top" button (and/or jump-to-search) after scroll. Small, self-contained. Newest ER. |

---

## DATA bucket

| ER | By | Date | Fix |
|---|---|---|---|
| `6938c1af` | Ryan | 05-06 | Staff last name Wimbles -> Wombles (Zak, Rod, Travis). Names live in the `staff` table; no `Wimbles` string in js. **Already applied** (status `done`). |

**SQL (for the record; runner does not execute):**

```sql
UPDATE staff SET last_name = 'Wombles' WHERE last_name = 'Wimbles';
```

---

## DUPLICATE bucket

| ER | By | Date | Canonical | Note |
|---|---|---|---|---|
| `aa508852` | Riley | 04-09 | `fd6c122d` | Work list too small on phone / cannot delete or drag-drop; delivered (touch drag-drop v1.460). |
| `86bbaf50` | Lynn | 05-13 | `b19975d4` | Gordon parts missing / window returned to Vogt = part return-to-supplier. Folds into the open Parts-returns ask. |
| `67daba15` | Brandon | 06-09 | `d2561e11` | Completion / pick-up date for records; delivered. |
| `fa1c1d6e` | Sofia | 06-09 | `a22739b9` | Returning check-in sent no confirmation email; same defect, now fixed. |
| `5057c7fc` | Roland | 06-09 | `91a03260` | Remove cashed-out RO from a manager worklist; exact duplicate. |
| `fc6b29f9` | Sofia | 06-17 | `298e0e67` | Returning-customer check-in duplicated the RO; confirming test obs, now fixed. |

---

## DONE bucket (close / keep closed -- 46)

Test entries (keep declined): `33750cb3`, `19dc2016`, and carried-over
`f73a1e5a` ("Testing dictation 123").

| ER | By | Date | Cat | Evidence (current code) |
|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | UI/UX | Test entry (Make-A-Wish trial). Already declined. |
| `79b2c805` | Sofia | 04-09 | Feature | Cashed-out/Cashiered ROs stay viewable/editable via Delivered/Cashed Out board filter; docs still attachable. |
| `75124b03` | Ryan | 04-09 | RO Mgmt | Warranty RO type on New RO (`js/render.js` roType 'warranty'); extended by `a5ff3e2d`. |
| `b2aa4bea` | Lynn | 04-09 | Feature | Scheduled future drop-off date (`planned_dropoff_date`, `js/ro-crud.js`) drives the reminder. Related `468aa376`. |
| `18611eee` | Lynn | 04-09 | Bug | Work list now fully view/manage (`js/work-list.js` add/view/touch drag-drop/remove over `manager_work_lists`). |
| `17492066` | Lynn | 04-09 | Notif | Served by `scheduled_notifications` (`process-scheduled-notifications` cron) + `3f415c88` multi-day reminders. |
| `fd6c122d` | Riley | 04-09 | Work List | Touch drag-drop reorder v1.460 + delete/remove (`js/work-list.js`). Canonical for dup `aa508852`. |
| `98bd7498` | Brandon | 04-09 | RO Mgmt | Sr Manager silo-specific work lists + silo tabs (`service_silo`, `js/work-list.js`) v1.305. |
| `19dc2016` | Roland | 04-11 | Check-In | Test entry (ER form / dictation). Already declined. |
| `1fe68261` | Rod | 04-13 | Work Orders | `js/shop-tasks.js` Internal Shop Task tracker (`shop_tasks.sql`), non-customer tasks. S128 v1.467. |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | 'Ready to Work' status in `js/config.js` STATUS_PROGRESS_MAP. |
| `b87eb2fb` | Lynn | 04-14 | RO Mgmt | Keep-plugged-in flag + keys/no-keys/keypad-code on RO (`js/ro-crud.js`). |
| `a5ff3e2d` | Lynn | 04-21 | RO Mgmt | Warranty + Repair RO type (`warranty_repair`, `js/render.js`) v1.465 S127. |
| `a7d1474e` | Lynn | 04-23 | RO Mgmt | Always-visible urgent-updates banner (`js/render.js` `urgentUpdate` + `.urgent-update-banner`) v1.466. |
| `dde90d2b` | Roland | 04-27 | RO Mgmt | Check-in streamlined + confirmation/auth email restored (see `a22739b9`); photo deferred. |
| `13e1cd87` | Tipton | 04-30 | Check-In | Return-to-Dashboard button after clock-out (checkin.html v1.34). |
| `e27311bf` | Ryan | 05-04 | Work Orders | WO/task templates editable + drag-drop reorder (`js/work-orders.js` `_initWOTaskDrag`). |
| `d2561e11` | Brandon | 05-06 | Feature | Completion/pick-up date as RO key date (`pickupDate`, `js/render.js`). Canonical for dup `67daba15`. |
| `dac9fdda` | Brandon | 05-06 | UI/UX | 'Off Lot - Returning' status (`js/config.js`) excluded from on-lot count. v1.460 S120. |
| `a8e90a7d` | Brandon | 05-20 | UI/UX | Manage-parts parts + freight total (`js/parts.js` totWholesale+totFreight=totCost). Extended by `671fa623`. |
| `1aeb3f58` | Lynn | 05-21 | UI/UX | Promised date large/highlighted + hover key-dates (`js/render.js`). NOTE: board sorts by priorityScore, not strictly promise-then-days. |
| `cbc70a86` | Lynn | 05-21 | UI/UX | customer-checkin v1.14 auto-adds drop-off RO to team leads' work lists via `add_ro_to_silo_work_lists()` RPC. S128. |
| `3f415c88` | Lynn | 05-26 | UI/UX | Multi-day / weekly-until-date reminders (`scheduled_notifications`, index.html). |
| `cd663d8f` | Bobby | 05-28 | UI/UX | Currency 2-decimal formatting on blur/edit (`ro-crud.js` `editDollarValue` toFixed(2)) v1.447. 425.00 preserved. |
| `34fc03c2` | Brandon | 05-29 | Feature | Keys-left confirmation on RO (`key_status`/`keypad_code`). NOTE: on dashboard RO, not yet on the check-in auth form. |
| `0b2e128c` | Lynn | 06-02 | UI/UX | clockOut copies tech notes into RO update timeline (checkin.html) v1.460 S120. |
| `468aa376` | Lynn | 06-02 | UI/UX | Scheduled drop date settable from entry-dash New RO (index.html), drives reminder. Related `b2aa4bea`. |
| `2b814250` | Roland | 06-07 | Calendar | RO key dates -> Google Calendar via `sync-ro-calendar` (server-side S129) + advance reminders. |
| `cd084233` | Roland | 06-07 | Work List | Admin manager-focused daily report = `send-admin-pnl-report` (WTD cost vs WO value, exceptions, WIP aging). |
| `a22739b9` | Sofia | 06-08 | UI/UX | Check-in confirmation email + auth-form copy sent again (customer-checkin `sendRAFEmail`) S97. |
| `91a03260` | Roland | 06-09 | Work List | Cashiered/Delivered ROs excluded from active Work List + WO-badge (`isActiveROForWoNudge`). Recommend cron spot-check. |
| `cdd77a8b` | Brandon | 06-10 | UI/UX | Total RVs distinguishes on-lot ('N on lot' sub-line, `js/render.js`) from total customers. v1.453. |
| `2c3d5633` | Ryan | 06-12 | UI/UX | Phone photo upload fixed (`js/photos.js` attaches input to DOM before .click()) v1.453 S114. |
| `74a33621` | Brandon | 06-16 | UI/UX | Arrow-key photo lightbox nav (`js/photos.js` `navigateLightbox`) v1.454. |
| `298e0e67` | Sofia | 06-16 | UI/UX | Returning check-in vs scheduled drop-off no longer duplicates RO (customer-checkin dup guard). |
| `9b823d25` | Roland | 06-16 | RO Mgmt | Adding a service silo emails that silo's manager(s) (`js/work-orders.js` `addServiceToRO`) v1.460 S120. |
| `9b046808` | Brandon | 06-17 | UI/UX | 'Scheduled' disposition separates drop-off-time units from on-lot; excluded from count (`js/render.js`). |
| `671fa623` | Brandon | 06-18 | UI/UX | Manage-parts ticket totals include freight (`js/parts.js` totFreight). Related `a8e90a7d`. |
| `5c45bb8d` | Sofia | 06-19 | UI/UX | Both defects fixed: dup-RO (`298e0e67`) + missing check-in auth email (`a22739b9`). |
| `b06a285f` | Lynn | 06-19 | UI/UX | Calendar hover panel by customer name lists all key dates (`js/render.js` `.kd-name-cal`) v1.465 S127. |
| `ee50d11c` | Lynn | 06-19 | UI/UX | Time-off entries editable after creation (time-off.html `editRequest` UPDATE) v1.4 S118. |
| `88395acc` | Lynn | 06-19 | UI/UX | Time-off partial-day window (`absence_start_time`/`absence_end_time`) v1.5 S127. |
| `80390f36` | Lynn | 06-23 | UI/UX | Shop Operations tile pinned to top + findable in search. v1.462. |
| `7f2d0d70` | Lynn | 06-26 | UI/UX | `send-dropoff-report` Monday heads-up (coming in + last week received) via `planned_dropoff_date`. S128. |
| `3f7fb4fc` | Brandon | 06-29 | UI/UX | Shop ROs static status (no on-lot dropdown), excluded from RVs-in-queue/on-lot (`js/render.js`) S129 v1.468. |
| `cb7742a8` | Lynn | 06-29 | UI/UX | New-customer check-in dates post to correct team calendar via `sync-ro-calendar` (server-side) S129 v1.468/1.469. |

*(Carried over, unchanged: `f73a1e5a` "Testing dictation 123" -- Roland, 04-13 -- test entry, remains declined; not re-triaged this run.)*

---

## Recommended action today

1. **Nothing to close or fix this pass** -- the bug bucket is empty and no open ER
   is already delivered. The 45 `done` + 9 `declined` rows in the DB are correct;
   the 10 `unreviewed` are all genuine open work.
2. **Two small, self-contained wins** worth pulling forward: `29654af7`
   (back-to-top button, S) and, once Lynn specifies layouts, `8d885331`
   (parts-entry field order, S). `e06d747e` (check-in float, S) needs a quick
   scoping note from the submitter first.
3. **Design, don't rush, the big rocks:** `ed1b873e` (auth swap, XL, security-
   critical) and `14f35f95` (parts inventory, XL) each deserve their own scoping
   session with Roland.
4. **Cluster the L's:** the Notifications & Task-Tracking pair (`5925fc01` +
   `af14b3cb`) should be designed as one task-tracker + reminders initiative;
   `0448de62` (remote e-sign) now rides naturally on the new `js/messaging.js`
   Project Blue surface as a Phase-3 item.
5. **One-line data note:** the Wimbles -> Wombles staff fix (`6938c1af`) is already
   applied; SQL retained above for the record.
