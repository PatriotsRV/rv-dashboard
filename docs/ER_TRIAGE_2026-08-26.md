# PRVS Enhancement-Request Triage - 2026-08-26 (headless runner)

Incremental pass over `enhancement_requests` against the current modular
codebase at HEAD **9c50b52** (index.html **v1.501**). Input snapshot
`er_input.json` fetched 2026-08-26 07:50 UTC, `full_pass=false`.

**Only 2 of the 105 rows were in `needs_triage_ids`** and were classified this
run - both brand-new ERs filed by Brandon on 08-25, both about the parts receipt
date. Every other row below **carries its stored `triage_bucket` /
`triage_loe` / `triage_verdict` unchanged**; those verdicts were stamped at
earlier HEADs (0223cb3 / 65fdb97 / a405b7a / df519c6 / e8666a9 / f501151) and
were NOT re-verified this run. Line anchors in carried verdicts may have
drifted; the "What changed" section below flags where that matters.

Runner is read-only: **no code was changed, no DB writes were made, no git
commands were run.** Status flips and the Data-bucket SQL are Roland's to run.

## Board snapshot

| DB status | Count |
|---|---|
| done | 62 |
| unreviewed | 28 |
| declined | 15 |
| **Total** | **105** |

## Verdict buckets (104 classified; 1 row carries no verdict)

| Bucket | Count |
|---|---|
| done | 62 |
| needed | 27 |
| duplicate | 13 |
| data | 2 |
| bug | **0** |

**Headline: the bug column is empty for the first time.** All five bugs on the
2026-08-08 board (`09084bc5`, `225a2535`, `d94a418a`, `d4d0eeba`, `5ca724ff`)
were fixed in the S171/S178 v1.494-v1.498 run and carry `done` verdicts. Two new
ERs arrived (`2af47890`, `1e434aff`), both Needed/S, both in Parts & Inventory,
and both are two halves of one small build. Open work is 27 enhancements: 15 S,
3 M, 4 L, 2 XL, plus 3 more S counted inside those groups.

## Classified this run (2)

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `2af47890` | Brandon Dillon | 08-25 | General UI/UX | **needed** | **S** | `js/parts.js` markPartReceived L449-478, updatePartInSupabase L539; `supabase/migrations/stamp_date_received_on_parts.sql` L33-36; `index.html` L3556 (`updatePartInSheet` -> `updatePartInSupabase`) | **Half-delivered; filed `needed` not `done` per the prefer-needed rail.** The DB half genuinely ships: the S135 trigger `trg_stamp_date_received` stamps `date_received` to the America/Chicago calendar day on any transition INTO `Received`, so the persisted date is already correct today. The CLIENT half is missing: `markPartReceived` sets `part.status = 'Received'` and nothing else, so the in-memory `part.dateReceived` stays `''` after the reopen at L473. Consequence: if staff then click the pencil and re-save that part, `showAddPartForm` populates `pf_dateReceived` from the stale blank (L308-311), `savePartForm` reads it back blank (L353) and `updatePartInSupabase` sends `date_received: null` (L539) - and the trigger deliberately will NOT re-stamp because `OLD.status` is already `'Received'` (migration L34), so the receipt date is **wiped**. Fix is 1-2 lines: set `part.dateReceived` to today's shop-local date in `markPartReceived` before the write. |
| `1e434aff` | Brandon Dillon | 08-25 | General UI/UX | **needed** | **S** | `js/parts.js` table headers L259-267, `renderPartsRows` L121-145, quick-Received cell L133-137, `pf_dateReceived` L242 | Confirmed absent at HEAD 9c50b52. The Manage Parts table - the "parts status page" Brandon means - has nine columns (Part Name, Part #, Supplier, Status, Qty, Wholesale, Retail, Quick, Actions) and `renderPartsRows` never prints `p.dateReceived`. Once a part is Received the Quick cell collapses to a bare tick (L136) with no date. The value exists on the row and is editable, but only inside the collapsed Add/Edit Part form (`pf_dateReceived` L242), so nobody reading the list can see when a part came in. Small: render `dateReceived` next to the tick (or add a Received column). **Build it together with `2af47890`** - without that fix the column would render blank for parts received via the quick button in the current session. |

---

## NEEDED - open enhancements by theme (27)

### RO Fields & Status (6)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `44419de9` | Lynn | 08-12 | **S** x4 | `js/config.js` STATUS_PROGRESS_MAP; `index.html` select / filter btns; `js/render.js` statusColorMap, ALL_STATUSES | None of the 4 values (Need Estimate, Awaiting Estimate, Estimate Declined, Job Declined/Close) exist. Each add must hit 9 hardcoded spots PLUS both DB CHECKs per S172 or saves throw 23514. Overlaps `972af03c` CANCELED - confirm scope with Lynn; bundle with `972af03c` + `3e746c18`. |
| `972af03c` | Lynn | 07-17 | **S** | `js/config.js` STATUS_PROGRESS_MAP | No Canceled/Cancelled RO status. Add the status plus color/priority maps, exclude it from the on-lot and queue counts, keep the record for notes. Canonical for duplicates `eb00a68d` and `611589ba`. |
| `3e746c18` | Lynn | 08-03 | **S** | `js/config.js` STATUS_PROGRESS_MAP; `js/render.js` count exclusions | No TOTALED OUT status. Distinct value from `972af03c`'s Canceled, but ONE build should add both terminal statuses and exclude both from the counts. **See the note below on v1.501.** |
| `0e172b2c` | Sofia | 07-16 | **M** | `js/duplicates.js` L35, L36, L40, L44 | Merge engine exists but groups strictly on the `PRVS-XXXX-XXXX` base RO id, so two ROs under different spellings of the same customer are never detected. Needs name/phone account dedup plus a verify-which-is-correct step. One-off cleanup was `3f254929`. |
| `5fbfa299` | Brandon | 06-30 | **M** | `js/ro-crud.js` archiveROInSupabase | Only the cashiered archive path exists; no soft-archive flag, status or board filter for quoted-but-never-progressed units. Needs a flag, a board filter and an un-archive path. Partly absorbed by `972af03c`'s Canceled status - decide which. |
| `a195e4ab` | Brandon | 08-20 | **M** | `index.html` Customer Pay block L7203-7246 / L7782-7804; `setROType` L2263-2267 | Zero check tracking - `check_number` / `checkNumber` / `payments` return 0 hits. The only payment capture is the one-value Customer Pay block, hybrid-RO-only, method Check but no check number, one amount plus datePaid, saved flat into `insurance_data` JSONB and never shown on the card. Needs a per-RO checks table plus add/list UI plus a cashiered mirror. |

### Messaging & E-sign (7)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `9aab7550` | Brandon | 07-21 | **S** | `messages.html` `.unread-dot` CSS L469, L1313, L1319; `markConversationUnread` L1980 | Still only partial. S166 added a MANUAL "Mark unread" button, but the visual treatment is unchanged: a single 8px gold dot applied only when `last_direction` is inbound AND status is open, with no row-level highlight. Small: add a bold/tinted unread row treatment. |
| `21fc3a9d` | Brandon | 07-21 | **S** | `messages.html` fixed rem font sizes | No user text-size control - `font-scale` / `fontScale` / `textSize` return zero hits. Small: add a persisted font-scale control for the conversation list. |
| `f1e6e311` | Brandon | 07-21 | **S** | `messages.html` `convoSearch` L604, resets L1151/L1170 | The conversation search is a plain text input with no clear affordance; the field is reset only programmatically. Small: add a clear (x) button that empties the search and re-runs the filter. |
| `6e9ca4dc` | Brandon | 07-21 | **S** | `js/messaging.js` refreshSignaturePreview L369, `#msgSigPreview` L350/L374; `messages.html` media query L565 | "Added to every send:" is hidden only inside the mobile max-width media query, so desktop messages.html and the RO-card composer still show it. Small: remove it or collapse it behind a toggle in all viewports; the signature itself keeps sending. |
| `ca0f7ed9` | Brandon | 08-03 | **S** | `messages.html` renderLeadInfo L1389, selectConversation L1744 | `renderLeadInfo` reads ONLY the conversation's own columns, so an existing dashboard customer shows "No RV or service type captured yet". `selectConversation` already computes phone-matched live ROs as `linked` for `renderContactInfo` but never passes them. Small: pass `linked`, fall back to RO rv. |
| `5fec3eca` | Sofia | 07-15 | **S** | `supabase/functions/send-quote-email` raf_copy branch L387 | The raf_copy payload has no `customerAddress`; the address appears only in the `lead_staff_notify` branch. Small: add `customerAddress` to the raf_copy payload and both templates. Edge-function deploy, no dashboard release. |
| `0448de62` | Sofia | 06-11 | **L** | `customer-checkin.html` on-device signature pad; `send-quote-email` raf_copy L386 | Signing is kiosk-only; `signing_token` / `remote_sign` / `signature_request` / `signing_link` return nothing. Needs a tokenized public signing page, a delivery path and signed-form storage. Pairs naturally with the S166 short-link machinery. |

### Parts & Inventory (5)

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `2af47890` | Brandon | 08-25 | **S** | `js/parts.js` L449-478 / L539; `stamp_date_received_on_parts.sql` L33-36 | **NEW this run.** Trigger stamps `date_received` server-side, but `markPartReceived` never mirrors it locally, and a subsequent form edit resends `date_received: null` which the trigger will not restamp - wiping the date. See the full verdict above. |
| `1e434aff` | Brandon | 08-25 | **S** | `js/parts.js` L259-267 / L121-145 / L242 | **NEW this run.** The Manage Parts table prints no receipt date; a Received part collapses to a bare tick. See the full verdict above. Ship with `2af47890`. |
| `8d885331` | Lynn | 04-09 | **S** | `js/parts.js` `pf_supplier` L181, `pf_partName` L190, `pf_serviceSilo` L208, `pf_returnDeadline` L226 | The parts-entry form exists and is unchanged. The ask is only to reorder those fields, which is quick once Lynn supplies the target layout or a mockup. **Blocked on detail, not on effort.** |
| `b19975d4` | Lynn | 04-09 | **L** | `js/config.js` PART_STATUSES L200; `js/parts.js` `pf_returnDeadline` L226 | Partial infra only: a `Returned` status and a Return Deadline input exist, but `rma` / `return_reason` / `supplier_credit` return zero hits - no RMA number, return reason, supplier credit or reverse-direction flow. Equals GH#24 Shop Ops Phase 2. Canonical for declined duplicate `86bbaf50`. |
| `14f35f95` | Lynn | 04-09 | **XL** | `js/parts.js` (per-RO line items only) | `inventory` / `on_hand` / `in_stock` / `stock_qty` return zero hits. New subsystem needed: inventory table, receive/consume/adjust flows, a searchable priced stock view, and links from the parts-request path. Largest item on the board. |

### Notifications & Task-Tracking (3)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `a03677e6` | Brandon | 08-07 | **S** | `js/ro-crud.js` L316/L358/L393/L545/L602; `js/render.js` ro_status box vs customer_comm; `js/parts.js` L654/L855 | Machine audit lines write as notes type `ro_status` and so crowd RO Status Updates - NOTIFICATION SENT/FAILED, SCHEDULED/CANCELLED, key-date and urgent rows, parts notes. `customer_comm` renders separately. Small: retarget the type. **NOTE Roland reversed this retarget for LEAD NOTES in v1.491 - confirm scope first.** Canonical for declined duplicate `2deb8f30`. |
| `af14b3cb` | Lynn | 05-21 | **L** | `js/shop-tasks.js` (no `ro_id`, no reminder column) | `ro_tasks` / `customer_task` / `task_tracker` return zero hits. Needs a per-RO tasks table, an add/complete UI and a daily open-task reminder. Canonical for declined duplicate `ece91cba`, which supplies the real spec (bulleted, anyone can add or check, record who and when). Pairs with `5925fc01`. |
| `5925fc01` | Lynn | 04-09 | **L** | `supabase/functions/send-manager-report`; `js/work-orders.js` L293; `js/ro-crud.js` L374 | PARTIAL. The daily in-progress digest half ships, and single-event notifies exist. Still absent: notify-on-every-RO-update and to-do scraping from status notes. Anchor of this theme; design together with `af14b3cb`. |

### Time Clock & Check-In (2)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `6bb61db2` | Riley | 07-22 | **S** | `js/render.js` compact-col-actions L228-235; `js/time-tracking.js` | Compact actions offer edit / parts / WO / notify / message / schedule but no check-in-out button, and `clocked in` / `activeTimeLog` return nothing. Small: add a compact check-in/out action plus a login-time active-time_log banner shown across views. **Treat as the canonical spec.** |
| `e06d747e` | Riley | 04-10 | **S** | `index.html`, `checkin.html`, `js/render.js` L228-235 | No floating or sticky tech check-in control anywhere. Underspecified - scope the target page and the exact behavior with the submitter. Almost certainly the same ask as `6bb61db2`; fold into it. |

### Board & Navigation UX (1)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `d1844e46` | Lynn | 07-16 | **S** | `js/render.js` renderBoard L30-39, shop-ops pin L35-37 | Sorting is one blended `calculatePriority` score; `sortBy` / `sortSelect` / `sortMode` find nothing, so there is no selectable strict promised-date-then-oldest-on-lot order. Small: add the strict sort option while keeping the shop pin. Related to `1aeb3f58`, which delivered only the promised-date chip. |

### Work Orders & Shop Tasks (1)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `2f752d40` | Jason Rubin | 07-21 | **S** | `js/work-orders.js` `woFormDollar` L360, `woFormEstHours` L365, save L649-650, dollarLocked L324 | Independent manual inputs, read separately on save; `LABOR_RATE` / `laborRate` / `labor_rate` return zero hits in the module (`js/insurance.js` laborRate L212 is the per-claim INSURANCE rate). Small: auto-populate `dollar_value` = effective hours x $195/hr, respecting the `dollarLocked` gate. |

### Scheduling & Dates (1)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `47428f75` | Lynn | 08-03 | **S** | `time-off.html` L368/L372/L752 | **UNDERSPECIFIED** - "update vacation/sick/personal calendar", no stated defect or target. time-off.html already has all three types, a color-coded month calendar, stats tiles, per-row Edit and exact partial-day windows. Filed Needed not Done because the ask maps to no gap without Lynn's detail; if it restates `ee50d11c` it is already delivered. **Scope with the submitter.** |

### Auth & Cost (1)

| ER | By | Date | LOE | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | **XL** | `js/auth.js` L69/L203-204, One Tap + `signInWithIdToken`; `js/config.js` GOOGLE_CONFIG | Still Google-based end to end; the kiosk uses the same path. Swapping to Supabase password / magic-link or shared role accounts drops roughly $500/mo but touches every page sign-in plus the email-keyed RBAC. **Dedicated design session with Roland.** |

---

## DATA - corrections for Roland (2)

Nothing below was executed by this runner.

| ER | By | Date | Verdict + SQL (carried) |
|---|---|---|---|
| `6938c1af` | Ryan Dillon | 05-06 | Already applied in the live DB - tech names read from `staff.name`, and submitter "Rod Wombles" now spells correctly in this very table. RESIDUAL, not a runtime bug: `supabase/migrations/staff_table.sql` L85-87 still seeds Rod/Zak/Travis as "Wimbles", so a re-seed would reintroduce it. Re-apply if ever needed:<br>`UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |
| `3f254929` | Sofia | 07-16 | **Already carried out in S178** - the misspelled Steve Hubert RO was soft-deleted; only the correct Huebert RO remains. SQL of record:<br>`SELECT id, ro_id, customer_name, status FROM repair_orders WHERE customer_name ILIKE '%ubert%';`<br>then, with the confirmed wrong id:<br>`UPDATE repair_orders SET deleted_at = now() WHERE id = '<wrong-uuid>' AND deleted_at IS NULL;`<br>The wider merge-accounts ask stays open as `0e172b2c`. |

---

## DUPLICATES - fold and decline (13)

| ER | By | Date | Folds into | DB status | Note (carried) |
|---|---|---|---|---|---|
| `aa508852` | Riley | 04-09 | `fd6c122d` | declined | Identical text and timestamp. Keep declined. |
| `86bbaf50` | Lynn | 05-13 | `b19975d4` | declined | Gordon window returned to Vogt = supplier return tracking. Keep declined. |
| `67daba15` | Brandon | 06-09 | `d2561e11` | declined | RO completion / pick-up date. Keep declined. |
| `fa1c1d6e` | Sofia | 06-09 | `a22739b9` | declined | Sheldon Gilbert check-in email. Keep declined. |
| `5057c7fc` | Roland | 06-09 | `91a03260` | declined | Prune cashed-out RO from the work list. Keep declined. |
| `fc6b29f9` | Sofia | 06-17 | `298e0e67` | declined | Retest report of the duplicate-RO guard. Keep declined. |
| `9031e101` | Riley | 07-21 | `d4d0eeba` | declined | iPhone work-list tap did not reach the unit. Canonical is now DONE (v1.497). Keep declined. |
| `1bc33dd6` | Brandon | 07-27 | `d4d0eeba` | declined | Same scroll-to-customer defect Brandon filed 07-22. Canonical is now DONE (v1.497). Keep declined. |
| `2b78696c` | Lynn | 07-27 | `29654af7` | declined | Same back-to-top ask Lynn filed 07-06. Canonical is now DONE (v1.498). Keep declined. |
| `eb00a68d` | Lynn | 08-03 | `972af03c` | declined | Same CANCELED status ask Lynn filed 07-17. Keep declined. |
| `2deb8f30` | Lynn | 08-13 | `a03677e6` | declined | Same ask - get machine-generated notification lines out of RO Status Updates. Lynn's "separate location view" wording folds in as spec. Keep declined. |
| `ece91cba` | Lynn | 08-17 | `af14b3cb` | declined | Same per-RO to-do list that persists until marked complete. This ER's added spec (bulleted, anyone can add or check, record who and when, plus the Repair Description vs RO Status split) folds into `af14b3cb`. Keep declined. |
| `611589ba` | Lynn | 08-24 | `972af03c` + `44419de9` | **unreviewed** | Both halves are already open ERs: CANCELED is `972af03c`, Closed is `44419de9`'s JOB DECLINED/CLOSE. Adds no new work item. **Still `unreviewed` - recommend `declined`.** See the v1.501 note below. |

---

## DONE - delivered (62)

Carried verdicts; the "verified at" HEAD is whatever the stored verdict names, not
this run's HEAD.

| ER | By | Date | Category | Mapped code | Verdict (carried) |
|---|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | General UI/UX | - | Test entry ("Testing out the Make-A-Wish function"). Not a real request. Already declined; keep declined. |
| `19dc2016` | Roland | 04-11 | Time Clock | - | Test entry ("Testing ER form / dictation"). Not a real request. Already declined; keep declined. |
| `79b2c805` | Sofia | 04-09 | New Feature | `closed-ros.html` L838/L725/L813, flat map L958-963; `js/photos.js` L253 | Cashed-out ROs reactivate with parts/time_logs/notes/audit_log/insurance_scans plus the WO subtree, and documents can be added to the reopened RO. The photo/doc library gap was Bug `09084bc5`, now also done. |
| `75124b03` | Ryan | 04-09 | RO Management | `js/ro-crud.js` warranty branch; `js/render.js` L121/L195/L372 | Warranty is a first-class RO type at creation with board filter, compact chip and card badge (plus the $0 No-Charge sub-badge). |
| `b2aa4bea` | Lynn | 04-09 | New Feature | `js/ro-crud.js` L413/L490; `js/render.js` L395 | `planned_dropoff_date` persists on create and edit, drives the blue Drop chip and the day-before reminder. Same field as `468aa376`. |
| `18611eee` | Lynn | 04-09 | Bug Report | `js/work-list.js` L195/L210/L223/L263/L304/L383 | Work-list members are viewable, reorderable and removable, with per-silo tabs. The desktop drag half was `5ca724ff`, now also done. |
| `17492066` | Lynn | 04-09 | Notifications | `scheduled_notifications.sql`; `process-scheduled-notifications`; `index.html` snRepeat L2993 | The per-RO Schedule Notification bell sets a future date/time, message and recipients with daily/weekly repeat - covers the advance-warning ask. |
| `fd6c122d` | Riley | 04-09 | Work List | `js/work-list.js` L195/L304/L389/L404-429 | Delete plus mouse and touch drag-reorder, persisted, with the mobile sizing fixed. Canonical for declined dup `aa508852`. |
| `98bd7498` | Brandon | 04-09 | RO Management | `js/work-list.js` L223/L263 | Per-silo Work List tabs give a separate list per job category. |
| `1fe68261` | Rod | 04-13 | Work Orders | `js/shop-tasks.js` L21/L130/L153/L180; `shop_tasks.sql` | Internal work not tied to a customer unit is tracked separately from ROs and the board. Matches the poly-barrel / roof-bay examples. |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | `js/config.js` L158; `js/render.js` L727/L741 | "Ready to Work" is a live RO status in the maps and both status dropdowns. |
| `b87eb2fb` | Lynn | 04-14 | RO Management | `keys_power_fields.sql`; `js/ro-crud.js` L415-417/L492-494 | key_status, keypad_code and keep_plugged_in persist on create and edit with a keys chip on the card. Same build as `34fc03c2`. |
| `a5ff3e2d` | Lynn | 04-21 | RO Management | `js/ro-crud.js` warranty_repair; `js/render.js` L122/L196/L373 | "Warranty + Repair" exists end to end and stays billable so the repair half bills normally. |
| `a7d1474e` | Lynn | 04-23 | RO Management | `urgent_update_field.sql`; `js/render.js` L324/L214; `js/ro-crud.js` L374 | Always-visible red URGENT banner plus compact siren, and setting it notifies silo managers and admins. The follow-up to move the INPUT to the tile was `50175fce`, now also done. |
| `dde90d2b` | Roland | 04-27 | RO Management | `customer-checkin.html` L2530/L2603 | Photo is optional and both the authorization-form copy and the staff notice are forced on every submission. |
| `13e1cd87` | Tipton | 04-30 | Time Clock | `checkin.html` L1516 | "Return to Dashboard" renders after clock-out, so no new tab is needed to clock into the next job. |
| `e27311bf` | Ryan | 05-04 | Work Orders | `js/work-orders.js` _initWOTaskDrag L461, L482 | Every WO task row has a grip handle and drag-to-reorder; DOM order drives sort_order, so templates are edited by load, reorder, Overwrite. |
| `d2561e11` | Brandon | 05-06 | New Feature | `key_dates_pickup_date.sql`; `js/ro-crud.js` L414/L491; `js/render.js` L397 | `pickup_date` persists and shows as the green Pickup chip and in the key-dates panel. Canonical for declined dup `67daba15`. |
| `dac9fdda` | Brandon | 05-06 | General UI/UX | `js/config.js` L154; `js/render.js` L718-721 | "Off Lot - Returning" separates units temporarily taken by the customer from units never on the lot, and is excluded from the on-lot count. |
| `a8e90a7d` | Brandon | 05-20 | General UI/UX | `js/parts.js` L152-161 | Manage Parts Ticket Totals bar sums parts, freight and total cost. Same build as `671fa623`. |
| `1aeb3f58` | Lynn | 05-21 | General UI/UX | `js/render.js` L396, L30-39 | Promised date is a large bold chip, red when overdue, and both promise date and days-on-lot feed the blended sort. The strict selectable sort is open ER `d1844e46`. |
| `cbc70a86` | Lynn | 05-21 | General UI/UX | `customer-checkin.html` L2381; `work_list_autoadd_rpc.sql` | Returning check-in calls `add_ro_to_silo_work_lists`, inserting `manager_work_lists` rows per involved silo lead with a sr-manager fallback. |
| `3f415c88` | Lynn | 05-26 | General UI/UX | `index.html` snRepeat L2993, read L3061 | Repeat none/daily/weekly plus repeat-until expands into one individually cancellable row per occurrence. |
| `cd663d8f` | Bobby | 05-28 | General UI/UX | `js/ro-crud.js` L1097; `js/parts.js` L307/L310 | Currency inputs show 2 decimals on edit-populate and in the parts modal. Cosmetic only - no data loss ever occurred. |
| `34fc03c2` | Brandon | 05-29 | New Feature | `keys_power_fields.sql`; `js/ro-crud.js` L415-417/L492-494 | The Keys selector is on New and Edit RO with a keys chip on each card. Covers `b87eb2fb`. |
| `0b2e128c` | Lynn | 06-02 | General UI/UX | `checkin.html` L1455 | Clock-out work notes are copied into the RO update timeline as a `ro_status` note. |
| `468aa376` | Lynn | 06-02 | General UI/UX | `js/ro-crud.js` L413/L490; `js/render.js` L395 | Planned drop-off date input on New and Edit RO, feeding the Drop chip and auto_dropoff_reminder. Same field as `b2aa4bea`. |
| `2b814250` | Roland | 06-07 | Calendar | `js/render.js` L395-397/L367; `sync-ro-calendar` | Key dates render as attention-grabbing chips plus a hover/tap panel, calendar integration runs server-side and idempotently, advance reminders go to managers/admins. |
| `cd084233` | Roland | 06-07 | Work List | `send-manager-report`, `send-admin-pnl-report`, `worklist-report.html` | Admin-level daily manager-worklist reporting with parts, labor and cost rollups. The manager scoring layer described at the end of the request is a future extension, not this ER. |
| `a22739b9` | Sofia | 06-08 | General UI/UX | `customer-checkin.html` L2530/L2603 | Notify-gate regression fixed - both sends forced on every submission. Canonical for declined dup `fa1c1d6e`. |
| `91a03260` | Roland | 06-09 | Work List | `prune_cashiered_worklists.sql` | Removes an RO from manager work lists the day AFTER Delivered/Cashed Out, deliberately keeping it visible on the day of cash-out. Canonical for declined dup `5057c7fc`. |
| `cdd77a8b` | Brandon | 06-10 | General UI/UX | `js/render.js` L656-658, L718-721 | Queue count drops Shop Operations ROs and the on-lot count excludes Not On Lot / Scheduled / Off Lot - Returning, so the tile shows a true on-lot number. |
| `2c3d5633` | Ryan | 06-12 | General UI/UX | `js/photos.js` L66 | The upload input is attached to the DOM before clicking - the iOS Safari dropped-change-event root cause. Phone photo upload completes. |
| `74a33621` | Brandon | 06-16 | General UI/UX | `js/photos.js` L396-397/L400/L404 | Lightbox supports arrow-key navigation plus Escape-to-close and prev/next buttons. |
| `298e0e67` | Sofia | 06-16 | General UI/UX | `customer-checkin.html` L2059/L2165, L2060/L2194 | The returning-customer guard reuses an existing scheduled RO, and v1.17 adds a blocking attach-vs-new dialog for the already-active case. Canonical for declined dup `fc6b29f9`. |
| `9b823d25` | Roland | 06-16 | RO Management | `js/work-orders.js` L244/L293; `service_added_notify_source.sql` | Adding a service to an RO emails the owning silo manager. Matches the roof-plus-water-heater example. |
| `9b046808` | Brandon | 06-17 | General UI/UX | `js/config.js` L157; `js/render.js` L718-721 | "Scheduled" is its own status and is excluded from days-on-lot stats and the on-lot count. |
| `671fa623` | Brandon | 06-18 | General UI/UX | `js/parts.js` L153/L155/L159-161 | Freight is summed across parts and rolled into Total Cost. Same build as `a8e90a7d`. |
| `5c45bb8d` | Sofia | 06-19 | General UI/UX | `customer-checkin.html` L2059/L2165/L2194/L2530 | Both halves of the David Flanagan report resolved - the Scheduled path reuses the RO and the authorization copy is forced on every submission. |
| `b06a285f` | Lynn | 06-19 | General UI/UX | `js/render.js` L367, L363-365, L395-397 | A calendar icon by the customer name opens a key-dates panel listing drop-off, promised and pickup in full, with colored summary chips. |
| `ee50d11c` | Lynn | 06-19 | General UI/UX | `time-off.html` L752 | Time-off entries are editable, not delete-only - Edit loads the entry back into the form and flips submit to "Update Record". |
| `88395acc` | Lynn | 06-19 | General UI/UX | `add_absence_window_to_time_off.sql`; `time-off.html` L368/L372 | Partial-day absence is an exact clock-out-to-return window rather than an hour total. |
| `80390f36` | Lynn | 06-23 | General UI/UX | `js/render.js` L35/L37/L61 | Shop Operations tiles pin to the top ahead of the priority sort, and a "shop operations shop" search token makes the tile findable. |
| `7f2d0d70` | Lynn | 06-26 | General UI/UX | `send-dropoff-report` | The Monday drop-off check email lists this week's scheduled drops plus last week's arrivals and flags any not yet on the right team's work list. Pairs with `cbc70a86`. |
| `3f7fb4fc` | Brandon | 06-29 | General UI/UX | `js/render.js` L656-658/L718-721/L249 | Shop ROs are excluded from both counts and the shop status dropdown is replaced by a static label. |
| `cb7742a8` | Lynn | 06-29 | General UI/UX | `sync-ro-calendar`; `customer-checkin.html` L2240 | Drop-off/promised/pickup events are posted server-side to the correct team calendar for every RO including front-desk check-ins. Fixes the repair-calendar miss. |
| `29654af7` | Lynn | 07-06 | General UI/UX | `index.html` L8212-8220; `css/dashboard.css` L3479-3498 | **DONE in v1.498 (S178).** `#backToTopBtn` with a smooth scroll handler plus a scroll listener that reveals it after meaningful scroll, pinned bottom-left clear of the toast stack. Canonical for declined dup `2b78696c`. Previously needed/S. |
| `39b75fab` | Sofia | 07-09 | General UI/UX | `js/photos.js` canManageFiles L618, L198-201, L376 | The Documents/Photos library supports rename, move-to-another-RO and delete, office-staff gated. |
| `1c7f1410` | Andrew Page | 07-16 | New Feature | `js/messaging.js` L347/L295/L428-435/L578-579 | Photo/PDF attach shipped S151b/S158 and video shipped S166. DB row is now `done` - the S171 paperwork closed it. |
| `3f254929` | Sofia | 07-16 | General UI/UX | (data) | Listed under DATA above; DB row is `done`. |
| `d432863a` | Brandon | 07-21 | General UI/UX | `index.html` L263 plus 12 more pages | **DONE in v1.498 (S178).** Favicon markup is in every page head across 13 pages; also closes the long-standing S139 favicon-404 console noise. Previously needed/S. |
| `d4d0eeba` | Brandon | 07-22 | General UI/UX | `index.html` L4342-4344; `js/qr.js` L198-214 | **DONE in v1.497 (S178)** - both navigation paths fixed. The Clear Filters toast action retries `scrollToROInBoard` after the re-render and reports an archived/missing RO; the messages `?ro=` deep link clears filters once behind a one-shot guard. Closes declined dups `9031e101` and `1bc33dd6`. Previously bug. |
| `9d3806e7` | Brandon | 07-22 | General UI/UX | `messages.html` L671/L669/L1599; `customer_note_entries_s154.sql` | The details pane has a customer-side Notes section plus an Add-Notes modal writing append-only rows keyed by phone. DB row is now `done`. |
| `ac394468` | Roland | 07-24 | General UI/UX | `js/enhancement.js` pageDefaults L43 | The erCategory select carries Messages/Texting plus Solar, Leads, Closed ROs, Reports, Time Off and Employee Guide. RESIDUAL, stated not hidden: erCategory exists in only 6 live HTML files, so messages/leads/time-off/guide cannot RAISE an ER - worth its own small ER. |
| `d62a26e4` | Roland | 07-24 | New Feature | `js/messaging.js` L290/L295/L347/L386-392/L578-579 | Multi-attach up to 5 with removable chips; the send path splits video links from MMS media because a carrier payload cap silently stripped multi-media sends. |
| `93b00023` | Roland | 07-24 | New Feature | `textly-webhook` v1.4 L487/L514-523/L617; `send-unreplied-reminder`; `unreplied_eod_reminder_s158.sql` | All three parts ship: inbound reopen with a `reopened` event row, owner notify only on the needs-reply flip, and the 4:30pm unread sweep. v1.4 disabled only the UNASSIGNED blast, which is not this ask. |
| `d94a418a` | Brandon | 07-27 | General UI/UX | `js/render.js` L33-39 formatNotesNewestFirst, comment L28-32 | **DONE in v1.497 (S178).** The splitter now reverses ENTRIES, not lines - split on the editField `---` separator or a newline before a `[MM/DD/YY` timestamp. Applied to both the RO Status box and Customer Comm, so a multi-line note reads in typed order. Previously bug. |
| `50175fce` | Lynn | 07-27 | General UI/UX | `js/ro-crud.js` L404/L1551; `index.html` L3968 | **DONE in v1.498 (S178).** `editUrgentUpdate(index)` wired to the `edit-urgent` card action; the red URGENT banner is clickable to edit or clear, writes target the RO UUID, are audit-logged, and still fire `urgent_update_notify`. Follow-on to `a7d1474e`. Previously needed/S. |
| `09084bc5` | Lynn | 08-04 | General UI/UX | `js/ro-crud.js` L662-684; `cashiered_full_detail_s171.sql` L32-38/L95-108/L114-141/L144; `closed-ros.html` L898-904 | **FIXED (S171, v1.494); bucket moved bug -> done.** `archiveROInSupabase` is a single manager-gated `archive_single_ro` RPC; the migration adds the 7 dropped columns, `archive_one_ro` writes them plus all seven child mirrors before the delete, and closed-ros.html v1.3b restores them on reactivate. This was the LARGE permanent-data-loss defect. |
| `225a2535` | Brandon | 08-05 | Bug Report | `js/insurance.js` L127-129/L47-49; `index.html` L6633/L6810; `js/ro-crud.js` L1376-1378 | **DONE in v1.497 (S178)** - the cross-customer bleed is closed. `window._lastEstimateScan` is stamped with `roKey = editingROSupabaseId` and every consumer gates on it; stale chips are cleared at Edit open. Previously bug (data-integrity). |
| `ac8265c8` | Lynn | 08-05 | General UI/UX | `add_mileage_s178.sql`; `index.html` L7670/L6743/L6766; `js/ro-crud.js` L567/L568/L1294; `js/render.js` L523-526 | **DONE in v1.498 (S178).** `repair_orders.mileage` plus the cashiered twin column, `archive_one_ro` recreated so cash-out carries it, Edit RO input next to VIN, audit label, and a card readout under the VIN. Previously needed/S. |
| `5ca724ff` | Lynn | 08-06 | General UI/UX | `js/work-list.js` L418-425 | **DONE in v1.497 (S178).** `dragstart` now calls `e.dataTransfer.setData('text/plain', ...)` and sets `effectAllowed = 'move'`, which Firefox and some Chrome paths require. The touch path is untouched, so mobile reorder is unchanged. Previously bug. |
| `fa35e3df` | Lynn | 08-18 | General UI/UX | `css/dashboard.css` L560/L609/L1210/L3039/L1930; `js/render.js` L26 | **DONE in v1.498 (S178).** The low-contrast blues were lightened for the dark card surface (`#3b82f6` -> `#7eb3ff`, `#667eea` -> `#a5b4fc`, key-date Drop chip `#0a84ff` -> `#5cadff`). All five sites carry the `[ER fa35e3df v1.498 S178]` marker. Previously needed/S. |

## Not triaged this run (1)

| ER | By | Date | DB status | Note |
|---|---|---|---|---|
| `f73a1e5a` | Roland | 04-13 | declined | "Testing dictation 123 testing enhancement request dictation" - not present in `needs_triage_ids` and carries no prior `triage_bucket`. Reads as a test entry, already declined. Worth including in a future full pass only to give it a recorded verdict. |

---

## What changed in the codebase since the last stored verdicts

Carried verdicts were stamped against HEADs between 0223cb3 (v1.492) and f501151
(v1.500). Current HEAD is 9c50b52 (v1.501). Two S183 changes bear on this board:

- **v1.501 added a 16th RO status, `Closed - No Charge`** (`js/config.js`
  STATUS_PROGRESS_MAP L174, slot 100; Spanish label L289; migration
  `closed_no_charge_s183.sql`). Its own code comment names totaled-out insurance
  claims and warranty closes as the use case. That overlaps three carried
  verdicts written before it existed - `3e746c18` (TOTALED OUT), `972af03c`
  (CANCELED) and `611589ba` (Closed + Canceled). **None of the three were in
  `needs_triage_ids`, so their verdicts are carried unchanged and none of them
  should be read as re-verified.** Worth a full pass, or at least asking Lynn
  whether `Closed - No Charge` already covers what she meant by "Closed".
- **`send-manager-report` v2.5-S183** added the lot-wide UNWATCHED ROs section.
  That is the tail end of `5925fc01`'s digest half; the notify-on-every-update
  and to-do-scraping halves of that ER are untouched and still open.
- Line anchors in carried verdicts that cite `js/config.js` L151-164 or
  `js/render.js` ALL_STATUSES are now stale by the S183 additions
  (STATUS_PROGRESS_MAP runs L151-175). The verdicts themselves still hold.

## Code checks run this session

Only the two new ERs were mapped, so only the parts path was walked:

- `js/parts.js` `markPartReceived` L449-478 - sets `part.status` only; **no**
  `dateReceived` assignment (confirms `2af47890`)
- `js/parts.js` `updatePartInSupabase` L539 - sends `date_received: part.dateReceived || null`
- `index.html` L3556 - `updatePartInSheet` is a straight alias for `updatePartInSupabase`
- `supabase/migrations/stamp_date_received_on_parts.sql` L33-36 - stamps only on
  the transition INTO `Received`; explicitly refuses to re-stamp an already-Received
  row (design note L15-20)
- `js/parts.js` `showAddPartForm` L302-312 - the edit form populates from the
  in-memory `part`, so a stale blank `dateReceived` round-trips back as null
- `js/parts.js` table headers L259-267 and `renderPartsRows` L121-145 - nine
  columns, no receipt date printed anywhere (confirms `1e434aff`)
- `js/parts.js` `openPartsStatusModal` L766-823 - the parts-status picker shows
  only the RO-level chip, no per-part dates either

---

## Recommended action today

1. **Ship the two new parts ERs as ONE small build.** `2af47890` (stamp
   `part.dateReceived` client-side in `markPartReceived`) and `1e434aff` (print
   the receipt date in the Manage Parts table) are the write half and the read
   half of the same ask from the same person on the same day. Doing only the
   display half would render blanks for every part received via the quick button
   in the current session; doing only the stamp half leaves Brandon still unable
   to see it. Both are contained in `js/parts.js` - no migration needed, the
   S135 trigger already covers the persistence.
2. **While in `markPartReceived`, close the wipe-on-edit hole.** Today, marking a
   part received and then editing that same part in the form silently nulls
   `date_received`, because the trigger deliberately will not re-stamp an
   already-Received row. Stamping locally fixes the display AND the wipe with the
   same line. This is the only defect-shaped finding this run.
3. **Decide `611589ba`.** It is the last `unreviewed` duplicate on the board and
   folds cleanly into `972af03c` + `44419de9`. But v1.501 shipped
   `Closed - No Charge` after that verdict was written, so ask Lynn whether that
   covers her "Closed" before declining - then flip it to `declined`.
4. **Consider a full pass next run.** `full_pass` was false and 103 of 105 rows
   were carried on stored verdicts written up to 18 days and two releases ago
   (v1.500, v1.501). The status-family ERs (`3e746c18`, `972af03c`, `44419de9`)
   are the ones most likely to have moved under them.
5. **Then the batches that were already queued:** the RO-status batch
   (`44419de9` + `972af03c` + `3e746c18`, one migration, both DB CHECK twins per
   the S172 lesson) and the messaging-polish batch (`f1e6e311`, `21fc3a9d`,
   `9aab7550`, `6e9ca4dc`, `ca0f7ed9` - all five confined to `messages.html`
   plus `js/messaging.js`).
6. **Still blocked on submitter detail:** `8d885331` (Lynn's parts-field layout),
   `47428f75` (Lynn's time-off clarification), `a03677e6` (confirm the note-stream
   retarget with Roland, who reversed the same change for lead notes in v1.491).
