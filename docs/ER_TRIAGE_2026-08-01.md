# PRVS Enhancement-Request Triage - 2026-08-01 (headless runner)

Incremental pass over `enhancement_requests` against the current modular
codebase (index.html **v1.487**, checkin.html v1.38, customer-checkin.html
**v1.17**, messages.html v1.17, time-off.html v1.5). Input snapshot
`er_input.json` fetched 2026-08-01T09:08:43Z: 88 rows, **87 re-triaged this
pass**, 1 carried unchanged. No DB writes, no code changes, no git operations -
this runner only classifies. Every Done and Bug verdict was re-checked by
grepping/reading the live code at v1.487, not assumed.

Baseline for the previous pass was v1.485. Only three files changed since
(index.html and js/auth.js for the v1.486/v1.487 One Tap work, and
customer-checkin.html v1.17), so **line-number citations in index.html shifted
by about +23 and in customer-checkin.html by about +60 to +65**. All affected
anchors were re-located and are re-cited below at their v1.487 positions.

## Board snapshot (DB `status`, all 88 rows)

| Status | Count |
|---|---|
| done | 49 |
| unreviewed | 30 |
| declined | 9 |
| **Total** | **88** |

## Triage buckets (87 classified this pass)

| Bucket | Count |
|---|---|
| done | 51 |
| needed | 23 |
| duplicate | 9 |
| bug | 2 |
| data | 2 |
| **Total** | **87** |

Open work item count: **25** (23 Needed + 2 Bug). Needed LOE mix:
**2 XL / 4 L / 2 M / 15 S**.

## What moved this pass

1. **`1c7f1410` flipped Done -> Needed/S.** Andrew Page asked to attach "photos
   or videos". The photo/PDF half shipped (S151b, S158) and prior passes called
   the whole ER done. Re-check at v1.487: `js/messaging.js` `input#msgAttach`
   accepts only `image/*,application/pdf` (L219), `_handleAttachPick` branches
   only on `isPdf` and otherwise runs `_compressImage` (L313-314), and a grep
   for `video` across `js/messaging.js` and `messages.html` returns **zero
   hits**. A video can be neither picked nor sent, so the ER is not fully
   delivered. Flagged per the "prefer needed over done" rail - a false Done
   hides real work.
2. **`298e0e67` / `5c45bb8d` / `fc6b29f9` strengthened.** customer-checkin.html
   v1.17 (shipped since the last pass) adds a blocking attach-vs-new dialog for
   the explicit-selection case where the matched RO is already active
   (L2129-2194, dialog L2665-2685) - the Tom Rather duplicate path. Still Done,
   now with a wider guard.
3. **`ed1b873e` re-anchored.** js/auth.js changed substantially in v1.485-v1.487,
   so the Google entry points moved: `initialize` L978, `prompt` L568, the new
   v1.486 `renderButton` fallback L541, `signInWithIdToken` L999 and L1176.
   Auth is still entirely Google-based; verdict unchanged (Needed/XL).
4. Everything else re-verified with the same verdict as the prior pass.

## Code checks run this pass

All of these returned **zero hits**, confirming the corresponding ERs are still open:

- `inventory` / `on_hand` / `in_stock` / `stock_qty` across js + supabase - confirms `14f35f95`
- word-boundary `rma` / `return_reason` / `supplier_credit` - confirms `b19975d4`
- `ro_tasks` / `task_tracker` / `customer_task` - confirms `af14b3cb`, `5925fc01`
- `signing_token` / `remote_sign` / `signature_request` / `signing_link` - confirms `0448de62`
- `backToTop` / `back-to-top` / `scroll-to-top` (only `window.scrollTo` in the repo:
  closed-ros.html L765, customer-checkin.html L1289) - confirms `29654af7`, `2b78696c`
- `favicon` / `rel=icon` / `apple-touch-icon` across every repo *.html - confirms `d432863a`
- case-insensitive `cancel` in js/config.js - confirms `972af03c`
- `LABOR_RATE` / `laborRate` / `labor_rate` / `195` in js/work-orders.js - confirms `2f752d40`
- `font-scale` / `fontScale` / `textSize` - confirms `21fc3a9d`
- `archived` / `abandoned` in js/render.js + js/config.js - confirms `5fbfa299`
- `sortBy` / `sortSelect` / `sortMode` (only closed-ros.html L321 and solar.html) - confirms `d1844e46`
- `clocked in` / `clocked into` / `activeTimeLog` - confirms `6bb61db2`
- `video` in js/messaging.js + messages.html - reclassifies `1c7f1410`

Positive checks: `js/render.js` L553 and L563 still `split('\n').map(escapeHtml).reverse()`
(bug `d94a418a` stands); `index.html` `scrollToROInBoard` now L4162 with the
toast at L4177, and `js/qr.js` `handleDeepLink` L198 still returns silently at
L203/L209 (bug `d4d0eeba` stands); `erCategory` still present in only 6 live
HTML files (residual noted on `ac394468`); `supabase/migrations/staff_table.sql`
L85-87 still seeds "Wimbles" (residual noted on `6938c1af`).

---

## Bugs - front of the line (2)

| ER | By | Date | Category | Severity | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `d94a418a` | Brandon | 07-27 | General UI/UX | **data-display** (render only, no data loss) | `js/render.js` L553 (RO Status), L563 (Customer Comm); upstream join in `js/ro-crud.js` | CONFIRMED on v1.487. Both note boxes render `split('\n') + map(escapeHtml) + reverse()`, which reverses EVERY line, so a multi-line note shows bottom-line-first - exactly the reported "latter portion near the top". Entry boundaries are already lost upstream because entries are joined with `\n`. The S159 history modal splits on the entry regex correctly; reuse that split for both boxes. |
| `d4d0eeba` | Brandon | 07-22 | General UI/UX | **small** (navigation, no data risk) | `index.html` `scrollToROInBoard` L4162, toast L4177; `js/work-list.js` L328; `js/qr.js` `handleDeepLink` L198, L203, L209 | CONFIRMED on v1.487, two paths. Work list: `scrollToROInBoard` only matches already-rendered `[data-ro-index]` cards, else toasts (its Clear Filters action re-renders but never then scrolls). Messages deep link: `handleDeepLink` returns silently when the RO is not in `currentFilteredData` or has no card. Canonical for duplicates `9031e101` and `1bc33dd6`; one fix (clear filters, re-render, scroll, highlight) closes all three. |

## Needed - by theme (23)

### Notifications & Task-Tracking

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `5925fc01` | Lynn | 04-09 | **L** | `supabase/functions/send-manager-report`; `js/work-orders.js` L293; `js/ro-crud.js` L370 | PARTIAL. The daily in-progress digest half ships, and single-event notifies exist (`service_added_notify`, `urgent_update_notify`). Still absent: notify-on-every-RO-update and to-do scraping from status notes. Anchor of this theme; pairs with `af14b3cb`. |
| `af14b3cb` | Lynn | 05-21 | **L** | `js/shop-tasks.js` (no ro_id link, no reminder column) | Still absent. `js/shop-tasks.js` is a non-customer shop checklist with no customer link. Needs a per-RO tasks table, an add/complete UI and a daily open-task reminder. Pairs with `5925fc01`. |

### Parts & Inventory

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `14f35f95` | Lynn | 04-09 | **XL** | `js/parts.js` (per-RO line items only) | Still absent. New subsystem: inventory table, receive/consume/adjust flows, a searchable priced stock view, and links from the parts-request path. Largest item on the board. |
| `b19975d4` | Lynn | 04-09 | **L** | `js/config.js` `PART_STATUSES` L200; `js/parts.js` `pf_returnDeadline` L226, read L343 | Partial infra only: a "Returned" status and a Return Deadline input exist, but no RMA number, return reason, supplier credit or reverse-direction flow. Equals GH#24 Shop Ops Phase 2. Canonical for declined duplicate `86bbaf50`. |
| `8d885331` | Lynn | 04-09 | **S** | `js/parts.js` L181, L190, L200, L204, L208, L226 | Form is unchanged; the ask is only to reorder those fields. Quick once Lynn supplies the target layout or a mockup. **Blocked on detail, not on effort.** |

### Messaging & e-sign

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `0448de62` | Sofia | 06-11 | **L** | `customer-checkin.html` signature pad; `supabase/functions/send-quote-email` L386 | Still kiosk-only signing; the raf_copy branch only emails a copy after signing. Needs a tokenized public signing page, a delivery path and signed-form storage. Pairs naturally with the SMS channel. |
| `1c7f1410` | Andrew | 07-16 | **S** | `js/messaging.js` L219 accept list, `_handleAttachPick` L313-314 | **REVISED from Done.** Photos/PDFs attach; video does not (accept excludes `video/*`, the pick handler only branches on PDF vs image-compress, zero `video` references). Small: add `video/*` accept, skip compression, keep the 5MB MMS cap. |
| `9aab7550` | Brandon | 07-21 | **S** | `messages.html` `.unread-dot` CSS L334, markup L1095, `unresponded` L1089 | Partial: a small gold dot renders, but only when `last_direction='inbound'` AND `status='open'`, and there is no row-level highlight. Small: stronger unread treatment plus a widened condition so a closed-then-reopened thread also flags. |
| `21fc3a9d` | Brandon | 07-21 | **S** | `messages.html` (fixed rem sizes, no scale control) | Still absent. Small: add a persisted font-scale control for the conversation list. |
| `f1e6e311` | Brandon | 07-21 | **S** | `messages.html` `convoSearch` L469, resets L1001/L1020, filter L1059, handler L2144 | Still absent - the search box is a plain input with no clear affordance. Small: add a clear (x) button that empties the search and re-runs the filter. |
| `6e9ca4dc` | Brandon | 07-21 | **S** | `js/messaging.js` `refreshSignaturePreview` L241, element L222, text L246, show L247; `messages.html` hide rule L430 inside the `max-width:760px` query L417 | Valid, still present on desktop: the "Added to every send:" preview is hidden only in the mobile media query, so desktop messages.html and the RO-card composer still show it. Small: remove it or put it behind a toggle in all viewports; the signature itself keeps sending. |

### RO fields & status

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `0e172b2c` | Sofia | 07-16 | **M** | `js/duplicates.js` `getBaseROId` L35, regex L36, `findDuplicateGroups` L40, grouping L44-46, `executeDupeMerge` L116 | Partial: an RO-level merge/delete engine exists but groups strictly on the `PRVS-XXXX-XXXX` base id, so two ROs under different spellings of one customer are never detected. Needs name/phone account dedup plus a verify-which-is-correct merge step. One-off cleanup for this case is `3f254929`. |
| `5fbfa299` | Brandon | 06-30 | **M** | `js/ro-crud.js` `archiveROInSupabase` L658, `archived_at` L692 | Still absent: the only archive path is the cashiered one; no soft-archive flag, status or board filter for quoted-but-never-progressed units. Needs a flag, a board filter and an un-archive path. Note the S161 lead-capture path keeps FUTURE quotes on the Messages board, but existing quote-only ROs still need this. Pairs with `972af03c`. |
| `972af03c` | Lynn | 07-17 | **S** | `js/config.js` `STATUS_PROGRESS_MAP` L151-163; `js/render.js` `ALL_STATUSES` L741-745, `statusColorMap` L727-739 | Still absent - there is no Canceled status anywhere; the maps end at Delivered/Cashed Out. Small: add a Canceled status plus color/priority maps, exclude it from the on-lot and queue counts, and keep the record for notes. Pairs with `5fbfa299`. |
| `50175fce` | Lynn | 07-27 | **S** | `js/ro-crud.js` L414 create, L491 edit, repopulate L1107, notify L370; `js/render.js` banner L324 | Valid and confirmed absent: `urgent_update` is writable only from the New/Edit RO forms and the card banner is read-only (its title even says "change it via Edit RO"). Small: add an inline edit-in-place control at the tile bottom reusing the existing editField / notify path so it still fires `urgent_update_notify`. Follow-on to `a7d1474e`. |

### Board & Navigation UX

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `29654af7` | Lynn | 07-06 | **S** | none (absent); nearest `window.scrollTo` are closed-ros.html L765, customer-checkin.html L1289 | Still absent. Small: add a scroll-triggered back-to-top / jump-to-search control, ideally present in the sidebar layout too. Canonical for duplicate `2b78696c`. |
| `d1844e46` | Lynn | 07-16 | **S** | `js/render.js` `renderBoard` sort L31-39, shop pin L35-37 | Still absent: the board sorts on the single blended `calculatePriority` score and has no sort selector at all. Small: add the strict promised-date-then-oldest-on-lot option while keeping the shop pin. Related to `1aeb3f58`, which delivered only the promised-date chip. |
| `d432863a` | Brandon | 07-21 | **S** | none (absent in every repo *.html head) | Still absent - every tab shows the generic browser icon. Small: add a Patriots-logo favicon plus apple-touch-icon to each page head (`logo.png` ships already but needs resizing to icon dimensions). |

### Time clock

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `e06d747e` | Riley | 04-10 | **S** | `js/render.js` `compact-col-actions` L228-235 | Still absent: no floating/sticky check-in control on index.html or checkin.html, and the compact action row has edit / parts / WO / notify / message / schedule only. **Underspecified** - scope the target page and exact behavior with the submitter. Pairs with `6bb61db2`. |
| `6bb61db2` | Riley | 07-22 | **S** | `js/render.js` `compact-col-actions` L228-235; no active-time_log indicator anywhere | Still absent, both halves. Small: add a compact check-in/out action plus a login-time active-`time_log` banner shown across views. Pairs with `e06d747e`. |

### Work Orders

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `2f752d40` | Jason | 07-21 | **S** | `js/work-orders.js` `woFormDollar` L360, `woFormEstHours` L365, save L649-650, `dollarLocked` gate L324 | Still absent: the dollar and hours inputs are independent manual fields with no labor-rate arithmetic (index.html `laborRate` is the per-claim INSURANCE rate, not the posted shop rate). Small: auto-populate `dollar_value` = effective hours x $195/hr, respecting the `dollarLocked` readonly gate. |

### Check-in & forms

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `5fec3eca` | Sofia | 07-15 | **S** | `supabase/functions/send-quote-email` raf_copy L386, destructure L387; lead_staff_notify L549, HTML L674, text L748 | Still absent: the raf_copy payload has no `customerAddress`; the address appears only in the lead_staff_notify branch. Small: add `customerAddress` to the raf_copy payload and both templates. |

### Auth & cost

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | **XL** | `js/auth.js` GOOGLE_CONFIG import L69, gapi init L202-204, `initialize` L978, `prompt` L568, v1.486 `renderButton` fallback L541, `signInWithIdToken` L999/L1176 | Still entirely Google-based; the check-in kiosk uses the same path. Swapping to Supabase password / magic-link or shared role accounts would drop roughly $500/mo but touches every page sign-in plus the email-keyed RBAC. Roland wants to scope this together. |

## Data bucket - SQL for Roland (nothing was executed by this runner)

**`6938c1af`** (Ryan, 05-06) - Wimbles -> Wombles. Already corrected in the live
DB; tech names read from `staff.name` and submitter "Rod Wombles" now spells
correctly in this very table. RESIDUAL (not a runtime bug):
`supabase/migrations/staff_table.sql` L85-87 still seeds Rod/Zak/Travis as
"Wimbles", so a re-seed would reintroduce it. Re-apply only if ever needed:

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

**`3f254929`** (Sofia, 07-16) - keep Steve Huebert, remove the misspelled
Hubert RO. Eyeball the rows first, then soft-delete the confirmed wrong id:

```sql
SELECT id, ro_id, customer_name, status
FROM repair_orders
WHERE customer_name ILIKE '%ubert%';

-- once the wrong id is confirmed:
UPDATE repair_orders SET deleted_at = now() WHERE id = '<wrong-id>';
```

The merge feature itself is tracked as `0e172b2c` (Needed/M).

## Duplicates - fold into the canonical ER (9)

| ER | By | Date | Fold into | Note |
|---|---|---|---|---|
| `aa508852` | Riley | 04-09 | `fd6c122d` (Done) | Identical text and timestamp. Already declined. |
| `86bbaf50` | Lynn | 05-13 | `b19975d4` (Needed/L) | Gordon window returned to Vogt = supplier return / warranty tracking. Already declined. |
| `67daba15` | Brandon | 06-09 | `d2561e11` (Done) | RO completion / pick-up date. Already declined. |
| `fa1c1d6e` | Sofia | 06-09 | `a22739b9` (Done) | Sheldon Gilbert check-in email. Already declined. |
| `5057c7fc` | Roland | 06-09 | `91a03260` (Done) | Same text, one second apart. Already declined. |
| `fc6b29f9` | Sofia | 06-17 | `298e0e67` (Done) | Retest report of the duplicate-RO guard. Already declined. |
| `9031e101` | Riley | 07-21 | `d4d0eeba` (Bug) | iPhone work-list click does not reach the unit - same root cause. **Still unreviewed; recommend declined.** |
| `1bc33dd6` | Brandon | 07-27 | `d4d0eeba` (Bug) | Same defect Brandon already filed 07-22. **Still unreviewed; recommend declined.** |
| `2b78696c` | Lynn | 07-27 | `29654af7` (Needed/S) | Same back-to-top ask Lynn filed 07-06. **Still unreviewed; recommend declined.** |

---

## Full per-ER table (all 88 rows)

`Bucket` is this pass's verdict. `-` in LOE means not applicable (only Needed
ERs carry an LOE). Verdicts are condensed here; the full text for the 87
re-triaged rows is in `er_verdicts.json`.

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict (condensed) |
|---|---|---|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | General UI/UX | done | - | - | Test entry ("Testing out the Make-A-Wish function"). Not a real request; already declined, keep declined. |
| `79b2c805` | Sofia | 04-09 | New Feature Idea | done | - | closed-ros.html `reactivateRO` L838 (buttons L725/L813); js/photos.js `uploadDocument` L253, input L207 | Cashed-out ROs reactivate from `cashiered_child_archives` and documents attach to any RO. |
| `75124b03` | Ryan | 04-09 | RO Management | done | - | js/ro-crud.js L1119; js/render.js L122, L196, L373 | v1.402: Warranty is a first-class RO type on new-RO creation, with board filter, chip and badge. |
| `5925fc01` | Lynn | 04-09 | New Feature Idea | needed | L | send-manager-report; js/work-orders.js L293; js/ro-crud.js L370 | PARTIAL: digest + single-event notifies ship; notify-on-every-update and note-scraped to-dos do not. |
| `b2aa4bea` | Lynn | 04-09 | New Feature Idea | done | - | js/ro-crud.js L409/L486/L508; js/render.js L395; key_dates_reminder_sources.sql | `planned_dropoff_date` persists and drives the Drop chip plus the day-before reminder. Same field as `468aa376`. |
| `18611eee` | Lynn | 04-09 | Bug Report | done | - | js/work-list.js L195, L210/L383, L223/L263, L304, L356 | Was real; fixed. Members are viewable, reorderable (mouse + touch) and removable. |
| `14f35f95` | Lynn | 04-09 | Parts & Ordering | needed | XL | js/parts.js (per-RO line items only) | No inventory/stock anywhere. New subsystem; largest item on the board. |
| `b19975d4` | Lynn | 04-09 | Parts & Ordering | needed | L | js/config.js L200; js/parts.js L226/L343 | Partial infra only ("Returned" status + return deadline); no RMA, reason, credit or reverse flow. = GH#24 Phase 2. |
| `17492066` | Lynn | 04-09 | Notifications / Email | done | - | scheduled_notifications.sql; process-scheduled-notifications; index.html L2944/L3012 | Schedule Notification bell sets future date/time, message, recipients, with repeat-until. |
| `8d885331` | Lynn | 04-09 | Parts & Ordering | needed | S | js/parts.js L181-L226 | Field reorder only. Blocked on detail, not effort. |
| `fd6c122d` | Riley | 04-09 | Work List | done | - | js/work-list.js L195, L304, L356/L404, L210/L383 | v1.460: delete + drag-reorder on mouse and touch, mobile sizing fixed. Canonical for `aa508852`. |
| `aa508852` | Riley | 04-09 | Work List | duplicate | - | see `fd6c122d` | Identical text and timestamp to `fd6c122d`. Already declined. |
| `98bd7498` | Brandon | 04-09 | RO Management | done | - | js/work-list.js L223, L263, L83 | Per-silo Work List tabs give a separate list per job category. |
| `e06d747e` | Riley | 04-10 | Time Clock / Check-In | needed | S | js/render.js L228-235 | No floating check-in control. Underspecified; scope with submitter. Pairs with `6bb61db2`. |
| `19dc2016` | Roland | 04-11 | Time Clock / Check-In | done | - | - | Test entry ("Testing ER form / dictation"). Already declined, keep declined. |
| `f73a1e5a` | Roland | 04-13 | General UI/UX | *(carried - not in this pass's scope)* | - | - | Test entry ("Testing dictation 123"); already declined in the DB. No triage bucket on record. |
| `1fe68261` | Rod | 04-13 | Work Orders | done | - | js/shop-tasks.js L21/L41/L153/L180; shop_tasks.sql | v1.467: shop tasks track internal work not tied to a customer unit. Matches the poly-barrel / roof-bay examples. |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | done | - | js/config.js L158; js/render.js L741-745, L733 | v1.410: "Ready to Work" is a live RO status in every map and dropdown. |
| `b87eb2fb` | Lynn | 04-14 | RO Management | done | - | keys_power_fields.sql; js/ro-crud.js L411-413/L488-490 | v1.458: `key_status`, `keypad_code`, `keep_plugged_in` persist with a keys chip on the card. Same build as `34fc03c2`. |
| `a5ff3e2d` | Lynn | 04-21 | RO Management | done | - | js/ro-crud.js L1119-1120; js/render.js L122, L196, L373 | v1.465: "Warranty + Repair" RO type end to end and still billable. |
| `a7d1474e` | Lynn | 04-23 | RO Management | done | - | urgent_update_field.sql; js/render.js L324, L214; js/ro-crud.js L370 | v1.466: always-visible red URGENT banner plus manager/admin notify. Follow-up to move the INPUT to the tile is `50175fce`. |
| `dde90d2b` | Roland | 04-27 | RO Management | done | - | customer-checkin.html raf_copy L2530, lead_staff_notify L2603 | Photo optional; both emails forced on every submission. |
| `13e1cd87` | Tipton | 04-30 | Time Clock / Check-In | done | - | checkin.html L1516, ES string L441 | v1.447 (checkin v1.34, now v1.38): "Return to Dashboard" renders after clock-out. |
| `e27311bf` | Ryan | 05-04 | Work Orders | done | - | js/work-orders.js `_initWOTaskDrag` L461, invoked L385/L583 | v1.464: WO task rows drag-to-reorder; DOM order drives `sort_order`, so templates edit by load-reorder-overwrite. |
| `d2561e11` | Brandon | 05-06 | New Feature Idea | done | - | key_dates_pickup_date.sql; js/ro-crud.js L410/L487; js/render.js L397, L365 | v1.457: `pickup_date` plus the green Pickup chip and key-dates panel. Canonical for `67daba15`. |
| `6938c1af` | Ryan | 05-06 | General UI/UX | data | - | staff_table.sql L85-87 (seed residual) | Already corrected in the live DB. Seed file would reintroduce "Wimbles"; SQL above if ever needed. |
| `dac9fdda` | Brandon | 05-06 | General UI/UX | done | - | js/config.js L154; js/render.js L730, L718-721 | v1.460: "Off Lot - Returning" separates temporarily-taken units from never-on-lot, and is excluded from the on-lot count. |
| `86bbaf50` | Lynn | 05-13 | RO Management | duplicate | - | see `b19975d4` | Gordon window returned to Vogt = supplier return / warranty tracking. Already declined. |
| `a8e90a7d` | Brandon | 05-20 | General UI/UX | done | - | js/parts.js L152-155, L159-161 | v1.456: Manage Parts Ticket Totals sum parts, freight and total cost. Same build as `671fa623`. |
| `1aeb3f58` | Lynn | 05-21 | General UI/UX | done | - | js/render.js L396, L33 | v1.457: large bold promised chip (red when overdue) and promise date + days-on-lot feed the blended sort. Strict sort is `d1844e46`. |
| `af14b3cb` | Lynn | 05-21 | General UI/UX | needed | L | js/shop-tasks.js (no customer link) | No per-customer task tracker or daily open-task reminder. Pairs with `5925fc01`. |
| `cbc70a86` | Lynn | 05-21 | General UI/UX | done | - | customer-checkin.html L2381; work_list_autoadd_rpc.sql | S128: check-in calls `add_ro_to_silo_work_lists` (SECURITY DEFINER), inserting rows per involved silo lead with an sr-manager fallback. |
| `3f415c88` | Lynn | 05-26 | General UI/UX | done | - | index.html snRepeat L2944, read L3012 | v1.463: Repeat none/daily/weekly plus repeat-until, expanded into individually cancellable rows. |
| `cd663d8f` | Bobby | 05-28 | General UI/UX | done | - | js/ro-crud.js L1093; js/parts.js L307/L310 | Cosmetic bug, fixed v1.447: currency inputs show 2 decimals. No data loss occurred. |
| `34fc03c2` | Brandon | 05-29 | New Feature Idea | done | - | keys_power_fields.sql; js/ro-crud.js L411-413/L488-490 | v1.458: Keys selector on New and Edit RO records whether keys are left with the unit. Covers `b87eb2fb`. |
| `0b2e128c` | Lynn | 06-02 | General UI/UX | done | - | checkin.html L1455, header note L5 | v1.460 (checkin v1.38): clock-out work notes are copied into the RO timeline as a `ro_status` note. |
| `468aa376` | Lynn | 06-02 | General UI/UX | done | - | js/ro-crud.js L409/L486; js/render.js L395 | Planned drop-off date input on New RO and (restored v1.457) Edit RO. Same field as `b2aa4bea`. |
| `2b814250` | Roland | 06-07 | Calendar | done | - | js/render.js L395-398, L367; sync-ro-calendar; key_dates_cal_event_ids.sql | Colored key-date chips + key-dates panel, server-side calendar sync (idempotent), advance reminders to managers/admins. |
| `ed1b873e` | Roland | 06-07 | General UI/UX | needed | XL | js/auth.js L69, L202-204, L978, L568, L541, L999/L1176 | Still entirely Google-based. Roughly $500/mo saving but touches every sign-in plus email-keyed RBAC. Scope with Roland. |
| `cd084233` | Roland | 06-07 | Work List | done | - | send-manager-report; send-admin-pnl-report; cron_admin_pnl_report.sql; worklist-report.html | Admin-level daily manager-worklist reporting ships. The manager scoring layer is a future extension, not this ER. |
| `a22739b9` | Sofia | 06-08 | General UI/UX | done | - | customer-checkin.html L2530, L2603 | Notify-gate regression; both sends forced on every submission. Canonical for `fa1c1d6e`. |
| `67daba15` | Brandon | 06-09 | General UI/UX | duplicate | - | see `d2561e11` | RO completion / pick-up date for records. Already declined. |
| `fa1c1d6e` | Sofia | 06-09 | General UI/UX | duplicate | - | see `a22739b9` | Sheldon Gilbert returning check-in sent no email. Already declined. |
| `91a03260` | Roland | 06-09 | Work List | done | - | prune_cashiered_worklists.sql L23-25 | Removes an RO from manager work lists the day AFTER Delivered / Cashed Out (Chicago-local date gate). Canonical for `5057c7fc`. |
| `5057c7fc` | Roland | 06-09 | Work List | duplicate | - | see `91a03260` | Same text, one second apart. Already declined. |
| `cdd77a8b` | Brandon | 06-10 | General UI/UX | done | - | js/render.js L656-658, L718-721 | v1.453/v1.454: `updateStats` splits the counts so the tile shows a true on-lot number against total customers. |
| `0448de62` | Sofia | 06-11 | General UI/UX | needed | L | customer-checkin.html signature pad; send-quote-email L386 | Signing is kiosk-only. Needs a tokenized public signing page, delivery path and signed-form storage. |
| `2c3d5633` | Ryan | 06-12 | General UI/UX | done | - | js/photos.js L66 | Mobile-only bug, fixed v1.453: the upload input is appended to the DOM before clicking (iOS Safari dropped-change-event root cause). |
| `74a33621` | Brandon | 06-16 | General UI/UX | done | - | js/photos.js L396-397, L404, L380/L382 | v1.454: lightbox arrow-key navigation plus Escape-to-close and prev/next buttons. |
| `298e0e67` | Sofia | 06-16 | General UI/UX | done | - | customer-checkin.html L2059/L2165; **v1.17 attach-vs-new L2129-2194, dialog L2665-2685** | Data-integrity bug, fixed and widened this cycle: the guard reuses a scheduled RO, and v1.17 adds a blocking attach-vs-new dialog for already-active ROs. Canonical for `fc6b29f9`. |
| `9b823d25` | Roland | 06-16 | RO Management | done | - | js/work-orders.js `addServiceToRO` L244, notify L293; service_added_notify_source.sql | v1.460: adding a service from one silo emails the owning silo manager. Matches the roof-plus-water-heater example. |
| `fc6b29f9` | Sofia | 06-17 | General UI/UX | duplicate | - | see `298e0e67` | Retest report of the duplicate-RO guard. Already declined. |
| `9b046808` | Brandon | 06-17 | General UI/UX | done | - | js/config.js L157; js/render.js L718-721 | v1.454: "Scheduled" is its own status and not-yet-arrived units are excluded from days-on-lot and the on-lot count. |
| `671fa623` | Brandon | 06-18 | General UI/UX | done | - | js/parts.js L153, L155, L159-161 | v1.456: Freight is summed across parts and rolled into Total Cost. Same build as `a8e90a7d`. |
| `5c45bb8d` | Sofia | 06-19 | General UI/UX | done | - | customer-checkin.html L2059/L2165, v1.17 L2129-2194; raf_copy L2530 | David Flanagan field report; both halves (duplicate RO + missing email) resolved. |
| `b06a285f` | Lynn | 06-19 | General UI/UX | done | - | js/render.js L367, rows L363-365, chips L395-398 | v1.465: a calendar icon beside the customer name opens a hover/tap key-dates panel. |
| `ee50d11c` | Lynn | 06-19 | General UI/UX | done | - | time-off.html Edit button L752, add-mode reset L835 | Was a real gap; time-off entries are now editable (day, person, absence type) via `editRequest()` and "Update Record". |
| `88395acc` | Lynn | 06-19 | General UI/UX | done | - | add_absence_window_to_time_off.sql; add_partial_hours_to_time_off.sql; time-off.html L368/L372, read L818-819 | Time Off v1.5: partial-day absence is an exact clock-out-to-return window, not an hour total. |
| `80390f36` | Lynn | 06-23 | General UI/UX | done | - | js/render.js L35-37, search token L61 | v1.462: Shop Operations tiles pin to the top and are findable by search regardless of customer name. |
| `7f2d0d70` | Lynn | 06-26 | General UI/UX | done | - | send-dropoff-report L137-141, L176 | Monday drop-off report lists the week's scheduled drops plus last week's arrivals and flags any missing from the right team's work list. Pairs with `cbc70a86`. |
| `3f7fb4fc` | Brandon | 06-29 | General UI/UX | done | - | js/render.js L656-658, L716-721, L249 | Small data-display bug, fixed v1.468: shop ROs are out of both counts and the shop status dropdown is a static label. |
| `cb7742a8` | Lynn | 06-29 | General UI/UX | done | - | sync-ro-calendar; customer-checkin.html POST L2240, header note L22 | v1.469 (check-in v1.15): server-side sync posts drop-off / promised / pickup to the correct team calendar for every RO, front-desk check-ins included. |
| `5fbfa299` | Brandon | 06-30 | General UI/UX | needed | M | js/ro-crud.js L658, L692 | No soft-archive flag, status or board filter for quoted-but-never-progressed units. Pairs with `972af03c`. |
| `29654af7` | Lynn | 07-06 | General UI/UX | needed | S | none (absent) | No back-to-top / jump-to-search control on the board. Canonical for `2b78696c`. |
| `39b75fab` | Sofia | 07-09 | General UI/UX | done | - | js/photos.js `canManageFiles` L618, per-doc row L198, lightbox Move/Delete L376 | v1.472: rename / move-to-another-RO / delete, office-staff gated. The misfiled Marco DeAlba docs can be moved or removed. |
| `5fec3eca` | Sofia | 07-15 | General UI/UX | needed | S | send-quote-email L386-387 vs L549/L674/L748 | The raf_copy payload carries no `customerAddress`; only lead_staff_notify does. Small: add it to the payload and both templates. |
| `1c7f1410` | Andrew | 07-16 | New Feature Idea | needed | S | js/messaging.js L219, L313-314 | **REVISED from Done.** Photos/PDFs attach; video cannot be picked or sent. Small: add `video/*`, skip compression, keep the 5MB cap. |
| `d1844e46` | Lynn | 07-16 | General UI/UX | needed | S | js/render.js L31-39, pin L35-37 | No selectable strict promised-then-oldest sort; the board has one blended score and no sort control. |
| `0e172b2c` | Sofia | 07-16 | General UI/UX | needed | M | js/duplicates.js L35, L36, L40, L44-46, L116 | Merge engine groups only on the `PRVS-XXXX-XXXX` base id, so misspelled-name pairs are never detected. One-off cleanup is `3f254929`. |
| `3f254929` | Sofia | 07-16 | General UI/UX | data | - | none (data only) | Keep Steve Huebert, soft-delete the Hubert RO. SQL above; nothing executed. Merge feature is `0e172b2c`. |
| `972af03c` | Lynn | 07-17 | General UI/UX | needed | S | js/config.js L151-163; js/render.js L741-745, L727-739 | No Canceled status anywhere. Small: add it with color/priority maps, exclude from counts, keep the record for notes. |
| `d432863a` | Brandon | 07-21 | General UI/UX | needed | S | none (absent) | No favicon in any page head. Small: add a Patriots-logo favicon plus apple-touch-icon. |
| `9031e101` | Riley | 07-21 | Work List | duplicate | - | see `d4d0eeba` | iPhone work-list click does not reach the unit; same root cause. Recommend declined. |
| `9aab7550` | Brandon | 07-21 | General UI/UX | needed | S | messages.html L334, L1089, L1095 | Partial: a small dot only for inbound+open threads, no row highlight. Small: stronger treatment plus a widened condition. |
| `21fc3a9d` | Brandon | 07-21 | General UI/UX | needed | S | messages.html (fixed rem sizes) | No user text-size control. Small: a persisted font-scale control for the conversation list. |
| `2f752d40` | Jason | 07-21 | Work Orders | needed | S | js/work-orders.js L360, L365, L649-650, L324 | No labor-rate arithmetic. Small: auto-populate `dollar_value` = hours x $195/hr, respecting the `dollarLocked` gate. |
| `f1e6e311` | Brandon | 07-21 | General UI/UX | needed | S | messages.html L469, L1001/L1020, L1059, L2144 | The conversation search has no clear affordance. Small: add a clear (x) button that re-runs the filter. |
| `6e9ca4dc` | Brandon | 07-21 | General UI/UX | needed | S | js/messaging.js L222/L241/L246/L247; messages.html L430 inside L417 | "Added to every send" is hidden only in the mobile media query, so desktop and the RO-card composer still show it. |
| `6bb61db2` | Riley | 07-22 | Time Clock / Check-In | needed | S | js/render.js L228-235; no active-time_log indicator | No compact check-in/out button and no persistent clocked-in reminder. Pairs with `e06d747e`. |
| `d4d0eeba` | Brandon | 07-22 | General UI/UX | **bug** | - | index.html L4162/L4177; js/work-list.js L328; js/qr.js L198/L203/L209 | CONFIRMED, severity small. Both the work-list click and the Messages deep link fail when the RO is filtered off the board. Canonical for `9031e101` and `1bc33dd6`. |
| `9d3806e7` | Brandon | 07-22 | General UI/UX | done | - | messages.html L528/L530, L1360/L1377, read L1326; customer_note_entries_s154.sql | S154: customer-side Notes section plus an Add-Notes modal writing append-only rows. **Recommend status=done.** |
| `ac394468` | Roland | 07-24 | General UI/UX | done | - | js/enhancement.js pageDefaults L43-55 | v1.482: the erCategory select covers Messages / Texting and every page. RESIDUAL: erCategory exists in only 6 live HTML files, so messages / leads / time-off / guide cannot RAISE an ER - worth its own small ER. |
| `d62a26e4` | Roland | 07-24 | New Feature Idea | done | - | js/messaging.js L202, L207, L219, L258-262, send chunking L456-461 | v1.482: up to 5 attachments with removable chips, chunked one media per MMS (carrier payload cap). The multi-attachment ask is fully met. |
| `93b00023` | Roland | 07-24 | New Feature Idea | done | - | textly-webhook L503-505, header note L8; send-unreplied-reminder; unreplied_eod_reminder_s158.sql | v1.482: reopen-on-inbound, owner notify only on the needs-reply flip, and the weekday 4:30pm unread sweep. All three parts ship. |
| `1bc33dd6` | Brandon | 07-27 | Work List | duplicate | - | see `d4d0eeba` | Same defect Brandon filed 07-22. Recommend declined. |
| `d94a418a` | Brandon | 07-27 | General UI/UX | **bug** | - | js/render.js L553, L563; upstream join in js/ro-crud.js | CONFIRMED, severity data-display. `reverse()` on every line shows multi-line notes bottom-first. Reuse the S159 history-modal entry split. |
| `50175fce` | Lynn | 07-27 | General UI/UX | needed | S | js/ro-crud.js L414/L491/L1107/L370; js/render.js L324 | `urgent_update` is form-only and the card banner is read-only. Small: inline edit-in-place at the tile bottom that still fires the notify. |
| `2b78696c` | Lynn | 07-27 | General UI/UX | duplicate | - | see `29654af7` | Same back-to-top ask Lynn filed 07-06. Recommend declined. |

---

## Recommended action today

1. **Ship the bug class first.** Both confirmed bugs are in the auto-fixable
   class and are cheap:
   - `d94a418a` (data-display): stop reversing every line in `js/render.js`
     L553/L563 and split on the S159 entry regex instead. Self-contained,
     one module.
   - `d4d0eeba` (small, navigation): make `scrollToROInBoard`
     (index.html L4162) and `js/qr.js handleDeepLink` clear filters, re-render,
     then scroll and highlight. **One fix retires three ERs** - it also closes
     duplicates `9031e101` and `1bc33dd6`.
   Together these are a single `fix/er-bugs-v1.488` branch.
2. **Then sweep the S pile.** 15 of the 23 open enhancements are S, and several
   are near-trivial and independently shippable: `d432863a` (favicon),
   `f1e6e311` (clear-search button), `6e9ca4dc` (drop the desktop signature
   preview), `21fc3a9d` (font scale), `9aab7550` (unread highlight),
   `5fec3eca` (address on the raf_copy email), `972af03c` (Canceled status),
   `2f752d40` (dollar = hours x $195), `29654af7` (back-to-top),
   `d1844e46` (strict promised-date sort), `50175fce` (urgent update on the
   tile), `1c7f1410` (`video/*` accept). A messaging-only batch
   (`f1e6e311` + `6e9ca4dc` + `21fc3a9d` + `9aab7550` + `1c7f1410`) is the
   tightest single release.
3. **Unblock the one S that is stuck on detail:** `8d885331` needs Lynn's target
   parts-field layout, and `e06d747e` needs Riley to say which page and what
   behavior the check-in "float" means. Neither is an effort problem.
4. **Pending Roland actions (nothing was executed by this runner):**
   - Status flips: `9d3806e7` -> `done`; `9031e101`, `1bc33dd6`, `2b78696c`
     -> `declined`.
   - Data SQL: the two statements in the Data-bucket section above
     (`6938c1af` is optional - already correct in the live DB; `3f254929`
     needs the SELECT eyeballed before the soft-delete).
5. **Plan, do not rush, the big ones:** `ed1b873e` (auth swap, XL) and
   `14f35f95` (parts inventory, XL) each deserve their own scoping session, and
   the two L notification/task-tracking ERs (`5925fc01`, `af14b3cb`) should be
   designed together rather than one-off.
