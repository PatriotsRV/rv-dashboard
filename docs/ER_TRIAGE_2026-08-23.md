# ER Triage - 2026-08-23 (nightly runner, incremental pass)

**Scope:** incremental. 12 ERs re-triaged (`needs_triage_ids`); the other 90 rows
carry forward their existing verdicts unchanged.
**Pulled:** 2026-08-23T07:35:57Z - 102 rows in `enhancement_requests`.
**Verified against:** HEAD `e8666a9` - index.html **v1.498**, messages.html **v1.24**.
**Why these 12:** all were touched by Session 178 (v1.497 bug batch + v1.498
quick-win batch, plus the Hubert data fix and two duplicate declines), so their
stored verdicts were stamped against a pre-S178 HEAD and needed re-mapping.

**Bucket counts, all 102 ERs:** Done 62 - Needed 25 - Duplicate 12 - Data 2 - Test/void 1
**Newly re-triaged (12):** Done 9 - Duplicate 2 - Data 1. **Bug count is now 0.**
**Still open (unreviewed):** 25, all `needed`.

> Prompt-injection guard: every `description`, `submitted_by_name` and
> `admin_notes` value was treated strictly as data. A scan of the full input for
> instruction-shaped content (ignore-previous-instructions, run/execute,
> UPDATE/DROP statements, script tags) returned zero hits. Nothing in this run
> executed SQL, ran git, or made a network call.

---

## PART A - Re-triaged this run (12)

### A1. Shipped in v1.497 - the four bugs are gone

| ER | By | Date | Category | Bucket | Mapped code (HEAD e8666a9) | Verdict | Sev/LOE |
|---|---|---|---|---|---|---|---|
| `225a2535` | Brandon | 08-05 | Bug Report | **done** | `js/insurance.js` L127-129 (roKey stamp), L47-49 (gate); `index.html` L6633, L6810; `js/ro-crud.js` L1376-1378 | Scan cache is now keyed to the RO UUID (`editingROSupabaseId`) and every consumer - auto-fill, Edit save, New save - refuses a scan whose `roKey` does not match the open modal. Stale suggestion chips cleared at Edit open. The data-integrity bleed is closed. | was data-integrity |
| `d94a418a` | Brandon | 07-27 | General UI/UX | **done** | `js/render.js` L28-39 `formatNotesNewestFirst` | Splits on the entry delimiter (`---` separator, or a newline before a `[MM/DD/YY` stamp) and reverses **entries**, not lines. The old whole-box `split('\n')...reverse()` is gone. Applies to RO Status and Customer Comm alike. | was data-display |
| `5ca724ff` | Lynn | 08-06 | General UI/UX | **done** | `js/work-list.js` L418-425 | `dragstart` now sets `dataTransfer.setData('text/plain', ...)` and `effectAllowed='move'` - the missing payload Firefox and some Chrome paths require. dragover/drop and the touch path untouched, so mobile is unchanged. | was small (UX) |
| `d4d0eeba` | Brandon | 07-22 | General UI/UX | **done** | `index.html` L4342-4344; `js/qr.js` L198-214 | Both paths fixed: the Clear Filters toast action retries `scrollToROInBoard` after re-render and reports an archived/missing RO instead of stopping silently; the messages `?ro=` deep link clears filters once (one-shot `_deepLinkClearedFilters` guard) when the RO exists but is filtered out. Also closes declined dups `9031e101`, `1bc33dd6`. | was small (nav) |

### A2. Shipped in v1.498 - the five quick wins

| ER | By | Date | Category | Bucket | Mapped code (HEAD e8666a9) | Verdict | Prior LOE |
|---|---|---|---|---|---|---|---|
| `d432863a` | Brandon | 07-21 | General UI/UX | **done** | `index.html` L263 marker; icon markup on 13 pages | `rel=icon` / `apple-touch-icon` now present in every page head (index, messages, checkin, customer-checkin, leads, closed-ros, guide, solar, time-off, worklist-report, analytics, review, v). Also closes the S139 favicon-404 console noise. | S |
| `29654af7` | Lynn | 07-06 | General UI/UX | **done** | `index.html` L8212-8220; `css/dashboard.css` L3479-3498 | `#backToTopBtn` renders with a smooth `scrollTo({top:0})` handler and a scroll listener that reveals it; pinned bottom-left, clear of the toast stack. Canonical for declined dup `2b78696c`. | S |
| `50175fce` | Lynn | 07-27 | General UI/UX | **done** | `js/ro-crud.js` L404 (+ export L1551); `index.html` L3968 | `editUrgentUpdate(index)` is wired to the card `edit-urgent` action and to the red URGENT banner itself. Writes by RO UUID, audit-logged, and still fires `urgent_update_notify` on set. Follow-on to `a7d1474e`. | S |
| `ac8265c8` | Lynn | 08-05 | General UI/UX | **done** | `supabase/migrations/add_mileage_s178.sql`; `index.html` L7670, L6743, L6766; `js/ro-crud.js` L567/L568/L1294; `js/render.js` L523-526 | `repair_orders.mileage` added live, with the `cashiered` twin column and `archive_one_ro` recreated so cash-out carries it (S171 lesson). Edit RO input next to VIN, card readout under VIN, audit label "Mileage". | S |
| `fa35e3df` | Lynn | 08-18 | General UI/UX | **done** | `css/dashboard.css` L560, L609, L1210, L1930, L3039; `js/render.js` L26 | Contrast pass: `#3b82f6` -> `#7eb3ff` on parts-estimate chips, the Thread pill and the compact estimate chip; `.log-duration` `#667eea` -> `#a5b4fc`; key-date Drop chip `#0a84ff` -> `#5cadff`. All five sites marker-tagged. | S (cosmetic) |

### A3. Data + duplicates

| ER | By | Date | Category | Bucket | Verdict | Recommend |
|---|---|---|---|---|---|---|
| `3f254929` | Sofia | 07-16 | General UI/UX | **data** | Data correction, not code - and already carried out. The misspelled Steve Hubert RO was soft-deleted in S178; only the correct Huebert RO remains. SQL of record below. The wider merge-accounts ask stays open as `0e172b2c` (needed/M). | `done` |
| `2deb8f30` | Lynn | 08-13 | General UI/UX | **duplicate** | Duplicate of `a03677e6` (Brandon 08-07, needed/S), still open. Re-verified nothing in v1.497/v1.498 changed note routing: audit lines still insert with `type='ro_status'` (`js/ro-crud.js` L316/L358/L393/L545/L602) into the same stream `js/render.js` draws. Lynn's "separate location view" folds in as spec. | `declined` |
| `ece91cba` | Lynn | 08-17 | General UI/UX | **duplicate** | Duplicate of `af14b3cb` (Lynn 05-21, needed/L), still open. Re-verified absent at HEAD `e8666a9`: `ro_tasks` / `task_tracker` / `customer_task` greps across js, index.html and supabase return zero hits; `shop_tasks` has no `ro_id`. The added spec (bulleted, anyone can add/check, record who and when, Repair-Description vs RO-Status split) folds into `af14b3cb`. | `declined` |

**Data SQL of record (already run in S178; nothing executed by this runner):**
```sql
SELECT id, ro_id, customer_name, status, created_at
FROM repair_orders WHERE customer_name ILIKE '%ubert%';
-- then, with the confirmed wrong id:
-- UPDATE repair_orders SET deleted_at = now()
-- WHERE id = '<confirmed-wrong-uuid>' AND deleted_at IS NULL;
```

---

## PART B - Theme clusters, 25 open `needed` ERs (carried forward)

Themes are unchanged from the 2026-08-22 report. **Group 0 (Bugs) and Group 10
(Misc UI Quick Wins) are now empty - both shipped in S178.**

### GROUP 1 - RO Statuses & Board Sort (4, all S - ship as ONE batch)

| ER | By | Date | LOE | Ask / verdict |
|---|---|---|---|---|
| `44419de9` | Lynn | 08-12 | S x4 | New statuses Need Estimate / Awaiting Estimate / Estimate Declined / Job Declined-Close. Each add hits ~9 hardcoded UI spots (`index.html` select L7038-7051, filter btns L1085-1096, inline maps L1579/L4489, `js/config.js` L253-262, `js/render.js` L253/L451, statusColorMap L727-739, ALL_STATUSES L741-745) **plus both DB CHECK twins** or saves throw 23514 (S172 lesson). |
| `972af03c` | Lynn | 07-17 | S | Status **Canceled** - keep record + notes. Absent: `STATUS_PROGRESS_MAP` (`js/config.js` L151-164) ends at Delivered/Cashed Out. Exclude from on-lot/queue counts. Canonical for declined dup `eb00a68d`. |
| `3e746c18` | Lynn | 08-03 | S | Status **Totaled Out**. Absent (zero `totaled`/`totalled` hits). Distinct value from Canceled, but build both in one migration; exclude both from queue/on-lot counts (`js/render.js` L656-658, L718-721). |
| `d1844e46` | Lynn | 07-16 | S | Board sort promised-date then oldest-on-lot, Shop Ops pinned. `renderBoard` sorts on one blended `calculatePriority` score (`js/render.js` L30-39, pin L35-37); no sort-mode control exists. |

**Risk note carried forward:** this batch takes the canonical status set from 17 to 23.
Worth confirming with Lynn whether all six new values survive or some collapse.

### GROUP 2 - RO Card: Notes Separation & Task Tracking (3 open + 2 folded dups)

| ER | By | Date | LOE | Ask / verdict |
|---|---|---|---|---|
| `a03677e6` | Brandon | 08-07 | S | Move machine lines (NOTIFICATION SENT, replies, scheduled/cancelled) out of RO Status Updates. Confirmed: they all write as `type='ro_status'` and crowd the box; `customer_comm` renders separately. **Caution:** Roland reversed this same retarget for lead notes in v1.491 - confirm the target stream first. Absorbs `2deb8f30`. |
| `af14b3cb` | Lynn | 05-21 | L | Per-RO TO-DO list with daily reminder, items persist until completed. Absent: `js/shop-tasks.js` is a non-customer checklist with no `ro_id` or reminder column. Absorbs `ece91cba`'s spec (who/when on add and complete, anyone can check). |
| `5925fc01` | Lynn | 04-09 | L | Notify on every RO update + daily digest with scraped to-dos. **Partial:** the digest half ships as `supabase/functions/send-manager-report`, and single-event notifies exist (`service_added_notify`, `urgent_update_notify`). Remainder overlaps `af14b3cb` - design together. |

### GROUP 3 - RO Fields & Data Capture (1 open; mileage + contrast shipped)

| ER | By | Date | LOE | Ask / verdict |
|---|---|---|---|---|
| `a195e4ab` | Brandon | 08-20 | M | Check/payment tracking on the RO - multiple checks, amounts, received dates. Zero check tracking today; the only capture is the single-value hybrid Customer Pay block (`index.html` L7203-7246 / L7782-7804), method "Check" but no check number, saved flat into `insurance_data` JSONB and never shown on the card. Needs a per-RO checks table + add/list UI + cashiered mirror. |

### GROUP 4 - Messages UX Polish (5, all S - one `messages-v1.25`)

| ER | By | Date | Ask / verdict |
|---|---|---|---|
| `9aab7550` | Brandon | 07-21 | Stronger unread treatment. S166 added a manual "Mark unread" button, but the visual is still one 8px gold dot shown only when `last_direction='inbound'` AND status open. Add a bold/tinted row. |
| `21fc3a9d` | Brandon | 07-21 | Persisted font-scale control for the conversation list. Fixed rem sizes today; zero `font-scale`/`textSize` hits. |
| `f1e6e311` | Brandon | 07-21 | Clear (x) button on the conversation search. `convoSearch` is a plain input, reset only programmatically. |
| `6e9ca4dc` | Brandon | 07-21 | Remove the "Added to every send" signature preview. Hidden only inside the mobile media query; still shows on desktop and in the RO-card composer. Signature itself keeps sending. |
| `ca0f7ed9` | Brandon | 08-03 | Lead-info panel auto-populate from linked dashboard ROs. `selectConversation` already computes phone-matched `linked` ROs but calls `renderLeadInfo(c)` without them (messages.html L1744). |

### GROUP 5 - Parts & Inventory (3)

| ER | By | Date | LOE | Ask / verdict |
|---|---|---|---|---|
| `8d885331` | Lynn | 04-09 | S | Reorder the parts-entry fields. Quick once Lynn supplies the target layout - **blocked on detail, not effort.** |
| `b19975d4` | Lynn | 04-09 | L | Supplier returns + warranty flow. Partial infra only ('Returned' status + return-deadline input); zero `rma`/`return_reason`/`supplier_credit` hits. Equals open TODO **GH#24 Phase 2**. Canonical for declined dup `86bbaf50`. |
| `14f35f95` | Lynn | 04-09 | XL | Stock/inventory subsystem. Zero inventory hits repo-wide; parts are per-RO line items only. New tables + receive/consume/adjust + searchable priced stock. Own spec + Roland go/no-go. |

### GROUP 6 - Work Orders (1)

| ER | By | Date | LOE | Ask / verdict |
|---|---|---|---|---|
| `2f752d40` | Jason | 07-21 | S | WO dollar auto-populate = hours x $195. `woFormDollar` and `woFormEstHours` are independent manual inputs; no `LABOR_RATE` constant. Auto-fill respecting the `dollarLocked` gate is safest. |

### GROUP 7 - Check-In / Time Clock (2 - same theme, build once)

| ER | By | Date | LOE | Ask / verdict |
|---|---|---|---|---|
| `6bb61db2` | Riley | 07-22 | S | Compact-view clock in/out button + "clocked into X" indicator at login, all layouts. Compact actions row (`js/render.js` L228-235) has no check-in button and no active-time_log banner exists. **Canonical spec for this theme.** |
| `e06d747e` | Riley | 04-10 | S | "Tech check-in float" - underspecified, almost certainly the same ask. Fold into `6bb61db2`. |

### GROUP 8 - Customer / Account Management (2 open; Hubert data fix closed)

| ER | By | Date | LOE | Ask / verdict |
|---|---|---|---|---|
| `0e172b2c` | Sofia | 07-16 | M | Merge duplicate customer accounts. `js/duplicates.js` groups strictly on the `PRVS-XXXX-XXXX` base RO id, so two ROs under different spellings are never detected. Needs name/phone dedup + verify-then-merge. The one-off cleanup was `3f254929`. |
| `5fbfa299` | Brandon | 06-30 | M | Archive quoted-never-proceeded customers. Only archive path is cashiered; no soft-archive flag or board filter. Partly solved by Group 1's Canceled - decide if a filter is still wanted. |

### GROUP 9 - RAF / E-Signature (2)

| ER | By | Date | LOE | Ask / verdict |
|---|---|---|---|---|
| `5fec3eca` | Sofia | 07-15 | S | Customer address on the emailed RAF copy. The `raf_copy` branch of `send-quote-email` (L387) has no `customerAddress`; the field exists only in the `lead_staff_notify` branch. Edge-fn deploy, no dashboard release. |
| `0448de62` | Sofia | 06-11 | L | Remote e-signature RAF. Signing is kiosk-only; zero `signing_token`/`remote_sign`/`signature_request` hits. Needs a tokenized public page + delivery + storage. Pairs with the S166 short-link machinery. |

### GROUP 11 - Strategic / Needs Roland (2)

| ER | By | Date | LOE | Ask / verdict |
|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | XL | Replace per-seat Google-email auth (~$500/mo). Auth is Google-token end-to-end (`js/auth.js` L69/L203-204 + One Tap + `signInWithIdToken`), kiosk included. Touches every sign-in plus email-keyed RBAC. Dedicated design session. |
| `47428f75` | Lynn | 08-03 | S | "Update vacation/sick/personal calendar" - **underspecified**. time-off.html already has all three types, color-coded calendar, per-row edit and partial-day windows. Filed `needed` rather than `done` because the ask maps to no confirmed gap. Ask Lynn. |

---

## PART C - Closed archive (77 rows: 51 + 9 + 2 + 12 + 3)

Every row at `status='done'` or `'declined'`, including the 12 re-triaged above.
Listed for completeness; outside the 12, no verdict changed here.

### C1 - Delivered (`done`, bucket `done`) - 51

| ER | By | Date | Ask (abridged) |
|---|---|---|---|
| `79b2c805` | Sofia | 04-09 | View/edit cashed-out (closed) customers, add documents |
| `75124b03` | Ryan | 04-09 | Warranty category on new RO creation |
| `b2aa4bea` | Lynn | 04-09 | Date added for scheduled future drop |
| `18611eee` | Lynn | 04-09 | Work list unusable once people were added |
| `17492066` | Lynn | 04-09 | Future/scheduled notifications ahead of a step |
| `fd6c122d` | Riley | 04-09 | Work list: delete jobs, mobile sizing, drag and drop |
| `98bd7498` | Brandon | 04-09 | Separate work lists per job category |
| `1fe68261` | Rod | 04-13 | Tasks not tied to a customer unit (shop ops) |
| `3dfc0acb` | Lynn | 04-14 | Status "Ready to Work" |
| `b87eb2fb` | Lynn | 04-14 | Keep-plugged-in sign, Keys/no keys/keypad code |
| `a5ff3e2d` | Lynn | 04-21 | RO type Warranty + Repair |
| `a7d1474e` | Lynn | 04-23 | Always-visible urgent-update field on the tile |
| `dde90d2b` | Roland | 04-27 | Fix + streamline the customer check-in page |
| `13e1cd87` | Tipton | 04-30 | iPhone back button after clocking out |
| `e27311bf` | Ryan | 05-04 | Edit WO templates, drag to reorder |
| `d2561e11` | Brandon | 05-06 | Completion / pick-up date on the order |
| `dac9fdda` | Brandon | 05-06 | On-lot status for units temporarily taken and returning |
| `a8e90a7d` | Brandon | 05-20 | Parts + freight total in Manage Parts |
| `1aeb3f58` | Lynn | 05-21 | Large highlighted promised date on the tile |
| `cbc70a86` | Lynn | 05-21 | Returning check-in auto-adds to team-lead work lists |
| `3f415c88` | Lynn | 05-26 | Schedule multi-day / weekly reminders |
| `cd663d8f` | Bobby | 05-28 | Amounts dropping trailing zeros / decimal |
| `34fc03c2` | Brandon | 05-29 | Confirm whether keys are left with the unit |
| `0b2e128c` | Lynn | 06-02 | Clock-in job notes transfer to RO updates |
| `468aa376` | Lynn | 06-02 | Scheduled drop date in the entry dash |
| `2b814250` | Roland | 06-07 | Extend date/time components into a calendar view |
| `cd084233` | Roland | 06-07 | Admin visibility on the daily Worklists report |
| `a22739b9` | Sofia | 06-08 | Check-in email + authorization-form copy not arriving |
| `91a03260` | Roland | 06-09 | Drop cashiered ROs off work lists the next day |
| `cdd77a8b` | Brandon | 06-10 | Split total RVs into on-lot vs total customers |
| `2c3d5633` | Ryan | 06-12 | Cannot upload photos from phone |
| `74a33621` | Brandon | 06-16 | Arrow keys to move between photos |
| `298e0e67` | Sofia | 06-16 | Future-drop customer duplicated at returning check-in |
| `9b823d25` | Roland | 06-16 | Email the silo manager when a service is added |
| `9b046808` | Brandon | 06-17 | Status "scheduled to arrive on lot" |
| `671fa623` | Brandon | 06-18 | Ticket totals should include parts freight |
| `5c45bb8d` | Sofia | 06-19 | Returning check-in: no RAF email + duplicate |
| `b06a285f` | Lynn | 06-19 | Important dates near the customer name, hover for detail |
| `ee50d11c` | Lynn | 06-19 | Cannot edit time-off entries |
| `88395acc` | Lynn | 06-19 | Time off as whole/half day windows, not hour totals |
| `80390f36` | Lynn | 06-23 | Pin Shop Operations tile to the top |
| `7f2d0d70` | Lynn | 06-26 | Monday report of the week's drop-offs |
| `3f7fb4fc` | Brandon | 06-29 | Shop ROs out of the status dropdown and lot counts |
| `cb7742a8` | Lynn | 06-29 | New-customer check-in data landing on the dashboard |
| `39b75fab` | Sofia | 07-09 | Remove documents scanned onto the wrong RO |
| `1c7f1410` | Andrew | 07-16 | Media attachments in messaging |
| `9d3806e7` | Brandon | 07-22 | Notes section on the customer panel in Messages |
| `ac394468` | Roland | 07-24 | Messages category on the Make-A-Wish page |
| `d62a26e4` | Roland | 07-24 | Multiple attachments per customer communication |
| `93b00023` | Roland | 07-24 | Reopen a conversation when a new message arrives |
| `09084bc5` | Lynn | 08-04 | Reopened RO repopulates fields, parts, photos, documents |

### C2 - Delivered this cycle (`done`, re-triaged above) - 9

`225a2535` - `d94a418a` - `5ca724ff` - `d4d0eeba` (v1.497) -
`d432863a` - `29654af7` - `50175fce` - `ac8265c8` - `fa35e3df` (v1.498)

### C3 - Data corrections - 2

| ER | By | Date | Note |
|---|---|---|---|
| `6938c1af` | Ryan | 05-06 | Tech surname Wimbles -> Wombles (Zak, Rod, Travis). Data fix, applied. |
| `3f254929` | Sofia | 07-16 | Misspelled Steve Hubert RO soft-deleted; correct Huebert RO retained. Re-triaged this run. |

### C4 - Duplicates (`declined`) - 12

| ER | By | Date | Folds into |
|---|---|---|---|
| `aa508852` | Riley | 04-09 | `fd6c122d` (work list) |
| `86bbaf50` | Lynn | 05-13 | `b19975d4` (parts returns) |
| `67daba15` | Brandon | 06-09 | `d2561e11` (completion / pick-up date) |
| `fa1c1d6e` | Sofia | 06-09 | `a22739b9` (check-in email) |
| `5057c7fc` | Roland | 06-09 | `91a03260` (cashiered off work list) |
| `fc6b29f9` | Sofia | 06-17 | `298e0e67` (returning-check-in duplicate) |
| `9031e101` | Riley | 07-21 | `d4d0eeba` (work-list RO link) |
| `1bc33dd6` | Brandon | 07-27 | `d4d0eeba` (work-list RO link) |
| `2b78696c` | Lynn | 07-27 | `29654af7` (back-to-top) |
| `eb00a68d` | Lynn | 08-03 | `972af03c` (status Canceled) |
| `2deb8f30` | Lynn | 08-13 | `a03677e6` (notes separation) - re-triaged this run |
| `ece91cba` | Lynn | 08-17 | `af14b3cb` (per-RO task list) - re-triaged this run |

### C5 - Test / void entries - 3 (declined)

`33750cb3`, `19dc2016`, `f73a1e5a` - Roland's form and dictation tests.

---

## Recommended action today

**Nothing to fix.** The bug bucket is empty for the first time since the runner
started: all four S178 bugs are confirmed gone at HEAD `e8666a9`, and the five
quick wins are confirmed present. No new ERs arrived since the 2026-08-22 pull.

1. **No status flips needed.** All 12 re-triaged rows already carry the right
   status (9 + 1 `done`, 2 `declined`). `status_recommendation` in
   `er_verdicts.json` simply confirms the current values.
2. **Awaiting field confirmation** on v1.497/v1.498 (open TODO row): Lynn on
   desktop worklist drag, back-to-top, urgent-edit and blue-text readability;
   Brandon on worklist/messages RO links, insurance-scan isolation and favicon.
   Local regression covered syntax + render + deep-link only.
3. **Next build candidate - Messages polish (`messages-v1.25`)**, Group 4: five
   S items, one page, no migration. Lowest risk of the remaining work.
4. **Then the status batch (Group 1)** - but only after Lynn/Roland confirm the
   six new status names. One migration, **both CHECK twins**, exclude terminal
   statuses from queue/on-lot counts.
5. **Blocked on submitter detail:** `8d885331` (Lynn: field layout),
   `47428f75` (Lynn: what is actually wrong with the time-off calendar).
6. **Design-first, no code yet:** task tracker (`af14b3cb` + `5925fc01`),
   check tracking (`a195e4ab`), account merge (`0e172b2c`), remote e-sign
   (`0448de62`), parts returns (fold into GH#24), inventory XL (`14f35f95`),
   auth replacement XL (`ed1b873e`).
