# PRVS Enhancement-Request Triage -- 2026-07-20 (headless runner)

Incremental pass over the `enhancement_requests` table against the current
modular codebase (index.html **v1.476**). 70 ERs re-triaged this run
(`needs_triage_ids`); 1 additional row carried over unchanged. Runner is
read-only -- no DB writes, no status flips, no code changes. Every verdict was
verified by grepping/reading the live code this run, not assumed.

## Board snapshot (bucket)

| Bucket | Count |
|---|---|
| done | 46 |
| needed | 15 |
| duplicate | 7 |
| data | 2 |
| carried-over (test, declined) | 1 |
| **Total** | **71** |

**15 open** -- all valid "Needed" enhancements; **no still-existing bugs** and
nothing new to close as Done. The four original bug reports in the open set
(`18611eee`, `fd6c122d`, `cd663d8f`, `2c3d5633`) were all confirmed **fixed** in
current code, so they land in Done, not Bug. Open-set LOE mix: **2 XL - 4 L -
3 M - 6 S**.

## Code checks run this run (v1.476)

- `js/parts.js` inventory/stock symbols -- **none** (grep 0; confirms `14f35f95`)
- `js/shop-tasks.js` `ro_id`/customer link -- **none** (confirms `af14b3cb` is unbuilt)
- floating/quick tech check-in button (index.html, checkin.html) -- **none** (confirms `e06d747e`)
- `js/auth.js` `signInWithIdToken` + Google One Tap -- **present** (confirms `ed1b873e` still Google)
- Canceled/Cancelled in render.js/config.js -- **none** (confirms `972af03c`)
- archived/abandoned status for quoted units -- **none** (confirms `5fbfa299`)
- page-level scroll-to-top / jump-to-search FAB -- **none** (confirms `29654af7`)
- `messaging.js` outbound MMS-from-modal + `projectblue-send` media path -- **deferred/none** (confirms `1c7f1410`)
- `send-quote-email` `raf_copy` branch (L386-540) vs `customerAddress` (only in `lead_staff_notify` L549+) -- **omitted** (confirms `5fec3eca`)
- `duplicates.js` merge engine groups by base RO id, not customer identity (confirms `0e172b2c` partial)
- edge fns present: `send-dropoff-report`, `send-admin-pnl-report`, `process-scheduled-notifications`, `sync-ro-calendar`, `send-quote-email`, `projectblue-send`

---

## Open ERs -- the 15 "Needed" (by theme, then size)

### Notifications & Task-Tracking

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `5925fc01` | Lynn | 04-09 | **L** | Notify the right team on every RO update; daily list of all in-progress jobs with to-dos scraped from status notes | Partial. Daily-digest half served by `send-manager-report`; still open: notify on EVERY generic RO update + auto-scraping to-dos from status notes. Prefer Needed over Done. |
| `af14b3cb` | Lynn | 05-21 | **L** | Per-customer task tracker with a daily reminder; tasks persist until marked complete | `shop-tasks.js` is a non-customer checklist with no `ro_id`/reminder column. Needs a per-RO tasks table + add/complete UI + daily open-task reminder. Pairs with `5925fc01`. |

### Parts & Inventory

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `14f35f95` | Lynn | 04-09 | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | New subsystem. `js/parts.js` has no stock/inventory (grep 0); parts are per-RO line items only. Inventory table + receive/consume/adjust + searchable stock + request links. Largest board item. |
| `b19975d4` | Lynn | 04-09 | **L** | Part returned-to-supplier / warrantied-by-supplier tracking | Infra only: `config.js` PART_STATUSES has 'Returned' + date fields, but no RMA#, credit, or reverse flow. = GH#24 Shop Ops Phase 2. Canonical for dup `86bbaf50`. |
| `8d885331` | Lynn | 04-09 | **S** | Rework the parts-entry field order | Form exists (`parts.js` ~178-244); request is only a reorder. Quick once Lynn gives the target layout. **Blocked on detail, not effort.** |

### Auth & cost

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | Still Google: `auth.js signInWithIdToken` + One Tap + GOOGLE_CONFIG; kiosk too. Swap to Supabase password/magic-link or shared role accounts; touches every page sign-in + RBAC. Roland wants to scope together. |

### Messaging & e-sign

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `0448de62` | Sofia | 06-11 | **L** | Send an authorization form remotely for e-signature | Signing is kiosk-only (`customer-checkin.html` signature pad); `raf_copy` is a post-signing email. Needs a tokenized public signing page + delivery + signed-form storage. Pairs with the messaging phase. |
| `1c7f1410` | Andrew | 07-16 | **M** | Attach media (photos/videos) in messaging | `messaging.js` MMS is render-only for inbound; outbound MMS-from-modal DEFERRED; `projectblue-send` has no media-send path. Needs a file/photo input + upload + media field to `projectblue-send`. |
| `5fec3eca` | Sofia | 07-15 | **S** | Show the customer's address on the emailed RAF copy | `send-quote-email` `raf_copy` branch (L386-540) omits `customerAddress`; it appears only in `lead_staff_notify` (L549+). Small: add `customerAddress` to the `raf_copy` payload + template. |

### RO fields / status

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `5fbfa299` | Brandon | 06-30 | **M** | Archive quoted-but-never-progressed customers/units | Only the cashiered archive exists (`ro-crud.js archiveROInSupabase`); no archived/abandoned flag or filter for quoted units. Pairs with `972af03c`. |
| `972af03c` | Lynn | 07-17 | **S** | Add a "Canceled" status; keep the record for notes | No Canceled/Cancelled in `render.js` ALL_STATUSES/statusColorMap or `config.js`. Small: add status + color/priority maps, exclude from on-lot/queue, keep record. Pairs with `5fbfa299`. |

### Duplicates & Merge

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `0e172b2c` | Sofia | 07-16 | **M** | Merge customer accounts (verify which is correct, delete the wrong one) | Partial: `duplicates.js` has an RO-level merge engine (`openDuplicateManager`/`executeDupeMerge`) grouped by base RO id, not customer identity. Needs name/phone account-dedup + verify-and-merge. Data cleanup for the live case is `3f254929`. |

### Scheduling & Dates

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `d1844e46` | Lynn | 07-16 | **S** | Sort dash by promised date then oldest on lot, shop-ops pinned | Board uses a single blended `calculatePriority`; shop-ops pinning exists (`render.js` L29) but there is no selectable strict "promised then oldest" sort. Small: add the strict sort option. |

### Work List & Navigation

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `29654af7` | Lynn | 07-06 | **S** | Back-to-top / quick-to-search button after scrolling | `index.html` has only targeted `scrollIntoView` calls, no page-level scroll-to-top / jump-to-search FAB. Small button. |

### Time clock

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `e06d747e` | Riley | 04-10 | **S** | Tech check-in "float" for ease of access | No floating/quick check-in button in `index.html`/`checkin.html` (grep 0). Underspecified; scope page + behavior with the submitter before build. |

---

## Recommended action today

1. **Ship the quick, well-specified S's:** `972af03c` (Canceled status),
   `5fec3eca` (address on RAF copy email), `29654af7` (back-to-top / jump-to-
   search), and `d1844e46` (strict promised-then-oldest sort) are all small,
   self-contained, and unblocked -- the highest-yield work on the board.
2. **Unblock the blocked S's** by asking the submitters for specifics: Lynn's
   parts field order (`8d885331`) and Riley's check-in float (`e06d747e`).
3. **Pair the M's that share a subsystem:** `5fbfa299` (archive quoted units) +
   `972af03c` (Canceled status) touch the same status/filter code; `0e172b2c`
   (account merge) extends `duplicates.js` and pairs with the `3f254929` data fix.
4. **Plan, don't rush, the XL's:** the auth swap (`ed1b873e`) and parts inventory
   (`14f35f95`) each deserve their own scoping/spec session.
5. **Data (hand to Roland, no code):** `3f254929` -- keep "Steve Huebert", remove
   the "Hubert" RO after confirming the id; `6938c1af` -- Wimbles->Wombles already
   applied. SQL is in the verdicts.
6. Nothing new to close as Done. Duplicates (`aa508852`, `86bbaf50`, `67daba15`,
   `fa1c1d6e`, `5057c7fc`, `fc6b29f9`, `5c45bb8d`) remain declined.

---

## Full ledger -- all 71 ERs

### Done (46) -- delivered, verified in v1.476

| ER | By | Date | Category | Mapped code / evidence |
|---|---|---|---|---|
| `79b2c805` | Sofia | 04-09 | New Feature | `closed-ros.html reactivateRO` restores cashiered detail; docs addable once reopened |
| `75124b03` | Ryan | 04-09 | RO Mgmt | Warranty RO type: `warrantyROBtn` -> `ro-crud.js setROType` ro_type; render badge/chip |
| `b2aa4bea` | Lynn | 04-09 | New Feature | `planned_dropoff_date` on New/Edit RO + kiosk + morning-before reminder + Drop chip |
| `18611eee` | Lynn | 04-09 | Bug (fixed) | `work-list.js` add/remove/silo + drag reorder persist to manager_work_lists |
| `17492066` | Lynn | 04-09 | Notif/Email | `scheduled_notifications` + `process-scheduled-notifications` cron reminders |
| `98bd7498` | Brandon | 04-09 | RO Mgmt | `work-list.js _renderWorkListSiloTabs`/`_setWorkListSilo` per-silo lists |
| `1fe68261` | Rod | 04-13 | Work Orders | `shop-tasks.js` addShopTask/complete/reopen/delete -- non-customer tasks |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | 'Ready to Work' status + filter + render color, in ALL_STATUSES |
| `b87eb2fb` | Lynn | 04-14 | RO Mgmt | Keys/No keys/Keypad + keypad code; ro-crud persists key_status/keypad_code |
| `a5ff3e2d` | Lynn | 04-21 | RO Mgmt | ro_type `warranty_repair` on New/Edit RO + filter + badge/chip |
| `a7d1474e` | Lynn | 04-23 | RO Mgmt | `urgent_update` field + `ro-crud.js _notifyUrgentUpdate` email; urgent_update_field.sql |
| `dde90d2b` | Roland | 04-27 | RO Mgmt | Check-in streamlined; RAF email forced every submission; photo optional |
| `13e1cd87` | Tipton | 04-30 | Time Clock | 'Return to Dashboard' on clock-out screen (`checkin.html showClockOutSummary`) |
| `e27311bf` | Ryan | 05-04 | Work Orders | `work-orders.js _initWOTaskDrag` + `saveWOTemplate` sort_order |
| `d2561e11` | Brandon | 05-06 | New Feature | `pickup_date` (key_dates_pickup_date.sql) + green Pickup chip (render.js) |
| `dac9fdda` | Brandon | 05-06 | UI/UX | 'Off Lot - Returning' status + filter; excluded from on-lot count |
| `a8e90a7d` | Brandon | 05-20 | UI/UX | `parts.js` Ticket Totals (wholesale/freight/cost/retail), tagged ER a8e90a7d |
| `1aeb3f58` | Lynn | 05-21 | UI/UX | `utils.js calculatePriority` blended promised + daysOnLot; strict sort is d1844e46 |
| `cbc70a86` | Lynn | 05-21 | UI/UX | `add_ro_to_silo_work_lists` rpc (work_list_autoadd_rpc.sql) on returning check-in |
| `3f415c88` | Lynn | 05-26 | UI/UX | `MAX_OCCURRENCES=60` expands scheduled_notifications daily/weekly until a date |
| `cd663d8f` | Bobby | 05-28 | Bug (fixed) | `ro-crud.js editDollarValue.toFixed(2)`; parts MONEY_FIELDS -- 425.00 keeps zeros |
| `34fc03c2` | Brandon | 05-29 | New Feature | Keys/keypad on RO modal (same build as b87eb2fb) |
| `0b2e128c` | Lynn | 06-02 | UI/UX | `checkin.html` inserts 'TECH NOTES: ...' ro_status line from clock-out notes |
| `468aa376` | Lynn | 06-02 | UI/UX | plannedDropoffDate + kiosk scheduledDropoffDate -> planned_dropoff_date (same as b2aa4bea) |
| `2b814250` | Roland | 06-07 | Calendar | `sync-ro-calendar` per-(date-type x silo) events via cal_event_ids; reminder notices |
| `cd084233` | Roland | 06-07 | Work List | `send-admin-pnl-report` + `worklist-report.html` + cron (parts/labor/cost/margin) |
| `a22739b9` | Sofia | 06-08 | UI/UX | `customer-checkin.html sendRAFEmail` every drop-off; raf_copy attaches signature |
| `91a03260` | Roland | 06-09 | Work List | `prune_cashiered_worklists()` cron removes Delivered/Cashed-Out from work lists |
| `cdd77a8b` | Brandon | 06-10 | UI/UX | `render.js statTotalOnLot` 'N on lot' sub-line; Scheduled/Off-Lot excluded |
| `2c3d5633` | Ryan | 06-12 | Bug (fixed) | `photos.js` appends input to DOM before click() (iOS fix) + HEIC/HEIF |
| `74a33621` | Brandon | 06-16 | UI/UX | `photos.js` Arrow keys -> navigateLightbox |
| `298e0e67` | Sofia | 06-16 | UI/UX | `customer-checkin.html reusedScheduledRO` updates the Scheduled RO -- no duplicate |
| `9b823d25` | Roland | 06-16 | RO Mgmt | `work-orders.js addServiceToRO` -> service_added_notify to silo managers |
| `9b046808` | Brandon | 06-17 | UI/UX | render.js excludes 'Scheduled' from days-on-lot + on-lot count |
| `671fa623` | Brandon | 06-18 | UI/UX | `parts.js` Ticket Totals include Freight in Total Cost (same as a8e90a7d) |
| `b06a285f` | Lynn | 06-19 | UI/UX | `render.js kd-name-cal`/kd-pop key-date list beside customer name, hover reveal |
| `ee50d11c` | Lynn | 06-19 | UI/UX | `time-off.html editRequest` -> UPDATE + per-row Edit button |
| `88395acc` | Lynn | 06-19 | UI/UX | `time-off.html` absence_start_time/end_time range on calendar chips |
| `80390f36` | Lynn | 06-23 | UI/UX | `render.js` shop-first sort pins Shop Operations + findable search token |
| `7f2d0d70` | Lynn | 06-26 | UI/UX | `send-dropoff-report` edge fn: coming/received by silo + work-list flag |
| `3f7fb4fc` | Brandon | 06-29 | UI/UX | render.js Shop Operations static status (cep-static-status), excluded from counts |
| `cb7742a8` | Lynn | 06-29 | UI/UX | `customer-checkin.html` POSTs to sync-ro-calendar so date lands on right calendar |
| `39b75fab` | Sofia | 07-09 | UI/UX | `photos.js deleteLibraryItem`/moveLibraryItem for mis-filed scans; canManageFiles() |
| `33750cb3` | Roland | 04-09 | UI/UX | Test entry (Make-A-Wish trial); no code action; keep declined |
| `19dc2016` | Roland | 04-11 | Time Clock | Test entry (ER form/dictation); no code action; keep declined |
| `fd6c122d` | Riley | 04-09 | Work List | Bug fixed: `work-list.js` remove + drag reorder (desktop/touch); canonical for aa508852 |

### Duplicate (7) -- fold into canonical; keep declined

| ER | By | Date | Folds into | Note |
|---|---|---|---|---|
| `aa508852` | Riley | 04-09 | `fd6c122d` | Identical work-list report; now Done |
| `86bbaf50` | Lynn | 05-13 | `b19975d4` | Gordon/Vogt window return = supplier return/warranty (open Needed/L) |
| `67daba15` | Brandon | 06-09 | `d2561e11` | Completion/pickup date; now Done (pickup_date) |
| `fa1c1d6e` | Sofia | 06-09 | `a22739b9` | Sheldon Gilbert check-in email; now Done |
| `5057c7fc` | Roland | 06-09 | `91a03260` | Remove cashed-out RO from work list; now Done |
| `fc6b29f9` | Sofia | 06-17 | `298e0e67` | Returning-customer duplicate retest; now Done |
| `5c45bb8d` | Sofia | 06-19 | `298e0e67` + `a22739b9` | David Flanagan duplicate + no RAF email; both now Done |

### Data (2) -- correction, no code (SQL handed to Roland)

| ER | By | Date | Correction |
|---|---|---|---|
| `6938c1af` | Ryan | 05-06 | Wimbles->Wombles in tech assignment; already applied (S117). `UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` if any remain |
| `3f254929` | Sofia | 07-16 | Keep 'Steve Huebert', remove misspelled 'Hubert' RO. `SELECT id,customer_name,status FROM repair_orders WHERE customer_name ILIKE '%hubert%';` then soft-delete the wrong id after Roland confirms |

### Carried over (1) -- not re-triaged this run

| ER | By | Date | Category | Note |
|---|---|---|---|---|
| `f73a1e5a` | Roland | 04-13 | UI/UX | Test entry ('Testing dictation'); already declined in DB; no bucket carried, unchanged |

---

*Generated 2026-07-20 by the headless ER-triage runner (STEPS 2-4), read-only,
against index.html v1.476. Verdicts data: `er_verdicts.json`. Dashboard:
`docs/er-triage-dashboard.html`.*
