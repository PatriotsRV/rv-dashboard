# Enhancement Request Triage -- 2026-06-15 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-15T13:19Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.451** (checkin.html v1.36, customer-checkin.html v1.11, send-manager-report v2.0-P1).
**Total ERs:** 48 (incl. 3 test entries + 4 exact/near duplicates -> **41 real requests**).
**Triaged this run:** 47 ids in `needs_triage_ids` (every row except the n15 test entry, carried over unchanged).

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `closed-ros.html`, `supabase/functions/*`) by grepping
the source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

---

## Headline

**What changed since the 2026-06-14 pass:**

- **n39 (Roland) -- Admin Manager-worklist report is now IN PROGRESS, not just queued.**
  **Manager Daily Report P1 shipped** this cycle (`supabase/functions/send-manager-report`
  **v2.0-P1**, admin-preview only): a deterministic rule engine + **List Activity** +
  **P&L Readiness** sections, rendered to admin recipients so Roland can validate the
  numbers before real per-manager sends. The remaining scope keeps n39 open: Fire Watch /
  "ROs that should be in a worklist but are not" (P2), AI narrative (P3), per-manager sends
  (P4), and manager scoring + profit-margin visibility (explicitly gated until data-trust
  > 90%). Stays **NEEDED/L** per the prefer-needed rail.
- **Twilio was torn down** (S106; `twilio-sms` / `twilio-webhook` edge functions removed).
  The notification cluster's messaging path is now the **Sendblue POC**, not Twilio --
  n8's verdict updated to reflect this.
- **One open bug:** **n47 (Ryan)** -- phone photo upload silently fails. Confirmed still
  present in `photos.js`.

The two prior-cycle bugs (n22 clock-out, n33 currency) remain fixed and confirmed in the
current tree. The only data-only item is **n25** (staff-name correction; SQL below).

---

## BUG bucket (front of the line)

| # | Submitter | Request | Evidence (current code) | Severity |
|---|---|---|---|---|
| n47 | Ryan | Can't upload pictures from phone -- acts like it accepts them, switches screens, no upload | `photos.js` `uploadPhoto` (L50-128) builds a **detached** dynamic `<input>` + async `onchange` closure. On iOS Safari the page often reloads on camera return, discarding the closure so `onchange` never fires and there is no error path. | functional, mobile-only; **small-medium** |

-> Fix: use a persistent in-DOM file input and verify the Storage write; needs a device
repro. Prefer fixing over closing (mobile uploads are core to check-in/RO photos).

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation restores full RO detail (parts/time_logs/photos). | done |
| n2 | Ryan | Warranty category on new RO | Warranty RO type shipped v1.402 (`ro-crud.js` roType=='warranty' L855 + warranty filter). | done |
| n5 | Lynn | Work list opened but couldn't view/do anything | Fixed Phase 14: `work-list.js` `renderWorkList` L269 + `removeFromWorkList` L195 + `_setWorkListSilo` L263. | done |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs` L223 / `_setWorkListSilo` L263). | done |
| n17 | Lynn | Add "Ready to Work" RO status | Shipped v1.410 (`config.js` STATUS_PROGRESS_MAP L131 slot 50). | done |
| n21 | Roland | Fix customer check-in, email out, remove photo, streamline | `customer-checkin.html` v1.11 made photo optional + auto-emails signed RAF every drop-off. | done |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | FIXED v1.447 (`checkin.html` Return-to-Dashboard button; still present v1.36). | done |
| n33 | Bobby | Amounts drop the period and the zeros (425.00 -> 425) | FIXED v1.447 (`ro-crud.js` `editDollarValue` L843 `.toFixed(2)` + `parts.js` MONEY_FIELDS L288). | done |
| n40 | Sofia | No check-in email / no copy of authorization form | FIXED v1.10/v1.11: `shouldNotifyStaff` fires on every drop-off + RAF always sends (getSignatureData typo fixed). | done |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod and Travis last name should be Wombles, not Wimbles, in tech assignment" | Data, not code. `staff.name` holds the value; not hardcoded. One SQL UPDATE (below). |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

Historical ROs that stored the tech name as free text are a separate optional backfill.

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete jobs / too small on phone / drag-and-drop" | Exact duplicate of **n10** (identical text + timestamp); already declined. Fold into n10. |
| n41 | Brandon | Completion / P/U date to know when orders left the facility | Duplicate of **n24** (same submitter); tracked under the Key Dates pass. Fold into n24. |
| n42 | Sofia | Returning customer (Sheldon Gilbert) check-in sent no email | Duplicate of **n40**; resolved by the v1.10/v1.11 staff-notify fix. Fold into n40. |
| n44 | Roland | Remove RO from worklist day after Cashiered Out/Delivered | Exact duplicate of **n43** (timestamps 1s apart). Fold into n43. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Scheduling & key dates  *(n4, n24, n29, n36, n37)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n4 | Lynn | Scheduled future drop-off date + email reminder to manager 1 day before | M |
| n36 | Lynn | Scheduled drop-date field in entry dashboard | S -- duplicate intent of n4 |
| n24 | Brandon | Completion / pickup date on the customer's order for reference (canonical for n41) | M |
| n29 | Lynn | Promised date large + highlighted on tile; sort by promise date then days-on-lot | M |
| n37 | Roland | Surface RO date/time (pickup/dropoff/reminders) as a prominent colored RO button + integrate into business calendar(s) with advance notifications to managers/admins | L |

-> **"Key Dates + Calendar" initiative.** n37 is the umbrella ask tying the
drop-off/promised/completion dates (n4/n24/n29/n36) to a calendar surface and the
notifications cluster below.

### Theme 2 -- Notifications, reminders & task-tracking  *(n3, n8, n20, n30, n32, n35)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + font/color | L (urgency partly exists) |
| n35 | Lynn | Transfer clock-in job notes -> RO updates | M |

-> **Largest opportunity.** A "Task Tracker + Reminders" subsystem. The outbound-messaging
path is now the **Sendblue POC** (Twilio torn down S106) -- design as one initiative.

### Theme 3 -- Parts  *(n6, n7, n9, n27, n28)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n28 | Brandon | Parts + freight TOTAL in Manage Parts | S -- per-part Freight Charge exists (`pf_coreCharge` L217); summed total row not built. Clean quick win. |
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier | L |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

### Theme 4 -- Work List usability  *(n10, n31, n43)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n10 | Riley | Mobile: too small to adjust on phone; touch-drag (delete + HTML5 drag-drop now work) | S -- mobile polish |
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |
| n43 | Roland | Auto-remove an RO from a manager's worklist the day after Cashiered Out/Delivered (canonical for n44) | M -- needs a daily job or status hook |

### Theme 5 -- RO fields & status nuance  *(n18, n34, n26, n19, n16)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n18 | Lynn | "Keep plugged in" flag; keys / no-keys / keypad code | M |
| n34 | Brandon | Confirm if customer leaves keys with the unit (RO / sign-in / auth form) | S -- overlaps n18 |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning vs never-on-lot | M |
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs existing Hybrid) |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |

### Theme 6 -- Work Orders / templates  *(n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will (`loadWOTemplate`/`saveWOTemplate` exist, no editing/reorder) | M |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague -- overlaps n22) |

### Theme 7 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

### Theme 8 -- Reporting & analytics  *(n39, n45)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n39 | Roland | Admin-level daily Manager-worklist report (parts/labor/costs, upcoming dates, ROs that should be in a worklist but are not) -> manager scoring + profit-margin visibility. **P1 shipped (send-manager-report v2.0-P1, admin-preview): rule engine + List Activity + P&L Readiness.** Remaining P2-P4 + gated scoring. | L (in progress) |
| n45 | Brandon | Split "Total RVs" / "RVs on Lot" stat so on-lot units are not conflated with "Not On Lot" (`render.js` `onLotROs` L594 exists; headline stat L611/L623 not split) | S -- dashboard-stat add |

### Theme 9 -- Check-In  *(n46)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n46 | Sofia | Send the authorization form (RAF) remotely for e-signature (`customer-checkin.html` signs on-site only) | L -- needs a public token-signing page + email link + signature store + staff notice |

---

## Recommended action today

1. **Fix the one open bug -- n47 (phone photo upload).** Small-medium, mobile-only;
   `photos.js` `uploadPhoto` needs a persistent in-DOM input + Storage-write verification.
   Needs a device repro before close.
2. **Close the resolved ERs:** n5, n12, n17, n21, n22, n33, n40 (set `status=done`); n11,
   n41, n42, n44 (`declined`, duplicates). n0/n14/n15/n1/n2 are already closed.
3. **Run the n25 staff-name SQL** (Roland; data only).
4. **n39 -- validate Manager Daily Report P1.** The admin-preview email is live; compare its
   List Activity + P&L Readiness numbers against the live dashboard before P2 (Fire Watch)
   and the eventual per-manager sends.

Standout near-term enhancement wins remain **n28 (parts+freight total, S)**, **n45 (on-lot
vs total stat split, S)**, and a coordinated **Key Dates + Calendar** pass (Theme 1,
anchored by Roland's n37). The strategic asks -- **n38 (auth cost reduction)** and **n39
(manager reporting, now P1-live)** -- continue under their own initiatives.
