# PRVS Enhancement-Request Triage — 2026-06-27 (Session 127)

Full pass over `enhancement_requests` against the current modular codebase
(index.html v1.464). Supabase MCP **read-only** this session — all DB writes are
handed to Roland as guarded SQL.

## Board snapshot (start of session)

| Status | Count |
|---|---|
| done | 34 |
| unreviewed | 16 |
| in-progress | 1 |
| declined | 9 |
| **Total** | **60** |

**17 open** (16 unreviewed + 1 in-progress) triaged below.

---

## Verdicts — the 17 open ERs

### ✅ Already DONE — recommend close now

| ER | By | Request | Evidence |
|---|---|---|---|
| `80390f36` | Lynn | Pin Shop Operations tile to top + make it findable by search | **Shipped v1.462** (`js/render.js` L29–36 shop-first sort; L59–61 synthetic `"shop operations shop"` search token). Reverted to `unreviewed` by the S125 data incident — needs re-closing. |

### 🔁 Largely delivered — confirm with Roland, then likely close

| ER | By | Request | Evidence / gap |
|---|---|---|---|
| `17492066` | Lynn | "Future notifications" — remind the job owner ahead of time (e.g. a day before a roof completes, call Jesus) | Schedule Notification modal (v1.411) + **recurring reminders** (v1.463) let a manager schedule a future/repeating email reminder per RO with date/time + multi-select recipients. Covers the core ask. Gap: recipients are silo managers/admins (not individual techs by name). Close as done, or keep open only for tech-level recipients. |

### 🟠/🟡 NEEDED — build candidates (scoped, shippable)

| ER | By | LOE | Request | Notes |
|---|---|---|---|---|
| `a5ff3e2d` | Lynn | M | Add a **"Warranty + Repair"** RO type (roof warranty WITH additional customer-pay repair) | New combined type alongside the existing Warranty type. Touches the New+Edit RO type buttons, `setROType`, badge, filter button, compact chip, `ro_type` value. Mirrors the v1.402 Warranty-type build. Distinct from Hybrid (= Insurance + Customer Pay). |
| `88395acc` | Lynn | M | Time-off entry as an **exact absence window** (clock-out time → return time; half-day AM/PM) instead of a total of hours | `time-off.html` currently stores `partial_hours` (a number). Needs start/end time (or AM/PM half-day) columns + form inputs + chip/calendar display. |
| `b06a285f` | Lynn | S–M | **Hover** near the customer name to show important dates/tasks as a list with full detail | Colored key-date chips already shipped v1.457 (drop/promised/pickup on the card). Remaining = a hover panel/tooltip listing the dates + detail. Smallest of the build candidates. |

### ❓ NEEDED but TOO VAGUE — need Lynn/solar to specify before building

| ER | By | Request | Why blocked |
|---|---|---|---|
| `8d885331` | Lynn | "update parts entering screen ordering… (moving fields around)" | No specifics on which fields / desired order. |
| `e06d747e` | solar | "Tech check in float for ease of access" | Unclear what "float" means (floating button? quick-access widget?). |

### 🔵 NEEDED — Medium/Large/XL design work (not today)

| ER | By | LOE | Theme |
|---|---|---|---|
| `1fe68261` | Rod | M | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) |
| `a7d1474e` | Lynn | L | Always-visible urgent-updates field on the tile + notify-on-modify + font/color |
| `5925fc01` | Lynn | L | Daily digest of all in-progress jobs + per-update notification (partly covered by the Manager Daily Report) |
| `af14b3cb` | Lynn | L | Per-customer task tracker; tasks persist until marked complete + daily reminder |
| `cbc70a86` | Lynn | L | Returning-customer check-in auto-adds the customer to each involved team-lead's work list |
| `7f2d0d70` | Lynn | M | Weekly Monday report of drop-offs received, cross-checked against worklists |
| `b19975d4` | Lynn | L | Mark a part returned-to-supplier / warrantied-by-supplier |
| `14f35f95` | Lynn | XL | In-house parts inventory/stock (order 25, 20 to stock, priced + searchable) |
| `0448de62` | Sofia | L | Send the RAF remotely for e-signature |
| `ed1b873e` | Roland | XL | Replace Google-email auth with DB auth (cut ~$500/mo of mailbox cost) |

---

## Theme clusters (Needed bucket)

- **Notifications & Task-Tracking** (biggest): 5925fc01, 17492066, a7d1474e, af14b3cb, cbc70a86, 7f2d0d70
- **Parts & Inventory**: 14f35f95, b19975d4, 8d885331
- **RO display / fields**: a5ff3e2d, b06a285f
- **Time clock / off**: 88395acc, e06d747e
- **Work / shop tasks**: 1fe68261
- **Auth & cost**: ed1b873e, 0448de62

---

## Recommended actions today

1. **Close `80390f36`** — re-mark done (shipped v1.462; reverted by the S125 incident).
2. **Confirm `17492066`** — close as done if the recurring/scheduled reminder feature satisfies it.
3. **Build a small batch** — best ROI: `b06a285f` (S–M) + `a5ff3e2d` (M) and/or `88395acc` (M).
4. **Ask Lynn/solar to clarify** `8d885331` and `e06d747e`.
5. The remaining 10 are L/XL — slot into the feature backlog, not today.
