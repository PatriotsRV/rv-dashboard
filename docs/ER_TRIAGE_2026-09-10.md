# ER Triage - 2026-09-10 (nightly runner, incremental pass)

**Scope:** all 33 `status='unreviewed'` ERs (62 done / 15 declined untouched; 110 rows total).
**Verified against:** HEAD `c58ba7c` - index.html **v1.505**, version.json 1.505.
**Pass type:** INCREMENTAL (`full_pass: false`). Only the **2 new** ERs (`929072dc`, `154e15a3`) were
mapped against v1.505 code in this run. The other 31 carry their existing verdicts forward unchanged,
each still stamped at the HEAD named inside it (0223cb3 / a405b7a / df519c6 / f501151 / 505e22b /
65fdb97) - they were **not** re-verified tonight.

**Bucket counts:** Bug 0 - Needed 32 - Data 0 - Duplicate 1
**LOE spread (Needed):** S 23 - M 3 - L 4 - XL 2

> **BUG QUEUE IS EMPTY.** All four bugs from the 2026-08-22 report shipped in S178:
> `225a2535` insurance-scan bleed (v1.497), `d94a418a` reversed note lines (v1.497),
> `5ca724ff` desktop worklist drag (v1.497), `d4d0eeba` RO click-through (v1.497).
> `3f254929` (Steve Huebert data fix) is also closed. This is the first triage in months
> with nothing in the front-of-the-line lane.

---

## NEW THIS PASS (2 ERs)

| ER | By | Date | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `929072dc` | Lynn | 09-09 | Needed | S | `js/render.js` L481-494 (key-dates row), L451 (`.customer-name`), L461-478 (badge stack); `css/dashboard.css` L942 | Neither half is built. The key-dates row renders **below** `card-ro-id` (L461), the parking / insurance / warranty / shop badges (L462-468) and the parts chips (L469-478), so it sits far from the customer name. Sizes: Drop `0.72rem/700` (L490), Promised `0.78rem/800` (L491) against `.customer-name` `1.4rem/700`. Small render/CSS change: hoist the row directly under the name and raise size/weight. **Superset of open ER `6ff111e7`** - ship as one change. |
| `154e15a3` | Bobby | 09-09 | Needed | S | `js/utils.js` L95 `normalisePhone`; `js/messaging.js` L39 `_last10` / L517 `_toE164`; `index.html` L6557, L7241, L7834; `js/render.js` L610 | Split ask. The **recognize** half already works - every matching path strips non-digits, so a dashed number already matches today. The **format** half is absent: `formatPhone` / `phoneFormat` / input-mask greps return zero hits, and `new_customerPhone` / `editCustomerPhone` are bare `type="tel"`, so whatever is typed is what is stored and shown. Small: one shared mask helper on input/blur across the RO forms + `customer-checkin.html` (L725, placeholder only today). Low risk precisely because storage is never used raw for matching. |

**Neither new ER contained prompt-injection content.** Both are ordinary UI/UX requests.

---

## GROUP 1 - RO Statuses & Board Sort (5 ERs - ship as ONE batch)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `44419de9` | Lynn | 08-12 | S x4 | New statuses: Need Estimate, Awaiting Estimate, Estimate Declined, Job Declined/Close |
| `972af03c` | Lynn | 07-17 | S | New status **Canceled** - keep record + notes, never delete (canonical; `eb00a68d` declined into it) |
| `3e746c18` | Lynn | 08-03 | S | New status **Totaled Out** |
| `d1844e46` | Lynn | 07-16 | S | Board sort: promised date -> oldest on lot, Shop Ops pinned top |
| `611589ba` | Lynn | 08-24 | **DUP** | "Closed and Canceled" - both halves already open (`972af03c` + `44419de9`). Adds no work item; folds in as confirmation. Recommend `declined` with pointer. |

**Risk note (carried forward, and now larger):** every status add touches ~9 hardcoded UI spots
**plus BOTH DB CHECK constraints** (`repair_orders` + the `cashiered` twin - the S172 lesson, where
'Off Lot - Returning' shipped UI-only and was broken 50 days). Since the 08-22 report the canonical
set has grown again - v1.501 added "Closed - No Charge" and v1.503 added "Delivered - No Review" -
so **the carried-over line counts in these verdicts have drifted and must be re-greped before
building.** Confirm with Lynn whether all six new values survive or some collapse into Canceled.
One migration, one release.

---

## GROUP 2 - RO Card: Notes Separation & Task Tracking (3 ERs)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `a03677e6` | Brandon | 08-07 | S | Move machine lines (NOTIFICATION SENT, replies, scheduled/cancelled) out of RO Status Updates. **CAUTION:** Roland reversed exactly this retarget for lead notes in v1.491 - confirm scope first. |
| `af14b3cb` | Lynn | 05-21 | L | Per-RO TO-DO list with daily reminder; items persist until completed. (`ece91cba` already declined into it, carrying the who/when spec detail.) |
| `5925fc01` | Lynn | 04-09 | L | Notify on every RO update + daily digest with scraped to-dos. Digest half exists (`send-manager-report`); remainder overlaps the task-tracker build. |

**Design together.** `af14b3cb` + `5925fc01` are one subsystem, not two tickets.

---

## GROUP 3 - RO Fields & Data Capture (2 ERs)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `a195e4ab` | Brandon | 08-20 | M | Check/payment tracking on the RO - multiple checks, amounts, received dates. Today only the single-value hybrid Customer Pay block (no check number). Needs a per-RO checks table + add/list UI + cashiered mirror. |
| `154e15a3` | Bobby | 09-09 | S | **NEW** - phone-number dash formatting on input. See "New this pass" above. |

Note: `ac8265c8` (mileage) and `fa35e3df` (blue font) both shipped in v1.498 and have left this group.

---

## GROUP 4 - Scheduling / Key Dates on the Card (2 ERs - ship as ONE change)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `6ff111e7` | Lynn | 08-27 | S | Enlarge + recolor the **Drop** date chip - filed 5 days after v1.498 recolored it, so the live remainder is the **size** ask (0.72rem/700, smallest of the three key dates). |
| `929072dc` | Lynn | 09-09 | S | **NEW** - promised **and** drop dates closer to the customer name, large bold text. |

**These are the same piece of work.** `929072dc` is the superset: it adds the repositioning ask and
extends the size ask to the Promised chip. Build once - hoist `key-dates-row` (js/render.js L481-494)
to sit directly under `.customer-name` (L451), above the badge stack, and lift both chips toward
`.customer-name`-adjacent weight. Roland may prefer to decline `6ff111e7` into `929072dc`;
this runner left both open rather than close an ER on its own judgement.

---

## GROUP 5 - Messages UX Polish (7 ERs, all S - one messages release)

| ER | By | Date | Ask |
|---|---|---|---|
| `9aab7550` | Brandon | 07-21 | Stronger unread treatment - S166 added a manual "Mark unread"; the visual is still just an 8px gold dot. |
| `21fc3a9d` | Brandon | 07-21 | Persisted font-scale control for the conversation list. |
| `f1e6e311` | Brandon | 07-21 | Clear (x) button on conversation search. |
| `6e9ca4dc` | Brandon | 07-21 | Remove the "Added to every send" signature preview (hidden on mobile only; still on desktop + RO-card composer). |
| `ca0f7ed9` | Brandon | 08-03 | Lead-info panel auto-populate from linked dashboard ROs - `selectConversation` already computes `linked` but never passes it to `renderLeadInfo`. |
| `472ccadb` | Andrew | 08-28 | Drag a thumbnail into the conversation to send it. Ask Andrew whether the thumbnail is an OS file (S) or an in-app photo (M). |
| `776384e7` | Andrew | 09-01 | Quick/canned responses in the composer (e.g. "Vroom parts deposit"). Ask Andrew for the starter list. |

**All seven are one messages session.** Two are blocked on a question to Andrew, not on effort.

---

## GROUP 6 - Parts & Inventory (5 ERs)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `2af47890` | Brandon | 08-25 | S | Stamp date received when a part is marked Received. **Half-built and subtly dangerous:** the DB trigger stamps on transition, but `markPartReceived` never sets `part.dateReceived`, so a later `savePartForm` resends `date_received` null and the trigger will not restamp - **wiping the date.** Do this one first in the group. |
| `1e434aff` | Brandon | 08-25 | S | Show the received date on the parts status table. Depends on `2af47890` landing the stamp. |
| `8d885331` | Lynn | 04-09 | S | Reorder parts-entry fields - **blocked on Lynn supplying the target layout**, not on effort. |
| `b19975d4` | Lynn | 04-09 | L | Supplier returns + warranty flow (RMA, reason, credit). Equals open TODO **GH#24 Phase 2**; canonical for declined `86bbaf50`. |
| `14f35f95` | Lynn | 04-09 | XL | Stock/inventory subsystem. New tables + receive/consume/adjust + searchable priced stock. Needs its own spec + Roland go/no-go. |

---

## GROUP 7 - Work Orders (1 ER)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `2f752d40` | Jason | 07-21 | S | WO dollar auto-populate = effective hours x posted $195 labor rate. Add a `LABOR_RATE` constant; respect the existing `dollarLocked` gate so a manual override sticks. |

---

## GROUP 8 - Check-In / Time Clock (2 ERs - build once)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `6bb61db2` | Riley | 07-22 | S | Compact-view clock in/out button + an "you are clocked into X" indicator at login, all layouts. **Canonical spec.** |
| `e06d747e` | Riley | 04-10 | S | "Tech check-in float" - underspecified; almost certainly the same ask. Fold into `6bb61db2`. |

---

## GROUP 9 - Customer / Account Management (2 ERs)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `0e172b2c` | Sofia | 07-16 | M | Merge duplicate customer accounts. `js/duplicates.js` groups strictly on the PRVS base RO id, so different spellings are never detected. Needs name/phone dedup + a verify-then-merge step. |
| `5fbfa299` | Brandon | 06-30 | M | Archive quoted-never-proceeded customers. Only archive path today is cashiering. Partially solved by Group 1's Canceled status - decide whether a soft-archive flag + board filter is still wanted on top. |

---

## GROUP 10 - RAF / E-Signature (2 ERs)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `5fec3eca` | Sofia | 07-15 | S | Customer address on the emailed RAF copy - add `customerAddress` to the `raf_copy` payload + template in `send-quote-email`. Edge-function deploy, no dashboard release. |
| `0448de62` | Sofia | 06-11 | L | Remote e-signature RAF - tokenized public signing page + delivery + signed-form storage. Pairs with the S166 short-link machinery. |

---

## GROUP 11 - Strategic / Needs Roland (2 ERs)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | XL | Replace per-seat Google auth (~$500/mo). Google-token end-to-end incl. kiosk; touches every sign-in plus the email-keyed RBAC. Dedicated design session. |
| `47428f75` | Lynn | 08-03 | S | "Update vacation/sick/personal calendar" - underspecified; time-off.html already has all three types, calendar, per-row edit and partial-day windows. **Ask Lynn what is actually wrong.** |

---

## Recommended action today

The bug lane is empty, so the front of the line is a genuine choice rather than a forced fix.

**Best single-session candidate - the card-prominence pair (Group 4).**
`6ff111e7` + `929072dc` are one small render/CSS change in `js/render.js` L451-494 plus
`css/dashboard.css`. It is Lynn's third filing on this exact area (after `1aeb3f58`, delivered
v1.457, and the v1.498 recolor), which is a strong signal the current treatment is not landing on
the shop floor. No migration, no DB risk, immediately visible.

**Runner-up - Parts pair (`2af47890` + `1e434aff`).** Small, but `2af47890` is not merely missing:
the half-built state can silently wipe an already-stamped received date on the next part edit.
That is closer to a latent bug than an enhancement, and it is the only item in the open set with a
data-loss shape. Consider promoting it if Roland wants the safest thing done first.

**Then, each roughly one session:**
1. Messages polish batch (Group 5) - five ready + two pending Andrew's answers.
2. Status batch (Group 1) - after Lynn confirms which of the six new values survive.
   **Re-grep the hardcoded spots first**: v1.501 and v1.503 both added terminal statuses, so the
   line numbers in those carried-over verdicts are stale.
3. WO labor-rate autofill + check-in button batch (Groups 7 + 8).
4. RAF address (`5fec3eca`) - edge-function deploy, can ride along with anything.

**Design-first (need input before code):** task tracker (Group 2) - check tracking (`a195e4ab`) -
account merge (`0e172b2c`) - remote e-sign (`0448de62`) - parts returns (fold into GH#24) -
inventory XL (`14f35f95`) - auth replacement XL (`ed1b873e`).

**Blocked on submitter detail:** `8d885331` (Lynn layout) - `47428f75` (Lynn clarification) -
`472ccadb` / `776384e7` (Andrew) - `e06d747e` (Riley, but foldable into `6bb61db2`).

**Status changes for Roland (this runner changes nothing):**
- `611589ba` -> `declined`, pointing at `972af03c` + `44419de9`.
- Optionally `6ff111e7` -> `declined`, pointing at `929072dc`, if you prefer one canonical ER for
  the card-prominence work.

**No Data-bucket SQL this pass.** Nothing to run.
