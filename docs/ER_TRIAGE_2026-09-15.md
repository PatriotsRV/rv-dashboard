# ER Triage -- 2026-09-15 (nightly runner, incremental pass)

**Scope:** incremental. 2 new ERs classified this run
(`98a2f2f5` Lynn, `4354157b` Bobby -- both filed 2026-09-14). The other 111 rows
were already triaged and unchanged, so their `triage_bucket` / `triage_loe` /
`triage_verdict` values are carried over verbatim.
**Verified against:** HEAD `616f4f3` -- index.html **v1.505**, messages.html **v1.25**.
**Input snapshot:** `er_input.json`, fetched 2026-09-15T12:31:56Z (113 rows).

**Table totals (113):** 36 `unreviewed` -- 62 `done` -- 15 `declined`
**Open (unreviewed) buckets:** Bug **0** - Needed **35** - Data **0** - Duplicate **1**
**Open LOE spread:** S **24** - M **5** - L **4** - XL **2**

> **The bug queue is empty.** All four bugs from the 2026-08-22 pass
> (`225a2535`, `d94a418a`, `5ca724ff`, `d4d0eeba`) shipped in v1.497/v1.498 and
> are now `status=done`. So is the one Data item (`3f254929`, Huebert). Nothing
> in the open set is a defect -- every open ER is an enhancement.

**New this run (both `needed`, both M, both need one clarifying question):**

| ER | By | Date | Category | Bucket | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|---|
| `98a2f2f5` | Lynn | 09-14 | General UI/UX | Needed | M | `js/render.js` L451/L619, `index.html` L7259/L7860/L2707, `js/ro-crud.js` L593/L676, `js/utils.js` L153/L237, `supabase/migrations/add_mileage_s178.sql` | Veteran parts-discount checkbox by the name/address block, always visible. **No veteran/discount feature exists anywhere** -- repo greps hit only branding copy (`customer-checkin.html` L631, `send-quote-email/index.ts` L182) and one sampleData note (`index.html` L1519). Nearest existing field is `customer_type`, which carries only Regular/VIP (`js/utils.js` L153 scores VIP, L237 maps the column). Needs a boolean on `repair_orders` **plus the `cashiered` twin plus `archive_one_ro()` recreated** (S171 lesson -- anything not enumerated there is silently dropped at cash-out; `add_mileage_s178.sql` is the exact template), a checkbox next to Customer Address on New RO (`index.html` L7259-7263) and Edit RO (L7860) with the field map entry (L2707), the save paths (`js/ro-crud.js` L593 insert / L676 update), and a chip by `.customer-name` (`js/render.js` L451). **"Visible always" is the real point:** the address row today is gated behind `shouldShow('contactInfo')` (`js/render.js` L619), so the flag must render outside the tile-visibility gate. |
| `4354157b` | Bobby | 09-14 | General UI/UX | Needed | M | `js/work-orders.js` L341-384/L389, `supabase/migrations/wo_redesign_phase_b.sql` L33-44/L70-75, `supabase/migrations/weekly_pnl_rpcs.sql` L32-34, `js/config.js` L99-134, `index.html` L7270 | Sublet line on the service silo. **`sublet` is a zero-hit grep repo-wide.** Primary reading -- an outside-vendor line item inside a silo work order: the Build/Edit WO form carries only status, total dollars, estimated hours, notes and task rows (`js/work-orders.js` L341-384, rows at L389), `service_tasks` has no vendor or cost column (`wo_redesign_phase_b.sql` L70-75 adds only parent/actual/billed hours + completion), and `weekly_pnl` costs **only** labor and parts per silo (`weekly_pnl_rpcs.sql` L32-34) -- there is no outside-vendor cost bucket, so sublet spend would be invisible in the P&L. Alternate reading -- a 9th "Sublet" silo: `SERVICE_SILOS` + both maps (`js/config.js` L99-134), the `service_work_orders.service_silo` CHECK (`wo_redesign_phase_b.sql` L33-44), the repairType checkboxes (`index.html` L7270) and the P&L `silo_map`. **Ask Bobby which** -- the two builds share almost nothing. |

---

## GROUP 1 -- RO Statuses & Board Sort (6 ERs -- ship as ONE batch)

| ER | By | Date | Category | Bucket | LOE | Ask / verdict |
|---|---|---|---|---|---|---|
| `44419de9` | Lynn | 08-12 | General UI/UX | Needed | S | New statuses Need Estimate / Awaiting Estimate / Estimate Declined / Job Declined-Close. None of the 4 exist; each add hits ~9 hardcoded UI spots plus BOTH DB CHECK twins or saves throw 23514. |
| `972af03c` | Lynn | 07-17 | General UI/UX | Needed | S | Status CANCELED, keep record + notes. Absent (`js/config.js` STATUS_PROGRESS_MAP); add status + color/priority maps, exclude from on-lot and queue counts. Canonical for declined `eb00a68d`. |
| `3e746c18` | Lynn | 08-03 | General UI/UX | Needed | S | Status TOTALED OUT. Case-insensitive grep for totaled/totalled = 0 hits. Distinct terminal value; build with CANCELED. |
| `b4db201b` | Lynn | 09-11 | General UI/UX | Needed | S | Status NO SHOW. The *behavior* half already ships -- `Closed - No Charge` (`js/config.js` L174) archives like a cash-out and `enqueue_review_request` fires only on Delivered/Cashed Out, so no review goes out. Only the NO SHOW label is missing (grep = 0). 20th status + both CHECK twins. |
| `611589ba` | Lynn | 08-24 | General UI/UX | **Duplicate** | -- | "Closed and Canceled in the status dropdown" -- both halves are already open ERs: CANCELED is `972af03c`, Closed is `44419de9`'s JOB DECLINED/CLOSE. Re-verified absent (STATUS_PROGRESS_MAP L151-171 lists 15, neither present). Adds no new work item. Recommend `declined` with a pointer. |
| `d1844e46` | Lynn | 07-16 | General UI/UX | Needed | S | Sort board by promised date then oldest-on-lot, Shop Ops pinned. `js/render.js` renderBoard sorts on one blended `calculatePriority` score (L30-39, shop pin L35-37); no selectable sort mode exists. |

**Risk note (unchanged from 08-22, and the reason this stays one batch):** every
status add touches ~9 hardcoded UI spots **plus both DB CHECK constraints**
(`repair_orders` + the `cashiered` twin). S172 shipped `Off Lot - Returning`
UI-only and it was broken for 50 days. Confirm the final name list with
Lynn/Roland first -- this batch would take the canonical set to ~20-23 values.
**One migration, one release.**

---

## GROUP 2 -- RO Card: Notes Separation & Task Tracking (3 ERs)

| ER | By | Date | Category | Bucket | LOE | Ask / verdict |
|---|---|---|---|---|---|---|
| `a03677e6` | Brandon | 08-07 | General UI/UX | Needed | S | Move machine lines (NOTIFICATION SENT/FAILED, SCHEDULED/CANCELLED, key-date + urgent + parts rows) out of RO Status Updates. They all write as notes type `ro_status` (`js/render.js` L553); `customer_comm` renders separately (L563). **CAUTION: Roland reversed exactly this retarget for LEAD notes in v1.491 -- confirm the target stream before building.** |
| `af14b3cb` | Lynn | 05-21 | General UI/UX | Needed | L | Per-RO TO-DO list with daily reminder; items persist until completed. `js/shop-tasks.js` is a non-customer shop checklist with no `ro_id` and no reminder column; `ro_tasks` / `customer_task` / `task_tracker` greps = 0. Needs a per-RO tasks table + add/complete UI + daily reminder. Declined `ece91cba` carries the best spec detail (bulleted, anyone adds/checks, notate who/when) -- fold it in as design input. |
| `5925fc01` | Lynn | 04-09 | New Feature Idea | Needed | L | Notify on every RO update + daily digest with scraped to-dos. **Partial:** the digest half ships as `send-manager-report`, and single-event notifies exist (`js/work-orders.js` L293, `js/ro-crud.js` L374). Remaining half overlaps `af14b3cb` -- design together. Theme anchor. |

---

## GROUP 3 -- RO Fields & Data Capture (3 ERs)

| ER | By | Date | Category | Bucket | LOE | Ask / verdict |
|---|---|---|---|---|---|---|
| `a195e4ab` | Brandon | 08-20 | General UI/UX | Needed | M | Track checks received on an RO (multiple, amounts, dates). Zero check/payment tracking; the only capture is the one-value hybrid Customer Pay block (`index.html` L7203-7246 / L7782-7804), hybrid-RO-only, method "Check" with no check number, saved flat into `insurance_data` JSONB and never shown on the card. Needs a per-RO checks table + add/list UI + cashiered mirror. |
| `98a2f2f5` | Lynn | 09-14 | General UI/UX | **Needed (NEW)** | M | Veteran parts-discount checkbox by name/address, always visible. Full verdict in the "New this run" table above. Two open questions for Lynn: (1) flag-only, or must it actually discount parts pricing (`js/parts.js` L69/L497 wholesale/retail)? (2) does the flag live on the RO or on the customer (it would then re-apply on every return visit)? |
| `154e15a3` | Bobby | 09-09 | General UI/UX | Needed | S | Auto-insert dashes when typing phone numbers. **Recognize half already works** -- matching strips non-digits (`js/utils.js` L95 normalisePhone, `js/messaging.js` L39/L517), so dashed input already matches today. Format half absent: `formatPhone`/`phoneFormat`/`mask` = 0 hits and `new_customerPhone` (L7241) / `editCustomerPhone` (L7834) are bare `type=tel`, so the raw typed string is stored and displayed. Shared input mask. |

---

## GROUP 4 -- Scheduling / Key-Date Prominence (2 ERs -- ship as ONE change)

| ER | By | Date | Category | Bucket | LOE | Ask / verdict |
|---|---|---|---|---|---|---|
| `929072dc` | Lynn | 09-09 | General UI/UX | Needed | S | Promised + drop dates closer to the customer name, large bold. Neither half built: the key-dates row (`js/render.js` L481-494) renders **below** the RO id (L461), the parking/insurance/warranty/shop badges (L462-468) and the parts chips (L469-478), so it sits far from `.customer-name` (L451). Sizes: Drop 0.72rem/700, Promised 0.78rem/800 vs `.customer-name` 1.4rem/700 (`css/dashboard.css` L942). Render/CSS only. |
| `6ff111e7` | Lynn | 08-27 | General UI/UX | Needed | S | Enlarge + recolor the drop date. Partly addressed -- `fa35e3df` recolored the Drop chip #0a84ff -> #5cadff in v1.498 -- but Lynn filed this 5 days **after** that shipped and adds a new *size* ask, still unbuilt at L490. **Strict subset of `929072dc`; ship both as one change.** |

---

## GROUP 5 -- Messages UX Polish (7 ERs, all S -- one messages release)

| ER | By | Date | Category | Bucket | LOE | Ask / verdict |
|---|---|---|---|---|---|---|
| `9aab7550` | Brandon | 07-21 | General UI/UX | Needed | S | Stronger unread treatment. S166 added a manual "Mark unread" button but the visual is unchanged: one 8px gold `.unread-dot` (CSS L469) only when `last_direction='inbound'` AND status open. Add a bold/tinted row treatment. |
| `21fc3a9d` | Brandon | 07-21 | General UI/UX | Needed | S | Persisted font-scale control for the conversation list. Fixed rem sizes; `font-scale`/`fontScale`/`textSize` = 0 hits. |
| `f1e6e311` | Brandon | 07-21 | General UI/UX | Needed | S | Clear (x) button on the conversation search. `convoSearch` (L604) is a plain input, reset only programmatically (L1151/L1170). |
| `6e9ca4dc` | Brandon | 07-21 | General UI/UX | Needed | S | Remove the "Added to every send" signature preview. `js/messaging.js` refreshSignaturePreview L369 writes it into `#msgSigPreview`; messages.html hides it only inside the mobile media query (L565), so desktop and the RO-card composer still show it. The signature keeps sending. |
| `ca0f7ed9` | Brandon | 08-03 | Messages / Texting | Needed | S | Auto-populate the lead-info panel from an existing dashboard customer. `renderLeadInfo` (L1389) reads only the conversation's own columns; `selectConversation` already computes phone-matched `linked` ROs for renderContactInfo but calls `renderLeadInfo(c)` without them (L1744). Pass `linked`, fall back to RO rv/service. |
| `472ccadb` | Andrew | 08-28 | General UI/UX | Needed | S | Drag a thumbnail into the conversation to send it. Composer has only a hidden file input + paperclip (`js/messaging.js` L347, L491-494) and `_handleAttachPick` reads `ev.target.files` (L422); dragover/drop/dataTransfer.files hits only the work-list and WO row-reorder code. Add a drop zone feeding the same upload path. **Ask Andrew whether the thumbnail is an OS file or an in-app photo -- the in-app variant is M, not S.** |
| `776384e7` | Andrew | 09-01 | General UI/UX | Needed | S | Quick/canned responses (e.g. "Vroom parts deposit"). Composer extras are attach/emoji/signature only (L341-351, L491-509); `quick_replies`/`canned`/`message_templates` repo-wide hits only a dormant comment in `sms_schema.sql` L11. Snippet picker in `composerExtrasHtml`, shared by messages.html and the RO-card modal. **Ask Andrew for the starter list.** |

---

## GROUP 6 -- Parts & Inventory (5 ERs)

| ER | By | Date | Category | Bucket | LOE | Ask / verdict |
|---|---|---|---|---|---|---|
| `2af47890` | Brandon | 08-25 | General UI/UX | Needed | S | Stamp date-received when Received is clicked. **DB half ships** -- trigger `trg_stamp_date_received` stamps the shop-local day on the transition into Received. **Client half missing:** `markPartReceived` (`js/parts.js` L449-478) sets `part.status` only, never `part.dateReceived`, so the in-memory row stays blank and a later `savePartForm` edit resends `date_received` null (L539) which the trigger will not restamp (OLD.status is already Received) -- **wiping the date.** Nearest thing to a defect in the open set; do this one first. |
| `1e434aff` | Brandon | 08-25 | General UI/UX | Needed | S | Show the received date in the parts status table. No receipt-date column -- headers are Part Name / Part # / Supplier / Status / Qty / Wholesale / Retail / Quick / Actions (`js/parts.js` L259-267) and `renderPartsRows` (L121-145) never prints `p.dateReceived`; it is only editable in the collapsed Add/Edit form (L242). **Depends on `2af47890` landing the stamp.** |
| `8d885331` | Lynn | 04-09 | Parts & Ordering | Needed | S | Reorder the parts-entry fields (`pf_supplier` L181, `pf_partName` L190, `pf_serviceSilo` L208, `pf_returnDeadline` L226). **Blocked on Lynn supplying the target order or a mockup, not on effort.** |
| `b19975d4` | Lynn | 04-09 | Parts & Ordering | Needed | L | Supplier returns + supplier warranty. Partial infra only: `PART_STATUSES` includes `Returned` and there is a Return Deadline input, but `rma`/`return_reason`/`supplier_credit` = 0 hits -- no RMA number, reason, credit or reverse flow. **Equals open TODO GH#24 Shop Ops Phase 2 -- merge into that build.** Canonical for declined `86bbaf50`. |
| `14f35f95` | Lynn | 04-09 | Parts & Ordering | Needed | XL | Parts stock inventory (order 25, use 5, 20 searchable in-house). `inventory`/`on_hand`/`in_stock`/`stock_qty` = 0 hits; parts are per-RO line items only. New subsystem: inventory table, receive/consume/adjust flows, searchable priced stock view, links from the parts-request path. **Needs its own spec + Roland go/no-go.** |

---

## GROUP 7 -- Work Orders (2 ERs)

| ER | By | Date | Category | Bucket | LOE | Ask / verdict |
|---|---|---|---|---|---|---|
| `2f752d40` | Jason | 07-21 | Work Orders | Needed | S | WO dollar auto-populates from total hours x the posted $195/hr labor rate. `woFormDollar` (L360) and `woFormEstHours` (L365) are independent manual inputs read separately on save (L649-650); `LABOR_RATE`/`laborRate` = 0 hits in the module (`js/insurance.js` L212 is the per-claim INSURANCE rate). Add a config constant + auto-fill, respecting the `dollarLocked` gate (L324). Auto-fill-but-editable is safest. |
| `4354157b` | Bobby | 09-14 | General UI/UX | **Needed (NEW)** | M | Sublet line on the service silo. Full verdict in the "New this run" table above. **Blocking question for Bobby: a sublet *line item* inside a silo WO, or a 9th "Sublet" *silo*?** If it is the line item, the real work is not the UI -- it is giving `weekly_pnl` a third cost bucket beside labor and parts (`weekly_pnl_rpcs.sql` L32-34) so sublet spend stops being invisible. Pairs naturally with `2f752d40` as one WO release. |

---

## GROUP 8 -- Check-In / Time Clock (2 ERs -- same theme, build once)

| ER | By | Date | Category | Bucket | LOE | Ask / verdict |
|---|---|---|---|---|---|---|
| `6bb61db2` | Riley | 07-22 | Time Clock / Check-In | Needed | S | Compact-view clock in/out button + a "you are clocked into X" indicator at login, in all layouts. `js/render.js` compact-col-actions (L228-235) has edit/parts/WO/notify/message/schedule and no check-in button; "clocked in"/`activeTimeLog` = 0 hits. **Treat as the canonical spec for this theme.** |
| `e06d747e` | Riley | 04-10 | Time Clock / Check-In | Needed | S | "Tech check-in float for ease of access." No floating/sticky check-in control in index.html or checkin.html. Underspecified and almost certainly the same ask -- **fold into `6bb61db2`.** |

---

## GROUP 9 -- Customer / Account Management (2 ERs)

| ER | By | Date | Category | Bucket | LOE | Ask / verdict |
|---|---|---|---|---|---|---|
| `0e172b2c` | Sofia | 07-16 | General UI/UX | Needed | M | Merge duplicate customer accounts (the Hubert/Huebert misspelling case). `js/duplicates.js` has a real merge/delete engine but groups strictly on the `PRVS-XXXX-XXXX` base RO id (regex L36, keying L44), so two ROs under different spellings of one customer are never detected. Needs name/phone similarity + a verify-which-is-correct step. The one-off cleanup was `3f254929` (done). |
| `5fbfa299` | Brandon | 06-30 | General UI/UX | Needed | M | Archive quoted-never-proceeded customers/units. The only archive path is the cashiered one (`js/ro-crud.js` L662); no soft-archive flag, status or board filter. **Largely solved by Group 1's CANCELED status** -- decide with Brandon whether a board filter / soft-archive is still wanted on top. |

---

## GROUP 10 -- RAF / E-Signature (2 ERs)

| ER | By | Date | Category | Bucket | LOE | Ask / verdict |
|---|---|---|---|---|---|---|
| `5fec3eca` | Sofia | 07-15 | General UI/UX | Needed | S | Customer address on the emailed RAF copy. The `raf_copy` branch of `send-quote-email` (L387) destructures to/cc/customerName/roId/signedDate/rv/services/workType/phone/customerType/serviceDesc/warrantyDesc/promisedDate/signatureDataUrl -- no `customerAddress`; the address exists only in the `lead_staff_notify` branch. **Edge-function deploy, no dashboard release** -- cheapest win on the board. |
| `0448de62` | Sofia | 06-11 | General UI/UX | Needed | L | Remote e-signature RAF. Signing is kiosk-only (on-device pad in customer-checkin.html); `signing_token`/`remote_sign`/`signature_request` = 0 hits. Needs a tokenized public signing page + delivery + signed-form storage. Pairs with the S166 short-link machinery. |

---

## GROUP 11 -- Strategic / Needs Roland (2 ERs)

| ER | By | Date | Category | Bucket | LOE | Ask / verdict |
|---|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | General UI/UX | Needed | XL | Replace per-seat Google-email auth (~$500/mo). Auth is Google-token end-to-end (`js/auth.js` L69/L203-204 + One Tap + `signInWithIdToken`; kiosk same path). A Supabase password/magic-link swap touches every page sign-in plus the email-keyed RBAC and RLS identity. **Dedicated design session with Roland.** |
| `47428f75` | Lynn | 08-03 | General UI/UX | Needed | S | "update vacation/sick/personal calendar" -- **UNDERSPECIFIED.** time-off.html already has all three types, a color-coded month calendar, stats tiles, per-row Edit (L752, ER `ee50d11c`) and exact partial-day windows (L368/L372). Filed Needed rather than Done only because the ask maps to no identified gap; if it restates `ee50d11c` it is already delivered. **Ask Lynn what is actually wrong.** |

---

# RECOMMENDED ACTION TODAY

**1. Answer four questions -- they are the whole critical path.** Five of the
open ERs cannot be built correctly without one sentence from their submitter,
and two of those are this run's new items:

| Ask | Who | Why it blocks |
|---|---|---|
| Sublet = a line item in a silo WO, or a 9th "Sublet" silo? | Bobby (`4354157b`) | The two builds share almost nothing; guessing wastes the session. |
| Veteran parts discount: flag-only, or real price math? And RO-level or customer-level? | Lynn (`98a2f2f5`) | Flag-only is M; price math pulls in parts pricing and the quote email. Customer-level changes the migration target. |
| Final status name list (Group 1) | Lynn / Roland | One migration, both CHECK twins; ~20-23 canonical values. Wrong list = a second migration. |
| Parts-entry target field order | Lynn (`8d885331`) | Pure blocker -- the work is minutes once the layout is known. |
| What is wrong with the time-off calendar? | Lynn (`47428f75`) | May already be delivered by `ee50d11c`. |
| Drag-a-thumbnail: OS file or in-app photo? | Andrew (`472ccadb`) | In-app is M, not S. |

**2. Ship the parts date-received pair (`2af47890` + `1e434aff`).** This is the
closest thing to a defect in the open set: `markPartReceived` never sets
`part.dateReceived`, so a later part edit resends `date_received` null and the
trigger will not restamp it -- the receipt date is silently **lost**. Two small
client-side changes in `js/parts.js`, no migration. **Do this first.**

**3. Mark `611589ba` `declined`** with a pointer to `972af03c` + `44419de9`
(Roland flips status himself; this runner changes nothing).

**4. Then, in order of value per session:**
- **Messages batch** (Group 5, all 7 S items) -- one `messages-v1.26` release.
- **Key-date prominence** (`929072dc` + `6ff111e7`) -- render/CSS only, one change, and Lynn has now asked for it three times.
- **Status batch** (Group 1) -- only after the name list is confirmed. One migration, both CHECK twins.
- **WO release** (`2f752d40` + `4354157b`) -- labor-rate autofill plus whichever sublet shape Bobby confirms.
- **`5fec3eca`** -- one field in an edge function, deployable on its own.
- **Check-in batch** (`6bb61db2` folding in `e06d747e`).

**5. Design-first, not code-first:** task tracker (`af14b3cb` + `5925fc01`) -
check tracking (`a195e4ab`) - account merge (`0e172b2c`) - remote e-sign
(`0448de62`) - parts returns (fold into GH#24) - inventory XL (`14f35f95`) -
auth replacement XL (`ed1b873e`).

**Nothing in this report changes code, data or ER status.** Verdicts for the two
new ERs are in `er_verdicts.json`.

---

# APPENDIX A -- Carried over: DONE (62)

Bucket and verdict carried verbatim from earlier passes; not re-verified this
run (idempotency rule: `triage_run_at >= updated_at`).

| ER | By | Date | Category | Bucket | Delivered as |
|---|---|---|---|---|---|
| `79b2c805` | Sofia | 04-09 | New Feature Idea | done | `closed-ros.html` reactivateRO L838 restores a cashed-out RO + children |
| `75124b03` | Ryan | 04-09 | RO Management | done | v1.402 -- Warranty is a first-class RO type at creation |
| `b2aa4bea` | Lynn | 04-09 | New Feature Idea | done | `planned_dropoff_date` written on create + edit (`js/ro-crud.js` L413/L490) |
| `18611eee` | Lynn | 04-09 | Bug Report | done | Work-list view/delete/reorder fixed (`js/work-list.js` L195 + drag rows) |
| `17492066` | Lynn | 04-09 | Notifications / Email | done | Per-RO Schedule Notification bell (future date/time, message, recipients) |
| `fd6c122d` | Riley | 04-09 | Work List | done | v1.460 -- delete + drag-reorder on mouse and touch |
| `98bd7498` | Brandon | 04-09 | RO Management | done | Per-silo Work List tabs = a list per job category |
| `1fe68261` | Rod | 04-13 | Work Orders | done | v1.467 -- `js/shop-tasks.js` non-customer shop tasks |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | done | v1.410 -- "Ready to Work" status (STATUS_PROGRESS_MAP L158) |
| `b87eb2fb` | Lynn | 04-14 | RO Management | done | v1.458 -- `keys_power_fields.sql`: key_status, keypad_code, keep_plugged_in |
| `a5ff3e2d` | Lynn | 04-21 | RO Management | done | v1.465 -- "Warranty + Repair" RO type end to end |
| `a7d1474e` | Lynn | 04-23 | RO Management | done | v1.466 -- `urgent_update_field.sql` + always-visible tile banner |
| `dde90d2b` | Roland | 04-27 | RO Management | done | check-in v1.17 -- photo optional, outbound email forced on every submit |
| `13e1cd87` | Tipton | 04-30 | Time Clock / Check-In | done | v1.447 / checkin v1.34 -- Return to Dashboard button |
| `e27311bf` | Ryan | 05-04 | Work Orders | done | v1.464 -- WO task rows have grip handles + drag-to-reorder |
| `d2561e11` | Brandon | 05-06 | New Feature Idea | done | v1.457 -- `key_dates_pickup_date.sql` + green Pickup chip |
| `6938c1af` | Ryan | 05-06 | General UI/UX | **data** | Wimbles -> Wombles already corrected in live `staff.name` |
| `dac9fdda` | Brandon | 05-06 | General UI/UX | done | v1.460 -- "Off Lot - Returning" status (slot 15) |
| `a8e90a7d` | Brandon | 05-20 | General UI/UX | done | v1.456 -- Manage Parts Ticket Totals (parts + freight + total) |
| `1aeb3f58` | Lynn | 05-21 | General UI/UX | done | v1.457 -- large bold promised-date chip, red when overdue |
| `cbc70a86` | Lynn | 05-21 | General UI/UX | done | check-in v1.17 -- `rpc('add_ro_to_silo_work_lists')` on returning check-in |
| `3f415c88` | Lynn | 05-26 | General UI/UX | done | v1.463 -- Repeat none/daily/weekly + repeat-until on notifications |
| `cd663d8f` | Bobby | 05-28 | General UI/UX | done | v1.447 -- currency inputs keep 2 decimals on edit-populate |
| `34fc03c2` | Brandon | 05-29 | New Feature Idea | done | v1.458 -- Keys selector on New + Edit RO (keys / no keys / keypad + code) |
| `0b2e128c` | Lynn | 06-02 | General UI/UX | done | v1.460 -- clock-out work notes copied into the RO update timeline |
| `468aa376` | Lynn | 06-02 | General UI/UX | done | Planned drop-off input on New RO + (restored v1.457) Edit RO |
| `2b814250` | Roland | 06-07 | Calendar | done | Drop-off / promised / pickup surface as colored key-date buttons + calendar sync |
| `cd084233` | Roland | 06-07 | Work List | done | `send-manager-report` -- admin-level daily worklist reporting |
| `a22739b9` | Sofia | 06-08 | General UI/UX | done | check-in v1.17 -- both check-in sends forced on every submit |
| `91a03260` | Roland | 06-09 | Work List | done | `prune_cashiered_worklists.sql` -- drops cashed-out ROs the next day |
| `cdd77a8b` | Brandon | 06-10 | General UI/UX | done | v1.453/v1.454 -- on-lot vs total counts split (`js/render.js` L656-658) |
| `2c3d5633` | Ryan | 06-12 | General UI/UX | done | v1.453 -- mobile photo upload fixed (input attached to DOM first) |
| `74a33621` | Brandon | 06-16 | General UI/UX | done | v1.454 -- lightbox arrow-key navigation + Escape to close |
| `298e0e67` | Sofia | 06-16 | General UI/UX | done | check-in v1.17 -- returning-customer duplicate guard + attach-vs-new confirm |
| `9b823d25` | Roland | 06-16 | RO Management | done | v1.460 -- `addServiceToRO` L244 queues the silo-manager notification |
| `9b046808` | Brandon | 06-17 | General UI/UX | done | v1.454 -- "Scheduled" is its own status (slot 45), out of the queue count |
| `671fa623` | Brandon | 06-18 | General UI/UX | done | v1.456 -- Freight line in the Ticket Totals |
| `5c45bb8d` | Sofia | 06-19 | General UI/UX | done | check-in v1.17 -- Flanagan duplicate + missing-email both fixed |
| `b06a285f` | Lynn | 06-19 | General UI/UX | done | v1.465 -- key-dates hover panel by the customer name |
| `ee50d11c` | Lynn | 06-19 | General UI/UX | done | time-off entries editable, not delete-only |
| `88395acc` | Lynn | 06-19 | General UI/UX | done | Time Off v1.5 -- exact partial-day windows |
| `80390f36` | Lynn | 06-23 | General UI/UX | done | v1.462 -- Shop Operations pinned top + findable by search |
| `7f2d0d70` | Lynn | 06-26 | General UI/UX | done | `send-dropoff-report` -- automated Monday drop-off check email |
| `3f7fb4fc` | Brandon | 06-29 | General UI/UX | done | v1.468 -- shop ROs excluded from RVs-in-Queue |
| `cb7742a8` | Lynn | 06-29 | General UI/UX | done | v1.469 / check-in v1.15 -- `sync-ro-calendar` owns the date events |
| `29654af7` | Lynn | 07-06 | General UI/UX | done | v1.498 -- `#backToTopBtn` (index.html L8212-8220) |
| `39b75fab` | Sofia | 07-09 | General UI/UX | done | v1.472 -- document rename / move-to-RO / delete |
| `1c7f1410` | Andrew | 07-16 | New Feature Idea | done | `#msgAttach` accepts image/pdf/video, multiple |
| `3f254929` | Sofia | 07-16 | General UI/UX | **data** | Misspelled Hubert RO soft-deleted in S178; only Huebert remains |
| `d432863a` | Brandon | 07-21 | General UI/UX | done | v1.498 -- favicon + apple-touch-icon in every page head |
| `d4d0eeba` | Brandon | 07-22 | General UI/UX | done | v1.497 -- both scroll-to-RO navigation paths fixed (index.html L4342-4344) |
| `9d3806e7` | Brandon | 07-22 | General UI/UX | done | S154 -- customer-side Notes section in the messages details pane |
| `ac394468` | Roland | 07-24 | General UI/UX | done | v1.482 -- Messages/Texting + every page in the `erCategory` select |
| `d62a26e4` | Roland | 07-24 | New Feature Idea | done | v1.482 -- multiple attachments per message |
| `93b00023` | Roland | 07-24 | New Feature Idea | done | v1.482 / textly-webhook v1.4 -- inbound reopens a closed conversation |
| `d94a418a` | Brandon | 07-27 | General UI/UX | done | v1.497 -- `formatNotesNewestFirst` (`js/render.js` L33-39) keeps line order |
| `50175fce` | Lynn | 07-27 | General UI/UX | done | v1.498 -- `editUrgentUpdate` inline from the tile (`js/ro-crud.js` L404) |
| `09084bc5` | Lynn | 08-04 | General UI/UX | done | Reopened ROs repopulate fields/parts/photos/docs (`js/ro-crud.js` L662-684) |
| `225a2535` | Brandon | 08-05 | Bug Report | done | v1.497 -- insurance-scan cache keyed by RO uuid (`js/insurance.js` L127-129) |
| `ac8265c8` | Lynn | 08-05 | General UI/UX | done | v1.498 -- `add_mileage_s178.sql` (+ cashiered twin + archive fn) |
| `5ca724ff` | Lynn | 08-06 | General UI/UX | done | v1.497 -- desktop drag now sets `dataTransfer` (`js/work-list.js` L418-425) |
| `fa35e3df` | Lynn | 08-18 | General UI/UX | done | v1.498 -- low-contrast blues lightened (`css/dashboard.css` L560/L609/L1210) |

---

# APPENDIX B -- Carried over: DECLINED (15)

| ER | By | Date | Category | Bucket | Folded into / reason |
|---|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | General UI/UX | done | Test entry ("Testing out the Make-A-Wish function"). Keep declined. |
| `19dc2016` | Roland | 04-11 | Time Clock / Check-In | done | Test entry ("Testing ER form / dictation"). Keep declined. |
| `f73a1e5a` | Roland | 04-13 | General UI/UX | (none) | Test entry ("Testing dictation 123"). Never bucketed; keep declined. |
| `aa508852` | Riley | 04-09 | Work List | duplicate | Identical text + timestamp to `fd6c122d` (done). |
| `86bbaf50` | Lynn | 05-13 | RO Management | duplicate | Gordon window returned to Vogt = supplier returns -> `b19975d4` (open, L). |
| `67daba15` | Brandon | 06-09 | General UI/UX | duplicate | Completion / P-U date -> `d2561e11` (done). |
| `fa1c1d6e` | Sofia | 06-09 | General UI/UX | duplicate | Gilbert check-in email -> `a22739b9` (done). |
| `5057c7fc` | Roland | 06-09 | Work List | duplicate | Cashed-out RO off the worklist -> `91a03260` (done). |
| `fc6b29f9` | Sofia | 06-17 | General UI/UX | duplicate | Returning-customer retest -> `298e0e67` (done). |
| `9031e101` | Riley | 07-21 | Work List | duplicate | iPhone worklist tap navigation -> `d4d0eeba` (now done, v1.497). |
| `1bc33dd6` | Brandon | 07-27 | Work List | duplicate | Same scroll-to-customer ask -> `d4d0eeba` (now done, v1.497). |
| `2b78696c` | Lynn | 07-27 | General UI/UX | duplicate | Back-to-top button -> `29654af7` (now done, v1.498). |
| `eb00a68d` | Lynn | 08-03 | General UI/UX | duplicate | Status CANCELED -> `972af03c` (open, S, Group 1). |
| `2deb8f30` | Lynn | 08-13 | General UI/UX | duplicate | Notifications out of RO Status -> `a03677e6` (open, S, Group 2). |
| `ece91cba` | Lynn | 08-17 | General UI/UX | duplicate | TO-DO list -> `af14b3cb` (open, L); carries the best spec detail. |

---

*Generated 2026-09-15 by the headless ER-triage runner (Phase 2a, read-only).
Procedure: `skills/prvs-er-triage/SKILL.md` STEPS 2-4. Spec:
`docs/specs/ER_TRIAGE_AUTOMATION_SPEC.md`. No code, data or ER status was
changed by this run.*
