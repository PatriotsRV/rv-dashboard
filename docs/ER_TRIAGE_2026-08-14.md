# PRVS Enhancement-Request Triage - 2026-08-14 (headless runner)

Incremental pass over `enhancement_requests` against the current modular
codebase at HEAD **a405b7a** (index.html **v1.494**). Input snapshot
`er_input.json` fetched 2026-08-14 08:18 UTC, `full_pass: false`.

Exactly **one** row was in `needs_triage_ids` this run - `2deb8f30`, Lynn's
08-13 filing. Every other row carries the verdict from its own
`triage_run_at` (2026-08-08 for 89 rows, 2026-08-09 for 7 rows, 2026-08-13
for 1 row) and is reproduced below unchanged. Line anchors inside those
carried verdicts are as of THAT run, not this one - see "Anchor drift".

Runner is read-only: **no code was changed, no DB writes were made, no git
commands were run.** Status flips and the Data-bucket SQL are Roland's to run.

## Board snapshot

| DB status | Count |
|---|---|
| done | 52 |
| declined | 13 |
| unreviewed | 34 |
| **Total** | **99** |

## Verdict buckets (98 classified; 1 row has never carried a verdict)

| Bucket | Count | Change since 2026-08-08 |
|---|---|---|
| done | 53 | +1 (`09084bc5` fixed by S171, bug -> done) |
| needed | 28 | +1 (`44419de9`, new 08-12) |
| duplicate | 11 | +1 (`2deb8f30`, new 08-13 - this run) |
| bug | 4 | -1 (`09084bc5` shipped) |
| data | 2 | unchanged |

**Headline: 4 open bugs and 28 open enhancements.** The LARGE
permanent-data-loss defect `09084bc5` that topped the last three reports is
**fixed and shipped** in v1.494 (S171). No LARGE-severity item remains on the
board. The one new ER this run is a duplicate, so it adds no work.

## What changed since the 2026-08-08 report

1. **`09084bc5` shipped (S171, v1.494 + closed-ros v1.3b hotfix `1c64c88`).**
   `js/ro-crud.js` `archiveROInSupabase` is now a single manager-gated
   `archive_single_ro` RPC call; migration `cashiered_full_detail_s171.sql`
   adds the 7 dropped columns and `archive_one_ro` writes them plus all seven
   child mirrors before the delete; `closed-ros.html` v1.3b restores them on
   reactivate. Bucket moved bug -> done on the 08-09 run. DB `status=done`.
2. **Roland cleared the paperwork recommended on 08-08.** `1c7f1410` and
   `9d3806e7` are now `status=done`; `9031e101`, `1bc33dd6`, `2b78696c` and
   `eb00a68d` are now `status=declined`. Six rows off the open count, as
   forecast, with no code work.
3. **Two new ERs arrived.** `44419de9` (Lynn, 08-12, four new RO statuses) was
   classified Needed/S on the 08-13 run. `2deb8f30` (Lynn, 08-13) is the only
   row triaged in this run and folds into `a03677e6`.
4. **S172 and S173 shipped no application code.** S172 was a DB-only CHECK-
   constraint widening for `Off Lot - Returning`; S173 produced only
   `docs/queries/customer_master_export_s173.sql`. Neither touches any verdict
   on this board.

## Anchor drift (verified this run at HEAD a405b7a)

S171 rewrote the archive path and shifted anchors in the files this board
cites most. Re-checked live this run:

- `js/ro-crud.js` `ro_status` writers: L302 -> **L303**, L344 -> **L345**,
  L379 -> **L380**, L571 -> **L573**, L582 -> **L584**.
- `index.html` notification audit writers: L3116 -> **L3141**,
  L3154 -> **L3179**.
- `js/parts.js` `ro_status` writers: L654 -> **L656**, L855 -> **L857**.
- `supabase/functions/process-scheduled-notifications/index.ts`:
  L187 -> **L189**, L215 -> **L217**.
- `js/render.js` note boxes unchanged at **L553** (RO Status Updates) and
  **L563** (Customer Comm).
- `index.html` `scrollToROInBoard` L4211 -> **L4234** (recorded on the 08-09
  run, still current).

Treat any other line number in a carried verdict as approximate.

## Bug spot-checks run this session

The four open bugs were not re-classified (not in `needs_triage_ids`), but
S171 touched `js/work-list.js` and `js/ro-crud.js`, so the three
code-verifiable ones were re-grepped to confirm S171 did not silently fix or
worsen them. All three still reproduce:

- `d94a418a` - `js/render.js` L553 and L563 still render
  `split('\n').map(escapeHtml).reverse()`. Still open.
- `5ca724ff` - `js/work-list.js` `dragstart` (now **L415**) still sets nothing
  on `e.dataTransfer`, no `setData`, no `effectAllowed`. There is a `dragover`
  preventDefault at **L421** but still no `dragenter` handler. Still open.
- `225a2535` - `window._lastEstimateScan` is still keyed on `mode` alone
  (`js/insurance.js` L43-44 read, L118 write) and `js/ro-crud.js` L1175 still
  deliberately keeps the cache across modal close. Still open.
- `d4d0eeba` - not re-grepped this run; the 08-09 pass confirmed
  `scrollToROInBoard` (index.html L4234-4250) still matches only rendered
  `[data-ro-index]` cards. Carried as open.

---

## BUGS - front of the line (4)

| ER | By | Date | Category | Severity | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `225a2535` | Brandon | 08-05 | Bug Report | **data-integrity** | `js/insurance.js` L118 / L43-44; `js/ro-crud.js` closeEditModal L1175 | `window._lastEstimateScan` is keyed only by `mode`, and `closeEditModal` deliberately KEEPS it. Scan RO A, close without saving, open RO B: A's values auto-fill B and saving B merges A's extracted data. Fix = key the cache to the RO id. Re-confirmed at HEAD a405b7a. |
| `d94a418a` | Brandon | 07-27 | General UI/UX | **data-display** | `js/render.js` L553 (RO Status), L563 (Customer Comm); `js/ro-crud.js` L143 | The card note boxes render via `split('\n').map(escapeHtml).reverse()`, reversing EVERY line, so a multi-line note reads bottom-line-first - exactly Brandon's "latter portion near the top". Entry boundaries are already lost upstream. Reuse the S159 history-modal entry-regex split. Re-confirmed at HEAD a405b7a. |
| `d4d0eeba` | Brandon | 07-22 | General UI/UX | **small (navigation)** | `index.html` scrollToROInBoard L4234-4250; `js/qr.js` handleDeepLink L198 (L207, L210); `js/work-list.js` L350 | Two paths, one root cause. Work list: only already-rendered `[data-ro-index]` cards match, else a toast whose Clear Filters action re-renders but never scrolls. Messages: the deep link returns silently when the RO is filtered out. Canonical for `9031e101` and `1bc33dd6` - ONE fix closes all three. |
| `5ca724ff` | Lynn | 08-06 | General UI/UX | **small (UX)** | `js/work-list.js` dragstart L415, dragover L421; contrast `js/work-orders.js` L482 | Desktop reorder IS wired, but `dragstart` never populates `e.dataTransfer` - no `setData`, not even the `effectAllowed` the WO drag sets - and there is no `dragenter` preventDefault, so Safari/Firefox desktop refuse the drag while the bespoke touch path works. Saved priority is intact. Needs a desktop browser repro. Re-confirmed at HEAD a405b7a. |

---

## NEEDED - open enhancements by theme (28)

### RO Fields & Status (7)

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `0e172b2c` | Sofia | 07-16 | **M** | `js/duplicates.js` L35, L36, L40, L44 | Merge engine exists but groups strictly on the `PRVS-XXXX-XXXX` base RO id, so two ROs under different spellings of the same customer are never detected. Needs name/phone account dedup plus a verify-which-is-correct step. One-off cleanup is `3f254929`. |
| `5fbfa299` | Brandon | 06-30 | **M** | `js/ro-crud.js` archiveROInSupabase (S171 rewrote this to the `archive_single_ro` RPC) | Only the cashiered archive path exists; no soft-archive flag, status or board filter for quoted-but-never-progressed units. Needs a flag, a board filter and an un-archive path. |
| `44419de9` | Lynn | 08-12 | **S** | `js/config.js` STATUS_PROGRESS_MAP L151-164; index.html select L7038-7051, filter btns L1085-1096, inline maps L1579/L4489; `js/config.js` ES L253-262; `js/render.js` L253/L451, statusColorMap L727-739, ALL_STATUSES L741-745 | NEEDED - none of NEED ESTIMATE / AWAITING ESTIMATE / ESTIMATE DECLINED / JOB DECLINED-CLOSE exist at HEAD a405b7a. Each add must hit 9 hardcoded spots PLUS both DB CHECKs per the S172 lesson, or saves throw 23514. Overlaps `972af03c` CANCELED - confirm scope with Lynn; bundle with `972af03c` + `3e746c18`. |
| `972af03c` | Lynn | 07-17 | **S** | `js/config.js` STATUS_PROGRESS_MAP L151-164 | No Canceled/Cancelled RO status. Add the status plus color/priority maps, exclude from on-lot and queue counts, keep the record for notes. Canonical for `eb00a68d`. |
| `3e746c18` | Lynn | 08-03 | **S** | `js/config.js` L151-164; `js/render.js` L656-658, L718-721 | No TOTALED OUT status. Distinct value from `972af03c`'s Canceled, but ONE build should add both terminal statuses and exclude both from the counts. |
| `ac8265c8` | Lynn | 08-05 | **S** | `js/ro-crud.js` create / edit RO field blocks | No mileage/odometer anywhere in the repo. One `repair_orders.mileage` column plus the New/Edit RO inputs and an optional card readout. Bundle with `3e746c18` + `972af03c`. |
| `50175fce` | Lynn | 07-27 | **S** | `js/ro-crud.js` urgent_update write + notify L380; `js/render.js` L324 | `urgent_update` is writable only from New/Edit RO; the card banner is read-only and even says "change it via Edit RO". Add an inline edit-in-place at the tile bottom that still fires `urgent_update_notify`. Follow-on to `a7d1474e`. |

### Messaging & E-sign (7)

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `0448de62` | Sofia | 06-11 | **L** | `customer-checkin.html` signature pad; `send-quote-email` raf_copy L386 | Signing is kiosk-only; the raf_copy branch just emails a copy after signing. Needs a tokenized public signing page, a delivery path and signed-form storage. |
| `ca0f7ed9` | Brandon | 08-03 | **S** | `messages.html` renderLeadInfo L1389, call site L1744 | `renderLeadInfo` reads only the conversation's own columns, so an existing dashboard customer shows "No RV or service type captured yet". `selectConversation` already computes phone-matched live ROs as `linked` but does not pass them. Small: pass `linked`, fall back to RO rv. |
| `5fec3eca` | Sofia | 07-15 | **S** | `send-quote-email` raf_copy destructure L387 | The raf_copy payload has no `customerAddress`; the address appears only in the `lead_staff_notify` branch. Add it to the payload and both templates. |
| `9aab7550` | Brandon | 07-21 | **S** | `messages.html` `.unread-dot` CSS L469, condition L1313, render L1319, L1980 | S166's manual "Mark unread" button helps, but the visual treatment is still a single 8px gold dot with no row-level highlight. Add a bold/tinted unread row. |
| `21fc3a9d` | Brandon | 07-21 | **S** | `messages.html` fixed rem sizing | No user text-size control anywhere on the page. Add a persisted font-scale control for the conversation list. |
| `f1e6e311` | Brandon | 07-21 | **S** | `messages.html` convoSearch L604, resets L1151/L1170 | Plain text input with no clear affordance; the field is only reset programmatically. Add a clear (x) button that empties the search and re-runs the filter. |
| `6e9ca4dc` | Brandon | 07-21 | **S** | `js/messaging.js` L369/L350/L374; `messages.html` media query L565 | "Added to every send:" is hidden ONLY inside the mobile max-width media query, so desktop messages.html and the RO-card composer still show it. Remove it or toggle it in all viewports; the signature itself keeps sending. |

### Notifications & Task-Tracking (3)

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `5925fc01` | Lynn | 04-09 | **L** | `send-manager-report`; `js/work-orders.js` L293; `js/ro-crud.js` urgent_update_notify L380 | PARTIAL. The daily in-progress digest and single-event notifies ship. Still absent: notify-on-every-RO-update and to-do scraping from status notes (`ro_tasks` / `task_tracker` / `customer_task` return zero hits). Anchor of this theme; pairs with `af14b3cb`. |
| `af14b3cb` | Lynn | 05-21 | **L** | `js/shop-tasks.js` (no ro_id, no reminder column) | The shop checklist is non-customer and has no RO link. Needs a per-RO tasks table, an add/complete UI and a daily open-task reminder. Pairs with `5925fc01`. |
| `a03677e6` | Brandon | 08-07 | **S** | `js/render.js` L553 vs L563; writers at `process-scheduled-notifications` L189/L217, `index.html` L3141/L3179, `js/ro-crud.js` L303/L345/L380/L573/L584, `js/parts.js` L656/L857 | Machine audit lines (NOTIFICATION SENT/FAILED/SCHEDULED/CANCELLED, key-date and urgent-update rows, parts notes) are all written as notes type `ro_status`, so they crowd RO Status Updates. A `customer_comm` stream already renders separately. Small: retarget the type. **Now filed twice** - Lynn's `2deb8f30` (08-13) folds in here, and she asks for a SEPARATE view rather than Brandon's Customer Comms destination. NOTE Roland reversed exactly this retarget for LEAD NOTES in v1.491 - settle destination and scope with him before building. |

### Parts & Inventory (3)

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `14f35f95` | Lynn | 04-09 | **XL** | `js/parts.js` (per-RO line items only) | No inventory/stock concept anywhere. New subsystem: inventory table, receive/consume/adjust flows, a searchable priced stock view, and links from the parts-request path. Largest item on the board. |
| `b19975d4` | Lynn | 04-09 | **L** | `js/config.js` PART_STATUSES L200; `js/parts.js` pf_returnDeadline L226 | Partial infra only - a 'Returned' status and a Return Deadline input, but no RMA number, return reason, supplier credit or reverse-direction flow. Equals GH#24 Shop Ops Phase 2. Canonical for declined dup `86bbaf50`. |
| `8d885331` | Lynn | 04-09 | **S** | `js/parts.js` pf_supplier L181, pf_partName L190, pf_serviceSilo L208, pf_returnDeadline L226 | Form is unchanged; the ask is only to reorder the fields. Quick once Lynn supplies the target layout. **Blocked on detail, not on effort.** |

### Board & Navigation UX (3)

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `29654af7` | Lynn | 07-06 | **S** | `index.html` / `js` / `css` - no scroll-to-top control | Board has only targeted `scrollIntoView` calls. Add a scroll-triggered back-to-top / jump-to-search control, ideally in the sidebar layout too. Canonical for dup `2b78696c`. |
| `d1844e46` | Lynn | 07-16 | **S** | `js/render.js` calculatePriority L30-39, shop pin L35-37 | Board sorts on one blended priority score; there is no selectable strict promised-then-oldest order. Add the strict sort option while keeping the shop pin. Related to `1aeb3f58`, which delivered only the chip. |
| `d432863a` | Brandon | 07-21 | **S** | all repo `*.html` heads | No favicon or apple-touch-icon on any page, so every tab shows the generic browser icon. `logo.png` already ships but needs resizing to icon dimensions. |

### Time Clock & Check-In (2)

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `e06d747e` | Riley | 04-10 | **S** | `js/render.js` compact-col-actions L228-235 | No floating/sticky check-in control on any page; the compact actions row has edit/parts/WO/notify/message/schedule only. Underspecified - scope the target page and behavior with the submitter. Pairs with `6bb61db2`. |
| `6bb61db2` | Riley | 07-22 | **S** | `js/render.js` L228-235; `js/time-tracking.js` | Same missing compact check-in/out action, plus no login-time "you are clocked into X" banner anywhere. Add both. Pairs with `e06d747e`. |

### Work Orders & Shop Tasks (1)

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `2f752d40` | Jason Rubin | 07-21 | **S** | `js/work-orders.js` woFormDollar L360, woFormEstHours L365, save L649-650, dollarLocked L324 | Dollar and hours are independent manual inputs; no labor rate exists in the module (`js/insurance.js` laborRate L212 is the per-claim INSURANCE rate). Auto-populate `dollar_value` = effective hours x $195/hr, respecting the dollarLocked gate. |

### Auth & Cost (1)

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | **XL** | `js/auth.js` L69, L203-204, One Tap + signInWithIdToken | Still fully Google-based, kiosk included. Swapping to Supabase password / magic-link or shared role accounts drops roughly $500/mo but touches every page sign-in plus the email-keyed RBAC. Scope with Roland. |

### Scheduling & Dates (1)

| ER | By | Date | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|
| `47428f75` | Lynn | 08-03 | **S** | `time-off.html` L368, L372, L752 | UNDERSPECIFIED - "update vacation/sick/personal calendar", no stated defect or target. All three types, a color-coded calendar, per-row Edit and exact partial-day windows already ship. Filed Needed rather than Done because the ask maps to no gap without Lynn's detail; if it restates `ee50d11c` it is already delivered. |

---

## DATA - corrections for Roland to run (2)

Nothing below was executed by this runner.

| ER | By | Date | Verdict + SQL |
|---|---|---|---|
| `6938c1af` | Ryan Dillon | 05-06 | Already applied in the live DB (tech names read from `staff.name`; submitter "Rod Wombles" now spells correctly in this very table). RESIDUAL, not a runtime bug: `supabase/migrations/staff_table.sql` L85-87 still seeds Rod/Zak/Travis as "Wimbles", so a re-seed would reintroduce it. Re-apply if ever needed:<br>`UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |
| `3f254929` | Sofia | 07-16 | Keep Steve Huebert, remove the misspelled Hubert RO. Eyeball first:<br>`SELECT id,ro_id,customer_name,status FROM repair_orders WHERE customer_name ILIKE '%ubert%';`<br>then, once the wrong id is confirmed:<br>`UPDATE repair_orders SET deleted_at=now() WHERE id='<wrong-id>';`<br>The merge FEATURE itself is tracked as `0e172b2c`. |

---

## DUPLICATES - fold and decline (11)

| ER | By | Date | Folds into | Note |
|---|---|---|---|---|
| `aa508852` | Riley | 04-09 | `fd6c122d` | Identical text and timestamp. Already declined. |
| `86bbaf50` | Lynn | 05-13 | `b19975d4` | Gordon window returned to Vogt = supplier return tracking. Already declined. |
| `67daba15` | Brandon | 06-09 | `d2561e11` | RO completion / pick-up date. Already declined. |
| `fa1c1d6e` | Sofia | 06-09 | `a22739b9` | Sheldon Gilbert check-in email. Already declined. |
| `5057c7fc` | Roland | 06-09 | `91a03260` | Prune cashed-out RO from work list. Already declined. |
| `fc6b29f9` | Sofia | 06-17 | `298e0e67` | Retest report of the duplicate-RO guard. Already declined. |
| `9031e101` | Riley | 07-21 | `d4d0eeba` | iPhone work-list tap does not reach the unit. Declined on 08-09 per the last report. |
| `1bc33dd6` | Brandon | 07-27 | `d4d0eeba` | Same scroll-to-customer defect Brandon filed 07-22. Declined on 08-09. |
| `2b78696c` | Lynn | 07-27 | `29654af7` | Same back-to-top ask Lynn filed 07-06. Declined on 08-09. |
| `eb00a68d` | Lynn | 08-03 | `972af03c` | Same CANCELED status ask Lynn filed 07-17. Declined on 08-09. |
| `2deb8f30` | Lynn | 08-13 | `a03677e6` | **NEW this run, the only row triaged.** "Store RO notifications somewhere other than RO status, it is cluttering the tech updates on the status. Keep in a separate location view." Same defect Brandon filed 08-07: machine audit lines write as notes type `ro_status` (index.html L3141/L3179; `process-scheduled-notifications` L189/L217; `js/ro-crud.js` L303/L345/L380/L573/L584; `js/parts.js` L656/L857) and so crowd the tech-facing RO Status Updates box at `js/render.js` L553. Only the destination differs - Lynn wants a NEW separate view, Brandon wants the existing `customer_comm` stream (L563). Folded into `a03677e6`; **still `unreviewed` - recommend declined.** |

---

## DONE - delivered (53)

Rows below carry their prior verdicts; only `09084bc5` changed bucket since
the last report.

| ER | By | Date | Category | Mapped code | Verdict |
|---|---|---|---|---|---|
| `09084bc5` | Lynn | 08-04 | General UI/UX | `js/ro-crud.js` archiveROInSupabase (single `archive_single_ro` RPC); `cashiered_full_detail_s171.sql` archive_one_ro; `closed-ros.html` v1.3b restore | **FIXED since the last report - bucket moved bug -> done on the 08-09 run.** The 26-column allowlist that dropped `photo_library`, the key dates, the keys-power fields and `urgent_update` is gone; one shared server-side body now writes all 7 columns plus all seven child mirrors before the delete, and reactivate restores them. Shipped v1.494 + hotfix `1c64c88`. DB `status=done`. |
| `33750cb3` | Roland | 04-09 | General UI/UX | - | Test entry ("Testing out the Make-A-Wish function"). Not a real request. Already declined; keep declined. |
| `19dc2016` | Roland | 04-11 | Time Clock | - | Test entry ("Testing ER form / dictation"). Not a real request. Already declined; keep declined. |
| `79b2c805` | Sofia | 04-09 | New Feature | `closed-ros.html` reactivateRO; `js/photos.js` L253 | Cashed-out ROs reactivate with parts/time_logs/notes/audit_log/insurance_scans plus the WO subtree, and documents can be added to the reopened RO. The photo/doc-library gap noted in prior reports is now closed by the S171 fix to `09084bc5`. |
| `75124b03` | Ryan | 04-09 | RO Management | `js/ro-crud.js` warranty branch; `js/render.js` L121/L195/L372 | Warranty is a first-class RO type at creation with board filter, compact chip and card badge (plus the $0 No-Charge sub-badge). |
| `b2aa4bea` | Lynn | 04-09 | New Feature | `js/ro-crud.js` create/edit; `js/render.js` L395 | `planned_dropoff_date` persists on create and edit, drives the blue Drop chip and the day-before reminder. Same field as `468aa376`. |
| `18611eee` | Lynn | 04-09 | Bug Report | `js/work-list.js` L195/L210/L223/L263/L304/L383 | Work-list members are viewable, reorderable and removable, with per-silo tabs. The DESKTOP drag half Lynn now reports is the open Bug `5ca724ff`. |
| `17492066` | Lynn | 04-09 | Notifications | `scheduled_notifications.sql`; `process-scheduled-notifications`; `index.html` snRepeat L2993 | The per-RO Schedule Notification bell sets a future date/time, message and recipients with daily/weekly repeat - covers the advance-warning ask. |
| `fd6c122d` | Riley | 04-09 | Work List | `js/work-list.js` L195/L304/touch handlers | Delete plus mouse and touch drag-reorder, persisted, with the mobile sizing fixed. Canonical for declined dup `aa508852`; the desktop regression is Bug `5ca724ff`. |
| `98bd7498` | Brandon | 04-09 | RO Management | `js/work-list.js` L223/L263 | Per-silo Work List tabs give a separate list per job category. |
| `1fe68261` | Rod | 04-13 | Work Orders | `js/shop-tasks.js` L21/L130/L153/L180; `shop_tasks.sql` | Internal work not tied to a customer unit is tracked separately from ROs and the board. Matches the poly-barrel / roof-bay examples. |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | `js/config.js` L158; `js/render.js` L727/L741 | "Ready to Work" is a live RO status in the maps and both status dropdowns. |
| `b87eb2fb` | Lynn | 04-14 | RO Management | `keys_power_fields.sql`; `js/ro-crud.js` create/edit | key_status, keypad_code and keep_plugged_in persist on create and edit with a keys chip on the card. Same build as `34fc03c2`. These are three of the seven columns S171 restored to the archive path. |
| `a5ff3e2d` | Lynn | 04-21 | RO Management | `js/ro-crud.js` warranty_repair; `js/render.js` L122/L196/L373 | "Warranty + Repair" exists end to end and stays billable so the repair half bills normally. |
| `a7d1474e` | Lynn | 04-23 | RO Management | `urgent_update_field.sql`; `js/render.js` L324/L214; `js/ro-crud.js` L380 | Always-visible red URGENT banner plus compact siren, and setting it notifies silo managers and admins. Lynn's follow-up to move the INPUT to the tile is open ER `50175fce`. |
| `dde90d2b` | Roland | 04-27 | RO Management | `customer-checkin.html` L2530/L2603 | Photo is optional and both the authorization-form copy and the staff notice are forced on every submission. |
| `13e1cd87` | Tipton | 04-30 | Time Clock | `checkin.html` L1516 | "Return to Dashboard" renders after clock-out, so no new tab is needed to clock into the next job. |
| `e27311bf` | Ryan | 05-04 | Work Orders | `js/work-orders.js` _initWOTaskDrag L461, L482 | Every WO task row has a grip handle and drag-to-reorder; DOM order drives sort_order, so templates are edited by load, reorder, Overwrite. |
| `d2561e11` | Brandon | 05-06 | New Feature | `key_dates_pickup_date.sql`; `js/ro-crud.js` create/edit; `js/render.js` L397 | `pickup_date` persists and shows as the green Pickup chip and in the key-dates panel. Canonical for declined dup `67daba15`. |
| `dac9fdda` | Brandon | 05-06 | General UI/UX | `js/config.js` L154; `js/render.js` L718-721 | "Off Lot - Returning" separates units temporarily taken by the customer from units never on the lot, and is excluded from the on-lot count. S172 widened the DB CHECK so the status can actually be saved. |
| `a8e90a7d` | Brandon | 05-20 | General UI/UX | `js/parts.js` L152-161 | Manage Parts Ticket Totals bar sums parts, freight and total cost. Same build as `671fa623`. |
| `1aeb3f58` | Lynn | 05-21 | General UI/UX | `js/render.js` L396, L30-39 | Promised date is a large bold chip, red when overdue, and both promise date and days-on-lot feed the blended sort. The strict selectable sort is open ER `d1844e46`. |
| `cbc70a86` | Lynn | 05-21 | General UI/UX | `customer-checkin.html` L2381; `work_list_autoadd_rpc.sql` | Returning check-in calls `add_ro_to_silo_work_lists`, inserting `manager_work_lists` rows per involved silo lead with a sr-manager fallback. |
| `3f415c88` | Lynn | 05-26 | General UI/UX | `index.html` snRepeat L2993, read L3061 | Repeat none/daily/weekly plus repeat-until expands into one individually cancellable row per occurrence. |
| `cd663d8f` | Bobby | 05-28 | General UI/UX | `js/ro-crud.js` L1097; `js/parts.js` L307/L310 | Currency inputs show 2 decimals on edit-populate and in the parts modal. Cosmetic only - no data loss ever occurred. |
| `34fc03c2` | Brandon | 05-29 | New Feature | `keys_power_fields.sql`; `js/ro-crud.js` create/edit | The Keys selector is on New and Edit RO with a keys chip on each card. Covers `b87eb2fb`. |
| `0b2e128c` | Lynn | 06-02 | General UI/UX | `checkin.html` L1455 | Clock-out work notes are copied into the RO update timeline as a `ro_status` note. |
| `468aa376` | Lynn | 06-02 | General UI/UX | `js/ro-crud.js` create/edit; `js/render.js` L395 | Planned drop-off date input on New and Edit RO, feeding the Drop chip and auto_dropoff_reminder. Same field as `b2aa4bea`. |
| `2b814250` | Roland | 06-07 | Calendar | `js/render.js` L395-397/L367; `sync-ro-calendar` | Key dates render as attention-grabbing chips plus a hover/tap panel, calendar integration runs server-side and idempotently, advance reminders go to managers/admins. |
| `cd084233` | Roland | 06-07 | Work List | `send-manager-report`, `send-admin-pnl-report`, `worklist-report.html` | Admin-level daily manager-worklist reporting with parts, labor and cost rollups. The manager scoring layer described at the end of the request is a future extension, not this ER. |
| `a22739b9` | Sofia | 06-08 | General UI/UX | `customer-checkin.html` L2530/L2603 | Notify-gate regression fixed - both sends forced on every submission. Canonical for declined dup `fa1c1d6e`. |
| `91a03260` | Roland | 06-09 | Work List | `prune_cashiered_worklists.sql` | Removes an RO from manager work lists the day AFTER Delivered/Cashed Out, deliberately keeping it visible on the day of cash-out. Canonical for declined dup `5057c7fc`. |
| `cdd77a8b` | Brandon | 06-10 | General UI/UX | `js/render.js` L656-658, L718-721 | Queue count drops Shop Operations ROs and the on-lot count excludes Not On Lot / Scheduled / Off Lot - Returning, so the tile shows a true on-lot number. |
| `2c3d5633` | Ryan | 06-12 | General UI/UX | `js/photos.js` L66 | The upload input is attached to the DOM before clicking - the iOS Safari dropped-change-event root cause. Phone photo upload completes. |
| `74a33621` | Brandon | 06-16 | General UI/UX | `js/photos.js` L396-397/L400/L404 | Lightbox supports arrow-key navigation plus Escape-to-close and prev/next buttons. |
| `298e0e67` | Sofia | 06-16 | General UI/UX | `customer-checkin.html` L2059/L2165, L2060/L2194 | The returning-customer guard reuses an existing scheduled RO, and v1.17 adds a blocking attach-vs-new dialog for the already-active case. Canonical for declined dup `fc6b29f9`. |
| `9b823d25` | Roland | 06-16 | RO Management | `js/work-orders.js` L244/L293; `service_added_notify_source.sql` | Adding a service to an RO emails the owning silo manager. Matches the roof-plus-water-heater example. |
| `9b046808` | Brandon | 06-17 | General UI/UX | `js/config.js` L157; `js/render.js` L718-721 | "Scheduled" is its own status and is excluded from days-on-lot stats and the on-lot count. |
| `671fa623` | Brandon | 06-18 | General UI/UX | `js/parts.js` L153/L155/L159-161 | Freight is summed across parts and rolled into Total Cost. Same build as `a8e90a7d`. |
| `5c45bb8d` | Sofia | 06-19 | General UI/UX | `customer-checkin.html` L2059/L2165/L2194/L2530 | Both halves of the David Flanagan report resolved - the Scheduled path reuses the RO and the authorization copy is forced on every submission. |
| `b06a285f` | Lynn | 06-19 | General UI/UX | `js/render.js` L367, L363-365, L395-397 | A calendar icon by the customer name opens a key-dates panel listing drop-off, promised and pickup in full, with colored summary chips. |
| `ee50d11c` | Lynn | 06-19 | General UI/UX | `time-off.html` L752 | Time-off entries are editable, not delete-only - Edit loads the entry back into the form and flips submit to "Update Record". |
| `88395acc` | Lynn | 06-19 | General UI/UX | `add_absence_window_to_time_off.sql`; `time-off.html` L368/L372 | Partial-day absence is an exact clock-out-to-return window rather than an hour total. |
| `80390f36` | Lynn | 06-23 | General UI/UX | `js/render.js` L35/L37/L61 | Shop Operations tiles pin to the top ahead of the priority sort, and a "shop operations shop" search token makes the tile findable. |
| `7f2d0d70` | Lynn | 06-26 | General UI/UX | `send-dropoff-report` | The Monday drop-off check email lists this week's scheduled drops plus last week's arrivals and flags any not yet on the right team's work list. Pairs with `cbc70a86`. |
| `3f7fb4fc` | Brandon | 06-29 | General UI/UX | `js/render.js` L656-658/L718-721/L249 | Shop ROs are excluded from both counts and the shop status dropdown is replaced by a static label. |
| `cb7742a8` | Lynn | 06-29 | General UI/UX | `sync-ro-calendar`; `customer-checkin.html` L2240 | Drop-off/promised/pickup events are posted server-side to the correct team calendar for every RO including front-desk check-ins. Fixes the repair-calendar miss. |
| `39b75fab` | Sofia | 07-09 | General UI/UX | `js/photos.js` canManageFiles L618, L198-201, L376 | The Documents/Photos library supports rename, move-to-another-RO and delete, office-staff gated. |
| `1c7f1410` | Andrew Page | 07-16 | New Feature | `js/messaging.js` L347/L290/L295/L428-434/L386-392/L578-579 | Photo/PDF attach shipped S151b/S158 and video shipped S166; `_msgAttachments` is capped at 5 with removable chips and the send path splits video links from MMS media. **DB row is now `status=done`** - the S171 paperwork closed it, no status action remains. |
| `9d3806e7` | Brandon | 07-22 | General UI/UX | `messages.html` L671/L669/L1599/L1624; `customer_note_entries_s154.sql` | The details pane has a customer-side Notes section plus an Add-Notes modal writing append-only rows keyed by phone. **DB row is now `status=done`** - the S171 paperwork closed it, no status action remains. |
| `ac394468` | Roland | 07-24 | General UI/UX | `js/enhancement.js` pageDefaults L43 | The erCategory select carries Messages/Texting plus Solar, Leads, Closed ROs, Reports, Time Off and Employee Guide. RESIDUAL, stated not hidden: erCategory exists in only 6 live HTML files (analytics, checkin, closed-ros, index, solar, worklist-report), so messages/leads/time-off/guide cannot RAISE an ER - worth its own small ER. |
| `d62a26e4` | Roland | 07-24 | New Feature | `js/messaging.js` L290/L295/L347/L386-392/L578-579 | Multi-attach up to 5 with removable chips; the send path splits video links from MMS media because a carrier payload cap silently stripped multi-media sends. |
| `93b00023` | Roland | 07-24 | New Feature | `textly-webhook` v1.4 L487/L514-523/L617; `send-unreplied-reminder`; `unreplied_eod_reminder_s158.sql` | All three parts ship: inbound reopen with a `reopened` event row, owner notify only on the needs-reply flip, and the 4:30pm unread sweep. v1.4 disabled only the UNASSIGNED blast, which is not this ask. |

## Not triaged (1)

| ER | By | Date | DB status | Note |
|---|---|---|---|---|
| `f73a1e5a` | Roland | 04-13 | declined | "Testing dictation 123 testing enhancement request dictation" - not present in `needs_triage_ids` on any run so far, so it still carries no `triage_bucket` and no verdict. Reads as a test entry, already declined; worth including in one future full pass purely to give it a recorded verdict. |

---

## Recommended action today

1. **Nothing is on fire.** With `09084bc5` shipped in v1.494, the board has no
   LARGE-severity item left. The remaining 4 bugs are one data-integrity and
   three cosmetic-to-small defects.
2. **`225a2535` is now the top bug** - key `window._lastEstimateScan` to the
   RO id instead of `mode` (and stop `closeEditModal` from preserving a
   foreign scan). It is the only remaining defect that writes one customer's
   data onto another's RO. One-line class of fix, no UI work.
3. **Batch the three cheap render/nav bugs into one branch:** `d94a418a`
   (stop reversing every line in the two note boxes at `js/render.js`
   L553/L563), `d4d0eeba` (clear filters, re-render, then scroll and
   highlight - closes `9031e101` and `1bc33dd6` too), and `5ca724ff` (set
   `dataTransfer` + a `dragenter` preventDefault on the work-list desktop
   drag at `js/work-list.js` L415). All three re-confirmed present at HEAD
   a405b7a; all three are self-contained.
4. **The RO-status build is now the biggest single win and needs one scoping
   call with Lynn first.** `44419de9` (4 new statuses), `972af03c` (Canceled),
   `3e746c18` (Totaled Out) and `ac8265c8` (mileage) all touch the same status
   maps, count exclusions and New/Edit RO blocks - and `44419de9`'s
   JOB DECLINED/CLOSE overlaps `972af03c`'s CANCELED, so build the final list
   with her rather than shipping near-synonyms. **Heed the S172 lesson: every
   new status value must widen BOTH the `repair_orders` and `cashiered` CHECK
   constraints or saves throw 23514** - v1.460 added `Off Lot - Returning` to
   all 7 UI spots and left it unsaveable for two months. `5fbfa299` (soft
   archive, M) rides along in the same area.
5. **`a03677e6` has now been filed twice** - Brandon 08-07 and Lynn 08-13
   (`2deb8f30`). Two independent submitters reporting the same clutter raises
   its priority, but the two ask for different destinations: Brandon wants the
   existing Customer Comms stream, Lynn wants a NEW separate view. Get
   Roland's call on the destination before building, especially since he
   reversed exactly this retarget for LEAD NOTES in v1.491.
6. **One messaging-polish build still covers five S items:** `f1e6e311`,
   `21fc3a9d`, `9aab7550`, `6e9ca4dc` and `ca0f7ed9` are all confined to
   `messages.html` plus `js/messaging.js`.
7. **Paperwork, one row only:** recommend `status=declined` on `2deb8f30`
   (folded into `a03677e6`). Everything recommended on 08-08 has already been
   applied - thank you.
8. **Unblock the two underspecified S's** by asking the submitters: Lynn's
   parts field order (`8d885331`) and time-off ask (`47428f75`), plus Riley's
   check-in float (`e06d747e`). All are small once specified.
9. **Plan, do not rush, the XLs:** the auth swap (`ed1b873e`) and parts
   inventory (`14f35f95`) each deserve their own scoping session.
