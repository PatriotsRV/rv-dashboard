# Enhancement Request Triage -- 2026-07-22 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-07-22T09:28Z).
**Codebase mapped against:** current `pre-prod` tree at **index.html v1.480** (messages-v1.11, checkin/customer-checkin current).
**Total ERs:** 78. **Triaged this run:** 77 ids in `needs_triage_ids`; 1 carried over unchanged (`f73a1e5a`, a declined test entry). Verdicts verified by grepping/reading the live code, not assumed.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

## Board snapshot (current DB status)

| Status | Count |
|---|---|
| done | 46 |
| declined | 9 |
| unreviewed | 23 |
| **Total** | **78** |

## This run's buckets (77 triaged)

| Bucket | Count |
|---|---|
| done (delivered / bug resolved / test) | 47 |
| needed (open enhancement) | 20 |
| duplicate | 7 |
| data (SQL, not code) | 2 |
| bug (still exists) | 1 |

## Headline

- **One item flipped Needed -> Done this pass:** `1c7f1410` (Andrew, "attach media to messaging"). The prior run marked it Needed/M ("UI attach + storage remain"); the current tree ships the full path in **v1.479 (S151b)** -- `messaging.js` `msgAttachBtn` + file input -> `_compressImage` -> upload to the `message-media` bucket -> `_msgAttachment`, and `sendCustomerMessage` passes `media_url` to `projectblue-send`. Recommend closing it Done.
- **One still-existing bug:** `9031e101` (Riley, iPhone Work-List tap does not navigate to the unit). Mobile-only, small severity. See the BUG section.
- **Seven brand-new ERs arrived 2026-07-21** (`d432863a`, `9031e101`, `9aab7550`, `21fc3a9d`, `2f752d40`, `f1e6e311`, `6e9ca4dc`). Six are small Needed enhancements (favicon, messaging polish x4, WO labor-dollar auto-calc); one is the bug above. All are S-sized except the bug.
- The remaining 20 open enhancements skew small: **2 XL - 4 L - 2 M - 12 S**. The heavy items are unchanged from S128 (parts inventory `14f35f95` XL, auth swap `ed1b873e` XL).

---

## BUG bucket (front of the line)

| ER | By | Date | Cat | Verdict (severity) |
|---|---|---|---|---|
| `9031e101` | Riley | 07-21 | Work List | **Still exists (mobile, small).** `work-list.js` name-tap -> `scrollToROInBoard` (index.html) only locates cards already rendered on the board; if the tapped RO is filtered out or off-screen on iPhone it shows an "RO not visible" toast instead of navigating. Needs a mobile repro; fix = ensure the tapped RO is unfiltered and scrolled to. |

---

## DATA bucket (SQL for Roland -- runner does not execute)

| ER | By | Date | Fix |
|---|---|---|---|
| `6938c1af` | Ryan | 05-06 | Wimbles -> Wombles in tech assignment; already applied per S117. If any remain: `UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |
| `3f254929` | Sofia | 07-16 | Keep "Steve Huebert", remove the misspelled "Hubert" RO. Find first: `SELECT id,customer_name,status FROM repair_orders WHERE customer_name ILIKE '%hubert%';` then soft-delete the wrong RO after Roland confirms the id. The reusable merge feature is `0e172b2c`. |

---

## DUPLICATE bucket (folded; keep declined)

| ER | By | Date | Canonical |
|---|---|---|---|
| `aa508852` | Riley | 04-09 | = `fd6c122d` (identical delete/too-small/drag text), now Done. |
| `86bbaf50` | Lynn | 05-13 | = `b19975d4` (Gordon window returned to Vogt = supplier return/warranty). Needed/L. |
| `67daba15` | Brandon | 06-09 | = `d2561e11` (RO completion/pickup date), now Done. |
| `fa1c1d6e` | Sofia | 06-09 | = `a22739b9` (Sheldon Gilbert returning check-in did not email), now Done. |
| `5057c7fc` | Roland | 06-09 | = `91a03260` (remove cashed-out RO from worklist day after Delivered), now Done. |
| `fc6b29f9` | Sofia | 06-17 | = `298e0e67` (returning-customer test duplicated the RO), now Done. |
| `5c45bb8d` | Sofia | 06-19 | = `298e0e67` + `a22739b9` (David Flanagan dup + no RAF email), both now Done. |

---

## DONE bucket (delivered / bug resolved / test -- close these)

| ER | By | Date | Cat | Evidence in current code |
|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | UI/UX | Test entry ("Testing Make-A-Wish"); keep declined. |
| `19dc2016` | Roland | 04-11 | Time Clock | Test entry ("Testing ER form / dictation"); keep declined. |
| `f73a1e5a` | Roland | (carry) | UI/UX | Test entry ("Testing dictation 123"); already declined; not re-triaged. |
| `79b2c805` | Sofia | 04-09 | Feature | `closed-ros.html` `reactivateRO` restores parts/time_logs/notes/photos so cashed-out customers reopen to edit/add docs. |
| `75124b03` | Ryan | 04-09 | RO Mgmt | Warranty RO type: index.html `warrantyROBtn` -> `ro-crud.js` `setROType`; render.js badge/chip. |
| `b2aa4bea` | Lynn | 04-09 | Feature | `planned_dropoff_date` persisted (ro-crud.js); New RO + kiosk fields; morning-before reminder + Drop chip. Same intent as `468aa376`. |
| `18611eee` | Lynn | 04-09 | Bug | Resolved: `work-list.js` add/remove/`_setWorkListSilo` + drag reorder persist to `manager_work_lists`. |
| `17492066` | Lynn | 04-09 | Notif | `scheduled_notifications` insert (index.html) + `process-scheduled-notifications` cron fire ahead-of-date reminders. |
| `fd6c122d` | Riley | 04-09 | Work List | Resolved: `work-list.js` remove btn + draggable reorder (desktop+touch); mobile layout. Canon for `aa508852`. |
| `98bd7498` | Brandon | 04-09 | RO Mgmt | `work-list.js` `_renderWorkListSiloTabs`/`_setWorkListSilo` filter by silo; one RO on multiple silo lists. |
| `1fe68261` | Rod | 04-13 | Work Orders | `shop-tasks.js` addShopTask/complete/reopen/delete -- standalone tasks not tied to an RO. |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | "Ready to Work" status + filter; render.js color; in ALL_STATUSES. |
| `b87eb2fb` | Lynn | 04-14 | RO Mgmt | Keys select + keypad code on RO modal; ro-crud.js persists `key_status`/`keypad_code`. Same build as `34fc03c2`. |
| `a5ff3e2d` | Lynn | 04-21 | RO Mgmt | `ro_type` `warranty_repair` on New/Edit; filter; render.js badge/chip. Distinct from Hybrid. |
| `a7d1474e` | Lynn | 04-23 | RO Mgmt | `urgent_update` field + ro-crud.js `_notifyUrgentUpdate` emails managers/admins; `urgent_update_field.sql`. |
| `dde90d2b` | Roland | 04-27 | RO Mgmt | customer-checkin.html photo optional + RAF email forced on every submission. |
| `13e1cd87` | Tipton | 04-30 | Time Clock | Resolved: checkin.html `showClockOutSummary` "Return to Dashboard" button (no new tab). |
| `e27311bf` | Ryan | 05-04 | Work Orders | `work-orders.js` `_initWOTaskDrag` + `saveWOTemplate` sort_order (drag-reorder templates). |
| `d2561e11` | Brandon | 05-06 | Feature | `pickup_date` (`key_dates_pickup_date.sql`) + green Pickup chip. Canon for `67daba15`. |
| `dac9fdda` | Brandon | 05-06 | UI/UX | "Off Lot - Returning" status + filter; render.js excludes from on-lot count. |
| `a8e90a7d` | Brandon | 05-20 | UI/UX | `parts.js` "Ticket Totals" (Parts/Freight/Total/Retail). Same feature as `671fa623`. |
| `1aeb3f58` | Lynn | 05-21 | UI/UX | `utils.js` `calculatePriority` blends promised buckets + daysOnLot; chips. Strict sort = open `d1844e46`. |
| `cbc70a86` | Lynn | 05-21 | UI/UX | customer-checkin.html rpc `add_ro_to_silo_work_lists` (`work_list_autoadd_rpc.sql`) inserts silo work-list rows. |
| `3f415c88` | Lynn | 05-26 | UI/UX | index.html `MAX_OCCURRENCES` expands scheduled_notifications (daily/weekly until a date). |
| `cd663d8f` | Bobby | 05-28 | UI/UX | Resolved: ro-crud.js `editDollarValue.toFixed(2)` + parts.js `MONEY_FIELDS`; 425.00 no longer shows 425. |
| `34fc03c2` | Brandon | 05-29 | Feature | Keys/keypad build on RO modal. Same build as `b87eb2fb`. |
| `0b2e128c` | Lynn | 06-02 | UI/UX | checkin.html inserts a "TECH NOTES:" ro_status line transferring clock-out notes into the RO. |
| `468aa376` | Lynn | 06-02 | UI/UX | `plannedDropoffDate` on New/Edit RO + kiosk. Same intent as `b2aa4bea`. |
| `2b814250` | Roland | 06-07 | Calendar | `sync-ro-calendar` edge fn creates/updates/deletes per-(date-type x silo) events via `cal_event_ids`. |
| `cd084233` | Roland | 06-07 | Work List | `send-admin-pnl-report` + `worklist-report.html` + cron (parts/labor/cost/margin). Scoring deferred. |
| `a22739b9` | Sofia | 06-08 | UI/UX | customer-checkin.html `sendRAFEmail` on every drop-off (CC staff); raf_copy attaches signature. Canon for `fa1c1d6e`. |
| `91a03260` | Roland | 06-09 | Work List | `prune_cashiered_worklists()` cron deletes worklist rows day after Delivered. Canon for `5057c7fc`. |
| `cdd77a8b` | Brandon | 06-10 | UI/UX | render.js `statTotalOnLot` sub-line; Scheduled/Off-Lot excluded -> accurate on-lot vs total. |
| `2c3d5633` | Ryan | 06-12 | UI/UX | Resolved: photos.js appends file input before click() (iOS fix) + HEIC accept. Phone upload hardened. |
| `74a33621` | Brandon | 06-16 | UI/UX | photos.js ArrowLeft/Right -> `navigateLightbox`. |
| `298e0e67` | Sofia | 06-16 | UI/UX | customer-checkin.html `reusedScheduledRO` updates the Scheduled RO instead of inserting. Canon for `fc6b29f9`,`5c45bb8d`. |
| `9b823d25` | Roland | 06-16 | RO Mgmt | work-orders.js `addServiceToRO` enqueues `service_added_notify` to the silo's managers. |
| `9b046808` | Brandon | 06-17 | UI/UX | render.js excludes "Scheduled" from days-on-lot + on-lot count. |
| `671fa623` | Brandon | 06-18 | UI/UX | parts.js Ticket Totals include Freight in Total Cost. Same feature as `a8e90a7d`. |
| `b06a285f` | Lynn | 06-19 | UI/UX | render.js `kd-name-cal`/`kd-pop` list key dates beside the name; CSS reveals on hover/focus. |
| `ee50d11c` | Lynn | 06-19 | UI/UX | time-off.html `editingRequestId`/`editRequest` -> UPDATE + per-row Edit button. |
| `88395acc` | Lynn | 06-19 | UI/UX | time-off.html `absence_start_time`/`absence_end_time` from-to range on calendar chips. |
| `80390f36` | Lynn | 06-23 | UI/UX | render.js shop-first sort pins Shop Operations to top + synthetic search token. |
| `7f2d0d70` | Lynn | 06-26 | UI/UX | `send-dropoff-report` edge fn (coming-this-week + received-last-week by silo, work-list flag). Was Needed/M in S128; shipped. |
| `cb7742a8` | Lynn | 06-29 | UI/UX | customer-checkin.html POSTs to `sync-ro-calendar` -> scheduled date lands on the right team calendar. |
| `3f7fb4fc` | Brandon | 06-29 | UI/UX | render.js `cep-static-status`; Shop Operations ROs excluded from queue/on-lot counts. |
| `39b75fab` | Sofia | 07-09 | UI/UX | photos.js `deleteLibraryItem` + `moveLibraryItem` (gated by `canManageFiles()`) fix mis-filed scans. |
| `1c7f1410` | Andrew | 07-16 | Feature | **NOW DONE (v1.479/S151b, overrides prior Needed):** messaging.js `msgAttachBtn` + file input -> `_compressImage` -> `message-media` upload -> `_msgAttachment`; `sendCustomerMessage` passes `media_url` to `projectblue-send`. |

---

## NEEDED bucket (20 open enhancements, by theme)

### Notifications & Task-Tracking
| ER | By | Date | LOE | Verdict |
|---|---|---|---|---|
| `5925fc01` | Lynn | 04-09 | **L** | Partial: the daily in-progress digest is served by the Manager Daily Report (send-manager-report). Still open: notify on every generic RO update + auto-scrape to-dos from status notes into a per-job list. |
| `af14b3cb` | Lynn | 05-21 | **L** | `shop-tasks.js` is a non-customer checklist with no ro_id/customer link or reminder. Needs a per-RO tasks table + add/complete UI + daily open-task reminder. Pairs with `5925fc01`. |

### Notifications & Email
| ER | By | Date | LOE | Verdict |
|---|---|---|---|---|
| `5fec3eca` | Sofia | 07-15 | **S** | `send-quote-email` raf_copy branch (L386) does not emit `customerAddress` (only the lead_staff_notify branch, L549+). Add address to the raf_copy payload + template. |

### Parts & Inventory
| ER | By | Date | LOE | Verdict |
|---|---|---|---|---|
| `14f35f95` | Lynn | 04-09 | **XL** | js/parts.js has no inventory/stock (grep 0); parts are per-RO line items. New subsystem: inventory table + receive/consume/adjust + searchable priced stock + parts-request links. Largest board item. |
| `b19975d4` | Lynn | 04-09 | **L** | config.js PART_STATUSES has "Returned" + return/warranty dates, but no RMA#, credit tracking, return reason, or reverse-direction flow. = GH#24 Shop Ops Phase 2. Canon for `86bbaf50`. |
| `8d885331` | Lynn | 04-09 | **S** | Parts-entry form exists (parts.js); request is only to reorder fields. Quick once Lynn gives the target layout. Blocked on detail, not effort. |

### RO fields / status
| ER | By | Date | LOE | Verdict |
|---|---|---|---|---|
| `5fbfa299` | Brandon | 06-30 | **M** | Only the cashiered archive exists (ro-crud.js `archiveROInSupabase`); no archived/abandoned status or filter for quoted-but-never-progressed units. Pairs with `972af03c`. |
| `972af03c` | Lynn | 07-17 | **S** | No Canceled/Cancelled in render.js ALL_STATUSES/statusColorMap or config.js (grep 0). Add a "Canceled" status + color/priority maps, exclude from counts, keep the record. Pairs with `5fbfa299`. |

### Data Integrity & Merge
| ER | By | Date | LOE | Verdict |
|---|---|---|---|---|
| `0e172b2c` | Sofia | 07-16 | **M** | duplicates.js has an RO-level merge/delete engine (`openDuplicateManager`/`executeDupeMerge`) but groups by base RO id, not customer identity. Needs name/phone account-dedup + verify-which-is-correct merge. One-off cleanup is `3f254929`. |

### Board Sort & Navigation
| ER | By | Date | LOE | Verdict |
|---|---|---|---|---|
| `29654af7` | Lynn | 07-06 | **S** | Only targeted scrollIntoView calls; no page-level scroll-to-top / jump-to-search FAB (grep 0). Add a scroll-triggered back-to-top / jump-to-search button. |
| `d1844e46` | Lynn | 07-16 | **S** | Board uses one blended `calculatePriority` score; shop-ops pinning exists but no selectable strict "promised date then oldest on lot" sort. Add the strict sort option. |

### Messaging
| ER | By | Date | LOE | Verdict |
|---|---|---|---|---|
| `9aab7550` | Brandon | 07-21 | **S** | messages.html already renders a gold unread-dot per conversation, but no row-level highlight for unchecked messages. Add a stronger unread treatment (bold row / tinted background). Prefer Needed over Done -- the ask is more prominence. |
| `21fc3a9d` | Brandon | 07-21 | **S** | messages.html uses fixed rem font sizes (html 17.5px) with no text-size control. Add a persisted font-scale/zoom control for the conversation list. |
| `f1e6e311` | Brandon | 07-21 | **S** | messages.html `convoSearch` is a plain text input with no clear/x button. Add a clear (x) button to reset the search quickly. |
| `6e9ca4dc` | Brandon | 07-21 | **S** | messaging.js `refreshSignaturePreview` renders a persistent "Added to every send:" preview (`msgSigPreview`) under the composer; users find it redundant. Remove or collapse it. |

### Work Orders
| ER | By | Date | LOE | Verdict |
|---|---|---|---|---|
| `2f752d40` | Jason | 07-21 | **S** | work-orders.js has manual `estimated_hours` + `dollar_value` but no auto-calc. Auto-populate `dollar_value` = total hours x $195 (posted labor rate), editable override. |

### Messaging & e-sign
| ER | By | Date | LOE | Verdict |
|---|---|---|---|---|
| `0448de62` | Sofia | 06-11 | **L** | Signing is kiosk-only (on-device pad); raf_copy is a post-signing email, no tokenized public signing page. Needs a tokenized public page + delivery + signed-form storage. |

### Auth & Cost
| ER | By | Date | LOE | Verdict |
|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | **XL** | Still Google-based: auth.js `signInWithIdToken` + One Tap + GOOGLE_CONFIG (33 refs). Swap to Supabase password/magic-link or shared role accounts to drop ~$500/mo; touches every page sign-in + RBAC. Roland to scope. |

### Time Clock
| ER | By | Date | LOE | Verdict |
|---|---|---|---|---|
| `e06d747e` | Riley | 04-10 | **S** | No floating/quick tech check-in button in index.html or checkin.html. Underspecified; scope the page + behavior with the submitter first. |

### UI / Branding
| ER | By | Date | LOE | Verdict |
|---|---|---|---|---|
| `d432863a` | Brandon | 07-21 | **S** | No favicon / apple-touch-icon / rel=icon in any page (grep 0) -- the tab shows a generic icon. Add a Patriots-logo favicon + apple-touch-icon to each page head. |

---

## Recommended action today

1. **Close as Done:** the 45 delivered items above, plus flip `1c7f1410` (messaging media -- shipped in v1.479, still marked unreviewed in the DB). Test entries `33750cb3`/`19dc2016`/`f73a1e5a` stay declined; the 7 duplicates stay declined.
2. **Run the two DATA SQLs** (`6938c1af` staff-name backfill if any remain; `3f254929` remove the misspelled "Hubert" RO after confirming the id). Roland runs SQL; the runner does not.
3. **Fix the one bug** `9031e101` (iPhone Work-List tap -> unit) once reproduced on a phone -- small, mobile-only.
4. **Quick Needed wins (S, well-defined):** the messaging polish cluster (`9aab7550`, `21fc3a9d`, `f1e6e311`, `6e9ca4dc`), the favicon (`d432863a`), the WO labor-dollar auto-calc (`2f752d40`), the RAF-email address (`5fec3eca`), the "Canceled" status (`972af03c`), scroll-to-top (`29654af7`), and the strict board sort (`d1844e46`). Ask Lynn for the parts-field layout (`8d885331`) to unblock that S.
5. **Plan, don't rush, the XLs:** parts inventory (`14f35f95`) and the auth swap (`ed1b873e`) each deserve their own scoping session.
