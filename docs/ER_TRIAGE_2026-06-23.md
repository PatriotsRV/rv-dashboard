# Enhancement Request Triage -- 2026-06-23 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-23T10:38Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.460** (checkin.html v1.38, customer-checkin.html v1.13, time-off.html v1.4).
**Total ERs:** 58 (incl. 3 test entries + 5 exact/near duplicates -> **50 real requests**).
**Triaged this run:** 57 ids in `needs_triage_ids`; n15 (Roland "Testing dictation 123") carried over unchanged.

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `closed-ros.html`, `time-off.html`,
`supabase/functions/*`, SQL migrations) by grepping the source -- not assuming --
and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

**Bucket totals (57 triaged):** DONE 33 - NEEDED 18 - DUPLICATE 5 - DATA 1 - BUG 0.

---

## Headline

The bug bucket is empty and stays empty. Every previously-confirmed bug shipped
and is re-confirmed in the current tree:

- The four **S120 (v1.460)** builds verify in code: service-add manager email
  (`work-orders.js addServiceToRO` -> `scheduled_notifications` source
  `service_added_notify`, migration `service_added_notify_source.sql`), clock-out
  tech notes -> RO timeline (`checkin.html` v1.38), the **Off Lot - Returning**
  status (`render.js statusColorMap` + `config.js STATUS_PROGRESS_MAP`, excluded
  from the on-lot count), and Work List touch-drag reorder (`work-list.js`
  touchstart/move/end -> `_reorderWorkList`).
- The **Key Dates** initiative (P1-P3, v1.457+v1.459) and **Keys+Power** fields
  (v1.458) all verify: `pickup_date`/`planned_dropoff_date`/`key_status`/
  `keypad_code`/`keep_plugged_in` persist in `ro-crud.js`; colored Drop/Promised/
  Pickup chips render in `render.js`; calendar sync (`scheduling.js
  syncKeyDateCalendars`) + day-before/morning-of reminders (`ro-crud.js
  _syncOneKeyDateReminder`) are present.

No enhancement requests have arrived since 2026-06-19, so this run is a
re-confirmation: all 57 prior verdicts hold against v1.460. Session 121 staged a
design-redesign bundle (Case A) that touches presentation only and changes none
of the ER-relevant behavior.

The only non-enhancement item remains **n25** -- a data-only staff-name
correction (already applied in the DB; SQL retained below for the record).

---

## BUG bucket (front of the line)

None open. Every confirmed bug (n22, n33, n40, n47, n49, n54) shipped in
v1.447 / v1.453 / v1.12-v1.13 and is re-verified in the current tree.

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code @ v1.460) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation (v1.2) restores full RO detail (parts/time_logs/photos). | done (already) |
| n2 | Ryan | Warranty category on new RO | Warranty RO type v1.402 (`ro-crud.js` roType=='warranty' L1007; `render.js` filter L112). | done (already) |
| n4 | Lynn | Scheduled future drop-off date + reminder | v1.457: Planned Drop Off Date on New+Edit RO + morning-before reminder; blue Drop chip. | done (already) |
| n5 | Lynn | Work list opened but couldn't view/act | Fixed Phase 14: `work-list.js` renderWorkList/removeFromWorkList/_setWorkListSilo. | done |
| n10 | Riley | Can't delete / too small / no drag-drop on phone | v1.460: touch-drag reorder (`work-list.js` touchstart/move/end); delete already worked. | done |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs`/`_setWorkListSilo`). | done |
| n17 | Lynn | Add "Ready to Work" RO status | Shipped v1.410 (`config.js STATUS_PROGRESS_MAP` slot 50). | done |
| n18 | Lynn | Keep-plugged-in flag; keys / no-keys / keypad code | v1.458: key_status/keypad_code/keep_plugged_in on RO (`ro-crud.js`); migration `keys_power_fields.sql`. | done |
| n21 | Roland | Fix check-in, ensure email out, remove photo, streamline | `customer-checkin.html`: photo optional + auto RAF/staff email; v1.10/v1.11. | done |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | v1.447: `checkin.html` v1.34 "Return to Dashboard" button. | done |
| n24 | Brandon | Completion / pickup date on the order | v1.457: pickup_date on New+Edit RO + green Pickup chip; v1.459 calendar+reminders. | done |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning | v1.460: "Off Lot - Returning" status (`render.js` statusColorMap), excluded from on-lot count. | done |
| n28 | Brandon | Parts + freight TOTAL in Manage Parts | v1.456: Ticket Totals bar (`parts.js` L158) -- Parts/Freight/Total/Retail. | done |
| n29 | Lynn | Promised date large/highlighted on tile | v1.457: amber Promised chip, red if overdue (`render.js` L354); v1.459 reminders. | done |
| n33 | Bobby | Amounts drop the period & zeros (425.00 -> 425) | v1.447: 2-decimal formatting (`ro-crud.js editDollarValue .toFixed(2)`; `parts.js MONEY_FIELDS`). | done |
| n34 | Brandon | Confirm if customer leaves keys with the unit | v1.458: key_status select + keypad_code on RO (`ro-crud.js`). | done |
| n35 | Lynn | Transfer clock-in job notes -> RO updates | v1.460: `checkin.html` v1.38 copies clock-out notes into RO timeline (TECH NOTES). | done |
| n36 | Lynn | Scheduled drop date field in entry dashboard | v1.457: Planned Drop Off Date on New+Edit RO (duplicate intent of n4). | done |
| n37 | Roland | RO date/time as colored button + calendar + notifications | Key Dates P1-P3: chips (`render.js`) + calendar (`scheduling.js`) + reminders (`ro-crud.js`). | done |
| n39 | Roland | Admin daily Manager-worklist report | `supabase/functions/send-manager-report` v2.3 + P4c reminders, daily cron. Scoring/margin deferred. | done |
| n40 | Sofia | Check-in no longer emails staff / RAF | v1.10/v1.11: `shouldNotifyStaff` fires every drop-off; `getSignatureData` fix restores RAF email. | done |
| n43 | Roland | Remove cashed-out RO from worklist next day | S118: daily worklist auto-prune; migration `prune_cashiered_worklists.sql`. | done |
| n45 | Brandon | Distinguish RVs on-lot vs total customers | v1.453: distinct on-lot count (`render.js statTotalOnLot` L667); v1.454 excludes Scheduled. | done |
| n47 | Ryan | Can't upload pictures from phone | v1.453: `photos.js uploadPhoto` attaches input to DOM before .click() (iOS fix). | done |
| n48 | Brandon | Arrow keys to move through photos | v1.454: lightbox keydown (`photos.js navigateLightbox`); Esc closes. | done |
| n49 | Sofia | Returning-customer check-in duplicates RO | v1.12: dedupe guard UPDATEs the scheduled RO; v1.13 hardened by phone OR email. | done |
| n50 | Roland | Email silo manager when a new service added to RO | v1.460: `work-orders.js addServiceToRO` queues `service_added_notify`; migration present. | done |
| n52 | Brandon | "Scheduled to arrive" status (not counted on lot) | v1.454: `render.js` on-lot count excludes Scheduled (fixed the count, not a new status). | done |
| n53 | Brandon | Ticket totals should include freight | v1.456: Ticket Totals Freight line = sum(core_charge) (`parts.js`). | done |
| n54 | Sofia | David Flanagan returning check-in: no RAF + duplicate | v1.13: submit-time auto-dedupe by phone OR email closes the residual v1.12 gap. | done |
| n56 | Lynn | Can't edit time-off entries | v1.4: `time-off.html editRequest` opens pre-filled modal + UPDATEs (day/person/type). | done |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod and Travis last name should be Wombles, not Wimbles, in tech assignment" | Data, not code. `staff.name` holds the value; already corrected in DB per S117 (no Wimbles rows remain). SQL retained below. |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete / too small on phone / drag-and-drop" | Exact dup of **n10** (identical text + timestamp); already declined. n10 delivered v1.460. |
| n41 | Brandon | Completion / P-U date selection on RO | Dup of **n24** (completion/pickup date); n24 delivered v1.457. Already declined. |
| n42 | Sofia | Returning customer (Sheldon Gilbert) didn't email | Dup of **n40** (check-in sent no email); fixed v1.10/v1.11. Already declined. |
| n44 | Roland | Remove cashed-out RO from worklist next day | Exact dup of **n43** (timestamps 1s apart); n43 delivered S118. Already declined. |
| n51 | Sofia | Test on Roland Shepard Jr did duplicate | Dup of **n49** (returning-customer duplicate RO); fixed v1.12/v1.13. Already declined. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Scheduling & key dates  *(n55)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n55 | Lynn | Hover near customer name to list important dates/tasks with details on hover | M -- key-date chips shipped v1.457; no hover tooltip; task list overlaps n30 |

-> The drop-off/promised/pickup/completion asks (n4/n24/n29/n36/n37) all shipped
in the Key Dates initiative. The only residual here is a hover/aggregate view.

### Theme 2 -- Notifications, reminders & task-tracking  *(n3, n8, n20, n30, n32)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + font/color | L |

-> **Largest opportunity.** v1.459 key-date reminders and v1.460 service-add
notify exist, but there is still no general scheduled-task notifier, no
task-tracker entity, and no recurring-reminder scheduler. Design as one
"Task Tracker + Reminders" subsystem.

### Theme 3 -- Parts  *(n6, n7, n9, n27)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier | L |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

-> The parts+freight totals asks (n28/n53) shipped v1.456. Remaining parts work
is return/warranty state (n7/n27) and the inventory subsystem (n6).

### Theme 4 -- Work List usability  *(n31)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |

-> The mobile/drag-drop (n10) and auto-prune (n43) asks shipped. Remaining is the
check-in -> work-list integration.

### Theme 5 -- RO fields & status nuance  *(n16, n19)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs existing Hybrid) |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |

-> Keys/power (n18/n34) and the on-lot/returning status (n26/n52) shipped.

### Theme 6 -- Work Orders / templates  *(n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will | M |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague -- overlaps n22) |

### Theme 7 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

### Theme 8 -- Check-In & Authorization Forms  *(n46)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n46 | Sofia | Send the RV authorization form (RAF) remotely for e-signature | L -- needs public token-signing page + email link + signature store + staff notice |

### Theme 9 -- Time Off  *(n57)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n57 | Lynn | Exact absence time window (clock-out time to return time; AM/PM half-day) instead of total hours | M -- `partial_hours` captures duration, not a start/end time-of-day |

-> The time-off edit ask (n56) shipped v1.4.

---

## Recommended action today

1. **No code changes are queued from this run** -- the bug bucket is empty and
   all 57 prior verdicts re-confirmed against v1.460.
2. **Close any still-open DONE-bucket ERs** Roland has not yet flipped (e.g. n5
   if still `unreviewed`); the DATA item n25 is already applied.
3. **n25 staff-name SQL** is retained above for the record (already run).

Strategic asks awaiting a design session with Roland: **n38** (auth cost
reduction, XL), the **Task Tracker + Reminders** cluster (Theme 2, the largest
remaining opportunity), **n6** (parts inventory subsystem, XL), and **n46**
(remote RAF e-signature, L).
