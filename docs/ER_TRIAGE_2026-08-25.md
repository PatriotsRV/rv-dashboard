# PRVS Enhancement-Request Triage - 2026-08-25 (headless runner)

Incremental pass over `enhancement_requests` against the current modular
codebase at HEAD **f501151** (index.html / version.json **v1.500**). Input
snapshot `er_input.json` fetched 2026-08-25 07:47 UTC, `full_pass=false`.

**Exactly one row was in `needs_triage_ids` this run** - `611589ba` (Lynn,
08-24). That ER was classified by grepping and reading the live code. **Every
other verdict below is carried over unchanged from its previous run** and was
NOT re-verified this run; each carries the HEAD it was stamped against. Where
carried verdicts cite line numbers that have since moved, the drift is listed
in "Anchor drift" below rather than silently rewritten into the verdict text.

Runner is read-only: **no code was changed, no DB writes were made, no git
commands were run.** Status flips and the Data-bucket SQL are Roland's to run.

## Board snapshot

| DB status | Count |
|---|---|
| done | 62 |
| declined | 15 |
| unreviewed | 26 |
| **Total** | **103** |

## Verdict buckets (all 103 rows)

| Bucket | Count | Change since 2026-08-08 |
|---|---|---|
| done | 62 | +10 |
| needed | 25 | -2 |
| duplicate | 13 | +3 (incl. `611589ba` this run) |
| data | 2 | 0 |
| bug | **0** | **-5** |
| not triaged | 1 | -1 |

**Headline: the bug bucket is empty.** All five bugs that were open at the
2026-08-08 pass - `09084bc5`, `225a2535`, `d94a418a`, `d4d0eeba`, `5ca724ff` -
now carry `done` verdicts (fixed across v1.494 S171 and v1.497/v1.498 S178).
Nothing on this board is currently classified as a defect.

**Open work is 25 enhancements**, none of them new: the single new ER this run
is a duplicate of two already-open items.

## New this run (1)

`611589ba` - Lynn Titel-Shepard, 2026-08-24, General UI/UX, source page
`index`: "need a status of Closed and Canceled in the drop down status menu".

Classified **duplicate**, recommend `status=declined` with a pointer. Both
halves are already open ERs:

- **Canceled** is `972af03c` (Lynn, 07-17, needed/S) - the canonical CANCELED
  item, which `eb00a68d` was already declined into on a prior run.
- **Closed** is the `JOB DECLINED/CLOSE` value inside `44419de9` (Lynn, 08-12,
  needed/S).

Verified absent at HEAD f501151 (v1.500) this run, not assumed:
`js/config.js` `STATUS_PROGRESS_MAP` **L151-171** holds 15 statuses and carries
neither Closed nor Canceled; `index.html` status select **L7262-7281** and
filter buttons **L1195-1207**, and `js/render.js` `ALL_STATUSES` **L790-797**,
all list the same 15. A case-insensitive grep for `cancelled`/`canceled` in
`js/config.js`, `js/render.js` and `js/ro-crud.js` returns only the
`scheduled_notifications` reminder-cancel paths (`js/ro-crud.js` L308, L689),
which are unrelated to RO status.

This ER adds no new work item. It is a third independent request for the same
terminal-status batch, which is worth recording as demand signal: `972af03c`,
`eb00a68d`, `3e746c18`, `44419de9` and now `611589ba` are five filings from
Lynn on the same missing set of terminal RO statuses.

## Anchor drift since the carried verdicts were stamped

Carried verdicts in the RO Fields & Status theme were stamped against HEADs
`0223cb3` (v1.492) through `df519c6` (v1.495). v1.497-v1.500 (S178-S182) moved
the status-family anchors. Re-read at HEAD f501151 this run:

| What | Stamped in carried verdicts | Current (HEAD f501151) |
|---|---|---|
| `js/config.js` STATUS_PROGRESS_MAP | L151-164 | **L151-171** (15 statuses) |
| `js/config.js` ES status labels | L253-262 | **L270-284** |
| `index.html` status `<select>` | L7038-7051 | **L7262-7281** |
| `index.html` status filter buttons | L1085-1096 | **L1195-1207** |
| `index.html` inline progress map | L1579 | **L1717** |
| `index.html` inline ES label map | L4489 | **L4684** |
| `js/render.js` compact status dropdown | L253 | **L266** |
| `js/render.js` card status dropdown | L451 | **L473** |
| `js/render.js` statusColorMap | L727-739 | **L769** |
| `js/render.js` ALL_STATUSES | L741-745 | **L790-797** |
| `js/render.js` nonShop queue filter | L656-658 | **L698-700** |
| `js/render.js` onLotCount | L718-721 | **L760-765** |

So a status add now touches **10 hardcoded UI spots**, not 9, **plus both DB
CHECK constraints** (`repair_orders` and the `cashiered` twin - see
`supabase/migrations/off_lot_returning_status_check_s172.sql` and
`status_approval_breakout_s175.sql`) or saves throw 23514. That S172 lesson
still stands: 'Off Lot - Returning' shipped UI-only and was broken for 50 days.

Anchors outside the status family were not re-checked this run.

---

## BUGS (0)

No ER on this board is currently classified as a bug. The five that were open
at the last full pass are all in the DONE table below with their fix evidence:
`09084bc5` (v1.494 S171, cash-out data loss), `225a2535` (v1.497 S178,
cross-customer insurance-scan bleed), `d94a418a` (v1.497 S178, note line
order), `d4d0eeba` (v1.497 S178, scroll-to-RO), `5ca724ff` (v1.497 S178,
desktop drag-reorder).

---

## NEEDED - open enhancements by theme (25)

All 25 are `status='unreviewed'` in the DB. Verdicts carried over unchanged.

### RO Fields & Status (6)

| ER | By | Date | LOE | Mapped code (as stamped) | Verdict |
|---|---|---|---|---|---|
| `972af03c` | Lynn | 07-17 | **S** | `js/config.js` STATUS_PROGRESS_MAP L151-164 | No Canceled/Cancelled RO status; the only `cancelled` hits in the repo are the scheduled_notifications and review_requests status CHECKs. Small: add the status plus color/priority maps, exclude it from the on-lot and queue counts, keep the record for notes. **Canonical for duplicates `eb00a68d` and now `611589ba`**; pairs with `3e746c18`. |
| `3e746c18` | Lynn | 08-03 | **S** | `js/config.js` L151-164; `js/render.js` L656-658, L718-721 | No TOTALED OUT status - a case-insensitive grep for totaled/totalled across config and render returns zero hits. Distinct value from `972af03c`'s Canceled, but ONE build should add both terminal statuses and exclude both from the queue / on-lot counts. |
| `44419de9` | Lynn | 08-12 | **S** | `index.html` select L7038-7051, filter btns L1085-1096, inline maps L1579/L4489; `js/config.js` L151-164, ES L253-262; `js/render.js` L253/L451/L727-739/L741-745 | None of the 4 values (NEED ESTIMATE, AWAITING ESTIMATE, ESTIMATE DECLINED, JOB DECLINED/CLOSE) exist. Each add must hit every hardcoded spot PLUS both DB CHECKs per S172 or saves throw 23514. Overlaps `972af03c` CANCELED and now `611589ba`'s "Closed" - confirm scope with Lynn; bundle with `972af03c` + `3e746c18`. |
| `0e172b2c` | Sofia | 07-16 | **M** | `js/duplicates.js` getBaseROId L35, regex L36, findDuplicateGroups L40, keying L44 | Merge engine exists but groups strictly on the `PRVS-XXXX-XXXX` base RO id, so two ROs created under different spellings of the same customer are never detected. Needs name/phone account dedup plus a verify-which-is-correct merge step. The one-off cleanup for this case is `3f254929`. |
| `5fbfa299` | Brandon | 06-30 | **M** | `js/ro-crud.js` archiveROInSupabase L662 | Only the cashiered archive path exists; no soft-archive flag, status or board filter for quoted-but-never-progressed units. Needs a flag, a board filter and an un-archive path. Pairs with `972af03c` - decide whether the Canceled status alone is enough. |
| `a195e4ab` | Brandon | 08-20 | **M** | `index.html` Customer Pay block L7203-7246 / L7782-7804, setROType L2263-2267, insurance_data write L6562/L6718 | Zero check tracking - `check_number`/`checkNumber`/`payments` return 0 hits. The only payment capture is the one-value hybrid-RO Customer Pay block: method "Check" but no check number, one amount plus datePaid, saved flat into `insurance_data` JSONB and never shown on the card. Needs a per-RO checks table + add/list UI + cashiered mirror. |

### Messaging & E-sign (7)

| ER | By | Date | LOE | Mapped code (as stamped) | Verdict |
|---|---|---|---|---|---|
| `ca0f7ed9` | Brandon | 08-03 | **S** | `messages.html` renderLeadInfo L1389, call site L1744 | `renderLeadInfo` reads ONLY the conversation's own columns (`c.rv`, `c.service_type`), so an existing dashboard customer shows "No RV or service type captured yet". `selectConversation` already computes phone-matched live ROs as `linked` for `renderContactInfo` but calls `renderLeadInfo(c)` without them. Small: pass `linked`, fall back to RO rv. |
| `5fec3eca` | Sofia | 07-15 | **S** | `supabase/functions/send-quote-email` raf_copy destructure L387 | The raf_copy payload has no `customerAddress`; the address appears only in the `lead_staff_notify` branch. Small: add it to the raf_copy payload and both templates. Edge-fn deploy, no dashboard release. |
| `9aab7550` | Brandon | 07-21 | **S** | `messages.html` `.unread-dot` CSS L469, condition L1313, render L1319, markConversationUnread L1980 | S166's manual "Mark unread" button helps, but the visual treatment is still a single 8px gold dot applied only when `last_direction='inbound'` AND `status='open'`, with no row-level highlight. Small: add a bold/tinted unread row. |
| `21fc3a9d` | Brandon | 07-21 | **S** | `messages.html` fixed rem sizing | No user text-size control anywhere on the page - `font-scale`/`fontScale`/`textSize` return zero hits. Small: add a persisted font-scale control for the conversation list. |
| `f1e6e311` | Brandon | 07-21 | **S** | `messages.html` convoSearch L604, resets L1151/L1170 | Plain text input with no clear affordance; the field is reset only programmatically. Small: add a clear (x) button that empties the search and re-runs the filter. |
| `6e9ca4dc` | Brandon | 07-21 | **S** | `js/messaging.js` refreshSignaturePreview L369, element L350, set L374; `messages.html` media query L565 | "Added to every send:" is hidden ONLY inside the mobile max-width media query, so desktop messages.html and the RO-card composer still show it. Small: remove it or collapse it behind a toggle in all viewports; the signature itself keeps sending. |
| `0448de62` | Sofia | 06-11 | **L** | `customer-checkin.html` signature pad; `send-quote-email` raf_copy L386 | Signing is kiosk-only and the raf_copy branch just emails a copy after signing; `signing_token`/`remote_sign`/`signature_request`/`signing_link` return nothing repo-wide. Needs a tokenized public signing page, a delivery path and signed-form storage. Pairs with the S166 short-link machinery. |

### Notifications & Task-Tracking (3)

| ER | By | Date | LOE | Mapped code (as stamped) | Verdict |
|---|---|---|---|---|---|
| `a03677e6` | Brandon | 08-07 | **S** | `js/render.js` L553 vs L563; writers at `process-scheduled-notifications` L187/L215, `index.html` L3116/L3154, `js/ro-crud.js` L302/L344/L379/L571/L582, `js/parts.js` L654/L855 | Machine audit lines (NOTIFICATION SENT/FAILED, SCHEDULED/CANCELLED, key-date and urgent-update rows, parts notes) are all written as notes type `ro_status`, so they crowd RO Status Updates; a `customer_comm` stream already renders separately. Small: retarget the type. **NOTE Roland reversed exactly this retarget for LEAD NOTES in v1.491 - confirm scope with him before building.** Canonical for declined duplicate `2deb8f30`. |
| `5925fc01` | Lynn | 04-09 | **L** | `supabase/functions/send-manager-report`; `js/work-orders.js` L293; `js/ro-crud.js` L374 | PARTIAL. The daily in-progress digest half ships, and single-event notifies exist (`service_added_notify`, `urgent_update_notify`). Still absent: notify-on-every-RO-update and to-do scraping from status notes - `ro_tasks`/`task_tracker`/`customer_task` return zero hits. Anchor of this theme; pairs with `af14b3cb`. |
| `af14b3cb` | Lynn | 05-21 | **L** | `js/shop-tasks.js` (no ro_id, no reminder column) | The shop checklist is non-customer and has no RO link. Needs a per-RO tasks table, an add/complete UI and a daily open-task reminder. Pairs with `5925fc01`. Canonical for declined duplicate `ece91cba`, which supplies the concrete spec (bulleted, anyone can add or check, record who and when, plus the Repair Description vs RO Status split). |

### Parts & Inventory (3)

| ER | By | Date | LOE | Mapped code (as stamped) | Verdict |
|---|---|---|---|---|---|
| `8d885331` | Lynn | 04-09 | **S** | `js/parts.js` pf_supplier L181, pf_partName L190, pf_serviceSilo L208, pf_returnDeadline L226 | Form is unchanged; the ask is only to reorder the fields. Quick once Lynn supplies the target layout or a mockup. **Blocked on detail, not on effort.** |
| `b19975d4` | Lynn | 04-09 | **L** | `js/config.js` PART_STATUSES L200; `js/parts.js` pf_returnDeadline L226 | Partial infra only - a 'Returned' status and a Return Deadline input, but `rma`/`return_reason`/`supplier_credit` return zero hits, so there is no RMA number, return reason, supplier credit or reverse-direction flow. Equals GH#24 Shop Ops Phase 2. Canonical for declined duplicate `86bbaf50`. |
| `14f35f95` | Lynn | 04-09 | **XL** | `js/parts.js` (per-RO line items only) | No inventory/stock concept anywhere - `inventory`/`on_hand`/`in_stock`/`stock_qty` return zero hits across js, migrations and functions. New subsystem: inventory table, receive/consume/adjust flows, a searchable priced stock view, and links from the parts-request path. Largest item on the board. |

### Board & Navigation UX (1)

| ER | By | Date | LOE | Mapped code (as stamped) | Verdict |
|---|---|---|---|---|---|
| `d1844e46` | Lynn | 07-16 | **S** | `js/render.js` calculatePriority L30-39, shop pin L35-37 | Board sorts on the single blended priority score; `sortBy`/`sortSelect`/`sortMode` find nothing, so there is no selectable strict promised-date-then-oldest-on-lot order. Small: add the strict sort option while keeping the shop pin. Related to `1aeb3f58`, which delivered only the promised-date chip. |

*(This theme shrank from 3 to 1 - `29654af7` back-to-top and `d432863a`
favicon both shipped in v1.498.)*

### Time Clock & Check-In (2)

| ER | By | Date | LOE | Mapped code (as stamped) | Verdict |
|---|---|---|---|---|---|
| `6bb61db2` | Riley | 07-22 | **S** | `js/render.js` compact-col-actions L228-235; `js/time-tracking.js` | The compact actions row has edit / parts / WO / notify / message / schedule but no check-in-out button, and `clocked in`/`clocked into`/`activeTimeLog` return nothing. Small: add a compact check-in/out action plus a login-time active-time_log banner shown across views. **Treat as the canonical spec**; pairs with `e06d747e`. |
| `e06d747e` | Riley | 04-10 | **S** | `js/render.js` compact-col-actions L228-235 | No floating or sticky tech check-in control on any page. Underspecified: scope the target page and exact behavior with the submitter. Almost certainly the same ask as `6bb61db2` - fold in and build once. |

### Work Orders & Shop Tasks (1)

| ER | By | Date | LOE | Mapped code (as stamped) | Verdict |
|---|---|---|---|---|---|
| `2f752d40` | Jason Rubin | 07-21 | **S** | `js/work-orders.js` woFormDollar L360, woFormEstHours L365, save L649-650, dollarLocked L324 | Dollar and hours are independent manual inputs; `LABOR_RATE`/`laborRate`/`labor_rate` return zero hits in the module (`js/insurance.js` laborRate L212 is the per-claim INSURANCE rate). Small: auto-populate `dollar_value` = effective hours x $195/hr, respecting the dollarLocked gate. |

### Auth & Cost (1)

| ER | By | Date | LOE | Mapped code (as stamped) | Verdict |
|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | **XL** | `js/auth.js` GOOGLE_CONFIG L69, gapi init L203-204, One Tap + signInWithIdToken | Still fully Google-based, kiosk included. Swapping to Supabase password / magic-link or shared role accounts drops roughly $500/mo but touches every page sign-in plus the email-keyed RBAC. **Dedicated design session with Roland.** |

### Scheduling & Dates (1)

| ER | By | Date | LOE | Mapped code (as stamped) | Verdict |
|---|---|---|---|---|---|
| `47428f75` | Lynn | 08-03 | **S** | `time-off.html` modalStartTime L368, modalEndTime L372, editRequest L752 | UNDERSPECIFIED - "update vacation/sick/personal calendar", no stated defect or target. All three types, a color-coded month calendar, stats tiles, per-row Edit and exact partial-day windows already ship. Filed Needed rather than Done because the ask maps to no gap without Lynn's detail; if it restates `ee50d11c` it is already delivered. **Ask Lynn.** |

---

## DATA - corrections (2)

Nothing below was executed by this runner. Both are already applied in the
live DB; the SQL is kept as the record.

| ER | By | Date | Category | Verdict + SQL |
|---|---|---|---|---|
| `6938c1af` | Ryan Dillon | 05-06 | General UI/UX | Already applied (tech names read from `staff.name`; submitter "Rod Wombles" now spells correctly in this very table). RESIDUAL, not a runtime bug: `supabase/migrations/staff_table.sql` L85-87 still seeds Rod/Zak/Travis as "Wimbles", so a re-seed would reintroduce it. Re-apply if ever needed:<br>`UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |
| `3f254929` | Sofia Pedroza | 07-16 | General UI/UX | Data correction, already carried out in S178 - the misspelled Steve Hubert RO was soft-deleted; only the correct Huebert RO remains. SQL of record:<br>`SELECT id, ro_id, customer_name, status FROM repair_orders WHERE customer_name ILIKE '%ubert%';`<br>then, with the confirmed wrong id:<br>`UPDATE repair_orders SET deleted_at = now() WHERE id = '<wrong-uuid>' AND deleted_at IS NULL;`<br>The wider merge-accounts ask stays open as `0e172b2c` (needed/M). |

---

## DUPLICATES - fold and decline (13)

| ER | By | Date | Folds into | DB status | Note |
|---|---|---|---|---|---|
| `611589ba` | Lynn | 08-24 | `972af03c` (Canceled) + `44419de9` (Closed) | **unreviewed** | **NEW this run.** "need a status of Closed and Canceled in the drop down status menu". Both halves already open. Verified at HEAD f501151: STATUS_PROGRESS_MAP L151-171 has neither value. **Recommend `declined` with a pointer to `972af03c`.** |
| `aa508852` | Riley | 04-09 | `fd6c122d` | declined | Identical text and timestamp (cannot delete jobs / too small on phone / no drag-drop). Keep declined. |
| `86bbaf50` | Lynn | 05-13 | `b19975d4` | declined | A Gordon window ordered and returned to Vogt is supplier return / warranty tracking. Keep declined. |
| `67daba15` | Brandon | 06-09 | `d2561e11` | declined | RO completion / pick-up date for records - Done via `repair_orders.pickup_date` and the green Pickup chip. Keep declined. |
| `fa1c1d6e` | Sofia | 06-09 | `a22739b9` | declined | Returning check-in did not send the email (Sheldon Gilbert) - Done; both sends are forced on every check-in. Keep declined. |
| `5057c7fc` | Roland | 06-09 | `91a03260` | declined | Remove a cashed-out RO from the manager work list the day after Delivered - Done via `prune_cashiered_worklists.sql`. Keep declined. |
| `fc6b29f9` | Sofia | 06-17 | `298e0e67` | declined | Retest report of the returning-customer duplicate guard - Done. Keep declined. |
| `9031e101` | Riley | 07-21 | `d4d0eeba` | declined | iPhone work-list tap does not reach the unit. Canonical `d4d0eeba` **has since been fixed in v1.497**, so this is closed by that release. Keep declined. |
| `1bc33dd6` | Brandon | 07-27 | `d4d0eeba` | declined | Same scroll-to-customer defect Brandon filed 07-22. Also closed by the v1.497 fix. Keep declined. |
| `2b78696c` | Lynn | 07-27 | `29654af7` | declined | Same back-to-top ask Lynn filed 07-06. Canonical `29654af7` **shipped in v1.498**, so this is delivered. Keep declined. |
| `eb00a68d` | Lynn | 08-03 | `972af03c` | declined | Same CANCELED status ask Lynn filed 07-17. Still unbuilt. Keep declined. |
| `2deb8f30` | Lynn | 08-13 | `a03677e6` | declined | Same ask: get machine-generated notification lines out of RO Status Updates. Nothing in v1.497/v1.498 changed note routing. Keep declined. |
| `ece91cba` | Lynn | 08-17 | `af14b3cb` | declined | Same per-RO to-do list. Its added spec (bulleted, anyone can add or check, record who and when, plus the Repair Description vs RO Status split) folds in as design input. Keep declined. |

---

## DONE - delivered (62)

Verdicts carried over unchanged; each names the HEAD it was verified against.
Rows marked **[fixed since last pass]** moved out of the bug bucket.

| ER | By | Date | Category | Mapped code | Verdict |
|---|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | General UI/UX | - | Test entry ("Testing out the Make-A-Wish function"). Not a real request. Already declined; keep declined. |
| `19dc2016` | Roland | 04-11 | Time Clock | - | Test entry ("Testing ER form / dictation"). Not a real request. Already declined; keep declined. |
| `79b2c805` | Sofia | 04-09 | New Feature Idea | `closed-ros.html` reactivateRO L838, buttons L725/L813, flat map L958-963; `js/photos.js` uploadDocument L253 | Cashed-out ROs reactivate with parts / time_logs / notes / audit_log / insurance_scans plus the WO subtree, and documents can be added to the reopened RO. The photo/doc-library gap noted here was Bug `09084bc5`, now also fixed. |
| `75124b03` | Ryan | 04-09 | RO Management | `js/ro-crud.js` warranty branch; `js/render.js` L121/L195/L372 | v1.402. Warranty is a first-class RO type at creation with board filter, compact chip and card badge (plus the $0 No-Charge sub-badge). |
| `b2aa4bea` | Lynn | 04-09 | New Feature Idea | `js/ro-crud.js` L413/L490; `js/render.js` L395; `key_dates_reminder_sources.sql` | `planned_dropoff_date` persists on create and edit, drives the blue Drop chip and the day-before reminder. Same field as `468aa376`. |
| `18611eee` | Lynn | 04-09 | Bug Report | `js/work-list.js` L195/L210/L223/L263/L304/L383 | Work-list members are viewable, reorderable and removable, with per-silo tabs. The desktop drag half Lynn reported separately was Bug `5ca724ff`, fixed in v1.497. |
| `17492066` | Lynn | 04-09 | Notifications / Email | `scheduled_notifications.sql`; `process-scheduled-notifications`; `index.html` snRepeat L2993 | The per-RO Schedule Notification bell sets a future date/time, message and recipients with daily/weekly repeat - covers the advance-warning ask. |
| `fd6c122d` | Riley | 04-09 | Work List | `js/work-list.js` L195/L304/L389/L404-429, save L210/L383 | v1.460. Delete plus mouse and touch drag-reorder, persisted, with the mobile sizing fixed. Canonical for declined dup `aa508852`. |
| `98bd7498` | Brandon | 04-09 | RO Management | `js/work-list.js` L223/L263 | Per-silo Work List tabs give a separate list per job category. |
| `1fe68261` | Rod | 04-13 | Work Orders | `js/shop-tasks.js` L21/L130/L153/L180; `shop_tasks.sql` | v1.467 (S128). Internal work not tied to a customer unit is tracked separately from ROs and the board. Matches the poly-barrel / roof-bay examples. |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | `js/config.js` L158; `js/render.js` L727/L741 | v1.410. "Ready to Work" is a live RO status in the maps and both status dropdowns. |
| `b87eb2fb` | Lynn | 04-14 | RO Management | `keys_power_fields.sql`; `js/ro-crud.js` L415-417/L492-494 | v1.458. `key_status`, `keypad_code` and `keep_plugged_in` persist on create and edit with a keys chip on the card. Same build as `34fc03c2`. |
| `a5ff3e2d` | Lynn | 04-21 | RO Management | `js/ro-crud.js` warranty_repair branch; `js/render.js` L122/L196/L373 | v1.465. "Warranty + Repair" exists end to end and stays billable so the repair half bills normally through work orders. |
| `a7d1474e` | Lynn | 04-23 | RO Management | `urgent_update_field.sql`; `js/render.js` L324/L214; `js/ro-crud.js` L374 | v1.466. Always-visible red URGENT banner plus compact siren, and setting it notifies silo managers and admins. Lynn's follow-up to move the INPUT to the tile shipped separately as `50175fce` (v1.498). |
| `dde90d2b` | Roland | 04-27 | RO Management | `customer-checkin.html` L2530/L2603 | Photo is optional and both the authorization-form copy and the staff notice are forced on every submission. |
| `13e1cd87` | Tipton | 04-30 | Time Clock / Check-In | `checkin.html` L1516 | v1.447 (checkin v1.34, now v1.38). "Return to Dashboard" renders after clock-out, so no new tab is needed to clock into the next job. |
| `e27311bf` | Ryan | 05-04 | Work Orders | `js/work-orders.js` _initWOTaskDrag L461, dragstart L482 | v1.464. Every WO task row has a grip handle and drag-to-reorder; DOM order drives `sort_order`, so templates are edited by load, reorder, Overwrite. |
| `d2561e11` | Brandon | 05-06 | New Feature Idea | `key_dates_pickup_date.sql`; `js/ro-crud.js` L414/L491; `js/render.js` L397, panel L365 | v1.457. `pickup_date` persists and shows as the green Pickup chip and in the key-dates panel. Canonical for declined dup `67daba15`. |
| `dac9fdda` | Brandon | 05-06 | General UI/UX | `js/config.js` L154; `js/render.js` L718-721 | v1.460. "Off Lot - Returning" separates units temporarily taken by the customer from units never on the lot, and is excluded from the on-lot count. |
| `a8e90a7d` | Brandon | 05-20 | General UI/UX | `js/parts.js` L152-161 | v1.456 (S117). Manage Parts Ticket Totals bar sums parts, freight and total cost. Same build as `671fa623`. |
| `1aeb3f58` | Lynn | 05-21 | General UI/UX | `js/render.js` L396, L30-39 | v1.457. Promised date is a large bold chip, red when overdue, and both promise date and days-on-lot feed the blended sort. The strict selectable sort is open ER `d1844e46`. |
| `cbc70a86` | Lynn | 05-21 | General UI/UX | `customer-checkin.html` L2381; `work_list_autoadd_rpc.sql` | Returning check-in calls `add_ro_to_silo_work_lists`, inserting `manager_work_lists` rows per involved silo lead with a sr-manager fallback. |
| `3f415c88` | Lynn | 05-26 | General UI/UX | `index.html` snRepeat L2993, read L3061 | v1.463. Repeat none/daily/weekly plus repeat-until expands into one individually cancellable row per occurrence. |
| `cd663d8f` | Bobby | 05-28 | General UI/UX | `js/ro-crud.js` L1097; `js/parts.js` L307/L310 | v1.447. Currency inputs show 2 decimals on edit-populate and in the parts modal. Cosmetic only - no data loss ever occurred. |
| `34fc03c2` | Brandon | 05-29 | New Feature Idea | `keys_power_fields.sql`; `js/ro-crud.js` L415-417/L492-494 | v1.458. The Keys selector is on New and Edit RO with a keys chip on each card. Covers `b87eb2fb`. |
| `0b2e128c` | Lynn | 06-02 | General UI/UX | `checkin.html` L1455 | v1.460 (checkin v1.38). Clock-out work notes are copied into the RO update timeline as a `ro_status` note. |
| `468aa376` | Lynn | 06-02 | General UI/UX | `js/ro-crud.js` L413/L490; `js/render.js` L395 | Planned drop-off date input on New and Edit RO (Edit restored v1.457), feeding the Drop chip and `auto_dropoff_reminder`. Same field as `b2aa4bea`. |
| `2b814250` | Roland | 06-07 | Calendar | `js/render.js` L395-397/L367; `sync-ro-calendar` | Key dates render as attention-grabbing chips plus a hover/tap panel, calendar integration runs server-side and idempotently via `cal_event_ids`, advance reminders go to managers/admins. |
| `cd084233` | Roland | 06-07 | Work List | `send-manager-report`, `send-admin-pnl-report`, `worklist-report.html` | Admin-level daily manager-worklist reporting with parts, labor and cost rollups. The manager scoring layer described at the end of the request is a future extension, not this ER. |
| `a22739b9` | Sofia | 06-08 | General UI/UX | `customer-checkin.html` L2530/L2603 | Notify-gate regression fixed - both sends forced on every submission. Canonical for declined dup `fa1c1d6e`. |
| `91a03260` | Roland | 06-09 | Work List | `prune_cashiered_worklists.sql` | Removes an RO from manager work lists the day AFTER Delivered/Cashed Out, deliberately keeping it visible on the day of cash-out. Canonical for declined dup `5057c7fc`. |
| `cdd77a8b` | Brandon | 06-10 | General UI/UX | `js/render.js` L656-658, L718-721 | v1.453/v1.454. Queue count drops Shop Operations ROs and the on-lot count excludes Not On Lot / Scheduled / Off Lot - Returning, so the tile shows a true on-lot number. |
| `2c3d5633` | Ryan | 06-12 | General UI/UX | `js/photos.js` L66 | v1.453. The upload input is attached to the DOM before clicking - the iOS Safari dropped-change-event root cause. Phone photo upload completes. |
| `74a33621` | Brandon | 06-16 | General UI/UX | `js/photos.js` L396-397/L400/L404, buttons L380/L382 | v1.454. Lightbox supports arrow-key navigation plus Escape-to-close and prev/next buttons. |
| `298e0e67` | Sofia | 06-16 | General UI/UX | `customer-checkin.html` L2059/L2165, L2060/L2194 | The returning-customer guard reuses an existing scheduled RO, and v1.17 adds a blocking attach-vs-new dialog for the already-active case. Canonical for declined dup `fc6b29f9`. |
| `9b823d25` | Roland | 06-16 | RO Management | `js/work-orders.js` L244/L293; `service_added_notify_source.sql` | v1.460. Adding a service to an RO emails the owning silo manager. Matches the roof-plus-water-heater example. |
| `9b046808` | Brandon | 06-17 | General UI/UX | `js/config.js` L157; `js/render.js` L718-721 | v1.454. "Scheduled" is its own status and is excluded from days-on-lot stats and the on-lot count. |
| `671fa623` | Brandon | 06-18 | General UI/UX | `js/parts.js` L153/L155/L159-161 | v1.456 (S117). Freight is summed across parts and rolled into Total Cost. Same build as `a8e90a7d`. |
| `5c45bb8d` | Sofia | 06-19 | General UI/UX | `customer-checkin.html` L2059/L2165/L2194/L2530 | Both halves of the David Flanagan report resolved - the Scheduled path reuses the RO and the authorization copy is forced on every submission. |
| `b06a285f` | Lynn | 06-19 | General UI/UX | `js/render.js` L367, L363-365, L395-397 | v1.465. A calendar icon by the customer name opens a key-dates panel listing drop-off, promised and pickup in full, with colored summary chips. |
| `ee50d11c` | Lynn | 06-19 | General UI/UX | `time-off.html` L752 | Time-off entries are editable, not delete-only - Edit loads the entry back into the form and flips submit to "Update Record", so day, person and absence type can all be changed. |
| `88395acc` | Lynn | 06-19 | General UI/UX | `add_absence_window_to_time_off.sql`; `time-off.html` L368/L372 | Time Off v1.5. Partial-day absence is an exact clock-out-to-return window rather than an hour total. |
| `80390f36` | Lynn | 06-23 | General UI/UX | `js/render.js` L35/L37/L61 | v1.462. Shop Operations tiles pin to the top ahead of the priority sort, and a "shop operations shop" search token makes the tile findable. |
| `7f2d0d70` | Lynn | 06-26 | General UI/UX | `send-dropoff-report` | The Monday drop-off check email lists this week's scheduled drops plus last week's arrivals and flags any not yet on the right team's work list. Pairs with `cbc70a86`. |
| `3f7fb4fc` | Brandon | 06-29 | General UI/UX | `js/render.js` L656-658/L718-721/L249 | v1.468. Shop ROs are excluded from both counts and the shop status dropdown is replaced by a static label. |
| `cb7742a8` | Lynn | 06-29 | General UI/UX | `sync-ro-calendar`; `customer-checkin.html` L2240 | v1.469 (check-in v1.15). Drop-off/promised/pickup events are posted server-side to the correct team calendar for every RO including front-desk check-ins. Fixes the repair-calendar miss. |
| `29654af7` | Lynn | 07-06 | General UI/UX | `index.html` #backToTopBtn L8212-8220, boot log L3748; `css/dashboard.css` L3479-3498 | **[shipped since last pass]** v1.498 (S178). Back-to-top button with smooth scroll and a scroll listener that reveals it, pinned bottom-left clear of the toast stack. Canonical for declined dup `2b78696c`. Previously needed/S. |
| `39b75fab` | Sofia | 07-09 | General UI/UX | `js/photos.js` canManageFiles L618, L198-201, L376 | v1.472. The Documents/Photos library supports rename, move-to-another-RO and delete, office-staff gated. |
| `1c7f1410` | Andrew Page | 07-16 | New Feature Idea | `js/messaging.js` L347/L290/L295/L428-434/L386-392/L578-579 | Photo/PDF attach shipped S151b/S158 and video shipped S166; 5-file cap with removable chips. DB row is now `done` - no status action remains. |
| `d432863a` | Brandon | 07-21 | General UI/UX | all repo `*.html` heads; `index.html` L263 marker | **[shipped since last pass]** v1.498 (S178). Favicon + apple-touch-icon markup on all 13 pages; also closes the long-standing S139 favicon-404 console noise. Previously needed/S. |
| `d4d0eeba` | Brandon | 07-22 | General UI/UX | `index.html` L4342-4344; `js/qr.js` L198-214 | **[fixed since last pass]** v1.497 (S178). The Clear Filters toast action now retries `scrollToROInBoard` after the re-render and reports an archived/missing RO; the messages `?ro=` deep link clears filters once behind a one-shot guard. Also closes declined dups `9031e101` and `1bc33dd6`. Previously bug/small. |
| `9d3806e7` | Brandon | 07-22 | General UI/UX | `messages.html` L671/L669/L1599; `customer_note_entries_s154.sql` | S154. The details pane has a customer-side Notes section plus an Add-Notes modal writing append-only rows keyed by phone. DB row is now `done`. |
| `ac394468` | Roland | 07-24 | General UI/UX | `js/enhancement.js` pageDefaults L43 | v1.482 (S158). The erCategory select carries Messages/Texting plus Solar, Leads, Closed ROs, Reports, Time Off and Employee Guide. RESIDUAL, stated not hidden: erCategory exists in only 6 live HTML files, so messages/leads/time-off/guide cannot RAISE an ER - worth its own small ER. |
| `d62a26e4` | Roland | 07-24 | New Feature Idea | `js/messaging.js` L290/L295/L347/L386-392/L578-579 | v1.482 (S158a). Multi-attach up to 5 with removable chips; the send path splits video links from MMS media because a carrier payload cap silently stripped multi-media sends. |
| `93b00023` | Roland | 07-24 | New Feature Idea | `textly-webhook` v1.4 L487/L514-523/L617; `send-unreplied-reminder`; `unreplied_eod_reminder_s158.sql` | v1.482 (S158). All three parts ship: inbound reopen with a `reopened` event row, owner notify only on the needs-reply flip, and the 4:30pm unread sweep. v1.4 disabled only the UNASSIGNED blast, which is not this ask. |
| `d94a418a` | Brandon | 07-27 | General UI/UX | `js/render.js` formatNotesNewestFirst L33-39, comment L28-32 | **[fixed since last pass]** v1.497 (S178). Notes now split on the entry delimiter and reverse ENTRIES, not lines; the old split-newline/reverse that flipped every line inside a note is gone. Applied to both RO Status and Customer Comm. Previously bug/data-display. |
| `50175fce` | Lynn | 07-27 | General UI/UX | `js/ro-crud.js` editUrgentUpdate L404, export L1551; `index.html` L3968 | **[shipped since last pass]** v1.498 (S178). The red URGENT banner is clickable to edit or clear; the write targets the RO UUID, is audit-logged, and still fires `urgent_update_notify`. Follow-on to `a7d1474e`. Previously needed/S. |
| `09084bc5` | Lynn | 08-04 | General UI/UX | `js/ro-crud.js` L662-684; `cashiered_full_detail_s171.sql` L32-38/L95-108/L114-141/L144; `closed-ros.html` L898-904 | **[fixed since last pass]** v1.494 (S171). `archiveROInSupabase` is now a single manager-gated `archive_single_ro` RPC; the 7 dropped columns and all seven child mirrors are written before the delete, and reactivate restores them. Previously bug/LARGE permanent-data-loss - the highest-severity item on the prior board. |
| `225a2535` | Brandon | 08-05 | Bug Report | `js/insurance.js` L127-129/L47-49; `index.html` L6633/L6810; `js/ro-crud.js` L1376-1378 | **[fixed since last pass]** v1.497 (S178). `window._lastEstimateScan` is stamped with `roKey = editingROSupabaseId` and every consumer gates on it, closing the cross-customer bleed. Previously bug/data-integrity (Kain/Ivins family). |
| `ac8265c8` | Lynn | 08-05 | General UI/UX | `add_mileage_s178.sql`; `index.html` L7670/L6743/L6766; `js/ro-crud.js` L567/L568/L1294; `js/render.js` L523-526 | **[shipped since last pass]** v1.498 (S178). `repair_orders.mileage` plus the cashiered twin column, Edit RO input next to VIN, audit label, and a card readout under the VIN. Previously needed/S. |
| `5ca724ff` | Lynn | 08-06 | General UI/UX | `js/work-list.js` L418-425 | **[fixed since last pass]** v1.497 (S178). `dragstart` now calls `e.dataTransfer.setData` and sets `effectAllowed='move'`, which is what Firefox and some Chrome paths require. Touch path untouched. Previously bug/small. |
| `fa35e3df` | Lynn | 08-18 | General UI/UX | `css/dashboard.css` L560/L609/L1210/L3039/L1930; `js/render.js` L26 | **[shipped since last pass]** v1.498 (S178). Low-contrast blues lightened for the dark card surface (#3b82f6 -> #7eb3ff, #667eea -> #a5b4fc, key-date #0a84ff -> #5cadff). Previously needed/S. |

---

## Not triaged this run (1)

| ER | By | Date | DB status | Note |
|---|---|---|---|---|
| `f73a1e5a` | Roland | 04-13 | declined | "Testing dictation 123 testing enhancement request dictation" - not present in `needs_triage_ids`, so no verdict was produced, and it carries no prior `triage_bucket` either. Reads as a test entry, already declined; worth adding to a future pass only to give it a recorded verdict. |

---

## Recommended action today

1. **Decline `611589ba` with a pointer.** It is the only row needing a status
   flip this run. Recommended: `status='declined'`, pointing at `972af03c`
   (Canceled) and `44419de9` (Closed / JOB DECLINED-CLOSE). Roland runs it:
   ```sql
   UPDATE enhancement_requests
      SET status = 'declined',
          admin_notes = 'Duplicate of 972af03c (CANCELED) and 44419de9 (JOB DECLINED/CLOSE). Folded into the RO status batch.'
    WHERE id = '611589ba-2619-4d52-ae18-c1385370ce54';
   ```

2. **Promote the RO status batch to the front of the queue.** This is now the
   most-requested thing on the board: five separate filings from Lynn
   (`972af03c`, `eb00a68d`, `3e746c18`, `44419de9`, `611589ba`) for the same
   missing terminal statuses, and `5fbfa299` (Brandon) is partly solved by the
   same build. Before any code, get Lynn and Roland to confirm the final list -
   the union of every filing is Canceled, Closed, Totaled Out, Need Estimate,
   Awaiting Estimate, Estimate Declined, Job Declined/Close, which would take
   the canonical set from 15 to 22. Some of those plainly collapse (Closed vs
   Job Declined/Close vs Canceled). **One migration, one release**, and per the
   drift table each value must hit 10 hardcoded UI spots plus BOTH DB CHECK
   twins (`repair_orders` + `cashiered`) or saves throw 23514 - the S172 lesson
   where 'Off Lot - Returning' shipped UI-only and was broken for 50 days.
   Terminal statuses must also be excluded from the queue count
   (`js/render.js` L698-700) and the on-lot count (L760-765).

3. **No bug work is queued.** For the first time since this board has been
   tracked, the bug bucket is empty. The five prior bugs were closed by v1.494
   and v1.497. Nothing needs a hotfix branch today.

4. **No Data SQL is outstanding.** Both Data-bucket ERs (`6938c1af`,
   `3f254929`) are already applied in the live DB; their SQL is retained above
   only as the record. The one residual is cosmetic and dormant:
   `supabase/migrations/staff_table.sql` L85-87 still seeds "Wimbles", so a
   re-seed would reintroduce the misspelling.

5. **Chase the two blocked-on-submitter items** so they stop recycling through
   every pass: `8d885331` (Lynn - target parts-form field order) and
   `47428f75` (Lynn - what is actually wrong with the time-off calendar).

**Next sessions, unchanged from the last pass:** messages polish batch
(`9aab7550`, `21fc3a9d`, `f1e6e311`, `6e9ca4dc`, `ca0f7ed9` - one
`messages-v1.25`); WO labor-rate autofill (`2f752d40`) plus the check-in button
batch (`6bb61db2` + `e06d747e`); then the design-first items (task tracker
`af14b3cb`/`5925fc01`, check tracking `a195e4ab`, account merge `0e172b2c`,
remote e-sign `0448de62`, parts returns into GH#24 `b19975d4`, inventory XL
`14f35f95`, auth replacement XL `ed1b873e`).
