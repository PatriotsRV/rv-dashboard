# Enhancement Request Triage -- 2026-06-25 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-25T10:16Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.460** (checkin.html v1.38, customer-checkin.html v1.13, closed-ros.html v1.2, time-off.html v1.4).
**Total ERs:** 59 (incl. 3 test entries + 5 exact duplicates -> **51 distinct requests**).
**Triaged this run:** 58 ids in `needs_triage_ids`. n15 (test entry) carried over unchanged.

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `closed-ros.html`, `time-off.html`, `supabase/functions/*`,
SQL migrations) by grepping/reading the source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

**Bucket counts:** DONE 34 (incl. 3 tests) - NEEDED 19 - DUPLICATE 5 - DATA 1 - BUG 0.

---

## Headline

The bug bucket is empty again -- every previously reported defect is confirmed
fixed in the current tree, re-verified this run by grep:

- **n22** (Tipton) return-to-dashboard -> `checkin.html` v1.34 button (confirmed L36).
- **n33** (Bobby) currency dropping decimals -> `ro-crud.js` `editDollarValue .toFixed(2)`
  (L983) + `parts.js` `MONEY_FIELDS` (L307-310).
- **n40/n42** (Sofia) check-in sent no email -> `customer-checkin.html` v1.10/v1.11
  (`shouldNotifyStaff` + `getSignatureData` RAF fix).
- **n47** (Ryan) phone photo upload -> `photos.js` `uploadPhoto` appends input to DOM
  (L66) before `.click()` (L140).
- **n49/n54** (Sofia) returning-customer duplicate RO -> `customer-checkin.html` v1.12/v1.13
  dedupe guard.

Since the 2026-06-07 report, a large block of NEEDED items shipped to prod and
moved to DONE, all confirmed in current code: Key Dates (drop/promised/pickup
chips + reminders + calendar, v1.457/v1.459 -- n4, n24, n29, n36, n37), Keys+Power
RO fields (v1.458 -- n18, n34), parts Ticket Totals + Freight (v1.456 -- n28, n53),
work-list touch-drag reorder (v1.460 -- n10), per-silo work lists (n12), tech-notes
to RO timeline (v1.460 -- n35), service-added notify (v1.460 -- n50), worklist
auto-prune (S118 -- n43), 'Off Lot - Returning' status (v1.460 -- n26), time-off
entry editing (v1.4 -- n56), and the admin manager-worklist report (n39).

The only data-only item is **n25** (staff-name correction; SQL below -- already
applied per S117, included for the record). No code changes are queued from this
run.

---

## BUG bucket (front of the line)

None open. All prior bugs are shipped and re-verified in the current tree -- see Headline.

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` v1.2 reactivation restores full RO detail (parts/time_logs/photos). | done |
| n2 | Ryan | Warranty category on new RO | Warranty RO type v1.402 (`ro-crud.js` roType=='warranty' L1007; `render.js` warranty filter L112). | done |
| n5 | Lynn | Work list opened but couldn't view/do anything | Fixed Phase 14: `work-list.js` `renderWorkList` (L269) / `removeFromWorkList` (L195) / `_setWorkListSilo` (L263). | done |
| n10 | Riley | Can't delete / too small / no drag-drop on phone | v1.460: `work-list.js` touch-drag (`_reorderWorkList` L366 via touchstart/move/end); delete already worked. | done |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs` L223 / `_setWorkListSilo` L263). | done |
| n17 | Lynn | Add "Ready to Work" RO status | v1.410 (`config.js` STATUS_PROGRESS_MAP slot 50, L132). | done |
| n18 | Lynn | "Keep plugged in" flag; keys / no keys / keypad code | v1.458: `key_status`/`keypad_code`/`keep_plugged_in` on New+Edit RO (`ro-crud.js` L341-343/L422-424); migration `keys_power_fields.sql`. | done |
| n4 | Lynn | Scheduled future drop-off date + reminder | v1.457: planned drop-off on New+Edit RO (`ro-crud.js` L339/L420) + morning-before reminder; blue Drop chip (`render.js` L353). | done |
| n36 | Lynn | Scheduled drop date in entry dash | Satisfied by n4 (planned drop-off on New+Edit RO + reminder). | done |
| n24 | Brandon | Completion / pickup date for reference | v1.457: `pickup_date` on New+Edit RO (`ro-crud.js` L340/L421) + green Pickup chip (`render.js` L355); v1.459 calendar+reminders. | done |
| n29 | Lynn | Promised date large/highlighted on tile | v1.457: prominent amber promised chip, red if overdue (`render.js` L354); v1.459 reminders. Residual sort-by-promised toggle deferred. | done |
| n37 | Roland | RO dates as colored buttons + business calendar | Key Dates P1-P3 (v1.457/v1.459): Drop/Promised/Pickup chips (`render.js` L353-355) + per-silo Google Calendar sync (`scheduling.js` `syncKeyDateCalendars` L380) + reminders. | done |
| n21 | Roland | Fix customer check-in; email out; remove photo; streamline | `customer-checkin.html` v1.6 photo optional + RAF auto-email every drop-off; staff-notify v1.10/v1.11. | done |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | FIXED v1.447 (`checkin.html` v1.34 Return-to-Dashboard button, L36). | done |
| n33 | Bobby | Amounts drop the period and the zeros (425.00 -> 425) | FIXED v1.447 (`ro-crud.js` `editDollarValue .toFixed(2)` L983 + `parts.js` MONEY_FIELDS L307-310). | done |
| n34 | Brandon | Confirm if customer leaves keys with the unit | v1.458: `key_status` select (keys/no_keys/keypad) + `keypad_code` on New+Edit RO (`ro-crud.js` L341-342) + card chip. | done |
| n35 | Lynn | Transfer clock-in job notes -> RO updates | v1.460: `checkin.html` v1.38 copies clock-out work notes into the RO update timeline as a TECH NOTES ro_status note. | done |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning | v1.460: 'Off Lot - Returning' status (`render.js` statusColorMap L675; `config.js` L128; excluded from on-lot count L665). | done |
| n28 | Brandon | Parts + freight TOTAL in Manage Parts | v1.456: Ticket Totals bar (`parts.js` L147-160) - Parts/Freight(sum core_charge)/Total/Retail; recomputes on change. | done |
| n53 | Brandon | Freight included in ticket totals | v1.456: Ticket Totals bar includes Freight = sum(core_charge) (`parts.js` L153/L160). Satisfied by n28. | done |
| n39 | Roland | Admin daily manager-worklist report | `supabase/functions/send-manager-report` + daily cron. Residual manager-scoring + profit-margin deferred. | done |
| n40 | Sofia | Check-in no longer emails staff / no RAF copy | FIXED v1.10/v1.11: `shouldNotifyStaff` fires every drop-off + `getSignatureData` RAF fix. | done |
| n43 | Roland | Remove cashed-out RO from worklist next day | S118: daily auto-prune (migration `prune_cashiered_worklists.sql`). | done |
| n45 | Brandon | Distinguish on-lot RVs vs total customers | v1.453: distinct on-lot count (`render.js` statTotalOnLot L667-668); v1.454 excludes Scheduled. | done |
| n47 | Ryan | Can't upload pictures from phone | FIXED v1.453 (`photos.js` `uploadPhoto` appendChild L66 before .click() L140). | done |
| n48 | Brandon | Arrow keys to move through photos | v1.454: lightbox keydown (`photos.js` L389-393) Arrow Left/Right via `navigateLightbox`, Escape closes. | done |
| n49 | Sofia | Returning future-drop check-in duplicates RO | FIXED v1.12 (dedupe guard) + v1.13 hardening (`customer-checkin.html`). | done |
| n50 | Roland | Notify silo manager when a service is added to an RO | v1.460: `work-orders.js` `addServiceToRO` (L243) queues service_added_notify (L292); migration `service_added_notify_source.sql`. | done |
| n52 | Brandon | Status for scheduled-to-arrive (not yet on lot) | v1.454: on-lot/days-on-lot stats exclude 'Scheduled' (`render.js` L619/L623/L662). | done |
| n54 | Sofia | Scheduled returning customer: no RAF + duplicate | FIXED v1.13: submit-time dedupe by normalized phone/email + RAF send (`customer-checkin.html`). | done |
| n56 | Lynn | Can't edit time-off entries | v1.4: `time-off.html` `editRequest` (L806) opens modal pre-filled and UPDATEs; Edit button per row (L748). | done |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod and Travis last name should be Wombles, not Wimbles, in tech assignment" | Data, not code. `staff.name` holds the value (not hardcoded). Per S117 already corrected in DB; SQL below for the record. |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete jobs / too small on phone / drag-and-drop" | Exact duplicate of **n10** (identical text + timestamp); already declined. Fold into n10 (delivered v1.460). |
| n41 | Brandon | Completion / pickup date for records | Duplicate of **n24** (pickup_date, delivered v1.457); already declined. |
| n42 | Sofia | Returning customer check-in sent no email (Sheldon Gilbert) | Duplicate of **n40** (check-in notify, fixed v1.10/v1.11); already declined. |
| n44 | Roland | Remove cashed-out RO from worklist next day | Exact duplicate of **n43** (timestamps 1s apart); already declined. Folded into n43 (S118). |
| n51 | Sofia | Returning-customer duplicate test (Roland Shepard Jr) | Duplicate of **n49** (dup-RO guard, fixed v1.12/v1.13); already declined. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Notifications, reminders & task-tracking  *(n3, n8, n20, n30, n32)* -- biggest open cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes + per-update notification | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + adjustable font/color | L |

-> **Largest remaining opportunity.** v1.459 key-date reminders + v1.460 service-add
notify exist, but no general scheduled-task notifier or task-tracker entity (grep
confirmed absent). Design as one "Task Tracker + Reminders" subsystem; ties to the
deferred Twilio/Sendblue messaging path.

### Theme 2 -- Parts  *(n6, n7, n9, n27)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, priced and searchable) | XL |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier | L |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |

-> `parts.js` has no inventory/stock or return/warranty state (grep confirmed).
n6 is a new subsystem; n7/n27 are one return-state feature.

### Theme 3 -- RO fields & status nuance  *(n16, n19)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs existing Hybrid) |

### Theme 4 -- Work Orders / templates  *(n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will | M |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague -- overlaps n22) |

-> `work-orders.js` has `loadWOTemplate`/`saveWOTemplate` (create/apply) but no
template editing or reorder (grep: none).

### Theme 5 -- Work List  *(n31)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |

### Theme 6 -- Scheduling & key dates  *(n55)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n55 | Lynn | Hover near customer name to show important dates/tasks as a detail list | M |

-> Colored key-date chips shipped v1.457, but no hover tooltip aggregating
dates/tasks. Builds on the Key Dates work.

### Theme 7 -- Check-In & authorization  *(n46)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n46 | Sofia | Send the RV authorization form (RAF) remotely for e-signature | L |

-> `customer-checkin.html` signs the RAF only on-site (`getSignatureData`); no
tokenized remote-signing route (grep confirmed absent).

### Theme 8 -- Time Off  *(n57)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n57 | Lynn | Time-off needs an exact absence time window (clock-out to return; AM/PM half-day) instead of total hours | M |

-> Partial-day hours exist (`partial_hours`) but capture DURATION, not a start/end
window. Builds on the v1.4 time-off editing work (n56).

### Theme 9 -- Board & navigation  *(n58)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n58 | Lynn | Pin the Shop Operations RO tile to top of the board + make it findable by search | S |

-> Shop ROs render a Shop Operations badge (`render.js` L333) but there is no
pin/sort-to-top or search match for the tile.

### Theme 10 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

---

## Recommended action today

1. **Close the resolved ERs** still open in the DB: set `status=done` for the DONE-bucket
   rows that are not yet closed (n1, n2, n5, n10, n12, n17, n18, n4, n36, n24, n29, n37,
   n21, n22, n33, n34, n35, n26, n28, n53, n39, n40, n43, n45, n47, n48, n49, n50, n52,
   n54, n56). Duplicates (n11, n41, n42, n44, n51) -> `declined`. Most are already closed;
   `status_recommendation` in `er_verdicts.json` lists the target per id.
2. **n25 staff-name SQL** is data-only and already applied per S117 -- run again only if any
   `Wimbles` rows resurface.
3. **No code changes are queued from this run** -- the bug bucket is empty.

Near-term enhancement priorities: the **Task Tracker + Reminders** subsystem (Theme 1,
the biggest open cluster, n3/n8/n20/n30/n32) and the **Parts return-state** feature
(n7/n27, L). The two strategic asks -- **n38 (auth cost reduction, XL)** and the
manager-scoring residual of n39 -- warrant a design session with Roland before scoping.
