# PRVS Enhancement-Request Triage - 2026-07-27 (headless nightly run)

Automated pass over the `enhancement_requests` table against the current
modular codebase (index.html **v1.483**, pre-prod, Session 159). Input snapshot
`er_input.json` fetched 2026-07-27T10:45Z: **84 rows, 83 flagged for triage**
(the one skipped row is the `f73a1e5a` dictation test, already declined).

Every verdict below was re-checked by grepping/reading the live code this run -
no verdict was carried over on trust. Runner is read-only: **no status flips, no
code changes, no SQL executed.** Roland flips status himself.

## Board snapshot (DB status, before any flips)

| Status | Count |
|---|---|
| done | 49 |
| unreviewed | 26 |
| declined | 9 |
| **Total** | **84** |

## Triage result (this run's buckets, 83 rows)

| Bucket | Count |
|---|---|
| Done | 53 |
| Needed | 21 |
| Duplicate | 6 |
| Bug | 2 |
| Data | 1 |

**24 genuinely open items: 21 Needed + 2 Bugs + 1 Data correction.**
Size mix of the Needed bucket: **2 XL - 4 L - 3 M - 12 S.** The board has
shifted decisively toward small work since the 06-28 pass: S125-S159 shipped the
M-sized cluster (work-list auto-add, Monday drop-off report, shop tasks), and
the July intake is almost all quick UI polish.

Two verdicts changed direction versus the last written report (2026-06-28):
`cbc70a86`, `7f2d0d70` and `1fe68261` moved **Needed -> Done** (shipped S128/S129);
`1c7f1410` and `9d3806e7` arrived and are already **Done** (S151b/S154/S158).

## Code checks run this run

- inventory / on_hand / in_stock / stock_qty in `js/` + `supabase/migrations/` - **none** (confirms `14f35f95`)
- RMA / return reason / supplier credit - **none** beyond `PART_STATUSES` 'Returned' (`js/config.js` L200) and `pf_returnDeadline` (`js/parts.js` L226) (confirms `b19975d4`)
- per-RO tasks table (`ro_tasks` / `customer_task` / `task_tracker`) - **none**; `js/shop-tasks.js` has no `ro_id` link (confirms `af14b3cb`)
- `signing_token` / `esign` / `remote_sign` across `js/` + `supabase/` - **none** (confirms `0448de62`)
- Google auth still primary: `js/auth.js` L69 imports `GOOGLE_CONFIG`, One Tap + `signInWithIdToken` (confirms `ed1b873e`)
- `backToTop` / `window.scrollTo` in `index.html`, `js/*.js`, `css/*.css` - **none** (confirms `29654af7`)
- `favicon` / `rel=icon` / `apple-touch-icon` across all 13 `*.html` - **none** (confirms `d432863a`)
- Canceled RO status in `STATUS_PROGRESS_MAP` (L151-164) / `ALL_STATUSES` (L741-745) - **none** (confirms `972af03c`)
- `sortBy` / `sortSelect` / `sortMode` in `js/render.js` - **none**, board sorts on blended `calculatePriority` L31-39 (confirms `d1844e46`)
- labor-rate arithmetic / `LABOR_RATE` / `195` in `js/work-orders.js` - **none** (confirms `2f752d40`)
- `font-scale` / `fontScale` / `textSize` in `messages.html` - **none** (confirms `21fc3a9d`)
- `customerAddress` in `send-quote-email` - present only in the `lead_staff_notify` branch (L549/674/748), **absent** from `raf_copy` (L386-539) (confirms `5fec3eca`)
- `erCategory` present in only 6 of 13 HTML pages (residual noted under `ac394468`)

---

## Open items - Bugs first

Both are the same root cause and should be fixed as one change.

| ER | By | Date | Category | Bucket | Mapped code | Verdict | Severity |
|---|---|---|---|---|---|---|---|
| `9031e101` | Riley Scott | 07-21 | Work List | **Bug** | `js/work-list.js` L328 -> `index.html` `scrollToROInBoard` L4079 | Tapping a unit in the work list only scans **already-rendered** `[data-ro-index]` cards and matches `ro._supabaseId`; if the RO is filtered off the board it falls through to a "RO not visible" toast (L4094) instead of navigating. Fix: clear filters, re-render, then scroll to the tapped RO. | small (mobile nav, no data risk) |
| `d4d0eeba` | Brandon Dillon | 07-22 | General UI/UX | **Bug** | `index.html` L4079 + `js/qr.js` `handleDeepLink` L198 | Two paths, one cause. Work-list path as above. Messages path: the ro-chip link is `index.html?ro=<ro_id>` and `handleDeepLink` returns silently when the RO is not in `currentFilteredData` (L207) or has no rendered card (L209-210). Fix both together. | small (nav, no data risk) |

## Open items - Needed, by theme

### Notifications & Task-Tracking

| ER | By | Date | Category | Mapped code | Verdict | LOE |
|---|---|---|---|---|---|---|
| `af14b3cb` | Lynn Titel-Shepard | 05-21 | General UI/UX | `js/shop-tasks.js`, `supabase/migrations/` | Per-customer task tracker with daily reminder. Still absent: `shop-tasks.js` is a non-customer checklist with no `ro_id` and no reminder column; no per-RO tasks table exists. Needs table + add/complete UI + daily open-task reminder. | **L** |
| `5925fc01` | Lynn Titel-Shepard | 04-09 | New Feature Idea | `supabase/functions/send-manager-report`, `js/work-orders.js` L293 | **Partial.** The daily in-progress digest is served by the Manager Daily Report, and single-event notifies exist (`service_added_notify`, `urgent_update_notify`). Still open: notify on *every* RO update, and scraping to-dos out of status notes. Anchor of this theme. | **L** |

### Parts & Inventory

| ER | By | Date | Category | Mapped code | Verdict | LOE |
|---|---|---|---|---|---|---|
| `14f35f95` | Lynn Titel-Shepard | 04-09 | Parts & Ordering | `js/parts.js` (per-RO line items only) | In-house priced stock (order 25, use 5, 20 to searchable inventory). Zero inventory symbols anywhere in the codebase. New subsystem: inventory table + receive/consume/adjust + searchable stock + parts-request links. Largest item on the board. | **XL** |
| `b19975d4` | Lynn Titel-Shepard | 04-09 | Parts & Ordering | `js/config.js` L200, `js/parts.js` L226 | Return-to-supplier / warrantied-by-supplier tracking. Infra only: a 'Returned' part status and a Return Deadline date. No RMA number, return reason, credit or reverse-direction flow. Equals GH#24 Shop Ops Phase 2. Canonical for `86bbaf50`. | **L** |
| `8d885331` | Lynn Titel-Shepard | 04-09 | Parts & Ordering | `js/parts.js` L181-235 | Reorder the parts-entry fields. Form is unchanged (`pf_supplier` L181, `pf_partName` L190, `pf_qty` L200, `pf_status` L204, `pf_serviceSilo` L208, `pf_returnDeadline` L226). **Blocked on detail, not effort** - needs Lynn's target layout. | **S** |

### Authorization Form & E-Sign

| ER | By | Date | Category | Mapped code | Verdict | LOE |
|---|---|---|---|---|---|---|
| `0448de62` | Sofia Pedroza | 06-11 | General UI/UX | `customer-checkin.html`, `send-quote-email` L386 | Remote e-signature for the authorization form. Signing is kiosk-only (on-device pad); `raf_copy` only mails a copy *after* signing. Needs a tokenized public signing page + delivery + signed-form storage. Now pairs naturally with the Textly SMS channel. | **L** |
| `5fec3eca` | Sofia Pedroza | 07-15 | General UI/UX | `supabase/functions/send-quote-email` L386-539 | Show the customer's address on the emailed RAF copy. The `raf_copy` destructure (L387) has no `customerAddress`; only `lead_staff_notify` emits it (L549/674/748). Add the field to the payload plus the HTML and text templates. | **S** |

### Record Lifecycle & Cleanup (archive / cancel / merge)

New cluster this run - three ERs that all ask the same question: how does a
record leave the active board without being destroyed? Design them together.

| ER | By | Date | Category | Mapped code | Verdict | LOE |
|---|---|---|---|---|---|---|
| `5fbfa299` | Brandon Dillon | 06-30 | General UI/UX | `js/ro-crud.js` L658, L692 | Archive quoted-but-never-progressed customers/units. The only archive path is the cashiered one (`archiveROInSupabase` -> `cashiered_child_archives`); no archived/abandoned flag, status or board filter. Needs a soft-archive flag + board filter + un-archive path. | **M** |
| `0e172b2c` | Sofia Pedroza | 07-16 | General UI/UX | `js/duplicates.js` L35-51, L150 | Merge duplicate customer accounts (Hubert / Huebert). **Partial** - an RO-level merge engine exists but groups strictly on the `PRVS-XXXX-XXXX` base RO id (L36, L44), so a misspelled-name second RO is never detected. Needs name/phone account dedup + a verify-which-is-correct step. | **M** |
| `972af03c` | Lynn Titel-Shepard | 07-17 | General UI/UX | `js/config.js` L151-164, `js/render.js` L741-745 | Add a Canceled status that keeps the record for notes. No Canceled/Cancelled status exists in either status list. Add the status + color/priority maps and exclude it from on-lot and queue counts. | **S** |

### Messaging UX

All four are small polish items on `messages.html` / `js/messaging.js` and should
ship as one bundle.

| ER | By | Date | Category | Mapped code | Verdict | LOE |
|---|---|---|---|---|---|---|
| `9aab7550` | Brandon Dillon | 07-21 | General UI/UX | `messages.html` L260, L873, L879 | Make unchecked new messages clearly visible. **Partial** - a small gold `.unread-dot` renders only when `last_direction === 'inbound' && status === 'open'`, with no row-level highlight. Add a bold/tinted row and widen the condition so a reopened thread also flags. | **S** |
| `21fc3a9d` | Brandon Dillon | 07-21 | General UI/UX | `messages.html` | Rescale the conversation-list text. Fixed rem sizes, no user text-size control anywhere. Add a persisted font-scale control. | **S** |
| `f1e6e311` | Brandon Dillon | 07-21 | General UI/UX | `messages.html` L395, L845, L1597 | Clear (x) button on the messaging search bar. `convoSearch` is a plain input reset only programmatically (L788, L806). Add the button + re-run the filter. | **S** |
| `6e9ca4dc` | Brandon Dillon | 07-21 | General UI/UX | `js/messaging.js` L222, L241-246; `messages.html` L356 | Drop the redundant "Added to every send" strip. `messages.html` hides it **only** inside the `max-width:760px` media query, so desktop messages and the RO-card composer still show it. Remove it or collapse behind a toggle in all viewports; the signature keeps sending. | **S** |

### Board UX & Navigation

| ER | By | Date | Category | Mapped code | Verdict | LOE |
|---|---|---|---|---|---|---|
| `29654af7` | Lynn Titel-Shepard | 07-06 | General UI/UX | `index.html`, `js/*.js`, `css/*.css` | Back-to-top / jump-to-search button. No such control exists - only targeted `scrollIntoView` calls. Add a scroll-triggered control, including the sidebar layout. | **S** |
| `d1844e46` | Lynn Titel-Shepard | 07-16 | General UI/UX | `js/render.js` L31-39 | Sort by promised date then oldest on lot, shop ops still pinned. The board sorts on one blended `calculatePriority` score; no selectable sort mode exists. The shop pin (L35-37) is already there. Add the strict sort option. Distinct from `1aeb3f58`, which only delivered the promised-date chip. | **S** |
| `d432863a` | Brandon Dillon | 07-21 | General UI/UX | all 13 `*.html` | Patriots-logo tab icon. No favicon / `rel=icon` / `apple-touch-icon` on any page. Add favicon + apple-touch-icon to each head. | **S** |

### Time Clock & Check-In

Both need the submitter to confirm the target surface before build.

| ER | By | Date | Category | Mapped code | Verdict | LOE |
|---|---|---|---|---|---|---|
| `e06d747e` | Riley Scott | 04-10 | Time Clock / Check-In | `index.html`, `checkin.html`, `js/render.js` L228-235 | Tech check-in "float" for ease of access. No floating/sticky check-in control anywhere; compact row actions are edit/parts/WO/notify/message/schedule only. **Underspecified** - scope page + behavior with the submitter. Pairs with `6bb61db2`. | **S** |
| `6bb61db2` | Riley Scott | 07-22 | Time Clock / Check-In | `js/render.js` L228-235 | Check-in/out button on the compact view + a persistent "you are clocked into X" indicator at login. Neither exists. Add the compact action plus a login-time active-`time_log` banner across views. Pairs with `e06d747e`. | **S** |

### Work Orders

| ER | By | Date | Category | Mapped code | Verdict | LOE |
|---|---|---|---|---|---|---|
| `2f752d40` | Jason Rubin | 07-21 | Work Orders | `js/work-orders.js` L324, L360, L365, L649-650 | Auto-populate WO dollar value from total hours x $195/hr. `woFormDollar` and `woFormEstHours` are independent manual inputs read separately on save; no labor-rate constant exists. Use the task `est_hours` rollup when present, else the WO-level figure, with an editable override that respects the `dollarLocked` gate. | **S** |

### Auth & Cost

| ER | By | Date | Category | Mapped code | Verdict | LOE |
|---|---|---|---|---|---|---|
| `ed1b873e` | Roland Shepard Jr | 06-07 | General UI/UX | `js/auth.js` L44-48, L69; `js/config.js` | Replace ~$500/mo Google-email auth with DB-specific auth. Still Google One Tap + `signInWithIdToken`, RBAC keyed on email, kiosk included. Options: Supabase password / magic-link, or shared role accounts. Touches every page's sign-in plus RBAC. Roland wants to scope this together. | **XL** |

## Open item - Data correction (no code change)

| ER | By | Date | Category | Verdict |
|---|---|---|---|---|
| `3f254929` | Sofia Pedroza | 07-16 | General UI/UX | Keep **Steve Huebert**, remove the misspelled **Hubert** RO. SQL for Roland to run after eyeballing the rows: `SELECT id, ro_id, customer_name, status FROM repair_orders WHERE customer_name ILIKE '%ubert%';` then, once the wrong id is confirmed: `UPDATE repair_orders SET deleted_at = now() WHERE id = '<wrong-id>';` **Nothing was executed by this runner.** The merge *feature* is tracked separately as `0e172b2c`. |

---

## Closed - Duplicates (6, all already `declined`)

| ER | By | Date | Folds into | Verdict |
|---|---|---|---|---|
| `aa508852` | Riley Scott | 04-09 | `fd6c122d` | Identical text and timestamp (cannot delete / too small on phone / no drag-drop). Canonical is Done. |
| `86bbaf50` | Lynn Titel-Shepard | 05-13 | `b19975d4` | "Gordon parts missing / window ordered and returned to Vogt" is supplier return + warranty tracking. Canonical is Needed/L. |
| `67daba15` | Brandon Dillon | 06-09 | `d2561e11` | RO completion / pick-up date for records. Canonical is Done (`pickup_date` + Pickup chip). |
| `fa1c1d6e` | Sofia Pedroza | 06-09 | `a22739b9` | Returning check-in did not email (Sheldon Gilbert). Canonical is Done. |
| `5057c7fc` | Roland Shepard Jr | 06-09 | `91a03260` | Remove a cashed-out RO from the work list the day after Delivered. Canonical is Done. |
| `fc6b29f9` | Sofia Pedroza | 06-17 | `298e0e67` | Retest of the returning-customer duplicate. Canonical is Done. |

## Closed - Done (53, verified present in v1.483)

Two are test entries kept declined rather than done; the rest are shipped features
re-confirmed in code this run.

| ER | By | Date | Category | Mapped code | Verdict |
|---|---|---|---|---|---|
| `33750cb3` | Roland Shepard Jr | 04-09 | General UI/UX | - | Test entry ("Testing out the Make-A-Wish function"). No code action. Keep **declined**. |
| `19dc2016` | Roland Shepard Jr | 04-11 | Time Clock / Check-In | - | Test entry ("Testing ER form / dictation"). No code action. Keep **declined**. |
| `79b2c805` | Sofia Pedroza | 04-09 | New Feature Idea | `closed-ros.html` L838; `js/photos.js` L207 | `reactivateRO` restores a cashed-out RO from `cashiered_child_archives` for editing; `uploadDocument` adds documents to any RO. |
| `75124b03` | Ryan Dillon | 04-09 | RO Management | `js/ro-crud.js` L1119 | Warranty RO type on new-RO creation, plus the warranty board filter/badge. Shipped v1.402. |
| `b2aa4bea` | Lynn Titel-Shepard | 04-09 | New Feature Idea | `js/ro-crud.js` L409/486/508; `key_dates_reminder_sources.sql` | Scheduled future drop-off date, feeding the Drop chip and the `auto_dropoff_reminder` day-before reminder. |
| `18611eee` | Lynn Titel-Shepard | 04-09 | Bug Report | `js/work-list.js` L195, L223, L263 | Was a real bug. Work-list members are now viewable, reorderable and removable. |
| `17492066` | Lynn Titel-Shepard | 04-09 | Notifications / Email | `scheduled_notifications.sql`; `process-scheduled-notifications`; `index.html` L2862 | Per-RO Schedule Notification bell: future date/time, message, recipients, daily/weekly repeat. Covers the advance-warning ask. |
| `fd6c122d` | Riley Scott | 04-09 | Work List | `js/work-list.js` L195, L210, L304, L389, L404 | Delete + mouse and touch drag-reorder + mobile sizing. Shipped v1.460. Canonical for `aa508852`. |
| `98bd7498` | Brandon Dillon | 04-09 | RO Management | `js/work-list.js` L223, L263 | Per-silo Work List tabs give a separate list per job category. |
| `1fe68261` | Rod Wombles | 04-13 | Work Orders | `js/shop-tasks.js`; `supabase/migrations/shop_tasks.sql` | Shop Tasks panel tracks non-customer work (poly barrels, roof bays). Shipped v1.467 (S128). **Was Needed/M on 06-28.** |
| `3dfc0acb` | Lynn Titel-Shepard | 04-14 | Work Orders | `js/config.js` L158; `js/render.js` L743 | "Ready to Work" RO status. Shipped v1.410. |
| `b87eb2fb` | Lynn Titel-Shepard | 04-14 | RO Management | `keys_power_fields.sql`; `js/ro-crud.js` L411/488 | Keys / no keys / keypad + code + keep-plugged-in on the RO, with a card chip. Shipped v1.458. |
| `a5ff3e2d` | Lynn Titel-Shepard | 04-21 | RO Management | `js/ro-crud.js` L1119; `js/render.js` L122/196/373 | "Warranty + Repair" RO type - keeps warranty details but stays billable. Shipped v1.465. |
| `a7d1474e` | Lynn Titel-Shepard | 04-23 | RO Management | `urgent_update_field.sql`; `js/render.js` L324; `css/dashboard.css` L3446 | Always-visible red URGENT banner on the card; changing it notifies silo managers + admins. Shipped v1.466. |
| `dde90d2b` | Roland Shepard Jr | 04-27 | RO Management | `customer-checkin.html` L2467, L2540 | Photo optional; RAF copy + staff notice forced on every check-in submission. |
| `13e1cd87` | Tipton Scott | 04-30 | Time Clock / Check-In | `checkin.html` L1516 | Return-to-Dashboard button after clock-out. Shipped v1.447 (checkin v1.34). |
| `e27311bf` | Ryan Dillon | 05-04 | Work Orders | `js/work-orders.js` L385, L461, L583 | Drag-reorder WO task rows; DOM order drives `sort_order`, so templates are edited by load-reorder-Overwrite. Shipped v1.464. |
| `d2561e11` | Brandon Dillon | 05-06 | New Feature Idea | `key_dates_pickup_date.sql`; `js/render.js` L365, L397 | `pickup_date` + green Pickup key-date chip. Shipped v1.457. Canonical for `67daba15`. |
| `6938c1af` | Ryan Dillon | 05-06 | General UI/UX | `staff` table (runtime) | Data correction applied - names read from `staff.name` and submitter "Rod Wombles" now spells correctly. **Residual:** the seed file `supabase/migrations/staff_table.sql` L85-87 still inserts "Wimbles", so a re-seed reintroduces it. Re-apply if needed: `UPDATE staff SET name = REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |
| `dac9fdda` | Brandon Dillon | 05-06 | General UI/UX | `js/config.js` L154; `js/render.js` L253/451 | "Off Lot - Returning" status separates temporarily-taken units from never-arrived ones; both excluded from the on-lot count. Shipped v1.460. |
| `a8e90a7d` | Brandon Dillon | 05-20 | General UI/UX | `js/parts.js` L153-161 | Manage Parts Ticket Totals: parts + freight + total cost. Shipped v1.456. |
| `1aeb3f58` | Lynn Titel-Shepard | 05-21 | General UI/UX | `js/render.js` L33, L390-398 | Promised date as a large bold chip, red when overdue; promise date + days-on-lot feed the blended priority sort. Shipped v1.457. The strict selectable sort remains open as `d1844e46`. |
| `cbc70a86` | Lynn Titel-Shepard | 05-21 | General UI/UX | `customer-checkin.html` L2319; `work_list_autoadd_rpc.sql` L25 | Returning check-in auto-inserts `manager_work_lists` rows per involved silo lead, falling back to sr-managers. Shipped S128. **Was Needed/M on 06-28.** |
| `3f415c88` | Lynn Titel-Shepard | 05-26 | General UI/UX | `index.html` L2862, L2930 | Reminder Repeat none/daily/weekly + repeat-until, expanded into individually cancellable rows (cap 60). Shipped v1.463. |
| `cd663d8f` | Bobby Thatcher | 05-28 | General UI/UX | `js/ro-crud.js` L1093; `js/parts.js` L307-310 | Was a cosmetic bug - currency inputs now show 2 decimals. No data loss occurred. Fixed v1.447. |
| `34fc03c2` | Brandon Dillon | 05-29 | New Feature Idea | `keys_power_fields.sql` L6-7; `js/ro-crud.js` L411/488/1100 | Keys selector on New/Edit RO confirms whether keys are left with the unit. Shipped v1.458. Covers `b87eb2fb`. |
| `0b2e128c` | Lynn Titel-Shepard | 06-02 | General UI/UX | `checkin.html` L1455 | Clock-out work notes copied into the RO update timeline as a TECH NOTES row. Shipped v1.460. |
| `468aa376` | Lynn Titel-Shepard | 06-02 | General UI/UX | `js/ro-crud.js` L409, L486 | Planned drop-off date on New RO and (restored v1.457) Edit RO. Same field as `b2aa4bea`. |
| `2b814250` | Roland Shepard Jr | 06-07 | Calendar | `js/render.js` L363-398; `sync-ro-calendar`; `key_dates_cal_event_ids.sql` | Key dates as attention-grabbing chips + hover panel, server-side team-calendar events, advance reminders to managers/admins. |
| `cd084233` | Roland Shepard Jr | 06-07 | Work List | `send-manager-report`; `send-admin-pnl-report`; `worklist-report.html` | Admin-level daily manager-worklist reporting with parts, labor and cost. The manager **scoring** layer described at the end remains a future extension, not this ER. |
| `a22739b9` | Sofia Pedroza | 06-08 | General UI/UX | `customer-checkin.html` L2467, L2540 | Was a notify-gate regression - both the check-in notice and the RAF copy are now forced. Canonical for `fa1c1d6e`. |
| `91a03260` | Roland Shepard Jr | 06-09 | Work List | `prune_cashiered_worklists.sql` | Cashed-out RO drops off manager work lists the day *after* Delivered (Chicago-local gate). Canonical for `5057c7fc`. |
| `cdd77a8b` | Brandon Dillon | 06-10 | General UI/UX | `js/render.js` L656-658, L674-676 | Total RVs splits on-lot vs total customers; shop ROs and not-yet-arrived units excluded. Shipped v1.453/v1.454. |
| `2c3d5633` | Ryan Dillon | 06-12 | General UI/UX | `js/photos.js` L66 | Was a mobile-only functional bug - the upload input is now attached to the DOM before click, fixing the iOS Safari dropped-change-event. Fixed v1.453. |
| `74a33621` | Brandon Dillon | 06-16 | General UI/UX | `js/photos.js` L380, L382, L396 | Arrow-key lightbox navigation + Escape-to-close. Shipped v1.454. |
| `298e0e67` | Sofia Pedroza | 06-16 | General UI/UX | `customer-checkin.html` L18-28, L2047-2049 | Was a data-integrity bug - a returning check-in reuses the existing scheduled RO instead of duplicating. Canonical for `fc6b29f9`. |
| `9b823d25` | Roland Shepard Jr | 06-16 | RO Management | `js/work-orders.js` L265, L293; `service_added_notify_source.sql` | Adding a service to an RO emails the owning silo manager. Shipped v1.460. |
| `9b046808` | Brandon Dillon | 06-17 | General UI/UX | `js/config.js` L157; `js/render.js` L674-676 | "Scheduled" is its own status and not-yet-arrived units are excluded from on-lot and days-on-lot. Shipped v1.454. |
| `671fa623` | Brandon Dillon | 06-18 | General UI/UX | `js/parts.js` L153-161 | Freight is summed into the parts ticket total. Shipped v1.456. |
| `5c45bb8d` | Sofia Pedroza | 06-19 | General UI/UX | `customer-checkin.html` L2047-2049, L2467 | Both halves of the David Flanagan report fixed - no duplicate RO, and the RAF email sends. |
| `b06a285f` | Lynn Titel-Shepard | 06-19 | General UI/UX | `js/render.js` L363-367; `css/dashboard.css` L3438 | Calendar icon beside the customer name opens a full key-dates panel on hover or tap. Shipped v1.465. |
| `ee50d11c` | Lynn Titel-Shepard | 06-19 | General UI/UX | `time-off.html` L752, L864, L895 | Time-off entries are editable - per-row Edit loads the entry back into the form and flips the button to "Update Record". |
| `88395acc` | Lynn Titel-Shepard | 06-19 | General UI/UX | `add_absence_window_to_time_off.sql`; `time-off.html` L368-372, L781-796 | Exact absence window (clock-out to return) rather than an hour total. Time Off v1.5. |
| `80390f36` | Lynn Titel-Shepard | 06-23 | General UI/UX | `js/render.js` L35-37, L61 | Shop Operations tiles pinned to the top and reliably matched by search. Shipped v1.462. |
| `7f2d0d70` | Lynn Titel-Shepard | 06-26 | General UI/UX | `supabase/functions/send-dropoff-report` L176 | Monday drop-off report lists the week's scheduled drops + last week's arrivals and flags any not on the right team's work list. **Was Needed/M on 06-28.** |
| `3f7fb4fc` | Brandon Dillon | 06-29 | General UI/UX | `js/render.js` L249, L653-658 | Was a small data-display bug - shop ROs excluded from queue/on-lot counts and their status dropdown replaced by a static label. Fixed v1.468. |
| `cb7742a8` | Lynn Titel-Shepard | 06-29 | General UI/UX | `sync-ro-calendar`; `customer-checkin.html` L2182 | Server-side calendar sync posts drop-off/promised/pickup to the correct team calendar for every RO including front-desk check-ins. Shipped v1.469. |
| `39b75fab` | Sofia Pedroza | 07-09 | General UI/UX | `js/photos.js` L198-201, L376-377 | Rename / move-to-another-RO / delete for documents and photos, office-staff gated. Shipped v1.472 - the Marco DeAlba docs can be moved off the Ruiz RO. |
| `1c7f1410` | Andrew Page | 07-16 | New Feature Idea | `js/messaging.js` L202, L207, L216-219, L256-264 | Message attachments ship: images + PDFs, multiple, up to 5, removable chips, `media_url` to `textly-send`. S151b extended S158. **Recommend status = done.** |
| `9d3806e7` | Brandon Dillon | 07-22 | General UI/UX | `messages.html` L452-454, L514, L1000, L1034; `customer_note_entries_s154.sql` | Customer-side Notes section + Add-Notes modal writing append-only rows keyed by phone. Shipped S154. **Recommend status = done.** |
| `ac394468` | Roland Shepard Jr | 07-24 | General UI/UX | `index.html` L7505-7507; `js/enhancement.js` L43-53 | Make-a-Wish categories now cover every page and `pageDefaults` maps them. Shipped v1.482. **Residual:** the lamp modal (`erCategory`) exists in only 6 of 13 HTML files - messages / leads / time-off / review / guide still cannot raise an ER. Worth a fresh small ER. |
| `d62a26e4` | Roland Shepard Jr | 07-24 | New Feature Idea | `js/messaging.js` L202, L207, L219, L256-264 | Multi-attachment composer, chunked to one media per MMS after a carrier payload cap silently stripped multi-media sends. Shipped v1.482 / S158a. |
| `93b00023` | Roland Shepard Jr | 07-24 | New Feature Idea | `textly-webhook` L505; `send-unreplied-reminder`; `unreplied_eod_reminder_s158.sql` | All three parts live: inbound reopens the conversation and logs a `reopened` event, owner notify fires on the needs-reply flip only, and a 4:30pm unread sweep runs on cron. Shipped v1.482 (S158). |

## Not triaged this run

| ER | By | Date | Reason |
|---|---|---|---|
| `f73a1e5a` | Roland Shepard Jr | 04-13 | "Testing dictation 123" - test entry, excluded from `needs_triage_ids`, already `declined`. No bucket assigned. |

---

## Recommended action today

1. **Fix the bug pair first.** `9031e101` and `d4d0eeba` are one root cause with
   two entry points (work-list tap, Messages ro-chip deep link). One change -
   clear filters, re-render, then scroll and highlight - closes both. Small,
   auto-fixable class, no data risk. This is the only code work the triage
   recommends jumping the queue.
2. **Ship the Messaging UX bundle.** `9aab7550`, `21fc3a9d`, `f1e6e311` and
   `6e9ca4dc` are four S's confined to `messages.html` / `js/messaging.js`.
   Bundling them is one session and clears a sixth of the open board. Brandon
   filed all four on the same day, so a single release answers him fully.
3. **Then the Board UX trio.** `d432863a` (favicon), `29654af7` (back to top)
   and `d1844e46` (strict sort) are independent S's with no design questions.
4. **Unblock the two underspecified S's** by asking the submitters: Lynn's parts
   field order (`8d885331`) and Riley's check-in float (`e06d747e`, which should
   be scoped together with `6bb61db2`). All three are small *once specified*.
5. **Design the Record Lifecycle cluster as one.** `5fbfa299` (archive),
   `972af03c` (cancel) and `0e172b2c` (account merge) are the same underlying
   question. Deciding the model once avoids three incompatible one-offs.
6. **Hand Roland the one data fix.** `3f254929` - Huebert/Hubert. SQL is in the
   Data section above; verify the rows before running the soft delete.
7. **Plan, do not rush, the XLs.** The auth swap (`ed1b873e`) and parts inventory
   (`14f35f95`) each deserve their own scoping session. Auth is the higher-value
   one - roughly $500/mo - and Roland has asked to scope it together.
8. **Status flips for Roland** (runner made none): 53 Done and 6 Duplicate rows
   are ready. The 26 `unreviewed` rows include `1c7f1410` and `9d3806e7`, both
   verified shipped - those two are the only *new* Done flips this run.
9. **Two residuals worth their own ERs:** the `staff_table.sql` seed still
   spells "Wimbles" (`6938c1af`), and the Make-a-Wish lamp is missing from
   messages / leads / time-off / review / guide (`ac394468`).
