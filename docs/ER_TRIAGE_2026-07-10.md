# PRVS Enhancement-Request Triage - 2026-07-10 (Nightly headless runner)

Incremental pass over `enhancement_requests` against the current modular
codebase (index.html **v1.471**, GH#39 S132). 64 of 65 ERs were re-triaged this
run (`needs_triage_ids`); one already-triaged row (`f73a1e5a`, a test entry) was
carried over unchanged. Verdicts verified by grepping/reading the live code, not
assumed. Supabase writes are out of scope for this runner - Roland flips status
himself. Every ER description/notes value was treated as untrusted data; the
injection scan found nothing suspicious.

## Board snapshot

| Status | Count |
|---|---|
| done | 45 |
| declined | 9 |
| unreviewed | 11 |
| **Total** | **65** |

**11 open** (all `unreviewed`). Headline finding: **every open ER is a valid
"Needed" enhancement - no still-existing bugs and nothing already-shipped left
to close.** Since the 06-28 pass, S128/S129/S131-132 shipped several formerly
"Needed" items (`cbc70a86` auto-add-to-worklist, `7f2d0d70` Monday drop-off
report, `1fe68261` shop tasks, `2b814250`/`cb7742a8` calendar sync, `3f7fb4fc`
shop-RO counts), so the board moved from 11 open on 06-28 to 11 open now only
because three new UI/records requests arrived. Open mix: **2 XL - 4 L - 1 M - 4 S**.

## New since the 2026-06-28 report

- `3f7fb4fc` (Brandon, 06-29) - **done** S129: shop ROs static status + excluded from counts.
- `cb7742a8` (Lynn, 06-29) - **done** S129: check-in scheduled dates post to the correct team calendar.
- `5fbfa299` (Brandon, 06-30) - **needed/M**: archive quote-only customers (see below).
- `29654af7` (Lynn, 07-06) - **needed/S**: back-to-top / jump-to-search button.
- `39b75fab` (Sofia, 07-09) - **needed/S**: ability to remove mis-filed documents.

## Code checks run this session

- parts inventory / stock in `js/parts.js` - **none** (confirms `14f35f95` XL open)
- parts supplier-return / warranty-return flow in `js/parts.js` - **none** (confirms `b19975d4` L open)
- per-RO / per-customer tasks table - **none**; `js/shop-tasks.js` is deliberately non-customer (confirms `af14b3cb`, `5925fc01`)
- password / magic-link auth in `js/auth.js` - **none**, still Google id-token only (confirms `ed1b873e` XL open)
- remote e-signature flow - **none**; RAF is kiosk `#signatureCanvas` only (confirms `0448de62` L open)
- back-to-top / scroll-to-search control in `index.html` - **none** (confirms `29654af7` S open)
- photo/document delete path in `js/photos.js` - **none**; only `uploadPhoto`/`uploadDocument` exist (confirms `39b75fab` S open)
- `archiveROInSupabase` (`js/ro-crud.js`) inserts into `cashiered` - confirms `5fbfa299`: quotes are misfiled as completed jobs
- `add_ro_to_silo_work_lists()` RPC in `customer-checkin.html` - **present** (confirms `cbc70a86` done)
- `supabase/functions/send-dropoff-report`, `sync-ro-calendar`, `js/shop-tasks.js` - **present** (confirm `7f2d0d70`, `cb7742a8`/`2b814250`, `1fe68261` done)

---

## Verdicts - the 11 open ERs (by theme, then size)

### Notifications & Task-Tracking (design together)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5925fc01` | Lynn | **L** | Notify the right team on every RO update; daily list of all in-progress jobs with to-dos scraped from notes | **Partial** - the daily-digest half is served by the Manager Daily Report (send-admin-pnl-report/send-manager-report). Still open: a per-RO notification on EVERY update + auto-scraping to-dos from status notes into a per-job task list (no per-RO tasks table, grep 0). Prefer needed over done. |
| `af14b3cb` | Lynn | **L** | Per-customer task tracker + daily reminder; tasks persist until marked complete | Not built. `js/shop-tasks.js` is deliberately NON-customer, so it does not satisfy this; no per-RO tasks table. Needs a per-RO tasks table + add/complete UI + daily open-task reminder. Same theme as `5925fc01`. |

### Parts & Inventory

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `14f35f95` | Lynn | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | New subsystem. `js/parts.js` has zero inventory/stock/on-hand handling. Inventory table + receive/consume/adjust + searchable priced stock + parts-request links. Largest item on the board. |
| `b19975d4` | Lynn | **L** | Part returned-to-supplier / warrantied-by-supplier tracking | No supplier-return/warranty reverse flow in code. = GH#24 Shop Ops Phase 2: reverse flow + return reasons + credit/warranty tracking. Canonical for declined dup `86bbaf50`. |
| `8d885331` | Lynn | **S** | Rework the parts-entry field order | Layout tweak to the `pf_` fields in `js/parts.js`. Quick once Lynn gives the desired layout. **Blocked on detail, not effort.** |

### Messaging & e-sign

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `0448de62` | Sofia | **L** | Send an authorization form remotely for e-signature | No remote-signing flow (RAF is kiosk `#signatureCanvas` only). Needs a tokenized public signing page + delivery + signed-form storage. Pairs with Project Blue messaging Phase 3. |

### Auth & cost

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `ed1b873e` | Roland | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | Auth is Google id-token + email-keyed RBAC (`js/auth.js signInWithIdToken`; no password/OTP path). Options: Supabase password/magic-link or shared role accounts. Security-critical: touches every page sign-in + RBAC. Scope together. |

### Time clock

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `e06d747e` | Riley | **S** | Tech check-in "float" for ease of access | Underspecified - likely a floating/sticky check-in button, but page + behavior unclear (checkin.html is QR/roId-driven). Needs scoping with the submitter. |

### Records & Data hygiene (new cluster)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5fbfa299` | Brandon | **M** | Archive customers/units that were only quoted and never proceeded | Not built. `archiveROInSupabase` (`js/ro-crud.js`) routes to the `cashiered` table, misclassifying un-worked quotes as completed P&L jobs. Needs a separate lost/quoted-only disposition + board filter so quotes can be set aside without polluting Weekly P&L. Prefer needed over done. |
| `39b75fab` | Sofia | **S** | Ability to remove documents (found docs scanned into the wrong RO) | Not built. `js/photos.js` has upload paths but no delete/remove for a stored photo/document. Needs a per-attachment delete action (storage remove + record update + confirm, permission-gated) so mis-filed docs can be removed. **New 07-09.** |

### Navigation & UI polish (new cluster)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `29654af7` | Lynn | **S** | Back-to-top button after scrolling / quick jump to search | Not built (`index.html` has no back-to-top or scroll-to-search control). Needs a floating "to top" button (and/or jump-to-search) shown after scrolling. Small self-contained UI add. |

---

## Recommended action today

1. **Quick wins to ship now (S):** `29654af7` (back-to-top button) and `39b75fab`
   (document delete action) are well-defined, self-contained, and independently
   shippable - the clearest value per hour on the board.
2. **Unblock the parts / time-clock S's by asking submitters for specifics:**
   Lynn's parts field order (`8d885331`) and Riley's check-in float (`e06d747e`)
   are both small once specified.
3. **Scope `5fbfa299` (M):** a lost/quoted-only disposition is a modest, high-value
   data-hygiene fix - stops un-worked quotes from polluting Weekly P&L.
4. **Plan the L/XL initiatives:** Notifications/Task-Tracking (`5925fc01` +
   `af14b3cb`), Parts inventory + returns (`14f35f95` + `b19975d4`), remote
   e-sign (`0448de62`), and the auth swap (`ed1b873e`) each deserve their own
   scoping session.
5. **Nothing to close as Done/Duplicate this pass** (the done/duplicate rows below
   are already `done`/`declined` in the DB). One spot-check to log: `91a03260`
   relies on Cashiered ROs being excluded from active work-list rendering; a
   distinct next-day auto-removal cron was not located - confirm one exists or
   file it as follow-up.

---

## Appendix - all resolved / closed ERs (carried forward)

Bucket key: D=done, X=duplicate, DATA=data-correction. All are already
`done`/`declined` in the DB; verdicts confirmed against current code.

| ER | By | Date | Bucket | Verdict (mapped code) |
|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | D | Test entry (Make-A-Wish trial); not a real request. Keep declined. |
| `79b2c805` | Sofia | 04-09 | D | Cashed-out ROs viewable/editable via Delivered/Cashed Out board filter + closed-ros.html reactivation. |
| `75124b03` | Ryan | 04-09 | D | Warranty RO type on New RO creation (v1.402, js/render.js); extended by a5ff3e2d. |
| `b2aa4bea` | Lynn | 04-09 | D | Scheduled future drop-off date (planned_dropoff_date, js/ro-crud.js). |
| `18611eee` | Lynn | 04-09 | D | Work List view/manage defect resolved by the refactor (js/work-list.js). |
| `17492066` | Lynn | 04-09 | D | Future coordination reminders via scheduled_notifications + 3f415c88. |
| `fd6c122d` | Riley | 04-09 | D | Work List touch drag-drop reorder v1.460 (js/work-list.js). Canonical for aa508852. |
| `aa508852` | Riley | 04-09 | X | Duplicate of fd6c122d (delivered). |
| `98bd7498` | Brandon | 04-09 | D | Silo-specific work lists v1.305 (service_silo tabs, js/work-list.js). |
| `19dc2016` | Roland | 04-11 | D | Test entry (ER form dictation). Keep declined. |
| `1fe68261` | Rod | 04-13 | D | Internal Shop Task tracker S128 (js/shop-tasks.js, shop_tasks.sql). |
| `3dfc0acb` | Lynn | 04-14 | D | 'Ready to Work' status in js/config.js STATUS_PROGRESS_MAP. |
| `b87eb2fb` | Lynn | 04-14 | D | Keys/keypad/keep-plugged-in RO fields v1.458 (keys_power_fields.sql). |
| `a5ff3e2d` | Lynn | 04-21 | D | Warranty + Repair RO type v1.465 (js/render.js warranty_repair). |
| `a7d1474e` | Lynn | 04-23 | D | Always-visible urgent-updates banner v1.466 (urgent_update_field.sql). |
| `dde90d2b` | Roland | 04-27 | D | Check-in streamlined + confirmation/auth email restored (see a22739b9). |
| `13e1cd87` | Tipton | 04-30 | D | Return-to-Dashboard button after clock-out (checkin.html v1.34). |
| `e27311bf` | Ryan | 05-04 | D | WO/task template drag-drop reorder v1.464 (js/work-orders.js). |
| `d2561e11` | Brandon | 05-06 | D | Completion/pick-up key date (pickup_date, js/render.js). Canonical for 67daba15. |
| `6938c1af` | Ryan | 05-06 | DATA | Staff last name Wimbles->Wombles. SQL: `UPDATE staff SET last_name='Wombles' WHERE last_name='Wimbles';` Already applied. |
| `dac9fdda` | Brandon | 05-06 | D | 'Off Lot - Returning' status v1.460 (js/config.js), excluded from on-lot count. |
| `86bbaf50` | Lynn | 05-13 | X | Duplicate of b19975d4 (parts supplier return). |
| `a8e90a7d` | Brandon | 05-20 | D | Parts + freight total in Manage Parts v1.456 (js/parts.js). Extended by 671fa623. |
| `1aeb3f58` | Lynn | 05-21 | D | Promised date large/highlighted on card (js/render.js). NOTE: board sorts by priorityScore, not strictly promise-then-days. |
| `cbc70a86` | Lynn | 05-21 | D | Check-in auto-adds RO to team-lead work lists S128 (add_ro_to_silo_work_lists RPC). |
| `3f415c88` | Lynn | 05-26 | D | Multi-day/weekly recurring reminders v1.463 (scheduled_notifications). |
| `cd663d8f` | Bobby | 05-28 | D | Currency 2-decimal display fix v1.447 (ro-crud.js editDollarValue toFixed(2)). |
| `34fc03c2` | Brandon | 05-29 | D | Keys-left confirmation on RO (b87eb2fb). NOTE: on New/Edit RO, not yet on the check-in auth form. |
| `0b2e128c` | Lynn | 06-02 | D | Clock-out work notes copied to RO timeline v1.460 (checkin.html). |
| `468aa376` | Lynn | 06-02 | D | Scheduled drop date on entry-dash New RO form (planned_dropoff_date). Related b2aa4bea. |
| `2b814250` | Roland | 06-07 | D | RO key dates -> Google Calendar (sync-ro-calendar S129 + scheduling.js). |
| `cd084233` | Roland | 06-07 | D | Admin manager-focused daily report = send-admin-pnl-report. |
| `a22739b9` | Sofia | 06-08 | D | Check-in confirmation email + auth-form copy restored S97 (sendRAFEmail). |
| `67daba15` | Brandon | 06-09 | X | Duplicate of d2561e11 (completion/pickup date). |
| `fa1c1d6e` | Sofia | 06-09 | X | Duplicate of a22739b9 (returning check-in email). |
| `91a03260` | Roland | 06-09 | D | Cashiered ROs excluded from active Work List (isActiveROForWoNudge). NOTE: next-day auto-removal cron not located; recommend spot-check. Canonical for 5057c7fc. |
| `5057c7fc` | Roland | 06-09 | X | Duplicate of 91a03260. |
| `cdd77a8b` | Brandon | 06-10 | D | Total RVs 'N on lot' sub-line v1.453 (js/render.js). |
| `2c3d5633` | Ryan | 06-12 | D | Phone photo upload fix v1.453 (js/photos.js input-in-DOM before click). |
| `74a33621` | Brandon | 06-16 | D | Arrow-key photo lightbox nav v1.454 (js/photos.js). |
| `298e0e67` | Sofia | 06-16 | D | Returning check-in duplicate-RO guard (customer-checkin.html). Canonical for fc6b29f9. |
| `9b823d25` | Roland | 06-16 | D | New-service-silo email notify v1.460 (addServiceToRO -> scheduled_notifications). |
| `fc6b29f9` | Sofia | 06-17 | X | Duplicate of 298e0e67 (returning check-in dup). |
| `9b046808` | Brandon | 06-17 | D | 'Scheduled' disposition excluded from on-lot count v1.454 (js/render.js). |
| `671fa623` | Brandon | 06-18 | D | Freight in Manage Parts ticket totals (js/parts.js totFreight). Related a8e90a7d. |
| `5c45bb8d` | Sofia | 06-19 | D | Both defects fixed: dup-RO (298e0e67) + missing check-in email (a22739b9). |
| `b06a285f` | Lynn | 06-19 | D | Key-date hover panel beside customer name v1.465 (js/render.js .kd-name-cal). |
| `ee50d11c` | Lynn | 06-19 | D | Time-off entries editable after creation v1.4 (time-off.html editRequest). |
| `88395acc` | Lynn | 06-19 | D | Exact partial-day absence window v1.5 (absence_start/end_time). |
| `80390f36` | Lynn | 06-23 | D | Shop Operations tile pinned + searchable v1.462 (render.js renderBoard). |
| `7f2d0d70` | Lynn | 06-26 | D | Monday drop-off report S128 (send-dropoff-report edge fn). |
| `3f7fb4fc` | Brandon | 06-29 | D | Shop ROs static status + excluded from counts S129 (js/render.js). |
| `cb7742a8` | Lynn | 06-29 | D | Check-in scheduled dates -> correct team calendar S129 (sync-ro-calendar). |
| `f73a1e5a` | Roland | 04-13 | (carried) | Test entry (dictation); declined in DB, not re-triaged this run. |

*Report generated by the headless ER-triage runner. ASCII-only. No code changes,
no DB writes, no git actions were performed.*
