# PRVS Enhancement-Request Triage -- 2026-07-25 (headless nightly runner)

Incremental pass over `enhancement_requests` (fetched 2026-07-25 09:01 UTC)
against the current modular codebase (index.html **v1.481**, S156 head).
83 of the 84 rows were re-triaged this run; 1 row (`f73a1e5a`) was outside the
run set and is carried over unchanged. Read-only on the codebase and on the DB:
no code was changed, no status was flipped, and the one Data-bucket item is
emitted as guarded SQL for Roland. Every Done/Bug verdict below was re-verified
by grepping/reading the live code, not assumed.

## Board snapshot (DB `status`, all 84 rows)

| Status | Count |
|---|---|
| done | 46 |
| declined | 9 |
| unreviewed | 29 |
| **Total** | **84** |

## This run's verdicts (83 classified)

| Bucket | Count |
|---|---|
| done (already delivered) | 50 |
| needed (open enhancement) | 24 |
| duplicate | 6 |
| bug (still present) | 2 |
| data (SQL, no code) | 1 |

Of the **29 `unreviewed`** rows, 2 are already delivered and can be closed
(`1c7f1410`, `9d3806e7`); the real open backlog is **24 needed + 2 bugs + 1 data
correction**. The needed mix is **2 XL - 4 L - 3 M - 15 S**: unlike the 06-28
pass, the board is now dominated by small, well-specified wins, almost all of
them in Messages.

Nothing that looked like an instruction to the triage runner was found in any
`description` / `admin_notes` value this run; no prompt-injection flags raised.

## Code checks run this session

- Make-A-Wish category select (index.html L7455-7466) + ER admin filter (L7548+) -- **no Messages option**; genie lamp present on only 6 of 13 pages (confirms `ac394468`)
- `js/messaging.js` attach path -- `input#msgAttach` (L208) has **no `multiple`**, `accept="image/*"` only, single `_msgAttachment` (L192) (confirms `d62a26e4`)
- `supabase/functions/textly-send` L127-171 -- **already accepts a media array** and maps it to Textable `media[]` (sizes `d62a26e4` down to S)
- `supabase/functions/textly-webhook` inbound upsert L447-452 -- **never sets `status`**, so an inbound message does not reopen a closed conversation (confirms `93b00023`a)
- `assigned_inbound_notify` fork L565-621 -- exists, but dedupes on a 60-minute window, not on the reopening message (`93b00023`b partial)
- unread / 16:30 reminder in `supabase/` -- **none** (`stale_message_alarm` is about stalled *outbound* sends) (confirms `93b00023`c)
- `scrollToROInBoard` (index.html L4047) + `js/qr.js handleDeepLink` L207/L210 -- both still bail out silently/with a toast when the RO is filtered out (confirms bugs `9031e101`, `d4d0eeba`)
- parts inventory / stock symbols in `js/*.js` -- **none** (confirms `14f35f95`)
- RMA / return-reason / credit symbols -- **none**; only `PART_STATUSES` 'Returned' (confirms `b19975d4`)
- per-RO customer task table in `supabase/migrations/` -- **none**; `shop_tasks.sql` is the non-customer list (confirms `af14b3cb`)
- `js/auth.js signInWithIdToken` + `GOOGLE_CONFIG` (config.js L79) -- **still Google-based** (confirms `ed1b873e`)
- favicon / apple-touch-icon in any `*.html` -- **none** (confirms `d432863a`)
- `Canceled` in render.js / config.js / ro-crud.js -- **none** (confirms `972af03c`)
- `window.scrollTo` / back-to-top in index.html + js -- **none** (confirms `29654af7`)
- `send-quote-email` raf_copy branch L386-387 -- no `customerAddress` (confirms `5fec3eca`)
- `js/work-orders.js` woFormDollar L360 / woFormEstHours L365 -- independent manual fields, no auto-calc (confirms `2f752d40`)
- `add_ro_to_silo_work_lists` RPC called at customer-checkin.html L2319 -- **is** wired (confirms `cbc70a86` = Done)
- `js/photos.js` canManageFiles / deleteLibraryItem / renameLibraryDoc / moveLibraryItem -- present (confirms `39b75fab` = Done)
- `customer_note_entries` + notesModal in messages.html -- present (confirms `9d3806e7` = Done)

---

## Bugs -- front of the line (2, both still present, both small)

Both are the **same root cause**: navigating to an RO only works if that RO
happens to be rendered on the board right now. Fix them together.

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `9031e101` | Riley | small | Tapping a unit in the Work List on iPhone does not take you to the unit | Still present. `js/work-list.js` L328 -> `scrollToROInBoard` (index.html L4047) scans only already-rendered `.ro-card[data-ro-index]` nodes; a filtered-out RO falls through to the "RO not visible" toast (with a Clear Filters action) instead of navigating. |
| `d4d0eeba` | Brandon | small | Clicking an RO in my list, or an RO link in Messages, does not track to it | Still present. Work-list path as above; the Messages path is `index.html?ro=<ro_id>` (messages.html L1106) -> `js/qr.js handleDeepLink`, which returns silently at L207 (not in current filter view) or L210 (no card). |

Recommended fix (single change, auto-fix-class): clear the active filters, force
a re-render, then scroll + highlight the target RO; fall back to the toast only
if the RO truly is not in `currentData`.

## Data bucket -- SQL for Roland, no code (1)

`3f254929` (Sofia) -- keep **Steve Huebert**, remove the misspelled **Hubert** RO:

```sql
-- 1) identify both rows
SELECT id, ro_id, customer_name, status, created_at
FROM repair_orders
WHERE customer_name ILIKE '%ubert%';

-- 2) after confirming which id is the misspelling, soft-delete it
UPDATE repair_orders SET deleted_at = now() WHERE id = '<wrong-id>';
```

The account-merge *feature* Sofia also asked for is `0e172b2c` (Needed/M).

---

## Open "Needed" ERs by theme (24)

### Messaging & e-sign (7 -- the biggest cluster, and mostly S)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `d62a26e4` | Roland | **S** | Attach more than one photo/document to a customer message | Only one is possible: `input#msgAttach` (messaging.js L208) has no `multiple` and accepts `image/*`; `_msgAttachment` is a single object (L192); send posts one `media_url` (L434). Backend is already ready -- `textly-send` v1.1 accepts an array -> Textable `media[]` (L127-171) and inbound render already handles arrays (L93). Front-end only. |
| `9aab7550` | Brandon | **S** | Highlight new/unchecked messages more clearly | Partial: a gold unread-dot renders per conversation (messages.html L239/L826) but only when `last_direction='inbound' AND status='open'`, and there is no row-level treatment. Add a bold row / tinted background. |
| `21fc3a9d` | Brandon | **S** | Rescale the conversation-list text | Absent: messages.html uses fixed rem sizes (html 17.5px) with no user text-size control. Add a persisted font-scale. |
| `f1e6e311` | Brandon | **S** | Clear (x) button on the messaging search box | Absent: `convoSearch` (L373) is a plain input, cleared only programmatically (L735/L753). |
| `6e9ca4dc` | Brandon | **S** | Remove the "Added to every send" line under every chat | Valid: `refreshSignaturePreview` (messaging.js L230-235) renders a persistent `#msgSigPreview` (L211). Remove it or hide it behind a toggle. |
| `93b00023` | Roland | **M** | Inbound message should reopen the conversation, notify the owner once, and remind at 4:30 if unanswered | (a) **absent** -- textly-webhook inbound upsert (L447-452) never sets `status`, so a closed conversation stays closed and vanishes from the default Open filter (messages.html L800); reopen is manual only (L1284). (b) **partial** -- `assigned_inbound_notify` (L565-621) exists but dedupes on a 60-min window, not on the reopen. (c) **absent** -- no unread/16:30 sweep in `supabase/`. |
| `0448de62` | Sofia | **L** | Send the authorization form remotely for e-signature | Absent: signing is kiosk-only (customer-checkin.html on-device pad) and `raf_copy` is only a post-signing email; no tokenized signing page or token table. Needs a public signing page + delivery + signed-form storage. |

### Parts & Inventory (3)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `8d885331` | Lynn | **S** | Rework the parts-entry field order | Form exists (parts.js `pf_*`, L235); only the target layout is missing. **Blocked on detail, not effort.** |
| `b19975d4` | Lynn | **L** | Part returned to / warrantied by the supplier | Infra only: `PART_STATUSES` (config.js L200) has 'Returned'; no RMA#, return reason, credit tracking, or reverse flow. = GH#24 Shop Ops Phase 2. Canonical for declined dup `86bbaf50`. |
| `14f35f95` | Lynn | **XL** | In-house parts inventory (order 25, use 5, 20 to priced searchable stock) | Nothing in `js/*.js` handles stock. New subsystem: inventory table + receive/consume/adjust + searchable stock + parts-request links. Largest item on the board. |

### Notifications & Task-Tracking (3)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5fec3eca` | Sofia | **S** | Show the customer's address on the emailed authorization-form copy | Absent in the `raf_copy` branch of send-quote-email (L386-387); the address exists only in `lead_staff_notify` (L549/674/748). Add `customerAddress` to the payload + both templates. |
| `af14b3cb` | Lynn | **L** | Per-customer task tracker with a daily reminder until complete | `shop-tasks.js` is a non-customer checklist (no `ro_id`, no reminder column) and no per-RO tasks table exists. Needs table + add/complete UI + daily open-task reminder. Pairs with `5925fc01`. |
| `5925fc01` | Lynn | **L** | Notify the right team on every RO update; daily digest with to-dos scraped from status notes | Partial: the digest half is served by `send-manager-report`, and single-event notifies exist (`service_added_notify`, `urgent_update_notify`); notify-on-every-update and note-scraping are unbuilt. Anchor of this theme. |

### RO fields/status (3)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `972af03c` | Lynn | **S** | Add a Canceled status, keep the record for notes | No Canceled/Cancelled in render.js / config.js / ro-crud.js. Add the status + color/priority maps, exclude from on-lot and queue counts. Pairs with `5fbfa299`. |
| `5fbfa299` | Brandon | **M** | Archive quoted-but-never-progressed customers/units | Only the cashiered archive exists (`archiveROInSupabase`, ro-crud.js L646); no archived/abandoned flag or filter. |
| `0e172b2c` | Sofia | **M** | Merge duplicate customer accounts after verifying which is correct | Partial: `js/duplicates.js` merges/deletes at RO level but groups by base RO id, not customer identity. Needs name/phone account dedup + verify-then-merge. Data cleanup for the live case is `3f254929`. |

### Dashboard UI polish (4 -- new theme this pass)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `d432863a` | Brandon | **S** | Patriots logo as the browser tab icon | No favicon / rel=icon / apple-touch-icon in any page. Add both to every page head. |
| `29654af7` | Lynn | **S** | Button to jump back to the top / the search bar | No `window.scrollTo` or back-to-top control anywhere; only targeted `scrollIntoView`. Add a scroll-triggered FAB. |
| `d1844e46` | Lynn | **S** | Sort by promised date then oldest on lot, shop ops pinned | The board sorts on one blended `calculatePriority` score (render.js L29-36); the shop pin exists, a selectable strict sort does not. |
| `ac394468` | Roland | **S** | Add a Messages category to Make-A-Wish; every page should be represented | Category select (index.html L7455-7466) has no Messages option and the admin filter (L7548+) matches; the lamp exists only on index/checkin/analytics/solar/closed-ros/worklist-report, so messages, leads, time-off, review and guide cannot raise an ER at all. |

### Time clock (2)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `6bb61db2` | Riley | **S** | Check-in/out button on the compact view + a live "what am I clocked into" reminder | `compact-col-actions` (render.js L228-235) has no check-in button and no persistent clocked-into indicator exists. Add the compact action + a login-time active `time_log` banner. Pairs with `e06d747e`. |
| `e06d747e` | Riley | **S** | Tech check-in "float" for ease of access | Absent, and still underspecified -- scope the page and behavior with the submitter. |

### Work Orders (1)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `2f752d40` | Jason | **S** | Auto-populate the WO dollar amount from total hours x the $195/hr posted rate | `woFormDollar` (L360) and `woFormEstHours` (L365) are independent manual fields. Auto-calc using the task est_hours rollup when present, else WO-level (L145-148), with an editable override. |

### Auth & cost (1)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `ed1b873e` | Roland | **XL** | Replace Google-email auth (~$500/mo) with database auth | Still Google: `auth.js signInWithIdToken` + One Tap + `GOOGLE_CONFIG` (config.js L79), kiosk included. Swap touches every page sign-in plus the email-keyed RBAC. Roland wants to scope it together. |

---

## Full per-ER table (all 84)

Bucket key: D = done, N = needed, B = bug, X = duplicate, T = data.
`f73a1e5a` was outside this run's triage set and carries no verdict.

| ER | By | Created | Category | Bucket | LOE | Mapped code | Verdict (short) |
|---|---|---|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | General UI/UX | D | - | - | Test entry; keep declined |
| `79b2c805` | Sofia | 04-09 | New Feature Idea | D | - | closed-ros.html reactivateRO; photos.js L253 | Closed ROs reopen for edit + docs |
| `75124b03` | Ryan | 04-09 | RO Management | D | - | ro-crud.js ro_type; index.html | Warranty RO type v1.402 |
| `5925fc01` | Lynn | 04-09 | New Feature Idea | N | L | send-manager-report | Digest done; per-update notify + note scraping open |
| `b2aa4bea` | Lynn | 04-09 | New Feature Idea | D | - | key_dates_reminder_sources.sql | Scheduled drop date + reminder |
| `18611eee` | Lynn | 04-09 | Bug Report | D | - | work-list.js renderWorkList | Work-list members viewable/removable |
| `14f35f95` | Lynn | 04-09 | Parts & Ordering | N | XL | js/parts.js (no stock) | Parts inventory subsystem |
| `b19975d4` | Lynn | 04-09 | Parts & Ordering | N | L | config.js L200 | Supplier return/warranty flow |
| `17492066` | Lynn | 04-09 | Notifications / Email | D | - | scheduled_notifications.sql | Schedule Notification |
| `8d885331` | Lynn | 04-09 | Parts & Ordering | N | S | parts.js L235 | Field reorder; blocked on detail |
| `fd6c122d` | Riley | 04-09 | Work List | D | - | work-list.js removeFromWorkList | Delete + mobile + drag-drop |
| `aa508852` | Riley | 04-09 | Work List | X | - | - | Dup of fd6c122d |
| `98bd7498` | Brandon | 04-09 | RO Management | D | - | work-list.js silo tabs | Per-category work lists |
| `e06d747e` | Riley | 04-10 | Time Clock / Check-In | N | S | render.js L228 | Tech check-in float; underspecified |
| `19dc2016` | Roland | 04-11 | Time Clock / Check-In | D | - | - | Test entry; keep declined |
| `f73a1e5a` | Roland | 04-13 | General UI/UX | - | - | - | Carried over; not in this run's set (declined test entry in DB) |
| `1fe68261` | Rod | 04-13 | Work Orders | D | - | shop-tasks.js; shop_tasks.sql | Shop Tasks panel v1.467 |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | D | - | STATUS_PROGRESS_MAP | Ready to Work status v1.410 |
| `b87eb2fb` | Lynn | 04-14 | RO Management | D | - | keys_power_fields.sql | Keys + keep-plugged-in |
| `a5ff3e2d` | Lynn | 04-21 | RO Management | D | - | ro-crud.js L1068 | Warranty + Repair type v1.465 |
| `a7d1474e` | Lynn | 04-23 | RO Management | D | - | urgent_update_field.sql | URGENT banner + notify v1.466 |
| `dde90d2b` | Roland | 04-27 | RO Management | D | - | customer-checkin.html; send-quote-email | Photo optional, email forced |
| `13e1cd87` | Tipton | 04-30 | Time Clock / Check-In | D | - | checkin.html v1.34 | Return-to-Dashboard button |
| `e27311bf` | Ryan | 05-04 | Work Orders | D | - | work-orders.js | Template drag-reorder v1.464 |
| `d2561e11` | Brandon | 05-06 | New Feature Idea | D | - | key_dates_pickup_date.sql | Completion/pickup date |
| `6938c1af` | Ryan | 05-06 | General UI/UX | D | - | staff.name | Wimbles -> Wombles applied |
| `dac9fdda` | Brandon | 05-06 | General UI/UX | D | - | render.js L720 | Off Lot - Returning status |
| `86bbaf50` | Lynn | 05-13 | RO Management | X | - | - | Dup of b19975d4 |
| `a8e90a7d` | Brandon | 05-20 | General UI/UX | D | - | parts.js L153-160 | Parts + freight total v1.456 |
| `1aeb3f58` | Lynn | 05-21 | General UI/UX | D | - | render.js calculatePriority | Promised date highlighted + sorted |
| `af14b3cb` | Lynn | 05-21 | General UI/UX | N | L | shop-tasks.js (no ro_id) | Per-customer task tracker |
| `cbc70a86` | Lynn | 05-21 | General UI/UX | D | - | work_list_autoadd_rpc.sql; checkin L2319 | Check-in auto-adds to silo lists |
| `3f415c88` | Lynn | 05-26 | General UI/UX | D | - | process-scheduled-notifications | Repeat reminders |
| `cd663d8f` | Bobby | 05-28 | General UI/UX | D | - | ro-crud.js L1037; parts.js L307 | Currency 2 decimals v1.447 |
| `34fc03c2` | Brandon | 05-29 | New Feature Idea | D | - | keys_power_fields.sql | Keys selector + card chip |
| `0b2e128c` | Lynn | 06-02 | General UI/UX | D | - | time-tracking.js | Clock-in notes -> RO updates |
| `468aa376` | Lynn | 06-02 | General UI/UX | D | - | key dates | Scheduled drop date in entry dash |
| `2b814250` | Roland | 06-07 | Calendar | D | - | sync-ro-calendar | RO dates -> business calendar |
| `ed1b873e` | Roland | 06-07 | General UI/UX | N | XL | auth.js; config.js L79 | Replace Google auth (~$500/mo) |
| `cd084233` | Roland | 06-07 | Work List | D | - | send-manager-report; send-admin-pnl-report | Admin manager-worklist report |
| `a22739b9` | Sofia | 06-08 | General UI/UX | D | - | customer-checkin.html | Check-in email + RAF forced |
| `67daba15` | Brandon | 06-09 | General UI/UX | X | - | - | Dup of d2561e11 |
| `fa1c1d6e` | Sofia | 06-09 | General UI/UX | X | - | - | Dup of a22739b9 |
| `91a03260` | Roland | 06-09 | Work List | D | - | prune_cashiered_worklists.sql | Auto-prune cashed-out ROs |
| `5057c7fc` | Roland | 06-09 | Work List | X | - | - | Dup of 91a03260 |
| `cdd77a8b` | Brandon | 06-10 | General UI/UX | D | - | render.js L706-720 | On-lot vs total customers split |
| `0448de62` | Sofia | 06-11 | General UI/UX | N | L | customer-checkin.html (kiosk only) | Remote e-signature |
| `2c3d5633` | Ryan | 06-12 | General UI/UX | D | - | photos.js uploadPhoto | Phone photo upload fixed |
| `74a33621` | Brandon | 06-16 | General UI/UX | D | - | photos.js L344/L404 | Arrow-key lightbox nav |
| `298e0e67` | Sofia | 06-16 | General UI/UX | D | - | customer-checkin.html | Scheduled RO reused, no dup |
| `9b823d25` | Roland | 06-16 | RO Management | D | - | service_added_notify_source.sql | Silo-add manager notify |
| `fc6b29f9` | Sofia | 06-17 | General UI/UX | X | - | - | Dup of 298e0e67 |
| `9b046808` | Brandon | 06-17 | General UI/UX | D | - | render.js L675/L720 | Scheduled excluded from on-lot |
| `671fa623` | Brandon | 06-18 | General UI/UX | D | - | parts.js L153/L160 | Freight line in ticket totals |
| `5c45bb8d` | Sofia | 06-19 | General UI/UX | D | - | customer-checkin.html | Flanagan dup + RAF email fixed |
| `b06a285f` | Lynn | 06-19 | General UI/UX | D | - | key-date chips v1.457/v1.465 | Key dates by customer name |
| `ee50d11c` | Lynn | 06-19 | General UI/UX | D | - | time-off.html | Time-off entries editable |
| `88395acc` | Lynn | 06-19 | General UI/UX | D | - | add_absence_window_to_time_off.sql | Partial-day time off |
| `80390f36` | Lynn | 06-23 | General UI/UX | D | - | render.js L29-36/L61 | Shop ops pinned + searchable |
| `7f2d0d70` | Lynn | 06-26 | General UI/UX | D | - | send-dropoff-report | Monday drop-off check email |
| `3f7fb4fc` | Brandon | 06-29 | General UI/UX | D | - | render.js L249/L654-656 | Shop ROs out of counts v1.468 |
| `cb7742a8` | Lynn | 06-29 | General UI/UX | D | - | sync-ro-calendar | Dates -> correct team calendar |
| `5fbfa299` | Brandon | 06-30 | General UI/UX | N | M | ro-crud.js L646 | Archive quoted-only units |
| `29654af7` | Lynn | 07-06 | General UI/UX | N | S | index.html (no scrollTo) | Back-to-top / jump-to-search |
| `39b75fab` | Sofia | 07-09 | General UI/UX | D | - | photos.js deleteLibraryItem/moveLibraryItem | Doc rename/move/delete v1.472 |
| `5fec3eca` | Sofia | 07-15 | General UI/UX | N | S | send-quote-email L386-387 | Address on RAF copy email |
| `1c7f1410` | Andrew | 07-16 | New Feature Idea | D | - | messaging.js L192-434; textly-send | MMS attach shipped (single) |
| `d1844e46` | Lynn | 07-16 | General UI/UX | N | S | render.js L29-36 | Strict promised-date sort |
| `0e172b2c` | Sofia | 07-16 | General UI/UX | N | M | duplicates.js getBaseROId | Customer-account merge |
| `3f254929` | Sofia | 07-16 | General UI/UX | T | - | repair_orders (data) | Delete misspelled Hubert RO -- SQL above |
| `972af03c` | Lynn | 07-17 | General UI/UX | N | S | render.js ALL_STATUSES | Add Canceled status |
| `d432863a` | Brandon | 07-21 | General UI/UX | N | S | all *.html heads | Patriots-logo favicon |
| `9031e101` | Riley | 07-21 | Work List | B | small | work-list.js L328; index.html L4047 | iPhone work-list tap does not navigate |
| `9aab7550` | Brandon | 07-21 | General UI/UX | N | S | messages.html L239/L826 | Stronger unread highlight |
| `21fc3a9d` | Brandon | 07-21 | General UI/UX | N | S | messages.html (fixed rem) | Rescale conversation list |
| `2f752d40` | Jason | 07-21 | Work Orders | N | S | work-orders.js L360/L365 | WO dollars from hours x $195 |
| `f1e6e311` | Brandon | 07-21 | General UI/UX | N | S | messages.html L373 | Clear (x) on message search |
| `6e9ca4dc` | Brandon | 07-21 | General UI/UX | N | S | messaging.js L211/L230-235 | Drop the signature preview |
| `6bb61db2` | Riley | 07-22 | Time Clock / Check-In | N | S | render.js L228-235 | Compact check-in/out + clocked-into banner |
| `d4d0eeba` | Brandon | 07-22 | General UI/UX | B | small | index.html L4047; qr.js L207/L210 | RO links from list/messages silently fail |
| `9d3806e7` | Brandon | 07-22 | General UI/UX | D | - | customer_note_entries_s154.sql | Customer notes in Messages (S154) |
| `ac394468` | Roland | 07-24 | General UI/UX | N | S | index.html L7455-7466; enhancement.js L41-46 | Messages category + lamp on every page |
| `d62a26e4` | Roland | 07-24 | New Feature Idea | N | S | messaging.js L192/L208/L434 | Multiple attachments per message |
| `93b00023` | Roland | 07-24 | New Feature Idea | N | M | textly-webhook L447-452/L565-621 | Reopen on inbound + owner notify + 16:30 reminder |

---

## Recommended action today

1. **Ship the two bugs as one fix** (`9031e101` + `d4d0eeba`). One root cause,
   one small change, deterministic expected result -- squarely in the
   auto-fix-eligible class (<= 3 files, no auth/money/delete/DB).
2. **Close the two stale `unreviewed` rows**: `1c7f1410` (MMS attach shipped
   S151b) and `9d3806e7` (customer notes shipped S154) -> `status='done'`.
   Roland flips these; the runner does not.
3. **Run the one Data SQL** for `3f254929` (Steve Huebert) after confirming the
   wrong RO id.
4. **Batch the Messages S-cluster into one release.** `d62a26e4`, `9aab7550`,
   `21fc3a9d`, `f1e6e311` and `6e9ca4dc` are five small, well-specified changes
   in the same two files, and `d62a26e4` needs no backend work at all because
   `textly-send` already takes a media array. Highest value per hour on the board.
5. **Then `93b00023` (M)** -- part (a) is effectively a defect: a new inbound
   message on a closed conversation leaves it hidden behind the default Open
   filter, so staff can miss a live customer. Worth doing right after the
   S-cluster, or pulled forward if Roland wants the visibility risk closed first.
6. **Unblock the two stalled S's** by asking the submitters: Lynn's parts field
   order (`8d885331`) and Riley's check-in float (`e06d747e`).
7. **Plan, do not rush, the XLs**: the auth swap (`ed1b873e`) and parts
   inventory (`14f35f95`) each deserve their own scoping session.
