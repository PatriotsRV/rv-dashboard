# PRVS Enhancement-Request Triage - 2026-07-31 (headless nightly run)

Incremental pass (`full_pass: false`) over `enhancement_requests`, snapshot
fetched 2026-07-31T09:48Z. **87 of 88** rows were re-classified against the
current codebase (index.html **v1.485**); 1 row was carried over untouched.
Every verdict below was re-verified by grepping/reading the live code this
run - nothing was assumed from the prior pass.

**No code was changed, no DB writes were made, no status was flipped.** Status
changes are Roland's call; the recommendations are collected at the bottom.

## Board snapshot

| Status (DB) | Count |
|---|---|
| done | 49 |
| declined | 9 |
| unreviewed | 30 |
| **Total** | **88** |

### Verdict buckets (the 87 classified this run)

| Bucket | Count |
|---|---|
| done | 52 |
| needed | 22 |
| duplicate | 9 |
| bug | 2 |
| data | 2 |

**Real open work: 24 items** - 22 Needed (2 XL / 4 L / 2 M / 14 S) + 2 Bugs.
The other 6 `unreviewed` rows are resolved and only awaiting a status flip.

Codebase is byte-identical to the 2026-07-30 run: the only commit since
(`cf43055`, Session 164) touched docs and a migration file, and index.html
stays at v1.485. So the finding is stability, not stagnation - **all 86 prior
verdicts re-confirmed, zero drift, zero new regressions.**

## Code checks run this session

Absence claims (all returned **zero** code hits, confirming the ER is still open):

- `inventory` / `on_hand` / `in_stock` / `stock_qty` across js + supabase - **none** (confirms `14f35f95`)
- `rma` / `return_reason` / `supplier_credit` - **none** (confirms `b19975d4`)
- `ro_tasks` / `task_tracker` / `customer_task` - **none** (confirms `af14b3cb`, `5925fc01`)
- `signing_token` / `remote_sign` / `signature_request` / `signing_link` - **none** (confirms `0448de62`)
- `favicon` / `rel=icon` / `apple-touch-icon` across all `*.html` - **none** (confirms `d432863a`)
- `backToTop` / `scroll-to-top` on the dashboard - **none**; the only `window.scrollTo` calls are closed-ros.html L765 and customer-checkin.html L1277 (confirms `29654af7`, `2b78696c`)
- `sortBy` / `sortSelect` / `sortMode` on the main board - **none** (only closed-ros.html + solar.html) (confirms `d1844e46`)
- `fontScale` / `font-scale` / `textSize` - **none** (confirms `21fc3a9d`)
- `LABOR_RATE` / `laborRate` / `195` inside js/work-orders.js - **none**; index.html `laborRate` is the per-claim **insurance** rate field, not the posted shop rate (confirms `2f752d40`)
- `cancel` (case-insensitive) in js/config.js - **none**; STATUS_PROGRESS_MAP L151-164 and ALL_STATUSES L741-745 both end at Delivered/Cashed Out (confirms `972af03c`)
- `customerAddress` in the `raf_copy` destructure (send-quote-email L387) - **absent**; present only in the `lead_staff_notify` branch L549 (confirms `5fec3eca`)

Presence checks (confirming Done verdicts) - spot-verified across all modules:
js/shop-tasks.js, js/work-list.js (L195/L223/L263/L304), js/photos.js
(L66/L253/L396), js/work-orders.js (L293/L461), js/render.js
(L324/L367/L395-398/L656/L718/L741), js/ro-crud.js (L409-413/L486-490/L1093/L1119),
js/messaging.js (L202/L207/L219), js/duplicates.js (L35-46), js/auth.js
(L69/L203/L483), customer-checkin.html (L2182/L2319/L2467/L2540), checkin.html
(L1455/L1516), closed-ros.html (L725/L813/L838), time-off.html (L368/L372/L752),
messages.html (L334/L430/L469/L530/L1089), 15 migrations and 8 edge functions
(send-dropoff-report, send-manager-report, send-unreplied-reminder,
sync-ro-calendar, process-scheduled-notifications, send-quote-email,
textly-webhook, send-admin-pnl-report). All present.

**Prompt-injection scan: clean.** No ER `description`, `submitted_by_name` or
`admin_notes` value contained instruction-like text aimed at this runner. (ER
`3f254929` asks to delete an account - that is a normal staff data request,
bucketed `data` with SQL emitted for Roland, not executed.)

---

## Open work by theme

### Notifications & Task-Tracking

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `af14b3cb` | Lynn | **L** | Per-customer task tracker + daily reminder; tasks persist until complete | Still absent. js/shop-tasks.js is a non-customer checklist with no `ro_id` and no reminder column. Needs a per-RO tasks table, add/complete UI, daily open-task reminder. |
| `5925fc01` | Lynn | **L** | Notify the right team on every RO update; daily list of in-progress jobs with to-dos scraped from notes | **Partial.** Digest half ships as send-manager-report; single-event notifies exist (`service_added_notify` work-orders.js L293, `urgent_update_notify`). Still open: notify-on-every-update + to-do scraping. Anchor of this theme. |

### Parts & Inventory

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `14f35f95` | Lynn | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | Still absent - parts are per-RO line items only. New subsystem: inventory table, receive/consume/adjust, searchable priced stock view, parts-request links. Largest item on the board. |
| `b19975d4` | Lynn | **L** | Part returned-to-supplier / warrantied-by-supplier | **Partial infra only:** PART_STATUSES includes `Returned` (config.js L200) and parts.js has a Return Deadline (L226). No RMA number, return reason, supplier credit or reverse flow. = GH#24 Shop Ops Phase 2. Canonical for `86bbaf50`. |
| `8d885331` | Lynn | **S** | Rework the parts-entry field order | Form fields all present (parts.js L181-226). Quick once Lynn supplies the target layout. **Blocked on detail, not effort.** |

### Board & Navigation UX

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `29654af7` | Lynn | **S** | Back-to-top / jump-to-search button after scrolling | Still absent on the dashboard; board has only targeted `scrollIntoView`. Canonical for `2b78696c` (Lynn re-raised 07-27). |
| `d1844e46` | Lynn | **S** | Selectable sort: promised date, then oldest on lot, shop ops pinned | Still absent - renderBoard uses the single blended `calculatePriority` (render.js L31-39) with the shop pin L35-37. Add the strict sort option, keep the pin. |
| `d432863a` | Brandon | **S** | Patriots-logo tab icon | No favicon in any page head. Add favicon + apple-touch-icon (logo.png needs icon-size resizing). |

### Messaging UX

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `9aab7550` | Brandon | **S** | Make unchecked new messages clearly visible | **Partial:** gold `.unread-dot` (messages.html L334/L1095) fires only when `last_direction='inbound' AND status='open'` (L1089); no row-level highlight. Add a stronger treatment and widen the condition so reopened threads flag. |
| `21fc3a9d` | Brandon | **S** | Rescale the conversation-list text | Fixed rem sizes, no user text-size control. Add a persisted font-scale control. |
| `f1e6e311` | Brandon | **S** | Clear (x) button on the messaging search | `convoSearch` (L469) is a plain input, reset only programmatically (L1001/L1020). Add a clear button that re-runs the filter. |
| `6e9ca4dc` | Brandon | **S** | Remove the "Added to every send" footer under each chat | **Valid, still visible on desktop.** refreshSignaturePreview sets `#msgSigPreview` to display:block (messaging.js L246-247); messages.html hides it only inside the `max-width:760px` media query (L430, query L417-435). Remove or toggle it in all viewports; the signature keeps sending. |

### Time Clock & Check-In

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `e06d747e` | solar | **S** | Tech check-in "float" for ease of access | Still absent. compact-col-actions (render.js L228-235) has edit/parts/WO/notify/message/schedule, no check-in. Underspecified - scope page + behavior with the submitter. Pairs with `6bb61db2`. |
| `6bb61db2` | Riley | **S** | Compact-view check-in/out button + a live "what am I clocked into" indicator at login | Still absent (no persistent active-time_log indicator anywhere). Add the compact action plus a login-time banner across views. Pairs with `e06d747e`. |

### RO Lifecycle & Status

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5fbfa299` | Brandon | **M** | Archive quoted-but-never-progressed customers/units | Still absent - the only archive path is the cashiered one (ro-crud.js L658/L692). Needs a soft-archive flag, board filter and un-archive path. The S161 lead-capture path keeps *future* quotes on the Messages board, but existing quote-only ROs still need this. Pairs with `972af03c`. |
| `972af03c` | Lynn | **S** | Add status CANCELED - keep the record for notes | No Canceled status in STATUS_PROGRESS_MAP or ALL_STATUSES. Add it plus color/priority maps, exclude from on-lot and queue counts. Pairs with `5fbfa299`. |

### RO fields & status

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `50175fce` | Lynn | **S** | Move the urgent-update **input** onto the tile instead of inside Edit RO | Confirmed: `urgent_update` is writable only from New/Edit RO (ro-crud.js L414/L467/L491); the card banner is read-only and render.js L324 literally titles it "change it via Edit RO". Add inline edit-in-place reusing editField so it still fires `urgent_update_notify`. Follow-on to `a7d1474e`. |

### Work Orders

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `2f752d40` | Jason | **S** | Auto-populate the WO dollar amount from total hours x $195/hr | Still absent - `woFormDollar` (L360) and `woFormEstHours` (L365) are independent manual inputs with no rate arithmetic. Auto-populate from the effective hours (task rollup else WO figure), respecting the `dollarLocked` gate (L324). |

### Customer & RO Data Integrity

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `0e172b2c` | Sofia | **M** | Merge duplicate customer accounts (Hubert / Huebert), with customer verification | **Partial:** js/duplicates.js merges at RO level but groups strictly on the `PRVS-XXXX-XXXX` base id (L36/L44-46), so two spellings of one customer are never detected. Needs name/phone account dedup + a verify-which-is-correct step. One-off cleanup is `3f254929`. |

### Check-in & Auth Form

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5fec3eca` | Sofia | **S** | Show the customer's address on the emailed authorization-form copy | Confirmed: the `raf_copy` destructure (send-quote-email L387) has no `customerAddress`; it exists only in `lead_staff_notify` (L549, HTML L674, text L748). Add it to the payload and both templates. |

### Messaging & e-sign

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `0448de62` | Sofia | **L** | Send an authorization form remotely for e-signature | Still absent - signing is kiosk-only; `raf_copy` (L386) only emails a copy after signing. Needs a tokenized public signing page, a delivery path and signed-form storage. Pairs with the Textly SMS channel. |

### Auth & Cost

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `ed1b873e` | Roland | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | Still Google-based: auth.js imports GOOGLE_CONFIG (L69), inits gapi (L203-204), One Tap (L483) + signInWithIdToken (L593); the kiosk shares the path. Security-critical swap touching every page sign-in plus the email-keyed RBAC. Roland wants to scope this together. |

---

## Bugs (front of the line)

| ER | By | Severity | Verdict |
|---|---|---|---|
| `d4d0eeba` | Brandon | **small** (navigation, no data risk) | **CONFIRMED on v1.485, two paths.** Work list: `scrollToROInBoard` (index.html L4139) only matches already-rendered `[data-ro-index]` cards; otherwise it toasts (L4154) and that toast's *Clear Filters* action re-renders but never then scrolls. Messages: `js/qr.js handleDeepLink` (L198) returns silently when the RO is not in `currentFilteredData` (L207) or has no card (L210). Fix both paths: clear filters -> re-render -> scroll -> highlight. **Canonical for `9031e101` and `1bc33dd6` - one fix closes all three.** |
| `d94a418a` | Brandon | **data-display** (render only, no data loss) | **CONFIRMED on v1.485.** js/render.js L553 renders the card RO Status box via `split('\n').map(escapeHtml).reverse()`, reversing *every line*, so a multi-line note displays bottom-line-first - exactly the reported "latter portion near the top". L563 does the same to Customer Comm. Entry boundaries are lost upstream (ro-crud.js joins entries with `\n`). The S159 history modal already splits on the entry regex correctly; reuse that split for both boxes. |

---

## Data corrections (SQL for Roland - nothing was executed)

| ER | By | Verdict |
|---|---|---|
| `6938c1af` | Ryan | Already applied in the live DB (tech names read `staff.name`; submitter "Rod Wombles" now spells correctly in this very table). **Residual, not a runtime bug:** the seed file `supabase/migrations/staff_table.sql` L85-87 still inserts Rod/Zak/Travis as "Wimbles", so a re-seed would reintroduce it. Re-apply if ever needed: `UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |
| `3f254929` | Sofia | Keep Steve **Huebert**, remove the misspelled Hubert RO. Eyeball first: `SELECT id,ro_id,customer_name,status FROM repair_orders WHERE customer_name ILIKE '%ubert%';` then, once the wrong id is confirmed: `UPDATE repair_orders SET deleted_at=now() WHERE id='<wrong-id>';` The merge *feature* is tracked as `0e172b2c`. |

---

## Duplicates - fold into the canonical ER

| ER | By | Date | Fold into | Note |
|---|---|---|---|---|
| `aa508852` | Riley | 04-09 | `fd6c122d` (Done) | Identical text + timestamp. Already declined. |
| `86bbaf50` | Lynn | 05-13 | `b19975d4` (Needed/L) | Gordon window returned to Vogt = supplier return tracking. Already declined. |
| `67daba15` | Brandon | 06-09 | `d2561e11` (Done) | RO completion / pick-up date. Already declined. |
| `fa1c1d6e` | Sofia | 06-09 | `a22739b9` (Done) | Sheldon Gilbert check-in email. Already declined. |
| `5057c7fc` | Roland | 06-09 | `91a03260` (Done) | Prune cashed-out ROs from work lists. Already declined. |
| `fc6b29f9` | Sofia | 06-17 | `298e0e67` (Done) | Retest of the duplicate-RO guard. Already declined. |
| `9031e101` | Riley | 07-21 | `d4d0eeba` (**Bug**) | iPhone work-list tap does not reach the unit - same `scrollToROInBoard` root cause. **Needs declining.** |
| `1bc33dd6` | Brandon | 07-27 | `d4d0eeba` (**Bug**) | Same scroll-to-customer defect. **Needs declining.** |
| `2b78696c` | Lynn | 07-27 | `29654af7` (Needed/S) | Same back-to-top ask, re-raised. **Needs declining.** |

---

## Delivered / Done - all 52 re-verified on v1.485

| ER | By | Date | Category | Mapped code | Verdict |
|---|---|---|---|---|---|
| `79b2c805` | Sofia | 04-09 | New Feature | closed-ros.html L838; js/photos.js L253 | `reactivateRO` restores a cashed-out RO from `cashiered_child_archives`; `uploadDocument` adds docs to any RO. |
| `75124b03` | Ryan | 04-09 | RO Mgmt | js/ro-crud.js L1119; js/render.js L121-122/L196/L373 | Warranty is a first-class RO type on creation, with board filter, chip and badge. v1.402. |
| `b2aa4bea` | Lynn | 04-09 | New Feature | js/ro-crud.js L409/L486/L508; js/render.js L395 | `planned_dropoff_date` + Drop chip + day-before reminder. Same field as `468aa376`. |
| `18611eee` | Lynn | 04-09 | Bug Report | js/work-list.js L195/L223/L263/L304 | Was real; work-list members are now viewable, reorderable, removable. |
| `17492066` | Lynn | 04-09 | Notif/Email | scheduled_notifications.sql; index.html L2921 | Schedule Notification bell: future date/time, recipients, daily/weekly repeat-until. |
| `fd6c122d` | Riley | 04-09 | Work List | js/work-list.js L195/L304/L356/L383 | Delete + mouse/touch drag-reorder, mobile sizing fixed. v1.460. Canonical for `aa508852`. |
| `98bd7498` | Brandon | 04-09 | RO Mgmt | js/work-list.js L83/L223/L263 | Per-silo Work List tabs = a separate list per job category. |
| `1fe68261` | Rod | 04-13 | Work Orders | js/shop-tasks.js L21/L41/L153/L180; shop_tasks.sql | Shop tasks not tied to a customer unit. v1.467. Matches the poly-barrel / roof-bay examples. |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | js/config.js L158; js/render.js L741-745 | "Ready to Work" is a live status. v1.410. |
| `b87eb2fb` | Lynn | 04-14 | RO Mgmt | keys_power_fields.sql; js/ro-crud.js L411-413/L488-490 | `key_status` + `keypad_code` + `keep_plugged_in`, keys chip on card. v1.458. |
| `a5ff3e2d` | Lynn | 04-21 | RO Mgmt | js/ro-crud.js L1119-1120; js/render.js L122/L196/L373 | "Warranty + Repair" type end to end, stays billable. v1.465. |
| `a7d1474e` | Lynn | 04-23 | RO Mgmt | urgent_update_field.sql; js/render.js L324/L214 | Always-visible red URGENT banner + manager/admin notify. v1.466. Input-on-tile follow-up = `50175fce`. |
| `dde90d2b` | Roland | 04-27 | RO Mgmt | customer-checkin.html L2467/L2540 | Photo optional; `raf_copy` + `lead_staff_notify` forced on every submission. |
| `13e1cd87` | Tipton | 04-30 | Time Clock | checkin.html L1516/L441 | "Return to Dashboard" after clock-out. v1.447 / checkin v1.34 (now v1.38). |
| `e27311bf` | Ryan | 05-04 | Work Orders | js/work-orders.js L461/L385/L583 | WO task rows drag-to-reorder; DOM order drives `sort_order`. v1.464. |
| `d2561e11` | Brandon | 05-06 | New Feature | key_dates_pickup_date.sql; js/render.js L397/L367 | `pickup_date` + green Pickup chip + key-dates panel. v1.457. Canonical for `67daba15`. |
| `dac9fdda` | Brandon | 05-06 | General UI | js/config.js L154; js/render.js L718-721 | "Off Lot - Returning" separates temporarily-taken units; excluded from on-lot count. v1.460. |
| `a8e90a7d` | Brandon | 05-20 | General UI | js/parts.js L153/L155/L160-161 | Ticket Totals bar sums parts + freight + total cost. v1.456. Same build as `671fa623`. |
| `1aeb3f58` | Lynn | 05-21 | General UI | js/render.js L396/L33 | Large bold promised-date chip (red when overdue); promise date + days-on-lot feed `calculatePriority`. Strict sort = `d1844e46`. |
| `cbc70a86` | Lynn | 05-21 | General UI | customer-checkin.html L2319; work_list_autoadd_rpc.sql | `add_ro_to_silo_work_lists()` inserts `manager_work_lists` rows per involved silo lead. S128. |
| `3f415c88` | Lynn | 05-26 | General UI | index.html L2921/L2989 | Repeat none/daily/weekly + repeat-until, one cancellable row per occurrence. v1.463. |
| `cd663d8f` | Bobby | 05-28 | General UI | js/ro-crud.js L1093; js/parts.js L307/L310 | Currency inputs keep 2 decimals on edit-populate. v1.447. No data was ever lost. |
| `34fc03c2` | Brandon | 05-29 | New Feature | keys_power_fields.sql; js/ro-crud.js L411-413/L488-490 | Keys selector on New + Edit RO records whether keys are left. v1.458. Covers `b87eb2fb`. |
| `0b2e128c` | Lynn | 06-02 | General UI | checkin.html L1455/L5 | Clock-out notes copied into the RO timeline as a "TECH NOTES" `ro_status` row. v1.460. |
| `468aa376` | Lynn | 06-02 | General UI | js/ro-crud.js L409/L486; js/render.js L395 | Scheduled drop date on New + Edit RO. Same field as `b2aa4bea`. |
| `2b814250` | Roland | 06-07 | Calendar | js/render.js L395-398/L367; sync-ro-calendar | Key-date chips + hover panel, server-side calendar sync (idempotent via `cal_event_ids`), advance reminders. |
| `cd084233` | Roland | 06-07 | Work List | send-manager-report; send-admin-pnl-report; worklist-report.html | Admin-level daily manager-worklist report with parts/labor/cost rollups. Manager *scoring* remains a future extension. |
| `a22739b9` | Sofia | 06-08 | General UI | customer-checkin.html L2467/L2540 | Notify-gate regression fixed; both sends forced. Canonical for `fa1c1d6e`. |
| `91a03260` | Roland | 06-09 | Work List | prune_cashiered_worklists.sql L23-25 | Removes the RO the day *after* cash-out (Chicago-local date gate). Canonical for `5057c7fc`. |
| `cdd77a8b` | Brandon | 06-10 | General UI | js/render.js L656-658/L718-721 | Stats split: shop ROs out of the queue figure; on-lot excludes Not On Lot / Scheduled / Off Lot - Returning. v1.453-4. |
| `2c3d5633` | Ryan | 06-12 | General UI | js/photos.js L66 | iOS Safari dropped-change-event fixed by appending the input to the DOM before clicking. v1.453. |
| `74a33621` | Brandon | 06-16 | General UI | js/photos.js L396-397/L380/L382 | Lightbox arrow-key navigation + Escape-to-close + prev/next buttons. v1.454. |
| `298e0e67` | Sofia | 06-16 | General UI | customer-checkin.html L2047/L2136/L2194 | Duplicate-RO guard reuses the existing scheduled RO. Canonical for `fc6b29f9`. |
| `9b823d25` | Roland | 06-16 | RO Mgmt | js/work-orders.js L293; service_added_notify_source.sql | Adding a service to an RO emails the owning silo manager. v1.460. |
| `9b046808` | Brandon | 06-17 | General UI | js/config.js L157; js/render.js L718-721 | "Scheduled" is its own status and is excluded from on-lot / days-on-lot. v1.454. |
| `671fa623` | Brandon | 06-18 | General UI | js/parts.js L153/L155/L160-161 | Freight rolled into Total Cost. v1.456. Same build as `a8e90a7d`. |
| `5c45bb8d` | Sofia | 06-19 | General UI | customer-checkin.html L2047/L2136/L2467 | Both halves of the David Flanagan report (duplicate RO + missing email) resolved. |
| `b06a285f` | Lynn | 06-19 | General UI | js/render.js L367/L395-398 | Calendar icon by the name opens a full key-dates panel on hover/tap. v1.465. |
| `ee50d11c` | Lynn | 06-19 | General UI | time-off.html L752 | Per-row Edit -> `editRequest()` reloads the entry; day, person and type are all editable. |
| `88395acc` | Lynn | 06-19 | General UI | add_absence_window_to_time_off.sql; time-off.html L368/L372 | Exact absence window (clock-out to return) rather than an hour total. Time Off v1.5. |
| `80390f36` | Lynn | 06-23 | General UI | js/render.js L35-37 | Shop Operations pinned above the priority sort + searchable via a token. v1.462. |
| `7f2d0d70` | Lynn | 06-26 | General UI | send-dropoff-report L138-141/L176 | Monday drop-off email: this week's scheduled drops + last week's arrivals, cross-checked against `manager_work_lists`. |
| `3f7fb4fc` | Brandon | 06-29 | General UI | js/render.js L656-658/L718-721/L249 | Shop ROs out of queue + on-lot counts; status dropdown replaced by a static label. v1.468. |
| `cb7742a8` | Lynn | 06-29 | General UI | sync-ro-calendar; customer-checkin.html L2182/L10 | Server-side calendar sync posts to the correct team calendar for every RO incl. front-desk check-ins. v1.469. |
| `39b75fab` | Sofia | 07-09 | General UI | js/photos.js L198/L376 | Documents library supports rename / move-to-another-RO / delete, office-staff gated. v1.472. |
| `1c7f1410` | Andrew | 07-16 | New Feature | js/messaging.js L216/L219/L202/L207 | Composer attaches photos/PDFs, `multiple`, up to 5, removable chips. S151b + S158. **Needs status=done.** |
| `9d3806e7` | Brandon | 07-22 | General UI | messages.html L528/L530/L1326; customer_note_entries_s154.sql | Customer-side Notes section + Add-Notes modal, append-only rows keyed by phone. S154. **Needs status=done.** |
| `ac394468` | Roland | 07-24 | General UI | js/enhancement.js pageDefaults | Make-a-Wish categories for every page. v1.482. **Residual:** `erCategory` ships in only 6 live HTML files (analytics, checkin, closed-ros, index, solar, worklist-report), so messages / leads / time-off / guide cannot *raise* an ER - worth its own small ER. |
| `d62a26e4` | Roland | 07-24 | New Feature | js/messaging.js L219/L202/L207/L258-262 | Multi-attach up to 5; send path chunks one media per MMS (carrier payload cap). v1.482. |
| `93b00023` | Roland | 07-24 | New Feature | textly-webhook L474/L503-505; send-unreplied-reminder; unreplied_eod_reminder_s158.sql | All three parts: reopen on inbound, owner notified only on the needs-reply flip, weekday 21:30 UTC unread sweep. v1.482. |
| `33750cb3` | Roland | 04-09 | General UI | - | Test entry ("Testing out the Make-A-Wish function"). Already declined; keep declined. |
| `19dc2016` | Roland | 04-11 | Time Clock | - | Test entry ("Testing ER form / dictation"). Already declined; keep declined. |

### Carried over untouched (not in `needs_triage_ids`)

| ER | By | Date | Status | Note |
|---|---|---|---|---|
| `f73a1e5a` | Roland | 04-13 | declined | "Testing dictation 123" - test entry, never triaged (`triage_bucket` is still null). Not re-classified this run; already declined in the DB. |

---

## Recommended action today

1. **Ship the two bugs together - they are the whole bug class and both are
   cheap.** `d4d0eeba` (scroll-to-RO, one fix closes `9031e101` + `1bc33dd6`)
   and `d94a418a` (`.reverse()` on the note boxes, render.js L553/L563). Both
   are in the auto-fixable class (small / data-display, no data risk), so they
   qualify for a single `fix/er-bugs-v1.486` branch off `pre-prod`.
2. **Then take a bite out of the 14 S-size backlog.** Four are one small
   messaging bundle - `f1e6e311` (search clear button), `6e9ca4dc` (drop the
   "Added to every send" footer on desktop), `9aab7550` (stronger unread
   treatment), `21fc3a9d` (font scale). Another natural pair is `972af03c`
   (Canceled status) with `5fbfa299` (archive quote-only ROs, M).
3. **Flip 6 statuses** (see below) - that clears a fifth of the `unreviewed`
   queue with zero code work.
4. **Unblock the two stalled S's by asking the submitters:** Lynn's parts field
   order (`8d885331`) and solar's check-in float (`e06d747e`). Small *once
   specified*; both have been carried for months on missing detail alone.
5. **Do not rush the XLs.** `ed1b873e` (auth swap, ~$500/mo at stake) and
   `14f35f95` (parts inventory) each need their own scoping session.
6. Run the two Data-bucket SQL statements when convenient (`6938c1af` is
   already applied - the seed-file residual only matters on a re-seed;
   `3f254929` needs eyeballing before the update).

### Pending Roland actions

**Status flips (6 rows) - nothing was written by this runner:**

| ER | Current | Recommend | Why |
|---|---|---|---|
| `1c7f1410` | unreviewed | `done` | Message media attach shipped S151b/S158. |
| `9d3806e7` | unreviewed | `done` | Customer notes section shipped S154. |
| `9031e101` | unreviewed | `declined` | Duplicate of `d4d0eeba`. |
| `1bc33dd6` | unreviewed | `declined` | Duplicate of `d4d0eeba`. |
| `2b78696c` | unreviewed | `declined` | Duplicate of `29654af7`. |
| `3f254929` | unreviewed | `done` *after* the SQL | Data correction; flip once the Hubert row is removed. |

**SQL:** the two statements in the Data-corrections section above.
**No release, no version bump, no code change this run.**
