# PRVS Enhancement-Request Triage - 2026-08-07 (headless nightly runner)

Incremental pass over `enhancement_requests` against the current modular
codebase (index.html **v1.491**, messages.html **v1.23**, git HEAD
**362137d**). Input snapshot `er_input.json` fetched 2026-08-07 08:16 UTC:
96 rows, 95 flagged for triage. Verdicts verified by grepping/reading the
live code, not assumed. No code was changed, no SQL was run, no ER status
was flipped - Roland owns status changes.

**HEAD is unchanged since the 2026-08-06 pass** (both runs sit on 362137d),
so the 94 previously-stored verdicts were re-checked against the same tree
rather than re-derived from scratch. What was actually re-run this pass is
listed under "Code checks run this session". One ER is genuinely new
(`5ca724ff`, filed 2026-08-06 16:06 UTC, after yesterday's run) and got a
full fresh mapping.

## Board snapshot

| Bucket | Count |
|---|---|
| done | 52 |
| needed | 26 |
| duplicate | 10 |
| bug | 5 |
| data | 2 |
| **Triaged this pass** | **95** |
| not in `needs_triage_ids` (carried) | 1 |
| **Total rows** | **96** |

By DB status: 52 `done`, 12 `declined`, 32 `unreviewed`.

**31 real open work items**: 5 Bugs + 26 Needed. LOE mix of the Needed
bucket: **2 XL - 4 L - 2 M - 18 S**. The board is now dominated by
small, well-specified items; the residue of L/XL work is unchanged from
the last several passes.

Headline finding this pass: **one new Bug** (`5ca724ff`, desktop Work List
reorder) and **no new enhancement requests**. The two large risks carried
from 2026-08-04 are still open and still the most consequential items on
the board: `09084bc5` (permanent data loss at cash-out) and `225a2535`
(insurance scan data bleeding between customers).

## Code checks run this session

- `js/work-list.js` drag paths read in full (L290-430) for the new ER - both
  mouse and touch handlers present; `dragstart` sets no `dataTransfer`
- `js/work-orders.js` `_initWOTaskDrag` (L461-513) read as the comparison
  implementation - it at least sets `effectAllowed` (L482)
- `index.html` `scrollToROInBoard` read in full (L4201-4217) - confirms Bug
  `d4d0eeba` (rendered-cards-only scan, toast fallback never scrolls)
- `js/render.js` L553 / L563 - confirms Bug `d94a418a` (`.reverse()` on every
  note line in both the RO Status and Customer Comm boxes)
- `js/ro-crud.js` `archiveROInSupabase` read in full (L658-701) - confirms Bug
  `09084bc5`: 24-column allowlist, no `photo_library` / key dates /
  `urgent_update`, then a hard `delete` at L697
- `window._lastEstimateScan` traced across `js/insurance.js` (L43, L118),
  `js/ro-crud.js` (L1192) and `index.html` (L6617-6620) - confirms Bug
  `225a2535`, cache keyed on `mode` only
- Absence sweeps returning **zero hits** (confirm the Needed bucket):
  `inventory|on_hand|in_stock|stock_qty` (14f35f95),
  `rma|return_reason|supplier_credit` (b19975d4),
  `ro_tasks|task_tracker|customer_task` (5925fc01, af14b3cb),
  `signing_token|remote_sign|signature_request|signing_link` (0448de62),
  `backToTop|back-to-top|scrollToTop` (29654af7, 2b78696c),
  `favicon|apple-touch-icon` (d432863a),
  `sortBy|sortSelect|sortMode` (d1844e46),
  `mileage|odometer` (ac8265c8),
  `font-scale|fontScale|textSize` (21fc3a9d),
  `LABOR_RATE|labor_rate` in `js/work-orders.js` (2f752d40),
  `archived|abandoned` in render/config (5fbfa299),
  `cancelled` / `totaled` as an RO status (972af03c, 3e746c18, eb00a68d)
- `js/config.js` `STATUS_PROGRESS_MAP` (L151-164) read in full - 12 statuses,
  ends at Delivered/Cashed Out
- Done-claim anchors re-confirmed by grep: `js/render.js` (L121-122, L195-196,
  L367, L372-373, L656-658, L718-721), `js/photos.js` (L66, L198, L253, L376,
  L396-400), `js/messaging.js` (L290-295, L347-350, L428-435, L578-579),
  `js/duplicates.js` (L35-44, L116), `js/shop-tasks.js`, `js/enhancement.js`
  `pageDefaults` (L43), `js/work-orders.js` (L293, L360, L365, L649-650),
  `checkin.html` (L1455, L1516), `customer-checkin.html` (L2059-2060, L2165,
  L2194, L2240, L2381, L2530, L2603), `time-off.html` (L368, L372, L752)
- All 21 `supabase/functions/*` and every migration cited in a Done verdict
  exist on disk

---

## Bugs - front of the line (5)

| ER | By | Date | Category | Bucket | Mapped code | Verdict | Severity |
|---|---|---|---|---|---|---|---|
| `09084bc5` | Lynn Titel-Shepard | 08-04 | General UI/UX | **bug** | `js/ro-crud.js` L666-693, L697; `cashiered_child_archives.sql`; `closed-ros.html` reactivateRO | Cash-out writes `cashiered` via a 24-column allowlist that omits `photo_library`, `planned_dropoff_date`, `pickup_date`, `key_status`, `keypad_code`, `keep_plugged_in`, `urgent_update`, then hard-deletes the `repair_orders` row. The child mirror covers 7 child tables only - no photo/doc mirror - so reactivation cannot restore them. | **LARGE - permanent data loss** |
| `225a2535` | Brandon Dillon | 08-05 | Bug Report | **bug** | `js/insurance.js` L43-44, L118; `js/ro-crud.js` L1192; `index.html` L6617-6620 | `window._lastEstimateScan` is keyed on `mode` only and `closeEditModal` deliberately keeps it. Scan RO A, close without saving, open RO B: A's values auto-fill B and saving B merges A's extracted data. Fix: key the cache to the RO id. | **data-integrity** |
| `d94a418a` | Brandon Dillon | 07-27 | General UI/UX | **bug** | `js/render.js` L553, L563 | The card RO Status box renders `split('\n').map(escapeHtml).reverse()`, reversing *every* line, so a multi-line note shows bottom-line-first - exactly the reported "latter portion near the top". L563 does the same to Customer Comm. Entry boundaries are lost upstream in `js/ro-crud.js`. Reuse the S159 history-modal entry regex. | data-display |
| `d4d0eeba` | Brandon Dillon | 07-22 | General UI/UX | **bug** | `index.html` L4201-4217; `js/qr.js` handleDeepLink L198 | `scrollToROInBoard` only scans already-rendered `[data-ro-index]` cards; otherwise it toasts, and the toast's Clear Filters action re-renders but never then scrolls. The Messages deep link returns silently when the RO is not in `currentFilteredData`. Canonical for `9031e101` and `1bc33dd6` - one fix closes all three. | small (navigation) |
| `5ca724ff` | Lynn Titel-Shepard | 08-06 | General UI/UX | **bug (NEW)** | `js/work-list.js` L304, L389-401, L404-429; cf. `js/work-orders.js` L482 | **New this pass.** Desktop reorder *is* wired (`row.draggable`, dragstart/dragover/drop) but `dragstart` never populates `e.dataTransfer` - no `setData`, not even the `effectAllowed` the WO task drag sets - and there is no `dragenter` preventDefault. Safari and Firefox desktop refuse a drag with an empty data store, while the bespoke touch path works. Needs a desktop browser repro (which browser, does the row lift at all) before the fix is locked in. | small (UX only; saved priority intact) |

---

## Needed - open enhancements by theme (26)

### Notifications & Task-Tracking (3)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `5925fc01` | Lynn Titel-Shepard | 04-09 | New Feature Idea | **L** | `supabase/functions/send-manager-report`; `js/work-orders.js` L293; `js/ro-crud.js` L370 | PARTIAL. The daily in-progress digest half ships, and single-event notifies exist (`service_added_notify`, `urgent_update_notify`). Still absent: notify-on-every-RO-update and to-do scraping from status notes. Anchor of this theme. |
| `af14b3cb` | Lynn Titel-Shepard | 05-21 | General UI/UX | **L** | `js/shop-tasks.js` (no ro_id link) | No per-RO task table, no reminder column, zero hits for `ro_tasks`/`customer_task`/`task_tracker`. Needs a per-RO tasks table, an add/complete UI and a daily open-task reminder. Pairs with `5925fc01`. |
| `5fec3eca` | Sofia Pedroza | 07-15 | General UI/UX | **S** | `supabase/functions/send-quote-email` raf_copy branch | The `raf_copy` payload carries no `customerAddress`; the address appears only in the `lead_staff_notify` branch. Add it to the payload and both templates. |

### Parts & Inventory (3)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `14f35f95` | Lynn Titel-Shepard | 04-09 | Parts & Ordering | **XL** | `js/parts.js` (per-RO line items only) | Zero code hits for inventory/on_hand/in_stock/stock_qty. New subsystem: inventory table, receive/consume/adjust flows, searchable priced stock view, links from the parts-request path. Largest item on the board. |
| `b19975d4` | Lynn Titel-Shepard | 04-09 | Parts & Ordering | **L** | `js/config.js` PART_STATUSES; `js/parts.js` pf_returnDeadline L226 | Partial infra only - a `Returned` status and a Return Deadline input exist, but no RMA number, return reason, supplier credit or reverse-direction flow. Equals GH#24 Shop Ops Phase 2. Canonical for `86bbaf50`. |
| `8d885331` | Lynn Titel-Shepard | 04-09 | Parts & Ordering | **S** | `js/parts.js` L181, L190, L208, L226 | The form is unchanged; the ask is only to reorder those fields. Quick once Lynn supplies the target layout. **Blocked on detail, not on effort.** |

### RO Fields & Status (5) - bundle these into one build

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `5fbfa299` | Brandon Dillon | 06-30 | General UI/UX | **M** | `js/ro-crud.js` L658; `js/render.js`, `js/config.js` | The only archive path is cash-out. No soft-archive flag, status or board filter for quoted-but-never-progressed units. Needs a flag, a board filter and an un-archive path. |
| `972af03c` | Lynn Titel-Shepard | 07-17 | General UI/UX | **S** | `js/config.js` L151-164; `js/render.js` statusColorMap / ALL_STATUSES | No Canceled RO status - the map ends at Delivered/Cashed Out, and the only `cancelled` hits are notification/review-request CHECKs. Add the status plus color/priority maps and exclude it from the on-lot and queue counts. Canonical for `eb00a68d`. |
| `3e746c18` | Lynn Titel-Shepard | 08-03 | General UI/UX | **S** | `js/config.js` L151-164; `js/render.js` L656-658, L718-721 | No `totaled`/`totalled` anywhere. Distinct value from `972af03c`'s Canceled, kept as its own item, but ONE build should add both terminal statuses and exclude both from the counts. |
| `ac8265c8` | Lynn Titel-Shepard | 08-05 | General UI/UX | **S** | `js/ro-crud.js` L409-415, L486-491 | Zero hits for mileage/odometer repo-wide - the RO records rv/vin but never mileage. One `repair_orders.mileage` column plus the New/Edit RO inputs and an optional card readout. |
| `50175fce` | Lynn Titel-Shepard | 07-27 | General UI/UX | **S** | `js/ro-crud.js` L414, L491, L370; `js/render.js` L324 | `urgent_update` is writable only from the New/Edit RO forms and the card banner is read-only (its title even says "change it via Edit RO"). Add an inline edit-in-place control at the tile bottom that still fires `urgent_update_notify`. Follow-on to `a7d1474e`. |

### Messages UX (5) - one messages.html polish pass

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `9aab7550` | Brandon Dillon | 07-21 | General UI/UX | **S** | `messages.html` `.unread-dot`; `markConversationUnread` | S166's manual Mark-unread button helps, but the visual treatment is still one 8px gold dot, shown only when `last_direction` is inbound AND status is open. Add a bold/tinted unread row treatment. |
| `21fc3a9d` | Brandon Dillon | 07-21 | General UI/UX | **S** | `messages.html` (fixed rem sizes) | No user text-size control anywhere; zero hits for font-scale/fontScale/textSize. Add a persisted font-scale control for the conversation list. |
| `f1e6e311` | Brandon Dillon | 07-21 | General UI/UX | **S** | `messages.html` `convoSearch` | Plain text input, no clear affordance, reset only programmatically. Add a clear (x) button that empties the search and re-runs the filter. |
| `6e9ca4dc` | Brandon Dillon | 07-21 | General UI/UX | **S** | `js/messaging.js` L350, L370; `messages.html` mobile media query | "Added to every send:" is hidden only inside the `max-width:760px` query, so desktop messages.html and the RO-card composer still show it. Remove it or collapse it behind a toggle in all viewports; the signature itself keeps sending. |
| `ca0f7ed9` | Brandon Dillon | 08-03 | Messages / Texting | **S** | `messages.html` renderLeadInfo / selectConversation | `renderLeadInfo` reads only the conversation's own columns, so an existing dashboard customer shows "No RV or service type captured yet" even though `selectConversation` already computed phone-matched live ROs as `linked`. Pass `linked` through and fall back to RO `rv`. |

### Board & Navigation UX (3)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `29654af7` | Lynn Titel-Shepard | 07-06 | General UI/UX | **S** | `index.html`, `js/`, `css/` (nothing) | No back-to-top control exists; the board has only targeted `scrollIntoView` calls. Add a scroll-triggered back-to-top / jump-to-search control, present in the sidebar layout too. Canonical for `2b78696c`. |
| `d1844e46` | Lynn Titel-Shepard | 07-16 | General UI/UX | **S** | `js/render.js` L30-39, L35-37 | Sorting is the single blended `calculatePriority` score with the shop-ops pin; no selectable sort mode exists. Add a strict promised-then-oldest-on-lot option while keeping the shop pin. Related to `1aeb3f58`, which delivered only the chip. |
| `d432863a` | Brandon Dillon | 07-21 | General UI/UX | **S** | all `*.html` heads (nothing) | Zero hits for favicon/rel=icon/apple-touch-icon, so every tab shows the generic browser icon. Add a Patriots-logo favicon plus apple-touch-icon to each page head (`logo.png` ships already but needs icon-size variants). |

### Time Clock (2)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `e06d747e` | Riley Scott | 04-10 | Time Clock / Check-In | **S** | `index.html`, `checkin.html`, `js/render.js` L228-235 | No floating/sticky check-in control, and the compact action row has no check-in button. **Underspecified** - scope the target page and exact behavior with the submitter. Pairs with `6bb61db2`. |
| `6bb61db2` | Riley Scott | 07-22 | Time Clock / Check-In | **S** | `js/render.js` L228-235; `js/time-tracking.js` | No compact check-in/out action and nothing that surfaces the active `time_log` at login. Add a compact check-in/out action plus a "you are clocked into X" banner across views. |

### Work Orders (1)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `2f752d40` | Jason Rubin | 07-21 | Work Orders | **S** | `js/work-orders.js` L324, L360, L365, L649-650 | `woFormDollar` and `woFormEstHours` are independent manual inputs read separately on save; no labor rate constant exists in the module. Auto-populate `dollar_value` = effective hours x $195/hr, respecting the `dollarLocked` gate. |

### Data Integrity & Duplicates (1)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `0e172b2c` | Sofia Pedroza | 07-16 | General UI/UX | **M** | `js/duplicates.js` L35, L40, L44, L116 | The merge engine groups strictly on the `PRVS-XXXX-XXXX` base RO id, so two ROs under different spellings of the same customer are never detected. Needs name/phone account dedup plus a verify-which-is-correct merge step. The one-off cleanup for the Huebert case is `3f254929`. |

### Messaging & e-sign (1)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `0448de62` | Sofia Pedroza | 06-11 | General UI/UX | **L** | `customer-checkin.html` signature pad; `send-quote-email` raf_copy | Signing is kiosk-only and the email only sends a copy *after* signing; zero hits for signing_token/remote_sign/signature_request/signing_link. Needs a tokenized public signing page, a delivery path and signed-form storage. Pairs naturally with the SMS channel. |

### Auth & Cost (1)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `ed1b873e` | Roland Shepard Jr | 06-07 | General UI/UX | **XL** | `js/auth.js`, `js/config.js` GOOGLE_CONFIG | Still Google id-token based (gapi init + One Tap + `signInWithIdToken`), kiosk included. Swapping to Supabase password/magic-link or shared role accounts saves roughly $500/mo but touches every page sign-in plus the email-keyed RBAC. Scope with Roland. |

### Time Off (1)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `47428f75` | Lynn Titel-Shepard | 08-03 | General UI/UX | **S** | `time-off.html` L270-272, L368, L372, L752 | **UNDERSPECIFIED** - "update vacation/sick/personal calendar" names no defect or target. The page already has all three types, a color-coded month calendar, stats tiles, per-row Edit and exact partial-day windows. Filed Needed rather than Done because the ask maps to no gap without Lynn's detail; if it restates `ee50d11c` it is already delivered. |

---

## Data corrections (2) - SQL for Roland, nothing executed

| ER | By | Date | Bucket | Verdict + SQL |
|---|---|---|---|---|
| `3f254929` | Sofia Pedroza | 07-16 | **data** | Keep Steve Huebert, remove the misspelled Hubert RO. Eyeball first: `SELECT id,ro_id,customer_name,status FROM repair_orders WHERE customer_name ILIKE '%ubert%';` then, once the wrong id is confirmed: `UPDATE repair_orders SET deleted_at=now() WHERE id='<wrong-id>';` The merge *feature* is `0e172b2c`. |
| `6938c1af` | Ryan Dillon | 05-06 | **data** | Already applied in the live DB (tech names read `staff.name`; submitter "Rod Wombles" now spells correctly in this very table). RESIDUAL, not a runtime bug: `supabase/migrations/staff_table.sql` L85-87 still seeds Rod/Zak/Travis as "Wimbles", so a re-seed would reintroduce it. Re-apply if ever needed: `UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |

---

## Duplicates (10) - fold, do not build

| ER | By | Date | Folds into | Note |
|---|---|---|---|---|
| `aa508852` | Riley Scott | 04-09 | `fd6c122d` (done) | Identical text and timestamp. Already declined. |
| `86bbaf50` | Lynn Titel-Shepard | 05-13 | `b19975d4` (needed/L) | A Gordon window returned to Vogt is supplier return/warranty tracking. Already declined. |
| `67daba15` | Brandon Dillon | 06-09 | `d2561e11` (done) | RO completion / pick-up date for records. Already declined. |
| `fa1c1d6e` | Sofia Pedroza | 06-09 | `a22739b9` (done) | Sheldon Gilbert check-in email report. Already declined. |
| `5057c7fc` | Roland Shepard Jr | 06-09 | `91a03260` (done) | Remove cashed-out RO from work list next day. Already declined. |
| `fc6b29f9` | Sofia Pedroza | 06-17 | `298e0e67` (done) | Retest report of the duplicate-RO guard. Already declined. |
| `9031e101` | Riley Scott | 07-21 | `d4d0eeba` (**bug**) | iPhone Work List tap does not reach the unit - same `scrollToROInBoard` defect. **Recommend `declined`.** |
| `1bc33dd6` | Brandon Dillon | 07-27 | `d4d0eeba` (**bug**) | Same scroll-to-entry defect, filed five days later. **Recommend `declined`.** |
| `2b78696c` | Lynn Titel-Shepard | 07-27 | `29654af7` (needed/S) | Back-to-top button, already filed 07-06. **Recommend `declined`.** |
| `eb00a68d` | Lynn Titel-Shepard | 08-03 | `972af03c` (needed/S) | RO status CANCELED, already filed 07-17. **Recommend `declined`.** |

---

## Done (52) - delivered and re-verified at HEAD 362137d

| ER | By | Date | Category | Mapped code | Verdict |
|---|---|---|---|---|---|
| `33750cb3` | Roland Shepard Jr | 04-09 | General UI/UX | - | Test entry ("Testing out the Make-A-Wish function"). Not a real request. Already declined; keep declined. |
| `19dc2016` | Roland Shepard Jr | 04-11 | Time Clock / Check-In | - | Test entry ("Testing ER form / dictation"). Already declined; keep declined. |
| `79b2c805` | Sofia Pedroza | 04-09 | New Feature Idea | `closed-ros.html` reactivateRO; `js/photos.js` L207, L253 | Reactivation restores the RO plus parts/time_logs/notes/audit_log/insurance_scans and the WO subtree; documents can be added to the reopened RO. The photo/doc library not surviving cash-out is Bug `09084bc5`, not this ask. |
| `75124b03` | Ryan Dillon | 04-09 | RO Management | `js/ro-crud.js` warranty branch; `js/render.js` L121, L195, L372 | v1.402: Warranty is a first-class RO type at creation, with board filter, compact chip and card badge (plus the $0 No-Charge sub-badge). |
| `b2aa4bea` | Lynn Titel-Shepard | 04-09 | New Feature Idea | `js/ro-crud.js` L409, L486; `js/render.js` L395 | `planned_dropoff_date` on create and edit, driving the blue Drop chip and the day-before reminder. Same field as `468aa376`. |
| `18611eee` | Lynn Titel-Shepard | 04-09 | Bug Report | `js/work-list.js` L195, L210, L223, L263, L304, L383 | Was a real bug; fixed. Members are viewable, reorderable and removable, with per-silo tabs. The desktop-half regression Lynn now reports is Bug `5ca724ff`. |
| `17492066` | Lynn Titel-Shepard | 04-09 | Notifications / Email | `scheduled_notifications.sql`; `process-scheduled-notifications` | The per-RO Schedule Notification bell sets a future date/time, message and recipients, with daily/weekly repeat until a chosen date. |
| `fd6c122d` | Riley Scott | 04-09 | Work List | `js/work-list.js` L195, L304, L389, L404-429 | v1.460: delete plus drag-reorder on mouse and touch, persisted by `_saveWorkListOrder`, mobile sizing fixed. Canonical for `aa508852`. Desktop regression tracked as `5ca724ff`. |
| `98bd7498` | Brandon Dillon | 04-09 | RO Management | `js/work-list.js` L223, L263 | Per-silo Work List tabs give a separate list per job category. |
| `1fe68261` | Rod Wombles | 04-13 | Work Orders | `js/shop-tasks.js` L21, L130, L153; `shop_tasks.sql` | v1.467: internal (non-customer) shop task tracker, separate from ROs and the board. Matches the poly-barrel / roof-bay examples. |
| `3dfc0acb` | Lynn Titel-Shepard | 04-14 | Work Orders | `js/config.js` L158 | v1.410: "Ready to Work" is a live RO status in the progress map, color map and both status dropdowns. |
| `b87eb2fb` | Lynn Titel-Shepard | 04-14 | RO Management | `keys_power_fields.sql`; `js/ro-crud.js` L411-413, L488-490 | v1.458: `key_status` / `keypad_code` / `keep_plugged_in` persisted on create and edit with a keys chip on the card. Same build as `34fc03c2`. |
| `a5ff3e2d` | Lynn Titel-Shepard | 04-21 | RO Management | `js/ro-crud.js` warranty_repair; `js/render.js` L122, L196, L373 | v1.465: "Warranty + Repair" RO type end to end, staying billable so the repair half bills normally. |
| `a7d1474e` | Lynn Titel-Shepard | 04-23 | RO Management | `urgent_update_field.sql`; `js/render.js` L214, L324; `js/ro-crud.js` L370 | v1.466: always-visible red URGENT banner on the card plus compact siren, and setting it notifies silo managers and admins. Lynn's follow-up to move the *input* onto the tile is `50175fce`. |
| `dde90d2b` | Roland Shepard Jr | 04-27 | RO Management | `customer-checkin.html` L2530, L2603 | Photo optional and both emails forced on every submission (`raf_copy` + `lead_staff_notify`). |
| `13e1cd87` | Tipton Scott | 04-30 | Time Clock / Check-In | `checkin.html` L36, L1516 | v1.447: "Return to Dashboard" button after clock-out, so no new tab is needed to clock into the next job. |
| `e27311bf` | Ryan Dillon | 05-04 | Work Orders | `js/work-orders.js` L461, L480-511 | v1.464: grip handle and drag-to-reorder on every WO task row (mouse + touch); DOM order drives `sort_order`, so templates are edited by load, reorder, Overwrite. |
| `d2561e11` | Brandon Dillon | 05-06 | New Feature Idea | `key_dates_pickup_date.sql`; `js/ro-crud.js` L410, L487; `js/render.js` L397 | v1.457: `pickup_date` persisted and surfaced as the green Pickup chip and in the key-dates panel. Canonical for `67daba15`. |
| `dac9fdda` | Brandon Dillon | 05-06 | General UI/UX | `js/config.js` L154; `js/render.js` L718-721 | v1.460: "Off Lot - Returning" separates units the customer temporarily took from units never on the lot, and is excluded from the on-lot count. |
| `a8e90a7d` | Brandon Dillon | 05-20 | General UI/UX | `js/parts.js` L152-155, L159-161 | v1.456: Manage Parts Ticket Totals bar sums parts, freight and total cost. Same build as `671fa623`. |
| `1aeb3f58` | Lynn Titel-Shepard | 05-21 | General UI/UX | `js/render.js` L30-39, L396 | v1.457: promised date as a large bold chip, red when overdue; promise date and days-on-lot both feed the blended priority sort. The strict selectable sort is `d1844e46`. |
| `cbc70a86` | Lynn Titel-Shepard | 05-21 | General UI/UX | `customer-checkin.html` L2381; `work_list_autoadd_rpc.sql` | S128: check-in calls `add_ro_to_silo_work_lists()`, inserting `manager_work_lists` rows for each involved silo lead with an sr-manager fallback. |
| `3f415c88` | Lynn Titel-Shepard | 05-26 | General UI/UX | `index.html` snRepeat; `process-scheduled-notifications` | v1.463: Repeat none/daily/weekly plus repeat-until, expanded into one cancellable row per occurrence. |
| `cd663d8f` | Bobby Thatcher | 05-28 | General UI/UX | `js/ro-crud.js` toFixed(2); `js/parts.js` MONEY_FIELDS | v1.447 cosmetic fix: currency inputs show 2 decimals on edit-populate and in the parts modal. No data was ever lost. |
| `34fc03c2` | Brandon Dillon | 05-29 | New Feature Idea | `keys_power_fields.sql`; `js/ro-crud.js` L411-413, L488-490 | v1.458: Keys selector (left / no keys / keypad + code, keep-plugged-in) on New and Edit RO with a card chip. Covers `b87eb2fb`. |
| `0b2e128c` | Lynn Titel-Shepard | 06-02 | General UI/UX | `checkin.html` L5, L1455 | v1.460: clock-out work notes copied into the RO update timeline as a `ro_status` note row. |
| `468aa376` | Lynn Titel-Shepard | 06-02 | General UI/UX | `js/ro-crud.js` L409, L486; `js/render.js` L395 | Planned drop-off input on New RO and (restored v1.457) Edit RO, feeding the Drop chip and the auto reminder. Same field as `b2aa4bea`. |
| `2b814250` | Roland Shepard Jr | 06-07 | Calendar | `js/render.js` L367, L395-397; `sync-ro-calendar` | Key dates render as colored chips plus a hover/tap panel, server-side calendar sync is idempotent via `cal_event_ids`, and advance reminders go to managers/admins. |
| `cd084233` | Roland Shepard Jr | 06-07 | Work List | `send-manager-report`, `send-admin-pnl-report`, `worklist-report.html` | Admin-level daily manager-worklist reporting with parts/labor/cost rollups. The manager *scoring* layer at the end of the request is a future extension, not this ER. |
| `a22739b9` | Sofia Pedroza | 06-08 | General UI/UX | `customer-checkin.html` L2530, L2603 | Was a notify-gate regression; both sends are now forced on every submission. Canonical for `fa1c1d6e`. |
| `91a03260` | Roland Shepard Jr | 06-09 | Work List | `prune_cashiered_worklists.sql` | Removes an RO from work lists the day *after* Delivered/Cashed Out, deliberately keeping it visible on the day of cash-out. Canonical for `5057c7fc`. |
| `cdd77a8b` | Brandon Dillon | 06-10 | General UI/UX | `js/render.js` L656-658, L718-721 | v1.453/v1.454: shop ROs dropped from the queue figure and Not On Lot / Scheduled / Off Lot - Returning excluded from the on-lot count, so the tile shows a true on-lot number. |
| `2c3d5633` | Ryan Dillon | 06-12 | General UI/UX | `js/photos.js` L66 | v1.453: the upload input is attached to the DOM before clicking, fixing the iOS Safari dropped-change-event that silently switched screens. |
| `74a33621` | Brandon Dillon | 06-16 | General UI/UX | `js/photos.js` L380, L382, L396-400 | v1.454: lightbox arrow-key navigation plus Escape-to-close, alongside prev/next buttons. |
| `298e0e67` | Sofia Pedroza | 06-16 | General UI/UX | `customer-checkin.html` L2059-2060, L2165, L2194 | Was a data-integrity bug; the returning-customer guard reuses an existing scheduled RO, and v1.17 adds a blocking attach-vs-new dialog. Canonical for `fc6b29f9`. |
| `9b823d25` | Roland Shepard Jr | 06-16 | RO Management | `js/work-orders.js` L244, L293; `service_added_notify_source.sql` | v1.460: adding a service silo to an RO emails the owning silo manager. Matches the roof-plus-water-heater example. |
| `9b046808` | Brandon Dillon | 06-17 | General UI/UX | `js/config.js` L157; `js/render.js` L718-721 | v1.454: "Scheduled" is its own status and not-yet-arrived units are excluded from days-on-lot and the on-lot count. |
| `671fa623` | Brandon Dillon | 06-18 | General UI/UX | `js/parts.js` L153, L155, L159-161 | v1.456: a Freight line summed across parts and rolled into Total Cost. Same build as `a8e90a7d`. |
| `5c45bb8d` | Sofia Pedroza | 06-19 | General UI/UX | `customer-checkin.html` L2059, L2165, L2530 | Both halves of the David Flanagan report resolved: the Scheduled returning path reuses the RO, and the auth-form copy is forced on every submission. |
| `b06a285f` | Lynn Titel-Shepard | 06-19 | General UI/UX | `js/render.js` L367, L395-397 | v1.465: a calendar icon by the customer name opens a key-dates panel listing drop-off/promised/pickup in full on hover or tap. |
| `ee50d11c` | Lynn Titel-Shepard | 06-19 | General UI/UX | `time-off.html` L752, editRequest | Was a real gap; time-off entries are editable, not delete-only - Edit reloads the entry and flips the button to "Update Record", so day, person and type can all change. |
| `88395acc` | Lynn Titel-Shepard | 06-19 | General UI/UX | `add_absence_window_to_time_off.sql`; `time-off.html` L368, L372 | Time Off v1.5: partial-day absence captured as an exact clock-out-to-return window rather than an hour total. |
| `80390f36` | Lynn Titel-Shepard | 06-23 | General UI/UX | `js/render.js` L35, L37, L61 | v1.462: Shop Operations tiles pinned above the priority sort, plus a "shop operations shop" search token so the tile is always findable. |
| `7f2d0d70` | Lynn Titel-Shepard | 06-26 | General UI/UX | `supabase/functions/send-dropoff-report` | The automated Monday drop-off email lists this week's scheduled drops plus last week's received units and reads `manager_work_lists` to flag any missing from the right team's list. Pairs with `cbc70a86`. |
| `3f7fb4fc` | Brandon Dillon | 06-29 | General UI/UX | `js/render.js` L656-658, L718-721, cep-static-status | v1.468: shop ROs excluded from RVs-in-Queue and the on-lot count, and their status dropdown replaced by a static label. |
| `cb7742a8` | Lynn Titel-Shepard | 06-29 | General UI/UX | `sync-ro-calendar`; `customer-checkin.html` L22, L2240 | v1.469: server-side calendar sync owns drop-off/promised/pickup events for every RO including front-desk check-ins, fixing the repair-calendar miss. |
| `39b75fab` | Sofia Pedroza | 07-09 | General UI/UX | `js/photos.js` L198-201, L376, canManageFiles | v1.472: rename, move-to-another-RO and delete for documents and photos, office-staff gated - so the Marco DeAlba docs on the Ruiz RO can be moved or removed. |
| `1c7f1410` | Andrew Page | 07-16 | New Feature Idea | `js/messaging.js` L295, L347, L428-435, L578-579 | Photo/PDF half shipped S151b/S158, video half S166: the composer accepts images, PDFs and video, up to 5 per send, splitting video into links vs MMS media. **DB row still `unreviewed` - recommend `done`.** |
| `9d3806e7` | Brandon Dillon | 07-22 | General UI/UX | `messages.html` detailNoteView, openNotesModal; `customer_note_entries_s154.sql` | S154: customer-side Notes section in the details pane plus an Add-Notes modal writing append-only rows keyed by phone. **DB row still `unreviewed` - recommend `done`.** |
| `ac394468` | Roland Shepard Jr | 07-24 | General UI/UX | `js/enhancement.js` L43 pageDefaults | v1.482: the `erCategory` select carries Messages/Texting plus Solar, Leads, Closed ROs, Reports, Time Off and Employee Guide. RESIDUAL, stated not hidden: `erCategory` exists in only 6 live HTML files (analytics, checkin, closed-ros, index, solar, worklist-report), so messages/leads/time-off/guide cannot *raise* an ER - worth its own small ER. |
| `d62a26e4` | Roland Shepard Jr | 07-24 | New Feature Idea | `js/messaging.js` L290, L295, L347, L386-392, L578-579 | v1.482/S158a: multiple attachments with removable per-file chips, capped at 5, and the send path splits video links from MMS media because a carrier payload cap silently stripped multi-media sends. |
| `93b00023` | Roland Shepard Jr | 07-24 | New Feature Idea | `textly-webhook`; `send-unreplied-reminder`; `unreplied_eod_reminder_s158.sql` | v1.482 (S158), all three parts: inbound reopens the conversation and logs a `reopened` event, the owner notify fires only on the needs-reply flip, and the 4:30pm unread sweep runs on a weekday cron. |

---

## Recommended action today

1. **Fix `09084bc5` first.** It is the only item on the board that destroys
   customer data permanently, and every day it stays open another cash-out
   drops that RO's photo/doc library, key dates and urgent update with no
   recovery path. The fix is bounded: widen the `cashiered` allowlist
   (`js/ro-crud.js` L666-693 and the `archive_cashiered_ros` SQL twin) and add
   a photo/doc mirror to `cashiered_child_archives` so `reactivateRO` can
   restore it. Note this changes the archive schema, so it wants its own
   session and a real cash-out/reactivate round trip in the Chrome regression.
2. **Then `225a2535`** - one-line class of fix (key `_lastEstimateScan` to the
   RO id instead of `mode`), but it currently writes one customer's insurance
   data onto another's RO. Small change, high consequence.
3. **One cheap bug bundle:** `d94a418a` (stop reversing note lines) +
   `d4d0eeba` (clear filters, re-render, scroll, highlight). `d4d0eeba` also
   closes duplicates `9031e101` and `1bc33dd6`, so one fix retires three rows.
4. **Get a repro for the new ER `5ca724ff`** before building. The desktop drag
   path exists in code; the most likely cause is the empty `dataTransfer` on
   `dragstart` (Safari/Firefox refuse such a drag). Ask Lynn which browser she
   is on and whether the row lifts at all - if it is Chrome, the diagnosis is
   wrong and it needs a live look.
5. **Bundle the RO-fields S items into one build:** `972af03c` (Canceled) +
   `3e746c18` (Totaled Out) + `ac8265c8` (mileage), and exclude both new
   terminal statuses from the queue and on-lot counts. `eb00a68d` folds into
   `972af03c`. One session should cover all three ERs and one duplicate.
6. **Bundle the Messages UX S items:** `9aab7550`, `21fc3a9d`, `f1e6e311`,
   `6e9ca4dc`, `ca0f7ed9` are all messages.html polish and share one
   regression pass.
7. **Unblock the two underspecified items** by asking the submitters:
   `8d885331` (Lynn's parts field order), `e06d747e` (Riley's check-in float)
   and `47428f75` (Lynn's time-off calendar ask, which may just restate the
   already-delivered `ee50d11c`).
8. **Status flips for Roland** (this runner changes nothing): set `done` on
   `1c7f1410` and `9d3806e7`; set `declined` on `9031e101`, `1bc33dd6`,
   `2b78696c`, `eb00a68d`. Run the two Data-bucket SQL blocks above only after
   eyeballing the rows.
9. **Plan, do not rush, the XLs:** `ed1b873e` (auth swap, ~$500/mo saving) and
   `14f35f95` (parts inventory) each need their own scoping session.

## Notes and caveats from this run

- **Incidental finding, not an ER:** `js/work-list.js` L286 and L298 use
  curly quotes (U+201D) instead of straight quotes to open HTML `style`
  attributes, so the Work List empty-state message and the per-row silo tag
  render unstyled. Found while mapping `5ca724ff`; cosmetic, no data risk. No
  code was changed by this runner.
- **One row was not triaged:** `f73a1e5a-2f15-4b3c-8e2a-3e25b9d32276` (Roland,
  04-13, "Testing dictation 123 testing enhancement request dictation") is not
  in `needs_triage_ids` and has no stored `triage_bucket`, so it carries with
  null verdict fields. It is a declined test entry, so nothing is at risk, but
  it will keep falling through the incremental filter until someone triages it
  once.
- **No status changes, no code changes, no SQL executed, no network calls.**
  Only `er_verdicts.json`, this report, and
  `docs/er-triage-dashboard.html` were written.
- **Prompt-injection screen:** every `description`, `submitted_by_name` and
  `admin_notes` value was read as data only. No ER contained instruction-like
  content this pass, so nothing was flagged for that reason.
