# PRVS Enhancement-Request Triage -- 2026-07-17 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched
2026-07-17T09:11Z; incremental pass, `full_pass=false`).
**Codebase mapped against:** current `pre-prod` tree at **index.html v1.475**
(checkin.html v1.34+, customer-checkin.html v1.6+, time-off.html v1.5).
**Total ERs:** 70. **Triaged this run:** 69 ids in `needs_triage_ids`; 1 row
(`f73a1e5a`, a "Testing dictation" test entry, already declined) carried over
unchanged.

Every ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `closed-ros.html`, `time-off.html`, `worklist-report.html`,
`supabase/functions/*`, SQL migrations) by grepping/reading the source, not
assuming, and sorted into Done / Bug / Needed / Data / Duplicate.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

## Board snapshot (start of run, all 70)

| Status | Count |
|---|---|
| done | 46 |
| declined | 9 |
| unreviewed | 15 |
| **Total** | **70** |

## Triage outcome (69 re-triaged)

| Bucket | Count |
|---|---|
| done | 47 (incl. 2 test entries) |
| duplicate | 6 |
| data | 2 |
| needed | 14 |

## Headline

The board has moved a long way since the 2026-06-28 pass (v1.466 -> v1.475).
**Every "needed" item flagged in the last three reports that had shipped has now
been verified in code and confirmed Done**, including the large Notifications /
Key-Dates / Calendar initiative:

- Scheduled/advance notifications (`scheduled_notifications` + `process-scheduled-notifications` cron), recurring reminders (v1.463), key-date day-before/morning-of reminders, urgent-update banner + notify (v1.466), service-added notify (v1.460).
- Calendar push to per-silo business calendars (`sync-ro-calendar`) + colored RO calendar buttons.
- Weekly drop-off reconciliation report (`send-dropoff-report`).
- Returning-customer check-in auto-adds to each team lead's work list (`add_ro_to_silo_work_lists`).
- Admin manager-worklist reporting (`send-manager-report` admin preview + `send-admin-pnl-report` + `worklist-report.html`).
- Warranty + Repair RO type (v1.465), keys / keep-plugged-in fields (v1.458), pickup/completed key date (v1.457), non-customer shop tasks (`shop-tasks.js`), WO template drag-reorder (v1.464), time-off exact windows + editing (v1.5), doc delete/move (v1.472), arrow-key photo nav.

**Bug bucket is empty.** The two historical bug reports still in the queue --
`cd663d8f` (currency 425.00 -> 425) and `2c3d5633` (phone photo upload silently
fails) -- are both confirmed **fixed** in current code, so they land in Done, not Bug.
The returning-customer duplicate-RO reports (`298e0e67`, `5c45bb8d`, and the
Sofia/Sheldon email-not-sent reports) are also resolved.

**15 open (unreviewed) ERs triaged:** 14 are valid open enhancements (Needed),
1 is a data correction. No new code is queued from this read-only run.

---

## DONE bucket (verified against v1.475 -- recommend close)

| ER | By | Request | Evidence (current code) |
|---|---|---|---|
| `18611eee` | Lynn | Work List opened but couldn't view/act | work-list.js renderWorkList/removeFromWorkList/_workListActiveSilo; drag-reorder |
| `98bd7498` | Brandon | Different work lists per category | Per-silo tabs: work-list.js _renderWorkListSiloTabs/_setWorkListSilo |
| `fd6c122d` | Riley | Work List: delete + phone size + drag-drop | Delete + desktop/touch drag-reorder (work-list.js:387-430). Canonical for aa508852 |
| `75124b03` | Ryan | Warranty category on new RO | Warranty RO type buttons (index.html); render.js badge; ro-crud.js ro_type |
| `a5ff3e2d` | Lynn | Warranty + Repair combined RO type | v1.465: warranty_repair type, buttons, filter, badge, notes-prefix |
| `79b2c805` | Sofia | See/edit cashed-out customers + add docs | closed-ros.html reactivateRO restores parts/time_logs/photos from cashiered_* |
| `39b75fab` | Sofia | Remove docs from RO (wrong-customer scan) | v1.472 photos.js deleteLibraryItem + openMoveFileModal/moveLibraryItem |
| `74a33621` | Brandon | Arrow-key navigation between photos | photos.js ArrowLeft/ArrowRight -> navigateLightbox |
| `b2aa4bea` / `468aa376` | Lynn | Scheduled future drop-off date field | plannedDropoffDate on New/Edit RO + kiosk scheduledDropoffDate -> planned_dropoff_date |
| `d2561e11` | Brandon | Completion / pickup date for records | v1.457 pickup_date column + green Pickup key-date chip. Canonical for 67daba15 |
| `1aeb3f58` | Lynn | Promised date large + highlighted + sort | render.js bold amber Promised chip (red overdue) + blended priority sort |
| `b06a285f` | Lynn | Hover near name to see important dates | render.js kd-name-cal + kd-pop hover panel |
| `dac9fdda` | Brandon | On-lot sub-status: temporarily taken/returning | "Off Lot - Returning" status, excluded from on-lot count |
| `9b046808` | Brandon | "Scheduled to arrive" not counted as on-lot | render.js excludes Scheduled from on-lot + days-on-lot |
| `3f7fb4fc` | Brandon | Remove Shop RO status dropdown / counts | render.js static status for shop; excluded from queue/on-lot counts |
| `80390f36` | Lynn | Pin Shop Operations tile + make searchable | v1.462 shop-first sort + synthetic search token |
| `cdd77a8b` | Brandon | Total RVs: on-lot vs total customers | render.js statTotalOnLot split + "N on lot" sub-line |
| `b87eb2fb` / `34fc03c2` | Lynn / Brandon | Keep-plugged-in + keys/no-keys/keypad | v1.458 Keys select + keypad code + plugged-in checkbox; card chips |
| `3dfc0acb` | Lynn | "Ready to Work" status | Status option + filter + priority + color + check-in flippable |
| `a8e90a7d` / `671fa623` | Brandon | Parts + freight TOTAL in Manage Parts | v1.456 Ticket Totals row incl. Freight (core_charge) in Total Cost |
| `13e1cd87` | Tipton | iPhone: back to dashboard after clock-out | checkin.html v1.34 Return-to-Dashboard button |
| `e27311bf` | Ryan | Edit WO templates + drag-drop reorder | v1.464 work-orders.js _initWOTaskDrag + sort_order persist |
| `1fe68261` | Rod | Non-customer/shop tasks (poly barrels, roof bays) | S128 shop-tasks.js + shop_tasks table (CRUD, assignee, silo) |
| `88395acc` | Lynn | Time-off exact absence window (not just hours) | time-off.html v1.5 half-day presets + start/return time inputs |
| `ee50d11c` | Lynn | Edit existing time-off entries | S118 editRequest(id) -> UPDATE (editingRequestId); per-row Edit |
| `0b2e128c` | Lynn | Transfer clock-in job notes -> RO updates | S120 clock-out work notes written into RO notes (checkin.html:1435) |
| `dde90d2b` | Roland | Fix/streamline customer check-in + force email | customer-checkin.html v1.6 photo optional + RAF email forced |
| `a22739b9` | Sofia | Check-in email + copy of auth form not sent | sendLeadStaffNotification + sendRAFEmail (raf_copy) on every submit. Canonical for fa1c1d6e |
| `298e0e67` | Sofia | Returning drop-off duplicates the RO | customer-checkin.html reuses the Scheduled RO. Canonical for fc6b29f9 |
| `5c45bb8d` | Sofia | Flanagan: returning dup + no RAF email | Resolved via RO-reuse + forced RAF email (with 298e0e67 / a22739b9) |
| `2c3d5633` | Ryan | Can't upload pics from phone (silent fail) | Fixed: photos.js appends input to DOM before click (iOS) + HEIC/HEIF |
| `cd663d8f` | Bobby | Amounts drop the period/zeros (425.00->425) | Fixed v1.447: 2-decimal formatting on currency inputs; no data loss |
| `17492066` | Lynn | Future notifications (remind owner ahead of time) | scheduled_notifications engine; per-RO Schedule Notification to any staff |
| `3f415c88` | Lynn | Multi-day / weekly-until-complete reminders | v1.463 recurring reminders (daily/weekly + repeat-until) |
| `a7d1474e` | Lynn | Always-visible urgent-updates field + notify | v1.466 urgent-update banner (render.js) + _notifyUrgentUpdate |
| `9b823d25` | Roland | Notify silo manager when a service is added | v1.460 addServiceToRO -> service_added_notify |
| `cbc70a86` | Lynn | Returning check-in auto-adds to team worklists | add_ro_to_silo_work_lists RPC on drop-off check-in |
| `7f2d0d70` | Lynn | Weekly Monday drop-off reconciliation report | send-dropoff-report (coming-in + received-last-week vs worklist) |
| `cb7742a8` | Lynn | New check-in date -> correct team calendar | customer-checkin.html calls sync-ro-calendar after RO create |
| `2b814250` | Roland | RO dates as colored button + calendar + notify | Colored calendar buttons + sync-ro-calendar push; advance notice via email reminders |
| `91a03260` | Roland | Remove cashed-out RO from worklist next day | prune_cashiered_worklists() daily pg_cron. Canonical for 5057c7fc |
| `cd084233` | Roland | Admin manager-worklist report (parts/labor/cost) | send-manager-report admin preview + send-admin-pnl-report + worklist-report.html |

Two test entries also land here for closure: `33750cb3` and `19dc2016`
(recommend `declined`; both already declined).

**Notes on partials (Done, with a tracked gap):**
- `cd084233` core is delivered; **manager scoring** is deliberately not built (report is GUIDE-not-JUDGE) and **"ROs that should be on a worklist but are not"** detection is not yet built -- future work, not a blocker for closing the core ask.
- `2b814250` has calendar buttons + per-silo push but no native Google Calendar popup reminders; advance notice is delivered via the `scheduled_notifications` email reminders instead.
- `17492066` reminders target managers/admins/any staff, not subcontractors by name.

---

## DATA bucket (SQL for Roland -- runner does not execute anything)

| ER | By | Request | Fix |
|---|---|---|---|
| `6938c1af` | Ryan | Zak/Rod/Travis last name Wimbles -> Wombles | Data, not code (staff.name; no Wimbles string in js). Already applied per S117. |
| `3f254929` | Sofia | Delete mis-spelled "Hubert" duplicate of Steve Huebert | Data cleanup. The account-merge *feature* is tracked under `0e172b2c`. |

**`6938c1af` SQL** (only if any rows remain):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

**`3f254929` -- locate the duplicate first, then remove the wrong one via the
dashboard's soft-delete after Roland confirms the id** (do NOT auto-run a delete):

```sql
SELECT id, ro_id, customer_name, status, created_at
FROM repair_orders
WHERE customer_name ILIKE '%hubert%'
ORDER BY created_at;
```

Correct spelling is **Steve Huebert**; the mis-spelled **Hubert** row is the one
to remove.

---

## DUPLICATE bucket (fold into canonical; already declined)

| ER | By | Canonical | Status of canonical |
|---|---|---|---|
| `aa508852` | Riley | `fd6c122d` (Work List delete/size/drag) | Done |
| `86bbaf50` | Lynn | `b19975d4` (part return/warranty to supplier) | Needed/L |
| `5057c7fc` | Roland | `91a03260` (drop cashed-out RO from worklist) | Done |
| `67daba15` | Brandon | `d2561e11` (RO completion/pickup date) | Done |
| `fa1c1d6e` | Sofia | `a22739b9` (check-in email not sent) | Done |
| `fc6b29f9` | Sofia | `298e0e67` (returning check-in duplicates RO) | Done |

---

## NEEDED bucket (14 open enhancements + LOE)

LOE mix: **2 XL - 5 L - 2 M - 5 S.** Grouped by theme so overlapping wishes are
designed together.

### Notifications & Task-Tracking
| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5925fc01` | Lynn | L | Notify on every RO update + daily list with to-dos scraped from status notes | **Partial.** Daily-digest half largely served by the Manager Daily Report. Still missing: a notification on *every* generic RO update (only urgent/service-add/key-date events fire today) and note-scraped to-dos. Anchor of this cluster. |
| `af14b3cb` | Lynn | L | Per-customer task tracker + daily reminder; tasks persist until complete | No per-customer tracker (shop-tasks.js is deliberately non-customer, no reminder). Needs a per-RO tasks table + add/complete UI + daily open-task reminder. Pairs with 5925fc01. |

### Parts & Inventory
| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `14f35f95` | Lynn | XL | In-house parts inventory/stock (order 25, 20 to searchable priced stock) | New subsystem. Nothing in parts.js handles stock (only per-RO receipt). Largest board item. |
| `b19975d4` | Lynn | L | Part returned-to-supplier / warrantied-by-supplier | No structured workflow -- only a static "Returned" status + free-text fields. = GH#24 Shop Ops Phase 2. Canonical for dup 86bbaf50. |
| `8d885331` | Lynn | S | Rework parts-entry field order | **Blocked on detail, not effort.** Form has 5 sections today; no target layout given. Quick once Lynn supplies a mockup. |

### Messaging & e-sign
| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `0448de62` | Sofia | L | Send an authorization form remotely for e-signature | No remote-signing flow (RAF is kiosk-only). Needs a tokenized public signing page + delivery + signed-form storage. |
| `1c7f1410` | Andrew | M | Attach media (photos/videos) to a message | Outbound send is text-only (inbound MMS is render-only; modal MMS-send deferred). Needs a file input + upload + media field to projectblue-send. |

### Auth & cost
| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `ed1b873e` | Roland | XL | Replace Google-email auth (~$500/mo) with DB auth | Still Google id-token + email RBAC. Security-critical swap; touches every page's sign-in + RBAC. Roland wants to scope together. |

### RO fields / status & data quality
| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `0e172b2c` | Sofia | L | Merge duplicate customer accounts / verify + delete wrong one | duplicates.js only merges by shared RO-id base -- no fuzzy name match (won't catch Hubert vs Huebert). Needs name-based dedup + verify-which-is-correct flow. Specific data cleanup is under 3f254929. |
| `5fbfa299` | Brandon | M | Archive quote-only customers/units that never moved forward | No archive concept for quotes (only the unrelated cashiered flow). Needs an archived status/flag + a hide-quoted filter. |
| `5fec3eca` | Sofia | S | Show customer address on the emailed auth form | Address is NOT on the RAF copy (only on the staff-notify email). Small: add address to the send-quote-email raf_copy payload + template. |

### Dashboard UX / sorting
| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `d1844e46` | Lynn | S | Sort dash by promised date then oldest-on-lot, shop ops pinned | Shop-ops pinning exists; no strict promise-then-oldest sort/control (board uses one blended priority score). Small: add a strict sort option keeping shop-ops pinned. |
| `29654af7` | Lynn | S | Back-to-top / quick-jump-to-search button | No such control on the dashboard. Small: a scroll-triggered back-to-top / to-search button. |

### Time clock / Check-In
| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `e06d747e` | Riley | S | Tech check-in "float" for ease of access | **Underspecified.** No floating/sticky check-in exists (check-in is per-tile/QR). Likely a floating button, but behavior unclear -- scope with the submitter. |

---

## Recommended action today

1. **Close the verified Done set** (see the Done table -- 45 real requests +
   `33750cb3`/`19dc2016` test entries). The bulk of the shift since 06-28 is the
   Notifications / Key-Dates / Calendar initiative shipping.
2. **Run the data corrections:** `6938c1af` (staff-name SQL, likely already
   applied) and `3f254929` (locate + soft-delete the mis-spelled "Hubert" RO
   after confirming the id; do not auto-delete).
3. **Unblock the two spec-blocked S's** by asking submitters for specifics:
   Lynn's parts field order (`8d885331`) and Riley's check-in float (`e06d747e`).
4. **Ship the easy S's next:** `5fec3eca` (address on the RAF email),
   `29654af7` (back-to-top), and `d1844e46` (strict promised-then-oldest sort)
   are all small and well-defined.
5. **Plan, don't rush, the XL's:** the auth swap (`ed1b873e`) and parts
   inventory (`14f35f95`) each deserve their own scoping session. The
   Notifications/Task-Tracking L's (`5925fc01`, `af14b3cb`) should be designed
   as one initiative, and `0e172b2c` (name-based account merge) alongside the
   `3f254929` cleanup.
6. **Bug bucket is empty** -- nothing to auto-fix this run.
