# Enhancement Request Triage -- 2026-06-17 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-17T11:53Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.452** (checkin.html v1.37, customer-checkin.html v1.11).
**Total ERs:** 51 (incl. 3 test entries + 4 exact/near duplicates -> **44 real requests**).
**Triaged this run:** 50 ids in `needs_triage_ids` (every row except the n15 test entry, carried over unchanged).

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `closed-ros.html`, `worklist-report.html`,
`supabase/functions/*`) by grepping the source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

---

## Headline

**The bug bucket is no longer empty.** Two functional defects are confirmed open
against current code, both newly surfaced since the v1.447 report:

- **n49** (Sofia, data-integrity, **large**) -- a returning customer who already
  has a *future scheduled-drop RO* checks in and the system inserts a SECOND RO.
  `customer-checkin.html` `selectReturningCustomer` (L1445) -> submit insert
  (L2025-2050) has **no dedupe** against an existing Scheduled/future-drop RO; it
  only catches a 23505 on the generated RO id. Fix = match + update the existing
  scheduled RO instead of inserting. Confirm scope with Roland before fixing.
- **n47** (Ryan, mobile-only, **small-medium**) -- phone photo upload appears to
  accept files then switches screens with no upload and no error toast.
  `photos.js` `uploadPhoto` (L50-128) builds a *detached* dynamic `<input>` +
  async `onchange` (L57); iOS Safari often reloads on camera return, discarding
  it so `onchange` never fires (no error path). Fix = persistent in-DOM input +
  verify the Storage write. Needs a device repro.

All prior "done" verdicts were re-verified in the current tree and still hold
(warranty RO type, per-silo work lists, Ready-to-Work status, currency 2-decimal
fix, Return-to-Dashboard button, check-in staff-notify + RAF email). The only
data-only item is **n25** (staff-name SQL; Roland runs it).

**Three brand-new ERs** arrived 2026-06-16:

- **n48** (Brandon) -- arrow-key navigation through photos. The lightbox already
  has prev/next buttons (`photos.js` `navigateLightbox` L370) but no keydown
  listener; small add. **NEEDED / S.**
- **n49** (Sofia) -- the duplicate-RO defect above. **BUG.**
- **n50** (Roland) -- email the silo manager when another silo adds a service to
  an RO. `work-orders.js` `addServiceToRO` (L243-262) sends no notification.
  **NEEDED / M** (Notifications cluster).

The strategic in-flight item **n39** (admin Manager-worklist report) has advanced
to P4 (per-manager sends live + daily cron, send-manager-report v2.1-P4); it
stays NEEDED/L with only the manager-scoring + profit-margin phase outstanding.

---

## BUG bucket (front of the line)

| # | Submitter | Request | Mapped code | Severity |
|---|---|---|---|---|
| n49 | Sofia | Returning-customer check-in duplicates an RO when a future scheduled-drop RO already exists | `customer-checkin.html` selectReturningCustomer L1445; insert L2025-2050 (no dedupe) | **large** (data integrity) |
| n47 | Ryan | Can't upload photos from phone -- accepts then switches screens, no upload | `photos.js` uploadPhoto L50-128 (detached input + async onchange L57) | small-medium (mobile-only) |

Both prefer **fixing over closing**. n49 needs a scope confirm from Roland (match
the existing scheduled RO vs. block the duplicate); n47 needs an on-device repro.

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation restores full RO detail (cashiered_parts/time_logs/photos, L950-956). | done (already) |
| n2 | Ryan | Warranty category on new RO | Warranty RO type shipped v1.402 (`ro-crud.js` roType=='warranty' L855; `render.js` filter L112). | done (already) |
| n5 | Lynn | Work list opened but couldn't view/do anything | Fixed Phase 14: `work-list.js` renderWorkList L269 + removeFromWorkList L195 + _setWorkListSilo L263. | done |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs` L223 / `_setWorkListSilo` L263). | done |
| n17 | Lynn | Add "Ready to Work" RO status | Shipped v1.410 (`config.js` STATUS_PROGRESS_MAP L131 slot 50). DB mislabeled in-progress. | done |
| n21 | Roland | Fix customer check-in, email out, remove photo, streamline | `customer-checkin.html` v1.11 made photo optional + forces RAF/lead-staff email every submission. | done |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | FIXED v1.447 (`checkin.html` Return-to-Dashboard button L1487, `[ER BUGFIX S94] n22`; still present at v1.37). | done |
| n33 | Bobby | Amounts drop the period and the zeros (425.00 -> 425) | FIXED v1.447 (`ro-crud.js` editDollarValue L843 `.toFixed(2)` + `parts.js` MONEY_FIELDS L288). Cosmetic, no data loss. | done |
| n40 | Sofia | Check-in no longer emails staff / no auth-form copy | FIXED v1.10/v1.11 (`customer-checkin.html` shouldNotifyStaff L2210 fires every drop-off; RAF always sends; getSignatureData typo fixed). | done |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod and Travis last name should be Wombles, not Wimbles, in tech assignment" | Data, not code. `staff.name` holds the value; not hardcoded. One SQL UPDATE (below). |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

Historical ROs that stored the tech name as free text are a separate optional backfill.

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete jobs / too small on phone / drag-and-drop" | Exact duplicate of **n10** (identical text + timestamp); already declined. Fold into n10. |
| n41 | Brandon | Completion / P-U date to record when units leave the lot | Duplicate of **n24** (same submitter); same Key Dates pass. Fold into n24. |
| n42 | Sofia | Returning customer (Sheldon Gilbert) check-in sent no email | Duplicate of **n40**; resolved by the v1.10/v1.11 staff-notify fix. Fold into n40. |
| n44 | Roland | Auto-remove cashed-out RO from manager worklist | Exact duplicate of **n43** (timestamps 1s apart). Fold into n43. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Scheduling & key dates  *(n4, n24, n29, n36, n37)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n4 | Lynn | Scheduled future drop-off date + email reminder to manager 1 day before | M |
| n36 | Lynn | Scheduled drop-date field in entry dashboard | S -- duplicate intent of n4 |
| n24 | Brandon | Completion / pickup date on the customer's order for reference (canonical for n41) | M |
| n29 | Lynn | Promised date large + highlighted on tile; sort by promise date then days-on-lot | M |
| n37 | Roland | Surface RO date/time (pickup/dropoff/reminders) as a prominent colored RO button + integrate into business calendar(s) with advance notifications | L |

-> **"Key Dates + Calendar" initiative.** n37 is the umbrella ask tying the
drop-off/promised/completion dates (n4/n24/n29/n36) to a calendar surface and the
notifications cluster below.

### Theme 2 -- Notifications, reminders & task-tracking  *(n3, n8, n20, n30, n32, n35, n50)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + font/color | L |
| n35 | Lynn | Transfer clock-in job notes -> RO updates (`time-tracking.js` workNotes L53, no auto-copy) | M |
| n50 | Roland | Email the silo manager when another silo adds a service to an RO (`work-orders.js` addServiceToRO L243-262, no notify) | M |

-> **Largest opportunity.** A "Task Tracker + Reminders" subsystem; overlaps the
deferred messaging (Sendblue POC) work -- design as one initiative. n50 is a
concrete cross-silo notification trigger that the same notifier can serve.

### Theme 3 -- Parts  *(n6, n7, n9, n27, n28)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n28 | Brandon | Parts + freight TOTAL in Manage Parts (per-part freight `pf_coreCharge` L217 exists; no summed total) | S -- clean quick win |
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier (canonical for n27) | L |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

### Theme 4 -- Work List usability  *(n10, n31, n43)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n10 | Riley | Mobile: too small on phone; touch drag-reorder (delete + HTML5 DnD reorder already shipped) | S -- mobile polish |
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |
| n43 | Roland | Auto-remove an RO from a manager's work list the day after Cashiered Out/Delivered (no status hook today) | M |

### Theme 5 -- RO fields & status nuance  *(n16, n18, n19, n26, n34)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n18 | Lynn | "Keep plugged in" flag; keys / no-keys / keypad code | M |
| n34 | Brandon | Confirm if customer leaves keys with the unit (RO / sign-in / auth form) | S -- overlaps n18 |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning vs never-on-lot | M |
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs existing Hybrid) |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |

### Theme 6 -- Work Orders / templates  *(n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will (`work-orders.js` loadWOTemplate L399/saveWOTemplate L468, no edit/reorder) | M |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague -- overlaps n22) |

### Theme 7 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

### Theme 8 -- Reporting & analytics  *(n39, n45)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n39 | Roland | Admin-level daily Manager-worklist report -> manager scoring + profit-margin visibility (P1-P4 shipped, send-manager-report v2.1-P4 per-manager sends + daily cron live; scoring/margin phase outstanding) | L |
| n45 | Brandon | Split the dashboard "Total RVs"/"RVs on Lot" stat so on-lot is not conflated with "Not On Lot" (`render.js` onLotROs L594 exists; headline stat L611 not split) | S |

### Theme 9 -- Check-In & authorization forms  *(n46)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n46 | Sofia | Send the RV authorization form (RAF) remotely for e-signature (on-site signing only today; no tokenized remote-signing route) | L |

### Theme 10 -- Photos & media  *(n48)*  [new]
| # | Submitter | Request | LOE |
|---|---|---|---|
| n48 | Brandon | Arrow-key navigation through photos (lightbox `navigateLightbox` L370 exists; no keydown listener wired) | S |

---

## Recommended action today

1. **Bug bucket has two open defects.** Queue **n49** (returning-customer
   duplicate RO -- data integrity, large; confirm match-vs-block scope with
   Roland first) and **n47** (mobile photo upload -- needs a device repro). These
   jump to the front of the line per the triage rails.
2. **Close the resolved ERs:** n5, n12, n17, n21, n22, n33, n40 (set `status=done`);
   n11, n41, n42, n44 (`declined`, duplicates). n0/n14/n15/n1/n2 already closed.
3. **Run the n25 staff-name SQL** (Roland; data only).
4. **Quick enhancement wins** if a session opens up: **n45** (split Total/on-lot
   stat, S), **n28** (parts+freight total, S), **n48** (arrow-key photo nav, S).

Standout coordinated passes remain the **Key Dates + Calendar** initiative
(Theme 1, anchored by n37) and the **Task Tracker + Reminders** subsystem
(Theme 2, now including n50's cross-silo trigger). The strategic asks **n38**
(auth cost reduction) and the remaining **n39** scoring/margin phase still warrant
a design session with Roland before scoping.
