# PRVS Enhancement-Request Triage - 2026-07-28 (headless nightly runner)

Incremental pass (`full_pass: false`, input fetched 2026-07-28 09:38 UTC) over the
`enhancement_requests` table against the current `pre-prod` codebase
(index.html **v1.483**, messages.html **v1.16**, checkin.html v1.38,
customer-checkin.html v1.15). 88 rows in the table; **87 re-classified this run**,
1 carried over untouched. Read-only on the codebase and on the DB: no code was
changed, no SQL was executed, no ER status was flipped. Roland flips status himself.

Every `done` / `bug` / "still absent" verdict below was re-verified by grepping or
reading the live code this run - not carried forward on faith. Line references were
re-checked against current files; the messages.html references moved because of the
v1.15/v1.16 broadcast work and are updated here.

## Board snapshot

| Status (in DB) | Count |
|---|---|
| done | 49 |
| declined | 9 |
| unreviewed | 30 |
| **Total** | **88** |

| Triage bucket (this run) | Count |
|---|---|
| done | 52 |
| needed | 22 |
| duplicate | 8 |
| bug | 3 |
| data | 2 |
| **Classified** | **87** |
| carried over, not triaged | 1 |

**30 rows are still `unreviewed`.** They break down as **22 Needed** (2 XL / 4 L /
2 M / 14 S), **3 Bugs**, **1 Data fix**, **2 already-Done** awaiting a status flip,
and **2 Duplicates** awaiting a decline.

New since the last run: 4 ERs filed 2026-07-27 (`1bc33dd6`, `d94a418a`,
`50175fce`, `2b78696c`). Two are duplicates of existing open items, one is a
newly-confirmed display bug, one is a small new enhancement.

## Code checks run this run

Confirmations that a requested thing is still **absent** (zero hits):

- `inventory` / `on_hand` / `in_stock` / `stock_qty` across `js/`, `supabase/migrations/`, `supabase/functions/` - none (confirms `14f35f95`)
- `rma` / `return_reason` / `supplier_credit` across `js/`, `supabase/migrations/` - none (confirms `b19975d4`)
- `ro_tasks` / `customer_task` / `task_tracker` - none (confirms `af14b3cb`, `5925fc01`)
- `signing_token` / `esign` / `remote_sign` - none (confirms `0448de62`)
- `backToTop` / `back-to-top` / `window.scrollTo` in `index.html`, `js/`, `css/` - none (confirms `29654af7`, `2b78696c`)
- `favicon` / `rel=icon` / `apple-touch-icon` across every repo `*.html` - none (confirms `d432863a`)
- `sortBy` / `sortSelect` / `sortMode` in `js/render.js`, `index.html` - none (confirms `d1844e46`)
- case-insensitive `cancel` in `js/config.js`, `js/render.js` - none (confirms `972af03c`)
- `LABOR_RATE` / `195` in `js/work-orders.js` - none (confirms `2f752d40`)
- `font-scale` / `fontScale` / `textSize` / `zoom` in `messages.html` - none (confirms `21fc3a9d`)

Confirmations that a delivered thing is still **present** (spot-read, not assumed):
`js/config.js` STATUS_PROGRESS_MAP L151-164, `js/render.js` L35-37 / L61 / L228-235 /
L249 / L324 / L367 / L389-399 / L553 / L656-658 / L674-676 / L721-723,
`js/work-list.js` L195 / L223 / L263 / L304 / L328, `js/photos.js` L66 / L198 / L376 /
L396-397, `js/parts.js` L153-161 / L226 / L307-310, `js/work-orders.js` L293 / L324 /
L360-365 / L461, `js/ro-crud.js` L409 / L486 / L658 / L946 / L1093 / L1119,
`js/duplicates.js` L35-49, `js/auth.js` L69 / L203-204, `js/messaging.js` L202-264,
`messages.html` L303 / L399 / L438 / L497-499 / L808 / L949-955 / L1110-1127,
`customer-checkin.html` L2047 / L2182 / L2313 / L2467 / L2540, `closed-ros.html` L838,
`time-off.html` L368 / L752, `checkin.html` L1516,
`supabase/functions/send-quote-email` L386-387 / L549, `send-dropoff-report` L176,
`textly-webhook` L505, and migrations `keys_power_fields`, `urgent_update_field`,
`key_dates_pickup_date`, `key_dates_reminder_sources`, `prune_cashiered_worklists`,
`work_list_autoadd_rpc`, `shop_tasks`, `scheduled_notifications`,
`service_added_notify_source`, `add_absence_window_to_time_off`,
`unreplied_eod_reminder_s158`, `customer_note_entries_s154`.

---

## Bugs - front of the line (3)

| ER | By | Date | Category | Mapped code | Severity | Verdict |
|---|---|---|---|---|---|---|
| `d94a418a` | Brandon Dillon | 07-27 | General UI/UX | `js/render.js` L553; `js/ro-crud.js` L140, L946; `css/dashboard.css` L1234 | **data-display** | **NEW, CONFIRMED.** The card RO Status box does `roStatusNotes.split('\n').map(escapeHtml).reverse().join('\n')` - it reverses **every line**, so any multi-line note renders bottom-line-first. That is exactly "displays latter portion of messages near the top". Entry boundaries are already lost upstream (`ro-crud.js` L140 joins entries with `\n`). The S159 history modal is correct - `showStatusHistoryModal` (L946) splits on the entry regex `/\n---\n\|\n(?=\[\d{2}\/\d{2}\/\d{2})/` and uses `white-space:pre-wrap`. Fix = reuse that entry-level split in `render.js`. CSS already has `pre-wrap`. Smallest, cleanest fix on the board. |
| `d4d0eeba` | Brandon Dillon | 07-22 | General UI/UX | `index.html` L4079-4094; `js/qr.js` L198-210; `js/work-list.js` L328 | small (navigation) | **Canonical navigation bug, two paths.** Work list: `scrollToROInBoard` only scans already-rendered `[data-ro-index]` cards; a filtered-off RO falls through to a "RO not visible" toast (L4094). The toast now offers a Clear Filters action, but that only re-renders - it never then scrolls to the RO, so the symptom stands. Messages: `handleDeepLink` returns silently when the RO is not in `currentFilteredData` (L207) or has no card (L209-210). Fix both: clear filters, re-render, then scroll and highlight. |
| `9031e101` | Riley Scott | 07-21 | Work List | `js/work-list.js` L328; `index.html` L4079-4094 | small (mobile nav) | Same root cause as `d4d0eeba`, reported from the iPhone work list. Kept as its own Bug row because the submitter differs; fix once, close both. |

`1bc33dd6` (Brandon, 07-27) is the same defect again - folded as a duplicate of
`d4d0eeba` below rather than opened as a fourth bug.

All three sit inside the spec section-5 auto-fixable class (small / data-display,
no auth, no money, no deletes, no migration). `d94a418a` is confined to one line of
`js/render.js` and is the obvious first candidate.

---

## Data corrections - SQL for Roland, nothing executed (2)

| ER | By | Date | Category | Verdict |
|---|---|---|---|---|
| `3f254929` | Sofia Pedroza | 07-16 | General UI/UX | Keep Steve **Huebert**, remove the misspelled **Hubert** RO. Eyeball first: `SELECT id,ro_id,customer_name,status FROM repair_orders WHERE customer_name ILIKE '%ubert%';` then, once the wrong id is confirmed: `UPDATE repair_orders SET deleted_at=now() WHERE id='<wrong-id>';` The generic account-merge feature is tracked as `0e172b2c`. |
| `6938c1af` | Ryan Dillon | 05-06 | General UI/UX | Already applied live - tech names read from `staff.name`, and submitter "Rod Wombles" now spells correctly in this very table. RESIDUAL (not a runtime bug): `supabase/migrations/staff_table.sql` L85-87 still seeds Rod/Zak/Travis as "Wimbles", so a re-seed would reintroduce it. Re-apply if ever needed: `UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |

---

## Needed - 22 open enhancements by theme

### Notifications & Task-Tracking (3)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `5925fc01` | Lynn Titel-Shepard | 04-09 | New Feature Idea | **L** | `supabase/functions/send-manager-report`; `js/work-orders.js` L293; `js/ro-crud.js` L370 | PARTIAL. The daily in-progress digest half ships, and single-event notifies exist (`service_added_notify`, `urgent_update_notify`). Still absent: notify-on-every-RO-update and to-do scraping from status notes. Anchor of the initiative; pairs with `af14b3cb`. |
| `af14b3cb` | Lynn Titel-Shepard | 05-21 | General UI/UX | **L** | `js/shop-tasks.js` | Still absent. `shop-tasks.js` is a non-customer checklist with no `ro_id` link and no reminder column. Needs a per-RO tasks table, add/complete UI and a daily open-task reminder. |
| `5fec3eca` | Sofia Pedroza | 07-15 | General UI/UX | **S** | `supabase/functions/send-quote-email` L386-387, L549, L674, L748 | Still absent. The `raf_copy` branch destructures 14 fields with no `customerAddress`; the address only exists in the `lead_staff_notify` branch. Add `customerAddress` to the `raf_copy` payload and both templates. |

### Parts & Inventory (3)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `14f35f95` | Lynn Titel-Shepard | 04-09 | Parts & Ordering | **XL** | `js/parts.js` (per-RO line items only) | Still absent - zero inventory/stock hits anywhere. New subsystem: inventory table, receive/consume/adjust flows, searchable priced stock view, links from the parts-request path. Largest item on the board. |
| `b19975d4` | Lynn Titel-Shepard | 04-09 | Parts & Ordering | **L** | `js/config.js` L200; `js/parts.js` L226, L343 | Partial infra only: `PART_STATUSES` includes `Returned` and a Return Deadline input exists, but no RMA number, return reason, supplier credit or reverse-direction flow. Equals GH#24 Shop Ops Phase 2. Canonical for declined dup `86bbaf50`. |
| `8d885331` | Lynn Titel-Shepard | 04-09 | Parts & Ordering | **S** | `js/parts.js` L181-226 | Form fields all exist (`pf_supplier`, `pf_partName`, `pf_qty`, `pf_status`, `pf_serviceSilo`, `pf_returnDeadline`); the ask is only to reorder them. **Blocked on detail, not on effort** - needs Lynn's target layout. |

### Messaging UX (4)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `9aab7550` | Brandon Dillon | 07-21 | General UI/UX | **S** | `messages.html` L303, L949, L955 | Partial. A small gold `.unread-dot` renders, but only when `last_direction='inbound'` AND `status='open'`, and there is no row-level highlight. Add a stronger unread treatment (bold row / tinted background) and widen the condition so a closed-then-reopened thread also flags. |
| `21fc3a9d` | Brandon Dillon | 07-21 | General UI/UX | **S** | `messages.html` (fixed rem sizes) | Still absent - no user text-size control anywhere on the page. Add a persisted font-scale control for the conversation list. |
| `f1e6e311` | Brandon Dillon | 07-21 | General UI/UX | **S** | `messages.html` L438, L862, L880, L919, L1889 | Still absent. `convoSearch` is a plain input, reset only programmatically. Add a clear (x) button that empties it and re-runs the filter. |
| `6e9ca4dc` | Brandon Dillon | 07-21 | General UI/UX | **S** | `js/messaging.js` L222, L246; `messages.html` L399 | Valid and still visible on desktop. `refreshSignaturePreview` writes "Added to every send:" into `#msgSigPreview`; `messages.html` hides it only inside the `max-width:760px` mobile media query, so desktop messages.html and the RO-card composer still show it. Remove it or collapse behind a toggle in all viewports; the signature itself keeps sending. |

### RO fields & status (4)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `5fbfa299` | Brandon Dillon | 06-30 | General UI/UX | **M** | `js/ro-crud.js` L658, L692 | Still absent. The only archive path is the cashiered one (`archiveROInSupabase` into `cashiered_child_archives`); no archived/abandoned flag, status or board filter for quoted-but-never-progressed units. Needs a soft-archive flag, board filter and un-archive path. Pairs with `972af03c`. |
| `0e172b2c` | Sofia Pedroza | 07-16 | General UI/UX | **M** | `js/duplicates.js` L35-49, L51 | Partial. The merge/delete engine groups strictly on the `PRVS-XXXX-XXXX` base RO id (L36, L44), so two ROs under different spellings of one customer are never detected. Needs name/phone account dedup plus a verify-which-is-correct merge step. One-off cleanup for the reported case is `3f254929`. |
| `972af03c` | Lynn Titel-Shepard | 07-17 | General UI/UX | **S** | `js/config.js` L151-164; `js/render.js` L741-745 | Still absent - no Canceled status; both maps end at Delivered/Cashed Out. Add a Canceled status plus color/priority maps, exclude it from on-lot and queue counts, keep the record for notes. Pairs with `5fbfa299`. |
| `50175fce` | Lynn Titel-Shepard | 07-27 | General UI/UX | **S** | `js/render.js` L324; `index.html` L6169, L6702; `js/ro-crud.js` L414, L467, L491 | **NEW, confirmed absent.** `urgent_update` is writable only from the New/Edit RO forms; the card banner is read-only and even titles itself "change it via Edit RO". Add an inline edit-in-place urgent-update control at the bottom of the tile, reusing the existing `editField` / notify path so it still fires `urgent_update_notify`. Follow-on to delivered ER `a7d1474e`. |

### Board & Navigation UX (3)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `29654af7` | Lynn Titel-Shepard | 07-06 | General UI/UX | **S** | `index.html`, `js/`, `css/` (no hits) | Still absent - only targeted `scrollIntoView` calls exist. Add a scroll-triggered back-to-top / jump-to-search control, present in the sidebar layout too (`css/sidebar-layout.css`). **Canonical for new duplicate `2b78696c`** - Lynn re-raised the same ask on 07-27, which is a signal it is worth doing now. |
| `d1844e46` | Lynn Titel-Shepard | 07-16 | General UI/UX | **S** | `js/render.js` L31-39 | Still absent. `renderBoard` sorts on the single blended `calculatePriority` score with the shop-ops pin at L35-37; no selectable strict promised-date-then-oldest order. Add the strict sort option while keeping the shop pin. Related to `1aeb3f58`, which delivered only the promised-date chip. |
| `d432863a` | Brandon Dillon | 07-21 | General UI/UX | **S** | every repo `*.html` (no hits) | Still absent - every tab shows the generic browser icon. Add a Patriots-logo favicon plus apple-touch-icon to each page head. |

### Time clock (2)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `e06d747e` | Riley Scott | 04-10 | Time Clock / Check-In | **S** | `js/render.js` L228-235; `index.html`, `checkin.html` | Still absent - no floating/sticky check-in control, and the compact action row offers edit / parts / WO / notify / message / schedule only. **Underspecified**: scope the target page and behavior with the submitter. Pairs with `6bb61db2`. |
| `6bb61db2` | Riley Scott | 07-22 | Time Clock / Check-In | **S** | `js/render.js` L228-235 | Still absent - no compact check-in/out button and no persistent "clocked into" / active `time_log` indicator anywhere. Add a compact check-in/out action plus a login-time active-time_log banner shown across views. Pairs with `e06d747e`. |

### Work Orders (1)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `2f752d40` | Jason Rubin | 07-21 | Work Orders | **S** | `js/work-orders.js` L324, L360, L365, L649-650 | Still absent. `woFormDollar` and `woFormEstHours` are independent manual inputs read separately on save, with no labor-rate arithmetic in the module. Auto-populate `dollar_value` = effective hours x the posted $195/hr rate (task `est_hours` rollup when present, else the WO-level figure), with an editable override that respects the existing `dollarLocked` readonly gate. |

### Messaging & e-sign (1)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `0448de62` | Sofia Pedroza | 06-11 | General UI/UX | **L** | `customer-checkin.html` (kiosk pad); `supabase/functions/send-quote-email` L386 | Still absent. Signing is kiosk-only and the `raf_copy` branch only emails a copy after signing. Needs a tokenized public signing page, a delivery path and signed-form storage. Now pairs naturally with the Textly SMS channel. |

### Auth & cost (1)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `ed1b873e` | Roland Shepard Jr | 06-07 | General UI/UX | **XL** | `js/auth.js` L44-48, L69, L203-204; `js/config.js` GOOGLE_CONFIG | Still Google-based. Sign-in is `google.accounts` One Tap plus `signInWithIdToken`; the check-in kiosk uses the same path. Swapping to Supabase password / magic-link or shared role accounts would drop roughly $500/mo but touches every page sign-in plus the email-keyed RBAC. Roland wants to scope this together. |

---

## Duplicates - fold and decline (8)

| ER | By | Date | Fold into | Verdict |
|---|---|---|---|---|
| `1bc33dd6` | Brandon Dillon | 07-27 | `d4d0eeba` | **NEW.** Same defect Brandon already filed on 07-22: clicking a customer never scrolls to the right board entry. Same verified root cause (`scrollToROInBoard` only matches rendered cards). No separate work item. Recommend `declined`. |
| `2b78696c` | Lynn Titel-Shepard | 07-27 | `29654af7` | **NEW.** Same ask Lynn filed on 07-06: a quick button back to the top after scrolling to a tile. Re-verified still unbuilt. No separate work item. Recommend `declined`. |
| `aa508852` | Riley Scott | 04-09 | `fd6c122d` | Identical text and timestamp (cannot delete jobs / too small on phone / no drag-drop), which is Done. Already declined; keep declined. |
| `86bbaf50` | Lynn Titel-Shepard | 05-13 | `b19975d4` | Gordon window ordered and returned to Vogt is supplier return / warranty tracking. Already declined; keep declined. |
| `67daba15` | Brandon Dillon | 06-09 | `d2561e11` | RO completion / pick-up date for records, Done via `repair_orders.pickup_date`. Already declined; keep declined. |
| `fa1c1d6e` | Sofia Pedroza | 06-09 | `a22739b9` | Returning check-in did not send the email (Sheldon Gilbert); the RAF copy and staff notice are now forced. Already declined; keep declined. |
| `5057c7fc` | Roland Shepard Jr | 06-09 | `91a03260` | Remove a cashed-out RO from the manager work list the day after Delivered, Done via `prune_cashiered_worklists.sql`. Already declined; keep declined. |
| `fc6b29f9` | Sofia Pedroza | 06-17 | `298e0e67` | Retest report of the returning-customer duplicate RO, Done via the check-in duplicate guard. Already declined; keep declined. |

---

## Done - delivered and re-verified (52)

Two of these are still `unreviewed` in the DB and need a status flip: **`1c7f1410`**
and **`9d3806e7`**. The rest are already `done` (or, for the two test entries,
already `declined`).

| ER | By | Date | Category | Mapped code | Verdict |
|---|---|---|---|---|---|
| `1c7f1410` | Andrew Page | 07-16 | New Feature Idea | `js/messaging.js` L202-264; `messages.html` L808 | **Flip to done.** `msgAttachBtn` (L216) plus `input#msgAttach` with `accept="image/*,application/pdf"` and `multiple` (L219), uploads into `_msgAttachments` capped by `MSG_MEDIA_MAX_COUNT` 5 with removable per-file chips; messages.html shares the same composer extras. Photos, videos-as-files and PDFs all attach today. |
| `9d3806e7` | Brandon Dillon | 07-22 | General UI/UX | `messages.html` L497-499, L1070, L1110, L1127; `customer_note_entries_s154.sql` | **Flip to done.** (S154) The details pane has a customer-side Notes section plus an Add-Notes modal writing append-only rows to `customer_note_entries`, keyed by phone. |
| `ac394468` | Roland Shepard Jr | 07-24 | General UI/UX | `index.html` erCategory selects; `js/enhancement.js` L43-53 | Delivered v1.482 (S158): the category select carries Messages / Texting plus Solar, Leads, Closed ROs, Reports, Time Off and Employee Guide, and `pageDefaults` maps every page. **RESIDUAL, stated not hidden:** `erCategory` exists in only 6 HTML files (analytics, checkin, closed-ros, index, solar, worklist-report), so messages / leads / time-off / guide still cannot *raise* an ER. The dropdown ask is delivered; the missing-button gap is worth its own small ER. |
| `d62a26e4` | Roland Shepard Jr | 07-24 | New Feature Idea | `js/messaging.js` L202, L207, L219, L256-264, L300-302 | Delivered v1.482 / S158a: multi-attach up to 5 photos/PDFs with removable chips; the send path chunks into one media per MMS because a carrier total-payload cap silently stripped multi-media sends. |
| `93b00023` | Roland Shepard Jr | 07-24 | New Feature Idea | `textly-webhook` L505; `send-unreplied-reminder`; `unreplied_eod_reminder_s158.sql` | Delivered v1.482 (S158), all three parts: webhook v1.3 reopens a conversation on a non-keyword inbound and logs a `reopened` event; the owner notify fires only on the needs-reply flip; the 4:30pm unread sweep runs on cron. |
| `39b75fab` | Sofia Pedroza | 07-09 | General UI/UX | `js/photos.js` L198, L376 | Delivered v1.472: the Documents / Photos library supports rename, move-to-another-RO and delete, office-staff gated via `canManageFiles`. |
| `cb7742a8` | Lynn Titel-Shepard | 06-29 | General UI/UX | `sync-ro-calendar`; `customer-checkin.html` L2182 | Delivered v1.469 (check-in v1.15): the edge fn owns drop-off / promised / pickup events server-side and posts them to the correct team calendar for every RO including front-desk check-ins, idempotent via `cal_event_ids`. |
| `3f7fb4fc` | Brandon Dillon | 06-29 | General UI/UX | `js/render.js` L249, L653-658, L721-722 | Was a small data-display bug; fixed v1.468: `roType='shop'` ROs are excluded from RVs-in-Queue and the on-lot count, and the shop status dropdown is a static label on card and compact panel. |
| `7f2d0d70` | Lynn Titel-Shepard | 06-26 | General UI/UX | `send-dropoff-report` L176 | Delivered: the automated Monday drop-off email lists this week's scheduled drops plus last week's received units and reads `manager_work_lists` to flag any not yet on the right team's list. Pairs with `cbc70a86`. |
| `80390f36` | Lynn Titel-Shepard | 06-23 | General UI/UX | `js/render.js` L35-37, L61 | Delivered v1.462: Shop Operations tiles pin to the top ahead of the priority sort, and a "shop operations shop" search token makes the tile findable. |
| `88395acc` | Lynn Titel-Shepard | 06-19 | General UI/UX | `add_absence_window_to_time_off.sql`; `time-off.html` L368 | Delivered Time Off v1.5: partial-day absence is an exact clock-out-to-return window, not an hour total. |
| `ee50d11c` | Lynn Titel-Shepard | 06-19 | General UI/UX | `time-off.html` L752, L835 | Was a real gap; delivered: a per-row Edit button drives `editRequest()`, which loads the entry back into the form and flips the button to "Update Record", so day, person and absence type can all be changed. |
| `b06a285f` | Lynn Titel-Shepard | 06-19 | General UI/UX | `js/render.js` L367; `css/dashboard.css` L3438-3440 | Delivered v1.465: a calendar icon beside the customer name opens a key-dates panel listing drop-off, promised and pickup in full on hover or tap. |
| `5c45bb8d` | Sofia Pedroza | 06-19 | General UI/UX | `customer-checkin.html` L2047, L2136, L2467 | Was a duplicate-RO plus missing-email bug (David Flanagan): the Scheduled returning-customer path reuses the existing RO and the RAF copy is forced on every submission. Both halves resolved. |
| `671fa623` | Brandon Dillon | 06-18 | General UI/UX | `js/parts.js` L153-161 | Delivered v1.456 (S117): Ticket Totals include a Freight line summed across parts, rolled into Total Cost. |
| `9b046808` | Brandon Dillon | 06-17 | General UI/UX | `js/config.js` L157; `js/render.js` L674-676, L721-723 | Delivered v1.454: `Scheduled` is its own status and not-yet-arrived Scheduled units are excluded from both days-on-lot stats and the on-lot count. |
| `9b823d25` | Roland Shepard Jr | 06-16 | RO Management | `js/work-orders.js` L293; `service_added_notify_source.sql` | Delivered v1.460: `addServiceToRO` queues a `service_added_notify` row, so when one silo adds a service the owning silo manager is emailed. Matches the roof-plus-water-heater example. |
| `298e0e67` | Sofia Pedroza | 06-16 | General UI/UX | `customer-checkin.html` L18-28, L2047, L2136, L2194 | Was a data-integrity bug; fixed (v1.12 S114 / v1.13 S118): the returning-customer guard reuses an existing scheduled RO instead of inserting a new one. Canonical for declined dup `fc6b29f9`. |
| `74a33621` | Brandon Dillon | 06-16 | General UI/UX | `js/photos.js` L380, L382, L396-397 | Delivered v1.454: the lightbox supports Left/Right arrow navigation and Escape-to-close as well as prev/next buttons. |
| `2c3d5633` | Ryan Dillon | 06-12 | General UI/UX | `js/photos.js` L66 | Was a mobile-only functional bug; fixed v1.453: the upload input is attached to the DOM before clicking, which was the iOS Safari dropped-change-event root cause. |
| `cdd77a8b` | Brandon Dillon | 06-10 | General UI/UX | `js/render.js` L656-658, L721-723 | Delivered v1.453/v1.454: the `nonShop` filter drops Shop Operations ROs from the queue figure and `onLotCount` excludes Not On Lot / Scheduled / Off Lot - Returning, so the tile shows a true on-lot number against total customers. |
| `91a03260` | Roland Shepard Jr | 06-09 | Work List | `prune_cashiered_worklists.sql` | Delivered: an RO leaves manager work lists the day *after* Delivered / Cashed Out - it deliberately stays visible the day of cash-out, gated on the Chicago-local last-update date. Canonical for declined dup `5057c7fc`. |
| `a22739b9` | Sofia Pedroza | 06-08 | General UI/UX | `customer-checkin.html` L2467, L2540 | Was a notify-gate regression; fixed: both sends are forced on every submission, so the check-in notice and the authorization-form copy both arrive. Canonical for declined dup `fa1c1d6e`. |
| `cd084233` | Roland Shepard Jr | 06-07 | Work List | `send-manager-report`; `send-admin-pnl-report`; `worklist-report.html` | Delivered: admin-level daily manager-worklist reporting covers manager list contents, parts and labor activity and overall cost. The manager *scoring* layer described at the end of the request remains a future extension, not this ER. |
| `2b814250` | Roland Shepard Jr | 06-07 | Calendar | `js/render.js` L367, L389-399; `sync-ro-calendar`; `scheduled_notifications` | Delivered: RO date/time values surface as colored key-date chips plus a hover/tap panel, server-side business-calendar integration runs through the edge fn, and advance reminders go to managers/admins. |
| `468aa376` | Lynn Titel-Shepard | 06-02 | General UI/UX | `js/ro-crud.js` L409, L486; `js/render.js` L394 | Delivered: the planned drop-off date input exists on New RO and (restored v1.457) Edit RO, feeding the Drop-off chip and the auto reminder. Same field as `b2aa4bea`. |
| `0b2e128c` | Lynn Titel-Shepard | 06-02 | General UI/UX | `checkin.html` L5 (clockOut) | Delivered v1.460: a tech's clock-out work notes are copied into the RO update timeline as a `ro_status` note row. |
| `34fc03c2` | Brandon Dillon | 05-29 | New Feature Idea | `keys_power_fields.sql`; `js/ro-crud.js` L411, L488, L1100 | Delivered v1.458: the Keys selector is on New and Edit RO (keys left / no keys / keypad plus code box and keep-plugged-in) with a keys chip on each card. Covers `b87eb2fb`. |
| `cd663d8f` | Bobby Thatcher | 05-28 | General UI/UX | `js/ro-crud.js` L1093; `js/parts.js` L307, L310 | Was a cosmetic bug; fixed v1.447: currency inputs show 2 decimals on edit-populate and in the parts modal. No data loss occurred. |
| `3f415c88` | Lynn Titel-Shepard | 05-26 | General UI/UX | `index.html` snRepeat; `process-scheduled-notifications` | Delivered v1.463: Repeat none/daily/weekly plus a repeat-until date expands into one individually cancellable row per occurrence (cap 60). |
| `cbc70a86` | Lynn Titel-Shepard | 05-21 | General UI/UX | `customer-checkin.html` L2313; `work_list_autoadd_rpc.sql` | Delivered (S128): check-in calls `add_ro_to_silo_work_lists()` (SECURITY DEFINER), inserting `manager_work_lists` rows for each involved silo lead with an sr-manager fallback. |
| `1aeb3f58` | Lynn Titel-Shepard | 05-21 | General UI/UX | `js/render.js` L33, L395 | Delivered v1.457: the promised date is a large bold chip, red when overdue, and promise date plus days-on-lot feed the blended priority sort. The strict user-selectable sort is tracked separately as open ER `d1844e46`. |
| `a8e90a7d` | Brandon Dillon | 05-20 | General UI/UX | `js/parts.js` L153-161 | Delivered v1.456 (S117): the Ticket Totals bar sums parts, freight and total cost. Same build as `671fa623`. |
| `dac9fdda` | Brandon Dillon | 05-06 | General UI/UX | `js/config.js` L154; `js/render.js` L253, L451, L721-723 | Delivered v1.460: `Off Lot - Returning` separates units temporarily taken by the customer from `Not On Lot` units never on the lot, and is excluded from the on-lot count. |
| `d2561e11` | Brandon Dillon | 05-06 | New Feature Idea | `key_dates_pickup_date.sql`; `js/render.js` L367, L396 | Delivered v1.457: `repair_orders.pickup_date` surfaces as the green Pickup chip and in the key-dates panel. Canonical for declined dup `67daba15`. |
| `e27311bf` | Ryan Dillon | 05-04 | Work Orders | `js/work-orders.js` L385, L461, L583 | Delivered v1.464: every WO task row has a grip handle and drag-to-reorder; DOM order drives `sort_order`, so a template is edited by loading it, reordering, then Overwrite. |
| `13e1cd87` | Tipton Scott | 04-30 | Time Clock / Check-In | `checkin.html` L36, L441, L1516 | Was a real gap; delivered v1.447 (checkin v1.34): a "Return to Dashboard" button renders after clock-out, so the tech no longer opens a new tab to clock into the next job. |
| `dde90d2b` | Roland Shepard Jr | 04-27 | RO Management | `customer-checkin.html` L2467, L2540 | Delivered: the photo is optional and both the RAF copy and the staff notice are forced on every submission. |
| `a7d1474e` | Lynn Titel-Shepard | 04-23 | RO Management | `urgent_update_field.sql`; `js/render.js` L214, L324; `css/dashboard.css` L3446; `js/ro-crud.js` L370 | Delivered v1.466: an always-visible red URGENT banner on the card (compact marker too), and setting or changing it notifies the silo manager(s) and admins. Lynn's follow-up to move the *input* onto the tile is open ER `50175fce`. |
| `a5ff3e2d` | Lynn Titel-Shepard | 04-21 | RO Management | `js/ro-crud.js` L1119; `js/render.js` L122, L196, L373 | Delivered v1.465: the "Warranty + Repair" RO type exists end to end and stays billable, so the repair half bills normally through work orders. |
| `b87eb2fb` | Lynn Titel-Shepard | 04-14 | RO Management | `keys_power_fields.sql`; `js/ro-crud.js` L411, L488 | Delivered v1.458: `key_status`, `keypad_code` and `keep_plugged_in` persist on create and edit with a keys chip on the card. Same build as `34fc03c2`. |
| `3dfc0acb` | Lynn Titel-Shepard | 04-14 | Work Orders | `js/config.js` L158; `js/render.js` L743 | Delivered v1.410: `Ready to Work` is a live RO status and appears in every status dropdown. |
| `1fe68261` | Rod Wombles | 04-13 | Work Orders | `js/shop-tasks.js` L21, L41, L153, L180; `shop_tasks.sql` | Delivered v1.467 (S128): Shop Tasks track internal work not tied to a customer unit, separate from ROs and the board. Matches the poly-barrel / roof-bay examples. |
| `19dc2016` | Roland Shepard Jr | 04-11 | Time Clock / Check-In | - | Test entry ("Testing ER form / dictation"), not a real request. No code action. Already declined; keep declined. |
| `98bd7498` | Brandon Dillon | 04-09 | RO Management | `js/work-list.js` L83, L223, L263 | Delivered: per-silo Work List tabs give a separate list per job category. |
| `fd6c122d` | Riley Scott | 04-09 | Work List | `js/work-list.js` L195, L210, L304, L356, L383 | Delivered v1.460: delete plus drag-reorder on mouse and touch, order persisted, mobile sizing fixed. Canonical for declined dup `aa508852`. |
| `17492066` | Lynn Titel-Shepard | 04-09 | Notifications / Email | `scheduled_notifications.sql`; `process-scheduled-notifications`; `index.html` snRepeat | Delivered: the per-RO Schedule Notification bell sets a future date/time, message and recipients, with daily/weekly repeat until a chosen date. Covers the advance-warning ask. |
| `18611eee` | Lynn Titel-Shepard | 04-09 | Bug Report | `js/work-list.js` L195, L223, L263 | Was a real bug; fixed: added members are viewable, reorderable and removable via `renderWorkList`, `removeFromWorkList` and the silo tabs. No defect remains. |
| `b2aa4bea` | Lynn Titel-Shepard | 04-09 | New Feature Idea | `js/ro-crud.js` L409, L486, L508; `js/render.js` L394; `key_dates_reminder_sources.sql` | Delivered: `planned_dropoff_date` is written on create and edit and drives both the blue Drop chip and the day-before manager reminder. |
| `75124b03` | Ryan Dillon | 04-09 | RO Management | `js/ro-crud.js` L1119; `js/render.js` L122, L196, L373 | Delivered v1.402: Warranty is a first-class RO type on new-RO creation plus the warranty board filter/badge. |
| `79b2c805` | Sofia Pedroza | 04-09 | New Feature Idea | `closed-ros.html` L725, L813, L838; `js/photos.js` L207, L253 | Delivered: `reactivateRO` restores a cashed-out RO from `cashiered_child_archives` so it can be edited again, and `uploadDocument` adds documents to any RO. |
| `33750cb3` | Roland Shepard Jr | 04-09 | General UI/UX | - | Test entry ("Testing out the Make-A-Wish function"), not a real request. No code action. Already declined; keep declined. |

---

## Carried over - not triaged this run (1)

| ER | By | Date | Category | Status | Note |
|---|---|---|---|---|---|
| `f73a1e5a` | Roland Shepard Jr | 04-13 | General UI/UX | declined | Not in `needs_triage_ids`, so untouched by this run and it carries no `triage_bucket`. Content is a test entry ("Testing dictation 123 testing enhancement request dictation"); already declined in the DB. Listed for completeness only. |

---

## Recommended action today

1. **Fix `d94a418a` first.** It is the cleanest bug on the board: one line in
   `js/render.js` (L553), the correct entry-level split already exists three files
   away in `js/ro-crud.js` L946, no DB change, no auth/money/delete path. Squarely
   inside the spec section-5 auto-fixable class.
2. **Then fix the navigation bug once, close three ERs.** `d4d0eeba` is canonical;
   `9031e101` and `1bc33dd6` are the same root cause reported from the work list and
   from Messages. Both paths need the same treatment - clear filters, re-render,
   *then* scroll and highlight. Note the existing Clear Filters toast action
   (`index.html` L4094) looks like a fix but does not scroll, which is why users
   keep re-filing this.
3. **Four status flips for Roland** (no code): `1c7f1410` -> `done`,
   `9d3806e7` -> `done`, `1bc33dd6` -> `declined`, `2b78696c` -> `declined`. That
   takes `unreviewed` from 30 to 26.
4. **One data SQL to run** after eyeballing the rows: the Steve Huebert / Hubert
   cleanup (`3f254929`). SQL is in the Data section above; nothing was executed.
5. **Cheap batch of S-size wins.** 14 of the 22 open enhancements are S, and four of
   them (`9aab7550`, `21fc3a9d`, `f1e6e311`, `6e9ca4dc`) are all `messages.html`
   conversation-list polish from Brandon - one messaging-UX pass could clear the
   whole cluster. `d432863a` (favicon) and `2b78696c`/`29654af7` (back-to-top) are
   similarly quick and now double-reported.
6. **Unblock the one stalled S** by asking the submitters for specifics: Lynn's parts
   field order (`8d885331`) and the tech check-in float (`e06d747e` / `6bb61db2`).
   Small once specified, unbuildable until then.
7. **Plan, do not rush, the XLs:** the auth swap (`ed1b873e`) and parts inventory
   (`14f35f95`) each deserve their own scoping session.
8. **One doc follow-up:** `ac394468` is genuinely delivered for the dropdown, but the
   Make-A-Wish *button* still only exists on 6 of 13 pages. Worth raising as its own
   small ER rather than leaving it buried in a Done verdict.
