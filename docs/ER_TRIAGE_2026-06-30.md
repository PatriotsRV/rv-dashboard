# PRVS Enhancement-Request Triage -- 2026-06-30 (headless nightly runner)

Incremental re-pass over the `enhancement_requests` board against the current
modular codebase (index.html **v1.467**, post-Session-128). 61 of 62 ERs were
flagged for re-triage; the 1 unchanged row (`f73a1e5a`, a test entry) is carried
over. Verdicts verified by grepping/reading the live code, not assumed. No code
was changed this run (triage only); no DB status was flipped (Roland flips
status himself).

## What changed since the 2026-06-28 report

- **Session 128 shipped three ERs (v1.467)** that were `needed/M` last pass and
  are now **done**, confirmed present in code:
  - `cbc70a86` -- returning check-in auto-adds to each team lead's work list
    (manager_work_lists insert in `customer-checkin.html`, confirmed).
  - `7f2d0d70` -- Monday drop-off report
    (`supabase/functions/send-dropoff-report/`, confirmed present).
  - `1fe68261` -- non-customer shop task list (`js/shop-tasks.js`, confirmed present).
- **Two new ERs** arrived and were triaged fresh against code:
  - `3f7fb4fc` (Brandon) -> **bug** (small / data-display): shop ROs still get a
    status dropdown and are counted in the on-lot / RVs totals.
  - `cb7742a8` (Lynn) -> **needed/M**: a new check-in's scheduled date does not
    post to the team calendar.

## Board snapshot (62 total)

| Status | Count |
|---|---|
| done | 43 |
| declined | 9 |
| unreviewed | 10 |
| **Total** | **62** |

Open (`unreviewed`) = **10**: 9 valid Needed enhancements + 1 newly found bug.

## Triage bucket distribution (61 re-triaged this run)

| Bucket | Count |
|---|---|
| done (delivered / not a real request) | 44 |
| needed (open enhancement) | 9 |
| duplicate | 6 |
| data | 1 |
| bug | 1 |

## Code checks run this session

- `manager_work_lists` insert in `customer-checkin.html` -- **present** (confirms cbc70a86 shipped).
- `js/shop-tasks.js` -- **present** (confirms 1fe68261 shipped).
- `supabase/functions/send-dropoff-report/index.ts` -- **present** (confirms 7f2d0d70 shipped).
- Shop-RO status dropdown / count exclusion in `js/render.js` -- **absent**: `statusDropdown`
  block (~L425) is not gated on `roType`, unlike the urgency selector (L305); `onLotCount`
  (L686) and `totalRVs` (L691) exclude only Not-On-Lot/Scheduled/Off-Lot, not shop (drives 3f7fb4fc).
- Calendar hook in check-in pages -- **absent**: calendar event creation lives only in
  `js/scheduling.js` (manual admin gapi flow); `customer-checkin.html` has 0 calendar refs (drives cb7742a8).
- Per-customer task-tracker subsystem -- **absent** (grep: no ro_tasks/customer_tasks; shop_tasks is shop-level). Confirms af14b3cb, 5925fc01 scraping half.
- Parts inventory / stock state in `js/parts.js` -- **absent** (confirms 14f35f95).
- Parts supplier-return / warranty workflow -- **absent** (confirms b19975d4).

---

## Open ERs by theme (the 10 actionable items)

### Notifications & Task-Tracking

| ER | By | Bucket | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `5925fc01` | Lynn | needed | L | Notify the right team on every RO update; daily list of all in-progress jobs with to-dos scraped from notes | PARTIAL: daily-digest half largely served by the Manager Daily Report (send-manager-report / send-admin-pnl-report). Open: per-RO-update notification + auto-scraping to-dos from status notes. Anchor of this theme. |
| `af14b3cb` | Lynn | needed | L | Per-customer task tracker + daily reminder; tasks persist until marked complete | No per-RO task-tracker (grep: no ro_tasks/customer_tasks; shop_tasks is shop-level). Needs a per-RO tasks table + add/complete UI + daily open-task reminder. |

### Parts & Inventory

| ER | By | Bucket | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `8d885331` | Lynn | needed | S | Rework the parts-entry screen field order | Underspecified -- no target layout given. Quick once Lynn supplies the field order / a mockup. BLOCKED on detail, not effort. |
| `b19975d4` | Lynn | needed | L | Part returned-to-supplier / warrantied-by-supplier tracking | No supplier-return/warranty workflow in `js/parts.js`. = GH#24 Shop Ops Phase 2 Parts Returns. Canonical for declined dup `86bbaf50`. |
| `14f35f95` | Lynn | needed | XL | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | Nothing in `js/parts.js` handles stock/inventory. New subsystem: inventory table + receive/consume/adjust + searchable stock + parts-request links. Largest item on the board. |

### Scheduling/Dates

| ER | By | Bucket | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `cb7742a8` | Lynn | needed | M | New check-in's scheduled service date was not added to the team (repair) calendar | NEW. Calendar event creation lives only in `js/scheduling.js` (manual admin gapi flow); `customer-checkin.html` has no calendar hook (0 refs). Needs check-in scheduled-date -> team-calendar wiring (server-side/service-account write or a dashboard scheduling prompt). Never built. |

### Messaging & e-sign

| ER | By | Bucket | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `0448de62` | Sofia | needed | L | Send an authorization form remotely for e-signature | No remote-signing flow (RAF is kiosk-only today). Needs a tokenized public signing page + delivery + signed-form storage. Pairs with Sendblue Phase 3. |

### Auth & cost

| ER | By | Bucket | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `ed1b873e` | Roland | needed | XL | Replace Google-email auth (~$500/mo) with DB-specific auth | Auth today is Google id-token + email-keyed RBAC (`js/auth.js`). Security-critical swap to Supabase password/magic-link or shared role accounts; touches every page sign-in + RBAC. Roland wants to scope together. |

### Time clock

| ER | By | Bucket | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `e06d747e` | solar | needed | S | Tech check-in "float" for ease of access | Underspecified -- likely a floating/sticky check-in button, but page + behavior unclear (`checkin.html` is QR/roId-driven). Needs scoping with the submitter. |

### Bug (newly found this run)

| ER | By | Bucket | Severity | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `3f7fb4fc` | Brandon | bug | small / data-display | Remove shop ROs' status dropdown; stop counting them in RVs-in-queue / on-lot | Valid defect. `js/render.js`: `statusDropdown` block (~L425) is not gated on `roType` (urgency selector at L305 already uses the `roType !== 'shop'` pattern); `onLotCount` (L686) + `totalRVs` (L691) exclude only Not-On-Lot/Scheduled/Off-Lot, so shop ROs inflate both. Fix = gate the status dropdown on `roType !== 'shop'` and exclude shop ROs from the counts. Within the auto-fixable class (single module, <~40 lines). |

---

## Closed / not-actionable ERs (carried verdicts)

### Delivered (bucket = done)

| ER | By | Request | Evidence |
|---|---|---|---|
| `79b2c805` | Sofia | Edit/add docs on cashed-out customers | closed-ros.html reactivation restores full RO detail. |
| `75124b03` | Ryan | Warranty category on new RO | Warranty RO type v1.402 (ro_type branch + filter). |
| `b2aa4bea` | Lynn | Scheduled future drop date | planned_dropoff_date across js + entry dash. See 468aa376. |
| `18611eee` | Lynn | Work list: can't view/act on added people | Fixed Phase 14 (work-list.js renderWorkList/removeFromWorkList). |
| `17492066` | Lynn | Future/advance notifications | process-scheduled-notifications edge fn. |
| `fd6c122d` | Riley | Work list: delete/size/drag-drop | Delete works; mobile + reorder addressed. Canonical for dup aa508852. |
| `98bd7498` | Brandon | Different work lists per category | Per-silo Work List tabs (work-list.js). |
| `1fe68261` | Rod | Non-customer shop tasks | S128 v1.467: shop_tasks table + js/shop-tasks.js (confirmed). |
| `3dfc0acb` | Lynn | Status "Ready to Work" | v1.410 STATUS_PROGRESS_MAP slot 50. |
| `b87eb2fb` | Lynn | Keep-plugged-in + keys/keypad | keep_plugged + keypad fields. Canonical for 34fc03c2. |
| `a5ff3e2d` | Lynn | RO type "Warranty + Repair" | warranty_repair RO type (guide v1.3). |
| `a7d1474e` | Lynn | Always-visible urgent updates + notify | render.js urgentUpdate banner (L304). |
| `dde90d2b` | Roland | Streamline check-in + force email | customer-checkin: photo optional + forced RAF/lead email. |
| `13e1cd87` | Tipton | Return-to-dashboard after clock-out | checkin.html v1.34 Return-to-Dashboard button. |
| `e27311bf` | Ryan | Edit/reorder WO templates | js/work-orders.js template edit/drag. |
| `d2561e11` | Brandon | Completion/pickup date reference | Shipped. Canonical for dup 67daba15. |
| `dac9fdda` | Brandon | On-lot status for temporarily-away units | "Off Lot - Returning" (render.js onLotCount L686). Related 9b046808. |
| `a8e90a7d` | Brandon | Parts + freight total in Manage Parts | freight + partsTotal in js/parts.js. Canonical for 671fa623. |
| `1aeb3f58` | Lynn | Promised date highlighted + sorted | promised_date + daysOnLot. |
| `cbc70a86` | Lynn | Auto-add to work list on returning check-in | S128: manager_work_lists insert in customer-checkin.html (confirmed). Enables 7f2d0d70. |
| `3f415c88` | Lynn | Multi-day / weekly-until-complete reminders | weekly recurrence in scheduled-notification path. |
| `cd663d8f` | Bobby | Currency dropping .00 | Fixed v1.447 (ro-crud.js editDollarValue toFixed(2); parts.js MONEY_FIELDS). |
| `34fc03c2` | Brandon | Confirm keys left with unit | keys/keypad fields. Overlaps b87eb2fb. |
| `0b2e128c` | Lynn | Clock-in notes -> RO updates | Shipped. |
| `468aa376` | Lynn | Scheduled drop date in entry dash | planned_dropoff across js + entry dash. Same intent as b2aa4bea. |
| `2b814250` | Roland | RO key dates -> calendar integration | Key dates on tile + key-date hover + js/scheduling.js + scheduled notifications. (Auto-calendar on check-in remains open as cb7742a8.) |
| `cd084233` | Roland | Admin-level manager-focused daily report | send-admin-pnl-report edge fn. |
| `a22739b9` | Sofia | Check-in email + RAF copy regression | customer-checkin forces RAF/lead email. Canonical for dups fa1c1d6e/5c45bb8d. |
| `91a03260` | Roland | Remove cashiered RO from work list next day | cashiered_child_archives.sql / cron_scrub_deleted_ros. Canonical for dup 5057c7fc. |
| `cdd77a8b` | Brandon | RVs-on-lot vs total customers | render.js onLotCount L686 + "on lot" sub-line. |
| `2c3d5633` | Ryan | Mobile photo upload silently fails | Fixed in js/photos.js uploadPhoto. |
| `74a33621` | Brandon | Arrow-key photo navigation | ArrowLeft/ArrowRight handlers in js/photos.js. |
| `298e0e67` | Sofia | Returning check-in duplicates scheduled RO | Returning check-in reconciles instead of duplicating. Canonical for dup fc6b29f9. |
| `9b823d25` | Roland | Notify silo manager on added service | Service-add notification in js/work-orders.js + js/app.js. |
| `9b046808` | Brandon | "Scheduled to arrive" status | render.js onLotCount L686 excludes "Scheduled". Related dac9fdda. |
| `671fa623` | Brandon | Freight in Manage-Parts ticket totals | freight in partsTotal. Extension of a8e90a7d. |
| `5c45bb8d` | Sofia | Returning check-in: no email + duplicate | Forced check-in email + duplicate reconciliation. Combines a22739b9 + 298e0e67. |
| `b06a285f` | Lynn | Key dates near customer name w/ hover | render.js key-date hover panel (L340-348). |
| `ee50d11c` | Lynn | Edit time-off entries | time-off v1.5 editable entries. |
| `88395acc` | Lynn | Time-off exact from/to time | time-off v1.5 from/to + whole/half-day. |
| `80390f36` | Lynn | Pin Shop Operations tile + searchable | render.js shop sort (L35-37) + search blob (L61). |
| `7f2d0d70` | Lynn | Monday drop-off report | S128: supabase/functions/send-dropoff-report (confirmed). Pairs with cbc70a86. |
| `33750cb3` | Roland | (test entry) | "Testing Make-A-Wish"; not a real request. Declined in DB. |
| `19dc2016` | Roland | (test entry) | "Testing ER form / dictation"; not a real request. Declined in DB. |

### Data correction (bucket = data)

| ER | By | Request | SQL (for Roland; not run) |
|---|---|---|---|
| `6938c1af` | Ryan | Staff last name Wimbles -> Wombles (Zak, Rod, Travis) | `UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles') WHERE name ILIKE '%Wimbles%';` Appears already applied (status=done). |

### Duplicates (bucket = duplicate; fold into canonical)

| ER | By | Folds into |
|---|---|---|
| `aa508852` | Riley | `fd6c122d` (work-list delete/size/drag) |
| `86bbaf50` | Lynn | `b19975d4` (parts return-to-supplier) |
| `67daba15` | Brandon | `d2561e11` (completion/pickup date) |
| `fa1c1d6e` | Sofia | `a22739b9` (check-in no-email) |
| `5057c7fc` | Roland | `91a03260` (remove cashiered RO from work list) |
| `fc6b29f9` | Sofia | `298e0e67` (returning check-in duplicate-RO) |

### Carried over (not re-triaged this run)

| ER | By | Note |
|---|---|---|
| `f73a1e5a` | Roland | Test entry ("Testing dictation 123..."); declined in DB. Unchanged since S125, not in this run's re-triage set. |

---

## Recommended action today

1. **Confirm the three S128 ships in the DB.** `cbc70a86`, `7f2d0d70`, and
   `1fe68261` are verified delivered in code and already `done` in the input;
   no further status action needed -- just confirm.
2. **Fix the one bug (`3f7fb4fc`).** It is in the auto-fixable class: gate the
   shop-RO status dropdown on `roType !== 'shop'` (copy the L305 urgency pattern)
   and exclude `roType === 'shop'` from `onLotCount` (L686) and `totalRVs`
   (L691) in `js/render.js`. Single module, small diff, deterministic.
3. **Scope the new `cb7742a8` (M)** with Lynn: decide whether the check-in's
   scheduled date should auto-create a Google Calendar event (needs a
   server-side / service-account path, since the public check-in page cannot run
   the admin gapi flow in `js/scheduling.js`) or simply surface for one-tap
   dashboard scheduling.
4. **Unblock the two S's** by asking submitters for specifics: Lynn's parts
   field order (`8d885331`) and solar's check-in float (`e06d747e`).
5. **Plan, don't rush, the two XL's:** auth swap (`ed1b873e`) and parts
   inventory (`14f35f95`) each deserve their own scoping session.
