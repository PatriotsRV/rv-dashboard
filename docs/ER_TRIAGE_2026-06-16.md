# Enhancement Request Triage -- 2026-06-16 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-16T12:14Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.451** (checkin.html, customer-checkin.html v1.11, send-manager-report v2.0-P1-preview).
**Total ERs:** 48 (incl. 3 test entries + 4 exact/near duplicates -> **41 real requests**).
**Triaged this run:** 47 ids in `needs_triage_ids` (every row except the n15 test entry, carried over unchanged).

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `closed-ros.html`, `worklist-report.html`,
`supabase/functions/*`) by grepping the source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

No ER `description`, `submitted_by_name`, or `admin_notes` value contained
prompt-injection / instruction-like content this run; all are genuine requests.

---

## Headline

One bug is open and front of the line:

- **n47** (Ryan) "phone photo upload acts accepted then switches screens with no
  upload" -> verified still present. `photos.js` `uploadPhoto` (L41) builds a
  **detached dynamic `<input>`** (L51) with an `async onchange` closure (L57);
  on iOS Safari the camera round-trip reloads the page and discards the detached
  input, so `onchange` never fires and there is no error path. Mobile-only,
  small-medium. Fix: persistent in-DOM input + verify the Storage write. Needs a
  device repro; prefer fixing over closing.

Since the 2026-06-07 report, the prior Sofia check-in email defect (**n40**) was
fixed in **v1.10/v1.11** (S97) and is confirmed in current code -- it now reads
**DONE**. Its two follow-on reports (n41, n42) and Roland's duplicate worklist
ask (n44) fold into their canonicals.

The only non-enhancement item besides the bug is **n25** -- a data-only
staff-name correction (SQL below; Roland runs it).

The strategic asks from Roland remain open: **n37** (RO calendar integration),
**n38** (database-auth to replace costly Google email logins), and **n39**
(admin manager-worklist report) -- n39's **P1 has shipped** as
`send-manager-report` v2.0-P1-preview (admin-preview only).

---

## BUG bucket (front of the line)

| # | Submitter | Request | Evidence (current code) | Severity |
|---|---|---|---|---|
| n47 | Ryan | Phone: photo upload acts accepted, switches screens, nothing uploads | `photos.js` `uploadPhoto` L41: detached dynamic `<input>` L51 + `async onchange` L57; iOS Safari reloads on camera return and discards it, so `onchange` never fires (no error toast). | functional, mobile-only, small-medium |

Fix plan: replace the detached input with a persistent in-DOM `<input type=file>`
and verify the Supabase Storage write succeeds before switching screens. Requires
a phone repro before close.

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation restores full RO detail (parts/time_logs/photos). | done (already) |
| n2 | Ryan | Warranty category on new RO | Warranty RO type v1.402 (`ro-crud.js` `roType=='warranty'` L855; `render.js` warranty filter L112). | done (already) |
| n5 | Lynn | Work list opened but couldn't view/do anything | Fixed Phase 14: `work-list.js` `renderWorkList` L269 + `removeFromWorkList` L195 + `_setWorkListSilo` L263. | done (already) |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs` L223 / `_setWorkListSilo` L263). | done (already) |
| n17 | Lynn | Add "Ready to Work" RO status | Shipped v1.410 (`config.js` `STATUS_PROGRESS_MAP` L131 slot 50). | done (already) |
| n21 | Roland | Fix customer check-in, email out, remove photo, streamline | `customer-checkin.html` v1.11: photo optional + signed RAF auto-emails every drop-off; staff-notify gap closed v1.10/v1.11 (n40). | done (already) |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | FIXED v1.447 (`checkin.html` Return-to-Dashboard button L1482, n22 marker). | done (already) |
| n33 | Bobby | Amounts drop the period and the zeros (425.00 -> 425) | FIXED v1.447 (`ro-crud.js` `editDollarValue` L843 `.toFixed(2)` + `parts.js` `MONEY_FIELDS` L288). | done (already) |
| n40 | Sofia | Check-in stopped sending the staff email / RAF copy | FIXED v1.10/v1.11 (S97): `shouldNotifyStaff` L2210 fires on every drop-off + signed RAF always sends; v1.11 fixed `getSignatureData` typo L2194. | done (already) |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod and Travis last name should be Wombles, not Wimbles, in tech assignment" | Data, not code. `staff.name` holds the value; not hardcoded. One SQL UPDATE (below). |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

Historical ROs that stored the tech name as free text are a separate optional backfill.

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete jobs / too small on phone / drag-and-drop" | Exact duplicate of **n10** (identical text + timestamp); already declined. Fold into n10. |
| n41 | Brandon | Completion / pickup date to record when units leave the lot | Duplicate of **n24** (same submitter); no completion/pickup field exists (grep). Fold into n24. |
| n42 | Sofia | Returning customer check-in sent no email (Sheldon Gilbert) | Duplicate of **n40**; same no-email defect, now fixed v1.10/v1.11 (fires for returning customers too). Fold into n40. |
| n44 | Roland | Auto-remove cashed-out RO from manager worklist | Exact duplicate of **n43** (identical text, timestamps 1s apart). Fold into n43. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Scheduling & key dates  *(n4, n24, n29, n36, n37)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n4 | Lynn | Scheduled future drop-off date + email reminder to manager 1 day before | M |
| n36 | Lynn | Scheduled drop-date field in entry dashboard | S -- duplicate intent of n4 |
| n24 | Brandon | Completion / pickup date on the customer's order for reference | M -- canonical for n41 |
| n29 | Lynn | Promised date large + highlighted on tile; sort by promise date then days-on-lot (`promised_date` exists L244/L304; tile emphasis + sort do not) | M |
| n37 | Roland | Surface RO date/time (pickup/dropoff/reminders) as a prominent colored RO button + integrate into business calendar(s) with advance notifications to managers/admins | L |

-> **"Key Dates + Calendar" initiative.** n37 is the umbrella ask that ties the
drop-off/promised/completion dates (n4/n24/n29/n36) to a calendar surface and the
notifications cluster below.

### Theme 2 -- Notifications, reminders & task-tracking  *(n3, n8, n20, n30, n32, n35, n46)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time (messaging path now Sendblue POC; Twilio torn down S106) | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + font/color | L (urgency partly exists) |
| n35 | Lynn | Transfer clock-in job notes -> RO updates (`time-tracking.js` logs `workNotes` L53; no auto-copy) | M |
| n46 | Sofia | Send the RV authorization form (RAF) remotely for e-signature (today RAF signs on-site only: `signatureCanvas` L884 + `getSignatureData` L1754) | L |

-> **Largest opportunity.** A "Task Tracker + Reminders" subsystem; overlaps the
deferred SMS work (now Sendblue POC). n46 adds a public token-signing + email-link
path to the check-in/RAF flow -- design alongside the notify cluster.

### Theme 3 -- Parts  *(n6, n7, n9, n27, n28)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n28 | Brandon | Parts + freight TOTAL in Manage Parts (per-part Freight Charge `pf_coreCharge` L217 exists; summed total row not built) | S -- clean quick win |
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier | L -- canonical for n27 |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

### Theme 4 -- Work List usability  *(n10, n31, n43)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n10 | Riley | Mobile: too small on phone; touch-drag reorder (delete + HTML5 drag-drop reorder `row.draggable` L304 / `_saveWorkListOrder` L210 now shipped; no touchmove handler) | S -- mobile polish |
| n43 | Roland | Auto-remove an RO from a manager's worklist the day after it is marked Cashiered Out/Delivered (only manual `removeFromWorkList` L195 today) | M -- canonical for n44 |
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |

### Theme 5 -- RO fields & status nuance  *(n18, n34, n26, n19, n16, n45)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n18 | Lynn | "Keep plugged in" flag; keys / no-keys / keypad code | M |
| n34 | Brandon | Confirm if customer leaves keys with the unit (RO / sign-in / auth form) | S -- overlaps n18 |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning vs never-on-lot | M |
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs existing Hybrid) |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |
| n45 | Brandon | Split dashboard "Total RVs" vs "RVs on Lot" (render.js `onLotROs` L594 exists; headline `statTotal` L611 conflates) | S -- dashboard-stat add |

### Theme 6 -- Work Orders / templates  *(n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will (`work-orders.js` has `loadWOTemplate` L399 / `saveWOTemplate` L468; no editing/reorder) | M |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague -- overlaps n22) |

### Theme 7 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

### Theme 8 -- Reporting & analytics  *(n39)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n39 | Roland | Admin-level daily Manager-worklist report (parts/labor/costs, upcoming dates, ROs that should be in a worklist but are not) -> manager scoring + profit-margin visibility | L -- **P1 shipped** (`send-manager-report` v2.0-P1-preview, admin-preview); remaining: Fire Watch P2, AI narrative P3, per-manager sends P4, scoring/profit (gated) |

---

## Recommended action today

1. **Fix the open bug n47** (phone photo upload) -- front of the line, but it
   needs a device repro first; the fix is a persistent in-DOM file input +
   Storage-write verification. Mobile-only, small-medium.
2. **Close the resolved ER n40** (`status=done`) -- already done in DB; confirm.
   n0/n14/n15/n1/n2/n5/n12/n17/n21/n22/n33 are already closed.
3. **Decline the duplicates** n41, n42, n44 (`declined`; fold into n24/n40/n43).
   n11 already declined.
4. **Run the n25 staff-name SQL** (Roland; data only).

Standout near-term enhancement wins remain **n28 (parts+freight total, S)**,
**n45 (split Total RVs vs on-lot stat, S)**, and a coordinated **Key Dates +
Calendar** pass (Theme 1, anchored by Roland's n37). The strategic asks --
**n38 (auth cost reduction)** and **n39 (manager reporting/scoring, P1 live)** --
continue under design with Roland.
