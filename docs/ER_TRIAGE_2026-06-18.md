# Enhancement Request Triage -- 2026-06-18 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-18T11:10Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.453** (checkin.html v1.37, customer-checkin.html v1.12).
**Total ERs:** 53 (incl. 3 test entries + 5 exact/near duplicates -> **45 real requests**).
**Triaged this run:** 52 ids in `needs_triage_ids` (every row except the n15 test entry, carried over unchanged).

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `closed-ros.html`, `supabase/functions/*`) by grepping
the source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

---

## Headline

**The bug bucket is empty again -- the two open bugs from the last report shipped
in Session 114 and are confirmed in the current tree:**

- **n49** (Sofia) "returning-customer check-in against a future scheduled-drop RO
  duplicates the RO" -> FIXED **v1.12 (S114)**: `customer-checkin.html` now has a
  returning-customer dedupe guard (L2029-2082) that matches an existing
  `Scheduled` / future-drop RO and UPDATES it (merging the new check-in details +
  logging a conversion note) instead of inserting a second RO. **Now DONE.**
- **n47** (Ryan) "can't upload pictures from phone -- accepts then switches
  screens with no upload" -> FIXED **v1.453 (S114)**: `photos.js` `uploadPhoto`
  now attaches the file `<input>` to the DOM off-screen (L57-66, tagged
  `ER 2c3d5633`) so iOS Safari no longer drops the `change` event after the
  camera/picker returns. **Now DONE** (device-confirm recommended).

**One NEEDED item also shipped this cycle and moves to DONE:**

- **n45** (Brandon) "Total RVs should distinguish units on the lot from total
  customers" -> FIXED **v1.453 (S114)**: `render.js` now surfaces a distinct
  on-lot count (`onLotCount` L628 "X on lot" sub-line + `totalRVs` L631) separate
  from the headline total. Was needed/S; **now DONE.**

Two brand-new ERs arrived 2026-06-17:

- **n51** (Sofia) "did a test on a returning customer Roland Shepard Jr, it did
  duplicate" -- a confirmation report of the **n49** dup-RO defect, which is now
  fixed in v1.12. **DUPLICATE of n49**; fold and decline.
- **n52** (Brandon) "add a 'scheduled to arrive on lot' status -- units with a
  drop-off time are wrongly counted as already on the lot." Real open enhancement
  (NEEDED/M). Builds directly on the just-shipped n45 on-lot count and overlaps
  n26. See RO-fields theme.

The only remaining non-enhancement item is **n25** -- a data-only staff-name
correction (SQL below; Roland runs it).

---

## BUG bucket (front of the line)

None still open. Both prior bugs (n47, n49) shipped in Session 114 -- see Headline.

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation restores full RO detail (parts/time_logs/photos). | done (already) |
| n2 | Ryan | Warranty category on new RO | Warranty RO type shipped v1.402 (`ro-crud.js` `roType=='warranty'` L855; `render.js` filter L112). | done (already) |
| n5 | Lynn | Work list opened but couldn't view/do anything | Fixed Phase 14: `work-list.js` `renderWorkList` L269 + `removeFromWorkList` L195 + `_setWorkListSilo` L263. | done |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs` L223 / `_setWorkListSilo` L263). | done (already) |
| n17 | Lynn | Add "Ready to Work" RO status | Shipped v1.410 (`config.js` `STATUS_PROGRESS_MAP` L131 slot 50). | done (already) |
| n21 | Roland | Fix customer check-in, email out, remove photo, streamline | `customer-checkin.html` made photo optional + auto-emails the signed RAF on every drop-off. | done (already) |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | FIXED v1.447 (`checkin.html` "Return to Dashboard" button L1487, `[ER BUGFIX S94] n22`). | done (already) |
| n33 | Bobby | Amounts drop the period and the zeros (425.00 -> 425) | FIXED v1.447 (`ro-crud.js` `editDollarValue` L843 `.toFixed(2)` + `parts.js` `MONEY_FIELDS` L288). | done (already) |
| n40 | Sofia | Check-in no longer emails staff / no RAF copy | FIXED v1.10/v1.11 (S97): `shouldNotifyStaff` L2286 fires every drop-off + RAF always sends; `getSignatureData` typo fix L1760. | done (already) |
| **n45** | **Brandon** | **Total RVs: split on-lot vs total customers** | **FIXED v1.453 (S114): `render.js` on-lot count L628 "X on lot" sub-line + `totalRVs` L631.** | **done (was needed/S)** |
| **n47** | **Ryan** | **Can't upload pictures from phone** | **FIXED v1.453 (S114): `photos.js` `uploadPhoto` DOM-attached input L57-66 (iOS Safari change-event fix).** | **done (was bug)** |
| **n49** | **Sofia** | **Returning-customer check-in duplicates a future-drop RO** | **FIXED v1.12 (S114): `customer-checkin.html` dedupe guard L2029-2082 updates the existing scheduled RO instead of inserting.** | **done (was bug)** |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod and Travis last name should be Wombles, not Wimbles, in tech assignment" | Data, not code. `staff.name` holds the value; not hardcoded. One SQL UPDATE (below). |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

Historical ROs that stored the tech name as free text are a separate optional backfill.

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete jobs / too small on phone / drag-and-drop" | Exact duplicate of **n10** (identical text + timestamp); already declined. Fold into n10. |
| n41 | Brandon | Completion / P-U date selection in the RO | Duplicate of **n24** (same submitter, same completion/pickup-date ask). Fold into n24. |
| n42 | Sofia | Returning customer (Sheldon Gilbert) check-in sent no email | Duplicate of **n40** (same check-in-email defect); now fixed v1.10/v1.11. Fold into n40. |
| n44 | Roland | Remove cashed-out RO from manager work list next day | Exact duplicate of **n43** (identical text, timestamps 1s apart). Fold into n43. |
| n51 | Sofia | "Test on returning customer Roland Shepard Jr, it did duplicate" | Duplicate of **n49** (returning-customer dup-RO); now fixed v1.12 (S114). Fold into n49; decline. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Scheduling & key dates  *(n4, n36, n24, n29, n37)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n4 | Lynn | Scheduled future drop-off date + email reminder to manager 1 day before | M |
| n36 | Lynn | Scheduled drop-date field in entry dashboard | S -- duplicate intent of n4 |
| n24 | Brandon | Completion / pickup date on the customer's order for reference (canonical for n41) | M |
| n29 | Lynn | Promised date large + highlighted on tile; sort by promise date then days-on-lot | M |
| n37 | Roland | Surface RO date/time as a prominent colored RO button + integrate into business calendar(s) with advance notifications | L |

-> **"Key Dates + Calendar" initiative.** n37 is the umbrella ask tying the
drop-off/promised/completion dates (n4/n24/n29/n36) to a calendar surface and the
notifications cluster below.

### Theme 2 -- Notifications, reminders & task-tracking  *(n3, n8, n30, n32, n20, n35, n50)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + font/color | L (urgency partly exists) |
| n35 | Lynn | Transfer clock-in job notes -> RO updates | M |
| n50 | Roland | Email the silo manager when another silo adds a service to an RO | M (`addServiceToRO` L243-262 sends no notice) |

-> **Largest opportunity.** A "Task Tracker + Reminders" subsystem; messaging path
is now the Sendblue POC (Twilio torn down S106) -- design as one initiative.

### Theme 3 -- Parts  *(n28, n9, n7, n27, n6)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n28 | Brandon | Parts + freight TOTAL in Manage Parts | S -- per-part Freight Charge exists (`pf_coreCharge` L217); summed total row not built. Clean quick win. |
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier | L (canonical for n27) |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

### Theme 4 -- Work List usability  *(n10, n31, n43)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n10 | Riley | Mobile: too small on phone + touch drag-and-drop (delete + HTML5 DnD reorder now work) | S -- mobile/touch polish |
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |
| n43 | Roland | Auto-remove an RO from a manager's work list the day after Cashiered Out/Delivered (canonical for n44) | M -- needs a daily job or status hook |

### Theme 5 -- RO fields & status nuance  *(n18, n34, n26, n19, n16, n52)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n18 | Lynn | "Keep plugged in" flag; keys / no-keys / keypad code | M |
| n34 | Brandon | Confirm if customer leaves keys with the unit (RO / sign-in / auth form) | S -- overlaps n18 |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning vs never-on-lot | M |
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs existing Hybrid) |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |
| n52 | Brandon | "Scheduled to arrive on lot" status so scheduled units aren't counted on-lot | M -- builds on n45; `config.js` has `Scheduled` L130 but on-lot count L628 includes it; overlaps n26 |

### Theme 6 -- Work Orders / templates  *(n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will | M (`loadWOTemplate` L399 / `saveWOTemplate` L468 exist; no edit/reorder) |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague -- overlaps n22) |

### Theme 7 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

### Theme 8 -- Reporting & analytics  *(n39)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n39 | Roland | Admin-level daily Manager-worklist report (parts/labor/costs, upcoming dates, missing-from-worklist ROs) -> manager scoring + profit-margin | L -- P1-P4 shipped (`send-manager-report`); scoring + margin gated |

### Theme 9 -- Check-in & authorization forms  *(n46)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n46 | Sofia | Send the RV authorization form (RAF) remotely for e-signature | L -- `customer-checkin.html` signs on-site only (`getSignatureData` L1760); no tokenized remote-signing route. Needs public signing page + email link + capture/store + staff notice |

### Theme 10 -- Photos & media  *(n48)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n48 | Brandon | Arrow-key navigation through photos instead of clicking each | S -- lightbox + `navigateLightbox` L382 exist; no keydown ArrowLeft/ArrowRight listener. Add keydown handler (+Esc) |

---

## Recommended action today

1. **Close the newly resolved ERs** (set `status=done`): **n45** (on-lot count
   split), **n47** (phone photo upload), **n49** (returning-customer dup-RO) --
   all shipped in Session 114 and confirmed in v1.453 / customer-checkin v1.12.
   **Decline n51** (duplicate of the now-fixed n49). Prior done/declined ERs
   (n0/n1/n2/n5/n12/n14/n15/n17/n21/n22/n33/n40, dups n11/n41/n42/n44) need no action.
2. **Run the n25 staff-name SQL** (Roland; data only).
3. **No code changes are queued from this run** -- the bug bucket is empty.

Standout near-term enhancement wins: **n28 (parts+freight total, S)**,
**n48 (arrow-key photo nav, S)**, and **n52 (scheduled-to-arrive status, M)** which
finishes the on-lot accuracy work started by the just-shipped n45. The strategic
asks -- **n38 (auth cost reduction)**, **n39 (manager reporting/scoring)**, and the
coordinated **Key Dates + Calendar** pass (Theme 1, anchored by n37) -- warrant a
design session with Roland before scoping.
