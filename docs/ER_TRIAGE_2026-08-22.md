# ER Triage — 2026-08-22 (Session 178)

**Scope:** all 37 `status='unreviewed'` ERs (52 done / 13 declined untouched).
**Verified against:** HEAD `0d2687b` — index.html **v1.496**, messages.html **v1.24**.
Nightly-runner verdicts (stamped at v1.492–v1.495 HEADs) were spot-re-verified at current HEAD for all 4 bugs — **all 4 still present**.

**Bucket counts:** 🐛 Bug 4 · ✅ Needed 30 · 🗃 Data 1 · 👯 Duplicate 2
**Grouping requested by Roland:** by function first, then LOE/risk within each group.

---

## GROUP 0 — 🐛 BUGS (front of the line)

| ER | By | Date | Severity | Finding (re-verified 2026-08-22) |
|---|---|---|---|---|
| `225a2535` | Brandon | 08-05 | 🔴 **data-integrity** | Insurance scan bleeds between customers. `window._lastEstimateScan` set per scan (`js/insurance.js` L118) but read keyed **by mode only** (L43-44), and `closeEditModal` deliberately KEEPS it (`js/ro-crud.js` L1326). Scan RO A → close unsaved → open RO B → A's insurance data merges onto B. **Same wrong-RO family as the Kain/Ivins incident (S176).** Fix: key the cache by RO UUID (`editingROSupabaseId` already exists since v1.496) + clear on modal close. |
| `d94a418a` | Brandon | 07-27 | 🟠 data-display | Multi-line RO Status notes render **bottom-line-first**: `js/render.js` L571 does `split('\n').map(escapeHtml).reverse()`. Same pattern at L581 for Customer Comm notes. Render-only, no data loss. Fix is removing/reworking the reverse (decide: intent was newest-note-first per note-block, but it reverses lines *within* one note). |
| `5ca724ff` | Lynn | 08-06 | 🟡 small (UX) | Desktop worklist drag-reorder dead. `js/work-list.js` dragstart (L415) never calls `e.dataTransfer.setData` / sets `effectAllowed` — Firefox (and some Chrome paths) won't start a drag without it. Mobile path works. ~2-line fix. |
| `d4d0eeba` | Brandon | 07-22 | 🟡 small (nav) | Clicking an RO in My Work List / Messages doesn't land on the card. `scrollToROInBoard` (index.html L4307) only matches already-rendered `[data-ro-index]` cards, else just toasts; its Clear Filters action re-renders but never re-scrolls. Messages-link path via `js/qr.js` same class. |

**Recommended:** one branch `fix/er-bugs-v1.497` off pre-prod, all four together, `225a2535` first. All are contained client-side fixes; no DB changes.

---

## GROUP 1 — RO Statuses & Board Sort (4 ERs, all S individually — ⚠️ ship as ONE careful batch)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `44419de9` | Lynn | 08-12 | S×4 | New statuses: **Need Estimate, Awaiting Estimate, Estimate Declined, Job Declined/Close** |
| `972af03c` | Lynn | 07-17 | S | New status **Canceled** — keep record + notes, never delete |
| `3e746c18` | Lynn | 08-03 | S | New status **Totaled Out** |
| `d1844e46` | Lynn | 07-16 | S | Board sort: promised date → oldest on lot, Shop Ops pinned top (needs a sort *mode*, today it's one blended priority score, `js/render.js` L30-39) |

**Risk note:** every status add touches ~9 hardcoded UI spots **+ BOTH DB CHECK constraints** (`repair_orders` + `cashiered` twin — the S172 lesson: 'Off Lot - Returning' shipped UI-only and was broken 50 days). S175 just took the canonical set 12→17; this batch takes it to **23** — worth asking Lynn/Roland whether all 6 new values survive, or whether e.g. Estimate Declined / Job Declined/Close / Canceled collapse. Terminal statuses must also be excluded from on-lot/queue counts. **One migration + one release.**

---

## GROUP 2 — RO Card: Notes Separation & Task Tracking (5 ERs, 2 duplicates)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `a03677e6` | Brandon | 08-07 | S | Move machine lines (NOTIFICATION SENT, customer reply, scheduled/cancelled…) out of RO Status Updates. ⚠️ Roland **reversed** a similar retarget for lead notes in v1.491 — confirm target stream before building. |
| `2deb8f30` | Lynn | 08-13 | dup | **Duplicate of `a03677e6`** (same ask, "separate location view"). → mark `declined` w/ pointer. |
| `af14b3cb` | Lynn | 05-21 | L | Per-RO TO-DO list with daily reminder; items persist until completed. |
| `ece91cba` | Lynn | 08-17 | dup | **Duplicate of `af14b3cb`** — but adds real spec: bulleted, anyone can add/check, notate who/when; also defines Repair Description vs RO Status roles. → fold into `af14b3cb` as spec, mark `declined` w/ pointer. |
| `5925fc01` | Lynn | 04-09 | L (partial) | Notify on every RO update + daily digest w/ scraped to-dos. Digest half **exists** (`send-manager-report`); remainder overlaps the task-tracker build. Design together with `af14b3cb`. |

---

## GROUP 3 — RO Fields & Data Capture (3 ERs)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `ac8265c8` | Lynn | 08-05 | S | `repair_orders.mileage` column + New/Edit inputs (+card readout). Zero mileage/odometer hits repo-wide. Could ride the Group 1 migration. |
| `a195e4ab` | Brandon | 08-20 | M | Check/payment tracking on the RO — multiple checks, amounts, received dates. Today only the single-value hybrid Customer Pay block exists. Needs a small `payments` design decision. |
| `fa35e3df` | Lynn | 08-18 | S (cosmetic) | Kill hard-to-read blue font: `#3b82f6` parts chip, `.comm-thread-link`, `#0a84ff` key-date chip, `#667eea` log-duration — all ≈4.0-4.6:1 contrast. Pure CSS. |

---

## GROUP 4 — Messages UX Polish (5 ERs, all S — one messages release, Brandon's batch)

| ER | By | Date | Ask |
|---|---|---|---|
| `9aab7550` | Brandon | 07-21 | Stronger unread treatment — today only an 8px gold dot; add row highlight/bold. |
| `21fc3a9d` | Brandon | 07-21 | Persisted font-scale control for the conversation list. |
| `f1e6e311` | Brandon | 07-21 | Clear (×) button on conversation search. |
| `6e9ca4dc` | Brandon | 07-21 | Remove "✍️ Added to every send" signature preview (already hidden on mobile; kill/collapse on desktop too). |
| `ca0f7ed9` | Brandon | 08-03 | Lead-info panel auto-populate from linked dashboard ROs — `selectConversation` already computes `linked` ROs but `renderLeadInfo(c)` never receives them (messages.html L1744). |

**All five ≈ one `messages-v1.25` session.**

---

## GROUP 5 — Parts & Inventory (3 ERs)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `8d885331` | Lynn | 04-09 | S | Reorder parts-entry fields — **blocked on Lynn supplying the target order/mockup**, not on effort. |
| `b19975d4` | Lynn | 04-09 | L | Supplier returns + warranty flow (RMA, reason, credit). Overlaps open TODO **GH#24 Phase 2 Parts Returns** — merge into that build. |
| `14f35f95` | Lynn | 04-09 | XL | Stock/inventory subsystem (order 25, use 5, 20 searchable in-house stock). New tables + receive/consume/adjust flows. Needs its own spec + Roland go/no-go. |

---

## GROUP 6 — Work Orders (1 ER)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `2f752d40` | Jason | 07-21 | S | WO dollar auto-populate = total hours × posted $195 labor rate. Add a `LABOR_RATE` config constant; decide override behavior (auto-fill but editable is safest). |

---

## GROUP 7 — Check-In / Time Clock (2 ERs — same theme, build once)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `6bb61db2` | Riley | 07-22 | S | Compact-view clock in/out button + an "you're clocked into X" indicator visible at login, all layouts. **Treat as the canonical spec.** |
| `e06d747e` | Riley | 04-10 | S | "Tech check-in float" — underspecified; almost certainly the same ask. Fold into `6bb61db2`. |

---

## GROUP 8 — Customer / Account Management (3 ERs)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `0e172b2c` | Sofia | 07-16 | M | Merge duplicate customer accounts (misspelling → two ROs). `js/duplicates.js` merges only same-base-RO-id groups; needs name/phone-similarity detection + verify-then-merge. |
| `5fbfa299` | Brandon | 06-30 | M | Archive quoted-never-proceeded customers. **Partially solved by Group 1's Canceled status** — decide if status alone is enough or a board filter/soft-archive is still wanted. |
| `3f254929` | Sofia | 07-16 | 🗃 Data | Delete the misspelled **Steve Hubert** RO (correct = Huebert, customer verified). SQL for Roland below. |

**Data SQL (Roland runs in SQL Editor — eyeball first, then soft-delete the wrong id):**
```sql
SELECT id, ro_id, customer_name, status, created_at
FROM repair_orders WHERE customer_name ILIKE '%ubert%';
-- then, with the confirmed wrong id:
-- UPDATE repair_orders SET deleted_at = now(), deleted_by = 'roland@patriotsrvservices.com'
-- WHERE id = '<confirmed-wrong-uuid>' AND deleted_at IS NULL;
```

---

## GROUP 9 — RAF / E-Signature (2 ERs)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `5fec3eca` | Sofia | 07-15 | S | Customer address on the emailed RAF copy — add `customerAddress` to the `raf_copy` payload + template in `send-quote-email` (+ checkin sender). Edge-fn deploy, no release. |
| `0448de62` | Sofia | 06-11 | L | Remote e-signature RAF — tokenized public signing page + delivery + storage. Real build; pairs naturally with the short-link machinery from S166. |

---

## GROUP 10 — Misc UI Quick Wins (3 ERs, all S)

| ER | By | Date | Ask |
|---|---|---|---|
| `d432863a` | Brandon | 07-21 | Patriots-logo favicon on every page — **also closes the long-standing favicon-404 TODO (S139)**. |
| `29654af7` | Lynn | 07-06 | Back-to-top / jump-to-search button after scrolling. |
| `50175fce` | Lynn | 07-27 | Urgent Update editable inline from the tile (reuse editField path so `urgent_update_notify` still fires). |

---

## GROUP 11 — Strategic / Needs Roland (2 ERs)

| ER | By | Date | LOE | Ask |
|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | XL | Replace per-seat Google-email auth (≈$500/mo). Auth is Google-token end-to-end (`js/auth.js` + One Tap + `signInWithIdToken`); moving to Supabase password/magic-link touches auth, RLS identity, kiosk. **Dedicated design session with Roland.** |
| `47428f75` | Lynn | 08-03 | ? | "update vacation/sick/personal calendar" — underspecified; time-off.html already has all three types + calendar + edit. **Ask Lynn what's actually wrong/wanted.** |

---

## Recommended sequencing

**Now (this session candidate):**
1. 🐛 **Bug batch** — `fix/er-bugs` branch: `225a2535` (data-integrity, Kain/Ivins family) + `d94a418a` + `5ca724ff` + `d4d0eeba`. One v1.497 release.
2. 🗃 Hubert SQL → Roland runs.
3. 👯 Mark the 2 duplicates `declined` (with pointers) once Roland approves.

**Next sessions (each ≈ one session):**
4. Messages polish batch (`messages-v1.25`, all Brandon's S items).
5. Status batch (Group 1) — after Lynn/Roland confirm the 6 status names; one migration, both CHECK twins.
6. Misc-UI + cosmetic batch (favicon, back-to-top, urgent-inline, blue-font, mileage).
7. WO labor-rate autofill + check-in button batch.

**Design-first (need input before code):**
- Task tracker (Group 2 L items) · check tracking (`a195e4ab`) · account merge (`0e172b2c`) · remote e-sign · parts returns (fold into GH#24) · inventory XL · auth replacement XL.

**Blocked on submitter detail:** `8d885331` (Lynn layout) · `47428f75` (Lynn clarification).
