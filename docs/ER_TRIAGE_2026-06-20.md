# Enhancement Request Triage -- 2026-06-20 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-20T10:10Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.457** (checkin.html v1.37, customer-checkin.html v1.12, parts/render/photos/ro-crud verified by grep).
**Total ERs:** 58 (incl. 3 test entries + 5 duplicates -> **50 real requests**).
**Triaged this run:** 57 ids in `needs_triage_ids` (every row except the n15 test entry, carried over unchanged).

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `time-off.html`, `closed-ros.html`, edge functions, SQL
migrations) by grepping the source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

Counts this run: **22 done - 1 bug - 29 needed - 1 data - 5 duplicate**.

---

## Headline

**Four new ERs arrived 2026-06-19** (n54-n57) and were classified fresh against
current code:

- **n54 (Sofia)** -- "David Flanagan checked in as returning customer, he was in
  Dashboard as scheduled and again it didn't send authorization form by email and
  it duplicated." This is a **field report dated AFTER the v1.12 dedupe fix and the
  v1.10/v1.11 RAF-email fix shipped**, so it is NOT folded into the (now done)
  n49/n40. Classified **BUG**: the v1.12 returning-customer guard
  (`customer-checkin.html` L2036) only fires when the selected returning record
  has `_source === 'repair_orders'` with a matching `id`; if the receptionist
  matched David from another search source, the guard is skipped and a second RO
  inserts -- plus the RAF email reportedly did not send. Needs device repro and a
  widened guard. (Per the rail: prefer bug over a false "done".)
- **n55 (Lynn)** -- hover near the customer name to show important dates/tasks as a
  list. Colored key-date chips shipped v1.457 but there is no hover tooltip
  aggregating dates, and the task list overlaps the unbuilt task-tracker (n30).
  **NEEDED / M.**
- **n56 (Lynn)** -- cannot edit time-off entries. `time-off.html` offers only a
  Delete button (`deleteRequest` L867); there is no edit/update path, so any
  correction needs delete + re-create. **NEEDED / M.**
- **n57 (Lynn)** -- time-off needs an exact absence time window (clock-out to
  return; AM/PM half-day) instead of a total of hours. Partial-day *hours* exist
  (`partial_hours` migration + hours dropdown, `time-off.html` L344-357) but
  capture duration, not a time-of-day window. **NEEDED / M.**

**The S117 "Key Dates P1" pass (v1.456/v1.457) closed a batch of long-standing
NEEDED items**, confirmed in the current tree:

- **n4 / n36** (planned drop-off date + morning-before reminder) -> `ro-crud.js`
  L253/L311 (`planned_dropoff_date`), reminder cascade L318-382. **Now DONE.**
- **n24** (pickup/completion date) -> `ro-crud.js` L254/L312 (`pickup_date`) + green
  Pickup chip `render.js` L354. **Now DONE.**
- **n28 / n53** (parts + freight Ticket Totals) -> `parts.js` L148-160 totals bar
  (wholesale / freight = sum core_charge / total cost / retail). **Now DONE.**
- **n48** (arrow-key photo navigation) -> `photos.js` L385-393 keydown handler. **Now DONE.**
- **n52** (Scheduled units inflating the on-lot count) -> `render.js` L606-653 now
  excludes `Scheduled`. **Now DONE.**

The bug bucket holds **one** item -- the new n54 returning-customer regression.

---

## BUG bucket (front of the line)

| # | Submitter | Request | Mapped code | Verdict |
|---|---|---|---|---|
| n54 | Sofia | Returning customer (David Flanagan) was Scheduled, still duplicated the RO and sent no auth-form email | `customer-checkin.html` dedupe guard L2029-2082 (fires only when selected record `_source==='repair_orders'` w/ matching id, L2036); RAF email path | **BUG** (data-integrity + missing RAF email). Reported AFTER v1.12 / v1.10-11 fixes, so not a duplicate-of-done. Guard is skipped when the returning customer is matched from a non-`repair_orders` source -> a second RO inserts. Device repro + widen the guard to match a customer's existing Scheduled RO regardless of search source. Do NOT close. |

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation restores full RO detail (parts/time_logs/photos). | done (already) |
| n2 | Ryan | Warranty category on new RO | Warranty RO type v1.402 (`ro-crud.js` L855; `render.js` warranty filter L112). | done (already) |
| n5 | Lynn | Work list opened but couldn't view/do anything | Fixed Phase 14 (`work-list.js` renderWorkList L269 / removeFromWorkList L195 / _setWorkListSilo L263). | done |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs` L223 / `_setWorkListSilo` L263). | done |
| n17 | Lynn | Add "Ready to Work" RO status | Shipped v1.410 (`config.js` STATUS_PROGRESS_MAP L131 slot 50). | done |
| n21 | Roland | Fix customer check-in, email out, remove photo, streamline | `customer-checkin.html` photo optional + forces RAF/lead-staff email; staff-notify gap closed v1.10/v1.11. | done |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | FIXED v1.447 (`checkin.html` Return-to-Dashboard button L1487, `[ER BUGFIX S94] n22`). | done |
| n33 | Bobby | Amounts drop the period and zeros (425.00 -> 425) | FIXED v1.447 (`ro-crud.js` editDollarValue L843 `.toFixed(2)` + `parts.js` MONEY_FIELDS L288). | done |
| n40 | Sofia | Check-in sent no email / no auth-form copy | FIXED v1.10/v1.11 (`shouldNotifyStaff` fires every drop-off; RAF always sends; getSignatureData L1760 typo fixed). | done |
| n45 | Brandon | Split "Total RVs" vs on-lot count | DELIVERED v1.453 (`render.js` statTotalOnLot L649 sub-line); v1.454 excludes Scheduled. | done |
| n47 | Ryan | Phone photo upload accepts then nothing uploads | FIXED v1.453 (`photos.js` file input DOM-attached before `.click()` for iOS Safari, tagged ER 2c3d5633). | done (device-confirm) |
| n48 | Brandon | Arrow keys to move through photos | DELIVERED v1.454 (`photos.js` L385-393 keydown ArrowLeft/ArrowRight + Escape). | done |
| n49 | Sofia | Returning-customer check-in duplicates an RO | FIXED v1.12 (`customer-checkin.html` dedupe guard L2029-2082). NOTE: residual field report = new n54. | done |
| n52 | Brandon | Scheduled units counted as on-lot | DELIVERED v1.454 (`render.js` L606-653 excludes Scheduled from on-lot + days stats). | done |
| n53 | Brandon | Freight included in Manage Parts ticket totals | DELIVERED v1.456 (`parts.js` L153/L160 Freight = sum core_charge). | done |
| n4 | Lynn | Scheduled future drop-off date + reminder | DELIVERED v1.457 (`ro-crud.js` L253/L311 planned_dropoff_date + reminder cascade L318-382). | done |
| n36 | Lynn | Scheduled drop-date field in entry dashboard | DELIVERED v1.457 (drop-off date now on New + Edit RO; duplicate intent of n4). | done |
| n24 | Brandon | Completion / pickup date on the customer's order | DELIVERED v1.457 (`ro-crud.js` L254/L312 pickup_date + green Pickup chip `render.js` L354). | done |
| n28 | Brandon | Parts + freight TOTAL in Manage Parts | DELIVERED v1.456 (`parts.js` L148-160 Ticket Totals bar). | done |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod, Travis last name should be Wombles not Wimbles in tech assignment" | Data, not code. `staff.name` holds the value. Already corrected in DB per S117 (no Wimbles rows remain). SQL if any reappear (below). |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete jobs / too small on phone / drag-and-drop" | Exact duplicate of **n10** (same text + timestamp); already declined. |
| n41 | Brandon | Completion / P-U date to record when units leave the lot | Duplicate of **n24** (now delivered v1.457); already declined. |
| n42 | Sofia | Returning customer Sheldon Gilbert -- no check-in email | Duplicate of **n40** (fixed v1.10/v1.11); already declined. |
| n44 | Roland | Remove cashed-out RO from worklist day after | Exact duplicate of **n43** (1s apart); already declined. |
| n51 | Sofia | Test on Roland Shepard Jr -- it duplicated | Duplicate of **n49** (fixed v1.12); already declined. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Scheduling & key dates  *(n29, n37, n55)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n29 | Lynn | Promised date large/highlighted on tile; sort by promise date then days-on-lot | S -- amber chip shipped v1.457 (`render.js` L353); dedicated sort toggle deferred (board score already weights promised date) |
| n37 | Roland | RO date/time as a colored RO button + business-calendar integration + advance notifications | L -- PARTIAL: key-date chips shipped v1.457 (`render.js` L347-355); calendar (Phase 2) + advance email reminders (Phase 3) remain |
| n55 | Lynn | Hover near customer name to show important dates/tasks as a list | M -- chips exist; no hover tooltip; task list overlaps n30 [new] |

-> n4/n24/n36 shipped (v1.457) and are now in the DONE bucket; this theme has shrunk to tile-emphasis, calendar integration, and the hover summary.

### Theme 2 -- Notifications, reminders & task-tracking  *(n3, n8, n20, n30, n32, n35, n50)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs | L (messaging path now Sendblue POC; Twilio torn down S106) |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + font/color | L |
| n35 | Lynn | Transfer clock-in job notes -> RO updates | M (`time-tracking.js` workNotes L53; no auto-copy) |
| n50 | Roland | Email the silo manager when another silo adds a service to an RO | M (`work-orders.js` addServiceToRO L243-262; no notify) |

### Theme 3 -- Parts  *(n6, n7, n9, n27)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier | L -- canonical for n27 |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

-> n28 (parts + freight total) shipped v1.456 and moved to DONE.

### Theme 4 -- Work List  *(n10, n31, n43)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n10 | Riley | Mobile: too small on phone; touch drag-drop reorder | S -- delete + HTML5 DnD reorder shipped; remaining = phone sizing + touch-drag |
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |
| n43 | Roland | Auto-remove an RO from a manager's worklist day after Cashiered Out/Delivered | M (`work-list.js` manual removeFromWorkList L195 only) |

### Theme 5 -- RO fields & status nuance  *(n18, n34, n26, n19, n16)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n18 | Lynn | "Keep plugged in" flag; keys / no-keys / keypad code | M |
| n34 | Brandon | Confirm if customer leaves keys with the unit | S -- overlaps n18 |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning vs never-on-lot | M (distinct from n52, now done) |
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs Hybrid) |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |

### Theme 6 -- Work Orders / templates  *(n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will | M (`work-orders.js` loadWOTemplate L399 / saveWOTemplate L468; no editing/reorder) |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague -- overlaps n22) |

### Theme 7 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

### Theme 8 -- Reporting & analytics  *(n39)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n39 | Roland | Admin-level daily Manager-worklist report -> manager scoring + profit-margin | L -- P1-P4 shipped (`send-manager-report`); scoring + profit-margin remain |

### Theme 9 -- Check-In & Forms  *(n46)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n46 | Sofia | Send the authorization form remotely for e-signature | L -- on-site signing only (`customer-checkin.html` getSignatureData L1760); needs public token-signing page + email link + signature store + staff notice |

### Theme 10 -- Time Off / Calendar  *(n56, n57)*  [new]
| # | Submitter | Request | LOE |
|---|---|---|---|
| n56 | Lynn | Cannot edit time-off entries (modify day / person / type) | M -- `time-off.html` has Delete only (`deleteRequest` L867); add an in-place edit flow [new] |
| n57 | Lynn | Time-off needs an exact absence time window (clock-out to return; AM/PM half-day) instead of total hours | M -- partial-day *hours* exist (`partial_hours` migration + dropdown L344-357) but capture duration, not a time-of-day window [new] |

---

## Recommended action today

1. **Fix the bug first:** **n54** (returning-customer-of-Scheduled still
   duplicates + no RAF email). Reproduce on a device, then widen the
   `customer-checkin.html` dedupe guard so it matches a customer's existing
   Scheduled RO regardless of the receptionist's search source, and confirm the
   RAF email fires on that path. Keep n49 closed but cross-reference n54.
2. **Close the resolved ERs** (set `status=done`): n4, n24, n28, n36 (shipped
   v1.456/v1.457), plus the already-confirmed n45/n47/n48/n52/n53. n0/n14/n15/n1/
   n2/n5/n12/n17/n21/n22/n33/n40/n49 are already closed.
3. **n25 staff-name SQL** is a no-op (already corrected in DB); keep the SQL on
   hand only if a Wimbles row reappears.
4. **Duplicates** n11/n41/n42/n44/n51 are already `declined` -- no action.

Standout near-term enhancement wins: the **Time Off / Calendar** pair (n56 edit +
n57 time-window, both M and self-contained in `time-off.html`), and **n50** (silo
service-add notification, M). The biggest strategic asks remain **n38** (auth cost
reduction, XL) and **n39** (manager scoring + profit-margin, L) -- both warrant a
design session before scoping.
