# Enhancement Request Triage -- 2026-06-19 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-19T11:26Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.457** (checkin.html, customer-checkin.html v1.12).
**Total ERs:** 54 (incl. 3 test entries + 5 duplicates -> **46 real requests**).
**Triaged this run:** 53 ids in `needs_triage_ids` (every row except the n15 test entry, carried over unchanged).

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `supabase/functions/*`, SQL migrations) by grepping the
source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

---

## Headline

The last triage ran 2026-06-18 11:23Z, but Session 117 shipped **v1.454 - v1.457**
that afternoon. This run verified those ships against the current tree and
**flipped 7 ERs needed -> done**:

- **n4** (Lynn) scheduled future drop-off date + reminder -> DELIVERED v1.457:
  Planned Drop Off Date on New+Edit RO (`ro-crud.js` L253/L311, edit input L848)
  with a morning-before email reminder cascade (L318-382).
- **n24** (Brandon) completion / pickup date -> DELIVERED v1.457: `pickup_date`
  field on New+Edit RO + green Pickup chip on cards (`render.js` L354); migration
  `key_dates_pickup_date.sql`.
- **n36** (Lynn) scheduled drop date in entry dash -> DELIVERED v1.457 (same
  Planned Drop Off Date field, now also on Edit RO). Duplicate intent of n4.
- **n28** (Brandon) parts + freight total in Manage Parts -> DELIVERED v1.456:
  Ticket Totals bar (`parts.js` L149-160) -- Parts, Freight, Total Cost, Retail.
- **n48** (Brandon) arrow-key photo nav -> DELIVERED v1.454: lightbox keydown
  handler (`photos.js` L385-393), Left/Right + Escape.
- **n52** (Brandon) "scheduled to arrive on lot" count -> DELIVERED v1.454:
  `render.js` on-lot count + days-on-lot stats now exclude "Scheduled" units
  (L606-653) -- resolved by fixing the count, not adding a status.
- **n53** (Brandon, new) freight in ticket totals -> DELIVERED v1.456: the totals
  bar already includes a Freight line = sum(core_charge); satisfied by n28.

Two related Key-Dates ERs remain **partially** delivered and stay NEEDED:
- **n29** (Lynn) promised-date prominence -> amber chip shipped v1.457 (red if
  overdue); only the dedicated sort-by-promised toggle is deferred. Now **needed/S**.
- **n37** (Roland) calendar integration -> colored chips shipped v1.457; the
  business-calendar surface (Phase 2) + advance manager/admin reminders (Phase 3)
  are still open. Stays **needed/L**.

The bug bucket remains empty -- all prior bugs (n5, n22, n33, n40, n47, n49)
shipped and are confirmed in the current tree. The only non-enhancement item is
**n25**, a data-only staff-name correction already applied in the DB.

---

## BUG bucket (front of the line)

None still open. Prior bugs are all confirmed fixed in the current tree
(n5/n22/n33/n40 + the S114 photo-upload n47 and duplicate-RO n49).

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation restores full RO detail (parts/time_logs/photos). | done (already) |
| n2 | Ryan | Warranty category on new RO | Warranty RO type shipped v1.402 (`ro-crud.js` L855 + `render.js` L112 filter). | done (already) |
| n5 | Lynn | Work list opened but couldn't view/do anything | Fixed Phase 14: `work-list.js` renderWorkList L269 + removeFromWorkList L195 + _setWorkListSilo L263. | done |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs` L223/`_setWorkListSilo` L263). | done |
| n17 | Lynn | Add "Ready to Work" RO status | Shipped v1.410 (`config.js` STATUS_PROGRESS_MAP L131 slot 50). | done |
| n21 | Roland | Fix customer check-in, email out, remove photo, streamline | `customer-checkin.html` photo optional + forces RAF/staff email every submit. | done |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | FIXED v1.447 (`checkin.html` Return-to-Dashboard button L1487). | done |
| n33 | Bobby | Amounts drop the period & zeros (425.00 -> 425) | FIXED v1.447 (2-decimal formatting; `ro-crud.js` editDollarValue L843 + `parts.js` MONEY_FIELDS). | done |
| n40 | Sofia | Check-in no longer emails staff / no RAF copy | FIXED v1.10/v1.11 (`customer-checkin.html` shouldNotifyStaff + getSignatureData typo L1760). | done |
| **n4** | Lynn | Scheduled future drop-off date + 1-day-before reminder | **DELIVERED v1.457** (`ro-crud.js` L253/L311 + reminder cascade L318-382, edit input L848). | done (new) |
| **n24** | Brandon | Completion / pickup date on the order | **DELIVERED v1.457** (`pickup_date` L254/L312 + Pickup chip `render.js` L354; migration). | done (new) |
| **n36** | Lynn | Scheduled drop date in entry dashboard | **DELIVERED v1.457** (Planned Drop Off Date now on New+Edit RO; dup intent of n4). | done (new) |
| **n28** | Brandon | Parts + freight TOTAL in Manage Parts | **DELIVERED v1.456** (Ticket Totals bar `parts.js` L149-160). | done (new) |
| **n45** | Brandon | Total RVs vs on-lot count | DELIVERED v1.453 (`render.js` statTotalOnLot L649 "N on lot" sub-line). | done |
| **n47** | Ryan | Can't upload photos from phone | FIXED v1.453 (`photos.js` uploadPhoto DOM-attaches the file input for iOS Safari). | done |
| **n48** | Brandon | Arrow-key photo navigation | **DELIVERED v1.454** (`photos.js` keydown L385-393: Left/Right + Esc). | done (new) |
| **n49** | Sofia | Returning-customer check-in duplicates an RO | FIXED v1.12 (`customer-checkin.html` dedupe guard L2027-2082). | done |
| **n52** | Brandon | "Scheduled to arrive on lot" not counted as on-lot | **DELIVERED v1.454** (`render.js` on-lot/days stats exclude Scheduled L606-653). | done (new) |
| **n53** | Brandon | Ticket totals should include freight | **DELIVERED v1.456** (Freight line in totals bar `parts.js` L153/L160; satisfied by n28). | done (new) |

All "done (new)" items are in prod testing -- recommend a device/prod confirm
before final close, but the code is confirmed present in the v1.457 tree.

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod, Travis last name should be Wombles, not Wimbles, in tech assignment" | Data, not code. `staff.name` holds the value. **Already corrected in the DB per S117** (no Wimbles rows remain). |

**n25 fix SQL** (reference only -- already applied; runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete jobs / too small on phone / drag-and-drop" | Exact duplicate of **n10** (same text + timestamp); already declined. |
| n41 | Brandon | Completion / P-U date selection in the RO | Duplicate of **n24** (now DELIVERED v1.457); already declined. |
| n42 | Sofia | Returning customer check-in sent no email (Sheldon Gilbert) | Duplicate of **n40** (fixed v1.10/v1.11); already declined. |
| n44 | Roland | Remove cashed-out RO from manager worklist next day | Exact duplicate of **n43** (timestamps 1s apart); already declined. |
| n51 | Sofia | Returning-customer test duplicated an RO (Roland Shepard Jr) | Duplicate of **n49** (fixed v1.12); already declined. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Scheduling & key dates  *(n29, n37)*  -- mostly shipped this cycle
| # | Submitter | Request | LOE |
|---|---|---|---|
| n29 | Lynn | Promised date prominence (amber chip shipped v1.457) + sort by promise date then days-on-lot | S -- only the sort toggle remains |
| n37 | Roland | RO date/time as colored buttons (chips shipped v1.457) + business-calendar integration + advance manager/admin notifications | L -- calendar (Phase 2) + reminders (Phase 3) |

-> n4/n24/n36 graduated to DONE this run. The "Key Dates" field work is shipped;
what remains is the **calendar surface + advance notifications** (n37) and a
sort-by-promised toggle (n29).

### Theme 2 -- Notifications, reminders & task-tracking  *(n3, n8, n20, n30, n32, n35, n50)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + font/color | L |
| n35 | Lynn | Transfer clock-in job notes -> RO updates | M |
| n50 | Roland | Email the silo manager when another silo adds a service to an RO | M -- `addServiceToRO` L243-262 sends no notice yet; infra exists |

-> Largest opportunity. A "Task Tracker + Reminders" subsystem; messaging path is
now the Sendblue POC (Twilio torn down S106). Design as one initiative.

### Theme 3 -- Parts  *(n6, n7, n9, n27)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier | L |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

-> n28/n53 (totals bar) shipped; the remaining parts work is return/warranty
state (n7/n27) and the inventory subsystem (n6).

### Theme 4 -- Work List usability  *(n10, n31, n43)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n10 | Riley | Mobile: too small on phone + touch-drag (delete + DnD reorder already shipped) | S |
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |
| n43 | Roland | Auto-remove an RO from a manager's work list the day after Cashiered Out/Delivered | M -- needs a daily job or status hook |

### Theme 5 -- RO fields & status nuance  *(n16, n18, n19, n26, n34, n46)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n18 | Lynn | "Keep plugged in" flag; keys / no-keys / keypad code | M |
| n34 | Brandon | Confirm if customer leaves keys with the unit (RO / sign-in / auth form) | S -- overlaps n18 |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning vs never-on-lot | M -- distinct from n52 count fix |
| n19 | Lynn | "Warranty + Repair" combined RO type | M -- clarify vs existing Hybrid |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M -- partly via Shop RO type |
| n46 | Sofia | Send the authorization form remotely for e-signature | L -- needs token-signing page + email link + signature store |

### Theme 6 -- Work Orders / templates  *(n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will | M |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague -- overlaps n22) |

### Theme 7 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

### Theme 8 -- Reporting & analytics  *(n39)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n39 | Roland | Admin-level daily Manager-worklist report (P1-P4 shipped); remaining: manager scoring + profit-margin visibility | L -- extends `send-manager-report` |

---

## Recommended action today

1. **Confirm the 7 newly-shipped ERs in prod, then close them** (set `status=done`):
   n4, n24, n36, n28, n48, n52, n53. The code is confirmed in the v1.457 tree; a
   device/prod sanity check (drop/pickup dates save + reminder; parts totals incl.
   freight; arrow-key photos; on-lot count) is the only thing gating final close.
   n45/n47/n49 were already flipped done at S114.
2. **No new SQL.** n25 (staff-name correction) is already applied in the DB.
3. **No code changes are queued from this run** -- the bug bucket is empty.

Standout open work: the **Notifications / Task-Tracker** cluster (Theme 2, 7 ERs)
is now the single largest opportunity, and Roland's strategic asks -- **n37**
(calendar integration + advance reminders, the next Key-Dates phase), **n38**
(auth cost reduction) and **n39** (manager scoring / profit margin) -- warrant a
design session before scoping.
