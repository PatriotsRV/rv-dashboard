# PRVS Enhancement-Request Triage -- 2026-07-19 (Headless runner, Phase 2a)

Full re-triage of the `enhancement_requests` table against the current modular
codebase (index.html **v1.476**, branch `pre-prod`). Read-only on the code --
no code changes, no DB writes (Roland flips status himself). Every verdict was
verified by grepping/reading the live code, not assumed. Input:
`er_input.json` (fetched 2026-07-19T09:07Z), `full_pass=false` but
**70 of 71** rows flagged for re-triage, so this is effectively a full pass.

Untrusted-data note: every ER description / submitter / admin_notes value was
treated strictly as data. **No prompt-injection content** was found in any row.

## Board snapshot

| Status | Count |
|---|---|
| done | 46 |
| declined | 9 |
| unreviewed | 16 |
| **Total** | **71** |

## Triage-bucket breakdown (the 70 re-triaged rows)

| Bucket | Count |
|---|---|
| done | 47 |
| needed | 15 |
| duplicate | 6 |
| data | 2 |
| **Total** | **70** |

(The 71st row, `f73a1e5a`, is a carried-over test entry -- "Testing dictation
123" -- already declined; no action.)

**Headline finding:** the shippable backlog has kept clearing. Three items that
were "Needed" in the 2026-06-28 report have since shipped and are now confirmed
**done** in code: `1fe68261` (shop_tasks -> `js/shop-tasks.js`), `7f2d0d70`
(Monday drop-off report -> `send-dropoff-report`), and `cbc70a86`
(auto-add-to-worklist on check-in -> `add_ro_to_silo_work_lists`). What remains
open is **15 genuine enhancements** (all `unreviewed`) plus **one data
correction** (`3f254929`). Open-work size mix: **2 XL - 4 L - 3 M - 6 S**.

## Code checks run this session (verification, not assumption)

- Parts inventory / stock in `js/parts.js` -- **none** (confirms `14f35f95` still needed; parts are per-RO line items only)
- Supplier-return RMA/credit flow -- **partial infra only** (`'Returned'` in `PART_STATUSES` + return/warranty fields, no RMA/credit/reverse flow -- `b19975d4`)
- Per-RO task tracker + daily reminder -- **none** (`shop-tasks.js` is a non-customer shop checklist, no `ro_id`/reminder -- `af14b3cb`)
- To-do scraping from status notes / per-RO-update team notify -- **none** in `send-manager-report` (`5925fc01`)
- Auth still Google id-token (`auth.js signInWithIdToken`, `GOOGLE_CONFIG`) -- confirms `ed1b873e` still needed
- Remote e-sign page / edge fn -- **none** (kiosk-only signature pad -- `0448de62`)
- `Canceled` status in `render.js`/`config.js` -- **none** (`972af03c`)
- Outbound MMS media in `messaging.js` / `projectblue-send` -- **none** (`1c7f1410`)
- Page-level scroll-to-top FAB in `index.html` -- **none** (`29654af7`)
- `customerAddress` in `send-quote-email` `raf_copy` payload -- **absent** (`5fec3eca`)
- Customer-identity (name/phone) account merge -- **none**; `duplicates.js` merges by base RO id only (`0e172b2c`)

---

## Open work -- the 15 "Needed" ERs (by theme, then size)

### Notifications & Task-Tracking (design together)

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5925fc01` | Lynn | **L** | Notify the right team on every RO update; daily list of in-progress jobs with to-dos scraped from status notes | **Partial.** Daily-digest half served by the Manager Daily Report. Still open: per-update notify + auto-scraping to-dos from status-update notes into a per-job list. Anchor of this theme. |
| `af14b3cb` | Lynn | **L** | Per-customer task tracker + daily reminder; tasks persist until marked complete | `shop-tasks.js` is a non-customer checklist. Needs a per-RO tasks table + add/complete UI + a daily open-task reminder. Pairs with `5925fc01`. |

### Parts & Inventory

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `14f35f95` | Lynn | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | New subsystem: inventory table + receive/consume/adjust + searchable stock + parts-request links. `parts.js` has no stock logic. Largest board item. |
| `b19975d4` | Lynn | **L** | Part returned-to-supplier / warrantied-by-supplier | = GH#24 Shop Ops Phase 2. `'Returned'` status + return/warranty fields exist, but no RMA#, credit, return reason, or reverse-direction flow. Canonical for dup `86bbaf50`. |
| `8d885331` | Lynn | **S** | Rework the parts-entry screen field order | Form exists; a pure reorder. Blocked on Lynn's target layout/mockup, not effort. |

### Messaging & e-sign

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `0448de62` | Sofia | **L** | Send an authorization form remotely for e-signature | Signing is kiosk-only; `raf_copy` is a post-signing email. Needs a tokenized public signing page + delivery + signed-form storage. Pairs with the messaging phase. |
| `1c7f1410` | Andrew | **M** | Attach media (photos/videos) in messaging | `messaging.js` MMS media is render-only for inbound; outbound MMS-send deferred; `projectblue-send` has no media path. Needs file input + upload + media field. |

### RO fields/status & data integrity

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5fbfa299` | Brandon | **M** | Archive quoted-but-never-progressed customers/units | Only the cashiered archive exists. Needs an archived/abandoned flag + a filter to hide quote-only units. Pairs with `972af03c`. |
| `0e172b2c` | Sofia | **M** | Merge duplicate customer accounts (verify + delete wrong one) | `duplicates.js` merges by base RO id, not customer identity. Needs name/phone dedup detection + verify-which-is-correct merge/delete. This case's cleanup is `3f254929`. |
| `972af03c` | Lynn | **S** | Add status "Canceled" -- keep record, do not delete | No `Canceled` in `ALL_STATUSES`/color maps. Add status + color/priority + exclude from on-lot/queue counts, keep record. Pairs with `5fbfa299`. |

### Scheduling/Dates & Sorting

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `d1844e46` | Lynn | **S** | Sort dash by promised date then oldest on lot, shop-ops pinned | Board uses one blended `calculatePriority` score; shop-ops pinning exists. Needs a selectable strict promised-then-days-on-lot sort. |

### Check-in & RAF

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5fec3eca` | Sofia | **S** | Show customer address on the emailed RAF copy | `send-quote-email` `raf_copy` branch omits `customerAddress` (only on staff email). Add address to the payload + template. |

### Auth & cost

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `ed1b873e` | Roland | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | Auth still Google id-token + email RBAC (`auth.js`). Swap to Supabase password/magic-link or shared roles; touches every page sign-in + RBAC. Roland wants to scope together. |

### UI/Navigation

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `29654af7` | Lynn | **S** | Back-to-top / quick-to-search button after scrolling | `index.html` has only targeted `scrollIntoView`; no page-level scroll-to-top FAB. Small add. |

### Time clock

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `e06d747e` | solar | **S** | Tech check-in "float" for ease of access | No floating check-in button. Underspecified -- scope page + behavior with the submitter before build. |

---

## Data corrections (no code)

| ER | By | Verdict / SQL |
|---|---|---|
| `3f254929` | Sofia | Keep "Steve Huebert", remove the misspelled "Hubert" RO. Find: `SELECT id,customer_name,status FROM repair_orders WHERE customer_name ILIKE '%hubert%';` then soft-delete the wrong RO via the dashboard after Roland confirms the id. The merge *feature* is `0e172b2c`. (unreviewed) |
| `6938c1af` | Ryan | Wimbles -> Wombles tech-name fix; already applied (S117). If any remain: `UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` (already done) |

---

## Duplicates (fold + keep declined)

| ER | Folds into | Note |
|---|---|---|
| `aa508852` | `fd6c122d` | Identical work-list text; canonical now Done. |
| `86bbaf50` | `b19975d4` | Gordon/Vogt supplier return; canonical is Needed/L. |
| `67daba15` | `d2561e11` | RO pickup/completion date; canonical now Done. |
| `fa1c1d6e` | `a22739b9` | Sheldon Gilbert no-email; canonical now Done. |
| `5057c7fc` | `91a03260` | Remove cashed-out RO from worklist; canonical now Done. |
| `fc6b29f9` | `298e0e67` | Returning-customer duplicate RO; canonical now Done. |

---

## Delivered / closed (Done bucket -- 47, condensed)

All verified present in v1.476 code. Grouped by area with the mapped module:

- **RO fields / status / dates:** `75124b03` warranty RO type (index.html/render.js/ro-crud.js) - `a5ff3e2d` Warranty+Repair type - `b87eb2fb` / `34fc03c2` keys/keypad chips - `b2aa4bea` / `468aa376` planned_dropoff_date - `d2561e11` pickup_date chip - `3dfc0acb` Ready-to-Work status - `dac9fdda` Off-Lot-Returning status - `9b046808` Scheduled excluded from on-lot - `3f7fb4fc` shop RO static status + excluded from counts - `cdd77a8b` on-lot vs total sub-line - `1aeb3f58` promised-weighted priority - `b06a285f` key-dates hover pop - `80390f36` shop-ops pinned + searchable.
- **Work list / notifications / calendar:** `18611eee` / `fd6c122d` work-list view/delete/drag - `98bd7498` per-silo work-list tabs - `cbc70a86` auto-add-to-worklist on check-in - `91a03260` prune cashiered from worklist - `9b823d25` service-added notify - `a7d1474e` urgent-update field + notify - `17492066` person-ahead-of-date reminder - `3f415c88` recurring reminders - `cd084233` admin PnL/worklist report - `2b814250` / `cb7742a8` RO -> calendar sync - `7f2d0d70` Monday drop-off report.
- **Check-in / RAF:** `dde90d2b` / `a22739b9` RAF email forced on every check-in - `298e0e67` / `5c45bb8d` reuse Scheduled RO (no duplicate) - `0b2e128c` transfer clock-out notes to RO - `13e1cd87` Return-to-Dashboard after clock-out.
- **Parts / photos / files / time-off / WO:** `a8e90a7d` / `671fa623` ticket totals + freight - `cd663d8f` 2-decimal money display - `2c3d5633` iOS photo upload fix - `74a33621` arrow-key lightbox - `39b75fab` delete/move mis-filed docs - `ee50d11c` edit time-off entries - `88395acc` time-off start/return times - `e27311bf` WO template drag-reorder - `1fe68261` shop-tasks - `79b2c805` reactivate cashiered RO to add docs.
- **Test entries (declined):** `33750cb3`, `19dc2016`.

---

## Recommended action today

1. **Cheap, unblocked S wins** ready to build now (no submitter input needed):
   `972af03c` (Canceled status), `29654af7` (back-to-top button), `5fec3eca`
   (address on RAF copy), `d1844e46` (strict promised-then-lot sort). These four
   are small, self-contained, and fit the Phase-2b auto-fixable size envelope
   the closest of anything open.
2. **Unblock the two detail-blocked S's** by asking submitters for specifics:
   Lynn's parts field order (`8d885331`) and solar's check-in float
   (`e06d747e`).
3. **Pair the two data-integrity M's** (`5fbfa299` archive + `972af03c`
   Canceled) -- they share the status/flag + filter machinery.
4. **Emit the data SQL** for `3f254929` (Hubert/Huebert) for Roland to run; the
   general account-merge feature is the separate `0e172b2c`.
5. **Plan, don't rush, the XL's:** auth swap (`ed1b873e`) and parts inventory
   (`14f35f95`) each deserve their own scoping session.
6. Nothing new to close as Done/Duplicate this pass -- the Done/Duplicate rows
   are already `done`/`declined` in the DB.
