# PRVS Enhancement-Request Triage -- 2026-07-08 (Phase 2a nightly runner)

**Source:** `enhancement_requests` snapshot `er_input.json`, fetched
2026-07-08T09:27Z; **incremental pass** (`full_pass=false`).
**Codebase mapped against:** current `pre-prod` tree at **index.html v1.471**
(customer-checkin.html v1.15, checkin.html, time-off.html v1.5).
**Total ERs:** 64 -- **63 triaged this run** (`needs_triage_ids`) + 1 carried
over unchanged (`f73a1e5a`, a declined test entry).
**Real requests:** 64 minus 3 test entries (`33750cb3`, `19dc2016`, `f73a1e5a`)
and 6 duplicates -> **55 real requests**.

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `supabase/functions/*`, `css/dashboard.css`, SQL
migrations) by grepping/reading the source -- not assumed.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session -
**XL** = new subsystem.

## Board snapshot (by DB status)

| Status | Count |
|---|---|
| done | 45 |
| declined | 9 |
| unreviewed | 10 |
| **Total** | **64** |

## Triage buckets (63 triaged this run)

| Bucket | Count |
|---|---|
| done (already delivered) | 46 |
| needed (open enhancement) | 10 |
| duplicate | 6 |
| data (SQL, not code) | 1 |

**Headline:** the **bug bucket is empty** -- every prior bug report maps to a
fix already in the tree, re-verified against v1.471. **10 open Needed ERs**
remain (all `unreviewed`), an unchanged set except that **three items closed
since the 06-28 report shipped**: `1fe68261` (Shop Tasks, v1.467), `cbc70a86`
(auto-add to work list, checkin v1.14), and `7f2d0d70` (Monday drop-off report,
`send-dropoff-report`). Two ERs new since that report -- `5fbfa299` (archive
quote-only customers) and `29654af7` (back-to-top button) -- enter as Needed.
The open mix is now **2 XL - 4 L - 1 M - 3 S**.

## Code checks run this session (v1.471)

- Parts inventory / in-house stock -- **none** in `js/parts.js` (confirms `14f35f95`).
- Parts return-to-supplier / warranty-return -- **none** (confirms `b19975d4`).
- Per-customer / per-RO task-tracker table -- **none**; `js/shop-tasks.js` is
  deliberately non-customer (confirms `af14b3cb`, `5925fc01`).
- DB-native auth path -- **none**; `js/auth.js` is Google id-token only, no
  `signInWithPassword`/magic-link (confirms `ed1b873e`).
- Remote e-signature flow -- **none**; RAF is kiosk-only (confirms `0448de62`).
- Back-to-top / scroll-to-search control in `index.html` -- **none** (confirms `29654af7`).
- Quote-only "lost/archive" disposition -- **none**; `archiveROInSupabase`
  routes to Cashiered (confirms `5fbfa299`).
- Newly-shipped since 06-28, re-verified present: `shop-tasks.js` +
  `shop_tasks` migration (`1fe68261`); `customer-checkin.html` auto-add to work
  lists (`cbc70a86`); `send-dropoff-report` edge fn (`7f2d0d70`);
  `sync-ro-calendar` edge fn (`cb7742a8`, `2b814250`); shop-RO count/status
  guards in `render.js` (`3f7fb4fc`).

---

## NEEDED bucket -- the 10 open ERs (by theme, then size)

### Notifications & Task-Tracking

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `5925fc01` | Lynn | 2026-04-09 | **L** | Notify the right team on every RO update; daily digest of in-progress jobs with to-dos scraped from notes | **PARTIAL** -- daily digest served by the Manager Daily Report (`send-admin-pnl-report`/`send-manager-report`). Still open: per-RO notify on EVERY update + auto-scraping to-dos into a per-job task list; no per-RO tasks table. Prefer needed over done. |
| `af14b3cb` | Lynn | 2026-05-21 | **L** | Per-customer task tracker + daily reminder; tasks persist until marked complete | Not built. `js/shop-tasks.js` is non-customer, so it does not satisfy this. Needs a per-RO tasks table + add/complete UI + daily open-task reminder. Pairs with `5925fc01`. |

### Parts & Inventory

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `14f35f95` | Lynn | 2026-04-09 | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | Confirmed NOT built -- `js/parts.js` has zero stock/inventory handling. New subsystem: inventory table + receive/consume/adjust + searchable priced stock + parts-request links. Largest item on the board. |
| `b19975d4` | Lynn | 2026-04-09 | **L** | Part returned-to-supplier / warrantied-by-supplier tracking | Confirmed NOT built -- only a free-text `warranty_period`, no reverse flow. = GH#24 Shop Ops Phase 2 (Parts Returns). Canonical for declined dup `86bbaf50`. |
| `8d885331` | Lynn | 2026-04-09 | **S** | Rework the parts-entry field order | Layout tweak to `js/parts.js` `pf_` fields. Quick once Lynn gives the desired order/mockup. **BLOCKED on detail, not effort.** |

### Auth & Cost

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `ed1b873e` | Roland | 2026-06-07 | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | Confirmed open -- `js/auth.js` is Google id-token + email-keyed RBAC, no password/magic-link path. Options: Supabase email/password, magic-link, or shared role accounts. Security-critical; touches every page's sign-in + RBAC. Scope together. |

### Messaging & e-sign

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `0448de62` | Sofia | 2026-06-11 | **L** | Send an authorization form remotely for e-signature | Confirmed open -- RAF is kiosk-only (`customer-checkin.html #signatureCanvas`). Needs a tokenized public signing page + delivery + signed-form storage. Pairs with Project Blue messaging Phase 3. |

### RO fields & status

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `5fbfa299` | Brandon | 2026-06-30 | **M** | Archive quote-only customers/units that never moved to work | Confirmed NOT built -- `archiveROInSupabase` (`js/ro-crud.js`) routes to Cashiered, misclassifying un-worked quotes as completed P&L jobs. Needs a separate lost/quoted-only disposition + board filter. Prefer needed over done. |

### Time Clock / Check-In

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `e06d747e` | Riley (solar) | 2026-04-10 | **S** | Tech check-in "float" for ease of access | Underspecified -- likely a floating/sticky check-in button, but page + behavior unclear (`checkin.html` is QR/roId-driven). Needs scoping with the submitter. |

### Navigation & UI

| ER | By | Date | LOE | Request | Verdict (mapped code) |
|---|---|---|---|---|---|
| `29654af7` | Lynn | 2026-07-06 | **S** | Back-to-top / jump-to-search button after scrolling | Confirmed NOT built -- `index.html` has no back-to-top control (only `scrollToROInBoard`-style helpers). Small, self-contained UI add. Newest ER (07-06). |

---

## BUG bucket

**None still open.** Every prior bug report maps to a fix confirmed in v1.471 --
see the DONE bucket (`18611eee`, `fd6c122d`, `13e1cd87`, `cd663d8f`, `2c3d5633`,
`298e0e67`, `ee50d11c`, `a22739b9`, `cb7742a8`).

---

## DATA bucket (SQL, Roland runs it)

| ER | By | Date | Request | SQL / status |
|---|---|---|---|---|
| `6938c1af` | Ryan | 2026-05-06 | Staff last name Wimbles -> Wombles (Zak, Rod, Travis) | `UPDATE staff SET last_name='Wombles' WHERE last_name='Wimbles';` -- **already applied** (status done). No `Wimbles` string in `js`. |

---

## DUPLICATE bucket (fold into canonical; recommend declined)

| ER | By | Date | Folds into | Note |
|---|---|---|---|---|
| `aa508852` | Riley | 2026-04-09 | `fd6c122d` | Same work-list phone/drag-drop report; canonical delivered (touch drag-drop v1.460). |
| `86bbaf50` | Lynn | 2026-05-13 | `b19975d4` | Parts return-to-supplier; canonical = GH#24 Shop Ops Phase 2 (still Needed). |
| `67daba15` | Brandon | 2026-06-09 | `d2561e11` | Completion / pick-up date; canonical delivered. |
| `5057c7fc` | Roland | 2026-06-09 | `91a03260` | Remove cashed-out RO from worklist; canonical delivered. |
| `fa1c1d6e` | Sofia | 2026-06-09 | `a22739b9` | Returning check-in email not sent; canonical bug fixed. |
| `fc6b29f9` | Sofia | 2026-06-17 | `298e0e67` | Returning check-in duplicated RO; canonical bug fixed. |

---

## DONE bucket (close these ERs -- recommend done, except test/dup notes)

| ER | By | Date | Request | Evidence (current code) |
|---|---|---|---|---|
| `33750cb3` | Roland | 2026-04-09 | "Testing Make-A-Wish" | Test entry (already declined). |
| `19dc2016` | Roland | 2026-04-11 | "Testing ER form / dictation" | Test entry (already declined). |
| `79b2c805` | Sofia | 2026-04-09 | See/edit cashed-out customers, add documents | Delivered/Cashed Out board filter keeps closed ROs editable (`index.html`). |
| `75124b03` | Ryan | 2026-04-09 | Warranty category on new RO | Warranty RO type (`js/render.js` roType 'warranty'); extended by `a5ff3e2d`. |
| `b2aa4bea` | Lynn | 2026-04-09 | Date for scheduled future drop | `planned_dropoff_date` captured (`js/ro-crud.js`); drives drop-off reminder. |
| `18611eee` | Lynn | 2026-04-09 | Work list opened but couldn't view/manage | Fixed -- `js/work-list.js` full view/manage (add/view/reorder/remove). |
| `17492066` | Lynn | 2026-04-09 | Future coordination reminders (Jesus example) | `scheduled_notifications` cron + `3f415c88` multi-day reminders. |
| `fd6c122d` | Riley | 2026-04-09 | Can't delete/drag jobs; too small on phone | Fixed v1.460 -- touch drag-drop + delete (`js/work-list.js`). Canonical for `aa508852`. |
| `98bd7498` | Brandon | 2026-04-09 | Different work lists per category | v1.305 silo tabs + `service_silo` on `manager_work_lists`. |
| `1fe68261` | Rod | 2026-04-13 | Tasks not tied to a customer unit | Delivered v1.467 -- `js/shop-tasks.js` + `shop_tasks` migration. Verified. |
| `3dfc0acb` | Lynn | 2026-04-14 | Add "Ready to Work" status | `js/config.js` STATUS_PROGRESS_MAP ('Listo para Trabajar'). |
| `b87eb2fb` | Lynn | 2026-04-14 | Keep-plugged-in / keys / keypad code | `js/ro-crud.js` key_status/keypad_code/keep_plugged_in. |
| `a5ff3e2d` | Lynn | 2026-04-21 | RO type Warranty + Repair | v1.465 roType 'warranty_repair' (`js/render.js`), no forced $0. |
| `a7d1474e` | Lynn | 2026-04-23 | Always-visible urgent-updates field w/ notify | v1.466 urgent-update banner (`js/render.js` + `.urgent-update-banner`). |
| `dde90d2b` | Roland | 2026-04-27 | Streamline check-in; ensure email sends | Check-in streamlined + email restored (see `a22739b9`); photo deferred. |
| `13e1cd87` | Tipton | 2026-04-30 | Back button after clock-out | Fixed -- Return-to-Dashboard button (`checkin.html` v1.34). |
| `e27311bf` | Ryan | 2026-05-04 | Edit templates / drag-drop reorder | `js/work-orders.js` `_initWOTaskDrag`, DOM order saved. |
| `d2561e11` | Brandon | 2026-05-06 | Completion / pick-up date | `pickupDate` RO key date (`js/render.js`). Canonical for `67daba15`. |
| `dac9fdda` | Brandon | 2026-05-06 | Status for units temporarily taken | v1.460 'Off Lot - Returning' (`js/config.js`), excluded from on-lot count. |
| `a8e90a7d` | Brandon | 2026-05-20 | Parts + freight total in manage parts | `js/parts.js:152-161` totWholesale + totFreight = totCost. Extended by `671fa623`. |
| `1aeb3f58` | Lynn | 2026-05-21 | Promised date large/highlighted, sorted | Large/red promised date + hover panel (`js/render.js`). NOTE: sort weights days-on-lot primary (`js/utils.js`); visible ask delivered. |
| `cbc70a86` | Lynn | 2026-05-21 | Returning check-in auto-adds to team work lists | Delivered -- `customer-checkin.html` v1.14 auto-adds involved silos. Verified. |
| `3f415c88` | Lynn | 2026-05-26 | Multi-day / weekly reminders | `scheduled_notifications` weekly-until-date (`index.html`). |
| `cd663d8f` | Bobby | 2026-05-28 | 425.00 shows as 425 (currency rounding) | Fixed v1.447 -- `editDollarValue toFixed(2)` (`js/ro-crud.js`). |
| `34fc03c2` | Brandon | 2026-05-29 | Confirm keys left with unit | key_status/keypad_code on RO (see `b87eb2fb`). NOTE: on dashboard RO, not RAF yet. |
| `0b2e128c` | Lynn | 2026-06-02 | Clock-in job notes -> RO updates | v1.460 `checkin.html` clockOut copies notes into RO timeline. |
| `468aa376` | Lynn | 2026-06-02 | Scheduled drop date in entry dash | `planned_dropoff_date` on New RO form (`index.html`). Related `b2aa4bea`. |
| `2b814250` | Roland | 2026-06-07 | RO dates -> business calendar + notify | `sync-ro-calendar` edge fn (server-side) + `scheduled_notifications`. |
| `cd084233` | Roland | 2026-06-07 | Admin-level manager worklist report | `send-admin-pnl-report` edge fn (per-manager WTD cost vs WO value). Verified. |
| `a22739b9` | Sofia | 2026-06-08 | Check-in email + auth-form copy not sent | Fixed S97 -- `customer-checkin.html` v1.10/v1.11 sendRAFEmail. |
| `91a03260` | Roland | 2026-06-09 | Remove cashed-out RO from worklist next day | Cashiered ROs excluded from active work list (`isActiveROForWoNudge`). Spot-check the daily cron. |
| `cdd77a8b` | Brandon | 2026-06-10 | RVs on lot vs total customers | v1.453 'N on lot' sub-line (`js/render.js` statTotalOnLot). |
| `2c3d5633` | Ryan | 2026-06-12 | Can't upload photos from phone | Fixed v1.453 -- input attached to DOM before click (`js/photos.js`). |
| `74a33621` | Brandon | 2026-06-16 | Arrow-key photo navigation | v1.454 lightbox ArrowLeft/Right -> navigateLightbox (`js/photos.js`). |
| `298e0e67` | Sofia | 2026-06-16 | Returning check-in duplicates RO | Fixed -- duplicate guard reuses scheduled-drop RO (`customer-checkin.html`). Canonical for `fc6b29f9`. |
| `9b823d25` | Roland | 2026-06-16 | Notify silo manager on added service | v1.460 `addServiceToRO` queues `scheduled_notifications` (`js/work-orders.js`). |
| `9b046808` | Brandon | 2026-06-17 | Status "scheduled to arrive on lot" | 'Scheduled' disposition excluded from on-lot count (`js/render.js`). See `dac9fdda`. |
| `671fa623` | Brandon | 2026-06-18 | Include freight in manage-parts totals | `js/parts.js:153` totFreight summed into totCost. Related `a8e90a7d`. |
| `5c45bb8d` | Sofia | 2026-06-19 | Returning check-in: no email + duplicated | Both fixed (see `298e0e67` + `a22739b9`). |
| `b06a285f` | Lynn | 2026-06-19 | Key dates by name, hover to view | v1.465 calendar hover panel (`js/render.js` .kd-name-cal). |
| `ee50d11c` | Lynn | 2026-06-19 | Can't edit time-off entries | Fixed v1.4 -- `time-off.html` editRequest UPDATEs the record. |
| `88395acc` | Lynn | 2026-06-19 | Time-off exact absence window | v1.5 absence_start_time/absence_end_time (`time-off.html`). |
| `80390f36` | Lynn | 2026-06-23 | Pin Shop Operations tile + searchable | v1.462 pinned to top + findable in search. |
| `7f2d0d70` | Lynn | 2026-06-26 | Monday drop-off report | Delivered -- `send-dropoff-report` edge fn (this week in + last week reconciliation). Verified. |
| `3f7fb4fc` | Brandon | 2026-06-29 | Remove shop-RO status; exclude from counts | v1.468 static status + count guards (`js/render.js`). Verified. |
| `cb7742a8` | Lynn | 2026-06-29 | Scheduled date not on team calendar | Fixed v1.468/v1.469 -- `sync-ro-calendar` server-side (works from kiosk). Verified. |

*(Carried over, not re-triaged: `f73a1e5a` -- Roland test entry "Testing
dictation 123", 2026-04-13, already declined; no bucket assigned.)*

---

## Recommended action today

1. **Nothing to build blind.** Of the 10 open ERs, the two S's are the fastest
   *once specified*: `8d885331` (parts field order) and `e06d747e` (check-in
   float) are both **blocked on submitter detail, not effort** -- ask Lynn and
   Riley for specifics.
2. **Quick standalone win:** `29654af7` (back-to-top button) is a small,
   self-contained UI add with no dependencies -- the most shippable open item.
3. **Scoped M:** `5fbfa299` (quote-only archive disposition) is well-defined and
   avoids polluting Weekly P&L -- a good next design target.
4. **Plan, don't rush, the XL's:** auth swap (`ed1b873e`) and parts inventory
   (`14f35f95`) each deserve their own scoping/spec session.
5. **Cluster the L's:** the Notifications/Task-Tracking pair (`5925fc01`,
   `af14b3cb`) share a per-RO tasks table and should be designed together;
   `0448de62` (remote e-sign) pairs with the Project Blue messaging track;
   `b19975d4` (parts returns) is GH#24 Shop Ops Phase 2.
6. **Close-outs for Roland:** 46 Done + 6 Duplicate verdicts are ready to flip
   (`done` / `declined`); the `6938c1af` data fix is already applied. Roland
   flips status himself.
