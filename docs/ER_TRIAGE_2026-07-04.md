# PRVS Enhancement-Request Triage -- 2026-07-04 (nightly headless run)

Incremental re-triage of the `enhancement_requests` table against the current
modular codebase (index.html **v1.469**). Headless runner, `full_pass=false`:
62 of 63 rows re-classified (1 carried over unchanged). Supabase writes are NOT
performed by this runner -- Roland flips `status` himself. Verdicts verified by
grepping/reading the live code, not assumed.

## Board snapshot

| Status | Count |
|---|---|
| done | 45 |
| declined | 9 |
| unreviewed | 9 |
| **Total** | **63** |

**9 open** (all `unreviewed`). Since the 2026-06-28 pass (11 open), three items
shipped -- `cbc70a86` (auto-add to work list on check-in, customer-checkin.html
v1.14 S128), `7f2d0d70` (Monday drop-off report, send-dropoff-report S128), and
`1fe68261` (non-customer shop task tracker, js/shop-tasks.js S128) -- and one new
request arrived (`5fbfa299`, archive quote-only customers). Every open ER is a
valid **Needed** enhancement -- no still-live bugs, nothing already-shipped left
to close. LOE mix of the 9 open: **2 XL - 4 L - 1 M - 2 S**.

## Code checks run this pass

- parts inventory / stock in `js/parts.js` -- **none** (confirms `14f35f95` not built)
- supplier return / warranty workflow in `js/parts.js` -- **none**; only a
  `warranty_period` text field exists (confirms `b19975d4` not built)
- per-customer task tracker -- **none**; `js/shop-tasks.js` is deliberately
  NON-customer, so it does not satisfy `af14b3cb`
- archive path (`ro-crud.js archiveROInSupabase`) -- routes to **cashiered**,
  which misclassifies un-worked quotes as completed P&L (confirms `5fbfa299`)
- remote e-signature flow -- **none**; RAF is kiosk-only (confirms `0448de62`)
- auth model (`js/auth.js`) -- Google id-token + email-keyed RBAC, unchanged
  (confirms `ed1b873e` still open)

---

## Open ERs -- the 9 `unreviewed` (by theme, then size)

### Notifications & Task-Tracking

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5925fc01` | Lynn | **L** | Notify the right team on every RO update; daily comprehensive list of in-progress jobs with to-dos scraped from status notes | **PARTIAL** -- the daily-digest half is served now by the Manager Daily Report (send-manager-report). Still open: per-update notification + auto-scraping to-dos from status notes into a per-job list. Anchor of the initiative. |
| `af14b3cb` | Lynn | **L** | Per-customer task tracker + daily reminder; tasks persist until marked complete | Not built. `shop-tasks.js` is NON-customer by design. Needs a per-RO tasks table + add/complete UI + daily open-task reminder. Same theme as `5925fc01`. |

### Parts & Inventory

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `14f35f95` | Lynn | **XL** | In-house parts inventory (order 25, use 5, 20 to searchable priced stock) | New subsystem. Nothing in `js/parts.js` handles stock. Needs an inventory table + receive/consume/adjust + searchable stock view + parts-request links. Largest item on the board. |
| `b19975d4` | Lynn | **L** | Part returned-to-supplier / warrantied-by-supplier tracking | No supplier-return/warranty flow (only a `warranty_period` text field). = GH#24 Shop Ops Phase 2: reverse flow + return reasons + credit/warranty tracking. Canonical for declined dup `86bbaf50`. |
| `8d885331` | Lynn | **S** | Rework the parts-entry screen field order | Layout tweak to the `js/parts.js` form. Quick once Lynn gives the desired order/mockup. **BLOCKED on detail, not effort.** |

### RO fields/status

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `5fbfa299` | Brandon | **M** | Archive customers/units that were only quoted and never moved forward | Not built. Archiving today routes to Cashiered (`ro-crud.js archiveROInSupabase`), misclassifying un-worked quotes as completed P&L jobs. Needs a separate lost/quoted-only disposition + board filter. **Newest ER (06-30).** |

### Messaging & e-sign

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `0448de62` | Sofia | **L** | Send an authorization form remotely for e-signature | No remote-signing flow (RAF is kiosk-only). Needs a tokenized public signing page + delivery (email/SMS) + signed-form storage. Pairs with Sendblue Phase 3. |

### Auth & cost

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `ed1b873e` | Roland | **XL** | Replace Google-email auth (~$500/mo) with DB-specific auth | Security-critical swap of the id-token + email-RBAC model (`js/auth.js`). Options: Supabase password/magic-link or shared role accounts. Touches every page's sign-in + RBAC. Roland wants to scope together. |

### Time clock

| ER | By | LOE | Request | Verdict |
|---|---|---|---|---|
| `e06d747e` | Riley | **S** | Tech check-in "float" for ease of access | Underspecified -- likely a floating/sticky check-in button, but page + behavior unclear (checkin.html is QR/roId-driven today). Needs scoping with the submitter. |

---

## Delivered (Done) -- 44 ERs, verified in current code

| ER | By | Request | Evidence in current code |
|---|---|---|---|
| `79b2c805` | Sofia | Edit cashed-out customers to add documents | Cashiered/Delivered board filter keeps ROs viewable/editable |
| `75124b03` | Ryan | Warranty category on new RO | Warranty RO type, index.html:690 |
| `b2aa4bea` | Lynn | Scheduled future drop date | `planned_dropoff_date` + drop-off reminder (ro-crud.js) |
| `18611eee` | Lynn | Work-list view/manage broken (bug) | Fixed -- full view/manage in js/work-list.js |
| `17492066` | Lynn | Future coordination reminders | scheduled_notifications + `3f415c88` multi-reminders |
| `fd6c122d` | Riley | Work list too small / no drag-drop on phone (bug) | Fixed -- touch drag-drop v1.460; delete + reorder in work-list.js |
| `98bd7498` | Brandon | Separate work lists per category | Silo-specific work lists v1.305 |
| `1fe68261` | Rod | Non-customer shop tasks | js/shop-tasks.js S128 (shop_tasks table) |
| `3dfc0acb` | Lynn | Status "Ready to Work" | config.js:132 |
| `b87eb2fb` | Lynn | Keep-plugged-in sign + keys/keypad code | RO fields, cited 16x |
| `a5ff3e2d` | Lynn | Warranty + Repair RO type | v1.465 S127, index.html:691 |
| `a7d1474e` | Lynn | Always-visible urgent-updates field + notify | cited 21x |
| `dde90d2b` | Roland | Fix/streamline check-in + ensure email sends | Check-in streamlined; email restored (`a22739b9`) |
| `13e1cd87` | Tipton | Back button after clock-out (bug) | Return-to-Dashboard checkin.html v1.34 (v1.447) |
| `e27311bf` | Ryan | Editable templates, drag-drop reorder | cited 6x (work-orders.js) |
| `d2561e11` | Brandon | Completion / pick-up date | Key date, cited 12x (canonical for dup `67daba15`) |
| `dac9fdda` | Brandon | Temporarily-off-lot status | "Off Lot - Returning" v1.460, config.js:128 |
| `a8e90a7d` | Brandon | Parts + freight total in manage parts | parts.js L148-153 |
| `1aeb3f58` | Lynn | Promised date highlighted + sorted | Key-date render/sort in render.js |
| `cbc70a86` | Lynn | Auto-add to work list on returning check-in | customer-checkin.html v1.14 S128 (line 2304) |
| `3f415c88` | Lynn | Multi-day / weekly reminders | scheduled_notifications, cited 7x |
| `cd663d8f` | Bobby | Currency drops .00 (bug) | Fixed -- 2-decimal render v1.447 S94 |
| `34fc03c2` | Brandon | Confirm keys left with unit | Check-in/auth capture, cited 16x |
| `0b2e128c` | Lynn | Clock-in job notes -> RO updates | v1.460 S120 clockOut -> timeline |
| `468aa376` | Lynn | Scheduled drop date in entry dash | `planned_dropoff_date` (ro-crud.js) |
| `2b814250` | Roland | Key dates -> business calendar + notices | sync-ro-calendar edge fn + scheduling.js |
| `cd084233` | Roland | Admin daily manager-focused report | send-admin-pnl-report edge fn |
| `a22739b9` | Sofia | Check-in email / auth-form copy missing (bug) | Fixed -- shared New-Customer email path (v1.461) |
| `91a03260` | Roland | Remove cashed-out RO from worklist next day | Cashiered/Delivered excluded from active work list (spot-check auto-remove) |
| `cdd77a8b` | Brandon | On-lot vs total-customers count | Header count split, cited 6x |
| `2c3d5633` | Ryan | Phone photo upload (bug) | Fixed -- photos.js upload path, cited 4x |
| `74a33621` | Brandon | Arrow-key photo navigation | photos.js lightbox, cited 4x |
| `298e0e67` | Sofia | Returning-check-in duplicates RO (bug) | Fixed -- check-in reconciliation (canonical for `fc6b29f9`) |
| `9b823d25` | Roland | Email silo manager when service added | v1.460 S120, service_added_notify |
| `9b046808` | Brandon | Scheduled-to-arrive status | Scheduled disposition + on-lot exclusions, cited 5x |
| `671fa623` | Brandon | Freight in ticket totals | parts.js freight sum |
| `5c45bb8d` | Sofia | Check-in no email + duplicated (bug) | Both fixed (see `a22739b9`, `298e0e67`) |
| `b06a285f` | Lynn | Hover panel of key dates by name | v1.465 S127 (.kd-name-cal) |
| `ee50d11c` | Lynn | Edit time-off entries (bug) | Fixed -- time-off.html editable, cited 6x |
| `88395acc` | Lynn | Exact partial-day time-off window | time-off.html v1.5, cited 11x |
| `80390f36` | Lynn | Pin + findable Shop Operations tile | Pinned + searchable, cited 5x |
| `7f2d0d70` | Lynn | Monday drop-off report | send-dropoff-report edge fn S128 |
| `3f7fb4fc` | Brandon | Remove shop-RO status dropdown / counts | v1.469 S129, cited 5x |
| `cb7742a8` | Lynn | Check-in date not on team calendar (bug) | Fixed -- sync-ro-calendar server-side S129, cited 14x |

## Data corrections

| ER | By | Correction | SQL (Roland runs; already applied, status=done) |
|---|---|---|---|
| `6938c1af` | Ryan | Staff last name Wimbles -> Wombles (Zak, Rod, Travis) | `UPDATE staff SET last_name='Wombles' WHERE last_name='Wimbles';` (names live in the staff table, not code) |

## Declined -- duplicates + test entries (9)

| ER | By | Reason |
|---|---|---|
| `86bbaf50` | Lynn | Duplicate of `b19975d4` (part return to supplier) |
| `67daba15` | Brandon | Duplicate of `d2561e11` (completion / pick-up date) |
| `aa508852` | Riley | Duplicate of `fd6c122d` (work list phone drag/delete) |
| `fa1c1d6e` | Sofia | Duplicate of `a22739b9` (check-in email missing) |
| `fc6b29f9` | Sofia | Duplicate of `298e0e67` (returning check-in duplicated RO) |
| `5057c7fc` | Roland | Duplicate of `91a03260` (remove cashed-out RO from worklist) |
| `33750cb3` | Roland | Test entry (Make-A-Wish trial) |
| `19dc2016` | Roland | Test entry (ER form / dictation) |
| `f73a1e5a` | Roland | Test entry (dictation) -- carried over unchanged |

---

## Recommended action today

1. **Ship the two smallest once specified.** `8d885331` (parts field order) and
   `e06d747e` (check-in float) are both **S**, both blocked only on submitter
   detail -- ask Lynn for the field order and Riley for the exact float
   behavior, then they are quick.
2. **`5fbfa299` (archive quote-only, M) is the most shippable net-new item** --
   a distinct lost/quoted-only disposition + board filter, so quotes stop
   polluting Weekly P&L. Well-scoped and self-contained.
3. **Design the Notifications/Task-Tracking pair together** -- `5925fc01`
   (per-update notify + note-scraped to-dos) and `af14b3cb` (per-customer task
   tracker) share a per-RO tasks table; build once.
4. **Plan, don't rush, the XLs** -- the auth swap (`ed1b873e`) and parts
   inventory (`14f35f95`) each deserve their own scoping/spec session.
5. Nothing new to close as Done/Duplicate this pass; the board is all real work.
