# Enhancement Request Triage -- 2026-06-10 (Phase 2a nightly runner)

**Source:** `enhancement_requests` table (snapshot `er_input.json`, fetched 2026-06-10T10:58Z; incremental pass).
**Codebase mapped against:** current `pre-prod` tree at **v1.447** (checkin.html v1.34, customer-checkin.html **v1.11**).
**Total ERs:** 45 (incl. 3 test entries + 3 exact/fold duplicates -> **~38 real requests**).
**Triaged this run:** 44 ids in `needs_triage_ids` (every row except the n15 test entry, carried over unchanged).

Each ER was mapped to current code (`js/*.js`, `index.html`, `checkin.html`,
`customer-checkin.html`, `closed-ros.html`, `worklist-report.html`) by grepping
the source, not assuming, and sorted into:

- DONE -- already delivered; ER can be closed.
- BUG -- a defect that still exists in current code.
- NEEDED -- valid open enhancement, with a level-of-effort (LOE) estimate.
- DATA -- a data correction (SQL), not code.
- DUPLICATE -- folded into the canonical ER.

LOE key: **S** = <2h - **M** = 1/2-1 session - **L** = multi-session - **XL** = new subsystem.

---

## Headline

The bug bucket is empty again. The one open bug from the 2026-06-08/09 runs --
**n40** (Sofia: customer check-in stopped sending the staff email + the signed
authorization form) -- **shipped in Session 97** and is confirmed in the current
tree:

- **n40** (a22739b9) -> FIXED **v1.10/v1.11** of `customer-checkin.html`.
  `shouldNotifyStaff` (L2210) now fires on EVERY drop-off (standard New Work,
  Warranty, Hybrid; same-day and scheduled) plus New Customer Entries, so a
  `repair@` staff notice always goes out. The signed RAF copy now always sends
  (to the customer + cc `info@` when an email is given, else to `info@` alone,
  L2182-2200). **v1.11** also corrected the `getSignatureData()` typo (was
  `getSignatureDataURL()`, undefined -> ReferenceError that had aborted every
  signed-RAF email -- the real root cause). **Now DONE.**

Two new ERs from Sofia/Brandon/Roland arrived 2026-06-09 (n41-n44):

- **n42** (Sofia, returning customer "Sheldon Gilbert" got no email) is the
  **same defect as n40**, now resolved by the v1.10/v1.11 fix above (it covers
  returning-customer drop-offs). Folded into n40.
- **n41** (Brandon, completion / P-U date selection) re-states **n24**
  (same submitter) -- folded into n24, still NEEDED under Key Dates.
- **n43** (Roland) -- auto-remove an RO from a manager's work list the day after
  it is Cashiered Out / Delivered. NEEDED (Work List). **n44** is an exact
  duplicate of n43 (submitted 1 second later).

Notable correction to the prior run: **n10** (Riley, work-list usability) was
previously called "drag-drop unaddressed." Drag-and-drop reorder is in fact
**now shipped** (`work-list.js` `row.draggable` + `dragstart`/`dragover`/`drop`
+ `_saveWorkListOrder`, L304-392). The only remaining gap is phone sizing /
touch-drag (HTML5 DnD has no touch handler). Downgraded to a small mobile polish.

---

## BUG bucket (front of the line)

None still open. The one prior bug (n40) shipped in customer-checkin.html
v1.10/v1.11 -- see Headline.

---

## DONE bucket (close these ERs)

| # | Submitter | Request | Evidence (current code) | Recommend |
|---|---|---|---|---|
| n0 | Roland | "Testing Make-A-Wish" | Test entry. | declined (already) |
| n14 | Roland | "Testing ER form / dictation" | Test entry. | done (already) |
| n15 | Roland | "Testing dictation 123" | Test entry (carried over; not re-triaged). | done (already) |
| n1 | Sofia | See/edit cashed-out customers, add documents | `closed-ros.html` reactivation restores full RO detail (parts/time_logs/photos) -> edit + add docs. | done (already) |
| n2 | Ryan | Warranty category on new RO | Warranty RO type shipped v1.402 (`ro-crud.js` L853 `roType=='warranty'` branch + warranty filter). | done (already) |
| n5 | Lynn | Work list opened but couldn't view/do anything | Fixed Phase 14: `work-list.js` `renderWorkList` + `removeFromWorkList` + `_setWorkListSilo`. | done |
| n12 | Brandon | Different work lists per category | Per-silo Work List tabs (`_renderWorkListSiloTabs`/`_setWorkListSilo`). | done |
| n17 | Lynn | Add "Ready to Work" RO status | Shipped v1.410 (`config.js` STATUS_PROGRESS_MAP L131 slot 50, all dropdowns). | done |
| n21 | Roland | Fix customer check-in, email out, remove photo, streamline | `customer-checkin.html` v1.11: photo optional + signed RAF auto-emailed on every drop-off. | done |
| n22 | Tipton | iPhone: no way back to dashboard after clock-out | FIXED v1.447 (`checkin.html` v1.34 Return-to-Dashboard button, `[ER BUGFIX S94] n22`). | done |
| n33 | Bobby | Amounts drop the period and the zeros (425.00 -> 425) | FIXED v1.447 (`ro-crud.js` L841 `.toFixed(2)`; `parts.js` MONEY_FIELDS L279). | done |
| n40 | Sofia | Check-in no longer emails staff / no auth-form copy | FIXED v1.10/v1.11 (`customer-checkin.html`): `shouldNotifyStaff` fires on every drop-off + RAF always sends; `getSignatureData` typo fixed. | done |

---

## DATA bucket

| # | Submitter | Request | Fix |
|---|---|---|---|
| n25 | Ryan | "Zak, Rod and Travis last name should be Wombles, not Wimbles, in tech assignment" | Data, not code. `staff.name` holds the value; not hardcoded. One SQL UPDATE (below). |

**n25 fix SQL** (Roland runs -- runner does not execute SQL):

```sql
UPDATE staff SET name = REPLACE(name, 'Wimbles', 'Wombles')
WHERE name ILIKE '%Wimbles%';
```

Historical ROs that stored the tech name as free text are a separate optional backfill.

---

## DUPLICATE bucket

| # | Submitter | Request | Canonical |
|---|---|---|---|
| n11 | Riley | "Can't delete jobs / too small on phone / drag-and-drop" | Exact duplicate of **n10** (identical text + timestamp); already declined. Fold into n10. |
| n41 | Brandon | "Selection for customer completion or P/U date to know when orders left facility" | Duplicate of **n24** (same submitter, same ask). Fold into n24; still open under Key Dates. |
| n42 | Sofia | "Returning customer (Sheldon Gilbert) checked in, no email as before" | Same defect as **n40**; resolved by v1.10/v1.11. Fold into n40 (now done). |
| n44 | Roland | "Remove cashed-out RO from manager worklist day after" | Exact duplicate of **n43** (identical text, +1s). Fold into n43. |

---

## NEEDED bucket (open enhancements + LOE)

Grouped by theme -- overlapping wishes should be designed together rather than one-off.

### Theme 1 -- Scheduling & key dates  *(n4, n24, n29, n36, n37; n41 folded in)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n4 | Lynn | Scheduled future drop-off date + email reminder to manager 1 day before | M |
| n36 | Lynn | Scheduled drop-date field in entry dashboard | S -- duplicate intent of n4 |
| n24 | Brandon | Completion / pickup date on the customer's order (no `completion_date`/`pickup_date` field in code) | M -- also covers n41 |
| n29 | Lynn | Promised date large + highlighted on tile; sort by promise date then days-on-lot | M |
| n37 | Roland | Surface RO date/time (pickup/dropoff/reminders) as a prominent colored RO button + integrate into business calendar(s) with advance notifications to managers/admins | L |

-> **"Key Dates + Calendar" initiative.** n37 is the umbrella ask that ties the
drop-off/promised/completion dates (n4/n24/n29/n36/n41) to a calendar surface and
the notifications cluster below.

### Theme 2 -- Notifications, reminders & task-tracking  *(n3, n8, n20, n30, n32, n35)* -- biggest cluster
| # | Submitter | Request | LOE |
|---|---|---|---|
| n3 | Lynn | Daily digest of all in-progress jobs with to-dos scraped from status notes | L |
| n8 | Lynn | Scheduled/urgent task notifications to silo managers + techs ahead of time | L |
| n30 | Lynn | Per-customer task tracker; tasks persist until marked complete; daily reminder | L |
| n32 | Lynn | Notification button: schedule multiple days / weekly-until-complete | M |
| n20 | Lynn | Always-visible "urgent updates" field on tile + notify-on-modify + font/color | L (urgency partly exists) |
| n35 | Lynn | Transfer clock-in job notes -> RO updates | M |

-> **Largest opportunity.** A "Task Tracker + Reminders" subsystem; overlaps the
deferred Twilio/SMS work -- design as one initiative.

### Theme 3 -- Parts  *(n6, n7, n9, n27, n28)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n28 | Brandon | Parts + freight TOTAL in Manage Parts | S -- per-part money fields exist (MONEY_FIELDS); summed total row not built. Clean quick win. |
| n9 | Lynn | Rearrange parts entry/ordering fields | S (vague) |
| n7 | Lynn | Mark part returned-to-supplier / warrantied-by-supplier | L |
| n27 | Lynn | Parts missing / ordered-and-returned (Gordon window to Vogt) | S -- fold into n7 |
| n6 | Lynn | In-house parts inventory/stock (order 25, 20 to stock, searchable) | XL -- new subsystem |

### Theme 4 -- Work List usability  *(n10, n31, n43)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n10 | Riley | Mobile: too small to adjust on phone; touch-drag (delete + desktop drag-drop reorder now shipped) | S -- mobile polish |
| n31 | Lynn | Returning check-in auto-adds customer to each involved team-lead's work list | L |
| n43 | Roland | Auto-remove an RO from a manager's work list the day after Cashiered Out/Delivered (no auto-removal in `work-list.js` today) | M |

### Theme 5 -- RO fields & status nuance  *(n18, n34, n26, n19, n16)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n18 | Lynn | "Keep plugged in" flag; keys / no-keys / keypad code | M |
| n34 | Brandon | Confirm if customer leaves keys with the unit (RO / sign-in / auth form) | S -- overlaps n18 |
| n26 | Brandon | On-lot sub-status: temporarily taken & returning vs never-on-lot | M |
| n19 | Lynn | "Warranty + Repair" combined RO type | M (clarify vs existing Hybrid) |
| n16 | Rod | Non-customer/internal shop tasks (swap poly barrels, prep roof bays) | M (partly via Shop RO type) |

### Theme 6 -- Work Orders / templates  *(n23, n13)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n23 | Ryan | Edit WO templates; drag-and-drop reorder; change order at will | M |
| n13 | Riley | Tech check-in "float" for ease of access | S (vague -- overlaps n22) |

### Theme 7 -- Authentication & accounts  *(n38)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n38 | Roland | Replace Google-email auth (~$25/mo per mostly-unused mailbox, ~$500/mo total) with database-specific auth | XL -- touches `auth.js` + Supabase auth model; needs design input |

### Theme 8 -- Reporting & analytics  *(n39)*
| # | Submitter | Request | LOE |
|---|---|---|---|
| n39 | Roland | Admin-level daily Manager-worklist report (parts/labor/costs, upcoming dates, ROs that should be in a worklist but are not) -> manager scoring + profit-margin visibility | L -- extends `worklist-report.html` |

---

## Recommended action today

1. **Close the newly-resolved ER:** **n40** (Sofia check-in email/auth-form) ->
   `status=done` -- shipped in customer-checkin.html v1.10/v1.11 (S97). Its
   duplicate **n42** (Sheldon Gilbert recurrence) -> `done`/fold into n40.
   All other DONE rows (n0/n14/n15/n1/n2/n5/n12/n17/n21/n22/n33) are already closed.
2. **Decline the duplicates:** n11 (-> n10), n41 (-> n24), n44 (-> n43).
3. **Run the n25 staff-name SQL** (Roland; data only).
4. **No code changes are queued from this run** -- the bug bucket is empty.

Standout near-term enhancement wins: **n28 (parts+freight total, S)**,
**n10 (work-list mobile polish, S)**, and a coordinated **Key Dates + Calendar**
pass (Theme 1, anchored by Roland's n37). New this cycle: **n43** (auto-clear
cashiered ROs from manager work lists, M) fits cleanly into the Work List theme.
The two strategic asks -- **n38 (auth cost reduction)** and **n39 (manager
reporting/scoring)** -- still warrant a design session with Roland before scoping.
