# PRVS Enhancement-Request Triage - 2026-07-30 (headless runner)

Incremental pass over `enhancement_requests` against the current modular
codebase on `pre-prod` (index.html **v1.485**, messages.html v1.17,
checkin.html v1.38). Input snapshot `er_input.json` fetched
2026-07-30T09:33Z: 88 rows, 87 flagged for re-triage.

Every verdict below was re-verified by grepping/reading the live code at
v1.485, not carried forward on faith. Line numbers in index.html shifted by
roughly +45 versus the v1.484 pass (the S163 sync-gate header/comment block),
so all citations here are current-file line numbers.

No code was changed, no DB write was made, no git command was run. All ER
`description` / `submitted_by_name` / `admin_notes` values were treated
strictly as data; a scan for injection markers found only prior AI-triage
notes that quote SQL for Roland to run, no attempt to instruct this runner.

## Board snapshot

| DB status | Count |
|---|---|
| done | 49 |
| declined | 9 |
| unreviewed | 30 |
| **Total** | **88** |

## Triage buckets (this pass)

| Bucket | Count |
|---|---|
| done (delivered, verified on v1.485) | 52 |
| needed (open enhancement) | 22 |
| bug (defect still present) | 3 |
| data (DB correction, no code) | 2 |
| duplicate (fold into canonical) | 8 |
| not re-triaged this pass (`f73a1e5a`) | 1 |

**Headline: 25 real open work items - 3 confirmed bugs and 22 needed
enhancements.** The LOE mix has shifted sharply small since the June pass:
**14 S - 2 M - 4 L - 2 XL**. Fourteen small items now sit in four tight
clusters (Messaging UX, Board & Navigation UX, RO fields & status, Time
Clock), which means a couple of focused sessions could close more than half
the open board. v1.485 shipped no ER work (it was the connected-badge /
Sync Gate fix), so nothing moved out of `needed` this pass.

## Code checks run this session

Confirmed **absent** (so the matching ER stays open):

- `inventory` / `on_hand` / `in_stock` / `stock_qty` across js + supabase - none (14f35f95)
- `rma` / `return_reason` / `supplier_credit` across js + supabase - none (b19975d4)
- `signing_token` / `remote_sign` / e-sign flow - none (0448de62)
- `ro_tasks` / `customer_task` / `task_tracker` - none (af14b3cb, 5925fc01)
- `backToTop` / `scroll-to-top` / `window.scrollTo` - none (29654af7, 2b78696c)
- `favicon` / `apple-touch-icon` / `rel=icon` in any repo *.html - none (d432863a)
- `sortBy` / `sortSelect` / `sortMode` - none (d1844e46)
- `LABOR_RATE` / `laborRate` / `195` in js/work-orders.js - none (2f752d40)
- `cancel` (case-insensitive) in js/config.js + js/render.js - none (972af03c)
- `font-scale` / `fontScale` / `textSize` in messages.html - none (21fc3a9d)
- `customerAddress` in the `raf_copy` branch of send-quote-email - none (5fec3eca)
- any check-in control in `compact-col-actions` (js/render.js L228-235) - none (e06d747e, 6bb61db2)

Confirmed **present** (bugs that survive on v1.485):

- js/render.js L553 + L563 - `split('\n').map(escapeHtml).reverse()` on both note boxes (d94a418a)
- index.html L4139 `scrollToROInBoard` - rendered-cards-only scan, toast at L4154 (d4d0eeba, 9031e101, 1bc33dd6)
- js/qr.js L198 `handleDeepLink` - silent return at L207 / L210 (d4d0eeba)

---

## BUGS - front of the line (3)

| ER | By | Date | Category | Severity | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `d94a418a` | Brandon | 07-27 | General UI/UX | **data-display** | js/render.js L553, L563; js/ro-crud.js note join | CONFIRMED on v1.485. Both card note boxes render with `split('\n').map(escapeHtml).reverse().join('\n')`, reversing EVERY line - so a multi-line note shows bottom-line-first, exactly the reported "latter portion near the top". Entry boundaries are lost upstream (ro-crud joins entries with `\n`). L563 does the same to Customer Communication. The S159 history modal already splits on the entry regex correctly - reuse that split for both boxes. Render-only, no data loss. |
| `d4d0eeba` | Brandon | 07-22 | General UI/UX | **small** (navigation) | index.html L4139/L4154; js/qr.js L198/L207/L210 | CONFIRMED on v1.485, two paths. Work list: `scrollToROInBoard` only scans already-rendered `[data-ro-index]` cards; otherwise it toasts, and that toast's Clear Filters action re-renders but never scrolls to the tapped RO. Messages: `handleDeepLink` returns silently when the RO is not in `currentFilteredData` or has no card. Canonical for `9031e101` and `1bc33dd6` - one fix (clear filters, re-render, scroll, highlight) closes all three. |
| `9031e101` | Riley | 07-21 | Work List | **small** (mobile nav) | js/work-list.js L328 -> index.html L4139 | CONFIRMED on v1.485. Tapping a unit in the Work List on iPhone calls the same `scrollToROInBoard`, so a filtered-off RO produces the "RO not visible" toast instead of navigation. Same root cause as `d4d0eeba`; kept as its own Bug because it is a separate submitter and surface, but it is ONE code fix. |

---

## NEEDED - 22 open enhancements by theme

### Messaging UX (4 x S - one session closes the cluster)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `6e9ca4dc` | Brandon | 07-21 | General UI/UX | **S** | js/messaging.js L222/L246; messages.html L417/L430 | Still present on desktop. `refreshSignaturePreview` writes "Added to every send:" into `#msgSigPreview` on both surfaces; messages.html hides it only inside the `max-width:760px` mobile media query, so desktop messages.html and the RO-card composer still show it. Remove it or collapse it behind a toggle in all viewports - the signature itself keeps sending. |
| `f1e6e311` | Brandon | 07-21 | General UI/UX | **S** | messages.html L469, L1001, L1020, L1059, L2144 | `convoSearch` is a plain text input with no clear affordance; it is reset only programmatically. Add a clear (x) button that empties the search and re-runs the filter. |
| `9aab7550` | Brandon | 07-21 | General UI/UX | **S** | messages.html CSS L334, markup L1095, condition L1089 | Partial. A small gold `.unread-dot` renders per conversation, but only when `last_direction='inbound'` AND `status='open'`, and there is no row-level highlight. Add a stronger unread treatment (bold row / tinted background) and widen the condition so a closed-then-reopened thread also flags. |
| `21fc3a9d` | Brandon | 07-21 | General UI/UX | **S** | messages.html (fixed rem sizing throughout) | No user text-size control exists. Add a persisted font-scale control for the conversation list. |

### Board & Navigation UX (3 x S)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `29654af7` | Lynn | 07-06 | General UI/UX | **S** | index.html / js / css - only targeted `scrollIntoView` calls | Confirmed absent. Add a scroll-triggered back-to-top / jump-to-search control, ideally present in the sidebar layout too. **Canonical for duplicate `2b78696c`** (Lynn re-raised it 07-27). |
| `d1844e46` | Lynn | 07-16 | General UI/UX | **S** | js/render.js L31-39 (blended `calculatePriority`), shop pin L35-37 | No selectable sort exists - the board sorts on one blended priority score. Add a strict promised-date-then-oldest-on-lot sort option while keeping the Shop Operations pin. Related to `1aeb3f58`, which delivered only the promised-date chip. |
| `d432863a` | Brandon | 07-21 | General UI/UX | **S** | all repo *.html `<head>` blocks | No favicon anywhere, so every tab shows the generic browser icon. Add a Patriots-logo favicon plus apple-touch-icon to each page head (logo.png ships already but needs resizing to icon dimensions). |

### RO fields & status (2 x S, 2 x M)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `972af03c` | Lynn | 07-17 | General UI/UX | **S** | js/config.js L151-164; js/render.js L741-745 | No Canceled / Cancelled status exists - both status lists end at Delivered/Cashed Out. Add a Canceled status plus color/priority maps, exclude it from the on-lot and queue counts, and keep the record for notes. Pairs with `5fbfa299`. |
| `50175fce` | Lynn | 07-27 | General UI/UX | **S** | index.html L6841, L7354, L6308; js/ro-crud.js L414/L467/L491/L1107; js/render.js L324 | `urgent_update` is writable only from the New/Edit RO forms; the card banner is read-only and even titled "change it via Edit RO". Add an inline edit-in-place control at the tile bottom, reusing the existing editField / notify path so it still fires `urgent_update_notify`. Follow-on to delivered ER `a7d1474e`. |
| `5fbfa299` | Brandon | 06-30 | General UI/UX | **M** | js/ro-crud.js L658 `archiveROInSupabase`, L692 `archived_at` | The only archive path is the cashiered one; there is no archived / abandoned flag, status or board filter for quoted-but-never-progressed units. Needs a soft-archive flag, a board filter and an un-archive path. The S161 lead-capture path keeps FUTURE quotes on the Messages board rather than as ROs, but existing quote-only ROs still need this. Pairs with `972af03c`. |
| `0e172b2c` | Sofia | 07-16 | General UI/UX | **M** | js/duplicates.js L35 `getBaseROId`, L36 regex, L40-46 `findDuplicateGroups` | Partial. An RO-level merge/delete engine exists, but it groups strictly on the `PRVS-XXXX-XXXX` base RO id, so two ROs created under different spellings of the same customer (Hubert / Huebert) are never detected. Needs name/phone account dedup plus a verify-which-is-correct merge step. The one-off cleanup for that case is data ER `3f254929`. |

### Parts & Inventory (1 x S, 1 x L, 1 x XL)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `8d885331` | Lynn | 04-09 | Parts & Ordering | **S** | js/parts.js L181, L190, L200, L204, L208, L226 | Parts-entry form exists and is unchanged. The ask is only to reorder those fields - quick once Lynn supplies the target layout or a mockup. **Blocked on detail, not on effort.** |
| `b19975d4` | Lynn | 04-09 | Parts & Ordering | **L** | js/config.js L200 `PART_STATUSES`; js/parts.js L226/L343 | Partial infra only: `Returned` is a part status and a Return Deadline input exists, but there is no RMA number, return reason, supplier credit or reverse-direction flow anywhere. Equals GH#24 Shop Ops Phase 2. **Canonical for declined duplicate `86bbaf50`.** |
| `14f35f95` | Lynn | 04-09 | Parts & Ordering | **XL** | js/parts.js (per-RO line items only) | Confirmed absent. New subsystem needed: inventory table, receive/consume/adjust flows, a searchable priced stock view, and links from the parts-request path. Largest item on the board. |

### Notifications & Task-Tracking (2 x L)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `af14b3cb` | Lynn | 05-21 | General UI/UX | **L** | js/shop-tasks.js (no ro_id / customer link, no reminder column) | Confirmed absent. Shop Tasks is a non-customer checklist. Needs a per-RO tasks table, an add/complete UI and a daily open-task reminder. Pairs with `5925fc01`. |
| `5925fc01` | Lynn | 04-09 | New Feature Idea | **L** | supabase/functions/send-manager-report; js/work-orders.js L293; js/ro-crud.js L370 | PARTIAL. The daily in-progress digest half is served by send-manager-report, and single-event notifies exist (service_added_notify, urgent_update_notify). Still absent: notify-on-every-RO-update and to-do scraping from status notes. Anchor of this theme. |

### Authorization Form & E-Sign (1 x S, 1 x L - both touch send-quote-email)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `5fec3eca` | Sofia | 07-15 | General UI/UX | **S** | send-quote-email L386/L387 (raf_copy), L549/L674/L748 (lead_staff_notify) | The `raf_copy` destructure has no `customerAddress`; the address appears only in the staff-notify branch. Add `customerAddress` to the raf_copy payload and both templates. |
| `0448de62` | Sofia | 06-11 | General UI/UX | **L** | customer-checkin.html signature pad; send-quote-email L386 | Signing is kiosk-only and raf_copy only emails a copy after signing. Needs a tokenized public signing page, a delivery path and signed-form storage. Pairs naturally with the Textly SMS channel. |

### Time Clock & Check-In (2 x S)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `e06d747e` | Riley | 04-10 | Time Clock / Check-In | **S** | js/render.js L228-235; index.html; checkin.html | No floating or sticky tech check-in control anywhere, and the compact action row offers edit / parts / WO / notify / message / schedule only. Underspecified: scope the target page and exact behavior with the submitter. Pairs with `6bb61db2`. |
| `6bb61db2` | Riley | 07-22 | Time Clock / Check-In | **S** | js/render.js L228-235 | No compact check-in/out button and no persistent "clocked into" indicator. Add a compact check-in/out action plus a login-time active-`time_log` banner shown across views. Pairs with `e06d747e`. |

### Work Orders (1 x S)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `2f752d40` | Jason | 07-21 | Work Orders | **S** | js/work-orders.js L360, L365, L649-650, gate L324 | `woFormDollar` and `woFormEstHours` are independent manual inputs with no labor-rate arithmetic anywhere. Auto-populate `dollar_value` = effective hours x the posted $195/hr rate (task `est_hours` rollup when present, else the WO figure), with an editable override that respects the existing `dollarLocked` readonly gate. |

### Auth & Cost (1 x XL)

| ER | By | Date | Category | LOE | Mapped code | Verdict |
|---|---|---|---|---|---|---|
| `ed1b873e` | Roland | 06-07 | General UI/UX | **XL** | js/auth.js L69 (GOOGLE_CONFIG import), GROUP B L115, GROUP D L44-48 | Still Google-based on v1.485: gapi + `google.accounts` One Tap + `signInWithIdToken`, and the check-in kiosk uses the same path. Swapping to Supabase password / magic-link or shared role accounts would drop roughly $500/mo but touches every page sign-in plus the email-keyed RBAC. Roland wants to scope this together. |

---

## DATA corrections - SQL for Roland, nothing executed (2)

| ER | By | Date | Category | Verdict + SQL |
|---|---|---|---|---|
| `3f254929` | Sofia | 07-16 | General UI/UX | Keep Steve Huebert, remove the misspelled Hubert RO. Eyeball first: `SELECT id,ro_id,customer_name,status FROM repair_orders WHERE customer_name ILIKE '%ubert%';` then, once the wrong id is confirmed: `UPDATE repair_orders SET deleted_at=now() WHERE id='<wrong-id>';` The merge feature itself is `0e172b2c`. |
| `6938c1af` | Ryan | 05-06 | General UI/UX | Already applied in the live DB - tech names read from `staff.name`, and submitter "Rod Wombles" now spells correctly in this very table. **RESIDUAL (not a runtime bug): the seed file `supabase/migrations/staff_table.sql` L85-87 still inserts Rod/Zak/Travis as "Wimbles", so a re-seed would reintroduce it.** Re-apply if ever needed: `UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |

---

## DUPLICATES - fold into the canonical ER (8)

| ER | By | Date | Bucket | Canonical | Verdict |
|---|---|---|---|---|---|
| `1bc33dd6` | Brandon | 07-27 | duplicate | `d4d0eeba` | Same defect Brandon already filed 07-22 (clicking a customer never scrolls to the right board entry). Root cause re-verified at index.html L4139/L4154. **Recommend status=declined.** |
| `2b78696c` | Lynn | 07-27 | duplicate | `29654af7` | Same back-to-top ask Lynn filed 07-06; re-verified still unbuilt on v1.485. **Recommend status=declined.** |
| `aa508852` | Riley | 04-09 | duplicate | `fd6c122d` | Identical text and timestamp to `fd6c122d` (Done). Already declined in the DB. |
| `86bbaf50` | Lynn | 05-13 | duplicate | `b19975d4` | A Gordon window ordered and returned to Vogt is supplier return / warranty tracking. Already declined. |
| `67daba15` | Brandon | 06-09 | duplicate | `d2561e11` | Same RO completion / pick-up date ask, Done via `repair_orders.pickup_date`. Already declined. |
| `fa1c1d6e` | Sofia | 06-09 | duplicate | `a22739b9` | Same missing check-in email (Sheldon Gilbert), now forced on every submission. Already declined. |
| `5057c7fc` | Roland | 06-09 | duplicate | `91a03260` | Same day-after worklist prune, Done via `prune_cashiered_worklists.sql`. Already declined. |
| `fc6b29f9` | Sofia | 06-17 | duplicate | `298e0e67` | Retest report of the returning-customer duplicate RO, Done via the check-in guard. Already declined. |

---

## DONE - delivered and re-verified on v1.485 (52)

Two of these are still `unreviewed` in the DB and should be closed
(`1c7f1410`, `9d3806e7`); two are test entries already declined
(`33750cb3`, `19dc2016`).

| ER | By | Date | Category | Mapped code (evidence) | Verdict |
|---|---|---|---|---|---|
| `1c7f1410` | Andrew | 07-16 | New Feature Idea | js/messaging.js L216, L219, L202, L207, L258-264 | Messaging media attach ships: `msgAttachBtn` plus `input#msgAttach` with `accept="image/*,application/pdf"` and `multiple`, capped at 5 with removable per-file chips. Photos, videos-as-files and PDFs all attach today. **Recommend status=done.** |
| `9d3806e7` | Brandon | 07-22 | General UI/UX | messages.html L528, L530, L1326; customer_note_entries_s154.sql | Customer-side Notes section in the details pane plus an Add-Notes modal writing append-only rows keyed by phone. **Recommend status=done.** |
| `79b2c805` | Sofia | 04-09 | New Feature Idea | closed-ros.html L725/L813/L838; js/photos.js L207/L253 | `reactivateRO` restores a cashed-out RO from `cashiered_child_archives` so it can be edited; `uploadDocument` adds documents to any RO. |
| `75124b03` | Ryan | 04-09 | RO Management | js/ro-crud.js L1119; js/render.js L121, L195, L372 | Warranty is a first-class RO type on new-RO creation, with board filter, compact chip and card badge. (v1.402) |
| `b2aa4bea` | Lynn | 04-09 | New Feature Idea | js/ro-crud.js L409/L486/L508; js/render.js L395; key_dates_reminder_sources.sql | `planned_dropoff_date` on create and edit, blue Drop chip, day-before manager reminder. Same field as `468aa376`. |
| `18611eee` | Lynn | 04-09 | Bug Report | js/work-list.js L195, L223, L263 | Was a real bug. Added work-list members are now viewable, reorderable and removable, with per-silo tabs. |
| `17492066` | Lynn | 04-09 | Notifications / Email | scheduled_notifications.sql; process-scheduled-notifications; index.html L2921 | Per-RO Schedule Notification bell sets a future date/time, message and recipients, with daily/weekly repeat. Covers the advance-warning ask. |
| `fd6c122d` | Riley | 04-09 | Work List | js/work-list.js L195, L210, L304, L356+, L383 | Delete plus mouse and touch drag-reorder, order persisted; mobile sizing fixed. (v1.460) Canonical for `aa508852`. |
| `98bd7498` | Brandon | 04-09 | RO Management | js/work-list.js L83, L223, L263 | Per-silo Work List tabs give a separate list per job category. |
| `19dc2016` | Roland | 04-11 | Time Clock / Check-In | - | Test entry ("Testing ER form / dictation"), not a real request. Already declined. |
| `33750cb3` | Roland | 04-09 | General UI/UX | - | Test entry ("Testing out the Make-A-Wish function"), not a real request. Already declined. |
| `1fe68261` | Rod | 04-13 | Work Orders | js/shop-tasks.js L21, L41, L153, L180; shop_tasks.sql | Shop Tasks tracks internal work not tied to a customer unit - matches Rod's poly-barrel / roof-bay examples. (v1.467) |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | js/config.js L158; js/render.js L741-745 | "Ready to Work" is a live RO status (slot 50) in both dropdowns. (v1.410) |
| `b87eb2fb` | Lynn | 04-14 | RO Management | keys_power_fields.sql; js/ro-crud.js L411-413, L488-490 | `key_status` / `keypad_code` / `keep_plugged_in` persisted on create and edit, keys chip on the card. Same build as `34fc03c2`. (v1.458) |
| `a5ff3e2d` | Lynn | 04-21 | RO Management | js/ro-crud.js L1119; js/render.js L122, L196, L373 | "Warranty + Repair" RO type exists end to end and stays billable so the repair half bills normally. (v1.465) |
| `a7d1474e` | Lynn | 04-23 | RO Management | urgent_update_field.sql; js/render.js L324/L214; css/dashboard.css L3446; js/ro-crud.js L370 | Always-visible red URGENT banner on the card, notifying silo manager(s) and admins on change. Lynn's follow-up to move the INPUT onto the tile is open ER `50175fce`. (v1.466) |
| `dde90d2b` | Roland | 04-27 | RO Management | customer-checkin.html L2467 (raf_copy) + lead_staff_notify | Photo optional and the outbound email forced on every submission - authorization-form copy and staff notice both go out. |
| `13e1cd87` | Tipton | 04-30 | Time Clock / Check-In | checkin.html L1516, L441 | "Return to Dashboard" button renders after clock-out, so the tech no longer opens a new tab. (v1.447, checkin.html now v1.38) |
| `e27311bf` | Ryan | 05-04 | Work Orders | js/work-orders.js L461, L385, L583, L445 | Grip handle and drag-to-reorder on every WO task row; DOM order drives `sort_order`, so a template is edited by load-reorder-Overwrite. (v1.464) |
| `d2561e11` | Brandon | 05-06 | New Feature Idea | key_dates_pickup_date.sql; js/ro-crud.js L410/L487; js/render.js L366/L397 | `repair_orders.pickup_date` surfaced as the green Pickup chip and in the key-dates panel. (v1.457) Canonical for `67daba15`. |
| `dac9fdda` | Brandon | 05-06 | General UI/UX | js/config.js L154; js/render.js L253, L451, L718-721 | "Off Lot - Returning" separates units the customer temporarily took from "Not On Lot" units, and is excluded from the on-lot count. (v1.460) |
| `a8e90a7d` | Brandon | 05-20 | General UI/UX | js/parts.js L153, L155, L160-161 | Manage Parts Ticket Totals bar sums parts, freight and total cost. Same build as `671fa623`. (v1.456) |
| `1aeb3f58` | Lynn | 05-21 | General UI/UX | js/render.js L396, L33 | Promised date renders as a large bold chip, red when overdue, and feeds the blended priority sort. The strict user-selectable sort is open ER `d1844e46`. (v1.457) |
| `cbc70a86` | Lynn | 05-21 | General UI/UX | customer-checkin.html L2319; work_list_autoadd_rpc.sql | Check-in calls `add_ro_to_silo_work_lists()` (SECURITY DEFINER), inserting `manager_work_lists` rows per involved silo lead with an sr-manager fallback. (S128) |
| `3f415c88` | Lynn | 05-26 | General UI/UX | index.html L2921, L2989 | Repeat none/daily/weekly plus repeat-until, expanded into one cancellable `scheduled_notifications` row per occurrence. (v1.463) |
| `cd663d8f` | Bobby | 05-28 | General UI/UX | js/ro-crud.js L1093; js/parts.js L307, L310 | Was a cosmetic bug. Currency inputs show 2 decimals on edit-populate and in the parts modal. No data loss occurred. (v1.447) |
| `34fc03c2` | Brandon | 05-29 | New Feature Idea | keys_power_fields.sql; js/ro-crud.js L411-413, L488-490 | Keys selector (keys left / no keys / keypad + code + keep-plugged-in) on New and Edit RO with a card chip. Covers `b87eb2fb`. (v1.458) |
| `0b2e128c` | Lynn | 06-02 | General UI/UX | checkin.html L1455, header note L5 | Tech clock-out work notes copied into the RO update timeline as a "TECH NOTES" `ro_status` row. (v1.460, checkin.html v1.38) |
| `468aa376` | Lynn | 06-02 | General UI/UX | js/ro-crud.js L409, L486; js/render.js L395 | Planned drop-off date input on New RO and (restored v1.457) Edit RO, feeding the Drop chip and the auto-reminder. Same field as `b2aa4bea`. |
| `2b814250` | Roland | 06-07 | Calendar | js/render.js L367, L395-398; css/dashboard.css L3438-3443; sync-ro-calendar | Key dates render as colored attention-grabbing chips plus a hover/tap panel; business-calendar integration runs server-side, idempotent via `cal_event_ids`; advance reminders go to managers/admins. |
| `cd084233` | Roland | 06-07 | Work List | send-manager-report (parts select L269), send-admin-pnl-report, worklist-report.html | Admin-level daily manager-worklist reporting ships with parts, labor and cost rollups. The manager scoring layer described at the end of the request remains a future extension, not this ER. |
| `a22739b9` | Sofia | 06-08 | General UI/UX | customer-checkin.html L2467 + lead_staff_notify | Was a notify-gate regression. Both sends are now forced on every submission. Canonical for `fa1c1d6e`. |
| `91a03260` | Roland | 06-09 | Work List | prune_cashiered_worklists.sql L23-25 | Removes an RO from manager work lists the day AFTER Delivered / Cashed Out, gating on the Chicago-local last-update date. Canonical for `5057c7fc`. |
| `cdd77a8b` | Brandon | 06-10 | General UI/UX | js/render.js L656-658, L718-721 | `updateStats` splits the counts: Shop Operations ROs drop out of the queue figure and the on-lot count excludes Not On Lot / Scheduled / Off Lot - Returning. (v1.453/v1.454) |
| `2c3d5633` | Ryan | 06-12 | General UI/UX | js/photos.js L66 | Was a mobile-only functional bug: the upload input is now attached to the DOM before clicking it, which was the iOS Safari dropped-change-event root cause. (v1.453) |
| `74a33621` | Brandon | 06-16 | General UI/UX | js/photos.js L380, L382, L396-397, L404 | Lightbox arrow-key navigation plus Escape-to-close and prev/next buttons. (v1.454) |
| `298e0e67` | Sofia | 06-16 | General UI/UX | customer-checkin.html L2047, L2136, L2194 | Was a data-integrity bug. The returning-customer guard reuses an existing scheduled RO instead of inserting a new one. Canonical for `fc6b29f9`. |
| `9b823d25` | Roland | 06-16 | RO Management | js/work-orders.js L244, L265-266, L293; service_added_notify_source.sql | `addServiceToRO` queues a `service_added_notify` row so the owning silo manager is emailed. Matches the roof-plus-water-heater example. (v1.460) |
| `9b046808` | Brandon | 06-17 | General UI/UX | js/config.js L157; js/render.js L674-676, L718-721 | "Scheduled" is its own status and not-yet-arrived Scheduled units are excluded from days-on-lot and the on-lot count. (v1.454) |
| `671fa623` | Brandon | 06-18 | General UI/UX | js/parts.js L153, L155, L160-161 | Ticket Totals include a Freight line summed across parts, rolled into Total Cost. Same build as `a8e90a7d`. (v1.456) |
| `5c45bb8d` | Sofia | 06-19 | General UI/UX | customer-checkin.html L2047, L2136, L2467 | Was a duplicate-RO plus missing-email bug (David Flanagan). Both halves resolved: the Scheduled path reuses the existing RO and the raf_copy send is forced. |
| `b06a285f` | Lynn | 06-19 | General UI/UX | js/render.js L363-367; css/dashboard.css L3438-3443 | Calendar icon beside the customer name opens a key-dates panel listing drop-off, promised and pickup in full on hover or tap. (v1.465) |
| `ee50d11c` | Lynn | 06-19 | General UI/UX | time-off.html L752 (`editRequest`) | Was a real gap. Time-off entries are editable rather than delete-only; the form reloads the entry and flips to "Update Record". |
| `88395acc` | Lynn | 06-19 | General UI/UX | add_absence_window_to_time_off.sql; add_partial_hours_to_time_off.sql; time-off.html L368, L372 | Partial-day absence captured as an exact clock-out-to-return window rather than an hour total. (Time Off v1.5) |
| `80390f36` | Lynn | 06-23 | General UI/UX | js/render.js L35-37, L61 | Shop Operations tiles pinned to the top ahead of the priority sort, plus a "shop operations shop" search token. (v1.462) |
| `7f2d0d70` | Lynn | 06-26 | General UI/UX | send-dropoff-report (window L138-141), reads `manager_work_lists` | The automated Monday drop-off check email lists this week's scheduled drops plus last week's arrivals and flags any not yet on the right team's work list. Pairs with `cbc70a86`. |
| `3f7fb4fc` | Brandon | 06-29 | General UI/UX | js/render.js L656-658, L718-721, L249 | Was a small data-display bug. Shop ROs excluded from RVs-in-Queue and on-lot, and the shop status dropdown replaced by a static label. (v1.468) |
| `cb7742a8` | Lynn | 06-29 | General UI/UX | sync-ro-calendar; customer-checkin.html L2182, header note L10 | Drop-off / promised / pickup events are owned server-side and posted to the correct team calendar for every RO including front-desk check-ins. Fixes the repair-calendar miss Lynn reported. (v1.469) |
| `39b75fab` | Sofia | 07-09 | General UI/UX | js/photos.js L198, L376, L618, L647/L696/L721 | Documents / Photos library supports rename, move-to-another-RO and delete, office-staff gated - so the Marco DeAlba documents on the Ruiz RO can be moved or removed. (v1.472) |
| `ac394468` | Roland | 07-24 | General UI/UX | js/enhancement.js L43-53; erCategory in 6 HTML files | Make-a-Wish category dropdown now carries Messages / Texting plus Solar, Leads, Closed ROs, Reports, Time Off and Employee Guide. **RESIDUAL, stated not hidden: `erCategory` exists in only 6 live HTML files (analytics, checkin, closed-ros, index, solar, worklist-report), so messages / leads / time-off / guide cannot RAISE an ER - worth its own small ER.** (v1.482) |
| `d62a26e4` | Roland | 07-24 | New Feature Idea | js/messaging.js L202, L207, L219, L258-264, guard L300-306 | Composer supports up to 5 photo/PDF attachments with removable chips; the send path chunks into one media per MMS because a carrier total-payload cap silently stripped multi-media sends. (v1.482) |
| `93b00023` | Roland | 07-24 | New Feature Idea | textly-webhook L477, L505; send-unreplied-reminder; unreplied_eod_reminder_s158.sql | All three parts verified: a non-keyword inbound flips the conversation back to open and logs a "reopened" event; the assigned-owner notify fires only on the needs-reply flip; the 4:30pm sweep runs on a weekday 21:30 UTC cron. (v1.482) |

---

## Carried over, not re-triaged this pass (1)

| ER | By | Date | Category | DB status | Note |
|---|---|---|---|---|---|
| `f73a1e5a` | Roland | 04-13 | General UI/UX | declined | Not in `needs_triage_ids`, so no verdict was written for it. It is a test entry ("Testing dictation 123 testing enhancement request dictation"), already declined. Its stored `triage_bucket` / `triage_loe` / `triage_verdict` are all NULL, and its last `admin_notes` line ("Built + shipped v1.464 (WO task drag-reorder)") clearly belongs to a different ER - worth a one-line DB cleanup when convenient. |

---

## Recommended action today

1. **Ship the bug class first - two fixes close three ERs.**
   - `d94a418a` is the highest-value one: drop the blanket `.reverse()` at
     js/render.js L553 and L563 and split on the S159 entry regex instead, so
     multi-line RO Status and Customer Comm notes read in the order they were
     typed. Render-only, no migration, no data risk.
   - `d4d0eeba` + `9031e101` + `1bc33dd6` are one fix: make
     `scrollToROInBoard` (index.html L4139) and `handleDeepLink`
     (js/qr.js L198) clear filters, re-render, then scroll and highlight,
     instead of bailing out with a toast or silently.
2. **Then take the Messaging UX cluster in one session** - `6e9ca4dc`,
   `f1e6e311`, `9aab7550`, `21fc3a9d` are all S and all live in
   messages.html / js/messaging.js. Four ERs, one file pair, one regression.
3. **Then Board & Navigation UX** - `29654af7` (back to top, also closes
   duplicate `2b78696c`), `d432863a` (favicon), `d1844e46` (strict promised
   sort). Three more S items in index.html / js/render.js.
4. **Close the two Done-but-open ERs**: set `status='done'` on `1c7f1410`
   and `9d3806e7`, and `status='declined'` on the two open duplicates
   `1bc33dd6` and `2b78696c`. That drops `unreviewed` from 30 to 26.
5. **Run the two Data SQL snippets** (above) when convenient - `3f254929`
   needs Roland to eyeball the two `%ubert%` rows before the soft delete.
   Consider also correcting `staff_table.sql` L85-87 so a re-seed cannot
   reintroduce "Wimbles".
6. **Unblock the two stalled S items by asking the submitters**: Lynn's
   target parts field order (`8d885331`) and Riley's check-in float scope
   (`e06d747e` / `6bb61db2`). Both are small once specified.
7. **Plan, do not rush, the XLs**: the auth swap (`ed1b873e`, roughly
   $500/mo at stake) and parts inventory (`14f35f95`) each deserve their own
   scoping session. `ed1b873e` is security-critical - it touches every page
   sign-in plus the email-keyed RBAC.
