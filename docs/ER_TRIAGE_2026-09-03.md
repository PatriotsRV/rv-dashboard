# PRVS Enhancement-Request Triage - 2026-09-03 (headless runner)

Incremental pass over `enhancement_requests` against the current modular
codebase at HEAD **505e22b** (index.html **v1.502**, messages.html **v1.24**).
Input snapshot `er_input.json` fetched 2026-09-03 11:59 UTC.

`full_pass` was **false**. `needs_triage_ids` carried exactly **one** row
(`776384e7`, Andrew Page, 09-01), which was classified from scratch by grepping
the live code this run. Every other row below carries its **existing stored
verdict verbatim** - those were stamped at earlier HEADs and were NOT
re-verified this run. See "Carried verdicts that are now stale" for the two
that current code has probably moved past.

Runner is read-only: **no code was changed, no DB writes were made, no git
commands were run.** Status flips and the Data-bucket SQL are Roland's to run.

## Board snapshot

| DB status | Count |
|---|---|
| done | 62 |
| unreviewed | 31 |
| declined | 15 |
| **Total** | **108** |

## Verdict buckets (all 108 rows)

| Bucket | Count |
|---|---|
| done | 62 |
| needed | 30 |
| duplicate | 13 |
| data | 2 |
| bug | 0 |
| (never triaged) | 1 |

**Headline: the bug column is empty for the first time.** All four bugs that
led the 2026-08-22 report (`225a2535`, `d94a418a`, `5ca724ff`, `d4d0eeba`) plus
the LARGE data-loss bug `09084bc5` now carry `done` verdicts from v1.494 /
v1.497 / v1.498. The board is 30 open enhancements, 21 of them S, and one
duplicate still needing a paperwork decline.

## The one new classification this run

| ER | By | Date | Category | Bucket | LOE | Theme |
|---|---|---|---|---|---|---|
| `776384e7` | Andrew Page | 09-01 | General UI/UX | **needed** | **S** | Messaging & E-sign |

Ask: quick responses / canned replies in the message board, "like Kenect used
to have", the named example being "Vroom parts deposit".

Verdict: NEEDED (prefer needed over done). Absent at HEAD 505e22b
(messages.html v1.24): composer extras are attach/emoji/signature only
(`js/messaging.js` composerExtrasHtml L341-351, initComposerExtras L491-509);
quick_replies / canned / message_templates / sms_templates grep repo-wide hits
only a dormant comment in `supabase/migrations/sms_schema.sql` L11. Small:
snippet picker in composerExtrasHtml, shared by `messages.html` L671-673 and
the RO-card modal. Ask Andrew for the starter list.

Code checks run for this ER at HEAD 505e22b:

- `quick.?repl` / `canned` / `snippet` (case-insensitive) across `js`, `*.html`,
  `supabase` - the only `snippet` hits are the conversation-list preview text
  (`messages.html` L485, L932, L1389, L2140), not reply templates.
- `message_templates` / `sms_templates` / `quick_replies` / `canned_responses` /
  `reply_templates` repo-wide - one hit, a comment in the dormant
  `supabase/migrations/sms_schema.sql` L11 saying the template layer "comes in
  Stage 3". No such table was ever created.
- `composerExtrasHtml` is the single shared composer implementation for both the
  `messages.html` inbox (`#composerExtrasSlot`, L673, filled at boot L1116-1117)
  and the RO-card modal, so one snippet picker covers both surfaces.

Not a duplicate of Andrew's `472ccadb` (08-28, drag a thumbnail into the
conversation) - that is an attachment-path ask, this is a text-template ask.
Both live in the same composer and would ship well together.

## Carried verdicts that are now stale (flagged, NOT re-bucketed)

The runner may only classify ids in `needs_triage_ids`, so both rows below keep
their stored bucket. Both should be re-verified on the next full pass.

- **`af14b3cb` (Lynn, needed/L) and the task half of `5925fc01` (Lynn,
  needed/L).** Their stored verdicts read "greps for ro_tasks / task_tracker /
  customer_task return zero hits" and were stamped before S186. Session 186 has
  since shipped the **Task Manager**: `supabase/migrations/task_manager_s186.sql`
  creates `tasks` with an `ro_uuid` FK to `repair_orders` (L15), `task_events`
  (L50), `board_prefs` (L63) and the `ro_activity_feed` view, plus
  `supabase/functions/send-task-reminders` for the SMS/email nag loop and
  escalation, driven from `tasks.html`. That delivers the *persist-until-done +
  daily reminder* half of `af14b3cb` and most of the notify half of `5925fc01`.
  What is still genuinely open is the *placement* ask: a bulleted to-do list
  rendered on the RO card next to Repair Description and RO Status that anyone
  can add to and check off with who/when notation. `index.html` does not read
  the `tasks` table at all. Recommend re-scoping both ERs against S186 rather
  than rebuilding.

## NEEDED - open enhancements by theme (30)

### Messaging & E-sign (9)

| ER | By | Date | LOE | Verdict (carried unless marked NEW) |
|---|---|---|---|---|
| `776384e7` | Andrew Page | 09-01 | **S** | **NEW this run.** NEEDED (prefer needed over done). Absent at HEAD 505e22b (messages.html v1.24): composer extras are attach/emoji/signature only (js/messaging.js composerExtrasHtml L341-351, initComposerExtras L491-509); quick_replies / canned / message_templates / sms_templates grep repo-wide hits only a dormant comment in supabase/migrations/sms_schema.sql L11. Small: snippet picker in composerExtrasHtml, shared by messages.html L671-673 and the RO-card modal. Ask Andrew for the starter list. |
| `472ccadb` | Andrew Page | 08-28 | **S** | Confirmed absent at HEAD 505e22b (messages.html v1.24): the composer has only a hidden file input plus paperclip click (js/messaging.js msgAttach L347, initComposerExtras L491-494) and _handleAttachPick reads ev.target.files (L422); grep for dragover/drop/dataTransfer.files hits only js/work-list.js L430 and js/work-orders.js L485 row-reorder. Small: a drop zone feeding the same upload path. Ask Andrew if the thumbnail is an OS file or an in-app photo (that variant is M). |
| `ca0f7ed9` | Brandon Dillon | 08-03 | **S** | Confirmed still absent on messages.html v1.23 at HEAD 0223cb3: renderLeadInfo (L1389) reads ONLY the conversation's own columns (c.rv, c.service_type), so an existing dashboard customer shows 'No RV or service type captured yet'. selectConversation computes phone-matched live ROs as 'linked' for renderContactInfo but calls renderLeadInfo(c) without them (L1744). S168 touched the create-RO handoff, not this panel. Small: pass 'linked', fall back to RO rv. |
| `9aab7550` | Brandon Dillon | 07-21 | **S** | Still only partial on messages.html v1.23 at HEAD 0223cb3. S166 added a MANUAL 'Mark unread' button (markConversationUnread L1980) which helps, but the visual treatment is unchanged: a single 8px gold .unread-dot (CSS L469) applied only when last_direction is 'inbound' AND status is 'open' (L1313, rendered L1319), with no row-level highlight. Small: add a bold/tinted unread row treatment. |
| `21fc3a9d` | Brandon Dillon | 07-21 | **S** | Confirmed still absent on messages.html v1.23 at HEAD 0223cb3: the page uses fixed rem font sizes with no user text-size control - grep for font-scale / fontScale / textSize across js, html and css returns zero hits. Small: add a persisted font-scale control for the conversation list. |
| `f1e6e311` | Brandon Dillon | 07-21 | **S** | Confirmed still absent on messages.html v1.23 at HEAD 0223cb3: convoSearch (L604) is a plain text input with no clear affordance and the field is reset only programmatically (L1151, L1170). Small: add a clear (x) button that empties the messaging conversation search and re-runs the filter. |
| `6e9ca4dc` | Brandon Dillon | 07-21 | **S** | Valid, still present on desktop at HEAD 0223cb3: js/messaging.js refreshSignaturePreview (L369) writes 'Added to every send:' into #msgSigPreview (element L350, set L374). messages.html hides it only inside the mobile max-width media query (L565), so desktop messages.html and the RO-card composer still show it. Small: remove it or collapse it behind a toggle in all viewports; the signature itself keeps sending. |
| `5fec3eca` | Sofia Pedroza | 07-15 | **S** | Confirmed still absent at HEAD 0223cb3: the raf_copy branch of supabase/functions/send-quote-email (L387) destructures to / cc / customerName / roId / signedDate / rv / services / workType / phone / customerType / serviceDesc / warrantyDesc / promisedDate / signatureDataUrl with no customerAddress; the address appears only in the lead_staff_notify branch. Small: add customerAddress to the raf_copy payload and both templates. |
| `0448de62` | Sofia Pedroza | 06-11 | **L** | Confirmed still absent at HEAD 0223cb3: signing is kiosk-only (customer-checkin.html on-device signature pad) and the raf_copy branch of supabase/functions/send-quote-email (L386) only emails a copy after signing; grep for signing_token / remote_sign / signature_request / signing_link across js, supabase and the HTML pages returns nothing. Needs a tokenized public signing page, a delivery path and signed-form storage. |

**Seven of the nine are S and confined to `messages.html` + `js/messaging.js`.**
`776384e7` and `472ccadb` both land in `composerExtrasHtml` and should ship in
the same messages release as Brandon's four-item polish batch.

### RO Fields & Status (6)

| ER | By | Date | LOE | Verdict (carried) |
|---|---|---|---|---|
| `44419de9` | Lynn Titel-Shepard | 08-12 | **S** | NEEDED - none of the 4 values exist at HEAD a405b7a (v1.494); the canonical set is the 12 in js/config.js STATUS_PROGRESS_MAP L151-164. Each add must hit 9 hardcoded spots (index.html select L7038-7051, filter btns L1085-1096, inline maps L1579/L4489, config.js ES L253-262, render.js L253/L451, statusColorMap L727-739, ALL_STATUSES L741-745) PLUS both DB CHECKs per S172 or saves throw 23514. Overlaps 972af03c CANCELED - confirm scope with Lynn; bundle with 972af03c + 3e746c18. |
| `972af03c` | Lynn Titel-Shepard | 07-17 | **S** | Confirmed still absent at HEAD 0223cb3: no Canceled / Cancelled RO status - js/config.js STATUS_PROGRESS_MAP (L151-164) ends at Delivered/Cashed Out, and the only 'cancelled' hits in the repo are the scheduled_notifications and review_requests status CHECKs. Small: add the status plus color/priority maps, exclude it from the on-lot and queue counts, keep the record for notes. Canonical for duplicate eb00a68d; pairs with 3e746c18. |
| `3e746c18` | Lynn Titel-Shepard | 08-03 | **S** | Valid and confirmed absent at HEAD 0223cb3: a case-insensitive grep for totaled / totalled across js/config.js and js/render.js returns zero hits, and STATUS_PROGRESS_MAP (L151-164) ends at Delivered/Cashed Out. Distinct status value from 972af03c's Canceled, so kept as its own item, but ONE build should add both terminal statuses and exclude them from the queue / on-lot counts (js/render.js L656-658, L718-721). |
| `a195e4ab` | Brandon Dillon | 08-20 | **M** | NEEDED at HEAD df519c6 (v1.495): zero check tracking - grep check_number / checkNumber / payments across js, index.html, supabase = 0 hits. Only payment capture is the one-value Customer Pay block (index.html L7203-7246 / L7782-7804), hybrid-RO-only (setROType L2263-2267), method Check but no check number (L7229/L7797), one amount + datePaid, saved flat into insurance_data JSONB (L2414-2423, write L6562/L6718), never on the card. Needs a per-RO checks table + add/list UI + cashiered mirror. |
| `0e172b2c` | Sofia Pedroza | 07-16 | **M** | Partial, re-verified at HEAD 0223cb3: js/duplicates.js has an RO-level merge/delete engine (getBaseROId L35, findDuplicateGroups L40, executeDupeMerge) but it groups strictly on the PRVS-XXXX-XXXX base RO id (regex L36, keying L44), so two ROs created under different spellings of the same customer are never detected. Needs name/phone account dedup plus a verify-which-is-correct merge step. The one-off cleanup for this case is 3f254929. |
| `5fbfa299` | Brandon Dillon | 06-30 | **M** | Confirmed still absent at HEAD 0223cb3: the only archive path is the cashiered one (js/ro-crud.js archiveROInSupabase L662 into cashiered + cashiered_child_archives); a case-insensitive grep for archived / abandoned in js/render.js and js/config.js returns zero hits, so there is no soft-archive flag, status or board filter for quoted-but-never-progressed units. Needs a flag, a board filter and an un-archive path. Pairs with 972af03c. |

**Caveat on the status batch.** `44419de9`'s carried verdict counts 12 statuses
at v1.494. Two terminal statuses have shipped since (`v1.501` "Closed - No
Charge", `v1.502` outstanding-payments work), and `611589ba`'s later verdict
counts 15 at v1.500. The hardcoded-spot line anchors in these verdicts have
therefore moved. Re-anchor before building; the S172 lesson (**both** DB CHECK
twins, `repair_orders` and `cashiered`) still governs.

### Parts & Inventory (5)

| ER | By | Date | LOE | Verdict (carried) |
|---|---|---|---|---|
| `2af47890` | Brandon Dillon | 08-25 | **S** | Prefer needed over done. DB half ships: trigger trg_stamp_date_received (stamp_date_received_on_parts.sql L33-36) stamps date_received to the shop-local day on the transition into Received. Client half missing: js/parts.js markPartReceived (L449-478) sets part.status only, never part.dateReceived, so the in-memory row stays blank and a later savePartForm edit resends date_received null (L539), which the trigger will not restamp (OLD.status already Received) - wiping the date. |
| `1e434aff` | Brandon Dillon | 08-25 | **S** | Confirmed absent at HEAD 9c50b52 (v1.501): the Manage Parts table has no receipt-date column - headers are Part Name / Part # / Supplier / Status / Qty / Wholesale / Retail / Quick / Actions (js/parts.js L259-267) and renderPartsRows (L121-145) prints no p.dateReceived; the value is only editable in the collapsed Add/Edit Part form (pf_dateReceived L242). Small: render dateReceived beside the Received tick. Depends on 2af47890 landing the stamp. |
| `8d885331` | Lynn Titel-Shepard | 04-09 | **S** | Parts-entry form exists and is unchanged at HEAD 0223cb3: js/parts.js pf_supplier L181, pf_partName L190, pf_serviceSilo L208, pf_returnDeadline L226. The ask is only to reorder those fields, which is quick once Lynn supplies the target layout or a mockup. Blocked on detail, not on effort. |
| `b19975d4` | Lynn Titel-Shepard | 04-09 | **L** | Partial infra only, re-verified at HEAD 0223cb3: js/config.js PART_STATUSES (L200) includes 'Returned' and js/parts.js has a Return Deadline input (pf_returnDeadline L226), but a word-boundary grep for rma / return_reason / supplier_credit across js and supabase returns zero hits - no RMA number, return reason, supplier credit or reverse-direction flow. Equals GH#24 Shop Ops Phase 2. Canonical for declined duplicate 86bbaf50. |
| `14f35f95` | Lynn Titel-Shepard | 04-09 | **XL** | Confirmed still absent at HEAD 0223cb3: a word-boundary grep for inventory / on_hand / in_stock / stock_qty across js, supabase/migrations and supabase/functions returns zero hits. Parts remain per-RO line items only (js/parts.js). New subsystem needed: inventory table, receive/consume/adjust flows, a searchable priced stock view, and links from the parts-request path. Largest item on the board. |

`2af47890` and `1e434aff` are one build: `2af47890` lands the client-side stamp
(and closes the wipe-on-edit path its verdict describes), `1e434aff` renders it.

### Notifications & Task-Tracking (3)

| ER | By | Date | LOE | Verdict (carried) |
|---|---|---|---|---|
| `a03677e6` | Brandon Dillon | 08-07 | **S** | NEW ER, confirmed at HEAD 0223cb3: machine audit lines write as notes type 'ro_status' and so crowd RO Status Updates (js/render.js L553) - NOTIFICATION SENT/FAILED (process-scheduled-notifications L187/L215), SCHEDULED/CANCELLED (index.html L3116/L3154), key-date/urgent rows (js/ro-crud.js L302/L344/L379/L571/L582), parts notes (js/parts.js L654/L855). customer_comm renders separately (L563). Small: retarget the type. NOTE Roland reversed this retarget for LEAD NOTES in v1.491 - confirm scope. |
| `af14b3cb` | Lynn Titel-Shepard | 05-21 | **L** | Confirmed still absent at HEAD 0223cb3: js/shop-tasks.js is a non-customer shop checklist with no ro_id / customer link and no reminder column, and grep for ro_tasks / customer_task / task_tracker across js and supabase returns zero hits. Needs a per-RO tasks table, an add/complete UI and a daily open-task reminder. Pairs with 5925fc01. **STALE - see the S186 Task Manager note above.** |
| `5925fc01` | Lynn Titel-Shepard | 04-09 | **L** | PARTIAL, re-verified at HEAD 0223cb3. The daily in-progress digest half ships as supabase/functions/send-manager-report, and single-event notifies exist (service_added_notify js/work-orders.js L293; urgent_update_notify js/ro-crud.js L374). Still absent: notify-on-every-RO-update and to-do scraping from status notes - grep for ro_tasks / task_tracker / customer_task across js and supabase returns zero hits. Anchor of this theme; pairs with af14b3cb. **STALE - see the S186 Task Manager note above.** |

### Board & Navigation UX (2)

| ER | By | Date | LOE | Verdict (carried) |
|---|---|---|---|---|
| `6ff111e7` | Lynn Titel-Shepard | 08-27 | **S** | NEEDED (prefer needed over done): ER fa35e3df already recolored the Drop chip #0a84ff -> #5cadff in v1.498, but Lynn filed this 5 days AFTER that shipped and adds a NEW size ask. At HEAD 505e22b js/render.js L468 still renders Drop at font-size 0.72rem / weight 700, the smallest of the three key dates, vs the Promised chip L469 at 0.78rem / 800; the hover row L436 uses the same #5cadff. Small, render/CSS only: lift Drop to Promised-level size and weight and a stronger hue on the #1a1d24 card. |
| `d1844e46` | Lynn Titel-Shepard | 07-16 | **S** | Confirmed still absent at HEAD 0223cb3: js/render.js renderBoard sorts on the single blended calculatePriority score (L30-39) with the shop-ops pin at L35-37; grep for sortBy / sortSelect / sortMode finds nothing in index.html or js/render.js, so there is no selectable strict promised-date-then-oldest-on-lot order. Small: add the strict sort option while keeping the shop pin. Related to 1aeb3f58, which delivered only the promised-date chip. |

### Time Clock & Check-In (2)

| ER | By | Date | LOE | Verdict (carried) |
|---|---|---|---|---|
| `6bb61db2` | Riley Scott | 07-22 | **S** | Confirmed still absent at HEAD 0223cb3: js/render.js compact-col-actions (L228-235) has edit / parts / WO / notify / message / schedule but no check-in-out button, and grep for 'clocked in' / 'clocked into' / activeTimeLog across index.html, js/render.js and js/time-tracking.js returns nothing. Small: add a compact check-in/out action plus a login-time active-time_log banner shown across views. Pairs with e06d747e. |
| `e06d747e` | Riley Scott | 04-10 | **S** | Confirmed still absent at HEAD 0223cb3: no floating or sticky tech check-in control in index.html or checkin.html, and js/render.js compact-col-actions (L228-235) offers edit / parts / WO / notify / message / schedule only, with no check-in button. Underspecified: scope the target page and the exact behavior with the submitter. Pairs with 6bb61db2. |

### Work Orders & Shop Tasks (1)

| ER | By | Date | LOE | Verdict (carried) |
|---|---|---|---|---|
| `2f752d40` | Jason Rubin | 07-21 | **S** | Confirmed still absent at HEAD 0223cb3: js/work-orders.js has independent manual inputs woFormDollar (L360) and woFormEstHours (L365), read separately on save (L649-650), and grep for LABOR_RATE / laborRate / labor_rate in the module returns zero hits (js/insurance.js laborRate L212 is the per-claim INSURANCE rate). Small: auto-populate dollar_value = effective hours x $195/hr, respecting the dollarLocked gate (L324). |

### Scheduling & Dates (1)

| ER | By | Date | LOE | Verdict (carried) |
|---|---|---|---|---|
| `47428f75` | Lynn Titel-Shepard | 08-03 | **S** | UNDERSPECIFIED - 'update vacation/sick/personal calendar', no stated defect or target. time-off.html already has all three types, a color-coded month calendar, stats tiles, per-row Edit (L752 / editRequest, ER ee50d11c) and exact partial-day windows (modalStartTime L368, modalEndTime L372). Filed Needed not Done because the ask maps to no gap without Lynn's detail; if it restates ee50d11c it is already delivered. Scope with the submitter. |

### Auth & Cost (1)

| ER | By | Date | LOE | Verdict (carried) |
|---|---|---|---|---|
| `ed1b873e` | Roland Shepard Jr | 06-07 | **XL** | Confirmed still Google-based at HEAD 0223cb3: js/auth.js imports GOOGLE_CONFIG from js/config.js (L69), initialises gapi with it (L203-204) and signs in via google.accounts One Tap plus signInWithIdToken; the kiosk uses the same path. Swapping to Supabase password / magic-link or shared role accounts drops roughly $500/mo but touches every page sign-in plus the email-keyed RBAC. Scope with Roland. |

---

## BUGS (0)

No ER on this board carries a `bug` verdict. For the record, the five bugs
that led the last two reports all now carry `done` verdicts:

| ER | By | Date | Closed by |
|---|---|---|---|
| `09084bc5` | Lynn | 08-04 | v1.494 - `archive_single_ro` RPC + `cashiered_full_detail_s171.sql` (7 dropped columns, all 7 child mirrors, delete last); `closed-ros.html` v1.3b restores them |
| `225a2535` | Brandon | 08-05 | v1.497 - `_lastEstimateScan` stamped with `roKey = editingROSupabaseId`, every consumer gates on it |
| `d94a418a` | Brandon | 07-27 | v1.497 - `formatNotesNewestFirst` reverses ENTRIES, not lines |
| `d4d0eeba` | Brandon | 07-22 | v1.497 - both nav paths; also closes `9031e101` and `1bc33dd6` |
| `5ca724ff` | Lynn | 08-06 | v1.497 - `dataTransfer.setData` + `effectAllowed='move'` on work-list dragstart |

---

## DATA - corrections (2)

Nothing below was executed by this runner. Both are already applied in the live
DB; the SQL is kept only as the record.

| ER | By | Date | Verdict + SQL (carried) |
|---|---|---|---|
| `6938c1af` | Ryan Dillon | 05-06 | Data correction already applied in the live DB - tech names read from staff.name, and submitter 'Rod Wombles' now spells correctly in this very table. RESIDUAL (not a runtime bug): seed file supabase/migrations/staff_table.sql L85-87 still inserts Rod/Zak/Travis as 'Wimbles', so a re-seed would reintroduce it. Re-apply if ever needed:<br>`UPDATE staff SET name=REPLACE(name,'Wimbles','Wombles') WHERE name ILIKE '%Wimbles%';` |
| `3f254929` | Sofia Pedroza | 07-16 | DATA correction, not code - and already carried out. The misspelled Steve Hubert RO was soft-deleted in S178; only the correct Huebert RO remains. SQL of record:<br>`SELECT id, ro_id, customer_name, status FROM repair_orders WHERE customer_name ILIKE '%ubert%';`<br>then, with the confirmed wrong id:<br>`UPDATE repair_orders SET deleted_at = now() WHERE id = '<wrong-uuid>' AND deleted_at IS NULL;`<br>The wider merge-accounts ask stays open as 0e172b2c (needed/M). |

---

## DUPLICATES - fold and decline (13)

| ER | By | Date | DB status | Folds into | Note (carried) |
|---|---|---|---|---|---|
| `611589ba` | Lynn | 08-24 | **unreviewed** | `972af03c` + `44419de9` | DUPLICATE - both halves are already open ERs. CANCELED is 972af03c (needed/S, canonical; eb00a68d already declined into it). Closed is 44419de9's JOB DECLINED/CLOSE (needed/S). Re-verified absent at HEAD f501151 v1.500: js/config.js STATUS_PROGRESS_MAP L151-171 lists 15 statuses, no Closed and no Canceled. Adds no new work item; ships with the status batch. **Recommend `status=declined`.** |
| `ece91cba` | Lynn | 08-17 | declined | `af14b3cb` | Same per-RO to-do list that persists until marked complete. This ER's added spec (bulleted, anyone can add or check, record who and when, plus the Repair Description vs RO Status split) folds into af14b3cb. Already declined in the DB. |
| `2deb8f30` | Lynn | 08-13 | declined | `a03677e6` | Same ask: get machine-generated notification lines out of RO Status Updates. Re-verified that nothing in v1.497/v1.498 changed note routing. Lynn's 'separate location view' wording folds into a03677e6 as spec. Already declined in the DB. |
| `eb00a68d` | Lynn | 08-03 | declined | `972af03c` | Same CANCELED status ask Lynn filed 07-17. Re-verified still unbuilt at HEAD 65fdb97. Already declined in the DB. |
| `2b78696c` | Lynn | 07-27 | declined | `29654af7` | Same back-to-top ask Lynn filed 07-06. `29654af7` has since shipped in v1.498, so this line is fully closed out. |
| `1bc33dd6` | Brandon | 07-27 | declined | `d4d0eeba` | Same scroll-to-customer defect Brandon filed 07-22. `d4d0eeba` shipped in v1.497, so this line is fully closed out. |
| `9031e101` | Riley | 07-21 | declined | `d4d0eeba` | iPhone work-list tap does not reach the unit. Closed by the same v1.497 fix. |
| `fc6b29f9` | Sofia | 06-17 | declined | `298e0e67` | Retest report of the duplicate-RO guard. Already declined. |
| `5057c7fc` | Roland | 06-09 | declined | `91a03260` | Prune cashed-out RO from work list. Already declined. |
| `fa1c1d6e` | Sofia | 06-09 | declined | `a22739b9` | Sheldon Gilbert check-in email. Already declined. |
| `67daba15` | Brandon | 06-09 | declined | `d2561e11` | RO completion / pick-up date. Already declined. |
| `86bbaf50` | Lynn | 05-13 | declined | `b19975d4` | Gordon window returned to Vogt = supplier return tracking. Already declined. |
| `aa508852` | Riley | 04-09 | declined | `fd6c122d` | Identical text and timestamp. Already declined. |

**`611589ba` is the only duplicate still `unreviewed`** - one decline drops the
open count from 31 to 30 with no code work.

---

## DONE - delivered (62)

Carried verdicts; the HEAD each was last verified at is stated inline.

| ER | By | Date | Category | Verdict (carried) |
|---|---|---|---|---|
| `33750cb3` | Roland | 04-09 | General UI/UX | Test entry ('Testing out the Make-A-Wish function to see how this works'). Not a real request; no code mapping and no work item. Already declined in the DB; keep declined. |
| `79b2c805` | Sofia | 04-09 | New Feature | Delivered; re-verified at HEAD 0223cb3 (v1.492): closed-ros.html reactivateRO (L838, buttons L725/L813) restores a cashed-out RO plus parts/time_logs/notes/audit_log/insurance_scans and the WO subtree from the cashiered mirrors (flat map L958-963), and js/photos.js uploadDocument (L253, input L207) adds documents to the reopened RO. |
| `75124b03` | Ryan | 04-09 | RO Management | Delivered v1.402; re-verified at HEAD 0223cb3: Warranty is a first-class RO type at creation (js/ro-crud.js warranty branch restoring the Warranty Details fields) plus the board filter (js/render.js L121), the compact chip (L195) and the card badge (L372, with the $0 No-Charge sub-badge). Nothing further to build. |
| `b2aa4bea` | Lynn | 04-09 | New Feature | Delivered; re-verified at HEAD 0223cb3: planned_dropoff_date is written on RO create and edit (js/ro-crud.js L413, L490) and drives both the blue Drop key-date chip (js/render.js L395) and the day-before manager reminder (key_dates_reminder_sources.sql, auto_dropoff_reminder source). Same field as 468aa376. |
| `18611eee` | Lynn | 04-09 | Bug Report | Was a real bug; fixed and re-verified at HEAD 0223cb3: js/work-list.js renderWorkList plus removeFromWorkList (L195), drag-reorder (row.draggable L304, touch handlers persisted by _saveWorkListOrder L210/L383) and the per-silo tabs (L223 / L263) mean added members are viewable, reorderable and removable. The desktop drag half is now also fixed (see 5ca724ff, v1.497). |
| `17492066` | Lynn | 04-09 | Notifications | Delivered; re-verified at HEAD 0223cb3: the per-RO Schedule Notification bell sets a future date/time, message and recipients (scheduled_notifications.sql, fired by process-scheduled-notifications on cron), with daily/weekly repeat until a chosen date (index.html snRepeat L2993). Covers the advance-warning ask. |
| `fd6c122d` | Riley | 04-09 | Work List | Delivered v1.460; re-verified at HEAD 0223cb3: js/work-list.js supports delete (removeFromWorkList L195) and drag-reorder on both mouse (row.draggable L304, dragstart L389) and touch (L404-429), persisted by _saveWorkListOrder (L210, L383), with the mobile sizing fixed. Canonical for declined duplicate aa508852. |
| `98bd7498` | Brandon | 04-09 | RO Management | Delivered; re-verified at HEAD 0223cb3: per-silo Work List tabs give a separate list per job category (js/work-list.js _renderWorkListSiloTabs L223, _setWorkListSilo L263, both re-rendering renderWorkList). Nothing further to build. |
| `19dc2016` | Roland | 04-11 | Time Clock | Test entry ('Testing ER form / testing enhancement request form dictation'). Not a real request; no code mapping and no work item. Already declined in the DB; keep declined. |
| `1fe68261` | Rod Wombles | 04-13 | Work Orders | Delivered v1.467 (S128); re-verified at HEAD 0223cb3: js/shop-tasks.js (canSeeShopTasks L21, addShopTask L153, completeShopTask L180 behind the Done button L130) plus shop_tasks.sql track internal work not tied to a customer unit, separate from ROs and the board. |
| `3dfc0acb` | Lynn | 04-14 | Work Orders | Delivered v1.410; re-verified at HEAD 0223cb3: 'Ready to Work' is a live RO status (js/config.js STATUS_PROGRESS_MAP L158, slot 50) and appears in js/render.js ALL_STATUSES (L741) and statusColorMap (L727) and in both status dropdowns. |
| `b87eb2fb` | Lynn | 04-14 | RO Management | Delivered v1.458; re-verified at HEAD 0223cb3: keys_power_fields.sql adds key_status, keypad_code and keep_plugged_in; js/ro-crud.js persists them on create (L415-417) and edit (L492-494) and repopulates them on edit, with a keys chip on the card. Same build as 34fc03c2. |
| `a5ff3e2d` | Lynn | 04-21 | RO Management | Delivered v1.465; re-verified at HEAD 0223cb3: the 'Warranty + Repair' RO type exists end to end (js/ro-crud.js warranty_repair branch; js/render.js filter L122, compact chip L196, card badge L373) and stays billable so the repair half bills normally through work orders. |
| `a7d1474e` | Lynn | 04-23 | RO Management | Delivered v1.466; re-verified at HEAD 0223cb3: urgent_update_field.sql adds repair_orders.urgent_update, js/render.js L324 renders an always-visible red URGENT banner (plus the compact siren at L214), and setting it notifies silo managers and admins via urgent_update_notify (js/ro-crud.js L374). The follow-up 50175fce has since shipped in v1.498. |
| `dde90d2b` | Roland | 04-27 | RO Management | Delivered; re-verified on customer-checkin.html v1.17 at HEAD 0223cb3: the photo is optional and the outbound email is forced on every submission - it posts type raf_copy (L2530) and type lead_staff_notify (L2603) to send-quote-email. Nothing further to build. |
| `13e1cd87` | Tipton | 04-30 | Time Clock | Was a real gap; delivered v1.447 (checkin.html v1.34, now v1.38) and re-verified at HEAD 0223cb3: a 'Return to Dashboard' button renders after clock-out (checkin.html L1516), so the tech no longer has to open a new tab to clock into the next job. |
| `e27311bf` | Ryan | 05-04 | Work Orders | Delivered v1.464; re-verified at HEAD 0223cb3: the Build/Edit Work Order form gives every task row a grip handle and drag-to-reorder (js/work-orders.js _initWOTaskDrag L461, mouse dragstart L482 plus touch handlers); DOM order drives sort_order. |
| `d2561e11` | Brandon | 05-06 | New Feature | Delivered v1.457; re-verified at HEAD 0223cb3: key_dates_pickup_date.sql adds repair_orders.pickup_date, persisted in js/ro-crud.js (L414 create, L491 edit) and surfaced as the green Pickup key-date chip (js/render.js L397) and in the key-dates panel (L365). Canonical for declined duplicate 67daba15. |
| `dac9fdda` | Brandon | 05-06 | General UI/UX | Delivered v1.460; re-verified at HEAD 0223cb3: the 'Off Lot - Returning' status exists (js/config.js STATUS_PROGRESS_MAP L154, slot 15) and separates units temporarily taken by the customer from 'Not On Lot' units never on the lot; it is excluded from the on-lot count (js/render.js L718-721). |
| `a8e90a7d` | Brandon | 05-20 | General UI/UX | Delivered v1.456 (S117); re-verified at HEAD 0223cb3: the Manage Parts Ticket Totals bar sums parts, freight and total cost (js/parts.js totWholesale L152, totFreight L153, totCost L155, rendered L159-161). Same build as 671fa623. |
| `1aeb3f58` | Lynn | 05-21 | General UI/UX | Delivered v1.457; re-verified at HEAD 0223cb3: the promised date renders as a large bold key-date chip on the tile, red when overdue (js/render.js L396), and promise date plus days-on-lot both feed the blended calculatePriority sort (L30-39). The strict selectable sort is the open ER d1844e46. |
| `cbc70a86` | Lynn | 05-21 | General UI/UX | Delivered (S128); re-verified on customer-checkin.html v1.17 at HEAD 0223cb3: L2381 calls rpc('add_ro_to_silo_work_lists') (work_list_autoadd_rpc.sql, SECURITY DEFINER), which inserts manager_work_lists rows for each involved silo lead and falls back to sr-managers for unmapped silos. |
| `3f415c88` | Lynn | 05-26 | General UI/UX | Delivered v1.463; re-verified at HEAD 0223cb3: the Schedule Notification modal has Repeat none/daily/weekly plus a repeat-until date (index.html snRepeat L2993, read L3061) that expands into one individually cancellable scheduled_notifications row per occurrence. |
| `cd663d8f` | Bobby | 05-28 | General UI/UX | Was a cosmetic bug; fixed v1.447 and re-verified at HEAD 0223cb3: currency inputs show 2 decimals on edit-populate (js/ro-crud.js L1097 toFixed(2), marked [ER BUGFIX v1.447 S94]) and in the parts modal (js/parts.js MONEY_FIELDS L307, applied L310). No data loss occurred. |
| `34fc03c2` | Brandon | 05-29 | New Feature | Delivered v1.458; re-verified at HEAD 0223cb3: the Keys selector is on New and Edit RO (keys left / no keys / keypad plus code box and keep-plugged-in, keys_power_fields.sql; js/ro-crud.js L415-417 create, L492-494 edit) with a keys chip on each card. Covers b87eb2fb. |
| `0b2e128c` | Lynn | 06-02 | General UI/UX | Delivered v1.460 (checkin.html v1.38); re-verified at HEAD 0223cb3: a tech's clock-out work notes are copied into the RO update timeline as a notes-table ro_status row ('TECH NOTES (service): ...', checkin.html L1455). |
| `468aa376` | Lynn | 06-02 | General UI/UX | Delivered; re-verified at HEAD 0223cb3: the planned drop-off date input exists on New RO and (restored v1.457) Edit RO, persisted as planned_dropoff_date (js/ro-crud.js L413, L490) and feeding the Drop-off key-date chip (js/render.js L395) plus the auto_dropoff_reminder source. Same field as b2aa4bea. |
| `2b814250` | Roland | 06-07 | Calendar | Delivered; re-verified at HEAD 0223cb3: RO date/time values surface as colored key-date chips plus a hover/tap key-dates panel (js/render.js L395-397, kd-name-cal L367), server-side calendar integration runs through sync-ro-calendar (idempotent via repair_orders.cal_event_ids), and advance reminders go to managers/admins via scheduled_notifications. |
| `cd084233` | Roland | 06-07 | Work List | Delivered; re-verified at HEAD 0223cb3: admin-level daily manager-worklist reporting ships as send-manager-report (parts + labor + cost rollups) plus send-admin-pnl-report, with the worklist-report.html view. The manager scoring layer described at the end of the request remains a future extension. |
| `a22739b9` | Sofia | 06-08 | General UI/UX | Was a notify-gate regression; fixed and re-verified on customer-checkin.html v1.17 at HEAD 0223cb3: both sends are forced on every submission (raf_copy L2530 and lead_staff_notify L2603 to send-quote-email). Canonical for declined duplicate fa1c1d6e. |
| `91a03260` | Roland | 06-09 | Work List | Delivered; re-verified at HEAD 0223cb3: prune_cashiered_worklists.sql removes an RO from manager work lists the day AFTER it is marked Delivered / Cashed Out - it deliberately keeps the row visible on the day of cash-out. Canonical for declined duplicate 5057c7fc. |
| `cdd77a8b` | Brandon | 06-10 | General UI/UX | Delivered v1.453/v1.454; re-verified at HEAD 0223cb3: js/render.js updateStats splits the counts - the nonShop filter (L656-658) drops Shop Operations ROs from the queue figure and onLotCount (L718-721) excludes 'Not On Lot', 'Scheduled' and 'Off Lot - Returning' units. |
| `2c3d5633` | Ryan | 06-12 | General UI/UX | Was a mobile-only functional bug; fixed v1.453 and re-verified at HEAD 0223cb3: js/photos.js attaches the upload input to the DOM before clicking it (L66 document.body.appendChild(input)), which was the iOS Safari dropped-change-event root cause. |
| `74a33621` | Brandon | 06-16 | General UI/UX | Delivered v1.454; re-verified at HEAD 0223cb3: the photo lightbox supports keyboard arrow navigation plus Escape-to-close (js/photos.js keydown handler L400, ArrowLeft/ArrowRight -> navigateLightbox L396-397, fn L404) as well as prev/next buttons (L380, L382). |
| `298e0e67` | Sofia | 06-16 | General UI/UX | Was a data-integrity bug; fixed and STRENGTHENED in customer-checkin.html v1.17 (verified at HEAD 0223cb3): the returning-customer guard reuses an existing scheduled RO (reusedScheduledRO L2059, set L2165) and v1.17 adds a blocking attach-vs-new dialog (attachedToActiveRO L2060, set L2194). Canonical for declined duplicate fc6b29f9. |
| `9b823d25` | Roland | 06-16 | RO Management | Delivered v1.460; re-verified at HEAD 0223cb3: js/work-orders.js addServiceToRO (L244) queues a scheduled_notifications row with source 'service_added_notify' (L293; service_added_notify_source.sql), so when one silo adds a service to an RO the owning silo manager is emailed. |
| `9b046808` | Brandon | 06-17 | General UI/UX | Delivered v1.454; re-verified at HEAD 0223cb3: 'Scheduled' is its own RO status (js/config.js STATUS_PROGRESS_MAP L157, slot 45) and js/render.js excludes not-yet-arrived Scheduled units from the days-on-lot stats and from the on-lot count (L718-721). |
| `671fa623` | Brandon | 06-18 | General UI/UX | Delivered v1.456 (S117); re-verified at HEAD 0223cb3: the Manage Parts Ticket Totals include a Freight line summed across parts and roll it into Total Cost (js/parts.js totFreight L153, totCost L155, rendered L159-161). Same build as a8e90a7d. |
| `5c45bb8d` | Sofia | 06-19 | General UI/UX | Was a duplicate-RO plus missing-email bug (David Flanagan field report); fixed and re-verified on customer-checkin.html v1.17 at HEAD 0223cb3: the Scheduled returning-customer path reuses the existing RO (L2059, L2165, plus the v1.17 attach-vs-new confirm L2194) and the authorization-form copy is forced on every submission (L2530). |
| `b06a285f` | Lynn | 06-19 | General UI/UX | Delivered v1.465; re-verified at HEAD 0223cb3: a calendar icon beside the customer name opens a key-dates panel listing drop-off, promised and pickup in full on hover or tap (js/render.js kd-name-cal L367, rows L363-365), with the colored summary chips shipped v1.457 (L395-397). |
| `ee50d11c` | Lynn | 06-19 | General UI/UX | Was a real gap; delivered and re-verified at HEAD 0223cb3: time-off entries are editable rather than delete-only - time-off.html renders a per-row Edit button (L752) driving editRequest(), which loads the entry back into the form and flips the submit button to 'Update Record'. |
| `88395acc` | Lynn | 06-19 | General UI/UX | Delivered Time Off v1.5; re-verified at HEAD 0223cb3: partial-day absence is captured as an exact window (clock-out time to return time) rather than an hour total - add_absence_window_to_time_off.sql plus add_partial_hours_to_time_off.sql, with time-off.html modalStartTime / modalEndTime inputs (L368, L372). |
| `80390f36` | Lynn | 06-23 | General UI/UX | Delivered v1.462; re-verified at HEAD 0223cb3: js/render.js renderBoard pins Shop Operations tiles to the top of the board ahead of the priority sort (aShop L35, compare L37) and adds a 'shop operations shop' token to the search haystack (L61). |
| `7f2d0d70` | Lynn | 06-26 | General UI/UX | Delivered; re-verified at HEAD 0223cb3: send-dropoff-report is the automated Monday drop-off check email - it lists this week's scheduled drops (planned_dropoff_date window) plus last week's received units and reads manager_work_lists to flag any not yet on the right team's work list. Pairs with cbc70a86. |
| `3f7fb4fc` | Brandon | 06-29 | General UI/UX | Was a small data-display bug; fixed v1.468 and re-verified at HEAD 0223cb3: js/render.js excludes roType 'shop' ROs from RVs-in-Queue (nonShop L656-658) and from the on-lot count (L718-721), and the shop status dropdown is replaced by a static label (cep-static-status L249). |
| `cb7742a8` | Lynn | 06-29 | General UI/UX | Delivered v1.469 (check-in v1.15); re-verified at HEAD 0223cb3: sync-ro-calendar owns drop-off / promised / pickup events server-side and posts them to the correct team calendar for every RO including front-desk check-ins (customer-checkin.html POST L2240; idempotent via repair_orders.cal_event_ids). |
| `29654af7` | Lynn | 07-06 | General UI/UX | DONE in v1.498 (S178). index.html L8212-8220 renders #backToTopBtn with a smooth window.scrollTo({top:0}) handler plus a scroll listener that reveals it after meaningful scroll; css/dashboard.css L3479-3498 pins it bottom-left, clear of the toast stack. Canonical for duplicate 2b78696c. Previously bucket=needed/S. |
| `39b75fab` | Sofia | 07-09 | General UI/UX | Delivered v1.472; re-verified at HEAD 0223cb3: the Documents / Photos library supports rename, move-to-another-RO and delete, office-staff gated (js/photos.js canManageFiles L618 gating the per-doc button row L198-201 and the lightbox Move/Delete L376). |
| `1c7f1410` | Andrew Page | 07-16 | New Feature | Delivered; re-verified at HEAD 65fdb97 (v1.494). js/messaging.js input#msgAttach accepts image/*,application/pdf,video/* with multiple (L347), _msgAttachments is an array capped at MSG_MEDIA_MAX_COUNT 5 (L290/L295/L428-434) with removable chips (L386-392), and send splits video links from MMS media (L578-579). DB row is now status=done. |
| `3f254929` | Sofia | 07-16 | General UI/UX | (Data bucket - see the DATA section. DB row is status=done.) |
| `d432863a` | Brandon | 07-21 | General UI/UX | DONE in v1.498 (S178). Favicon markup is now in every page head - a grep for rel=icon / apple-touch-icon returns 3 hits each on 13 pages; index.html L263 carries the [ER d432863a v1.498 S178] marker. Also closes the long-standing S139 favicon-404 console noise. Previously bucket=needed/S. |
| `d4d0eeba` | Brandon | 07-22 | General UI/UX | DONE in v1.497 (S178) - both navigation paths fixed. index.html L4342-4344: the Clear Filters toast action now retries scrollToROInBoard after the re-render and reports an archived/missing RO instead of silently stopping. js/qr.js L198-214: the messages ?ro= deep link clears filters once, behind a one-shot _deepLinkClearedFilters guard. Also closes declined duplicates 9031e101 and 1bc33dd6. Previously bucket=bug. |
| `9d3806e7` | Brandon | 07-22 | General UI/UX | Delivered (S154); re-verified at HEAD 65fdb97 on messages.html: the details pane has a customer-side Notes section (detailNoteView L671, edit button L669) plus an Add-Notes modal (openNotesModal L1599) writing append-only rows in customer_note_entries (customer_note_entries_s154.sql, keyed by phone). DB row is now status=done. |
| `ac394468` | Roland | 07-24 | General UI/UX | Delivered v1.482 / S158; re-verified at HEAD 0223cb3: the erCategory select carries Messages / Texting plus Solar, Leads, Closed ROs, Reports, Time Off and Employee Guide, and js/enhancement.js pageDefaults (L43) maps every page. RESIDUAL, stated not hidden: erCategory exists in only 6 live HTML files, so messages / leads / time-off / guide cannot RAISE an ER - worth its own small ER. |
| `d62a26e4` | Roland | 07-24 | New Feature | Delivered v1.482 / S158a; re-verified at HEAD 0223cb3: js/messaging.js input#msgAttach carries multiple plus accept image/*,application/pdf,video/* (L347), _msgAttachments is an array (L290) capped by MSG_MEDIA_MAX_COUNT (L295, L428-435) with removable per-file chips (L386-392), and the send path splits video links from MMS media (L578-579). |
| `93b00023` | Roland | 07-24 | New Feature | Delivered v1.482 (S158); re-verified on textly-webhook v1.4 at HEAD 0223cb3, all three parts: a non-keyword inbound reopens a closed conversation and logs a 'reopened' conversation_events row (L487, L514-523); the assigned-owner notify fires only on the needs-reply flip (L617); and the 4:30pm sweep is send-unreplied-reminder with the weekday cron in unreplied_eod_reminder_s158.sql. |
| `d94a418a` | Brandon | 07-27 | General UI/UX | DONE in v1.497 (S178). js/render.js L33-39 formatNotesNewestFirst splits on the entry delimiter (the editField --- separator, or a newline before a [MM/DD/YY timestamp) and reverses ENTRIES, not lines; the old split-newline/map(escapeHtml)/reverse that flipped every line inside a single note is gone. Applied to both the RO Status box and Customer Comm. Previously bucket=bug. |
| `50175fce` | Lynn | 07-27 | General UI/UX | DONE in v1.498 (S178). js/ro-crud.js L404 defines editUrgentUpdate(index) and L1551 exports it; index.html L3968 wires the 'edit-urgent' card action and the red URGENT banner is now clickable to edit or clear. The write targets the RO UUID, is audit-logged, and still fires the urgent_update_notify path on set. Previously bucket=needed/S. |
| `09084bc5` | Lynn | 08-04 | General UI/UX | FIXED since the last pass - re-verified at HEAD 65fdb97 (v1.494); bucket moves bug -> done. js/ro-crud.js archiveROInSupabase L662-684 is now a single manager-gated archive_single_ro RPC call; cashiered_full_detail_s171.sql adds the 7 dropped columns (L32-38), archive_one_ro writes them (L95-108) plus all seven child mirrors (L114-141) before the delete (L144); closed-ros.html v1.3b restores them on reactivate (L898-904). |
| `225a2535` | Brandon | 08-05 | Bug Report | DONE in v1.497 (S178) - the cross-customer bleed is closed. js/insurance.js L127-129 stamps window._lastEstimateScan with roKey = editingROSupabaseId, and every consumer gates on it: renderCustomInsuranceFields L47-49 requires _scan.roKey to equal the open modal's UUID, and both save paths do the same (index.html L6633, L6810). Previously bucket=bug. |
| `ac8265c8` | Lynn | 08-05 | General UI/UX | DONE in v1.498 (S178). add_mileage_s178.sql (run live) adds repair_orders.mileage plus the cashiered twin column and recreates archive_one_ro so cash-out carries it. index.html L7670 adds the Edit RO input next to VIN, L6743 saves editMileage; js/ro-crud.js L567/L568/L1294 handle create/edit/load and js/render.js L523-526 prints it on the card under the VIN. Previously bucket=needed/S. |
| `5ca724ff` | Lynn | 08-06 | General UI/UX | DONE in v1.497 (S178). js/work-list.js L418-425: dragstart now calls e.dataTransfer.setData('text/plain', row.dataset.id \|\| dragSrcIdx) and sets effectAllowed = 'move', which is exactly what Firefox and some Chrome paths require before a drag will engage. The existing dragover/drop reorder and the bespoke touch path are untouched. Previously bucket=bug. |
| `fa35e3df` | Lynn | 08-18 | General UI/UX | DONE in v1.498 (S178). The low-contrast blues were lightened for the dark card surface: css/dashboard.css L560, L609, L1210 and the .compact-chip.ch-estimate rule at L3039 move #3b82f6 to #7eb3ff; L1930 moves .log-duration from #667eea to #a5b4fc; js/render.js L26 records the key-date Drop chip going #0a84ff to #5cadff. Previously bucket=needed/S. Note Lynn's follow-up size ask is the open ER 6ff111e7. |
| `6938c1af` | Ryan | 05-06 | General UI/UX | (Data bucket - see the DATA section. DB row is status=done.) |

## Not triaged this run (1)

| ER | By | Date | DB status | Note |
|---|---|---|---|---|
| `f73a1e5a` | Roland | 04-13 | declined | "Testing dictation 123 testing enhancement request dictation" - still carries no `triage_bucket` and was again absent from `needs_triage_ids`, so no verdict was produced. Reads as a test entry, already declined. Worth adding to one future full pass purely to give it a recorded verdict. |

---

## Recommended action today

1. **There is nothing on fire.** For the first time since this report series
   started, no ER carries a `bug` verdict. Every item below is planned work.

2. **Ship one messages release and close seven S items at once.** Andrew's new
   `776384e7` (quick responses) and his `472ccadb` (drag-drop attach) both land
   in `js/messaging.js` `composerExtrasHtml`, next to Brandon's `9aab7550`,
   `21fc3a9d`, `f1e6e311`, `6e9ca4dc` and `ca0f7ed9`. That is the densest,
   cheapest win on the board. Before building `776384e7`, ask Andrew for the
   starter snippet list - "Vroom parts deposit" is the only one he named, and
   whether the list is hardcoded or staff-editable is the only real design
   question (hardcoded = S, a `message_templates` table with an admin editor =
   M). Note `sms_schema.sql` L11 already anticipated an `sms_templates` table
   for "Stage 3"; that comment is the closest thing to a prior decision.

3. **Re-scope `af14b3cb` and `5925fc01` against the S186 Task Manager before
   touching either.** Their stored verdicts predate `tasks` / `task_events` /
   `send-task-reminders` and now overstate the gap. The remaining real ask is
   the RO-card bulleted list with who/when notation; `index.html` does not read
   the `tasks` table yet. This is the highest-value correction on the board and
   the runner deliberately did not re-bucket it - it is outside
   `needs_triage_ids`.

4. **Decline `611589ba`.** It is the only duplicate still `unreviewed`; both
   halves (CANCELED, Closed) are already tracked by `972af03c` and `44419de9`.
   One status flip, no code, open count 31 -> 30.

5. **Re-anchor the status batch before building it.** `44419de9`'s verdict
   counts 12 statuses at v1.494 and `611589ba`'s counts 15 at v1.500; v1.501
   added "Closed - No Charge". The 9-hardcoded-spot line numbers in those
   verdicts have moved. The S172 rule is unchanged: **both** CHECK constraints
   (`repair_orders` and the `cashiered` twin) or saves throw 23514.

6. **Pair the two parts-received ERs.** `2af47890` lands the client-side
   `dateReceived` stamp (its verdict also describes a live wipe path: a later
   `savePartForm` resends `date_received` null and the trigger will not
   restamp), and `1e434aff` renders the value in the Manage Parts table. One
   build, two ERs off the board.

7. **Still blocked on submitters:** `8d885331` (Lynn's parts field order),
   `47428f75` (Lynn's time-off ask), `e06d747e` (Riley's check-in float), and
   now the snippet list for `776384e7`. All are S once specified.

8. **Unchanged long game:** the two XLs (`14f35f95` parts inventory,
   `ed1b873e` auth replacement) each need their own scoping session with
   Roland; `0448de62` (remote e-sign) and `b19975d4` (parts returns, folds into
   GH#24) are the two Ls behind them.
