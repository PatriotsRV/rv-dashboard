# PRVS Dashboard — Claude Context History

> Historical archive. Read this at session end when updating completed work, version history, or session log. Companion to CLAUDE_CONTEXT.md.

---

## 📋 COMPLETED TODO ITEMS

> Archived from CLAUDE_CONTEXT.md active list.

| Priority | # | Task | Notes | Status |
|---|---|---|---|---|
| ✅ | GH#14 | **Parts chip states — Sourcing / Outstanding / Received / Estimate** | Four-state system: 🔍 Part Sourcing (neon orange), ⚠️ Parts Outstanding (yellow), ✅ Parts Received (green), 📋 Parts Estimate (blue, pulsing). `parts_status` column on `repair_orders`. Auto-flip to Received when all parts marked received. Manager/Admin set status modal. For Estimate Only toggle in Request Parts modal. Filter buttons for all four states. bobby@, solar@, brandon@ added to MANAGER_EMAILS + Supabase. | ✅ Done v1.284–v1.285 |
| 🟠 | GH#5 | **Work Assignment System — Phase 1 ✅ v1.295** | **DONE (Phase 1):** `staff` table (14 personnel seeded), `service_work_orders` + `service_tasks` tables, `is_silo_manager()` RLS function, `dollar_value` on `repair_orders`. 🔧 Work Orders button on every RO card → modal showing all 5 silos. Silo access control (canManageSilo / isSrManagerOrAdmin). Build/Edit WO form: status, dollar value, notes, full task list. Techs update own task status; managers update any in their silo. Dollar rollup → card refresh. **Pending (Phase 2):** (1) Lock urgency selector to Manager+Admin only. (2) GH#5 open questions: confirm multi-silo per RO ✅, task templates deferred to V1.5, Roof+PaintBody manager TBD (Ryan covers). (3) Tech "My Tasks" filtered view — techs currently see all tasks; a dedicated view showing only their assigned tasks across all ROs. (4) Task dependency system (V1.5 — Solar before Roof). | 🔄 Phase 1 done v1.295 — Phase 2 pending |
| 🟠 | GH#15 | **Virtual Lot Map + Parking Spot + QR Print** | Three-phase build: **(1) ✅ Parking Spot field — DONE v1.291/v1.292** — `parking_spot` dropdown on New/Edit RO + RO card yellow chip; lot positions R1–R20, B1–B14, Wash Bay 17/18, W1–W4, F1–F12; SQL migration: `ALTER TABLE repair_orders ADD COLUMN IF NOT EXISTS parking_spot TEXT`. **(2) ✅ QR Code print sheet — DONE v1.293** — `printQRLabel()` rebuilt: dual-sticker print window with 3"×3" windshield sticker (customer name, RV, RO#, parking spot chip) + 1"×1" key tag; QRCode.js generated fresh in print window; proper `@media print` CSS with dashed cut borders and physical inch sizing. **(3) Interactive Virtual Lot Map** — new dashboard view mirroring the physical whiteboard layout (upper bays, lower bays, outdoor spots); each cell shows customer name + repair type when occupied; Manager clicks to assign/move RV; color-coded by urgency; replaces the physical whiteboard. | 🔄 Phase 1+2 done — Phase 3 pending |
| ✅ | GH#13 | **Pre-deploy backup system** | `scripts/backup.sh` — 6-version rolling snapshots to `.backups/` before every push | ✅ Done |
| ✅ | GH#12 | **Spanish language toggle** | Full UI translation of presentation layer only — labels, buttons, modals, status text. DB stays English. `t()` function approach. Self-selectable toggle per user (localStorage). Globe icon in header. Both index.html (v1.277) and checkin.html (v1.27) done. | ✅ Done |
| ✅ | — | **Deploy roof-lookup Edge Function** | Confirmed deployed — 5 deployments, updated 2 days ago | ✅ Done |
| ✅ | — | **Test calendar re-auth on iPhone** | Full mobile OAuth round-trip flow — confirm Schedule modal reopens after auth | ✅ Done |
| ✅ | — | **GitHub Release v1.277** | Published by Roland | ✅ Done |
| ✅ | — | **GitHub Release v1.278** | Published | ✅ Done |
| ✅ | — | **GitHub Release v1.279** | Published | ✅ Done |
| ✅ | — | **Fix checkin.html Supabase auth for time_logs** | Fixed in v1.28/v1.301. `signInWithIdToken()` now called on sign-in; `getSession()` restores session on reload; `onAuthStateChange` keeps session in sync; `clockIn()` guards online insert. ✅ Anon INSERT/UPDATE policies removed 2026-04-05. | ✅ Done v1.28 |
| ✅ | — | **Remove anon policies on time_logs in Supabase** | Now that checkin.html v1.28 properly establishes an authenticated session, the temporary anon INSERT/UPDATE policies on `time_logs` can be safely deleted. Go to Supabase dashboard → Table Editor → time_logs → RLS Policies → delete "Anon can insert time_logs" and "Anon can update time_logs". | ✅ Done 2026-04-05 |
| ✅ | GH#16 | **Manager RO Work List** | Slide-in sidebar panel (right side, z-index 12000). "📋 My Work List" header button (visible to managers/sr_managers/admins). "📋 Add to My List" on every RO card. Stored in `manager_work_lists` Supabase table (manager_email, ro_id [UUID], ro_name, priority). Drag-and-drop reorder saves priority order. Sr Managers/Admins can view other managers' lists via picker dropdown. Click RO in list → scrolls board + blue highlight ring. Fixed 6 bugs during build: supabaseClient→getSB(), missing _initWorkListBtn definition, sorted-index mismatch (_supabaseId lookup), broken emoji escape (&#128203;), missing table columns (DROP+recreate), RLS policies. | ✅ Done — Session 31 |
| ✅ | — | **Confirm Merge Dupes button visible** | Confirmed working — button appears and merge flow executes correctly | ✅ Done |
| ✅ | — | **Fix Supabase rv-media bucket MIME types** | Roland confirmed bucket MIME list updated to include docx, xlsx, pptx, pdf, text, octet-stream | ✅ Done |
| ✅ | — | **Redeploy send-quote-email Edge Function v1.4** | photo_share type + CC repair@ on all emails — confirmed deployed | ✅ Done |
| ✅ | — | **Run SQL migration for Parts Request** | `has_open_parts_request BOOLEAN` column confirmed present in `repair_orders` table | ✅ Done |
| ✅ | — | **Redeploy send-quote-email Edge Function** | Confirmed deployed — 13 deployments, updated a day ago | ✅ Done |
| ✅ | GH#19 | **Enhancement Request Button** | Session 37: Floating genie lamp on all pages, 10-category modal + voice dictation, `enhancement_requests` table, admin view with filters, `send-er-report` Edge Function, pg_cron daily digest. | ✅ Done — Session 37 |
| ✅ | GH#22 | **Closed/Cashiered RO View** | Session 37: `closed-ros.html` v1.0 — card grid with photos, search/filter/sort, detail modal, reactivation (Managers+Admins). Yellow header button on dashboard (all users). | ✅ Done — Session 37 |

---
## ✅ Completed Work

- ✅ **GH#33 Phase 1 — DB UNIQUE partial index + checkin.html v1.30 guards (2026-04-21, Session 53, commit `6aabce0`)**: Migration `gh33_unique_open_time_log.sql` — `CREATE UNIQUE INDEX idx_time_logs_one_open_per_tech_ro ON time_logs (tech_email, ro_id) WHERE clock_out IS NULL`. Partial predicate excludes closed rows so legitimate re-clock-in after clock-out still works; a second OPEN row for same (tech, ro) is now physically impossible — 23505 unique_violation at INSERT. Zero duplicates pre-migration. checkin.html v1.28 → v1.30: `_clockInPending` module-level re-entry guard ignores re-taps during in-flight clockIn(); instant button lock on tap (`disabled=true` + spinner prefix + opacity 0.6) via `id="clockInBtn"`; pre-INSERT SELECT-guard checks for existing open row via `.eq('tech_email',...).eq('ro_id',...).is('clock_out', null).maybeSingle()`; if found, shows friendly toast and re-syncs local state; 23505 error handler is race-window backstop with same re-sync behavior; `drainQueue()` dedupe on offline-replay (SELECT-guard before INSERT, 23505 drop removes queued item to prevent replay loop); Spanish translation added for new "You are already clocked in on this RO" toast. Both root-cause dupe patterns (Mauricio 3-sec double-tap, Ignacio 8-sec offline-replay storm) killed at DB layer with defense-in-depth at client.

- ✅ **Twilio A2P 10DLC Registration — Stage 1 Setup (2026-04-21, Session 53)**: Full A2P 10DLC registration for Patriots RV Services LLC brand submitted and awaiting TCR vetting. Completed milestones: (1) Twilio account created at twilio.com with business email, upgraded to paid tier (trial accounts can't register A2P). (2) 10DLC number `+19404882313` purchased from Console → Phone Numbers → Buy a Number ($1.15/mo). (3) A2P Customer Profile submitted with `PATRIOTS RV SERVICES` (exact CP 575 name, no LLC suffix per EIN paperwork), EIN `92-2251112`, address `728 NORTHWOOD DR FLOWER MOUND TX 75022`, Direct Customer + LLC business type. (4) A2P Brand Registration submitted + APPROVED in ~1 minute (Low Volume Standard @ $4.50). (5) A2P Campaign Registration submitted (Low Volume Mixed use case, "Notify my users") — status: PENDING VETTING (1-5 day wait). Campaign includes 3 sample messages (shift-end reminder, ready-for-pickup, YES-extend confirmation), message flow description documenting verbal opt-in, phone-numbers content flag checked, embedded-links/lending/age-gated flags unchecked. (6) `+19404882313` attached to `Low Volume Mixed A2P Messaging Service` Sender Pool. (7) Messaging Service use case set to "Notify my users". (8) Privacy Policy + SMS Terms of Service updated on patriotsrvservices.com to A2P-compliant wording: new "SMS Communications" section in Privacy with the No-Share clause ("No mobile information will be shared with third parties or affiliates for marketing or promotional purposes"), STOP/HELP/message frequency/rates disclosures; Terms of Service expanded from 1-2 msg/week old copy to multi-use-case frequency disclosure covering both customer notifications (1-5 per service engagement) and employee reminders (up to 1 per workday). (9) Staff SMS opt-in populated for 17 of 18 rows via UPDATE ... FROM (VALUES ...) SQL block; Kevin (sr_manager, tester) intentionally excluded. Bobby/Brandon (parts_managers), Ryan/Roland (sr_managers), 9 techs + 4 managers all opted in with 2026-04-21 timestamp. Travis got his real phone number in-session after initial placeholder. Lynn added (though she's admin-only, not in staff — UPDATE no-op'd for her row as expected). Twilio identifiers saved: Account SID `AC[redacted-account-sid — see Roland's password manager under PRVS/Twilio]`, Auth Token NOT YET CAPTURED (Roland task), Phone Number `+19404882313`, Customer Profile SID `BU78337f7b8e9d72c3708b12f25475e30b`, Brand SID `BNb289df9a2a8b10497e69c1516f31d156`, Campaign SID `CM08facddb60de799a14f53235eb969f73`, Messaging Service SID `MGf1da01ce354e357be79f46a8172f2ec6`, External Brand ID `BDNA7GP`. Stage 2 (~30 min Cowork session after Campaign approval) will deploy edge functions + schedule 4:45 PM M-F pg_cron.

- ✅ **worklist-report.html v1.9 — auto-logout regime change (2026-04-21, Session 53, commit `346512b`)**: 12h cap cron killed (cron `auto-close-stale-time-logs` unscheduled + function `auto_close_stale_time_logs()` dropped). Replaced with M-F 5 PM CDT hard EOD close via new migration `replace_auto_logout_with_5pm_eod.sql` — creates `close_open_time_logs_eod()` SECURITY DEFINER function + schedules pg_cron `close-open-time-logs-eod` at `0 22 * * 1-5` (job ID 8). Function respects new `time_logs.extended_at` column (dormant until Twilio YES-extend replies activate it). Retroactive 12h → 8h labor recalc via `retroactive_12h_to_8h_fix.sql` — 9 past `auto_eod` rows rewritten to `clock_in + 8h` with `close_reason='auto_eod_8h_recalc'`. worklist-report.html audit query switched from `close_reason='auto_eod'` to `LIKE 'auto_%'`; render splits styling: `auto_eod_5pm` + legacy `auto_eod` → orange flag, `auto_eod_8h_recalc` → gray "8h recalc" (corrected, not flagged). Section header count reflects only flagged rows; when all prior auto-logouts are recalc'd, header switches to "all data-corrected" messaging. Dormant Twilio Stage 1 schema shipped: `staff.phone_number` + `staff.sms_opt_in_at` + `sms_log` table with RLS + `time_logs.reminded_at`/`extended_at`. Edge function skeletons `twilio-sms` + `twilio-webhook` committed (not yet deployed). Scott Kline's RO dupe cleanup: 7 rows deleted total (5 Ignacio 14:40 rapid-fire + 1 Mauricio 13:00 3-sec dupe + 1 synthetic Ignacio recalc over-written by real clock-out sibling caf749c4). Labor 28.25h → 19.02h on PRVS-27EF-5CEB 2026-04-17.

- ✅ **worklist-report.html v1.8 — NULL-silo fallback + Per-Silo Active Work List section (2026-04-21, Session 53, commit `f7fcd14`)**: `buildSiloLanes` was silently dropping every `manager_work_lists` row where `service_silo IS NULL`. Only Sr Managers populate that column (via silo tab picker in index.html); regular managers (Jason, Mauricio, Andrew, Riley) never set it, so their work list ROs never appeared in the chart. Fix: new helper `buildManagerSiloByEmail()` builds email → silo lookup from `staff.service_silo`, threaded through `renderLaborChart` → `buildSiloLanes` as NULL-fallback. parts_insurance intentionally excluded (not in SERVICE_SILOS). Part B: new collapsible **Per-Silo Active Work List** section pivots `_allWorkLists` by silo (not by manager), dedupes by `ro_id`, shows owner pills (Sr Manager blue, regular gray), sits between Labor Load chart and manager accordions. Each silo block: unique RO count, total hours, total labor $. Rows reuse `renderRORow` with `mIdx='ps'` for DOM ID distinctness. `togglePerSilo()` localStorage persistence (`prvs_report_persilo_open`). `scrollToROInReport()` updated to prefer manager-accordion row, fall back to per-silo row (force-opens collapsed section). 25/25 Node smoke-test assertions pass (manager→silo mapping, NULL-fallback bucketing, dedup across managers, owner aggregation, parts_insurance exclusion).

- ✅ **GH#32 Auto-logout stale time_logs (2026-04-19, Session 52)**: `supabase/migrations/auto_logout_time_logs.sql` — `time_logs.close_reason TEXT` column + `auto_close_stale_time_logs()` SECURITY DEFINER function (explicit `SET search_path = public`, RLS-bypassing for pg_cron) + pg_cron job `auto-close-stale-time-logs` daily at 02:00 UTC (9 PM CDT / 8 PM CST). Caps any open session > 12h old at `clock_in + 12h`, writes `close_reason='auto_eod'`. Migration run by Roland 2026-04-19 in Supabase SQL Editor. First manual invocation auto-closed 9 stale sessions from 2026-04-17 (Mauricio, Cooper, Ignacio × 7). Confirmed via `checkin.html:594-600` audit that auto-close releases the tech — next day they see Clock In available on any RO normally. DST offset caveat shared with other pg_cron jobs (update Nov 2026).
- ✅ **worklist-report.html v1.7 — Auto-logout audit on Staff Status tiles (2026-04-19, Session 52, commit `2410494`)**: `_allAutoClosed` query (time_logs joined with `repair_orders(ro_id, customer_name, rv, is_training)` where `close_reason='auto_eod'` AND `clock_out >= now()-14d`), added to `applyReportExclusions()` filter. Today's auto-closed rows in expanded tile detail get CSS class `auto-eod` (orange left-border + tint) with duration swapped for `⚠ auto Xh` badge. Prior-day auto-closes (last 14d not in today) render in a new `tile-auto-section` at the bottom of the expanded detail: `⚠ N auto-logouts · last 14 days` with date / clock-in–out / RO / duration per row. Tile's own live green/red color deliberately unchanged per Roland's per-session-only correction. Audit workflow: Roland expands a tile, spots a chronic pattern, goes to Supabase to adjust hours (inline edit deferred to GH#31 sub-item 5).
- ✅ **worklist-report.html v1.6 — Customer name in chart bar footer (2026-04-19, Session 52, commit `b0a2241`)**: Chart's per-bar label stack now reads `$73 · 12d · Roman · 808C-2` (bottom-up). Customer name is the primary identifier (bolder, ellipsis-truncated, `.lbl-customer` class). RO ID tail stays below as secondary disambiguator (smaller, dimmer, `.lbl-id` class). Both use `max-width: 100%; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;` so long names don't blow out narrow bar widths. Roland's ask: "The RO ID does not tell me *who* this RO is for while I'm looking at the chart." Full RO ID + RV + urgency + days still in the hover tooltip.
- ✅ **worklist-report.html v1.5 — Tester + training RO exclusion, bar spread fix, v1.4 urgency bug (2026-04-19, Session 52, commit `f675a8f`)**: New `isTester(email)` helper (resolves via `_allStaff` where `hourly_rate = 0`). New `applyReportExclusions()` runs after all loads in `loadAllData()`, mutating `_allWorkLists`, `_allROs`, `_allTimeLogs`, `_allActiveClockIns`, `_allTodayTimeLogs` (and `_allAutoClosed` added v1.7) to drop training ROs + tester time_logs. Manager-accordion `byManager` filter adds the tester check. `renderStaffTiles` `staffToShow` filter adds the tester check. SELECT changes: `is_training` + `urgency` on `repair_orders` (urgency was missing since v1.4 — all bar top-borders had been silently defaulting to gray, hidden bug caught and fixed in this pass). Active-clocks + today-logs joins add `is_training` for filter support. CSS fix for bars clustering on the left of each silo lane: `.labor-bar-wrap { flex: 1 1 48px; min-width: 48px; max-width: 100px; }` replacing `flex: 0 0 48px; max-width: 48px;`. Mobile breakpoint similarly grow-capped at 40/80. 7 exclusion-logic smoke assertions pass.
- ✅ **worklist-report.html v1.4 — Labor Load chart (2026-04-19, Session 52, commit `b9c5be8`)**: Pure-CSS stacked bar chart rendered above the manager accordions below the stats strip. One horizontal `.labor-silo-lane` per active silo (in `SERVICE_SILOS` order, skipping unsorted). Inside each lane, vertical bars per unique (silo, ro_id) pair from `_allWorkLists` — an RO on multiple silo lists appears in each. Bars sorted labor-cost desc. Bar height = labor $ (default) or hours via `$`/`Hrs` metric toggle in the header (state persisted to localStorage `prvs_report_labor_metric`). Global Y-scale = max across ALL bars for the active metric (peak readout shown in header). Bar styling: urgency color top-border (`urg-critical/high/medium/low` → red/orange/yellow/green), per-tech stacked segments colored by `techColor(email)` (deterministic HSL-hash, hue 170–330 avoiding urgency colors), yellow dashed reference line at `dollar_value` (cost mode only), corner ratio tag showing `laborCost / dollar_value` %. Labels below bar: primary metric (`$X,XXX` or `Xh`), days-on-lot, short RO ID tail (v1.6 swapped in customer name + ID). Tooltip: customer, RV, urgency, days, RO value, full labor breakdown. Click bar → `scrollToROInReport(roId)` smooth-scrolls the accordion row, forces open the manager body, flashes a 1.6s yellow highlight border (`ro-flash` keyframe). Collapsible section (`.labor-chart-body.collapsed`) default open, toggle state persists. 13 smoke-test assertions pass.
- ✅ **worklist-report.html v1.3 — Monetary analytics (2026-04-19, Session 52, commit `84a93dd`)**: `staff.hourly_rate NUMERIC(6,2) NOT NULL DEFAULT 0.00` column added via `supabase/migrations/add_staff_hourly_rate.sql`, seeded with 18 per-person rates from Roland (Ryan $40, Mauricio $24, Jason $30, Andrew $27, Riley $27, Bobby $33, Brandon $17, Nik $25, Ignacio $33, Tipton $17, Rod $25, Zak $25, Travis $15, Cooper $17, Rudy $27, Tommy $25; Roland/Kevin at $0 as testers). Sofia added by Roland via separate SQL at $0 — also tester. New JS helpers: `buildRateByEmail()`, `logHours(log)` (active clock-ins count against Date.now), `logCost(log, rateByEmail)` (lowercased email lookup with user_id fallback), `buildLaborStats(rateByEmail)` returning `{byRO, total}`, `fmtUsd(n)`. Built once per `renderReport()` pass. New per-RO labor chip in row strip with color gradient by labor/dollar_value ratio (<25% green, 25–50% amber, >50% red; no dollar_value falls back to absolute-dollar severity buckets). Per-silo labor subtotal in Sr Manager silo-subheader (`.silo-labor` span). Per-manager cumulative labor total in accordion header (`.manager-labor-total` span). Gold-bordered **Total Labor** chip on top stat strip (`.stat-chip.stat-chip-labor`). Admin-only (report is already admin-gated, no new guard needed). 4/4 scenarios pass in the smoke test — including active-clock-in counted against Date.now, case-insensitive email match, user_id fallback, unknown-tech/missing-rate → $0.
- ✅ **GH#30 Admin Delete RO — soft-delete with 1-week auto-scrub (2026-04-18, Session 51, v1.409)**: New soft-delete architecture on `repair_orders`. Added `deleted_at TIMESTAMPTZ` + `deleted_by TEXT` columns + partial index (migration `add_deleted_at.sql`). `loadDataFromSupabase` filters `.is('deleted_at', null)`. 🗑 Delete RO admin-only wrap in Edit RO modal footer next to Training toggle — confirmation dialog fetches and shows FK counts (notes/parts/time logs/audit entries). `softDeleteCurrentRO()` writes `deleted_at=NOW()` + `deleted_by=user.email` via `.eq('id', _supabaseId).select()` with row-count assertion so silent RLS rejections throw. 🗑 Recently Deleted admin-only header button next to Closed ROs with live count badge. `openRecentlyDeletedModal()` lists all soft-deleted rows with ↩ Restore (clears `deleted_at`) or 🗑 Delete Now (type-customer-name-to-confirm, explicit child cleanup mirroring scrub ordering). pg_cron job ID 6 `scrub-soft-deleted-ros` runs daily at `0 7 * * *` (02:00 CDT) via `scrub_soft_deleted_ros()` SECURITY DEFINER function — cascades service_tasks → service_work_orders → notes/parts/time_logs/audit_log/insurance_scans/manager_work_lists → repair_orders. Migrations run by Roland 2026-04-18 in Supabase SQL Editor. Feature tested end-to-end on Roman placeholder — soft-delete, Recently Deleted, Restore, soft-delete, Delete Now, verified permanently gone. Commit: `c6eecda`.
- ✅ **GH#29b DB dupe/test cleanup (2026-04-18, Session 51)**: Scan via `docs/dupe_detection_queries.sql` found only 1 dupe group — Roman / 2017 Thor Aria, created during today's check-in testing. Kept `1293920d` (PRVS-5493-808C-2, Awaiting parts, 5 audit entries). Hard-deleted placeholder `bc206e95` (PRVS-5493-808C, Not On Lot, 1 note) via new GH#30 feature. 7 SQL queries saved in repo for future ad-hoc scans. Fixed two column-name bugs in the queries: `time_logs` uses `ro_id` not `repair_order_id`; `audit_log.ro_id` is UUID not PRVS text id.
- ✅ **GH#29 Status-change bug fix — 13-site UUID-first lookup (2026-04-18, Session 51, v1.408)**: Managers and techs reported status changes not sticking. Root cause: 13 write-path lookups resolved `currentFilteredData[idx] → currentData` via `findIndex(item => item.customerName === ro.customerName && item.dateReceived === ro.dateReceived)` — with `(name, date)` dupes, `findIndex` returned the FIRST match, every write silently targeted the wrong row. Affected: updateROStatus/Urgency/Progress, editField, uploadPhoto/Document, setMainPhoto, archiveRO, openScheduleModal/confirmSchedule/proceedWithSchedule, openPartsModal, openEditRO. Fix: `ro._supabaseId ? currentData.findIndex(item => item._supabaseId === ro._supabaseId) : fallback(name+date)`. Applied via `patch_status_bug.py` Python regex (8 `item =>` sites + 5 `d =>` sites). Roland confirmed fix on dupe scenario AND unique ROs. Commit: `0a486e4`.
- ✅ **GH#29c Status-bug root-cause investigation (2026-04-18, Session 51)**: After v1.408 shipped, confirmed Supabase RLS policies permissive (`authenticated_full_access: ALL qual=true` + anon SELECT only) — silent-RLS theory dead. Roland's post-fix test showed unique-RO status changes stick. Leading hypothesis: v1.408 dupe-lookup fix resolved all staff-reported stickiness. Monitoring TODO (`GH#29c`) with DevTools Network playbook logged for if it resurfaces. Belt-and-suspenders follow-up: extend `.select()` + row-count assertion (already on GH#30 soft-delete) to all 13 v1.408 write sites. Not yet done.
- ✅ **v1.407 Tile bar wrap fix (2026-04-18, Session 51)**: `.view-mode-section` → `flex-wrap: wrap` + `row-gap: 12px`; stripped `margin-left: 12px` from all 8 header buttons so container `gap: 12px` handles wrapped-row spacing cleanly. Fixes Customer Check-In button overflowing off-screen. Discovered GH#25 (Customer Check-In header button) was already shipped — documented. Logged GH#26 (Not on Lot / On Lot statuses — already exist per Roland), GH#27 (drop-off RO lookup + pre-populate), GH#28 (Customer Arrived email) as 🟠 High intake-pipeline follow-ups. Commit: `1164a18`.
- ✅ **GH#19 Lead Staff Notification (2026-04-16, Session 50)**: Built staff notification email for `customer-checkin.html`. Fires on: (a) New Customer Entry submission (any work type), OR (b) RV Customer Drop Off submission when work type is `warranty` or `hybrid`. `send-quote-email` bumped v1.6→v1.8: added `lead_staff_notify` type with mode-aware subject tag (`🚨 {New|Returning} Customer{(Warranty)|(Hybrid)}{ — Drop Off}`), headerLabel branching (Warranty Drop Off / Hybrid Warranty Drop Off / New Customer Entry), branded HTML body (customer info, RV info, silos in canonical `SERVICE_SILOS` order, scheduling, warranty details, lead notes, dashboard deep-link CTA) + plain-text fallback. Recipient hardcoded to `repair@patriotsrvservices.com` (v1, later move to `app_config`). Shared-secret `X-PRVS-Secret` header auth (`PRVS_FUNCTION_SECRET` Supabase secret) — edge function validates header, returns 401 on mismatch. CORS updated with `x-prvs-secret` in `Access-Control-Allow-Headers`. Deployed with `--no-verify-jwt` flag (ES256 user JWT incompatible with `customer-checkin.html` unauthenticated context). Success toast on client after submit. customer-checkin.html v1.2→v1.3 wired up the payload build + post + toast.
- ✅ **Shared-Secret `X-PRVS-Secret` Pattern (2026-04-16, Session 50)**: Added `PRVS_FUNCTION_SECRET` constant to `index.html` (v1.406) and appended `X-PRVS-Secret` header to 4 edge function fetches: `photo_share`, `parts_ordered`, `parts_eta_update`, `parts_request`. Same pattern used on `customer-checkin.html` for `lead_staff_notify`. Rationale: browser-originated public-page calls need an auth gate without JWT verification. Rotation: update Supabase secret + both HTML constants, redeploy affected edge functions.
- ✅ **Mode Rename + Drop-Off Consistency Sweep (2026-04-16, Session 50)**: `customer-checkin.html` v1.3→v1.4 — UI labels changed "RV Drop-Off" → "RV Customer Drop Off", "Lead Conversion" → "New Customer Entry". Updated: mode tile titles/descriptions (description intentionally neutralized — doesn't imply "new customers only" since returning customers also use this path), header comment, form labels ("Planned Drop Off Date" ×2), review card ("Planned Drop Off:"), section comments. Internal `formState.mode` payload values (`'dropoff'`, `'lead'`) preserved. Variable/function names preserved (`dropoffChoice`, `leadDropoffDate`, `scheduledDropoffDate`, `plannedDropoff`, `selectDropoff()`, `.dropoff-opt`) — rename judged not worth regression risk. Edge function `send-quote-email` v1.7→v1.8: email body labels updated ("Planned Drop Off:", "New Customer Entry Notes", internal comments). Commits: ef505bc (mode rename), d4f0acb (sweep).
- ✅ **Live Test — Real Customer + Warranty Customer (2026-04-16, Session 50)**: Roland entered a real customer via `customer-checkin.html` front desk flow end-to-end — RO created, lead staff notification email delivered. Confirmed warranty customer drop-off variant also delivered as expected.
- ✅ **Worklist Report v1.2 — clickable tech tile expand (2026-04-14)**: Added clickable tech tiles to `worklist-report.html` Staff Status section. Clicking a tile expands to show the tech's full daily time log: each session with clock-in/out times, RO name, duration (or ACTIVE badge), gaps >5 min between sessions, and a summary line (session count + total hours). CSS expand panel + chevron rotation. Query 3b fetches today's `time_logs` with `repair_orders` join. `toggleTechTile()` function. All `escHtml`-protected. Commit: 3d62840.
- ✅ **send-manager-report v1.6→v1.8 (2026-04-14)**: v1.6 test fire confirmed multi-RV silo fix working. v1.7 changed RVs Waiting filter from ≥60 to ≥30 days on lot. v1.8 added red PARTS HOLD badge on individual RO line items with outstanding parts + named ROs in Key Flags PARTS HOLD section. Deployed as Supabase version 13. Commits: b0c3e5d (v1.7), 26d36e4 (v1.8).
- ✅ **GitHub Issue #20 — pg_cron DST update (2026-04-14)**: Created GitHub issue to track updating pg_cron UTC offset in November 2026 when clocks fall back from CDT to CST (13:00 UTC → 14:00 UTC for 8 AM local).
- ✅ **GH#23 Morning Manager Report — send-manager-report v1.5 (2026-04-13)**: Built `send-manager-report` Edge Function (v1.0→v1.5). Per-silo personalized email to all managers (sr_manager, manager, parts_manager). Section 1: Manager's Work List with cumulative dollar total. Section 2: RVs Waiting for their silo sorted by days on lot → urgency → RO type. Section 3: Key Flags (Critical, overdue >60 days, blocking parts). Red "YOUR WORK LIST IS EMPTY" banner per silo. Yellow "YOUR WORK LIST APPEARS STALE" banner (3+ days no update). Scheduled Mon–Fri 8 AM CDT via pg_cron job ID 5. Deployed and confirmed sending. v1.4 added dual-key roMap (UUID + text ro_id) for work list joins. v1.5 fixed sr_manager first-silo unassigned work list items. Commits: f875bc4 (v1.0), e369936 (v1.1), 0d8d462 (v1.2), 1055b82 (v1.2b), 665806b (v1.3), efe60bb (v1.3b), f28a2d7 (v1.3c), c7d2770 (v1.4), 4c3e9a7 (v1.5). Known issue deferred: data quality banner ($0.00 warning) not firing for sr_manager reports.
- ✅ **GH#18 orderedParts scope fix (2026-04-13)**: Fixed `orderedParts is not defined` error in Email Requestor / Part Ordered flow — variable scope issue in `_doNotifyPartsRequester()`. Commit: fc25a93.
- ✅ **Perplexity Computer Skills (2026-04-14, Session 48)**: Created `prvs-dashboard-startup` (Start Session protocol) and `prvs-dashboard-endsession` (End Session protocol) as persistent Perplexity account-level skills. Eliminates need to paste startup commands. Recovered crashed Session 47 context files.
- ✅ **customer-checkin.html backdrop redesign (2026-04-16, Session 49)**: Replaced marble CSS backdrop (radial gradients + veining overlay) with fixed full-bleed RV park mountain photo (`checkin-bg.jpg`). Photo on `body::before` with `saturate(1.25) contrast(1.05)` filter for vibrance. Light cream overlay (~20% opacity) on `body::after` for card readability. `z-index: -2` (photo) / `z-index: -1` (overlay). Commits: 71489b5, 6ac5c07.
- ✅ **Sr Manager Parts + Schedule Fix (2026-04-14, Session 47)**: Kevin McHenry (Sr Manager) couldn't see Set Parts Status or Schedule buttons. Root cause: 5 UI role gates only checked `isAdmin() || hasRole('Manager')`. Fix: added `hasRole('Sr Manager')` to all 5 gates. Commit: d5ddc36.
- ✅ **GH#24 Shop Operations RO — Phase 1 v1.405 (2026-04-14, Session 46)**: 5th RO type (`shop`) for non-customer parts ordering/returns. `.shop-hide` class, `data-was-required` pattern, purple badge, compact chip, filter button, Slack skip, Spanish translations. Converted existing shop ROs. Commits: 66a5fe8, ca07ba9, 13981a0.
- ✅ **GH#23 Morning Manager Report v1.8 (2026-04-14, Session 45)**: `send-manager-report` v1.8 deployed (Supabase version 13). Per-silo personalized email. ≥30 days lot filter. Red PARTS HOLD badge per RO. pg_cron Mon–Fri 8 AM CDT. Commits: b0c3e5d, 26d36e4.
- ✅ **Slack Integration GH#20 — v1.403 (2026-04-12)**: Built `slack-notify` Edge Function (v1.0→v1.1→v1.2) + 5 trigger points in index.html via `notifySlack()` fire-and-forget helper. Edge Function routes `{ event_type, payload }` to correct Slack webhook channel. v1.2 uses origin-only auth (validates `origin === 'https://patriotsrv.github.io'`). Events: `ro_created`/`ro_ready_pickup`/`ro_urgency_critical` → #ro-updates, `part_received`/`part_overdue`/`part_missing_eta` → #parts-alerts, `warranty_ro_opened` → #warranty-flags. Confirmed working: ro_created fires on new RO creation, ro_ready_pickup fires on status change. 3 triggers remaining to verify. Commits: 7e4746a, a729d7a, f0fc2b0.
- ✅ **send-parts-report v1.4–v1.7 (2026-04-12)**: v1.4 attempted thead row merge fix; v1.5 moved prompts to standalone div blocks (email-client safe); v1.6 added 15px fonts and numbered `<ol>/<li>` action steps per section; v1.7 minified all inline styles (~25-35KB reduction, prevents Gmail clipping). Morning email: "Good morning Bobby" + numbered priority order. Afternoon email: EOD checklist at bottom. Section prompts conditional on data count.
- ✅ **Warranty RO Type v1.402 (2026-04-12)**: Added 4th RO type to PRVS dashboard. Toggle button (red) in New/Edit RO modals. Auto-sets urgency to Critical on selection. Card shows red 🔄 Warranty badge + gray $0 No Charge badge. Warranty fields: Original RO# and Warranty Reason (stored as [WARRANTY: ...] prefix in notes). Filter button on board. Compact view chip. ro_type='warranty' stored directly in column. Commits: b241df3, 35c1c88.
- ✅ **Session 41 — send-parts-report v1.3 (2026-04-12)**: Updated Edge Function with contextual action prompts above each of 4 data sections; morning email adds "Good morning Bobby" banner with numbered priority order; afternoon email adds "Before You Leave Today" 6-item EOD checklist; conditional section prompts (empty = nothing to do, data = specific actions); plain text version updated to match. `isMorning` logic: `utcHour < 17` — 8 AM CDT = 13:00 UTC (morning path), 3 PM CDT = 20:00 UTC (afternoon path). Commit: 5445427.
- ✅ **Unified Search V1 (2026-04-12)** — Per UNIFIED_SEARCH_SPEC.md. 4 changes: (1) HTML search bar updated — label "🔍 Search:", wider placeholder listing all searchable fields, inline 🔍 icon. (2) JS filter in renderBoard() replaced — 10-field haystack (customerName, roId, rv, vin, technicianAssigned, repairDescription, parkingSpot, customerPhone, customerEmail, repairType), `.filter(Boolean).join(' ')`, case-insensitive includes. (3) TRANSLATIONS_ES updated with new keys. (4) setupI18n() data-i18n tags updated. Plus: post-render DOM highlight via TreeWalker wrapping matching text nodes in `<mark class="search-highlight">` — safe with escapeHtml since it operates on rendered DOM. `.search-highlight` CSS: gold background, inherits text color. Works in both standard and compact view. All existing filters chain via AND logic. Commit: 7a355a4.
- ✅ **Compact Manager View (2026-04-11)** — New 'compact' option in view-mode selector. Dense 6-column row layout: 56×42 RV photo thumbnail, customer/RO/RV, status/urgency, days/tech/spot, parts chips, action buttons. `viewPresets.compact` entry. Conditional render branch in `renderBoard()` `filtered.map()` callback (returns early for compact mode). `scrollToROInBoard()` updated to find `.ro-card-compact[data-ro-index]`. Tightened spacing (padding 6px/10px, gap 8px). "No Photo" placeholder when no image. Commits: 69ec674, 874a2ad.
- ✅ **Visual Cleanup Pass (2026-04-11)** — Calmed card animations (removed background-size pulse on .has-parts-request, softened chip-pulse, reduced gradient sweep on .parts-status-chip variants). Standardized button styling. Added utility classes: .card-ro-id, .card-parking-badge, .card-secondary-btn, .card-actions-primary, .card-actions-secondary. Reduced inline style proliferation.
- ✅ **Toast System (2026-04-11)** — 116 alert()→showToast, 4 confirm()→toast-with-action (splitfunction pattern with guard + _do* inner function), 3 destructive confirm() preserved. CSS toast stack (z-index 13000, fixed bottom-right) + JS IIFE. Toast types: success/warning/danger/info. Progress bar via rAF. Max 3 visible. Commit: 609201d.
- ✅ **Dead Code Cleanup (2026-04-11)** — 968 lines removed across 4 commits. Phase 1: 285 lines dead CSS. Phase 2: 584 lines dead JS (22 functions). Phase 3: 60 lines from secondary HTML files. Cross-file: 39 lines from solar.html. index.html 13,997→13,128.
- ✅ **Kenect Removal (2026-04-11)** — Kenect refused API keys. Removed 329 lines from index.html + 187-line Edge Function + dead vars. Updated backup.sh, MODULARIZATION_ROADMAP.md. Total 550 lines. Commit: c8bc2c8.
- ✅ **v1.400 Version Bump + slideIn Keyframe Fix (2026-04-11)** — Bumped v1.308→v1.400 for security remediation milestone. Fixed scanMilestoneBanner referencing non-existent `slideIn` keyframe — rewired to `toast-slide-in` (280ms cubic-bezier).
- ✅ **Security Remediation S4 — Anthropic API key to server-side proxy (2026-04-11)** — Created `claude-vision-proxy` Edge Function (JWT-verified proxy to Anthropic API, uses existing ANTHROPIC_API_KEY secret). Rewrote `callClaudeVision()` in index.html to POST to proxy instead of direct Anthropic API (removed apiKey param, added supabaseSession guard, apikey header for gateway validation). Removed API key input fields from New/Edit RO forms. Removed all `prvs_anthropic_key` localStorage operations + added one-time cleanup. Updated backup.sh. S4 hotfix: server-side JWT verification via `auth.getUser()`.
- ✅ **Security Remediation S3 — analytics + solar auth patch (2026-04-11)** — analytics.html: supabaseSession guard on submitEnhancementRequest. solar.html: Supabase client with persistSession/autoRefreshToken/storageKey (prvs_solar_auth), async session restore from getSession(), submitEnhancementRequest rewritten with session guard + real Supabase user identity. GH#17 created for full solar.html sign-in flow. Commit: 350bf35.
- ✅ **Security Remediation S2 — RBAC migration (2026-04-11)** — Replaced all hardcoded ADMIN_EMAILS/MANAGER_EMAILS/SR_MANAGER_EMAILS arrays across 5 HTML files with Supabase role-based access control. Phase 1: DB migration (users, user_roles, roles tables, 15 role entries, Sr Manager role added). Phase 2: index.html (12 steps — loadUserRoles, isAdmin/isSrManagerOrAdmin/isManagerOrAdmin rewritten, _staffCache integration, solar access fix). Phases 3-6: worklist-report.html (10 call sites), closed-ros.html (6 call sites), analytics.html (3 call sites + new signInWithIdToken flow), checkin.html (dead code removal). Commits: 0c04416, ee1c91e, 28c52f8, c920277.
- ✅ **Security Remediation S5 — DEBUG-gated logging + PDF guard (2026-04-11)** — `const DEBUG = false` + `log()`/`warn()` wrappers in index.html, checkin.html, closed-ros.html, worklist-report.html. 136 `console.log` → `log()`, 40 `console.warn` → `warn()`. 3 sensitive log lines permanently deleted (token preview, extracted data, id_token). PDF size guard (4.5 MB max) in `handleEstimateFile()`. Commit: f12d775.
- ✅ **Security Remediation S6 — Event delegation Phase 1 (2026-04-11)** — Delegated click + change listeners on `#boardGrid` at end of `setupEventListeners()`. 23 `onclick=` and 3 `onchange=` in card template replaced with `data-action`/`data-idx`/`data-sid`/`data-field` attributes. 21 action cases in switch statement. Commit: 9c81cf7.
- ✅ **Security Remediation S7 — CORS, session tokens, calendar config, search_path (2026-04-11)** — Dynamic `getCorsHeaders(req)` with `ALLOWED_ORIGIN` validation in 5 Edge Functions (kenect-proxy, roof-lookup, send-er-report, send-parts-report, send-quote-email). 5 Authorization headers in index.html changed to prefer `supabaseSession.access_token` over anon key. `CALENDAR_IDS` → `CALENDAR_IDS_FALLBACK` + `getCalendarId()` + `loadAppConfig()` from `app_config` table. `SET search_path = public` on `is_silo_manager()`. New migrations: `app_config_table.sql`, `fix_is_silo_manager_search_path.sql`. Commit: 385ddd5.
- ✅ **S1 XSS hotfix — 4 escapeHtml gaps (2026-04-11)** — `escapeHtml()` applied to: `ro.customerEmail` in card mailto link (href + display), `ro.customerEmail` in photo email input value, 4 parts table fields (`p.partName`, `p.partNumber`, `p.supplier`, `p.status`), `ro.status` in duplicate manager statusBadge. Total escapeHtml count: 44. Commit: d5acc07.
- ✅ **Supabase RBAC** — RLS on all 11 tables + storage, `has_role()` helper
- ✅ **checkin.html v1.26** — Supabase backend, offline-first IndexedDB queue
- ✅ **Nonce fixes (v1.262, v1.263)** — placement + hex encoding; Supabase auth working
- ✅ **Analytics button (v1.264)** — Admin-only, dark green, tile view header
- ✅ **Version tags** — Visible version badge on all 4 HTML files
- ✅ **Daily backup** — GitHub Actions, private repo, 30-day rolling, tested
- ✅ **GitHub PAT rotated** — `repo` + `workflow` scopes
- ✅ **solar.html v2.0** — React 18, roof planner, AI lookup via Edge Function proxy, PDF quote
- ✅ **PDF quote generation (solar.html)** — 2-page branded PDF via jsPDF + jspdf-autotable
- ✅ **Email PDF attachment** — send-quote-email Edge Function; PDF via nodemailer
- ✅ **RO Description fixes (v1.265, ×4)** — inline edit guard, fieldMap, NULL fallback, Edit modal textarea
- ✅ **Mobile filter cutoff fix** — `.filter-collapsible.open` max-height 300→600px
- ✅ **Calendar re-auth flow** — `reauthorizeCalendar()`, `prompt:'consent'`, `_pendingScheduleIndex` round-trip
- ✅ **Persistent Supabase auth** — `persistSession`, `autoRefreshToken`, `storageKey`, `onAuthStateChange`; 30-day sessions
- ✅ **`!accessToken` guard fixes** — 5 functions corrected to use `!getSB()`
- ✅ **Audit log user_id fix** — duplicate function removed; `user_id: supabaseSession?.user?.id` added
- ✅ **Chrome 145 nonce fix** — `params: { nonce: sbHashedNonce }` added
- ✅ **Supabase users 406 fix** — `getUserRole()` `.single()` → `.maybeSingle()`
- ✅ **Write function hardening (v1.266)** — Error handling + audit log: `updateFieldInSupabase`, `updateROStatus`, `updateROUrgency`, `updateROProgress`, `updatePhotoInSupabase`
- ✅ **Parts Request System (v1.267)** — `openPartsRequestModal()` with voice dictation; `submitPartsRequest()` writes notes (type `parts_request` + `ro_status`), sets `has_open_parts_request=true`, emails `parts@patriotsrvservices.com`; neon-pink pulsing chip + card border on RO card; `openPartsRequestDetails()` shows history; `markPartsOrdered()` clears flag + logs resolution; `send-quote-email` v1.2 handles `type:'parts_request'` branch
- ✅ **notes_type_check fix (v1.268)** — Changed parts request note insert from `type:'parts_request'` to `type:'ro_status'` with `🔩 PARTS REQUESTED:` body prefix; history query uses `.ilike('body', '%PARTS REQUESTED%')`
- ✅ **Parts Request photo attachments (v1.269)** — `_partsRequestFiles[]` global array + `previewPartsPhotos/removePartsPhoto/renderPartsPhotoPreview` helpers; orange "📷 Attach / Take Photo(s)" button in modal; thumbnails with × remove; on submit uploads via `uploadToSupabaseStorage`, adds to RO `photo_library`, passes `photoUrls[]` to Edge Function; `send-quote-email` v1.3 embeds photos as inline clickable thumbnails in email HTML
- ✅ **RO Description inline edit fix (v1.270)** — `showVoiceNotesModal` now accepts optional `prefillValue` param; modal pre-fills textarea with existing description, cursor placed at end; `editField` branches on `repairDescription`: full replace on save (not append), old value captured from `currentFilteredData` before mutation, `writeAuditLog` called with before/after; `roStatusNotes` and `customerCommunicationNotes` keep existing append + timestamp behavior
- ✅ **Document upload MIME fix (v1.272)** — `uploadToSupabaseStorage` accepts `skipContentType` option; `uploadDocument` passes `skipContentType:true` to bypass bucket MIME policy for non-image files; improved error message with fix instructions; Roland confirmed bucket MIME list updated in Supabase dashboard
- ✅ **Document modal refresh fix (v1.273)** — After document upload, `currentFilteredData[index].photoLibrary` explicitly synced from `currentData[originalIndex].photoLibrary`; `openPhotoLibrary` accepts `initialTab = 'photos'|'docs'` param; modal closes and reopens on Documents tab after upload so new doc is immediately visible
- ✅ **60-second visibility note (v1.274)** — Document upload success alert now tells user documents may take up to 60 seconds to appear due to Supabase Storage propagation delay
- ✅ **Photo lightbox viewer (v1.275)** — Photo thumbnails tap to open full-screen lightbox; `openPhotoLightbox(photoIdx, libIndex)` renders full-size photo with prev/next nav, photo counter, "💾 Open / Save" link (opens in new tab/prompts save), "⭐ Set as Main" button (non-main) or "Main Photo" label (main); `navigateLightbox(dir)` cycles photos; `closePhotoLightbox()` removes overlay; `window._libPhotos`, `_libMainUrl`, `_libRoIndex` set at modal open for lightbox navigation; "Set as Main" moved from grid thumbnails to lightbox view
- ✅ **Email photos to customer (v1.275)** — "📧 Email Photos to Customer" button appears below photo grid if `ro.customerEmail` exists and photos are present; `openPhotoEmailModal(index)` shows overlay with checkboxes per photo (all pre-checked), pre-filled recipient email, optional message textarea; `sendPhotosToCustomer(index)` calls Edge Function with `type:'photo_share'`; `send-quote-email` Edge Function v1.4 adds `photo_share` branch — branded customer email with inline photo grid, each photo as clickable full-size link; **Roland must redeploy Edge Function** for this to work
- ✅ **Pre-deploy backup system** — `scripts/backup.sh` creates timestamped snapshots of all 6 key files in `.backups/`, keeps last 6 versions, runs before every push per End of Session Checklist
- ✅ **Photo & document upload fix (v1.271)** — `uploadDocument` fully migrated from Google Drive (was using expired `accessToken` → 401) to Supabase Storage (`uploadToSupabaseStorage` → `addDocToLibrary`); `uploadPhoto` and `uploadDocument` guards changed from `!getSB()` to `!getSB() || !supabaseSession`; session re-check added inside async `onchange` callback; error message updated to "Session expired — please refresh"
- ✅ **Email photos auth fix (v1.276)** — `sendPhotosToCustomer` switched from `session?.access_token` to `SUPABASE_ANON_KEY` as Bearer token (consistent with `submitPartsRequest`); improved error handler checks `result.message` and HTTP status
- ✅ **CC on photo emails (Edge Function v1.4b)** — `send-quote-email` always CCs `repair@patriotsrvservices.com` on every photo_share email send
- ✅ **Spanish language toggle (v1.277, GH#12 — index.html)** — Globe toggle button (🌐 ES / 🌐 EN) in header; `TRANSLATIONS_ES` dictionary (~70 key/value pairs); `t(str)` helper (English key → Spanish or fallback); `getLang()`/`setLang()` reading/writing `prvs_lang` in localStorage; `translateStaticUI()` updates all `[data-i18n]` DOM elements and `[data-i18n-ph]` placeholders; `setupI18n()` tags static header/filter DOM elements at init() time; all RO card strings wrapped with `t()`: urgency options, status dropdown, info labels (Type/RV/VIN/Tech/Phone/Email/Address), section titles, placeholder texts, button labels, parts badges, insurance badges, QR section, time logs; `updateStats()` "RVs on Lot" translated; `setupI18n()` called via `setTimeout` at end of `init()`
- ✅ **Spanish language toggle (v1.27, GH#12 — checkin.html)** — Same `prvs_lang` localStorage key (shared with index.html); globe button in logo area; `TRANSLATIONS_ES` dict covering all check-in/out UI: RO info labels, clocked-in/out badges, timer header, service type section, work notes/dictation, action buttons, clock-out summary, auto clock-out modal, offline banner; `translateStaticUI()` called at DOMContentLoaded; `setLang()` triggers `render()` if already loaded
- ✅ **Duplicate ro_id 409 fix (v1.278)** — `generateROId()` is a deterministic 32-bit hash; same customer+RV+date (or any hash collision) produced identical IDs causing a Supabase 409 Conflict on insert. Added `async generateUniqueROId(name, rv, date)` which calls `generateROId()` then checks Supabase with `.maybeSingle()` before committing — if collision detected, appends `-2` through `-9`, then falls back to a base-36 timestamp suffix. Updated `appendToSupabase` to `await generateUniqueROId()`. Added try/catch at the New RO form submit handler so errors produce a user-visible alert instead of a silent uncaught promise rejection.
- ✅ **Video upload support (v1.279)** — Added `isVideoUrl(url)` helper (checks .mp4/.mov/.avi/.mkv/.webm/.m4v/.3gp extensions). `uploadPhoto` now accepts `image/*,video/*` and the image-only `continue` filter was widened to allow `video/` MIME types. Thumbnail grid renders a dark `🎬 VIDEO` tile for video URLs instead of a broken `<img>`. Lightbox renders `<video controls autoplay>` for videos and suppresses "Set as Main" (videos can't be an RO's main photo). Email modal shows videos as disabled/non-checkable with a `🎬 Video (not emailable)` label. "Add New Photo" button renamed "Add Photo / Video".
- ✅ **Fix RO insert race condition (v1.280)** — Replaced check-then-insert pattern (`generateUniqueROId` with pre-SELECT) with optimistic insert loop (`generateROIdCandidates` returns base + -2..-9 + timestamp fallback). `appendToSupabase` now tries each candidate in sequence; on `error.code === '23505'` it advances to the next; any other error is re-thrown. Eliminates the TOCTOU race where concurrent submits could both pass the pre-check and both fail on insert. Also added submit button disable/re-enable around the save call to prevent double-submissions.
- ✅ **Fix roId scope bug (v1.281)** — `appendToSupabase` optimistic insert loop used `for (const roId of candidates)`; the `console.log` after the loop referenced `roId` which is block-scoped to the loop body, causing `ReferenceError: roId is not defined`. Fixed by changing `console.log('✅ New RO saved:', roId)` to `console.log('✅ New RO saved:', data.ro_id)`.
- ✅ **Duplicate RO Manager (v1.282)** — `getBaseROId(roId)` strips `-2`/`-3`/`-XXXX` suffixes via regex to find the base PRVS hash. `findDuplicateGroups()` groups `currentData` by base ID and returns groups with 2+ members. Admin-only `🔗 Merge Dupes` button in header (hidden until dupes detected) with red badge count. `openDuplicateManager()` modal shows each group with radio buttons to pick the master RO. `executeDupeMerge()` reassigns foreign keys on `notes`, `parts`, `time_logs`, `insurance_scans`, `audit_log` tables; merges `photo_library` arrays; copies `rvPhotoUrl` from duplicate to master if master has none; DELETEs duplicate `repair_orders` rows; reloads data.
- ✅ **Fix isAdmin() timing bug for Merge Dupes button (v1.282b)** — `getUserInfo()` called with no Google token sets `currentUser = { email: 'unknown@user.com' }`. The `if (!currentUser)` guard in session restore then skips overwriting it with the real email. `isAdmin()` checks `currentUser.email` which is wrong. Fix: post-load dupe check now also checks `supabaseSession?.user?.email` directly against `ADMIN_EMAILS` (`const _sessionEmail = (supabaseSession?.user?.email || '').toLowerCase(); const _isAdminNow = isAdmin() || ADMIN_EMAILS.includes(_sessionEmail)`). Added `console.log('🔗 Dupe check: X duplicate(s) found, button shown/hidden')` for debugging.
- ✅ **Fix "Date RV Arrived on Lot" not saving (v1.283)** — `updateROInSupabase()` had `date_arrived` missing from its Supabase `.update()` payload. Value was correctly read from the Edit RO form into `formData.dateArrived` but silently dropped before the DB write. Fix: added `date_arrived: formData.dateArrived || null` to the payload. Explains why it appeared to work on some ROs (those whose date was auto-set via the status-change path) but failed on others (manually entered via Edit RO).
- ✅ **Parts chip states — three-state system (v1.284, GH#14)** — `parts_status` column added to `repair_orders` (CHECK constraint: 'sourcing', 'outstanding', 'received'). Three chips: 🔍 Part Sourcing (neon orange, pulsing), ⚠️ Parts Outstanding (yellow, pulsing), ✅ Parts Received (green). `openPartsStatusModal()` for Manager/Admin — click any chip to change state. `setPartsStatus()` writes to Supabase, logs note + audit entry. Auto-flip: `markPartReceived()` checks if all parts received/installed and calls `setPartsStatus(..., 'received')` automatically. Filter buttons: `ps-sourcing`, `ps-outstanding`, `ps-received`. `submitPartsRequest` sets `parts_status='outstanding'`. Backward compat with legacy `has_open_parts_request` boolean.
- ✅ **Parts Estimate chip + For Estimate Only toggle (v1.285, GH#14 cont.)** — Fourth state: 📋 Parts Estimate (blue, pulsing). Added to `parts_status` CHECK constraint. `For Estimate Only` toggle in Request Parts modal — sets `parts_status='estimate'` instead of `'outstanding'` when enabled. `syncEstimateToggle()` global function updates toggle visual (avoids inline `</script>` parser bug). Estimate button in `openPartsStatusModal`. `ps-estimate` filter button. Spanish: `ESTIMACIÓN DE PARTES`. bobby@, solar@, brandon@ added to `MANAGER_EMAILS` and Supabase `user_roles`.
- ✅ **ROLLBACK.md** — Emergency rollback guide committed to repo root. Step-by-step terminal instructions, version table with commands, hard-refresh note, GitHub Releases link, and direct Claude fallback option.
- ✅ **Rename Core Charge → Freight Charge (v1.286)** — UI label only in Add/Edit Part form Pricing section. DB column `core_charge` and JS variable `coreCharge` unchanged — no migration needed.
- ✅ **Wholesale + Retail Price columns in Manage Parts table (v1.287)** — Two new columns added between Qty and Quick: Wholesale (dark, right-aligned) and Retail (green, right-aligned). Formatted as `$X.XX` or `—` if not set. Empty-state colspan updated 7→9. git tag v1.287 created.
- ✅ **Sourcing added to individual part row status dropdown (v1.288)** — Added `'Sourcing'` to `PART_STATUSES` array (between In Transit and Received) and `PART_STATUS_COLORS` (neon orange #ff6a00, matching RO-level chip). Clarified that RO-level chips (Sourcing/Outstanding/Received/Estimate) are separate from per-part-row statuses.
- ✅ **Supplier Contact section moved to top of Add/Edit Part form (v1.289)** — New section order: 🏢 Supplier Contact → 🔩 Core Info → 📦 Order Info → 💰 Pricing → ✅ Receipt. Applies to both Add New Part and Edit Part (shared form).
- ✅ **Kenect messaging integration (v1.290, GH#10)** — `kenect-proxy` Supabase Edge Function committed (actions: test_credentials, get_locations, get_conversation, get_messages_by_phone, send_message, send_review_request). 💬 Messages button on RO cards (only when customerPhone exists). Chat-style conversation modal with inbound (left) / outbound (right) bubbles. Send message + Send Review Request buttons. Admin Settings Kenect section: Location ID input, Test Connection, Load Locations. `kenectCall(action, params, payload)` helper POSTs to Edge Function with anon key auth. Pending: Roland must deploy Edge Function + set KENECT_API_KEY secret.
- ✅ **Parking Spot field Phase 1 (v1.291, GH#15)** — `parking_spot` TEXT column added to `repair_orders`. Dropdown in New RO + Edit RO with grouped optgroups: Rear Lot (R1–R20), Interior Bays (B1–B14), Wash (Wash Bay 17/18, W1–W4), Front Lot (F1–F12). Yellow 📍 chip on RO card when spot assigned. SQL: `ALTER TABLE repair_orders ADD COLUMN IF NOT EXISTS parking_spot TEXT`.
- ✅ **Parking Spot lot remap (v1.292)** — All positions renamed from original whiteboard names to Roland's handwritten R/B/W/F system: R1–R20 (Rear Lot), B1–B14 (Interior Bays), Wash Bay 17, Wash Bay 18, W1–W4 (Wash), F1–F12 (Front Lot).
- ✅ **QR Print Sheet Phase 2 (v1.293, GH#15)** — `printQRLabel()` rebuilt as dual-sticker print window. 3"×3" windshield sticker: full QR code at 2.28", customer name, RV make, RO#, yellow parking spot chip. 1"×1" key tag: compact QR + RO#. Both generated fresh via QRCode.js in print window (no canvas-copy). `@media print` CSS with physical `in` units and dashed cut borders. Print button hidden on print.
- ✅ **QR scan → main RO tile deep link (v1.294, GH#15)** — QR codes now link to `/?ro={roId}` (main dashboard) instead of `checkin.html`. `handleDeepLink()` reads `?ro=` URL param, finds matching RO in `currentFilteredData`, scrolls to tile and pulses 3× blue highlight ring. `_deepLinkRoId` one-shot flag prevents re-triggering on subsequent `renderBoard()` calls. `openCheckIn()` (🚪 Tech Check In button) still goes to `checkin.html`.
- ✅ **Work Assignment System Phase 1 (v1.295, GH#5)** — `staff` table seeded (14 personnel: Ryan sr_manager, Mauricio+Jason repair managers, Andrew vroom, Riley solar, Bobby+Brandon parts_manager, 9 techs). `service_work_orders` + `service_tasks` tables + `is_silo_manager()` Supabase RLS function. `dollar_value` column added to `repair_orders`. `SERVICE_SILOS`, `WO_STATUS_LABELS`, `TASK_STATUS_LABELS`, `TASK_STATUS_COLORS`, `SR_MANAGER_EMAILS` constants. `MANAGER_EMAILS` updated (added jason@, riley@; removed solar@). `_staffCache`, `_workOrderCache` globals. `loadStaff()`, `isSrManagerOrAdmin()`, `canManageSilo()`, `getStaffTechs()`, `getStaffName()`, `loadWorkOrdersForRO()`, `openWorkOrderModal()`, `renderWorkOrderView()`, `openBuildWOForm()`, `buildWOTaskRowHtml()`, `addWOTaskRow()`, `submitWOForm()`, `updateTaskStatusWO()`, `computeAndSaveWORollup()` functions. 🔧 Work Orders button on every RO card. 5-silo Work Order modal. DB-level + JS-level silo access control. Dollar rollup triggers `renderBoard()`. `PRVS_WorkOrders_TrainingGuide.pdf` created.
- ✅ **Manager Training Guide PDF (Session 28)** — 16-responsibility training document for Manager role: 10 original items + 6 additions (Print QR Label, Assign Parking Spot, Assign Techs to Tasks, Set Dollar Amounts, Update Parts Status Chip, Log Customer Comm Notes). Saved as `PRVS_Manager_Training_Guide.pdf`.
- ✅ **Fix photo library null crash (v1.297, Session 28)** — `openPhotoLibrary()` crashed with `url.includes is not a function` on ROs with null/corrupt `photo_library` entries. Added sanitize step after `parseLibrary()`: filter `library.photos` to string-only, `library.docs` to non-null objects.
- ✅ **Video upload size error handling (v1.298, Session 28)** — Pre-flight 500 MB check in `uploadToSupabaseStorage()` catches oversized files before Supabase round-trip. Supabase error catch detects "maximum allowed size" and shows file size + actionable message.
- ✅ **Fix editField apostrophe/quote crash (v1.299, Session 28)** — `repairDescription`, `roStatusNotes`, `customerCommunicationNotes` oncclick attributes were embedding raw text as string literals — any apostrophe/newline/backtick caused `SyntaxError: Invalid or unexpected token`. Removed `currentValue` parameter from all three onclick attrs and `editField()` signature; function now reads value directly from `currentFilteredData[index][fieldName]`.
- ✅ **Add Kevin McHenry to SR_MANAGER_EMAILS (v1.300, Session 28)** — `kevin@patriotsrvservices.com` added to `SR_MANAGER_EMAILS` array. Staff table row confirmed `sr_manager` / NULL silo in Supabase. CLAUDE_CONTEXT.md staff roster updated.
- ✅ **Supabase Pro security cleanup (Session 28)** — Removed all anon write policies from 9 tables. Fixed `has_role` + `is_silo_manager` mutable search_path. Enabled leaked password protection + secure password change. Disabled new user signups. Restored anon INSERT/UPDATE on `time_logs` after checkin.html clock-in broke.
- ✅ **GH#16 Manager RO Work List (v1.303, Session 31)** — `manager_work_lists` Supabase table (manager_email, ro_id UUID, ro_name, priority). "📋 My Work List" header button (managers/sr_managers/admins). Slide-in sidebar panel with backdrop. "📋 Add to My List" on every RO card. `loadWorkList`, `addToWorkList`, `removeFromWorkList`, `renderWorkList`, `scrollToROInBoard`, `toggleWorkListPanel`, `_initWorkListBtn` JS functions. Drag-and-drop reorder with live Supabase priority update. Sr Manager/Admin picker to view other managers' lists. 6 bugs fixed during build.
- ✅ **Fix checkin.html Supabase auth for time_logs (checkin v1.28 / v1.301, Session 29)** — Root cause: `handleSignIn()` decoded Google JWT locally but never called `signInWithIdToken()` — every DB operation hit anon role. Fix: `generateNonce()` helper (hex SHA-256, same as index.html); nonce passed to GIS `initialize()` + `params` (Chrome 145 compat); `signInWithIdToken()` called with `rawNonce` in `handleSignIn()`; `supabaseSession` state var added; `getSB()` upgraded to `persistSession: true, autoRefreshToken: true`; `getSession()` called on `DOMContentLoaded` to restore 30-day session; offline identity fallback gated by `!isOnline`; `onAuthStateChange` listener; `clockIn()` guards online insert with `!supabaseSession`; `signOut()` calls `auth.signOut()`. Anon INSERT/UPDATE policies on `time_logs` removed 2026-04-05.
- ✅ **Work Orders bug-fix pass (v1.296, Session 27)** — 5 fixes: (1) `ro.supabaseId` → `ro._supabaseId` in 7 places — modal always showed "RO not found in database"; (2) `submitWOForm` guard `ro?.supabaseId` → `ro?._supabaseId` — save was silently no-op on every click; (3) All WO form inputs (status, dollar, notes, task title/desc/tech/status) removed dark `form-input`/`form-select` classes, switched to explicit white bg + dark text; (4) Silo emoji+label badge added to each task row header via `buildWOTaskRowHtml(index, task, opts, silo)`; (5) `openWorkOrderModal` now resolves filtered-list index to `currentData` index via `_supabaseId` lookup — was opening wrong RO when board was filtered/sorted. Roland added himself (`roland@patriotsrvservices.com`) to `staff` table as `sr_manager` via SQL to satisfy `is_silo_manager()` RLS check.

---

## 📜 Version History

| Version | Date | Summary |
|---|---|---|
| v1.0 | Early 2025 | Initial dashboard (Google Sheets backend) |
| v1.1 | — | Supabase migration begins |
| v1.26 | 2026-03 | checkin.html — Supabase backend, IndexedDB offline queue |
| v1.262 | 2026-03-19 | Fix nonce placement in `google.accounts.id.initialize` |
| v1.263 | 2026-03-19 | Fix nonce encoding (hex not base64); localStorage persistence |
| v1.264 | 2026-03-19 | Analytics button (Admin only); version badges on all files |
| solar v2.0 | 2026-03-19 | solar.html rebuilt — React 18, roof planner, AI lookup, PDF quote |
| v1.265 | 2026-03-20 | 4× RO description fixes; mobile filter fix; calendar re-auth; persistent auth; 406 fix; audit log fix |
| v1.266 | 2026-03-20 | Write function hardening — error handling + audit log on 5 functions |
| v1.267 | 2026-03-20 | Parts Request System — modal, voice dictation, neon-pink pulsing chip, auto-note, management email, Mark Ordered flow |
| v1.268 | 2026-03-20 | Fix notes_type_check constraint — use ro_status type + body prefix for parts request notes |
| v1.269 | 2026-03-20 | Parts Request photo attachments — upload to Storage, add to photo_library, inline email thumbnails |
| v1.270 | 2026-03-20 | RO Description inline edit — pre-fill modal with current content, full replace, before/after audit log |
| v1.271 | 2026-03-20 | Fix photo & document uploads — uploadDocument migrated to Supabase Storage; supabaseSession guard on both upload paths |
| v1.272 | 2026-03-22 | Fix document upload MIME rejection — skipContentType option on uploadToSupabaseStorage; document uploads bypass bucket MIME policy; improved error message |
| v1.273 | 2026-03-22 | Fix document modal refresh — sync currentFilteredData photoLibrary after addDocToLibrary; openPhotoLibrary initialTab param; reopens on docs tab after upload |
| v1.274 | 2026-03-22 | Add 60-second visibility note to document upload success alert |
| v1.275 | 2026-03-22 | Photo lightbox viewer (tap to view/save, prev/next nav, Set as Main from viewer); Email photos to customer (send selected photos to RO customer email via Edge Function); Edge Function v1.4 adds photo_share type |
| v1.276 | 2026-03-22 | Fix email photos auth — sendPhotosToCustomer uses SUPABASE_ANON_KEY (not session JWT); improved error handler |
| v1.277 | 2026-03-22 | Spanish language toggle (GH#12) — globe button, TRANSLATIONS_ES dict, t() helper, translateStaticUI(), setupI18n(); full RO card + stats translation |
| v1.278 | 2026-03-23 | Fix duplicate ro_id 409 crash — generateUniqueROId() checks for collision before insert, appends -2/-3 suffix; try/catch at submit handler for user-visible errors |
| v1.279 | 2026-03-23 | Video upload support — isVideoUrl() helper; Photos tab accepts video/*; 🎬 tile in grid; <video controls> in lightbox; email modal disables videos |
| v1.280 | 2026-03-23 | Fix RO insert race condition — optimistic insert loop replaces check-then-insert; 23505 → auto-retry next suffix; submit button disabled during save |
| v1.281 | 2026-03-23 | Fix roId scope bug — console.log after insert loop used block-scoped loop var; changed to data.ro_id |
| v1.282 | 2026-03-23 | Duplicate RO Manager — Admin-only 🔗 Merge Dupes button with badge count; modal groups dupes by base PRVS hash; pick master per group; merge notes/parts/time_logs/insurance_scans/audit_log FKs + photo_library; delete duplicates |
| v1.282b | 2026-03-23 | Fix isAdmin() timing bug — Merge Dupes button wasn't showing because getUserInfo() pollutes currentUser.email; post-load dupe check now also checks supabaseSession?.user?.email against ADMIN_EMAILS directly |
| v1.283 | 2026-03-23 | Fix "Date RV Arrived on Lot" not saving — date_arrived was missing from updateROInSupabase() payload; silently dropped on every Edit RO save |
| v1.284 | 2026-03-24 | Parts chip states (GH#14) — three-state system: 🔍 Part Sourcing (neon orange), ⚠️ Parts Outstanding (yellow), ✅ Parts Received (green); parts_status column; openPartsStatusModal; auto-flip on all parts received; filter buttons ps-sourcing/ps-outstanding/ps-received; submitPartsRequest sets parts_status='outstanding' |
| v1.285 | 2026-03-24 | Four-state parts system + Estimate (GH#14) — 📋 Parts Estimate chip (blue, pulsing); For Estimate Only toggle in Request Parts modal; syncEstimateToggle() global; ps-estimate filter; Spanish ESTIMACIÓN DE PARTES; bobby@/solar@/brandon@ added to Manager role; ROLLBACK.md created; git tag v1.285 |
| v1.286 | 2026-03-25 | Rename "Core Charge" label to "Freight Charge" in Add/Edit Part form — UI label only, DB column unchanged |
| v1.287 | 2026-03-25 | Add Wholesale and Retail Price columns to Manage Parts table — between Qty and Quick, formatted $X.XX or —; colspan 7→9; git tag v1.287 |
| v1.288 | 2026-03-25 | Add Sourcing to individual part row status dropdown — between In Transit and Received; neon orange color matching RO-level chip |
| v1.289 | 2026-03-25 | Move Supplier Contact section to top of Add/Edit Part form — new order: Supplier Contact → Core Info → Order Info → Pricing → Receipt |
| v1.290 | 2026-03-26 | Kenect messaging integration (GH#10) — kenect-proxy Edge Function; 💬 Messages button on RO cards; chat-style conversation modal; send message + review request; Admin Settings Kenect section |
| v1.291 | 2026-03-27 | Parking Spot field Phase 1 (GH#15) — parking_spot dropdown on New/Edit RO; yellow 📍 chip on RO card; all R/B/W/F lot positions; SQL migration |
| v1.292 | 2026-03-27 | Parking Spot lot remap — positions renamed per handwritten photo: R1–R20, B1–B14, Wash Bay 17/18, W1–W4, F1–F12 |
| v1.293 | 2026-03-27 | QR Print Sheet Phase 2 (GH#15) — dual-sticker print window: 3"×3" windshield sticker + 1"×1" key tag; QRCode.js fresh-generated; @media print CSS with physical inch sizing |
| v1.294 | 2026-03-27 | QR scan → main RO tile deep link — QR codes link to /?ro= instead of checkin.html; handleDeepLink() scrolls to tile + 3× blue highlight pulse; openCheckIn() (Tech Check In) unchanged |
| v1.295 | 2026-03-30 | GH#5 Work Assignment System Phase 1 — staff table (14 personnel), service_work_orders + service_tasks DB tables, is_silo_manager() RLS, dollar_value on repair_orders; SERVICE_SILOS / WO_STATUS_LABELS / TASK_STATUS_LABELS constants; SR_MANAGER_EMAILS; updated MANAGER_EMAILS (added jason@, riley@); 🔧 Work Orders button on every RO card; modal with all 5 silos; Build/Edit WO form; task CRUD; silo access control; dollar value rollup → card refresh |
| v1.296 | 2026-03-30 | Work Orders bug-fix pass (Session 27) — 5 fixes: (1) ro.supabaseId → ro._supabaseId in 7 places (modal showed "RO not found"); (2) submitWOForm guard fix (save was silent no-op); (3) all WO form inputs dark-theme fix (status/dollar/notes/task fields switched to white bg + dark text); (4) silo badge on each task row header; (5) openWorkOrderModal resolves filteredIndex → currentData index via _supabaseId (wrong RO was opening when board was filtered/sorted) |
| checkin v1.27 | 2026-03-22 | Spanish language toggle for checkin.html — same prvs_lang key, full check-in/out flow, auto clock-out modal, clock-out summary, offline banner translated |
| v1.297 | 2026-04-02 | Fix photo library crash on null entries — openPhotoLibrary() filters library.photos to string-only and library.docs to object-only immediately after parseLibrary(); prevents `url.includes is not a function` on corrupt/null array entries |
| v1.298 | 2026-04-02 | Fix video upload size error messaging — pre-flight 500 MB check in uploadToSupabaseStorage() throws user-friendly error before upload attempt; improved Supabase StorageApiError catch with file size in MB |
| v1.299 | 2026-04-02 | Fix editField apostrophe/quote crash — removed currentValue param entirely; editField(index, fieldName) reads value directly from currentFilteredData[index][fieldName]; eliminates SyntaxError when RO description contains apostrophes or quotes |
| v1.300 | 2026-04-02 | Add Kevin McHenry (kevin@patriotsrvservices.com) to SR_MANAGER_EMAILS array |
| v1.302 | 2026-04-05 | GH#5c + GH#5b — Work Orders polish + Task Templates: WO modal filters to active RO services; + Add Service button; ⏱️ Est. Hours per task (est_hours NUMERIC(5,2), rolls up to silo header); Save as Template / Load Template / Replace or Merge per silo; wo_task_templates + wo_template_tasks tables; template overlay z-index 100000; outside-click lock on New RO + WO modals. |
| v1.303 | 2026-04-05 | GH#16 Manager RO Work List — header button, slide-in panel, Add to My List on RO cards, drag-to-reorder, Supabase storage, Sr Manager view-other-lists picker. 6-bug fix pass: getSB(), _initWorkListBtn definition, _supabaseId lookup, emoji entity, table schema, RLS policies. |
| checkin v1.28 / v1.301 | 2026-04-05 | Fix Supabase auth for time_logs — `signInWithIdToken()` now called; `getSession()` restores 30-day session; `generateNonce()` added (hex SHA-256); `onAuthStateChange` listener; `clockIn()` session guard; `getSB()` uses `persistSession: true`; `signOut()` calls `auth.signOut()`. Anon policies on `time_logs` can now be removed. |
| v1.305 | 2026-04-06 | GH#16 enhancement — Sr Manager silo-specific work lists; silo tabs in sidebar; silo picker on add; service_silo column; worklist-report.html v1.1. |
| v1.306 | 2026-04-09 | Fix parts_status auto-flip gap — savePartForm() checks all parts Received/Installed after edit. |
| v1.307 | 2026-04-09 | GH#19 Enhancement Request system — genie lamp button on all pages, modal with category + voice dictation, admin view, send-er-report Edge Function, pg_cron daily digest. Parts request auto-creates Sourcing row in parts table. |
| v1.308 | 2026-04-09 | GH#22 Closed RO Archive — closed-ros.html v1.0 (card grid, search/filter, reactivation). Yellow "Closed ROs" header button (all users). Parts badge fix (Sourcing→"Requested" label, yellow chip). Work List remove button fix (DOM construction replaces inline onclick). |
| worklist-report v1.2 | 2026-04-11 | S2 Phase 3 RBAC migration — loadUserRoles, role-based isAdmin, _allStaff lookups, hardcoded ADMIN_EMAILS/SR_MANAGER_EMAILS/MANAGER_EMAILS arrays removed. |
| closed-ros v1.1 | 2026-04-11 | S2 Phase 4 RBAC migration — loadUserRoles, role-based isAdmin/isManagerOrAdmin/hasRole, _allStaff lookups, hardcoded email arrays removed. |
| analytics v1.1 | 2026-04-11 | S2 Phase 5 RBAC migration — loadUserRoles, async DOMContentLoaded/handleSignIn with signInWithIdToken, role-based isAdmin gate, hardcoded ADMIN_EMAILS removed. S3: supabaseSession guard on submitEnhancementRequest. |
| v1.400 | 2026-04-11 | Security Remediation milestone (S1–S7 complete). Version bumped from v1.308. Toast System: 116 alert→showToast, 4 confirm→toast-action, CSS+JS IIFE. Visual cleanup: calmed animations, standardized buttons, utility classes. Dead code cleanup: 968 lines removed. Kenect removal: 550 lines removed. Compact Manager View: dense 6-column row layout with RV photo thumbnail, viewPresets entry, conditional render branch. |
| checkin v1.29 | 2026-04-11 | S2 Phase 6 — dead ADMIN_EMAILS constant removed. |
| solar v2.1 | 2026-04-11 | S3 — Supabase client with persistSession + storageKey (prvs_solar_auth), session restore, session-guarded ER submit. |
| claude-vision-proxy v1.0 | 2026-04-11 | S4 — Edge Function proxying Claude Vision API calls with server-side ANTHROPIC_API_KEY. JWT-verified via auth.getUser(). CORS for patriotsrv.github.io. |
| — | 2026-04-11 | S5 — DEBUG-gated logging (`const DEBUG = false` + `log()`/`warn()` wrappers) in index/checkin/closed-ros/worklist-report. 136+40 console calls wrapped. 3 sensitive logs deleted. PDF 4.5 MB size guard. |
| — | 2026-04-11 | S6 — Event delegation Phase 1: delegated click+change on #boardGrid. 23 onclick + 3 onchange in card template → data-action attributes. 21 switch cases. |
| — | 2026-04-11 | S7 — CORS origin validation (getCorsHeaders) in 5 Edge Functions. Session token preference on 5 fetch calls. Calendar config table (app_config + getCalendarId). search_path fix on is_silo_manager. |
| — | 2026-04-11 | S1 hotfix — escapeHtml on 4 XSS gaps: customerEmail (×2), parts table fields (×4), statusBadge. Total escapeHtml: 44. |
| v1.401 | 2026-04-12 | Compact View Mobile Expansion — tap row body (≤768px) to expand inline panel (status/urgency dropdowns, tech, parking spot, quick-action buttons). Chevron affordance. One row at a time. Desktop compact unchanged. |
| v1.400 | 2026-04-11 | Security Remediation S1–S7 complete + Toast System (116 alert→showToast) + Visual cleanup + Dead code removal (968 lines) + Kenect removal (550 lines) + Compact Manager View + Unified Search V1 |
| send-parts-report v1.3 | 2026-04-12 | Session 41 — Contextual action prompts above each of 4 data sections; morning "Good morning Bobby" banner with numbered priority order; afternoon "Before You Leave Today" 6-item EOD checklist; conditional section prompts (empty = nothing to do, data = specific actions); plain text updated. `isMorning: utcHour < 17`. Commit: 5445427. |
| send-parts-report v1.7 | 2026-04-12 | Session 41 (cont.) — v1.4 thead row merge fix; v1.5 standalone div block prompts (email-client safe); v1.6 15px fonts + numbered `<ol><li>` action steps; v1.7 minified inline styles (~25-35KB reduction, prevents Gmail clipping). Commits: 4bf3d6d, 25e7f9a, 0bc2c1e, 7f6b41e. |
| v1.402 | 2026-04-12 | Warranty RO Type — 4th RO type (alongside standard, insurance, internal). Toggle button (red) in New/Edit RO modals. Auto-Critical urgency. Red 🔄 Warranty badge + gray $0 No Charge badge. Original RO# + Warranty Reason fields (stored as [WARRANTY: ...] prefix in notes). Filter button. Compact chip. ro_type='warranty' in column. "Filter by Insurance" → "Filter by RO Type" label. Commits: b241df3, 35c1c88. |
| v1.403 | 2026-04-12 | Slack Integration GH#20 — `slack-notify` Edge Function v1.2 (origin-only auth) + 5 trigger points in index.html via `notifySlack()`: ro_created, ro_ready_pickup, ro_urgency_critical, part_received, warranty_ro_opened. 3 Slack channels: #ro-updates, #parts-alerts, #warranty-flags. Confirmed working: ro_created + ro_ready_pickup. Commits: 7e4746a, a729d7a, f0fc2b0. |
| v1.404 | 2026-04-13 | Training RO flag (GH#24) — `is_training` boolean on `repair_orders`, badge, filter, Slack/report exclusions. DB migration: `ALTER TABLE repair_orders ADD COLUMN IF NOT EXISTS is_training BOOLEAN DEFAULT FALSE`. Commit: 84e538a. |
| v1.405 | 2026-04-14 | Shop Operations RO (GH#24 Phase 1) — 5th RO type (`shop`) for non-customer parts ordering/returns. `.shop-hide` class hides customer fields. `data-was-required` pattern for hidden required fields. Purple badge, compact chip, filter button. Slack skip. Spanish translations. Commits: 66a5fe8, ca07ba9, 13981a0. |
| v1.405 (fix) | 2026-04-14 | Sr Manager Parts + Schedule Fix — added `hasRole('Sr Manager')` to 5 UI role gates (Set Parts Status button, 3 Schedule buttons, `openPartsStatusModal` guard). Fixes kevin@ Sr Manager access. Commit: d5ddc36. |
| v1.406 | 2026-04-16 | `PRVS_FUNCTION_SECRET` constant + `X-PRVS-Secret` header on 4 edge function fetches (photo_share, parts_ordered, parts_eta_update, parts_request). Paired with customer-checkin.html lead staff notification flow (same shared-secret pattern). |
| v1.407 | 2026-04-18 | Header tile bar wrap fix (Session 51) — `.view-mode-section` → `flex-wrap: wrap` + `row-gap: 12px`; stripped `margin-left: 12px` from all 8 header buttons so container `gap: 12px` handles wrapped-row spacing cleanly. Fixes Customer Check-In button overflowing off-screen. Commit: 1164a18. |
| v1.408 | 2026-04-18 | GH#29 Status-change bug fix (Session 51) — 13 write-path lookups switched from `(customerName + dateReceived)` to UUID-first `ro._supabaseId` with name+date fallback. Fixes wrong-row writes when dupes exist. Applied via patch_status_bug.py. Commit: 0a486e4. |
| v1.409 | 2026-04-18 | GH#30 Admin Delete RO (Session 51) — soft-delete columns + load filter + Delete RO admin wrap in Edit RO modal footer + Recently Deleted admin header button + modal with Restore / Delete Now + pg_cron scrub job (job ID 6, daily 02:00 CDT) + `.select()` row-count assertions on writes. Two migrations run in Supabase: `add_deleted_at.sql`, `cron_scrub_deleted_ros.sql`. Commit: c6eecda. |
| customer-checkin v1.3 | 2026-04-16 | GH#19 Lead Staff Notification — `handleLeadStaffNotify()` builds payload + posts to `send-quote-email` with `X-PRVS-Secret` header. Fires for New Customer Entry (any work type) + RV Customer Drop Off when warranty/hybrid. Success toast after submit. |
| customer-checkin v1.4 | 2026-04-16 | Mode rename + consistency sweep — "RV Drop-Off" → "RV Customer Drop Off", "Lead Conversion" → "New Customer Entry". Labels, comments, review card updated. Variable/function names + `formState.mode` payload values (`'dropoff'`, `'lead'`) preserved. Mode description neutralized (doesn't imply new-customers-only). Commits: ef505bc, d4f0acb. |
| send-quote-email v1.8 | 2026-04-16 | Edge Function — added `lead_staff_notify` type. Mode-aware subject tag (🚨 {New\|Returning} Customer{(Warranty)\|(Hybrid)}{ — Drop Off}), headerLabel branching, branded HTML + plain-text bodies, dashboard deep-link CTA. CORS updated with `x-prvs-secret`. Shared-secret `PRVS_FUNCTION_SECRET` gate. Redeployed with `--no-verify-jwt`. v1.7 initial build, v1.8 string label sweep (Planned Drop Off, New Customer Entry Notes). |
| slack-notify v1.2 | 2026-04-12 | Edge Function — routes event_type to Slack webhook channels. v1.0: initial build with anon key auth. v1.1: switched to anon key header auth (fix 401). v1.2: origin-only auth (removed broken token check). Secrets: SLACK_WEBHOOK_PARTS_ALERTS, SLACK_WEBHOOK_RO_UPDATES, SLACK_WEBHOOK_WARRANTY_FLAGS. |
| send-manager-report v1.5 | 2026-04-13 | GH#23 Morning Manager Report Edge Function. Per-silo personalized email to all managers (sr_manager, manager, parts_manager). Section 1: Work List + dollar total. Section 2: RVs Waiting sorted by days on lot → urgency → RO type. Section 3: Key Flags (Critical, >60d, blocking parts). Red empty/yellow stale banners. pg_cron job ID 5 (Mon–Fri 8 AM CDT). v1.0→v1.5: 9 iterations fixing banner logic, roMap dual-key joins, sr_manager first-silo unassigned items. |
| v1.304 | 2026-04-06 | GH#18 Parts Notifications — `requested_by_email` on `repair_orders`; `notifyPartsRequester()` green email button; `notifyPartsEtaUpdate()` auto-fire on ETA save; `send-quote-email` v1.5 (parts_ordered + parts_eta_update types); `send-parts-report` Edge Function (4-section parts report); `parts-report.yml` cron Mon–Fri 8 AM + 3 PM CDT; ro_id FK bug fixed. |

---

## 📅 Session Log

| Date | Session | Summary |
|---|---|---|
| 2026-03-19 | 1 | GitHub MCP, RBAC SQL, checkin.html v1.26, nonce fixes v1.262+v1.263, CLAUDE_CONTEXT.md created |
| 2026-03-19 | 2 | Nonce encoding fixed (hex), Analytics button (v1.264), version badges, daily backup live, PAT rotated |
| 2026-03-19 | 3 | solar.html v2.0 deployed, roof-lookup Edge Function committed (needs CLI deploy) |
| 2026-03-20 | 4 | v1.265 — PDF quote (jsPDF), email attachment, 4× description fixes, mobile filter, calendar re-auth |
| 2026-03-20 | 5 | v1.265 cont. — persistent auth, 5× guard fixes, audit log fix, Chrome 145, 406 fix. v1.266 — write hardening |
| 2026-03-20 | 6 | CLAUDE_CONTEXT.md restructured with TODO list, Session Protocol, Known Issues, SESSION_STARTER.md created |
| 2026-03-20 | 7 | v1.267 — Parts Request System built end-to-end: modal, dictation, chip, email, resolution flow |
| 2026-03-20 | 8 | v1.268 — Fix notes_type_check constraint (parts_request → ro_status + body prefix). v1.269 — Photo attachments in Parts Request modal: _partsRequestFiles[], preview strip, upload to Storage, add to photo_library, inline thumbnails in email (Edge Function v1.3). Edge Function deploy instruction clarified. CLAUDE_CONTEXT Known Issues updated. |
| 2026-03-20 | 9 | v1.270 — RO Description inline edit fix: showVoiceNotesModal prefillValue param, full replace on save, writeAuditLog before/after. Start-of-session checklist followed. |
| 2026-03-20 | 10 | v1.271 — Fixed photo upload auth guard (!getSB() → !getSB()\|\|!supabaseSession, re-check in onchange). Migrated uploadDocument from Google Drive to Supabase Storage (eliminates 401). |
| 2026-03-21 | 11 | No code changes. Verified + marked ✅ Done: SQL migration, roof-lookup deploy, send-quote-email deploy, calendar re-auth iPhone test. Added GH#9 (parts autocomplete), GH#10 (Kenect API integration), GH#11 (solar battery Wh). Added GitHub Release step to End of Session Checklist. Dropped v1.265/v1.266 backfill release items. |
| 2026-03-22 | 12 | Added GH#12 (Spanish toggle), GH#13 (pre-deploy backup). Built scripts/backup.sh (6-version rotating snapshots). SESSION_STARTER.md overhauled (hardened rules, GitHub fallback path, RESET/PAUSE/STOP commands, Key Reference table). PRVS_Technician_Guide.docx created. v1.272 MIME fix. v1.273 document modal refresh fix. v1.274 60-second note. v1.275 photo lightbox viewer + email photos to customer + Edge Function v1.4 photo_share type. Context limit hit — session ended mid-work. |
| 2026-03-22 | 13 | v1.276 email auth fix (SUPABASE_ANON_KEY). Edge Function v1.4 CC on photo emails deployed. v1.277 Spanish toggle (GH#12) complete for index.html — TRANSLATIONS_ES dict, t(), translateStaticUI(), setupI18n(), full renderBoard() + updateStats() translation. v1.27 Spanish toggle for checkin.html — full check-in/out flow translated, same prvs_lang localStorage key. |
| 2026-03-23 | 14 | v1.278 — Fixed duplicate ro_id 409 crash: generateUniqueROId() collision check + -2/-3 suffix fallback; try/catch at New RO submit handler. GitHub Release v1.277 confirmed published by Roland. |
| 2026-03-23 | 15 | v1.279 — Video upload support restored: isVideoUrl() helper, uploadPhoto accepts video/*, 🎬 tile in thumbnail grid, <video controls> in lightbox, email modal disables video entries. |
| 2026-03-23 | 16 | v1.280 — Fix RO insert race condition: optimistic insert loop replaces generateUniqueROId check-then-insert; 23505 → retry next candidate; submit button disabled during save. v1.281 — Fix roId scope bug: console.log after loop used out-of-scope loop var; changed to data.ro_id. v1.282 — Duplicate RO Manager: getBaseROId/findDuplicateGroups, 🔗 Merge Dupes header button, master picker modal, executeDupeMerge reassigns all FK tables + merges photo_library + deletes dupes. |
| 2026-03-23 | 17 | v1.282b — Fix isAdmin() timing bug: getUserInfo() with no Google token pollutes currentUser.email to 'unknown@user.com'; Merge Dupes post-load check now also checks supabaseSession?.user?.email against ADMIN_EMAILS directly; added 🔗 Dupe check console.log for debugging. Merge Dupes confirmed working by Roland. End of Session Checklist run; backup.sh executed; CLAUDE_CONTEXT.md updated and pushed. |
| 2026-03-23 | 18 | v1.283 — Fix "Date RV Arrived on Lot" not saving: date_arrived was missing from updateROInSupabase() Supabase update payload; silently dropped on every Edit RO save. One-line fix. End of Session Checklist run. |
| 2026-03-24 | 19 | v1.284 — Three-state parts chip system (GH#14): parts_status column, openPartsStatusModal, setPartsStatus, auto-flip on all parts received, filter buttons. Added bobby@/solar@ to MANAGER_EMAILS + Supabase. v1.285 attempt — Parts Estimate chip + For Estimate Only toggle built, but inline `</script>` inside modal.innerHTML template literal broke the page (SyntaxError: Unexpected end of input). Roland restored v1.284 backup via direct GitHub push, causing branch divergence. Session ended at context limit mid-recovery. |
| 2026-03-24 | 20 | Recovered from git divergence (reset --hard origin/main). Re-applied all v1.285 changes: MANAGER_EMAILS (all 6 managers), syncEstimateToggle() global, submitPartsRequest estimate logic, estimate chip CSS, RO tile chip estimate case, openPartsStatusModal estimate button, ps-estimate filter + logic, Spanish translation. Committed and pushed v1.285. Created git tag v1.285 + release notes. Created ROLLBACK.md (emergency recovery guide). Discussed token usage / session pause causes. End of Session Checklist run. |
| 2026-03-25 | 21 | Start-of-session checklist followed. v1.286 — Renamed Core Charge → Freight Charge (UI label only). v1.287 — Added Wholesale + Retail Price columns to Manage Parts table; git tag v1.287 created. v1.288 — Added Sourcing to individual part row status dropdown (neon orange). v1.289 — Moved Supplier Contact section to top of Add/Edit Part form. Clarified RO-level chip vs per-part-row status distinction. End of Session Checklist run. |
| 2026-03-26 | 22 | Kenect API Swagger reviewed (pasted by Roland — Chrome MCP & WebFetch both blocked). GH#10 implemented: v1.290 — kenect-proxy Edge Function (test_credentials, get_locations, get_conversation, get_messages_by_phone, send_message, send_review_request); 💬 Messages button on each RO card with phone; chat-style conversation modal; send message + review request from dashboard; Admin Settings Kenect section (Location ID, Test Connection, Load Locations). Pending: Roland must deploy Edge Function + set KENECT_API_KEY secret. |
| 2026-03-27 | 23 | GH#15 Phase 1 — v1.291: 📍 Parking Spot field. Dropdown of all named lot positions (Interior Bays #1–#14, MR, Wash Bay #15, #17, Breeze Way N/S, Office, Front Parking, RV Temp, Gates). Shows as yellow chip on RO card below customer name. New RO + Edit RO forms both include it. Stored as parking_spot TEXT column — SQL migration required (ALTER TABLE repair_orders ADD COLUMN IF NOT EXISTS parking_spot TEXT). |
| 2026-03-27 | 24 | (Session resumed after context compaction) GH#15 Phase 1 lot remap — v1.292: Parking spot positions renamed per Roland's handwritten photo: R1–R20 (Rear Lot), B1–B14 (Interior Bays), Wash Bay 17, Wash Bay 18, W1–W4 (Wash), F1–F12 (Front Lot). SQL migration confirmed with no errors. GH#15 Phase 2 — v1.293: `printQRLabel()` rebuilt as dual-sticker QR print sheet; 3"×3" windshield sticker (customer name, RV make, RO#, parking spot chip) + 1"×1" key tag; QRCode.js generated fresh in print window with proper @media print CSS, dashed cut borders, physical inch sizing. |
| 2026-03-27 | 25 | v1.294 — QR scan destination changed from checkin.html to main dashboard RO tile. Kenect Zapier discussion: reviewed Kenect-provided PDF — Zapier integration is outbound-only for messaging (Send Message, Send Review Request, Push Lead as Actions; only Lead Push as Trigger — no inbound message trigger). Advised Roland to push for direct API access since conversation thread view requires inbound message querying. End of Session Checklist run; backup.sh executed; pushed. |
| 2026-03-30 | 26 | Kenect confirmed dead end — no direct API keys available. Pivoting to Twilio (fast-tracked). GH#5 Work Assignment System designed and built: staff table SQL (14 personnel), service_work_orders + service_tasks DB migration run in Supabase. v1.295 — full Work Orders UI: 🔧 button on every RO card, 5-silo modal, Build/Edit WO form with task CRUD, silo-level access control (canManageSilo / isSrManagerOrAdmin), tech task status updates, dollar value rollup. End of Session Checklist run; backup.sh; pushed. |
| 2026-03-30 | 27 | v1.296 — Work Orders bug-fix pass: 5 bugs fixed (ro._supabaseId mismatch in 7 places, silent save guard, dark-theme form inputs, silo badge on task rows, wrong RO opened when board filtered). Roland added Roland Shepard to staff table as sr_manager via SQL (RLS fix). GH#5c "Polish Work Orders UI" added to TODO. End of Session Checklist run; backup.sh; pushed. |
| 2026-04-02 | 28 | Created PRVS Manager Role Training Guide PDF (16 responsibilities, 5 phases). v1.297 — fix photo library null entry crash. v1.298 — fix video upload size error messaging (500 MB pre-flight check). v1.299 — fix editField apostrophe/quote crash (remove currentValue param). v1.300 — add Kevin McHenry to SR_MANAGER_EMAILS. Supabase Pro optimization: Security Advisor 60→31 warnings, removed anon write policies (except time_logs — restored after checkin.html breakage), fixed function search_path, enabled password protections. checkin.html anon role dependency documented as 🟠 TODO. End of Session Checklist run; backup.sh; pushed. |
| 2026-04-05 | 31 | GH#16 Manager RO Work List complete (v1.303). Full implementation: header button, sidebar panel, all JS functions, Supabase table. 6 bugs fixed during session: supabaseClient→getSB(), missing _initWorkListBtn function definition, wrong RO from sorted-index mismatch (fixed to _supabaseId lookup), broken emoji escape (&#128203;), manager_work_lists table schema mismatch (DROP+recreate), RLS policies. End of Session Checklist run; backup.sh executed; pushed. |
| 2026-04-05 | 29 | Added GH#16-19 to TODO list (Cowork/iPhone session): Manager RO Work List, Customer Check-In Page, Parts Ordered Email Notification, Enhancement Request Button. Fixed checkin.html Supabase auth (v1.28/v1.301): `signInWithIdToken()` was never called — all DB ops were hitting anon role. Added `generateNonce()`, `getSession()` restore, `onAuthStateChange`, `clockIn()` session guard, `persistSession: true`, proper `signOut()`. Roland action: remove anon INSERT/UPDATE policies on `time_logs` in Supabase now that proper auth is in place. |
| 2026-04-05 | 30 | GH#5c Work Orders polish + GH#5b Task Templates (folded in). WO modal filters to active services only. + Add Service button on WO modal. ⏱️ Est. Hours field per task (est_hours NUMERIC(5,2) — ALTER TABLE run). Est. hours roll up to silo header. WO Task Templates — Save as Template / Load Template / Replace or Merge. Two new Supabase tables: wo_task_templates + wo_template_tasks. Template overlay z-index raised to 100000. Outside-click disabled on New RO modal + WO modal to prevent data loss. 8-silo SERVICE_SILOS array confirmed. |
| 2026-04-05 | 32 | GH#18 Parts Ordered Email Notification (v1.304). `requested_by_email` captured on `submitPartsRequest`, stored to DB + local data. `notifyPartsRequester()` manual trigger button (green email) in Parts Status modal. `notifyPartsEtaUpdate()` auto-fires on ETA set/change in `savePartForm`. `send-quote-email` v1.5 adds `parts_ordered` + `parts_eta_update` email types. `send-parts-report` Edge Function deployed (4-section HTML email: open requests, ordered/in-transit, overdue, received 24h). `parts-report.yml` cron live (Mon–Fri 8 AM + 3 PM CDT). `ro_id` FK bug found + fixed (was `repair_order_id`) + redeployed. Pending: test Notify Requester button + ETA email. |
| 2026-04-06 | 33 | No code changes. Diagnosed Nik Polizzo checkin.html error — root cause: missing Supabase auth.users row; fix = Invite User or temp-enable signups. Also noted Riley Scott registered as personal Gmail. Added solar pricing TODO. Rebuilt PRVS_Manager_Training_Guide.pdf (13 pages, 21 sections) adding new sections: Est. Hours, Task Templates, Notify Requester, ETA Auto-Notification, Add to Work List, Manage Work List. Fixed emoji→black-square issue in ReportLab (Helvetica can't render emoji). Created PRVS_Manager_Training_Guide.docx (Word) for easy editing. Both saved to docs/. |
| 2026-04-06 | 34 | GH#16 enhanced — v1.305: Sr Manager silo-specific work lists. `service_silo` column on `manager_work_lists` (nullable TEXT). Silo tabs in sidebar panel. Silo picker popup on add (z-index 12500). Same RO can appear in multiple silo lists. `_workListActiveSilo` state var. worklist-report.html v1.1 created (admin-only report: manager accordion sections, KPI chips, time log detail, staff status tiles, auto-refresh). Version badge in index.html header was not updated (fixed Session 35). |
| 2026-04-07 | 35 | Fixed Riley Scott email: `riley@patriotsrvservices.com` → `solar@patriotsrvservices.com` in MANAGER_EMAILS (index.html + worklist-report.html). Riley can now see "My Work List" button. Fixed v1.305 version badge (header `<span>` still showed v1.304). Roland ran SQL to update `staff` table email. SESSION_PROTOCOL_TEMPLATE.md created — generic template for new Claude Projects with Start/Pause/Stop commands. Discussed session management best practices (fresh sessions + context files for continuity). End of Session Checklist run; backup.sh; pushed. |
| 2026-04-07 | 36 | No code changes. Confirmed Riley fix deployed + working. Zak Wombles hit "Your session is not ready yet" on checkin.html — same Supabase auth.users issue as Nik (Session 33). Roland invited Zak, verified fix. Added TODO to batch-invite remaining techs (ignacio@, tipton@, rod@, travis@, cooper@, rudy@, tommy@). Updated Known Issues with Supabase auth onboarding pattern. End of Session Checklist run; backup.sh; pushed. |
| 2026-04-09 | 37 | Large session (context compacted mid-session). **Early session:** Fixed send-parts-report `ro_id` FK bug (3 of 4 queries). Created Start/Pause/Stop session skills. Migrated parts report cron from GitHub Actions to Supabase pg_cron (reliability fix — GH Actions fired 2h late). Added GH#22 to TODO. **GH#19 Enhancement Request system:** Floating genie lamp on all 6 pages, `enhancement_requests` table + RLS, admin view with status/category filters, `send-er-report` Edge Function + pg_cron daily digest. Parts request now auto-creates Sourcing row in parts table. **GH#22 Closed RO Archive:** `closed-ros.html` v1.0 — card grid with photos, search/filter/sort, detail modal, reactivation. Yellow header button on dashboard. Fixed RLS auth (removed localStorage fallback). **Fixes:** `parts_status_check` DB constraint updated to include 'Sourcing'. Parts badge chip logic updated (Sourcing was falling through to "All Received"). Chip label changed to "Requested" for clarity. Work List remove button rewritten from inline onclick to DOM construction (fixed persistent SyntaxError from UUID/special chars in template literals). Closed ROs button color changed to bright yellow. v1.306→v1.308. End of Session Checklist run; backup.sh; pushed. |
| 2026-04-11 | 38 | **Security Remediation S1–S4 marathon session (context compacted mid-session).** S1 (XSS): Added `escapeHtml()` utility + applied to 34 interpolation points in index.html. S2 (RBAC): Phase 1 DB migrations (users, user_roles, roles tables, 15 role entries) executed by Perplexity QA pipeline. Phase 2: index.html RBAC rewrite (12 steps — loadUserRoles, isAdmin/hasRole/canSeeWorkList/isSrManagerOrAdmin rewritten, _staffCache integration, solar access fix, deleted ADMIN_EMAILS/MANAGER_EMAILS/SR_MANAGER_EMAILS). Phases 3-6: worklist-report.html (10 call sites), closed-ros.html (6 call sites), analytics.html (3 call sites + new signInWithIdToken flow), checkin.html (dead code removal). S3 (analytics auth): supabaseSession guard on analytics.html ER submit. solar.html: Supabase client with persistSession/storageKey, session restore, session-guarded ER submit. S4 (Anthropic key): Created claude-vision-proxy Edge Function (JWT-verified), rewrote callClaudeVision() to use proxy, removed API key inputs + localStorage ops, hotfix added server-side JWT verification via auth.getUser() + client apikey header. Updated CLAUDE_CONTEXT.md + CLAUDE_CONTEXT_HISTORY.md throughout. Commits by Roland from terminal (files too large for GitHub MCP). |
| 2026-04-12 | 41 | **Compact View Mobile Inline Expansion (v1.401) + overlap hotfix (df12590) + Bobby's Parts Manager Daily Checklist PDF + send-parts-report v1.3 (GH#18).** On mobile (≤768px), tapping a compact RO row body now expands an inline panel beneath the row with: Status dropdown (live saves), Urgency dropdown (live saves), Technician (read), Parking Spot (read), and 4 quick-action buttons (Edit RO, Parts, WO, Schedule). Chevron rotates on expand. One row expanded at a time. Desktop compact view fully unchanged. 6 CSS classes added (compact-chevron, compact-expand-panel, compact-expand-inner, compact-mobile-summary, cep-*, mobile media query block). boardGrid click handler updated with expand/collapse logic guarded by !e.target.closest('[data-action]'). Change handler updated with dropdown class-sync after save. escapeHtml on all panel values. Zero regressions. **send-parts-report v1.3:** Contextual action prompts above each of 4 data sections; morning email "Good morning Bobby" banner with numbered priority order; afternoon email "Before You Leave Today" 6-item EOD checklist; conditional section prompts (empty = nothing to do, data = specific actions); plain text version updated to match. `isMorning` logic: `utcHour < 17` (8 AM CDT = 13:00 UTC morning, 3 PM CDT = 20:00 UTC afternoon). ⚠️ Requires `supabase functions deploy send-parts-report` from Roland's machine. |
| 2026-04-12 | 41 (cont.) | send-parts-report v1.4→v1.7 (Gmail rendering fixes, numbered action steps, style minification); Warranty RO type v1.402 (4th type, auto-Critical, badges, fields, filter); "Filter by Insurance" → "Filter by RO Type" label; GH#19 opened (Customer Check-In Page) |
| 2026-04-12 | 42/43 | **Slack Integration GH#20 (v1.403).** Built `slack-notify` Edge Function v1.0→v1.2 + 5 trigger points in index.html (`notifySlack()` fire-and-forget helper). v1.0 initial build; v1.1 switched to anon key auth (fix 401); v1.2 origin-only auth (removed broken token check — CONFIRMED WORKING). Triggers: `ro_created` (new RO), `ro_ready_pickup` (status→Ready for pickup), `ro_urgency_critical` (urgency→Critical), `part_received` (part status→Received), `warranty_ro_opened` (warranty RO created). Channels: #ro-updates, #parts-alerts, #warranty-flags. Confirmed: ro_created + ro_ready_pickup fire correctly. 3 triggers remaining to verify next session. Session skill fixes (pg_cron clarification, backup.sh cloud caveat). Context files session close-out. 12 commits total. HEAD: f0fc2b0. |
| 2026-04-13 | 44 | **GH#23 Morning Manager Report (send-manager-report v1.5) + GH#18 orderedParts scope fix.** Built `send-manager-report` Edge Function v1.0→v1.5 (9 commits). Per-silo personalized email to all managers: Section 1 (Work List + dollar total), Section 2 (RVs Waiting sorted by days/urgency/RO type), Section 3 (Key Flags — Critical, >60d, blocking parts). Red "EMPTY" + yellow "STALE" data quality banners per silo. pg_cron job ID 5 (Mon–Fri 8 AM CDT). Deployed and confirmed sending. v1.4 dual-key roMap for work list joins. v1.5 sr_manager first-silo unassigned fix. Fixed `orderedParts is not defined` scope bug in `_doNotifyPartsRequester()` (commit fc25a93). **Known issue deferred:** Data quality banner ($0.00 warning) not firing for sr_manager — multi-silo work list lookup needs service_silo mapping in staff table. HEAD: 4c3e9a7. |
| 2026-04-14 | 45 | **send-manager-report v1.6→v1.8 + worklist-report v1.2 tech tile expand + GH Issue #20.** Fired v1.6 test — confirmed multi-RV silo fix working but ≥60 days filter showed all RVs. v1.7: changed to ≥30 days filter (commit b0c3e5d, Supabase v12). v1.8: red PARTS HOLD badge on individual RO line items + named ROs in Key Flags PARTS HOLD section (commit 26d36e4, Supabase v13). Roland verified report. Created GitHub issue #20 for pg_cron DST offset update in November 2026. Built worklist-report.html v1.2: clickable tech tiles in Staff Status section expand to show daily time log per tech (sessions with times/RO/duration, gap detection, ACTIVE badge, summary line). CSS expand panel + chevron, query 3b for today's time_logs, toggleTechTile() function (commit 3d62840). Roland verified on live site. HEAD: 3d62840. |
| 2026-04-14 | 46 | **GH#24 Shop Operations RO — Phase 1 (v1.405).** 5th RO type (`shop`) for non-customer parts ordering/returns. 🏪 Shop button in New/Edit RO modals. `.shop-hide` class hides customer fields (name, phone, email, RV, VIN, address, status, urgency, customerType, parking spot, RO value, promised date, repair type). Hidden required fields get `required` removed via `data-was-required` pattern (input + textarea + select). Purple badge on standard cards, compact chip. RO Type filter button. Slack notifications skipped for shop ROs. Spanish translations. Converted existing PRVS - SHOP SUPPLY STOCK PARTS and PATRIOTS RV LOST/BROKE ROs to new shop type. Parts migrated. Commits: 66a5fe8, ca07ba9, 13981a0. HEAD: da6c87f. |
| 2026-04-14 | 47 (Perplexity) | **Sr Manager Parts + Schedule Fix.** Kevin McHenry (`kevin@patriotsrvservices.com`, Sr Manager) could not see Set Parts Status button or Schedule buttons. Root cause: 5 UI role gates only checked `isAdmin() \|\| hasRole('Manager')` — did not include `Sr Manager`. The `staffRoleMap` maps `sr_manager` → `'Sr Manager'` (not `'Manager'`), so Kevin's `userRoles` was `['Sr Manager']` which didn't match `hasRole('Manager')`. Fix: added `hasRole('Sr Manager')` to all 5 gates: Set Parts Status button (line ~8538), 3 Schedule buttons (compact ~8225, expanded ~8277, card ~8543), `openPartsStatusModal()` guard (~11751). Backup ran. Session degraded — Comet local browser connection became unreliable, context file updates were not completed before crash. Commit: d5ddc36. Context files updated in Session 48 (Perplexity recovery). |
| 2026-04-14 | 48 (Perplexity) | **Session recovery + Perplexity skill creation.** Recovered crashed Session 47 context — used past session history to reconstruct Sr Manager fix details. Updated CLAUDE_CONTEXT.md and CLAUDE_CONTEXT_HISTORY.md for Sessions 46-47 (commit 7b11ea6). Created two custom Perplexity Computer skills: `prvs-dashboard-startup` (triggers on "Start Session" etc., runs full startup checklist) and `prvs-dashboard-endsession` (triggers on "End Session" etc., runs full close-out checklist). Added `isManagerOrAbove()` refactor TODO. Added Sr Manager Role Gate Pattern to Known Issues. Set reminder to explore Perplexity Spaces in 2 days. No code changes this session. HEAD: 7b11ea6 (pre-close-out). |
| 2026-04-12 | 40 | **Unified Search V1.** Implemented UNIFIED_SEARCH_SPEC.md in single session. 4 spec changes: HTML search bar (label + placeholder + icon), JS haystack filter (10 fields), TRANSLATIONS_ES, setupI18n(). Plus post-render DOM highlight (TreeWalker + `<mark>`, escapeHtml-safe). Works in standard + compact view. All existing filters chain via AND. Commit: 7a355a4. index.html 14,304→14,357 lines. |
| 2026-04-11 | 39 | **v1.400 session — Security milestone + Toast + Visual + Compact View.** Bumped v1.308→v1.400 (security milestone). Implemented Toast System per TOAST_SYSTEM_SPEC.md: 116 alert()→showToast, 4 confirm()→toast-with-action, CSS+JS IIFE, 3 destructive confirm() preserved. Fixed scanMilestoneBanner slideIn keyframe→toast-slide-in. Low-risk visual cleanup pass: calmed animations/pulses/gradients, standardized button styling, added utility classes (.card-ro-id, .card-parking-badge, .card-secondary-btn, .card-actions-primary/secondary), reduced inline style proliferation. Dead code cleanup: 968 lines removed (7 CSS blocks, 22 JS functions, cross-file audit). Kenect removal: 550 lines deleted (329 index.html + 187 Edge Function + dead vars). **Compact Manager View:** new 'compact' option in view-mode selector, dense 6-column row layout (photo thumbnail + customer/RO/RV + status/urgency + days/tech/spot + parts chips + actions), viewPresets compact entry, conditional render branch in renderBoard(), scrollToROInBoard() fixed for .ro-card-compact. Compact photo refinement: 56×42 RV thumbnail, tighter spacing. index.html 12,800→14,304 lines. 6 commits pushed to main. |
| 2026-04-16 | 49 | **customer-checkin.html backdrop redesign.** Replaced marble CSS backdrop (radial gradients + veining overlay) with fixed full-bleed RV park mountain photo (`checkin-bg.jpg`, 7.4 MB). Initial commit: cream overlay ~55-65% opacity (71489b5). Second commit: boosted vibrance (`saturate(1.25) contrast(1.05)` on `body::before`), reduced overlay to ~20% opacity on `body::after` (6ac5c07). Photo on `z-index: -2`, overlay `z-index: -1` — cards/text unaffected. Git lock file issues in sandbox required fresh `/tmp` clone approach for pushes + manual lock cleanup on Roland's machine. HEAD: 6ac5c07. |
| 2026-04-16 | 50 | **GH#19 Lead Staff Notification + Drop Off / New Customer Entry rename.** (1) Built staff notification email for `customer-checkin.html` front desk flow — fires on New Customer Entry submit (any work type) + RV Customer Drop Off submit when warranty/hybrid. `send-quote-email` v1.6→v1.8: new `lead_staff_notify` type with mode-aware subject tag (🚨 {New\|Returning} Customer{(Warranty)\|(Hybrid)}{ — Drop Off}), headerLabel branching, branded HTML + plain-text bodies, dashboard deep-link CTA. Recipient hardcoded to repair@patriotsrvservices.com (v1). (2) Shared-secret `X-PRVS-Secret` pattern established: `PRVS_FUNCTION_SECRET` Supabase secret + matching constants in index.html (v1.405→v1.406) and customer-checkin.html. Added header to 4 edge fetches in index.html (photo_share, parts_ordered, parts_eta_update, parts_request) + lead_staff_notify in customer-checkin.html. CORS updated with `x-prvs-secret`. Deployed send-quote-email with `--no-verify-jwt` (ES256 JWT incompatible with unauthenticated public page). (3) Mode rename sweep: "RV Drop-Off" → "RV Customer Drop Off", "Lead Conversion" → "New Customer Entry". UI labels, comments, review card, email strings updated. Internal `formState.mode` payload values (`'dropoff'`/`'lead'`) preserved; variable/function names preserved. Mode description neutralized (doesn't imply new-customers-only). (4) Live-tested end-to-end with a real customer + a warranty customer — emails delivered. Commits: ef505bc (mode rename), d4f0acb (sweep). Backup + End Session protocol run. |
| 2026-04-10 | P1 | **Perplexity Computer session (not a Claude Cowork session).** Full codebase scan: structural analysis (758 lines), secondary pages (547 lines), infrastructure (691 lines). Delivered UI/UX recommendations and identified 5 improvement areas. Created 5 implementation spec files in `docs/specs/`: (1) UNIFIED_SEARCH_SPEC.md — global search + fuzzy matching + keyboard nav; (2) TOAST_SYSTEM_SPEC.md — replace alert() calls with non-blocking toast notifications; (3) SECURITY_REMEDIATION_SPEC.md — remove hardcoded emails/keys, environment-based config, CSP headers (**ASAP weekend priority**); (4) TWILIO_SMS_SPEC.md — replace Kenect with Twilio for SMS/messaging; (5) MODULARIZATION_ROADMAP.md — long-term plan to break monolithic index.html into ES modules. Created PR #14 (safe fixes: backup.yml cron timing + backup.sh path) — **merged**. Created PR #15 (all 5 spec files + updated backup configs) — **merged**. Updated CLAUDE_CONTEXT.md with Implementation Specs section, spec table, execution order, Perplexity→Claude pipeline workflow, and file inventory additions. Updated PRVS_PROJECT_CONTEXT.md: synced to v1.308, added specs table, rewrote TODO list with spec references, updated Recently Completed, updated tech stack (Edge Functions, roles). Updated this file (CLAUDE_CONTEXT_HISTORY.md) with session entry. All context files pushed to main. |