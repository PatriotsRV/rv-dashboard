# PRVS Dashboard — Claude Context History

> Historical archive. Read this at session end when updating completed work, version history, or session log. Companion to CLAUDE_CONTEXT.md.

---

## 📋 COMPLETED TODO ITEMS

> Older completed TODO items relocated to CLAUDE_CONTEXT_ARCHIVE.md (Session 107).
## ✅ Completed Work

> Completed Work for Sessions <=96 relocated to CLAUDE_CONTEXT_ARCHIVE.md (Session 107).

- ✅ **Session 118 — check-in dup/RAF fix (checkin-v1.13) + ER quick-win rounds: Keys+Power (v1.458) + time-off edit (time-off-v1.4) + worklist auto-prune; 13 ERs resolved (2026-06-20)**: Began from a receptionist report that 6/19 customer check-ins STILL created duplicate ROs and didn't email the RAF. **Diagnosis:** pulled 6/19's check-ins (3 ROs) — 2 confirmed dupes: David Flanagan `PRVS-DA06-488B` (On Lot) over his existing Scheduled `PRVS-DA05-488B`, and Joe Sekoral `PRVS-JO06-75FC` (Scheduled) over existing `PRVS-JO06-610B`; neither carried the v1.12 "↩ no duplicate RO created" conversion note, and that note had fired 0 times since v1.12 shipped 6/17 -> the guard never ran. The live GitHub Pages page was confirmed ALREADY v1.12, so the guard's dependence on staff TAPPING the returning-customer search match is the gap; a stale cached kiosk page (pre-v1.11) is the unifying explanation for BOTH symptoms (the pre-v1.11 `getSignatureDataURL` typo aborted every RAF; pre-v1.12 had no dup guard; no service worker so a long-open tab never re-fetches). **(1) customer-checkin.html v1.12 -> v1.13** (branch on pre-prod -> main `eb35326`, tag checkin-v1.13, rollback `9483241`): submit-time auto-dedupe — when no search match is selected but a phone/email exists, query pending ROs (status in Scheduled, Not On Lot) and match by digits-only phone OR lowercased email, then merge into that RO (re-fetch + status re-check still gate it) instead of inserting; success screen says "↩ Attached to existing scheduled RO". Soft-deleted the 2 DELETE-named dupes (inline SQL — MCP read-only). PROD Chrome regression PASS (v1.13 boot, zero console errors). **(2) ER housekeeping:** closed 11 already-shipped ERs (298e0e67, 74a33621, 2c3d5633, cdd77a8b, a8e90a7d, 671fa623, d2561e11, 9b046808, 468aa376, b2aa4bea, 5c45bb8d). **(3) Keys + Power RO fields** (ERs 34fc03c2 Brandon + b87eb2fb Lynn; dashboard v1.457 -> **v1.458**, tag v1.458, main `bf5a43c`, rollback `eb35326`): NEW `keys_power_fields.sql` (additive `key_status` text / `keypad_code` text / `keep_plugged_in` bool, run live — key_status free text, no CHECK by design); Keys select + Keypad/Lock Code + Keep-plugged-in checkbox on New + Edit RO (index.html); persist in ro-crud create/update + openEditRO populate + edit form-data gather + audit labels; keys/power chip row on cards (render.js); normalizeRO map (utils.js + inline); app.js boot log. node --check all 5 files; PROD regression PASS. **(4) time-off.html v1.3 -> v1.4** (tag time-off-v1.4, main `aaa0ba7`, rollback `bf5a43c`; ER ee50d11c Lynn): list was delete-only; each row (admin or own record) now has an Edit button that opens the modal pre-filled and UPDATEs the record instead of inserting; edit skips the new-request notifications + confirmation modal. PROD regression PASS (loads clean, zero errors). **(5) Worklist auto-prune** (ER 91a03260 Roland, DB-only): NEW `prune_cashiered_worklists.sql` — `prune_cashiered_worklists()` (SECURITY DEFINER) + daily pg_cron `prune-cashiered-worklists [30 7 * * *]`; deletes manager_work_lists rows where the RO is 'Delivered/Cashed Out' AND `(updated_at AT TIME ZONE 'America/Chicago')::date < today` (ro_id::uuid cast), so a cashed-out RO stays the day it's cashed out and drops the next day; one-time run cleared 4 stale rows. **Totals:** 13 ERs resolved, open 40 -> 27. Each release promoted in-session via Sync Gate (four-hash PASS); the End doc commit rides pre-prod (main current at `aaa0ba7`). All DB writes (2 migrations, cron, soft-deletes, ER closes) handed to Roland as inline SQL (MCP read-only). NEW next-session TODO: ER completion notification email (email the requester when their ER is marked done, with what-was-done + how-to-test). PENDING ROLAND: confirm the kiosk device shows v1.13; GitHub Releases backlog now incl checkin-v1.13 / v1.458 / time-off-v1.4. New memory note `reference_checkin_dup_raf_stale_cache`.
- ✅ **Session 117 — ER blitz: FOUR releases v1.454-v1.457 SHIPPED to prod + ER board declutter + Key Dates Phase 1 (2026-06-18)**: "Knock out useful ERs" session. **v1.454** (`fix/er-bugs-v1.454` -> main `bbf4959`, tag v1.454, rollback `ed23193`): (a) ER 9b046808 (Brandon) — "Scheduled" units no longer counted as on-lot; render.js `onLotCount` + the avg/longest days-on-lot stats now exclude status 'Scheduled' (Roland chose the count-fix over a new status, since 'Scheduled' already means not-yet-arrived); (b) ER 74a33621 (Brandon) — photo lightbox now supports Left/Right arrow nav + Escape-to-close via a document keydown handler in photos.js openPhotoLightbox, torn down in closePhotoLightbox (removed before each re-render to avoid stacking). **v1.455** (`fix/er-board-open-v1.455` -> main `8e5d0e0`, tag v1.455, rollback `bbf4959`): ER admin board defaults to an "Open (active)" view — enhancement.js openERAdminView sets `_erFilterStatus='open'`, loadERAdminData handles 'open' via `.not('status','in','("done","declined")')`, index.html filter gained `<option value="open" selected>`; hides done+declined (archived) but keeps in-progress visible. **v1.456** (`fix/er-parts-total-v1.456` -> main `8822ead`, tag v1.456, rollback `8e5d0e0`): ER a8e90a7d (Brandon) — "Ticket Totals" bar in Manage Parts (parts.js openPartsModal): Parts(sum wholesale*qty) + Freight(sum core_charge per line) + Total Cost + Retail(sum retail*qty); recomputes on every add/edit/delete. **v1.457 — Key Dates Phase 1** (`fix/key-dates-p1-v1.457` -> main `9483241`, tag v1.457, rollback `8822ead`): NEW `repair_orders.pickup_date` (additive date, migration `key_dates_pickup_date.sql`, run live); Pickup/Completed input on New RO (`name="pickupDate"` via FormData) + Edit RO (`editPickupDate`); Edit RO ALSO regained a Planned Drop Off input (`editPlannedDropoffDate`) — **fixing a latent bug: the Edit RO form had no drop-off input, so the edit form-data builder never gathered it and every Edit-RO save wrote planned_dropoff_date=null, wiping the morning-before reminder**; gathered both in the edit builder (index.html ~5847), populated in openEditRO (ro-crud.js); persisted pickup_date in ro-crud create+update; mapped pickupDate<-pickup_date in normalizeRO (utils.js + index.html inline ~4650); colored key-date chip row on cards (render.js IIFE: blue Drop / amber Promised [red if past-due & open] / green Pickup). **ER hygiene:** archived 4 duplicate ERs (5057c7fc, fa1c1d6e, fc6b29f9, 67daba15) -> declined; marked shipped ERs in-progress + "IN PROD TESTING"; Wimbles->Wombles (ER 6938c1af) already correct in data -> done. Open ER board ~53 -> 35 (12 in-progress + 23 unreviewed). **Initiative spec:** NEW `docs/specs/KEY_DATES_SPEC.md` (3 dates x dashboard / per-service silo calendar / email reminders; reuse silo calendars, all 3 dates, phased; v1.411 already built drop-off date + reminder + the scheduled_notifications cron + Schedule-modal calendar write, so P2/P3 extend it). **Team handout:** `PRVS_Dashboard_Updates_2026-06-18.pdf`. Each release passed local + PROD Chrome regression; in-session promotes were Sync Gate Case B (four-hash PASS); the End doc commit is Case A. NEXT: Key Dates Phase 2 (silo calendar events; open decision = writing events without an interactive Google token) -> Phase 3 (reminders); batch GitHub Releases v1.454-v1.457 + the older backlog.
- ✅ **Session 116 — report email top "how to use this report" banner x3 + parts report names the person (2026-06-17, edge-fn only; index.html stays v1.453; pre-prod `7cc0760`, nothing to main)**: Roland opened the parts manager email, found the instructions hard to read, and asked for (a) a clear "For full instructions on how to use this report, Click Here" link at the very TOP of the report (moved up from the bottom footer), applied to the other report emails too, and (b) the parts report to show WHO requested/ordered each part so parts ordered by someone other than the parts manager get tracked + received by the right person. **Scoped via AskUserQuestion (both answered):** on the already-ordered boxes show **who ordered it** = `parts.ordered_by` (a free-text human name — e.g. "Bobby", "Ryan Dillon", "maricio" — filled on ~17/22 on-order parts); the Order-These box shows the RO requester; add the top banner to **all three** report emails. Investigated the schema first via the read-only Supabase MCP: `parts` has NO per-part `requested_by` (the requester lives at RO level in `repair_orders.requested_by_email`), but it has `ordered_by`, `received_by`, `installed_by_email`; `ordered_by` is a name not an email so it prints as-is. **Changes (3 edge fns):** `send-parts-report/index.ts` -> **v1.10** — new top blue `<a>` banner (`#3b82f6` border, links `guide.html#parts-managers`) above the verdict; added `ordered_by` to the orderedParts/overdueParts/receivedParts selects; added a full-staff `select('name,email')` email->name map + `reqName()` (staff name, else email local-part); each item line now appends a subtle grey "— requested by NAME" (Order box, from `ro.requested_by_email`) or "— ordered by NAME" (Call-the-Supplier + Came-In boxes, from `p.ordered_by`); plain-text body mirrors it; added a small `esc()` for the name (handles "Bobby & Lynn"). `send-manager-report/index.ts` -> **v2.1-P4b** — new `guideBanner` const (links `guide.html#report-glossary`) injected after the header in BOTH the admin combined body AND each per-manager body (the per-section `guideLink()` chips stay as-is). `send-admin-pnl-report/index.ts` — same banner inlined after the header (this report had NO guide link before). **Validation + ship:** all 3 esbuild-transpiled clean (`--external:npm:* --external:node:*`); Roland deployed all 3 to live Supabase (`supabase functions deploy`); committed pre-prod `7cc0760` (3 files, +53/-14, branch-explicit on pre-prod); live-fired the parts report from Roland's Mac (anon Bearer curl) -> HTTP 200, `version:"v1.10"`, 11 recipients, toDo 8 / needsOrdering 0 / callSupplier 6 / received 2 / waiting 16; Roland confirmed "looks good." Updated the guide-anchor-contract Known Issue (3 report banners now also depend on `#report-glossary` + `#parts-managers`, both already live on main since S113 — so this was safe to deploy without re-shipping guide.html). index.html UNCHANGED. Sync Gate Case A (no release; feature commit + End doc commit both on pre-prod).
- ✅ **Session 115 — `updated_at` auto-maintain trigger on all 14 tables + audit_codebase.py Class I/J (2026-06-17, DB-only + tooling; index.html stays v1.453; all on `pre-prod`, nothing to main)**: Began from Roland's report that the new customer check-in created a DUPLICATE RO on his Roland Shepard Jr test instead of updating. **Diagnosis = correct-by-design:** the S114 returning-customer guard (`customer-checkin.html` v1.12) only re-uses an existing RO when its status is `Scheduled` (or `Not On Lot` with no `date_received`); the only live Roland Shepard Jr match was PRVS-7CFE-2397 (In progress), so per spec ("any active/in-progress RO still creates a new RO") it correctly INSERTed PRVS-RO06-1F51. Confirmed against the DB (read-only Supabase MCP). **Verified the actual update path** by seeding the missing scenario: created a `Scheduled` test RO (PRVS-SCHD-TEST) via the Supabase SQL editor (MCP is read-only this session — provided SQL inline), Roland ran a returning-customer roof drop-off against it, and it UPDATED in place (Scheduled->On Lot, `date_received` set, `repair_type`->Roof, description appended) with the conversion note landing in `notes` and NO second RO created. Cleaned up: soft-deleted the stray PRVS-RO06-1F51 and, after review, PRVS-SCHD-TEST. **Latent gap found during the check:** `repair_orders.updated_at` stayed frozen at creation time after the check-in UPDATE — because NO table had an `updated_at`-maintaining trigger and app code set it inconsistently (a project-wide gap across 14 tables; the S114 dup-RO update branch was just one path that forgot it). Nothing reads `updated_at` for logic, so it was silent (no symptom, no failing test). Roland chose the durable DB-level fix: NEW `supabase/migrations/auto_set_updated_at.sql` — a shared `public.set_updated_at()` function + a `trg_set_updated_at` BEFORE UPDATE trigger on every public table with an `updated_at` column (repair_orders, parts, service_work_orders, service_tasks, time_logs, time_off_requests, enhancement_requests, scheduled_notifications, app_config, config, users, solar_project_store, solar_settings, wo_task_templates). Run in the live DB; verified all 14 triggers enabled via `pg_trigger` (NOTE: `information_schema.triggers` returned [] because it is privilege-filtered under the read-only MCP role — new memory note `reference_pg_trigger_not_information_schema`). Committed pre-prod `9798e76`. **Then hardened the audit scanner** (Roland: "put those in audit_codebase.py for later"; feeds the planned Fable heavy-scan): added **Class I** (static — flags `.update`/`.upsert` payloads that still hand-set `updated_at` on a trigger-covered table; found 6, all LOW/cleanup-only, in index.html + checkin.html + a config upsert) and **Class J** (DB-catalog invariants — the static script can't reach the DB, so `render_db_invariants()` PRINTS runnable SQL into the report; first invariant self-discovers any table with an `updated_at` column lacking the trigger and returns 0 rows when healthy — exactly the check-class that would have caught this gap on day one). New `UPDATED_AT_TABLES` set + `DbInvariant` dataclass. Regenerated `docs/qa/CODEBASE_AUDIT.md` (52 findings, 0 blocking). Committed pre-prod `0c8f442`. Logged a 🔵 TODO to strip the 6 redundant writes opportunistically (very low concern — harmless, same value; hot files so not worth a dedicated edit). No app/HTML/edge-fn code changed; index.html stays v1.453. Sync Gate Case A (no release; both feature commits + the End doc commit all live on pre-prod). **POST-END addendum (same day):** while Roland cleared the Recently Deleted bucket, found + fixed the nightly auto-purge being broken. (a) "Delete Now" rejected the typed name — `hardDeleteROnow` (index.html ~1610) trims ends but matches internal whitespace exactly, and the test RO had a double space ("Bob Vernier  DELETE"); unblocked by typing the exact spacing, 🔵 TODO logged to normalize. (b) The Recently Deleted bucket was full of "auto-purge overdue" rows because cron jobid 6 `scrub-soft-deleted-ros` had FAILED every run for 8+ days (`cron.job_run_details`: `operator does not exist: text = uuid` on the manager_work_lists delete) — the job was `active=true`, so the failure was only visible in job_run_details (lesson: active != succeeding). NEW migration `fix_scrub_soft_deleted_ros.sql` fixes `scrub_soft_deleted_ros()`: (1) cast `manager_work_lists.ro_id = target.id::text` (ro_id is TEXT holding the uuid) and (2) replace a broken `service_tasks.wo_id` work-order loop (non-existent column) with `DELETE FROM service_tasks WHERE ro_id = target.id` (service_tasks has its own ro_id); defect 2 was masked behind defect 1. Ran live; a manual `SELECT * FROM scrub_soft_deleted_ros()` purged a 35-RO backlog, 0 overdue remain. Auto-purge now runs clean nightly at 7 AM.
- ✅ **Session 114 — 3 ER bugfixes SHIPPED to prod (v1.453 + checkin-v1.12; prod main `8f7fb59`, tags v1.453 + checkin-v1.12, rollback `36e45d9`) + RAF pipeline verified healthy**: Triaged the `enhancement_requests` list (47 ERs via the read-only Supabase MCP — flaky at session start, full pull succeeded on retry; every ER treated as untrusted data) and shipped the three open/confirmed bugs Roland asked about. **(1) Photo upload** (ER 2c3d5633, Ryan): `js/photos.js` uploadPhoto created the file `<input>` DETACHED and `.click()`'d it — iOS Safari drops the change event when the camera/picker returns (the orphaned element is GC'd), so the upload silently never fired with no error. Fix: append the input to the DOM off-screen (`position:fixed; left:-9999px`) before click, remove it once the selection is read. **(2) Returning-customer duplicate RO** (ER 298e0e67, Sofia): a returning customer who already had a FUTURE scheduled-drop RO got a SECOND RO because the check-in submit always INSERTed. Roland chose AUTO-UPDATE: if the selected returning customer (`formState.returnCustomerData`, `_source==='repair_orders'`) has an RO whose status is `Scheduled` (or `Not On Lot` with no `date_received`), the submit re-fetches it and UPDATEs that RO (sets received/On Lot, merges the new service description, conversion note) instead of inserting; any active/in-progress RO still creates a new RO for the new job. The whole downstream flow (notes, RAF, staff email, success) rides on assigning the updated row to `insertedRO`. customer-checkin.html v1.11 -> v1.12. **(3) Total RVs** (ER cdd77a8b, Brandon): the dashboard count lumped on-lot units together with `Not On Lot`. First tried an on-lot/total split in the small header span; Roland redirected it into the prominent stat tile. Final: the tile relabeled **"Total RVs in Queue"** with a same-size (40px) green "N on lot" sub-line (`render.js` `statTotalOnLot`, on-lot = status != 'Not On Lot'); header reverted. index.html v1.452 -> v1.453 (photos.js + render.js + js/app.js boot log). **Verification:** node --check on all touched modules + the customer-checkin inline JS; local Chrome regression PASS (board 80 cards, tile shows "80 / 62 on lot", checkin v1.12 boots, only the pre-existing GSI client_id noise); diffs reviewed (no stray changes). **Ship:** branch `fix/er-bugs-v1.453` -> FF pre-prod -> main `8f7fb59`, tags v1.453 + checkin-v1.12, rollback `36e45d9`, Sync Gate Case B four-hash assertion PASS; PROD Chrome regression PASS (`?cb=` cache-bust; v1.453 boot log + tile + checkin v1.12). **ER status:** the 3 ERs set to `in-progress` + an "IN PROD TESTING" admin note (Roland ran the UPDATE SQL — MCP read-only this session). Roland wanted a literal "In prod testing" status, but `render.js` renderERAdminList hardcodes the status dropdown options, so an off-list value would render gray and mis-select; declined the new status in favor of in-progress+note (Known Issue logged). **(4) RAF email** — receptionist reported the Repair Authorization Form is not emailed after check-in. Traced `sendRAFEmail` + the `send-quote-email` `raf_copy` branch (both intact — getSignatureData + `node:buffer` import; the S114 v1.12 change does not touch them), then LIVE test-fired the deployed fn (raf_copy to roland@) -> HTTP 200 `{success:true}`: backend pipeline (secrets + Gmail send + signature attach) is HEALTHY. Likely real-world cause = inbox (RAF -> customer + info@ CC, not repair@; info@-only if no customer email) or trigger (signed drop-off only). Passed to receptionist; follow-up TODO + a Known Issue added so future "RAF not sent" reports start at inbox/trigger, not code. Sync Gate Case B (release shipped earlier this session; the End doc commit rides the FF, tags already pushed).
- ✅ **Session 113 — Employee Guide deep-links SHIPPED to prod (guide.html v1.1 + dashboard v1.452 + checkin v1.37; prod main `6fb37a1`, tags v1.452 + checkin-v1.37, rollback `619c366`)**: Finished the S112 guide and wired it into every surface, then did a full production rollout after close. **(1) guide.html -> v1.1:** added the NEW **"Your daily report, block by block"** section — a deep per-block walkthrough (what you see / why it matters / what to do) for all six Manager Daily Report blocks, each with a stable `#rb-*` anchor (`#rb-active #rb-idle #rb-firewatch #rb-readiness #rb-parts #rb-phantom`) so report emails can deep-link to the exact block; reused the existing `.step`/`.step-bar` visual language; updated the TOC + glossary cards (cards now CTA "Click Here to go In Depth on this topic"). Roland top-of-page design pass: H1 -> "Patriots RV Services RO Dashboard Guide"; a 3px blue (`#3b82f6`, the dashboard's primary accent) frame with a soft glow wrapping the hero + Start-here together; bold report names recolored dashboard yellow (`#facc15`); all `.section-head h2` underlined; intro paragraph reworded; section renamed "Your Manager daily report, explained." **(2) Embedded the links** (Roland chose a persistent header Guide link + deep-link ⓘ helpers): index.html v1.452 header **📖 Guide** button shown to all authenticated users (mirrors the closedROsBtn visibility) + ⓘ helper on the WO build form header (`guide.html#build-wo`, js/work-orders.js) + ⓘ helper on the part **Service Silo** label (`guide.html#part-silo`, js/parts.js); checkin.html v1.37 header **📖 Guide** link (`guide.html#tech`, all 3 version sites bumped); `send-manager-report` per-block **📖 Guide ›** links via a new `guideLink(anchor)` helper (`#rb-active/#rb-idle/#rb-firewatch/#rb-readiness` + parts silo-less `#rb-parts`); `send-parts-report` footer link (`#parts-managers`). **(3) Deferred** to avoid touching the hot card-render path mid-workday: flag-chip deep-links (`#flags`, js/render.js) + a link on the ✓ Mark Completed button (`#mark-completed`, js/work-orders.js) — new 🟡 TODO; ES toggle still deferred. **(4) Verification:** node --check on guide inline JS + js/app.js + js/parts.js + js/work-orders.js; a python pass confirming every cross-file `guide.html#anchor` resolves to a real id; HTML tag + CSS brace balance; esbuild both edge fns clean. **(5) Rollout (Roland deferred to after close — sound call, checkin.html was in the change set with techs clocked in; rated ~2-3/10 risk, all additive):** committed S113 to pre-prod `6fb37a1`; backup.sh; FF main `619c366..6fb37a1`; tags v1.452 + checkin-v1.37; Sync Gate Case B PASS (main == pre-prod == 6fb37a1, both drift directions empty); deployed both edge fns to live Supabase; PROD Claude-in-Chrome regression PASS (guide serves clean, index boots v1.452 per the app.js boot log, checkin v1.37 with the Guide link present; only console error = the pre-existing GSI "Google APIs failed to load within 10 seconds" unauth noise). **Gotcha logged:** guide.html anchor ids are now a contract across 5 linkers, and the report emails link to the PROD url, so guide.html must be on `main` before deploying the edge fns (the reason this was a full rollout, not a soak). Sync Gate Case B.
- ✅ **Session 112 — Employee Guide page (`guide.html` v1.0) BUILT (2026-06-16, new standalone page; index.html stays v1.451; pre-prod `15274e2`, not promoted to main)**: Roland wanted a single living dashboard page that the now-daily Manager/Parts reports can link into ("For more detailed guidance, click here"), combining last week's (S104) Tech/Lead, Silo-Manager, and Parts-Manager tutorials and growing over time as the AI-manager rollout continues. **Scoping locked up front** via AskUserQuestion: (1) **login-gated** like the other standalone pages (not public/unlisted); (2) **English first**, Spanish toggle deferred; (3) **build the page this session, wire the report/dashboard links next session**. **Approach:** read the S104 docgen sources (`scripts/docgen/make_quickcard.js` + `make_manager_guide.js`) for verbatim tutorial content, and lifted the auth bootstrap from `closed-ros.html` (Supabase Google `signInWithIdToken`, `persistSession`, role badge, nonce flow; storageKey `prvs_guide_auth`; read-only — the page does no DB writes beyond the role-badge lookup). **Page structure:** dark dashboard theme + Barlow Condensed; sticky TOC with IntersectionObserver scroll-spy; a hero + "Start here" intro framing the AI-assisted migration; a **"Your daily report, explained"** glossary of six cards mapping each Manager Daily Report block (Active work / Idle ROs / Fire Watch / P&L Readiness / Parts to tag / Phantom hours) to the matching section; the **Tech & Tech-Lead 5-step time clock** (incl. the OUR WORK IS DONE lead action); **Service Silo Managers** P&L steps (build WO w/ real $ + silo, keep time clean, blue Tech-done chip = QA/QC, ✓ Mark Completed = the revenue event); **Parts Managers** P&L steps (tag the silo or it lands in Unattributed, per-unit wholesale + freight, tag tech requests, re-tag old parts); a **Reading the Flags** table; and a "What's coming next" note. Reused the print cards' colored STEP-banner visual language. **Stable anchor ids for report deep-links:** `#report-glossary #tech #tech-step-1..5 #phantom-hours #silo-managers #build-wo #mark-completed #part-silo #part-cost #parts-managers #flags #whats-next`. **Roland review loop:** first render approved on content but the condensed font read too small + low-contrast grey — second pass set an 18px base, content ~1.3rem, headings/sidebar scaled up, greys lightened to near-white (`#eef2f7`/`#dbe4ee`/`#ffffff`). **Verification:** extracted the inline `<script>` and ran `node --check` (clean); a Python pass confirmed all 11 `href="#..."` anchors resolve to real element ids and the section/table/div tags + CSS braces balance. **Git:** Roland committed `guide.html` + the new embed-links TODO himself on pre-prod (`15274e2`, 642 insertions across 2 files); nothing promoted to main. **NEW 🟠 TODO (next session):** wire the deep-links into the appropriate RO-dashboard UI spots (WO modal → #build-wo/#mark-completed, parts silo dropdown → #part-silo, kiosk/check-in → #tech, flag chips → #flags) and into the `send-manager-report` + `send-parts-report` emails (edge-fn redeploy). **NOTE:** `scripts/backup.sh`'s fixed file list does not include `guide.html` (git is the backstop) — add it next time backup.sh is touched. **Pairs with** the deferred Spanish-toggle follow-up. Sync Gate Case A (no release; page + End doc commit on pre-prod).
- ✅ **Session 111 — Manager Daily Report P4 PER-MANAGER SEND LIVE + daily 7AM cron flipped (2026-06-16, edge-fn only; index.html stays v1.451; pre-prod `790e925`, not promoted to main)**: Took the Manager Daily Report from admin-preview-only (P1) to **real per-manager delivery (P4)**, live every weekday at 7AM. Rebuilt `send-manager-report` to **v2.1-P4**. **(1) Gated per-manager send:** `{"send":true}` + an `x-prvs-secret` header (`PRVS_FUNCTION_SECRET`, value in js/config.js `b8b5561b...`) emails each active manager their OWN card only (no cross-manager summary/ranking — GUIDE not JUDGE); default `{}` body stays the open admin-preview. Send mode fails safe (401 if the secret is unset/mismatched — caught when the literal placeholder was left in the first curl). Added `x-prvs-secret` to CORS allow-headers. **(2) Admin delivery:** send mode ALSO emails Roland+Lynn ONE combined "admin copy" (every manager card + the shop-spend box). Roland chose this OVER the spec D4 per-email admin CC — the 9x CC was dropped for a cleaner inbox; replies still route via replyTo info@. Refactored the per-manager card into a reusable `managerCardHtml()` so the admin copy and the manager emails render identical cards. **(3) Active work section** at the TOP of each card (Roland: lead with the good news, then cascade into needs-attention): every worked RO with hours (7d + today), techs today, and WO progress (done/total + "awaiting your sign-off"). Card order = scorecard -> Active work -> Idle -> Fire Watch -> P&L Readiness -> Parts. **(4) Admin-only Shop parts spend box:** ordered-today / ordered-WTD $ + open shop-parts $/count + a last-7-days itemized order list, for daily abuse-watch (Roland + wife). Identified the two PRVS - SHOP OPERATIONS ROs (PRVS-6021-42A6, PRVS-6020-CE43) by `customer_name ILIKE '%shop operations%'` and EXCLUDED their parts from the parts-manager silo-less list (was inflating it ~70 of 109). Parts select now pulls `wholesale_price, qty, core_charge`; cost = wholesale*qty + core_charge(freight); ~$15.7k shop open-parts at cost. **(5) Parts-manager silo-less box rewritten** with a What-this-is / How-to-clear action guide (Roland: "no instructions, will cause questions") — set Service Silo on customer ROs; leave shop/overhead parts blank. **Ops:** validated each iteration with esbuild (`--external:npm:*`, clean); deployed to live Supabase (CLI x4); fired the real per-manager send twice (first cut + improved version), each 9 managers / 0 failures / adminSent true, with admin-previews between to eyeball layout without emailing managers. **Cron jobid 5 flipped to send mode** via `cron.alter_job` (body `{"send":true}` + secret header; schedule unchanged `0 12 * * 1-5` = 7AM CDT weekdays) — MCP-verified has_send_flag + has_secret. Read-only Supabase MCP used throughout (shop-RO ID, parts-cost schema, cron verify). **NEW TODO:** Overhead/Shop silo for PARTS — no home exists, so shop parts sit permanently in Unattributed; pairs with the planned shop-expenses P&L rollup (admin/owner-only). Memory `project_manager_daily_report` updated. NEXT: P2 Fire Watch expansion. Sync Gate Case A (edge-fn only; no index.html bump; nothing promoted to main).
- ✅ **Session 110 — Manager Daily Report P1 SHIPPED to prod (admin-preview live) (2026-06-14, edge-fn only; index.html stays v1.451)**: Built and shipped P1 of the "AI Assistant Manager" — the top TODO from S109. Rebuilt `supabase/functions/send-manager-report/index.ts` to **v2.0-P1** per `docs/specs/MANAGER_DAILY_REPORT_SPEC.md`: a fully deterministic rule engine, **admin-preview ONLY** (one combined all-managers email to Roland+Lynn via `app_config.admin_report_recipients`; zero individual-manager mail until P4 — exactly the spec P1 boundary). **Per-manager content:** List Activity (techs clocked in today, hours today/WTD/7d, ROs worked vs idle), a grouped Idle box, a Fire Watch preview (F1 promised due/overdue 🔴, F4 part-past-ETA 🟠), and P&L Readiness (checks R1 has-WO / R2 WO-valued / R3 WO-silo / R5 tech-rate / R7 part-silo / R8 part-has-WO / R9 Done-Done-within-6-business-days, rolled into an RO-level readiness %). Every flag line carries an inline `↳` plain-language "how to fix" breadcrumb; a single legend at the top explains all three groups once. **Validated on live data via the read-only Supabase MCP** before writing code AND after: confirmed every column/type, the `manager_work_lists.ro_id::uuid` cast, the manager roster, and reproduced the spec 10.5 numbers — Ryan 19 ROs (post-guard, was 21 pre-guard), 117.9h/7d (exact), 11 idle @5 work-days, 47% RO-level readiness (9/19; same 10-vs-21≈48% pre-guard), 1 real fire (Aaron Stepich On Lot) with the sentinel `promised_date = 0001-01-01` correctly demoted to an invalid-date readiness flag rather than a 739k-day "fire." Other managers sane (15-33% ready — matches the 70%-no-WO data-hygiene reality). esbuild bundles clean each iteration. **S109 guards live:** invalid dates (year<2020) flagged not fired; soft-deleted + `is_training` ROs excluded before any compute; RO dedupe by id (Andrew's 9 list-rows -> 8 unique). **Roland feedback incorporated across 5 redeploy/test cycles:** idle threshold 3->5 work days (business-day calc); idle moved from inline comma-list to its own grouped amber box with each RO + status on a line; idle remediation spelled out 4 ways (tech clocks in / placeholder -> use the Schedule button to calendar it / waiting on parts-or-approval -> set status Awaiting parts|Awaiting Approval / not yours -> remove from list); chose per-item `↳` text breadcrumbs over subsection deep-links (Roland feared a browser-tab explosion); **fixed Gmail "trimmed content" (`•••`) collapse** in the 10-manager digest by moving the 3 repeated per-card why/how footers into ONE top legend; wording tweaks ("your work list"; P&L definition = "the RO(s) Work Order estimated value data ... has not been set yet"). DONE/excluded-from-fires status set = 'Delivered/Cashed Out' + 'Ready for pickup'. **Ops:** deployed to the live Supabase project (Roland ran `supabase functions deploy` x5); test-fired via `curl -X POST .../functions/v1/send-manager-report` with the anon Bearer + `-d '{}'` each cycle (200, `managersRendered:10`); moved cron jobid 5 `send-manager-report-morning` from 8 AM to **7 AM CDT** (`0 12 * * 1-5`) via `SELECT cron.alter_job(job_id=>5, schedule=>'0 12 * * 1-5')` (cron command already carries its own Bearer auth header — verified). First live delivery: Mon Jun 15 @ 7 AM to Roland+Lynn. **Git:** edge-fn committed pre-prod -> FF main `7dad7e7` mid-session, four-hash Sync Gate assertion passed (edge-fn only, no index.html bump, no tag — same as the S97 ER-digest ship). **NEXT:** P2 Fire Watch expansion (F2 promised-within-3d-no-work, F3 part-past-ETA-near-promised=critical, F5 ordered-no-ETA-aging, + weekend/Monday escalation per spec 5.3) -> P3 AI narrative (LLM summarizes the deterministic flags ONLY, rule-only fallback) -> P4 per-role + per-manager sends (each manager their ROs only) + Roland+Lynn CC on every + silent `manager_guidance_log`; **add an X-PRVS-Secret check to the fn before P4** (currently anon-firable, acceptable while admin-only). Memory `project_manager_daily_report` updated; 2 Known Issues added (Gmail digest trimming; P1-live/admin-only/no-secret-yet). Sync Gate Case B (code already on main; End doc commit rides the FF).
- ✅ **Session 109 — v1.451 WO Estimated Hours SHIPPED to prod + Manager Daily Report spec'd + P1 rule engine validated (2026-06-14)**: Design-heavy session that also shipped a feature. **(1) Manager Daily Report (the "AI Assistant Manager") designed + spec'd.** Roland's vision: per-manager morning email that detects whether the work list is actively worked (tech check-ins + hours per RO), then flags the whole list — including idle ROs — for near-term promised/delivery dates, overdue/past-ETA parts, reminders, and (later, via Sendblue inbound) customer "is it done?" questions, color-coded; the long arc is a guide that tells managers what they're avoiding, tied to weekly revenue goals + carrot/stick + an accountability ledger. **Locked the core architectural call (Roland agreed): separate GUIDE from JUDGE** — Iteration 1 is a pure assistant (flags + AI narrative + P&L Readiness), `manager_guidance_log` captured silently, NO scoring/HR output until data-trust >90% + human-in-loop (scoring on today's data would be indefensible — combed the structures as a CFO and found 82/117 active ROs (70%) have NO work order, 9 WOs valued $0, 39/294 (13%) unattributed labor, 114 silo-less open parts, 83 parts past ETA). Added a **P&L Readiness** completeness score as the fairest day-1 accountability metric (measures only the manager's own data-entry job, and it's the lever that drags data trust toward the >90% gate). NEW `docs/specs/MANAGER_DAILY_REPORT_SPEC.md` (~290 lines): vision, List Activity, Fire Watch catalog F1-F9 (severity bands + rules), P&L Readiness R1-R9 + score, margin/efficiency M1-M3, guidance-log schema, data deps, 6-phase plan, open decisions. Decisions LOCKED: repurpose existing `send-manager-report` (531-line fn, good foundation — NOT a new fn); admins Roland+Lynn CC'd on EVERY manager email; R9 Done-Done window 6 business days (was 3); R7 silo-less parts ALSO surfaced in the Parts Manager email; recipients manager+parts_manager+sr_manager (insurance via the `user_roles` "Insurance WO Writer" grant). **(2) estimated_hours BUILT + SHIPPED (v1.451).** Found `service_tasks` already has est_hours/actual_hours/billed_hours (68% of active WOs have task estimates) -> the new `service_work_orders.estimated_hours` (migration `wo_estimated_hours.sql`, run in prod, additive nullable numeric) is a BASIC-WO fallback only; effective WO estimate = task rollup when present, else WO-level (COALESCE, no double-count). js/work-orders.js: WO modal "Estimated Hours" input + save path + card-display COALESCE. Local Claude-in-Chrome test on Roland Shepard Jr's RO PASSED: field renders, save persisted estimated_hours=3.5 (DB-verified), card showed ~3.5h fallback, Vroom's 3h task-rollup precedence intact, clear->null verified, reverted clean. node --check both modules. Promoted Sync Gate Case B: FF pre-prod->main `4d9640a`, tag v1.451, rollback `9242b7e`, four-hash assertion passed. PROD: index.html v1.451 serving + board + zero console errors; app.js/work-orders.js still CDN-cached at v1.450 at check time (known ~10-min Pages JS-module lag — feature already proven on localhost+DB). **(3) P1 rule engine VALIDATED on live data** (Ryan's 21-RO list via Supabase MCP): 48% RO-level readiness, 117.9h/7d active, 14 idle, R1=3 no-WO, R2=5 $0-WO, R7=19 silo-less parts, F4=9 past-ETA, one real promised-date fire. Surfaced 2 guards (spec sec 10.5): filter sentinel/invalid dates (one RO promised 0001-01-01), exclude soft-deleted/training ROs first. Gotcha: `manager_work_lists.ro_id` is TEXT holding the UUID — needs `::uuid` cast. Manager report build = TOP TODO next session (P1 first). Sync Gate Case B (release shipped earlier this session; doc commit rides the FF, tag already pushed).
- ✅ **Session 108 — v1.450 SHIPPED to prod + Sendblue GH#39 Phase 2 POC rolled out & live-tested + messaging-automation spec (2026-06-14)**: Three arcs. **(1) v1.450 promoted to production** (Sync Gate Case B): rebased `chore/parts-status-cleanup` onto pre-prod (clean — the only divergence was docs-only S106/S107 commits), FF pre-prod, FF main `902d760`, tag v1.450, rollback `4becedb`, four-hash assertion passed; PROD Claude-in-Chrome regression green (v1.450 boot log, board + all parts-status chips/filters render, zero console errors); GitHub Release published. Removes the dead `parts_status='outstanding'` render alias (4 fallbacks in js/render.js + 3 in js/parts.js; unreachable since v1.449 Phase 2 CHECK-tightened it, 0 live rows). **(2) Sendblue Phase 2 POC ROLLED OUT + live-tested:** ran the `messages` migration in prod (17 cols, RLS authenticated-read, MCP-verified); set `SENDBLUE_API_KEY_ID`/`SENDBLUE_API_SECRET_KEY` secrets (existing keys still active — Supabase moved edge-fn secrets to **Edge Functions → Secrets** `/functions/secrets`, no longer under Project Settings); deployed `sendblue-send`. First send returned 502 — Sendblue's send-message API now REQUIRES `from_number`; patched the edge fn to send it (default shared line `+16466208124`, overridable via `SENDBLUE_FROM_NUMBER` for the future hosted office number 940-488-5047), redeployed, committed `a217693` on `feature/sendblue-poc`. Then 4 live tests via localhost:8765 ALL PASSED: outbound iMessage (verified iPhone), SMS fallback (verified Android), thread persistence (loadMessages reloads from DB), RO linkage (ro_id/ro_code correct, FK-join verified). messages table logs both success and failure rows. KEY FINDING: `is_imessage` is null at send time — Sendblue only reports iMessage-vs-SMS once the message resolves, via the delivery-status WEBHOOK (deferred), not the send response. **(3) Messaging automation framework designed + spec'd**: NEW `docs/specs/MESSAGING_AUTOMATION_SPEC.md` on NEW branch `feature/messaging-automation` (`4f10a75`, off pre-prod). LOCKED: customer-inbox-RO-tagged threading; one `notification_queue` + per-row lane flag (IMMEDIATE = Approval needed; DIGEST = ready-pickup / parts-arrived / work-started / EOD-tech-recap); digest = one combined text per customer; photos/docs via a link to a to-be-built viewer page; guards = quiet hours + kill switch + dedupe + opt-out stub; consent a HARD GATE before real customers. 6-phase plan + full testing matrix in the spec. Memory: `reference_sendblue_from_number_and_status_webhook`. Sync Gate Case B (release shipped earlier this session; doc commit rides the FF, tag already pushed).
- ✅ **Session 107 — CONTEXT-file archive split (2026-06-14, docs-only, no version change; index.html stays v1.449)**: Cleanup to stop the context docs eating session buffers. Created NEW `CLAUDE_CONTEXT_ARCHIVE.md` (cold tier, never read at session boundaries) and relocated cold content out of `CLAUDE_CONTEXT.md` and `CLAUDE_CONTEXT_HISTORY.md`: 106 completed ✅ TODO rows, 53 old Session Log entries (kept last 3 hot), File Inventory back-catalog (v1.430 and earlier), resolved/removed-system Known Issues (Twilio A2P/10DLC/SMS-skeleton, Kenect-REMOVED, Slack, the 2026-04-11 S2–S7 remediation cluster), and HISTORY for Sessions ≤96 / dated <2026-06-01. Relevance-based, conservative pruning — nothing deleted, only moved; zero-loss verified by line-multiset reconciliation against `.context_backups/` (gitignored). Result: CONTEXT 588→254 KB, HISTORY 366→42 KB, per-session read 954→297 KB (−69%). All active gotchas retained hot. Cleanup shipped to pre-prod `b2cbc30` (Roland ran git — sandbox FUSE index.lock trap). Memory note `reference_context_archive_split.md` added. Sync Gate: Case A (no release).
- ✅ **Session 106 — Twilio teardown SHIPPED to prod + TODO cleanup + v1.450 parts-status alias removal soaking (2026-06-14)**: Mixed session: a production teardown, a big TODO grooming pass, and a small dead-code removal staged on a branch. **(1) TODO hygiene.** Caught and fixed a pile of stale priority markers where the status cell already said done: flipped the **Kenect teardown** row (was 🔴, actually shipped S92 v1.445 — verified zero live refs in code); removed 3 completed **delete-inline cleanup** rows (Phase 6/7/10-11-12, all done S88); removed **GH#5c Polish Work Orders UI** (folded into the GH#5 Work Order Redesign umbrella, which is now the single placeholder); removed 6 more confirmed-done rows still flagged open (training guide S104, ER send-report upgrade S97, GitHub Release v1.445, Phase 4.5-E merge playbook, Slack teardown S92, Time Off Phase 1 S68); and — per Roland's decision to go with Sendblue / move away from Twilio + A2P — deleted the **5 deferred Twilio build rows** (number port, SMS umbrella, Stages 1/2/3) rather than leaving them parked. Open TODO count ~67 -> ~52. Sendblue (GH#39) remains the lone 🔴. **(2) TWILIO TEARDOWN (shipped to main, Sync Gate Case B).** Roland chose to remove the abandoned Twilio code completely. Footprint inventoried via Supabase MCP + repo grep: removed edge functions `twilio-sms` + `twilio-webhook` (committed S53 but NEVER deployed) and `docs/sms/` consent material (Employee_SMS_Consent_Form.docx/.pdf + sms-consent-page-content.md); added `supabase/migrations/twilio_teardown_drop_sms_log.sql` and dropped the **empty `sms_log`** table (MCP-verified 0 rows). Design call: KEEP `staff.phone_number` + `sms_opt_in_at` (16 real records) and `time_logs.reminded_at`/`extended_at` (all NULL, referenced by the live 5 PM EOD close fn) because a future Sendblue reminder/consent flow can reuse them; drop only the Twilio-shaped `sms_log`. CLI side was a complete NO-OP — `supabase functions list` confirmed neither function was deployed and `supabase secrets list` showed zero TWILIO_* secrets; no SMS cron existed either. Shipped: branch `chore/twilio-teardown` (`656c48e`) -> FF pre-prod -> FF main, tag `twilio-teardown`, rollback `0331915`, four-hash assertion passed, branch deleted local+remote. No app code touched (index.html stays v1.449), so no prod regression needed. **(3) v1.450 — dead parts_status 'outstanding' alias removal (SOAKING on branch).** Post-Phase-2 cleanup of the v1.446 render-as-Ordered alias: the value was backfilled to 'ordered' and CHECK-forbidden in v1.449 Phase 2 (S100), and MCP confirmed 0 live rows + the constraint (requested|sourcing|ordered|received|estimate), so the `=== 'outstanding'` branches were provably unreachable. Stripped 4 fallbacks in js/render.js (ps-ordered filter, compact chip, full-card chip class + label) and 3 in js/parts.js (_psLabel, currentChip class, labels map). Version bumped v1.449->v1.450 (index.html header + badge + boot console.log; js/app.js boot log); both modules pass node --check. Committed + pushed on branch `chore/parts-status-cleanup` (`7f08104`) and left SOAKING — pre-prod + main stay v1.449 until next session runs the Claude-in-Chrome regression + Sync Gate promotion (TODO + Known Issue added). **Sync Gate: Case B** (Twilio release shipped to main; v1.450 isolated on its own branch so the End-Session doc commit FF carries docs only). DB drops/keeps logged; two stale `audit_log` FK gotchas already in memory.
- ✅ **Session 105 — checkin v1.36: GH#29c-prev "status keeps resetting" ghost-write CLOSED + shipped to prod (2026-06-13)**: Roland reopened the long-standing complaint. Re-investigated against live data via the read-only Supabase MCP: detection query (current `repair_orders.status` vs the last `field_changed='status'` audit_log.new_value, where `updated_at` is later) surfaced 13 live ROs with unaudited status changes. **Root cause = the SAME `checkin.html` clock-in auto-flip**, with TWO residual defects beyond the S71 casing/audit fix: **(1)** it fired whenever status != 'In progress', so it silently overwrote deliberate manager states — traced two ROs end-to-end (Robert Cook PRVS-3A63-261A: Lynn set Awaiting parts Jun 2 → Ignacio/Mauricio clock-in Jun 3 flipped it; SHOP OPS PRVS-6020-CE43: Brandon set Awaiting parts May 26 → flipped). **(2)** the v1.33 audit_log insert silently FAILED for techs: `audit_log.user_id` has a FK to `users(id)` and most techs have NO `public.users` row, so `supabaseSession.user.id` raised an FK violation that the non-fatal catch swallowed — only 6 `'auto via clock-in'` audit rows ever landed vs 25 AUTO-STATUS notes (the 6 = Roland/Jason, who ARE in users). **Fix — checkin v1.36** (branch `fix/clockin-autoflip-guard`, checkin.html only +34/-6): `const FLIPPABLE_STATUSES = ['', 'Not On Lot', 'On Lot', 'Scheduled', 'Ready to Work']` allowlist (preserves both Awaiting states + all completed-side) and `user_id: null` on the audit insert. **Roland design call** via a scenario walk-through: preserve BOTH Awaiting states; a clock-in on a completed RO leaves it put. Verified `node --check` + a 12-status truth-table + localhost Test A (Awaiting parts preserved) / Test B (On Lot→In progress flip, audit row landed with user_id null) on tester ROs + PROD Claude-in-Chrome (`?cb=` cache-bust; v1.36 serving, clean console). **Shipped to production:** FF pre-prod→main `b6a3f09`, tag `checkin-v1.36`, rollback `63e49cc`. Of the 13, only 2 (the Awaiting-parts pair) reflected an overwritten manager decision; the other 3 were legit On Lot→In progress. Drafted a fix-notice email; Roland sent it to Lynn+Roland flagging the 2 ROs to reset. Two `audit_log` gotchas logged to memory (`reference_audit_log_fk_and_clockin_ghostwrite`): user_id FK→users(id) kills kiosk audit writes (use user_id:null); ro_id is ON DELETE CASCADE (cashiered ROs lose audit history). Sync Gate Case B (release merged+tagged earlier in session; doc commit rides the FF). branch `fix/clockin-autoflip-guard` mergeable for deletion.
- ✅ **Session 104 — P&L TRAINING DOCS shipped to docs/ (2026-06-12, docs-only, index.html stays v1.449)**: The S102-queued training-guide directive, scoped by Roland to "what MUST be done for the daily/weekly P&L to run properly" — current behaviors only. **DESIGN CALL up front:** Roland asked whether sub-service WO-level clock-in should be built first for finer tech time; decided DEFER (silo-level attribution is all the current weekly_pnl needs; compliance — missed clock-outs, wrong service picks — is the bottleneck, not granularity; a kiosk WO picker = a third thing to get wrong) → new 🟡 v2 TODO gated on data trust >90%, pairs with estimated_hours. **(1) `docs/PRVS_Tech_and_Lead_RO_Check_In_Procedure.docx/.pdf`** — tech + tech-lead time-clock card, 1 page EN + 1 page ES, "STEP N — WHEN-trigger" colored-banner format (Roland: ADHD-friendly, one action per line): every morning / every RO switch / end of every job / end of every day (phantom-hours ⚠, Mauricio 9.5h example) / tech-lead OUR WORK IS DONE. Every button label verified against checkin.html v1.35 source incl. exact ES strings; Roland hand-marked a PDF mid-session and all edits were incorporated + re-mirrored into ES (typo fixed "your are"→"you are"); his new "Clock In auto-logs you out of the last RO" line was code-verified (GH#33 trigger + amber toast) before printing. Iterations: rules→steps rewrite, font enlarged to fill page (emoji→colored bold text — tofu in LibreOffice pdf export), renamed from PRVS_PnL_Tech_QuickCard per Roland's file. **(2) `docs/PRVS_PnL_Manager_Guide.docx/.pdf`** — 2 pages, converted to the same STEP format on Roland's request: SILO MANAGERS (STEP 1 job scoped → WO w/ real Dollar Value + right silo, no WO/$0 = invisible revenue; STEP 2 every day → techs' time clean; STEP 3 blue Tech-done chip → QA/QC + inform customer; STEP 4 same week work finishes → ✓ Mark Completed = THE revenue event, don't batch, ↩ Reopen) + PARTS MANAGERS (STEP 1 Service Silo on EVERY part, blank = Unattributed [$19,068 June week]; STEP 2 wholesale-per-unit + freight fields; STEP 3 tech requests arrive silo-less → tag at processing; STEP 4 re-tag old parts) + READING THE FLAGS table (amber dot / ⚠ / Unattributed / blue chip → who fixes it). **Infra:** generator scripts committed `scripts/docgen/make_quickcard.js` + `make_manager_guide.js` (node + docx pkg; regenerate, never hand-edit the docx — new Known Issues entry). Old-name PRVS_PnL_Tech_QuickCard.* removed in the End-Session commit. Sync Gate Case A.
- ✅ **Session 103 — WO auto-flip trigger LIVE + weekly_pnl NULL-targets closed as not-a-bug (2026-06-12, DB-only, index.html stays v1.449)**: Condensed AM session, both queued S102 directives cleared. **(1) WO auto-flip:** `trg_auto_flip_wo_in_progress` AFTER INSERT on time_logs (SECURITY DEFINER; comma-split service_type → silo map mirroring the weekly_pnl RPC; Shop/blank/unmapped no-op; UPGRADE-ONLY — never touches in_progress/awaiting/approved/completed; audit row per flip `<tech> (auto via clock-in)` / `wo_status:<silo>`, same convention as checkin v1.33 RO auto-status). One-time backfill flipped the 16 existing not_started WOs that already had clocked silo hours (incl. both Dez Rock PRVS-7BE2-236F WOs — the original complaint; audit rows `PRVS system (historical backfill S103)`); pre-flight 16 → post-run 0, MCP-verified. Live smoke test PASSED on Roland’s RO PRVS-7CFE-2397 (new chassis Oil Change WO flipped at tech clock-in + audit row; other 3 WOs untouched). Migration `supabase/migrations/wo_auto_in_progress.sql` committed pre-prod `4ba301d` (Roland ran git). **Learned: kiosk clock-in is SINGLE-service by design.** **(2) weekly_pnl NULL-targets (S101 TODO):** NOT reproducible — seeds (4 rows eff. 2026-06-08), RPC tgt CTE (replicated raw), and grants all verified correct; live page via Claude-in-Chrome renders Target $15k/$20k×3 + scorecard $75,000 + trend MISS for week of Jun 8. S101 sighting = pre-seed or CDN-stale view; admin report lacks targets BY DESIGN until v2 item (a). Sync Gate Case A.
- ✅ **Session 102 — SILO WORK CALENDAR (worklist-report v1.17 + v1.18) BOTH SHIPPED to prod (2026-06-11)**: Roland's P&L add-on: optional calendar modal showing where each RO was worked over time per silo. Design locked up front (both layouts toggleable / clock-ins only / one header button w/ silo tabs / build now). **v1.17** (prod `7168fb8`, tag worklist-report-v1.17, rollback `d3f633d`): 📅 Calendar button in the P&L week-nav -> modal with TIMELINE layout (rows=ROs grouped by silo, cols=days, cells shaded by hours, per-tech tooltip splits) + CALENDAR layout (Mon-start grid, silo-colored chips; persisted prvs_pcal_layout), silo tabs All+8+Overhead, ranges Selected Week / Multi-Week 2-4-8 ENDING at anchor (look-back) / Month, nav clamped at present; attribution mirrors weekly_pnl RPC client-side; testers+training excluded; read-only, zero DB changes. Local gate passed first try. **v1.18 — three data-parity/visibility fixes from the gates** (prod `03b36bc`, tag worklist-report-v1.18, rollback `7168fb8`): (1) Shop-rule ORDER — prod regression caught pinned techs' Shop time landing in home silos; RPC checks Shop FIRST -> overhead regardless of pin; matched. (2) **Cashiered mirror union — Roland caught it**: May Roof showed ~2 quiet weeks vs 8-9 cashiered ROs in the P&L drill-down; the Saturday archiver deletes live time_logs, so v1.17's live-only fetch hid every since-archived RO (319 May rows); pcalFetch now unions `cashiered_time_logs` JSONB snapshots (PostgREST embed to cashiered for labels; `source_data->>clock_in` string-range filters w/ '+00:00' suffix; mirror shape verified via read-only Supabase MCP first). (3) EOD auto-close ⚠ markers — Roland questioned Mauricio's May 28 "Moving RVs 12.1h day"; MCP query proved auto_eod_5pm (7:28 AM in, never out, capped 5 PM = 9.5h phantom; real day ~2.5h/4 techs); all close_reason auto_eod* sessions now get amber ring (timeline) / ⚠+outline (chips) / per-tech ⚠ + phantom-hours callout (tooltips) / legend lines. PLUS visible version pill (header-right) driven by single WLR_VERSION const (Roland: "where is the page version?" — there wasn't one). PROD regression v1.18 PASSED: 580 May logs incl. mirrors, Roof 13 ROs (was 6), badge v1.18, zero console errors; NEW gotcha: hard refresh did NOT beat the Pages CDN — `?cb=` query param did. 4 Known Issues entries + 2 TODOs added. index.html UNCHANGED v1.449. Sync Gate Case B.
- ✅ **Session 101 — Weekly P&L P4 visual drill-down (worklist-report v1.16) SHIPPED + Daily Ops Health admin report LIVE (2026-06-11)**: (1) **v1.16 P4 drill-down** (branch `feature/weekly-pnl-p4`): silo rows chevron-expand IN PLACE (v1.15 modal REMOVED) — per-RO stacked horizontal bars (violet labor + magenta cumulative-parts segments scaled to the silo's max; dashed amber WO-value ref tick; green/red Cost÷WO %) on a dark Labor-Load-style panel with the textual detail (hours, parts lag, status chips, RO link) inline under each bar; NEW missing-data TRAINING info buttons (amber "i") on silo rows + any zero RO metric — click-popover explains exactly where the value gets set (WO Dollar Value + manager Done-Done for revenue, Check-In clock-ins for hours, Staff hourly rate for labor $, wholesale×qty+freight for parts, pnl_home_silo/WO-silo for Unattributed). Commits `e6714b4` + fixes; FF pre-prod -> main `ad3978a`, tag `worklist-report-v1.16`, rollback `5211519`, four-hash assertion passed. PROD regression PASSED via the NEW Control_Chrome AppleScript fallback (Claude-in-Chrome tab grouping failed; isolated-world caveat logged): Repair expanded 8 bars, popover open/close, collapse clean, no legacy modal, Labor Load + work lists intact. (2) **Daily Ops Health — Admin (rule-based v1)**: NEW edge fn `send-admin-pnl-report` (CFO frame: lead-with-exceptions flags; WTD scorecard; silo health w/ Labor% [amber>40/red>55 of completed+WIP] + data-completeness % [priced-WO coverage]; per-manager `manager_work_lists` blocks w/ WTD hours/labor/parts, WO value, Cost÷WO, pipeline $; WIP aging tech-done>3d; rule-based coaching reusing the v1.16 hint logic) + `cron_admin_pnl_report.sql` (6 AM CDT weekdays `0 11 * * 1-5`; recipients `app_config.admin_report_recipients` = Roland+Lynn, fallback roland@). Deployed by Roland (CLI x3); test fires: #4675 exposed the **manager_work_lists.ro_id = repair_orders UUID** gotcha (fixed `ac04949`), #4676 PASSED (200, 9 managers, 3 flags), polish `ad3978a`. Explained to Roland why Unattributed exists (parts-only; labor falls back to Overhead) -> spawned the parts-silo-at-source TODO. Found weekly_pnl NULL-targets bug (silo_targets seeded S99 but Target shows "—") -> TODO. FUSE locks (index/HEAD/remote-ref) forced mid-session switch to Claude-edits/Roland-runs-git. index.html UNCHANGED v1.449. Sync Gate Case B.
- ✅ **Session 100 — WEEKLY P&L v1.449 SHIPPED to production + GitHub Release + Parts Requested/Ordered Phase 2 (2026-06-10)**: Resumed from the Session 99 pause. (1) Promoted feature/weekly-pnl: backup + rollback anchor `8edd06d`, FF -> pre-prod -> main `9bb18ba` + tag v1.449 (four-hash assertion passed), branch deleted local+remote. PROD Claude-in-Chrome regression PASSED: v1.449 badge, 85 RVs/$366k board, module identity incl. the 2 new work-orders fns, zero app console errors; worklist-report v1.15 P&L section rendered LIVE weekly_pnl RPC data (Week of Jun 8 scorecard + silo matrix + 13-week trend with real history; Revenue Completed $0 expected pre-adoption); checkin.html v1.35 serving. Caught + fixed checkin's stale visible `.version` badge (said v1.33; a third bump site missed in both v1.34 + v1.35 — fixed on pre-prod `0c6da93`, gotcha logged). (2) GitHub Release v1.449 published via the NEW URL-prefill trick (releases/new?tag&title&body prefills the form; coordinate-click Publish — no extension-popup fight; logged to Known Issues as the standard method). (3) Parts Requested/Ordered Phase 2: MCP pre-checks (2 'outstanding' rows; cashiered has NO parts_status column), Roland ran backfill outstanding->ordered + constraint tighten to requested|sourcing|ordered|received|estimate, MCP-verified, migration record `parts_status_phase2_drop_outstanding.sql` committed `a50a6d3`. New TODO: strip the legacy 'outstanding' render alias. Next session: code-efficiency audit + P&L number verification + Roland training. Sync Gate Case B.
- ✅ **Session 99 — WEEKLY P&L P1-P3 BUILT + locally tested on feature/weekly-pnl (2026-06-10; paused pre-promote)**: Roland's CFO-frame per-silo weekly P&L per `docs/specs/WEEKLY_PNL_SPEC.md` v0.2. P1 data layer: 4 migrations run + validated (silo_targets seeded roof/solar/vroom $20k + repair $15k; SWO tech_done/completed cols; parts.service_silo 51 backfilled; staff.pnl_home_silo pins; cashiered cron Sat->Sun 5 PM; weekly_pnl + weekly_pnl_detail + mark_wo_tech_done RPCs, admin-gated/anon-revoked). Key model rules: home-silo pin beats clock-in service_type; wholesale is PER UNIT, core_charge = FREIGHT per line (cost = wholesale×qty + freight); parts costs stick with the RO cumulatively, GP-matched at recognition week. P2: worklist-report v1.15 P&L UI. P3: v1.449 completion buttons + parts silo dropdown + checkin v1.35 tech-lead Done. All 3 P3 tests passed on Kevin McHenry Tester incl. full tech-done -> Done-Done lifecycle. Memory: `reference_weekly_pnl_data_model`.
- ✅ **Session 98 — Sendblue GH#39 Phase 2 POC BUILT (outbound-only) — NOT rolled out; lives on branch feature/sendblue-poc (2026-06-09; v1.447 -> v1.448 on the branch only)**: Phase 1 cleared its gate (Session 96), so the dashboard-integration build began. Roland confirmed the production/number questions on a live Sendblue vendor call: Sendblue WILL host/SMS-enable the office number 940-488-5047 with voice staying on the current VOIP provider; the hosted number does iMessage + SMS fallback; a dedicated/hosted line accepts inbound from ANY number with no per-contact pre-registration; and the **pricing model is per NEW outbound-INITIATED contact/day, NOT per-message or per-line** ($1,000/yr per 5 new self-initiated contacts/day up to 25/day = $5,000/yr; over 25/day -> Enterprise $1,000/MONTH up to 50/day + CSM), with inbound-first contacts FREE and no A2P/10DLC fees (their infra). This SUPERSEDES the research-doc per-line/per-message estimate (memory `reference_sendblue_pricing`). The call ended before three technical questions — Q6 webhook auth/signature, Q7 delivery+read status mechanism + webhook per-account-vs-per-line config, Q8 MMS/media — which Roland will ask later; those gate the INBOUND half only. **POC scope = prove the RO dashboard can SEND an iMessage/SMS.** Built five additive pieces on `feature/sendblue-poc`: (1) `supabase/migrations/sendblue_messages_schema.sql` — NEW `messages` table (ro_id FK -> repair_orders.id, ro_code, direction, phone_to/from, body, media_url[], message_handle, status, is_imessage, error_code/msg, context, sent_by, timestamps; indexes; RLS authenticated-SELECT only, writes via the service role; kept separate from the dormant Twilio-shaped sms_log). (2) `supabase/functions/sendblue-send/index.ts` — NEW outbound edge fn (origin-locked CORS [prod + localhost:8765 for the POC] + X-PRVS-Secret gate; reads SENDBLUE_API_KEY_ID/SENDBLUE_API_SECRET_KEY, returns 503 if missing so it is dormant-safe; POSTs api.sendblue.co/api/send-message; logs a messages row via the service role; returns {ok,status,message_handle,is_imessage}). (3) `js/messaging.js` — NEW module (window bridge openMessagesModal/closeMessagesModal/sendSendblueMessage/loadMessages) rendering the RO thread + an editable E.164 phone + compose box, calling sendblue-send with the user JWT + X-PRVS-Secret (send-quote-email convention). (4) `index.html` — a "Message Customer (Sendblue)" button on the RO EDIT modal + v1.448 version markers. (5) `js/app.js` — imports messaging.js + window.PRVS_Messaging + boot-log bump. Validation: node --check (ESM) on messaging.js + app.js PASS; config exports confirmed; esbuild transpile of the edge fn clean. **Deferred at Roland's request ("we'll come back to this later"):** the migration is not run, the SENDBLUE_* secrets are not set, sendblue-send is not deployed, and nothing was smoke-tested. **Rollout next session:** run the messages migration in Supabase; set SENDBLUE_API_KEY_ID + SENDBLUE_API_SECRET_KEY; deploy sendblue-send; smoke-test a send from a real RO to Roland's VERIFIED iPhone (the free shared line only reaches verified contacts). Sync Gate Case A (no release; POC isolated on its own branch; End doc commit on pre-prod). Memory saved: `reference_sendblue_pricing`.
- ✅ **Session 97 - ER digest upgrade (7 AM + AI triage verdicts) + ER auto-fix Phase 2b built + Sofia check-in RAF fix (2026-06-09; no index.html change, v1.447 unchanged)**: (1) **send-er-report** email now renders the nightly AI triage per ER (bucket chip + S/M/L/XL effort badge + one-line verdict) plus a bucket/effort roll-up strip; text fields HTML-escaped; the daily-digest cron (jobid 3) moved from 3:30 PM to 7:00 AM CDT weekdays. Diagnosis proved the digest was never broken (8+ days of cron success; afternoon/weekday-only send) - Roland expected a morning email. Deployed + committed. (2) **ER auto-fix Phase 2b** built + shipped to main (`2927dcc`), dispatch-only and DRY_RUN-by-default: er_autofix_select.py (eligible bug selector w/ verdict-hint pre-filter) + er_autofix_runner_prompt.md (headless fixer, Edit/Write only, refuses out-of-rails work) + er_autofix_gate.py (hard rails: <=3 files / ~80 changed lines, path allowlist, content denylist, marker + version-bump, node --check; unit-tested pass/refuse/forbidden/oversize/badpath) + er_autofix_browser_check.mjs (Playwright Chromium boots the page on the unauth sampleData path; asserts version + zero fatal console errors + board render; REQUIRE_BOARD=soft until the first dry-run confirms render) + .github/workflows/er-autofix.yml. Scope cosmetic+data-display+small functional bugs; never auth/RLS/money/delete/DB/notifications; the LLM step sees only ANTHROPIC_API_KEY. Dry-run #1 (manual dispatch) PASSED with 'nothing eligible (skipped 1)' - the selector correctly excluded the only bug (Sofia's notification-class ER). Spec -> v0.2 (section 10). (3) **Sofia ER a22739b9** ('check-in no longer emails staff / no auth-form copy') FIXED across customer-checkin.html v1.9->v1.11 + send-quote-email: shouldNotifyStaff now fires on EVERY drop-off + lead (was lead/warranty/hybrid only) so repair@ gets every check-in; the staff-notify header reflects the real work type ('Drop Off (New Work)' etc., was hard-coded 'Warranty Drop Off'); and the signed RAF email ACTUALLY SENDS AGAIN - it had been silently failing for ALL customers due to TWO bugs: a nonexistent getSignatureDataURL() call (real fn getSignatureData()) throwing before the fetch, and Buffer being undefined in the Deno edge fn (added `import { Buffer } from "node:buffer"` for the raf_copy signature attachment). Verified live end-to-end: standard drop-off -> repair@ notification (correct header) + signed-RAF email with signature attachment to info@ (routes to Roland/Sofia/Lynn), no failure toast. ER marked done. Memory saved: reference_deno_edge_buffer_import, reference_er_triage_autofix_phase2b. Sync Gate Case B (code rode to main during the session; the End doc commit FFs main current; no new tag - no index.html bump).



















































---

## 📜 Version History

> Version History before 2026-06-01 relocated to CLAUDE_CONTEXT_ARCHIVE.md (Session 107).

| Version | Date | Summary |
|---|---|---|
| time-off-v1.4 (prod-live) | 2026-06-20 | **Time-off EDIT (Session 118, ER ee50d11c).** time-off.html list was delete-only; each row (admin or own record) now has an Edit button that opens the request modal pre-filled and UPDATEs the record (day/person/type/hours/reason) instead of inserting; edit skips notifications + the confirmation modal. FF main `aaa0ba7`, tag time-off-v1.4, rollback `bf5a43c`. Same commit shipped `prune_cashiered_worklists.sql` (ER 91a03260: daily cron prunes cashed-out ROs off manager worklists the day after). |
| v1.458 (prod-live) | 2026-06-20 | **Keys + Power RO fields (Session 118, ERs 34fc03c2 + b87eb2fb).** NEW additive columns `key_status` / `keypad_code` / `keep_plugged_in` on repair_orders (migration `keys_power_fields.sql`; key_status free text, no CHECK). Keys select + Keypad/Lock Code + Keep-plugged-in checkbox on New + Edit RO; keys/power chip row on cards (render.js); persist in ro-crud create/update + openEditRO; normalizeRO map (utils.js + inline); edit audit labels; app.js boot log. FF main `bf5a43c`, tag v1.458, rollback `eb35326`. |
| checkin-v1.13 (prod-live) | 2026-06-20 | **Check-in submit-time auto-dedupe (Session 118, ER 298e0e67 + dup half of 5c45bb8d).** customer-checkin.html v1.12 -> v1.13: when staff don't tap the returning-customer search match, the submit looks up an existing Scheduled / Not-On-Lot(no-arrival) RO by normalized phone OR lowercased email and merges into it instead of inserting a duplicate (the v1.12 guard only ran on the explicit-selection path — the cause of the 6/19 dupes). Explicit-selection path unchanged; re-fetch + status re-check still gate the merge; non-fatal. FF main `eb35326`, tag checkin-v1.13, rollback `9483241`. |
| v1.457 (prod-live) | 2026-06-18 | **Key Dates Phase 1 (Session 117).** NEW `repair_orders.pickup_date` (additive date, migration `key_dates_pickup_date.sql`). Pickup/Completed Date input on New RO + Edit RO; Edit RO regained a Planned Drop Off input (fixes a latent bug where edit-save nulled planned_dropoff_date + wiped its reminder). Colored key-date chip row on RO cards (blue Drop / amber Promised, red if overdue / green Pickup). normalizeRO maps pickupDate (utils.js + inline); ro-crud persists pickup_date (create+update); openEditRO populates the new fields. FF main `9483241`, tag v1.457, rollback `8822ead`. Companion: NEW `docs/specs/KEY_DATES_SPEC.md` (3 dates x dashboard/silo-calendar/email reminders; Phase 1 = dashboard). |
| v1.456 (prod-live) | 2026-06-18 | **Manage Parts "Ticket Totals" bar (Session 117, ER a8e90a7d).** parts.js openPartsModal now renders a totals bar below the parts table: Parts (sum wholesale*qty) + Freight (sum core_charge per line) + Total Cost + Retail (sum retail*qty); recomputes on every add/edit/delete. FF main `8822ead`, tag v1.456, rollback `8e5d0e0`. |
| v1.455 (prod-live) | 2026-06-18 | **ER admin board "Open (active)" default (Session 117).** enhancement.js openERAdminView defaults `_erFilterStatus='open'`; loadERAdminData handles 'open' via `.not('status','in','("done","declined")')`; index.html status filter gained `<option value="open" selected>`. Hides done+declined (archived); keeps unreviewed+in-progress. FF main `8e5d0e0`, tag v1.455, rollback `bbf4959`. |
| v1.454 (prod-live) | 2026-06-18 | **Two ER fixes (Session 117).** (1) ER 9b046808 (Brandon): render.js no longer counts 'Scheduled' units as on-lot (the Total RVs "N on lot" sub-line + avg/longest days-on-lot stats exclude 'Scheduled'); chose count-fix over a new status. (2) ER 74a33621 (Brandon): photo lightbox supports Left/Right arrow nav + Escape-to-close (photos.js keydown handler, torn down on close). FF main `bbf4959`, tag v1.454, rollback `ed23193`. |
| v1.453 + checkin v1.12 (prod-live) | 2026-06-17 | **Three ER bugfixes (Session 114).** (1) **Photo upload** (ER 2c3d5633, Ryan): js/photos.js uploadPhoto appended the file `<input>` to the DOM off-screen before `.click()` and removes it after selection — iOS Safari was dropping the change event on a detached input when returning from the camera (silent no-upload). (2) **Returning-customer duplicate RO** (ER 298e0e67, Sofia): customer-checkin.html submit now UPDATEs an existing Scheduled (or Not-On-Lot + no date_received) RO for the selected returning customer instead of inserting a 2nd RO; merges the new service description, logs a scheduled->arrived conversion note; active/in-progress ROs still create a new RO (Roland chose auto-update). checkin v1.11 -> v1.12. (3) **Total RVs** (ER cdd77a8b, Brandon): the stat tile (relabeled "Total RVs in Queue") shows a same-size green "N on lot" sub-line via render.js `statTotalOnLot` (on-lot = status != 'Not On Lot'); header `totalRVs` reverted. index.html v1.452 -> v1.453 (photos.js + render.js + app.js boot log). Branch `fix/er-bugs-v1.453` -> FF pre-prod -> main `8f7fb59`, tags v1.453 + checkin-v1.12, rollback `36e45d9`, four-hash assertion + local + PROD Chrome regression PASS. The 3 ERs set in-progress + "IN PROD TESTING" admin notes. Separately VERIFIED (no change): the RAF email pipeline — live `raf_copy` test fire returned HTTP 200 `{success:true}` (backend healthy; receptionist report is inbox/trigger). |
| v1.452 + checkin v1.37 + guide v1.1 (prod-live) | 2026-06-16 | **Employee Guide deep-links rollout (Session 113).** guide.html -> v1.1 (NEW "Your daily report, block by block" deep walkthrough with stable `#rb-*` anchors; hero restyle: blue `#3b82f6` frame around hero+Start-here, yellow `#facc15` report names, underlined section heads, reworded copy, "Click Here to go In Depth" glossary CTAs). index.html -> v1.452 (header 📖 Guide button for all auth users). js/work-orders.js (ⓘ helper -> `#build-wo`), js/parts.js (ⓘ helper -> `#part-silo`), js/app.js (boot log). checkin.html -> v1.37 (header Guide link -> `#tech`). send-manager-report (per-block `guideLink()` -> `#rb-*`, DEPLOYED) + send-parts-report (footer -> `#parts-managers`, DEPLOYED). FF pre-prod -> main `6fb37a1`, tags v1.452 + checkin-v1.37, rollback `619c366`, Sync Gate Case B four-hash PASS, PROD Chrome regression green. Deferred: flag-chip (`#flags`) + Mark Completed (`#mark-completed`) deep-links; ES toggle. |
| guide.html v1.0 (pre-prod -> prod S113) | 2026-06-16 | **Employee Guide page (Session 112; promoted to prod S113).** NEW login-gated standalone `guide.html` — consolidates the S104 Tech/Lead, Silo-Manager, and Parts-Manager tutorials into one living web page plus a "Your daily report, explained" glossary and a Reading-the-Flags table. Google id-token auth (mirrors closed-ros.html; storageKey `prvs_guide_auth`); read-only. Stable anchor ids for report deep-links (`#report-glossary #tech #mark-completed #part-silo #flags`, etc.). EN only (ES toggle pending). 18px base / ~1.3rem body after Roland's contrast+size pass. Verified: inline JS node --check clean, all 11 anchors resolve, CSS balanced. pre-prod `15274e2`; index.html unchanged (v1.451). NOT yet wired into the dashboard or report emails — next-session TODO. |
| v1.451 (prod-live) | 2026-06-14 | **WO Estimated Hours field (Session 109).** NEW `service_work_orders.estimated_hours` (additive nullable numeric, migration `wo_estimated_hours.sql`, run in prod) as a BASIC-WO fallback estimate. js/work-orders.js: WO modal "Estimated Hours - basic WO fallback" input + save path + card display where the effective estimate = sum of task `est_hours` when the WO has tasks, else the WO-level column (COALESCE, never double-counts). Discovered `service_tasks` already carries est_hours/actual_hours/billed_hours (68% of active WOs have task estimates), so this column only backstops task-less WOs. Local Claude-in-Chrome test on Roland Shepard Jr's RO PASSED end-to-end (render + save 3.5 DB-verified + card ~3.5h fallback + Vroom 3h task-rollup precedence + clear->null + revert). FF pre-prod->main `4d9640a`, tag v1.451, rollback `9242b7e`, four-hash assertion passed. Companion: NEW `docs/specs/MANAGER_DAILY_REPORT_SPEC.md` (Manager Daily Report Iteration-1, rule engine validated). |
| v1.450 (prod-live) | 2026-06-14 | **Remove dead parts_status='outstanding' render alias (Session 106 build, Session 108 promote).** The v1.446 legacy alias rendered any RO whose parts_status='outstanding' as Ordered. v1.449 Phase 2 (S100) backfilled outstanding->ordered and tightened the CHECK constraint to requested|sourcing|ordered|received|estimate, so the value is unreachable (MCP: 0 live rows + constraint confirmed). Stripped 4 fallbacks in js/render.js (ps-ordered filter, compact chip, full-card chip class + label) + 3 in js/parts.js (_psLabel, currentChip class, labels map). No DB change, no behavior change. **Session 108:** rebased `chore/parts-status-cleanup` onto pre-prod (clean), FF pre-prod -> main `902d760`, tag v1.450, rollback `4becedb`, four-hash assertion passed, PROD Claude-in-Chrome regression green, GitHub Release published. Prior soak commit `7f08104`. |
| twilio-teardown | 2026-06-14 | **Twilio/A2P code teardown (Session 106, shipped to main).** Roland moved messaging to Sendblue / away from Twilio after the A2P campaign was rejected twice (err 30909). Removed edge functions twilio-sms + twilio-webhook (committed S53, never deployed) + docs/sms consent material; dropped the empty sms_log table (0 rows) via supabase/migrations/twilio_teardown_drop_sms_log.sql. KEPT staff.phone_number + sms_opt_in_at (16 records) + time_logs.reminded_at/extended_at (live EOD fn) for Sendblue reuse. CLI no-op (functions never deployed, no TWILIO_* secrets). FF pre-prod->main `656c48e`, tag `twilio-teardown`, rollback `0331915`. index.html unchanged (v1.449). |
| checkin v1.36 | 2026-06-13 | **Clock-in auto-flip guard + audit FK fix (Session 105, GH#29c-prev close).** The v1.33 clock-in auto-flip fired on ANY status != 'In progress', silently overwriting manager-set Awaiting parts / Awaiting Approval (the "status keeps resetting" complaint), and its audit_log insert silently failed for techs (audit_log.user_id FK→users(id); techs absent from public.users = swallowed FK violation, 6 audits vs 25 notes). Fix: `FLIPPABLE_STATUSES = ['', 'Not On Lot', 'On Lot', 'Scheduled', 'Ready to Work']` allowlist (preserves both Awaiting + all completed-side; Roland call: leave completed ROs put) + `user_id: null` on the audit insert. Verified local Test A/B + PROD Chrome. Prod main `b6a3f09`, tag `checkin-v1.36`, rollback `63e49cc`. index.html unchanged (v1.449). |
| worklist-report v1.17 + v1.18 | 2026-06-11 | **Silo Work Calendar (Session 102) — both shipped to prod same session.** v1.17: optional 📅 Calendar modal off the Weekly P&L week-nav — Timeline (rows=ROs by silo, cols=days, hour-shaded cells, per-tech tooltips) + Calendar-grid (silo-colored chips) layouts, silo tabs (All+8+Overhead), Selected Week / Multi-Week 2-4-8 (ending at anchor) / Month ranges; attribution mirrors the weekly_pnl RPC; read-only, zero DB changes. Prod `7168fb8`, tag worklist-report-v1.17, rollback `d3f633d`. v1.18 (same session, fixes from the v1.17 gates): cashiered_time_logs mirror union (archiver deletes live time_logs — past weeks lost every since-archived RO; 319 May rows), Shop-before-pin attribution order (matches RPC), EOD auto-close ⚠ markers (phantom-hours callout; the Mauricio 9.5h find), visible version pill via single WLR_VERSION const. Prod `03b36bc`, tag worklist-report-v1.18, rollback `7168fb8`. index.html unchanged (v1.449). |
| worklist-report v1.16 + send-admin-pnl-report edge fn | 2026-06-11 | **Weekly P&L P4 visual drill-down + Daily Ops Health admin report (Session 101).** Silo rows chevron-expand in place (modal removed): per-RO stacked horizontal bars (labor + parts-cum segments, dashed WO-value ref tick, Cost÷WO %), dark Labor-Load-style panel, textual detail inline; missing-data training info buttons (amber "i") with where-to-fix popovers on silo + RO rows. NEW `send-admin-pnl-report` (rule-based v1, 6 AM CDT weekdays cron, Roland+Lynn via app_config): exception flags, WTD scorecard, silo health w/ Labor% + data-completeness, per-manager work-list blocks w/ Cost÷WO + pipeline, WIP aging >3d, coaching. Prod main `ad3978a`, tag `worklist-report-v1.16`, rollback `5211519`. index.html unchanged (v1.449). Gotcha fixed en route: manager_work_lists.ro_id = repair_orders UUID. |
| v1.449 + worklist-report v1.15 + checkin v1.35 | 2026-06-10 | **Weekly P&L Phases 1-3 (Session 99 build, Session 100 ship).** Two-stage WO completion: manager "✓ Mark Completed" (Done-Done = THE revenue-recognition event) + "↩ Reopen" + ✓done/🔵tech-done card chips (js/work-orders.js +markWOCompleted/reopenWOCompleted, audit-logged, canManageSilo-gated); parts Service Silo dropdown (parts.service_silo attribution; js/parts.js + js/config.js); worklist-report v1.15 Weekly P&L section (week scorecard / silo×week matrix w/ missing-revenue amber dots / per-RO drill-down w/ parts promptness lags / 13-week trend; admin-only); checkin.html v1.35 tech-lead "Our work is done" via mark_wo_tech_done RPC + Spanish (+ visible badge corrected to v1.35 on pre-prod `0c6da93` post-release). DB (run S99): silo_targets, SWO tech_done/completed cols, parts.service_silo, staff.pnl_home_silo pins, weekly_pnl/weekly_pnl_detail RPCs, cashiered cron Sat->Sun 5 PM. Cost basis: wholesale per unit × qty + core_charge (=freight). Prod main `9bb18ba`, tag v1.449, rollback `8edd06d`, GitHub Release published S100. v1.448 skipped (reserved by feature/sendblue-poc). |
| checkin v1.10/v1.11 + edge fns (S97) | 2026-06-09 | **Sofia check-in fix + ER digest upgrade; no index.html change.** customer-checkin v1.9->v1.10 (staff notify on every drop-off; RAF always sends, to=email or info@) ->v1.11 (fix getSignatureData typo that aborted the RAF). send-quote-email: staff-notify header reflects work type + `node:buffer` import (raf_copy signature attachment) - restores signed-RAF emails for ALL customers. send-er-report: AI triage verdicts in the daily email + digest moved to 7 AM CDT weekdays. ER auto-fix Phase 2b infra added (er-autofix.yml + scripts/er_autofix_*). Prod main `1407649`. No index.html version bump. |
| v1.448 (branch feature/sendblue-poc — NOT released) | 2026-06-09 | **Sendblue GH#39 Phase 2 POC (Session 98) — outbound-only, on a branch only.** NEW: `messages` table (sendblue_messages_schema.sql), `sendblue-send` edge fn (503 until SENDBLUE_* secrets set), `js/messaging.js` (window bridge openMessagesModal/sendSendblueMessage/loadMessages), a "Message Customer" button on the RO edit modal, app.js wiring. pre-prod/main `index.html` stays v1.447. Validated (node --check + esbuild) but NOT rolled out: migration not run, secrets not set, edge fn not deployed, no smoke test. Rollout deferred to next session. |
| v1.447 (prod-live) | 2026-06-07 | **ER-triage bugfixes (Session 94).** n33 — currency inputs (editDollarValue/new_dollarValue + part wholesale/retail/freight) show 2 decimals via edit-populate formatting (ro-crud.js + parts.js) plus a delegated `focusout` listener in index.html `init()`; COSMETIC only, no stored-value change. n22 (checkin.html v1.34) — "Return to Dashboard" button on the clock-out success screen + Spanish. n25 data fix: staff names Wimbles->Wombles (Rod/Zak/Travis) via SQL (0 historical repair_orders.technician rows to backfill). Branch `fix/er-bugs-v1.447` -> FF pre-prod -> FF main, tag v1.447, prod `9a56d36`, rollback `f9cf697`. **GitHub Pages did not auto-build the push — nudged live with empty commit `4b37b51`.** Local + PROD Claude-in-Chrome regression green. Companion non-index work: ER triage (37 ERs -> 9 Done / 3 Bug / ~24 Needed in 6 themes), docs/ER_TRIAGE_2026-06-07.md + er-triage-dashboard.html, docs/specs/ER_TRIAGE_AUTOMATION_SPEC.md, skills/prvs-er-triage, Supabase read-only MCP connected. |
| v1.446 (prod-live) | 2026-06-06 | **Parts "Requested → Ordered" state machine + simple parts report (Session 93).** RO-level flow Requested -> (Sourcing) -> Ordered -> Received. `submitPartsRequest` sets `'requested'` (was `'outstanding'`); manager-gated Ordered via the Set Parts Status modal; render.js chips + `ps-requested`/`ps-ordered` filters; css `.requested`/`.ordered` styles; orphaned `markPartsOrdered`/`_doMarkPartsOrdered` deleted; Spanish added; legacy `'outstanding'` renders as ordered. `send-parts-report` v1.8 -> v1.9 action-first rebuild (verdict banner + 3 fixed boxes; Call the Supplier covers overdue + no-ETA past a 3-business-day grace via `date_ordered`). DB: `repair_orders_parts_status_check` WIDENED to the union (Phase 1; Roland ran SQL); Phase 2 tighten + backfill deferred until soak. Branches `feature/parts-requested-ordered` (`90b5e9a`) + `chore/parts-report-simple` (`d95ef86`) -> FF pre-prod -> FF main, tag v1.446, prod main `90b5e9a`, rollback `e549026`. Local round-trip on Kevin McHenry Tester + PROD Claude-in-Chrome regression PASSED (module identity 3/3, 87 ROs, zero console errors). |
| v1.445 (prod-live) | 2026-06-05 | **Kenect + Slack dead-code teardown (Session 92).** KENECT: render.js Messages button, 'messages' case, 261-line inline KENECT block, Admin Settings Kenect section + 3 settings fns — all removed (the dead surface that survived the April 2026 removal and threw CORS on click). SLACK: notifySlack() + 5 call sites (inline + ro-crud.js + parts.js) + slack-notify edge fn source removed. Supabase: slack-notify + zombie kenect-proxy deployments deleted via CLI; 3 SLACK_WEBHOOK_* secrets unset. index.html 7,494 -> 7,155. Branch chore/kenect-slack-teardown -> FF pre-prod -> FF main, tag v1.445, prod main `022f254`, rollback `1cf0899`. Local gate + audit-log-verified write round-trip + PROD Claude-in-Chrome regression all PASSED (deadGlobals [], zero console errors). |
| v1.444 (prod-live) | 2026-06-04 | **Phase 19 CSS extraction (Session 91) — MODULARIZATION_ROADMAP 100% complete.** 3,381-line embedded `<style>` block -> NEW `css/dashboard.css` VERBATIM (byte-identical inner CSS, zero rule/JS changes); single `<link>` tag replaces it. index.html 10,858 -> 7,494 lines. FF pre-prod -> main, tag v1.444, prod main `05c3867`, rollback `930e5b4`. PROD regression passed (real stylesheet, 480 top-level rules, fully styled, zero errors). |
| v1.443 (prod-live) | 2026-06-04 | **DELETE-INLINE cleanup Phases 13/14/15/17/18 (Session 91).** Removed the 40 dead inline twins (qr 3, work-list 13, insurance 8, duplicates 5, enhancement 11); modules SOLE owners via app.js window bridge; byte-identical verified pre-delete; `// [PHASE N DELETED v1.443 S91]` markers remain. verify_inline.py + delete_inline.py rewritten + committed to scripts/. index.html 12,125 -> 10,858 lines. FF pre-prod -> main, tag v1.443, prod main `930e5b4`, rollback `b657bc3`. PROD regression passed (identity 40/40, 85 RVs, zero errors). |
| v1.442 (prod-live) | 2026-06-04 | **Modularization Phase 18 — `enhancement.js` ADDITIVE (Session 90) — ALL roadmap extraction phases (1-18) complete.** NEW `js/enhancement.js` — 11 GH#19 genie-lamp fns extracted VERBATIM (byte-identical 11/11) + window bridge from index.html's second classic script block; inline copies retained; app.js imports it (window.PRVS_Enhancement). closeERAdminView/loadERAdminData/saveERNote were post-roadmap additions; renderERAdminList stays in render.js; _erData/_erFilterStatus/_erFilterCategory let-state inline (declared -> strict-mode safe). FF pre-prod -> main, tag v1.442, prod main `5208c20`, rollback `909d580`. PROD regression passed (identity 11/11, Wishes badge 29 via module, 85 RVs, zero app errors). |
| v1.441 (prod-live) | 2026-06-04 | **Modularization Phase 17 — `duplicates.js` ADDITIVE (Session 90).** NEW `js/duplicates.js` — getBaseROId, findDuplicateGroups, openDuplicateManager, highlightDupeRows, executeDupeMerge extracted VERBATIM (byte-identical 5/5) + window bridge; inline copies retained; app.js imports it (window.PRVS_Duplicates). getBaseROId + highlightDupeRows were post-roadmap additions. window._dupeGroups always window.-prefixed. FF pre-prod -> main, tag v1.441, prod main `909d580`, rollback `5e4679c`. PROD regression passed after hard reload (NEW GOTCHA: GitHub Pages ~10-min JS cache served stale app.js on first check — logged in Known Issues). |
| v1.440 (prod-live) | 2026-06-04 | **Modularization Phase 15 — `insurance.js` ADDITIVE (Session 90).** NEW `js/insurance.js` — 8 Claude Vision estimate-scanner fns extracted VERBATIM (byte-identical 8/8) + window bridge: renderCustomFields, openEstimateScanner, handleEstimateFile, callClaudeVision, renderSuggestions, applyChip, applyChipConflict (post-roadmap), writeInsuranceData; inline copies retained; app.js imports it (window.PRVS_Insurance). Scanner helpers stay inline; window._lastEstimateScan/_pendingEstimateDoc always window.-prefixed. FF pre-prod -> main, tag v1.440, prod main `5e4679c`, rollback `65c9a91`. Local scan attempt proved module runtime ownership via stack frames (CORS-blocked on localhost BY DESIGN — claude-vision-proxy is single-origin). PROD regression passed (identity 8/8, 85 RVs, zero app errors). |
| v1.439 (prod-live) | 2026-06-03 | **Modularization Phase 14 — `work-list.js` ADDITIVE (Session 89).** NEW `js/work-list.js` — 13 Manager Work List fns (GH#16) extracted VERBATIM (byte-identical 13/13) + window bridge; inline copies retained; app.js imports it (window.PRVS_WorkList). Also DECLARED `let _workListActiveSilo = null;` inline — it was an undeclared implicit global that threw ReferenceError in the strict-mode module and aborted loadWorkList (caught + fixed at the local-test gate; empty list + invisible managers' lists were the symptom). FF pre-prod -> main, tag v1.439, prod main `7787448`, rollback `855bf80`. Claude-in-Chrome PROD regression passed (PRVS_WorkList 13 fns, identity true, 83 RVs warm, zero app errors). |
| v1.438 (prod-live) | 2026-06-03 | **Modularization Phase 13 — `qr.js` ADDITIVE (Session 89).** NEW `js/qr.js` — openQRModal, printQRLabel, handleDeepLink extracted VERBATIM (byte-identical 3/3) + window bridge; inline copies retained; app.js imports it (window.PRVS_QR). FF pre-prod -> main, tag v1.438, prod main `855bf80`, rollback `7c7e6af`. Claude-in-Chrome PROD regression passed (PRVS_QR 3 fns, identity true, 83 RVs, zero app errors). |
| v1.437 (prod-live) | 2026-06-02 | **DELETE-INLINE cleanup of Phases 6-12 (Session 88).** Removed the 73 now-dead inline function bodies (render 4, ro-crud 14, parts 17, work-orders 14, photos 13, time-tracking 7, scheduling 4) whose byte-identical module twins already owned `window.*` at runtime via the app.js `Object.assign(window,...)` bridge — so the inline bodies were dead code. Each verified byte-identical to its module export before removal (verify_inline.py: 73/73, 0 mismatches). index.html 16,100→12,077 lines. Left a `// [PHASE N DELETED v1.437 S88]` marker per fn. Retained inline: markPartsOrdered + _doMarkPartsOrdered (orphaned) + isSrOrAdmin (legacy alias). Deps still resolve via the shared global environment; ESM imports deferred to Phase 19. Branch `refactor/delete-inline-phases-6-12` → FF pre-prod (`a80b71f`→`90da4fa`) → FF main, tag v1.437, GitHub Release published. Verified: local (board, 6 modals, write persist+revert on $0 tester) + PROD Claude-in-Chrome regression (module ownership 22/22 window.* === PRVS_*.*, 83 RVs, Admin RBAC, zero console errors). Rollback `a80b71f`. Finding: dead Kenect UI still live (calls deleted kenect-proxy) → new Kenect teardown TODO. |
| v1.436 (prod-live) | 2026-06-02 | **Phase 12 scheduling.js — ADDITIVE Google Calendar extraction (Session 87).** 4 fns (reauthorizeCalendar, openScheduleModal, confirmSchedule, proceedWithSchedule) extracted VERBATIM + window bridge; inline copies retained. auth.js bare openScheduleModal call bridged. confirmSchedule/proceedWithSchedule create real Calendar events via gapi. Full create path live-verified (re-auth + real event on Solar calendar). Promoted pre-prod → main (rollback 8c79d06), tag v1.436. |
| v1.435 (prod-live) | 2026-06-02 | **Phase 11 time-tracking.js — ADDITIVE time-tracking extraction (Session 87).** 7 fns (loadTimeLogsFromSupabase, loadTimeLogsFromSheets, startTimeLogsAutoRefresh, manualRefreshTimeLogs, getTimeLogsForRO, calculateTotalHours, openTimeLogsModal) extracted VERBATIM + window bridge; inline copies retained. auth.js + render.js bare calls bridged; timeLogsData + timeLogsRefreshInterval shared via global lexical env. Live-verified (modal 33.1h/29 sessions; reload 541→542). Promoted pre-prod → main (rollback f9462bf), tag v1.435. |
| v1.434 (prod-live) | 2026-06-02 | **Phase 10 photos.js — ADDITIVE photo/document extraction (Session 87).** 13 fns extracted VERBATIM + window bridge; inline copies retained. parts.js bare uploadToSupabaseStorage call bridged. Live-verified (upload photo + doc on tester RO). Promoted pre-prod → main (rollback 7a3646c), tag v1.434. |
| v1.433 (prod-live) | 2026-06-01 | **Phase 9 work-orders.js — ADDITIVE work-order extraction (Session 86).** New `js/work-orders.js` (647 lines): 14 WO fns (loadStaff, loadWorkOrdersForRO, openWorkOrderModal, renderWorkOrderView, openAddServicePicker, addServiceToRO, openBuildWOForm, buildWOTaskRowHtml, loadWOTemplate, applyWOTemplate, saveWOTemplate, submitWOForm, updateTaskStatusWO, computeAndSaveWORollup) copied VERBATIM (script-extracted + byte-identical-verified — each module body minus the injected `export` matches index.html exactly + occurs once; `node --check` clean), exported + window-bridged. `js/app.js` imports it (window.PRVS_WorkOrders). index.html v1.432->v1.433 (header + badge L3761 + boot log + app.js boot log). Inline copies retained; bridge re-points window.* to byte-identical copies (all 14 `window.* === PRVS_WorkOrders.*` verified true on PROD). Branch refactor/work-orders-phase-9 `2ebdede` -> FF pre-prod -> FF main, tag `v1.433`, GitHub Release published. Validated: local non-destructive WO write test (updateTaskStatusWO flip in_progress->not_started->in_progress on Kevin McHenry Tester $0, persisted to service_tasks + reverted) + Claude-in-Chrome PROD regression (PRVS_WorkOrders 14 fns, module ownership, 83 RVs, zero errors). Rollback target `20f8da8`. |
| v1.432 (prod-live) | 2026-06-01 | **Phase 8 parts.js — ADDITIVE parts-system extraction (Session 85).** New `js/parts.js` (938 lines): 17 LIVE parts fns (loadPartsFromSupabase, openPartsModal, showAddPartForm, savePartForm, editPartRow, deletePartRow, markPartReceived, appendPartToSupabase, updatePartInSupabase, deletePartFromSupabase, openPartsRequestModal, submitPartsRequest, openPartsStatusModal, setPartsStatus, notifyPartsRequester, notifyPartsEtaUpdate, renderPartsPhotoPreview) copied VERBATIM (script-extracted + byte-identical-verified + `node --check` clean), exported + window-bridged. `js/app.js` imports it (window.PRVS_Parts). index.html v1.431->v1.432 (header + badge L3749 + boot log). Inline copies retained; bridge re-points window.* to byte-identical copies (`window.openPartsModal===PRVS_Parts.openPartsModal` verified local). **markPartsOrdered + _doMarkPartsOrdered EXCLUDED + left inline** (orphaned; reserved for the Requested/Ordered feature). Branch refactor/parts-phase-8 `e97c317` -> FF pre-prod (Session 85). **Promoted to main Session 86** (FF pre-prod→main `20f8da8`, tag v1.432, Claude-in-Chrome PROD regression PASSED — 17 fns, 89 RVs, zero errors; GitHub Release published; branch deleted). Validated: local non-destructive write test (setPartsStatus null->sourcing->null + audit_log via module copy) + local Chrome regression (PRVS_Parts 17 fns, module ownership, parts + status modals render, zero errors). Rollback target `e4b4f50`. |

---

## 📅 Session Log

> Session Log before 2026-06-01 relocated to CLAUDE_CONTEXT_ARCHIVE.md (Session 107).

| Date | Session | Summary |
|---|---|---|
